# 12g: the Kotlin/Native backend runs inside the Gradle daemon, and the release
# (-opt) devirtualization pass over the full-breadth iOS wrapper set (task 30,
# ~1017 generated classes) OOMs at 2g.
org.gradle.jvmargs=-Xmx12g -XX:MaxMetaspaceSize=1g -Dfile.encoding=UTF-8
org.gradle.caching=true
kotlin.mpp.enableCInteropCommonization=true
kotlin.mpp.applyDefaultHierarchyTemplate=false
kotlin.daemon.jvmargs=-Xmx4g

# Single source of truth for the Godot baseline version (the "next baseline bump"
# pin from architecture review F4). Dot form `<major.minor>.<status>` matches the
# export-template directory + extension_api.json version_status. Consumed by
# build.gradle.kts (iOS export-template path) and scripts/ios_template_preflight.sh;
# scripts/check_godot_version_pin.py asserts CI (.github/workflows/package.yml,
# dash form) and docs stay in sync. On a Godot bump, change ONLY this line, then run
# scripts/refresh_godot_api.sh + scripts/check_godot_version_pin.py.
kanamaGodotVersion=4.7.stable

# Task 63 — Temurin release whose per-platform `jmods` archives back the
# cross-target export runtimes (`jlinkGameRuntimeCross`). Must be the same JDK
# FEATURE version as the build JDK (25); patch levels may differ. Bumping this
# means re-pinning every SHA-256 in `kanamaRuntimeTargets` (build.gradle.kts).
kanamaTargetJdkRelease=25.0.4+7
