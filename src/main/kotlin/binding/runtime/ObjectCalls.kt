package net.multigesture.kanama.binding.runtime

import net.multigesture.kanama.api.Area2D
import net.multigesture.kanama.api.Area3D
import net.multigesture.kanama.api.BaseButton
import net.multigesture.kanama.api.GodotCallable
import net.multigesture.kanama.api.GodotObject
import net.multigesture.kanama.api.Material
import net.multigesture.kanama.api.Node
import net.multigesture.kanama.api.Node2D
import net.multigesture.kanama.api.Node3D
import net.multigesture.kanama.api.PhysicsBody3D
import net.multigesture.kanama.ffi.GodotFFI
import net.multigesture.kanama.types.AABB
import net.multigesture.kanama.types.Basis
import net.multigesture.kanama.types.Color
import net.multigesture.kanama.types.GodotReal
import net.multigesture.kanama.types.NodePath
import net.multigesture.kanama.types.Plane
import net.multigesture.kanama.types.Projection
import net.multigesture.kanama.types.Rect2
import net.multigesture.kanama.types.Rect2i
import net.multigesture.kanama.types.RID
import net.multigesture.kanama.types.Transform2D
import net.multigesture.kanama.types.Transform3D
import net.multigesture.kanama.types.Vector2
import net.multigesture.kanama.types.Vector2i
import net.multigesture.kanama.types.Quaternion
import net.multigesture.kanama.types.Vector3
import net.multigesture.kanama.types.Vector3i
import net.multigesture.kanama.types.Vector4
import java.lang.foreign.Arena
import java.lang.foreign.FunctionDescriptor
import java.lang.foreign.MemorySegment
import java.lang.foreign.ValueLayout.ADDRESS
import java.lang.foreign.ValueLayout.JAVA_BYTE
import java.lang.foreign.ValueLayout.JAVA_DOUBLE
import java.lang.foreign.ValueLayout.JAVA_FLOAT
import java.lang.foreign.ValueLayout.JAVA_INT
import java.lang.foreign.ValueLayout.JAVA_LONG

/**
 * Thin wrappers around the three GDExtension interface functions needed
 * to call methods on built-in Godot objects (ClassDB method binds, singletons).
 *
 * Used by script-language infrastructure to call
 * Engine::register_script_language and friends.
 */
object ObjectCalls {

    private val globalGetSingleton by lazy {
        GodotFFI.lookup(
            "global_get_singleton",
            FunctionDescriptor.of(ADDRESS, ADDRESS),
        )
    }

    private val classdbGetMethodBind by lazy {
        GodotFFI.lookup(
            "classdb_get_method_bind",
            FunctionDescriptor.of(ADDRESS, ADDRESS, ADDRESS, JAVA_LONG),
        )
    }

    private val classdbConstructObject by lazy {
        GodotFFI.lookup(
            "classdb_construct_object2",
            FunctionDescriptor.of(ADDRESS, ADDRESS),
        )
    }

    private val objectMethodBindPtrcall by lazy {
        GodotFFI.lookup(
            "object_method_bind_ptrcall",
            FunctionDescriptor.ofVoid(ADDRESS, ADDRESS, ADDRESS, ADDRESS),
        )
    }

    private val notificationBind by lazy {
        getMethodBind("Object", "notification", 4023243586L)
    }

    private class PtrcallScratch {
        private val arena = Arena.ofAuto()
        val args1: MemorySegment = arena.allocate(ADDRESS, 1)
        val args3: MemorySegment = arena.allocate(ADDRESS, 3)
        val boolCell: MemorySegment = arena.allocate(JAVA_BYTE)
        val byteRet: MemorySegment = arena.allocate(JAVA_BYTE)
        val intRet: MemorySegment = arena.allocate(JAVA_INT)
        val longRet: MemorySegment = arena.allocate(JAVA_LONG)
        val doubleCell: MemorySegment = arena.allocate(JAVA_DOUBLE)
        val doubleRet: MemorySegment = arena.allocate(JAVA_DOUBLE)
        val objectCell: MemorySegment = arena.allocate(ADDRESS)
        val arrayRet: MemorySegment = arena.allocate(8L, 8L)
        val packedArrayRet: MemorySegment = arena.allocate(BuiltinTypes.PACKED_ARRAY_SIZE, BuiltinTypes.PACKED_ARRAY_ALIGN)
        val vector2Cell: MemorySegment = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
        val vector2Ret: MemorySegment = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
        val rect2Ret: MemorySegment = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
        val colorCell: MemorySegment = arena.allocate(16L, 4L)
        // Phase F3 (review): fixed-size POD return buffers for the per-frame geometry getters, so
        // hot const getters (transform/vector polling) skip a per-call confined arena. Safe to reuse
        // per-thread because these are leaf const getters — the value is read back before the call
        // returns and the getter triggers no re-entrant JVM dispatch.
        val vector3Ret: MemorySegment = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
        val vector3iRet: MemorySegment = arena.allocate(12L, 4L)
        val vector2iRet: MemorySegment = arena.allocate(8L, 4L)
        val quaternionRet: MemorySegment = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
        val basisRet: MemorySegment = arena.allocate(GodotReal.SIZE_BYTES * 9, GodotReal.ALIGN_BYTES)
        val transform3dRet: MemorySegment = arena.allocate(GodotReal.SIZE_BYTES * 12, GodotReal.ALIGN_BYTES)

        fun setBoolArg(value: Boolean): MemorySegment {
            boolCell.set(JAVA_BYTE, 0, if (value) 1.toByte() else 0.toByte())
            args1.setAtIndex(ADDRESS, 0, boolCell)
            return args1
        }
    }

    private val ptrcallScratch = ThreadLocal.withInitial { PtrcallScratch() }

    private fun <T> callArrayReturn(
        methodBind: MemorySegment,
        instance: MemorySegment,
        args: MemorySegment,
        reader: (MemorySegment) -> List<T>,
    ): List<T> {
        Arena.ofConfined().use { arena ->
            val ret = arena.allocate(8L, 8L)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return try {
                reader(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, ret)
            }
        }
    }

    private fun readVariantReturn(
        methodBind: MemorySegment,
        instance: MemorySegment,
        args: MemorySegment,
        arena: Arena,
    ): Any? {
        val ret = arena.allocate(BuiltinTypes.VARIANT_SIZE, 8L)
        try {
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return BuiltinTypes.readVariantScalar(ret, arena)
        } finally {
            BuiltinTypes.destroyVariant(ret)
        }
    }

    private fun initVariantCell(arena: Arena, value: Any?): MemorySegment {
        val variant = arena.allocate(BuiltinTypes.VARIANT_SIZE, 8L)
        BuiltinTypes.initVariantFromAny(variant, value, arena)
        return variant
    }

    private val objectMethodBindCall by lazy {
        GodotFFI.lookup(
            "object_method_bind_call",
            FunctionDescriptor.ofVoid(ADDRESS, ADDRESS, ADDRESS, JAVA_LONG, ADDRESS, ADDRESS),
        )
    }

    private val objectDestroy by lazy {
        GodotFFI.lookup(
            "object_destroy",
            FunctionDescriptor.ofVoid(ADDRESS),
        )
    }

    /** Returns the Godot object pointer for a named engine singleton (e.g. "Engine"). */
    fun getSingleton(name: String): MemorySegment =
        globalGetSingleton.invoke(GodotStrings.makeStringName(name)) as MemorySegment

    /** Looks up a method bind by class name, method name, and ABI hash. Returns NULL on mismatch. */
    fun getMethodBind(className: String, methodName: String, hash: Long): MemorySegment =
        classdbGetMethodBind.invoke(
            GodotStrings.makeStringName(className),
            GodotStrings.makeStringName(methodName),
            hash,
        ) as MemorySegment

    fun constructObject(className: String): MemorySegment {
        val instance = classdbConstructObject.invoke(GodotStrings.makeStringName(className)) as MemorySegment
        // Objects created through ClassDB are not fully initialized until Godot
        // receives NOTIFICATION_POSTINITIALIZE. Theme/text controls rely on it.
        notifyPostinitialize(instance)
        return instance
    }

    fun notifyPostinitialize(instance: MemorySegment) {
        if (instance.address() == 0L) return
        ptrcallWithIntAndBoolArgs(notificationBind, instance, 0, false)
    }

    /**
     * Calls [methodBind] on [instance] via ptrcall with the given Object-typed
     * arguments. Each element of [objectArgs] is a Godot object pointer;
     * ptrcall convention requires passing a pointer-to-pointer for Object args,
     * which this function handles internally via a confined arena.
     */
    fun ptrcallWithObjectArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArgs: List<MemorySegment>,
    ) {
        Arena.ofConfined().use { arena ->
            val argsArray = if (objectArgs.isEmpty()) {
                MemorySegment.NULL
            } else {
                // For each Object* arg: allocate a cell holding the pointer,
                // then store the cell's address in the args array (double-indirect).
                val arr = arena.allocate(ADDRESS, objectArgs.size.toLong())
                objectArgs.forEachIndexed { i, obj ->
                    val cell = arena.allocate(ADDRESS)
                    cell.set(ADDRESS, 0, obj)
                    arr.setAtIndex(ADDRESS, i.toLong(), cell)
                }
                arr
            }
            objectMethodBindPtrcall.invoke(methodBind, instance, argsArray, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with one Object* arg and a bool return value.
     */
    fun ptrcallWithObjectArgRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val objCell = arena.allocate(ADDRESS)
            objCell.set(ADDRESS, 0, objectArg)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, objCell)
            val ret = arena.allocate(JAVA_BYTE)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_BYTE, 0) != 0.toByte()
        }
    }

    fun ptrcallWithObjectArgRetTransform2D(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
    ): Transform2D {
        Arena.ofConfined().use { arena ->
            val objCell = arena.allocate(ADDRESS)
            objCell.set(ADDRESS, 0, objectArg)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, objCell)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 6, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return readTransform2D(ret)
        }
    }

    /**
     * Calls [methodBind] with (Object*, String, int32) args and no return
     * value. Object args use ptrcall's pointer-to-pointer convention.
     */
    fun ptrcallWithObjectStringAndIntArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        text: String,
        intArg: Int,
    ) {
        Arena.ofConfined().use { arena ->
            val objCell = arena.allocate(ADDRESS)
            val stringCell = arena.allocate(8L, 8L)
            val intCell = arena.allocate(JAVA_INT)
            try {
                objCell.set(ADDRESS, 0, objectArg)
                GodotStrings.initString(stringCell, text)
                intCell.set(JAVA_INT, 0, intArg)
                val arr = arena.allocate(ADDRESS, 3)
                arr.setAtIndex(ADDRESS, 0, objCell)
                arr.setAtIndex(ADDRESS, 1, stringCell)
                arr.setAtIndex(ADDRESS, 2, intCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    /**
     * Calls [methodBind] with (Object*, int32, String) args and no return value.
     * Object args use ptrcall's pointer-to-pointer convention.
     */
    fun ptrcallWithObjectIntStringArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        intArg: Int,
        text: String,
    ) {
        Arena.ofConfined().use { arena ->
            val objCell = arena.allocate(ADDRESS)
            val intCell = arena.allocate(JAVA_INT)
            val stringCell = arena.allocate(8L, 8L)
            try {
                objCell.set(ADDRESS, 0, objectArg)
                intCell.set(JAVA_INT, 0, intArg)
                GodotStrings.initString(stringCell, text)
                val arr = arena.allocate(ADDRESS, 3)
                arr.setAtIndex(ADDRESS, 0, objCell)
                arr.setAtIndex(ADDRESS, 1, intCell)
                arr.setAtIndex(ADDRESS, 2, stringCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    /**
     * Calls [methodBind] with (String, bool, String) args and Object return value.
     */
    fun ptrcallWithStringBoolStringArgsRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        enabled: Boolean,
        text2: String,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val stringCell0 = arena.allocate(8L, 8L)
            val boolCell = arena.allocate(JAVA_BYTE)
            val stringCell2 = arena.allocate(8L, 8L)
            try {
                GodotStrings.initString(stringCell0, text)
                boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
                GodotStrings.initString(stringCell2, text2)
                val arr = arena.allocate(ADDRESS, 3)
                arr.setAtIndex(ADDRESS, 0, stringCell0)
                arr.setAtIndex(ADDRESS, 1, boolCell)
                arr.setAtIndex(ADDRESS, 2, stringCell2)
                val ret = arena.allocate(ADDRESS)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(ADDRESS, 0)
            } finally {
                GodotStrings.destroyString(stringCell0)
                GodotStrings.destroyString(stringCell2)
            }
        }
    }

    /**
     * Calls a vararg Object MethodBind with scalar Variant arguments and decodes
     * the scalar Variant return value.
     */
    fun callWithVariantArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        args: List<Any?>,
    ): Any? {
        Arena.ofConfined().use { arena ->
            val variantBuffer = if (args.isEmpty()) {
                MemorySegment.NULL
            } else {
                arena.allocate(BuiltinTypes.VARIANT_SIZE * args.size, 8)
            }
            val argPtrArray = if (args.isEmpty()) {
                MemorySegment.NULL
            } else {
                arena.allocate(ADDRESS, args.size.toLong())
            }
            var initialized = 0
            val retVariant = arena.allocate(BuiltinTypes.VARIANT_SIZE, 8)
            val errStruct = arena.allocate(16L, 8L)
            var retInitialized = false

            try {
                args.forEachIndexed { i, arg ->
                    val slot = variantBuffer.asSlice(BuiltinTypes.VARIANT_SIZE * i, BuiltinTypes.VARIANT_SIZE)
                    BuiltinTypes.initVariantFromAny(slot, arg, arena)
                    initialized += 1
                    argPtrArray.setAtIndex(ADDRESS, i.toLong(), slot)
                }

                objectMethodBindCall.invoke(
                    methodBind,
                    instance,
                    argPtrArray,
                    args.size.toLong(),
                    retVariant,
                    errStruct,
                )
                retInitialized = true

                val errorType = errStruct.get(JAVA_INT, 0)
                check(errorType == 0) {
                    val argument = errStruct.get(JAVA_INT, 4)
                    val expected = errStruct.get(JAVA_INT, 8)
                    "Object.call failed: error_type=$errorType argument=$argument expected=$expected"
                }

                return BuiltinTypes.readVariantScalar(retVariant, arena)
            } finally {
                for (i in 0 until initialized) {
                    BuiltinTypes.destroyVariant(
                        variantBuffer.asSlice(BuiltinTypes.VARIANT_SIZE * i, BuiltinTypes.VARIANT_SIZE),
                    )
                }
                if (retInitialized) {
                    BuiltinTypes.destroyVariant(retVariant)
                }
            }
        }
    }

    /**
     * Calls [methodBind] via ptrcall and reads an int32 return value.
     *
     * Use this for methods whose return type is `int`/`enum Error`, e.g.
     * Engine::register_script_language and unregister_script_language.
     */
    fun ptrcallWithObjectArgsRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArgs: List<MemorySegment>,
    ): Int {
        Arena.ofConfined().use { arena ->
            val argsArray = if (objectArgs.isEmpty()) {
                MemorySegment.NULL
            } else {
                val arr = arena.allocate(ADDRESS, objectArgs.size.toLong())
                objectArgs.forEachIndexed { i, obj ->
                    val cell = arena.allocate(ADDRESS)
                    cell.set(ADDRESS, 0, obj)
                    arr.setAtIndex(ADDRESS, i.toLong(), cell)
                }
                arr
            }
            val ret = arena.allocate(JAVA_INT)
            objectMethodBindPtrcall.invoke(methodBind, instance, argsArray, ret)
            return ret.get(JAVA_INT, 0)
        }
    }

    /**
     * Calls [methodBind] via ptrcall and reads an int64/enum return value.
     */
    fun ptrcallWithObjectArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArgs: List<MemorySegment>,
    ): Long {
        Arena.ofConfined().use { arena ->
            val argsArray = if (objectArgs.isEmpty()) {
                MemorySegment.NULL
            } else {
                val arr = arena.allocate(ADDRESS, objectArgs.size.toLong())
                objectArgs.forEachIndexed { i, obj ->
                    val cell = arena.allocate(ADDRESS)
                    cell.set(ADDRESS, 0, obj)
                    arr.setAtIndex(ADDRESS, i.toLong(), cell)
                }
                arr
            }
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, argsArray, ret)
            return ret.get(JAVA_LONG, 0)
        }
    }

    /**
     * Calls [methodBind] via ptrcall with one Object* arg and one bool arg.
     */
    fun ptrcallWithObjectAndBoolArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        boolArg: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 2)
            val objCell = arena.allocate(ADDRESS)
            objCell.set(ADDRESS, 0, objectArg)
            arr.setAtIndex(ADDRESS, 0, objCell)

            val boolCell = arena.allocate(java.lang.foreign.ValueLayout.JAVA_BYTE)
            boolCell.set(java.lang.foreign.ValueLayout.JAVA_BYTE, 0, if (boolArg) 1.toByte() else 0.toByte())
            arr.setAtIndex(ADDRESS, 1, boolCell)

            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] via ptrcall with one Object* arg, one bool arg, and bool return value.
     */
    fun ptrcallWithObjectAndBoolArgRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        boolArg: Boolean,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 2)
            val objCell = arena.allocate(ADDRESS)
            objCell.set(ADDRESS, 0, objectArg)
            arr.setAtIndex(ADDRESS, 0, objCell)

            val boolCell = arena.allocate(JAVA_BYTE)
            boolCell.set(JAVA_BYTE, 0, if (boolArg) 1.toByte() else 0.toByte())
            arr.setAtIndex(ADDRESS, 1, boolCell)

            val ret = arena.allocate(JAVA_BYTE)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_BYTE, 0) != 0.toByte()
        }
    }

    /**
     * Calls [methodBind] via ptrcall with one Object* arg, one bool arg, and int32 return value.
     */
    fun ptrcallWithObjectAndBoolArgRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        boolArg: Boolean,
    ): Int {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 2)
            val objCell = arena.allocate(ADDRESS)
            objCell.set(ADDRESS, 0, objectArg)
            arr.setAtIndex(ADDRESS, 0, objCell)

            val boolCell = arena.allocate(JAVA_BYTE)
            boolCell.set(JAVA_BYTE, 0, if (boolArg) 1.toByte() else 0.toByte())
            arr.setAtIndex(ADDRESS, 1, boolCell)

            val ret = arena.allocate(JAVA_INT)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_INT, 0)
        }
    }

    fun ptrcallWithObjectAndBoolArgRetDictionary(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        boolArg: Boolean,
    ): Map<String, Any?> {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 2)
            val objCell = arena.allocate(ADDRESS)
            val boolCell = arena.allocate(JAVA_BYTE)
            objCell.set(ADDRESS, 0, objectArg)
            boolCell.set(JAVA_BYTE, 0, if (boolArg) 1.toByte() else 0.toByte())
            arr.setAtIndex(ADDRESS, 0, objCell)
            arr.setAtIndex(ADDRESS, 1, boolCell)
            val ret = arena.allocate(8L, 8L)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return try {
                BuiltinTypes.readDictionaryScalars(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.DICTIONARY, ret)
            }
        }
    }

    /**
     * Calls [methodBind] with (Object*, Dictionary) and no return value.
     */
    fun ptrcallWithObjectAndDictionaryArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        values: Map<String, Any?>,
    ) {
        Arena.ofConfined().use { arena ->
            val objCell = arena.allocate(ADDRESS)
            val dict = arena.allocate(8L, 8L)
            try {
                objCell.set(ADDRESS, 0, objectArg)
                BuiltinTypes.initDictionary(dict, values)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, objCell)
                arr.setAtIndex(ADDRESS, 1, dict)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.DICTIONARY, dict)
            }
        }
    }

    fun ptrcallWithObjectAndBoolArgRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        boolArg: Boolean,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 2)
            val objCell = arena.allocate(ADDRESS)
            val boolCell = arena.allocate(JAVA_BYTE)
            objCell.set(ADDRESS, 0, objectArg)
            boolCell.set(JAVA_BYTE, 0, if (boolArg) 1.toByte() else 0.toByte())
            arr.setAtIndex(ADDRESS, 0, objCell)
            arr.setAtIndex(ADDRESS, 1, boolCell)
            val ret = arena.allocate(ADDRESS)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(ADDRESS, 0)
        }
    }

    /**
     * Calls [methodBind] via ptrcall with one Object* arg, one bool arg, and NodePath return value.
     */
    fun ptrcallWithObjectAndBoolArgRetNodePath(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        boolArg: Boolean,
    ): NodePath {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 2)
            val objCell = arena.allocate(ADDRESS)
            objCell.set(ADDRESS, 0, objectArg)
            arr.setAtIndex(ADDRESS, 0, objCell)

            val boolCell = arena.allocate(JAVA_BYTE)
            boolCell.set(JAVA_BYTE, 0, if (boolArg) 1.toByte() else 0.toByte())
            arr.setAtIndex(ADDRESS, 1, boolCell)

            val ret = arena.allocate(8L, 8L)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return try {
                BuiltinTypes.readTypedNodePath(ret, arena)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.NODE_PATH, ret)
            }
        }
    }

    /**
     * Calls [methodBind] with (Object*, Rect2i) args and no return value.
     */
    fun ptrcallWithObjectAndRect2iArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        rect: Rect2i,
    ) {
        Arena.ofConfined().use { arena ->
            val objCell = arena.allocate(ADDRESS)
            objCell.set(ADDRESS, 0, objectArg)
            val rectCell = arena.allocate(16L, 4L)
            rectCell.set(JAVA_INT, 0, rect.position.x)
            rectCell.set(JAVA_INT, 4, rect.position.y)
            rectCell.set(JAVA_INT, 8, rect.size.x)
            rectCell.set(JAVA_INT, 12, rect.size.y)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, objCell)
            arr.setAtIndex(ADDRESS, 1, rectCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (Object*, Rect2) args and no return value.
     */
    fun ptrcallWithObjectAndRect2Arg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        rect: Rect2,
    ) {
        Arena.ofConfined().use { arena ->
            val objCell = arena.allocate(ADDRESS)
            objCell.set(ADDRESS, 0, objectArg)
            val rectCell = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
            writeRect2(rectCell, rect)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, objCell)
            arr.setAtIndex(ADDRESS, 1, rectCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithObjectTwoIntColorLongRect2VariantBoolStringTwoBoolStringArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        image: MemorySegment,
        width: Int,
        height: Int,
        color: Color,
        inlineAlign: Long,
        region: Rect2,
        key: Any?,
        pad: Boolean,
        tooltip: String,
        sizeInPercent: Boolean,
        altText: Boolean,
        text: String,
    ) {
        Arena.ofConfined().use { arena ->
            val imageCell = arena.allocate(ADDRESS)
            val widthCell = arena.allocate(JAVA_INT)
            val heightCell = arena.allocate(JAVA_INT)
            val colorCell = arena.allocate(16L, 4L)
            val inlineAlignCell = arena.allocate(JAVA_LONG)
            val regionCell = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
            val keyVariant = initVariantCell(arena, key)
            val padCell = arena.allocate(JAVA_BYTE)
            val tooltipCell = arena.allocate(8L, 8L)
            val sizeInPercentCell = arena.allocate(JAVA_BYTE)
            val altTextCell = arena.allocate(JAVA_BYTE)
            val textCell = arena.allocate(8L, 8L)
            try {
                imageCell.set(ADDRESS, 0, image)
                widthCell.set(JAVA_INT, 0, width)
                heightCell.set(JAVA_INT, 0, height)
                colorCell.set(JAVA_FLOAT, 0, color.r)
                colorCell.set(JAVA_FLOAT, 4, color.g)
                colorCell.set(JAVA_FLOAT, 8, color.b)
                colorCell.set(JAVA_FLOAT, 12, color.a)
                inlineAlignCell.set(JAVA_LONG, 0, inlineAlign)
                writeRect2(regionCell, region)
                padCell.set(JAVA_BYTE, 0, if (pad) 1.toByte() else 0.toByte())
                GodotStrings.initString(tooltipCell, tooltip)
                sizeInPercentCell.set(JAVA_BYTE, 0, if (sizeInPercent) 1.toByte() else 0.toByte())
                altTextCell.set(JAVA_BYTE, 0, if (altText) 1.toByte() else 0.toByte())
                GodotStrings.initString(textCell, text)
                val args = arena.allocate(ADDRESS, 12)
                args.setAtIndex(ADDRESS, 0, imageCell)
                args.setAtIndex(ADDRESS, 1, widthCell)
                args.setAtIndex(ADDRESS, 2, heightCell)
                args.setAtIndex(ADDRESS, 3, colorCell)
                args.setAtIndex(ADDRESS, 4, inlineAlignCell)
                args.setAtIndex(ADDRESS, 5, regionCell)
                args.setAtIndex(ADDRESS, 6, keyVariant)
                args.setAtIndex(ADDRESS, 7, padCell)
                args.setAtIndex(ADDRESS, 8, tooltipCell)
                args.setAtIndex(ADDRESS, 9, sizeInPercentCell)
                args.setAtIndex(ADDRESS, 10, altTextCell)
                args.setAtIndex(ADDRESS, 11, textCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(textCell)
                GodotStrings.destroyString(tooltipCell)
                BuiltinTypes.destroyVariant(keyVariant)
            }
        }
    }

    fun ptrcallWithObjectAndStringNameArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        name: String,
    ) {
        Arena.ofConfined().use { arena ->
            val objCell = arena.allocate(ADDRESS)
            objCell.set(ADDRESS, 0, objectArg)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, objCell)
            arr.setAtIndex(ADDRESS, 1, GodotStrings.makeStringName(name))
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithTwoObjectArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: MemorySegment,
        second: MemorySegment,
    ) {
        Arena.ofConfined().use { arena ->
            val obj0 = arena.allocate(ADDRESS)
            val obj1 = arena.allocate(ADDRESS)
            obj0.set(ADDRESS, 0, first)
            obj1.set(ADDRESS, 0, second)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, obj0)
            arr.setAtIndex(ADDRESS, 1, obj1)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithTwoObjectArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: MemorySegment,
        second: MemorySegment,
    ): Long {
        Arena.ofConfined().use { arena ->
            val obj0 = arena.allocate(ADDRESS)
            val obj1 = arena.allocate(ADDRESS)
            obj0.set(ADDRESS, 0, first)
            obj1.set(ADDRESS, 0, second)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, obj0)
            arr.setAtIndex(ADDRESS, 1, obj1)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_LONG, 0)
        }
    }

    fun ptrcallWithTwoObjectUInt32ArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: MemorySegment,
        second: MemorySegment,
        value: Long,
    ): Long {
        Arena.ofConfined().use { arena ->
            val obj0 = arena.allocate(ADDRESS)
            val obj1 = arena.allocate(ADDRESS)
            val valueCell = arena.allocate(JAVA_INT)
            obj0.set(ADDRESS, 0, first)
            obj1.set(ADDRESS, 0, second)
            valueCell.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(value))
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, obj0)
            arr.setAtIndex(ADDRESS, 1, obj1)
            arr.setAtIndex(ADDRESS, 2, valueCell)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_LONG, 0)
        }
    }

    fun ptrcallWithLongAndTwoObjectArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        longValue: Long,
        first: MemorySegment,
        second: MemorySegment,
    ) {
        Arena.ofConfined().use { arena ->
            val longCell = arena.allocate(JAVA_LONG)
            val obj0 = arena.allocate(ADDRESS)
            val obj1 = arena.allocate(ADDRESS)
            longCell.set(JAVA_LONG, 0, longValue)
            obj0.set(ADDRESS, 0, first)
            obj1.set(ADDRESS, 0, second)
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, longCell)
            arr.setAtIndex(ADDRESS, 1, obj0)
            arr.setAtIndex(ADDRESS, 2, obj1)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithObjectRect2iAndVector2iArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        rect: Rect2i,
        position: Vector2i,
    ) {
        Arena.ofConfined().use { arena ->
            val objCell = arena.allocate(ADDRESS)
            objCell.set(ADDRESS, 0, objectArg)
            val rectCell = arena.allocate(16L, 4L)
            rectCell.set(JAVA_INT, 0, rect.position.x)
            rectCell.set(JAVA_INT, 4, rect.position.y)
            rectCell.set(JAVA_INT, 8, rect.size.x)
            rectCell.set(JAVA_INT, 12, rect.size.y)
            val positionCell = arena.allocate(8L, 4L)
            positionCell.set(JAVA_INT, 0, position.x)
            positionCell.set(JAVA_INT, 4, position.y)
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, objCell)
            arr.setAtIndex(ADDRESS, 1, rectCell)
            arr.setAtIndex(ADDRESS, 2, positionCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithTwoObjectRect2iAndVector2iArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstObjectArg: MemorySegment,
        secondObjectArg: MemorySegment,
        rect: Rect2i,
        position: Vector2i,
    ) {
        Arena.ofConfined().use { arena ->
            val firstObjCell = arena.allocate(ADDRESS)
            val secondObjCell = arena.allocate(ADDRESS)
            firstObjCell.set(ADDRESS, 0, firstObjectArg)
            secondObjCell.set(ADDRESS, 0, secondObjectArg)
            val rectCell = arena.allocate(16L, 4L)
            rectCell.set(JAVA_INT, 0, rect.position.x)
            rectCell.set(JAVA_INT, 4, rect.position.y)
            rectCell.set(JAVA_INT, 8, rect.size.x)
            rectCell.set(JAVA_INT, 12, rect.size.y)
            val positionCell = arena.allocate(8L, 4L)
            positionCell.set(JAVA_INT, 0, position.x)
            positionCell.set(JAVA_INT, 4, position.y)
            val arr = arena.allocate(ADDRESS, 4)
            arr.setAtIndex(ADDRESS, 0, firstObjCell)
            arr.setAtIndex(ADDRESS, 1, secondObjCell)
            arr.setAtIndex(ADDRESS, 2, rectCell)
            arr.setAtIndex(ADDRESS, 3, positionCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithObjectAndVector2iArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        value: Vector2i,
    ) {
        Arena.ofConfined().use { arena ->
            val objCell = arena.allocate(ADDRESS)
            objCell.set(ADDRESS, 0, objectArg)
            val vec = arena.allocate(8L, 4L)
            vec.set(JAVA_INT, 0, value.x)
            vec.set(JAVA_INT, 4, value.y)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, objCell)
            arr.setAtIndex(ADDRESS, 1, vec)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithObjectAndDoubleArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        value: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val objCell = arena.allocate(ADDRESS)
            objCell.set(ADDRESS, 0, objectArg)
            val doubleCell = arena.allocate(JAVA_DOUBLE)
            doubleCell.set(JAVA_DOUBLE, 0, value)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, objCell)
            arr.setAtIndex(ADDRESS, 1, doubleCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithObjectVector2iAndDoubleArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        vector: Vector2i,
        value: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val objCell = arena.allocate(ADDRESS)
            val vectorCell = arena.allocate(8L, 4L)
            val doubleCell = arena.allocate(JAVA_DOUBLE)
            objCell.set(ADDRESS, 0, objectArg)
            vectorCell.set(JAVA_INT, 0, vector.x)
            vectorCell.set(JAVA_INT, 4, vector.y)
            doubleCell.set(JAVA_DOUBLE, 0, value)
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, objCell)
            arr.setAtIndex(ADDRESS, 1, vectorCell)
            arr.setAtIndex(ADDRESS, 2, doubleCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with a single String argument and String return value.
     */
    fun ptrcallWithStringArgRetString(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: String,
    ): String {
        Arena.ofConfined().use { arena ->
            val argString = arena.allocate(8L, 8L)
            GodotStrings.initString(argString, value)
            val retString = arena.allocate(8L, 8L)
            try {
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, argString)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, retString)
                return GodotStrings.readString(retString)
            } finally {
                GodotStrings.destroyString(argString)
                GodotStrings.destroyString(retString)
            }
        }
    }

    /**
     * Calls [methodBind] with no arguments and String return value.
     */
    fun ptrcallNoArgsRetString(
        methodBind: MemorySegment,
        instance: MemorySegment,
    ): String {
        Arena.ofConfined().use { arena ->
            val retString = arena.allocate(8L, 8L)
            try {
                objectMethodBindPtrcall.invoke(methodBind, instance, MemorySegment.NULL, retString)
                return GodotStrings.readString(retString)
            } finally {
                GodotStrings.destroyString(retString)
            }
        }
    }

    /**
     * Calls [methodBind] with no arguments and decodes PackedStringArray return.
     */
    fun ptrcallNoArgsRetPackedStringList(
        methodBind: MemorySegment,
        instance: MemorySegment,
    ): List<String> {
        Arena.ofConfined().use { arena ->
            val ret = BuiltinTypes.allocatePackedArray(arena)
            objectMethodBindPtrcall.invoke(methodBind, instance, MemorySegment.NULL, ret)
            return try {
                BuiltinTypes.readPackedStringArray(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_STRING_ARRAY, ret)
            }
        }
    }

    /**
     * Calls [methodBind] with no arguments and decodes PackedByteArray return.
     */
    fun ptrcallNoArgsRetByteArray(
        methodBind: MemorySegment,
        instance: MemorySegment,
    ): ByteArray {
        Arena.ofConfined().use { arena ->
            val ret = BuiltinTypes.allocatePackedArray(arena)
            objectMethodBindPtrcall.invoke(methodBind, instance, MemorySegment.NULL, ret)
            return try {
                BuiltinTypes.readPackedByteArray(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, ret)
            }
        }
    }

    /**
     * Calls [methodBind] with no arguments and decodes PackedInt32Array return.
     */
    fun ptrcallNoArgsRetPackedInt32List(
        methodBind: MemorySegment,
        instance: MemorySegment,
    ): List<Int> {
        Arena.ofConfined().use { arena ->
            val ret = BuiltinTypes.allocatePackedArray(arena)
            objectMethodBindPtrcall.invoke(methodBind, instance, MemorySegment.NULL, ret)
            return try {
                BuiltinTypes.readPackedInt32Array(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_INT32_ARRAY, ret)
            }
        }
    }

    /**
     * Calls [methodBind] with one int32 arg and decodes PackedInt32Array return.
     */
    fun ptrcallWithIntArgRetPackedInt32List(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Int,
    ): List<Int> {
        Arena.ofConfined().use { arena ->
            val arg = arena.allocate(JAVA_INT)
            arg.set(JAVA_INT, 0, value)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, arg)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return try {
                BuiltinTypes.readPackedInt32Array(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_INT32_ARRAY, ret)
            }
        }
    }

    /**
     * Calls [methodBind] with one int32 arg and decodes PackedFloat32Array return.
     * Elements use Godot's scalar float ABI slot and are narrowed to Kotlin Float.
     */
    fun ptrcallWithIntArgRetPackedFloat32List(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Int,
    ): List<Float> {
        Arena.ofConfined().use { arena ->
            val arg = arena.allocate(JAVA_INT)
            arg.set(JAVA_INT, 0, value)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, arg)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return try {
                BuiltinTypes.readPackedFloat32Array(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_FLOAT32_ARRAY, ret)
            }
        }
    }

    /**
     * Calls [methodBind] with no arguments and decodes PackedInt64Array return.
     */
    fun ptrcallNoArgsRetPackedInt64List(
        methodBind: MemorySegment,
        instance: MemorySegment,
    ): List<Long> {
        Arena.ofConfined().use { arena ->
            val ret = BuiltinTypes.allocatePackedArray(arena)
            objectMethodBindPtrcall.invoke(methodBind, instance, MemorySegment.NULL, ret)
            return try {
                BuiltinTypes.readPackedInt64Array(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_INT64_ARRAY, ret)
            }
        }
    }

    /**
     * Calls [methodBind] with no arguments and decodes PackedFloat32Array return.
     * Elements use Godot's scalar float ABI slot and are narrowed to Kotlin Float.
     */
    fun ptrcallNoArgsRetPackedFloat32List(
        methodBind: MemorySegment,
        instance: MemorySegment,
    ): List<Float> {
        Arena.ofConfined().use { arena ->
            val ret = BuiltinTypes.allocatePackedArray(arena)
            objectMethodBindPtrcall.invoke(methodBind, instance, MemorySegment.NULL, ret)
            return try {
                BuiltinTypes.readPackedFloat32Array(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_FLOAT32_ARRAY, ret)
            }
        }
    }

    /**
     * Calls [methodBind] with no arguments and decodes PackedFloat64Array return.
     * Elements use Godot's scalar float ABI slot and stay Kotlin Double.
     */
    fun ptrcallNoArgsRetPackedFloat64List(
        methodBind: MemorySegment,
        instance: MemorySegment,
    ): List<Double> {
        Arena.ofConfined().use { arena ->
            val ret = BuiltinTypes.allocatePackedArray(arena)
            objectMethodBindPtrcall.invoke(methodBind, instance, MemorySegment.NULL, ret)
            return try {
                BuiltinTypes.readPackedFloat64Array(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_FLOAT64_ARRAY, ret)
            }
        }
    }

    /**
     * Calls [methodBind] with no arguments and decodes PackedVector2Array return.
     * Elements use GodotReal component storage, matching Vector2 ptrcall layout.
     */
    fun ptrcallNoArgsRetPackedVector2List(
        methodBind: MemorySegment,
        instance: MemorySegment,
    ): List<Vector2> {
        Arena.ofConfined().use { arena ->
            val ret = BuiltinTypes.allocatePackedArray(arena)
            objectMethodBindPtrcall.invoke(methodBind, instance, MemorySegment.NULL, ret)
            return try {
                BuiltinTypes.readPackedVector2Array(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR2_ARRAY, ret)
            }
        }
    }

    fun ptrcallNoArgsRetPackedVector2ListList(
        methodBind: MemorySegment,
        instance: MemorySegment,
    ): List<List<Vector2>> =
        callArrayReturn(methodBind, instance, MemorySegment.NULL, BuiltinTypes::readArrayPackedVector2Arrays)

    /**
     * Calls [methodBind] with no arguments and decodes PackedVector3Array return.
     * Elements use GodotReal component storage, matching Vector3 ptrcall layout.
     */
    fun ptrcallNoArgsRetPackedVector3List(
        methodBind: MemorySegment,
        instance: MemorySegment,
    ): List<Vector3> {
        Arena.ofConfined().use { arena ->
            val ret = BuiltinTypes.allocatePackedArray(arena)
            objectMethodBindPtrcall.invoke(methodBind, instance, MemorySegment.NULL, ret)
            return try {
                BuiltinTypes.readPackedVector3Array(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR3_ARRAY, ret)
            }
        }
    }

    /**
     * Calls [methodBind] with no arguments and decodes PackedColorArray return.
     * Elements use fixed 32-bit Color component storage.
     */
    fun ptrcallNoArgsRetPackedColorList(
        methodBind: MemorySegment,
        instance: MemorySegment,
    ): List<Color> {
        Arena.ofConfined().use { arena ->
            val ret = BuiltinTypes.allocatePackedArray(arena)
            objectMethodBindPtrcall.invoke(methodBind, instance, MemorySegment.NULL, ret)
            return try {
                BuiltinTypes.readPackedColorArray(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_COLOR_ARRAY, ret)
            }
        }
    }

    /**
     * Calls [methodBind] with no arguments and decodes PackedVector4Array return.
     * Elements use GodotReal component storage, matching Vector4 ptrcall layout.
     */
    fun ptrcallNoArgsRetPackedVector4List(
        methodBind: MemorySegment,
        instance: MemorySegment,
    ): List<Vector4> {
        Arena.ofConfined().use { arena ->
            val ret = BuiltinTypes.allocatePackedArray(arena)
            objectMethodBindPtrcall.invoke(methodBind, instance, MemorySegment.NULL, ret)
            return try {
                BuiltinTypes.readPackedVector4Array(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR4_ARRAY, ret)
            }
        }
    }

    /**
     * Calls [methodBind] with no arguments and decodes an Array of Object
     * values as non-owning wrappers.
     */
    fun ptrcallNoArgsRetObjectList(
        methodBind: MemorySegment,
        instance: MemorySegment,
    ): List<GodotObject> {
        Arena.ofConfined().use { arena ->
            val ret = arena.allocate(8L, 8L)
            objectMethodBindPtrcall.invoke(methodBind, instance, MemorySegment.NULL, ret)
            return try {
                BuiltinTypes.readArrayObjects(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, ret)
            }
        }
    }

    fun <T : Any> ptrcallNoArgsRetTypedObjectList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        wrapper: (MemorySegment) -> T?,
    ): List<T> =
        callArrayReturn(methodBind, instance, MemorySegment.NULL) { ret ->
            BuiltinTypes.readArrayObjects(ret, wrapper)
        }

    /**
     * Calls [methodBind] with no arguments and decodes an Array as scalar
     * Kotlin values, nested lists, maps, and non-owning object wrappers.
     */
    fun ptrcallNoArgsRetArray(
        methodBind: MemorySegment,
        instance: MemorySegment,
    ): List<Any?> {
        Arena.ofConfined().use { arena ->
            val ret = arena.allocate(8L, 8L)
            objectMethodBindPtrcall.invoke(methodBind, instance, MemorySegment.NULL, ret)
            return try {
                BuiltinTypes.readArrayScalars(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, ret)
            }
        }
    }

    fun ptrcallNoArgsRetNodePathList(
        methodBind: MemorySegment,
        instance: MemorySegment,
    ): List<NodePath> =
        callArrayReturn(methodBind, instance, MemorySegment.NULL, BuiltinTypes::readArrayNodePaths)

    fun ptrcallNoArgsRetPlaneList(
        methodBind: MemorySegment,
        instance: MemorySegment,
    ): List<Plane> =
        callArrayReturn(methodBind, instance, MemorySegment.NULL, BuiltinTypes::readArrayPlanes)

    fun ptrcallNoArgsRetRect2List(
        methodBind: MemorySegment,
        instance: MemorySegment,
    ): List<Rect2> =
        callArrayReturn(methodBind, instance, MemorySegment.NULL, BuiltinTypes::readArrayRect2)

    fun ptrcallNoArgsRetTransform3DList(
        methodBind: MemorySegment,
        instance: MemorySegment,
    ): List<Transform3D> =
        callArrayReturn(methodBind, instance, MemorySegment.NULL, BuiltinTypes::readArrayTransform3D)

    fun ptrcallNoArgsRetVector2List(
        methodBind: MemorySegment,
        instance: MemorySegment,
    ): List<Vector2> =
        callArrayReturn(methodBind, instance, MemorySegment.NULL, BuiltinTypes::readArrayVector2)

    fun ptrcallNoArgsRetVector3List(
        methodBind: MemorySegment,
        instance: MemorySegment,
    ): List<Vector3> =
        callArrayReturn(methodBind, instance, MemorySegment.NULL, BuiltinTypes::readArrayVector3)

    fun ptrcallNoArgsRetArrayList(
        methodBind: MemorySegment,
        instance: MemorySegment,
    ): List<List<Any?>> =
        callArrayReturn(methodBind, instance, MemorySegment.NULL, BuiltinTypes::readArrayArrays)

    fun ptrcallNoArgsRetByteArrayList(
        methodBind: MemorySegment,
        instance: MemorySegment,
    ): List<ByteArray> =
        callArrayReturn(methodBind, instance, MemorySegment.NULL, BuiltinTypes::readArrayPackedByteArrays)

    fun ptrcallNoArgsRetPackedStringListList(
        methodBind: MemorySegment,
        instance: MemorySegment,
    ): List<List<String>> =
        callArrayReturn(methodBind, instance, MemorySegment.NULL, BuiltinTypes::readArrayPackedStringArrays)

    fun ptrcallNoArgsRetTypedNodeList(
        methodBind: MemorySegment,
        instance: MemorySegment,
    ): List<Node> =
        callArrayReturn(methodBind, instance, MemorySegment.NULL) { ret ->
            BuiltinTypes.readArrayObjects(ret, ::Node)
        }

    fun ptrcallNoArgsRetTypedNode2DList(
        methodBind: MemorySegment,
        instance: MemorySegment,
    ): List<Node2D> =
        callArrayReturn(methodBind, instance, MemorySegment.NULL) { ret ->
            BuiltinTypes.readArrayObjects(ret, ::Node2D)
        }

    fun ptrcallNoArgsRetTypedNode3DList(
        methodBind: MemorySegment,
        instance: MemorySegment,
    ): List<Node3D> =
        callArrayReturn(methodBind, instance, MemorySegment.NULL) { ret ->
            BuiltinTypes.readArrayObjects(ret, ::Node3D)
        }

    fun ptrcallNoArgsRetTypedMaterialList(
        methodBind: MemorySegment,
        instance: MemorySegment,
    ): List<Material> =
        callArrayReturn(methodBind, instance, MemorySegment.NULL) { ret ->
            BuiltinTypes.readArrayObjects(ret, Material::wrap)
        }

    fun ptrcallNoArgsRetTypedArea2DList(
        methodBind: MemorySegment,
        instance: MemorySegment,
    ): List<Area2D> =
        callArrayReturn(methodBind, instance, MemorySegment.NULL) { ret ->
            BuiltinTypes.readArrayObjects(ret, ::Area2D)
        }

    fun ptrcallNoArgsRetTypedArea3DList(
        methodBind: MemorySegment,
        instance: MemorySegment,
    ): List<Area3D> =
        callArrayReturn(methodBind, instance, MemorySegment.NULL) { ret ->
            BuiltinTypes.readArrayObjects(ret, ::Area3D)
        }

    fun ptrcallNoArgsRetTypedBaseButtonList(
        methodBind: MemorySegment,
        instance: MemorySegment,
    ): List<BaseButton> =
        callArrayReturn(methodBind, instance, MemorySegment.NULL) { ret ->
            BuiltinTypes.readArrayObjects(ret, ::BaseButton)
        }

    fun ptrcallNoArgsRetTypedPhysicsBody3DList(
        methodBind: MemorySegment,
        instance: MemorySegment,
    ): List<PhysicsBody3D> =
        callArrayReturn(methodBind, instance, MemorySegment.NULL) { ret ->
            BuiltinTypes.readArrayObjects(ret, ::PhysicsBody3D)
        }

    fun ptrcallNoArgsRetVector2iList(
        methodBind: MemorySegment,
        instance: MemorySegment,
    ): List<Vector2i> =
        callArrayReturn(methodBind, instance, MemorySegment.NULL, BuiltinTypes::readArrayVector2i)

    /**
     * Calls [methodBind] with no arguments and decodes an Array of Vector3i values.
     */
    fun ptrcallNoArgsRetVector3iList(
        methodBind: MemorySegment,
        instance: MemorySegment,
    ): List<Vector3i> {
        Arena.ofConfined().use { arena ->
            val ret = arena.allocate(8L, 8L)
            objectMethodBindPtrcall.invoke(methodBind, instance, MemorySegment.NULL, ret)
            return try {
                BuiltinTypes.readArrayVector3i(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, ret)
            }
        }
    }

    /**
     * Calls [methodBind] with no arguments and decodes an Array of int values.
     */
    fun ptrcallNoArgsRetLongList(
        methodBind: MemorySegment,
        instance: MemorySegment,
    ): List<Long> {
        Arena.ofConfined().use { arena ->
            val ret = arena.allocate(8L, 8L)
            objectMethodBindPtrcall.invoke(methodBind, instance, MemorySegment.NULL, ret)
            return try {
                BuiltinTypes.readArrayLongs(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, ret)
            }
        }
    }

    fun ptrcallNoArgsRetRIDList(
        methodBind: MemorySegment,
        instance: MemorySegment,
    ): List<RID> {
        Arena.ofConfined().use { arena ->
            val ret = arena.allocate(8L, 8L)
            objectMethodBindPtrcall.invoke(methodBind, instance, MemorySegment.NULL, ret)
            return try {
                BuiltinTypes.readArrayRids(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, ret)
            }
        }
    }

    fun ptrcallNoArgsRetTypedStringList(
        methodBind: MemorySegment,
        instance: MemorySegment,
    ): List<String> {
        Arena.ofConfined().use { arena ->
            val ret = arena.allocate(8L, 8L)
            objectMethodBindPtrcall.invoke(methodBind, instance, MemorySegment.NULL, ret)
            return try {
                BuiltinTypes.readArrayStrings(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, ret)
            }
        }
    }

    fun ptrcallNoArgsRetStringNameList(
        methodBind: MemorySegment,
        instance: MemorySegment,
    ): List<String> {
        Arena.ofConfined().use { arena ->
            val ret = arena.allocate(8L, 8L)
            objectMethodBindPtrcall.invoke(methodBind, instance, MemorySegment.NULL, ret)
            return try {
                BuiltinTypes.readArrayStringNames(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, ret)
            }
        }
    }

    fun ptrcallNoArgsRetDictionaryList(
        methodBind: MemorySegment,
        instance: MemorySegment,
    ): List<Map<String, Any?>> {
        Arena.ofConfined().use { arena ->
            val ret = arena.allocate(8L, 8L)
            objectMethodBindPtrcall.invoke(methodBind, instance, MemorySegment.NULL, ret)
            return try {
                BuiltinTypes.readArrayDictionaries(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, ret)
            }
        }
    }

    /**
     * Calls [methodBind] with one bool argument and decodes an Array of Object values as non-owning
     * wrappers.
     */
    fun ptrcallWithBoolArgRetObjectList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        boolArg: Boolean,
    ): List<GodotObject> {
        val scratch = ptrcallScratch.get()
        objectMethodBindPtrcall.invoke(methodBind, instance, scratch.setBoolArg(boolArg), scratch.arrayRet)
        return try {
            BuiltinTypes.readArrayObjects(scratch.arrayRet)
        } finally {
            BuiltinTypes.destroyTyped(VariantType.ARRAY, scratch.arrayRet)
        }
    }

    fun <T : Any> ptrcallWithBoolArgRetTypedObjectList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        boolArg: Boolean,
        wrapper: (MemorySegment) -> T?,
    ): List<T> {
        val scratch = ptrcallScratch.get()
        objectMethodBindPtrcall.invoke(methodBind, instance, scratch.setBoolArg(boolArg), scratch.arrayRet)
        return try {
            BuiltinTypes.readArrayObjects(scratch.arrayRet, wrapper)
        } finally {
            BuiltinTypes.destroyTyped(VariantType.ARRAY, scratch.arrayRet)
        }
    }

    fun ptrcallWithBoolArgRetTypedNodeList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        boolArg: Boolean,
    ): List<Node> =
        ptrcallWithBoolArgRetTypedObjectList(methodBind, instance, boolArg, Node::fromHandle)

    fun ptrcallWithStringAndBoolArgRetObjectList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        boolArg: Boolean,
    ): List<GodotObject> {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val boolCell = arena.allocate(JAVA_BYTE)
            val ret = arena.allocate(8L, 8L)
            try {
                GodotStrings.initString(stringCell, text)
                boolCell.set(JAVA_BYTE, 0, if (boolArg) 1.toByte() else 0.toByte())
                val args = arena.allocate(ADDRESS, 2)
                args.setAtIndex(ADDRESS, 0, stringCell)
                args.setAtIndex(ADDRESS, 1, boolCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return BuiltinTypes.readArrayObjects(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, ret)
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun <T : Any> ptrcallWithStringAndBoolArgRetTypedObjectList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        boolArg: Boolean,
        wrapper: (MemorySegment) -> T?,
    ): List<T> {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val boolCell = arena.allocate(JAVA_BYTE)
            try {
                GodotStrings.initString(stringCell, text)
                boolCell.set(JAVA_BYTE, 0, if (boolArg) 1.toByte() else 0.toByte())
                val args = arena.allocate(ADDRESS, 2)
                args.setAtIndex(ADDRESS, 0, stringCell)
                args.setAtIndex(ADDRESS, 1, boolCell)
                return callArrayReturn(methodBind, instance, args) { ret ->
                    BuiltinTypes.readArrayObjects(ret, wrapper)
                }
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithStringTwoIntArgsRetObjectList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        first: Int,
        second: Int,
    ): List<GodotObject> {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val firstCell = arena.allocate(JAVA_INT)
            val secondCell = arena.allocate(JAVA_INT)
            val ret = arena.allocate(8L, 8L)
            try {
                GodotStrings.initString(stringCell, text)
                firstCell.set(JAVA_INT, 0, first)
                secondCell.set(JAVA_INT, 0, second)
                val args = arena.allocate(ADDRESS, 3)
                args.setAtIndex(ADDRESS, 0, stringCell)
                args.setAtIndex(ADDRESS, 1, firstCell)
                args.setAtIndex(ADDRESS, 2, secondCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return BuiltinTypes.readArrayObjects(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, ret)
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun <T : Any> ptrcallWithStringTwoIntArgsRetTypedObjectList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        first: Int,
        second: Int,
        wrapper: (MemorySegment) -> T?,
    ): List<T> =
        ptrcallWithStringTwoIntArgsRetObjectList(methodBind, instance, text, first, second)
            .mapNotNull { wrapper(it.handle) }

    fun ptrcallWithThreeIntTwoBoolArgsRetObjectList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Int,
        third: Int,
        firstBool: Boolean,
        secondBool: Boolean,
    ): List<GodotObject> {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(JAVA_INT)
            val secondCell = arena.allocate(JAVA_INT)
            val thirdCell = arena.allocate(JAVA_INT)
            val firstBoolCell = arena.allocate(JAVA_BYTE)
            val secondBoolCell = arena.allocate(JAVA_BYTE)
            val ret = arena.allocate(8L, 8L)
            firstCell.set(JAVA_INT, 0, first)
            secondCell.set(JAVA_INT, 0, second)
            thirdCell.set(JAVA_INT, 0, third)
            firstBoolCell.set(JAVA_BYTE, 0, if (firstBool) 1.toByte() else 0.toByte())
            secondBoolCell.set(JAVA_BYTE, 0, if (secondBool) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 5)
            args.setAtIndex(ADDRESS, 0, firstCell)
            args.setAtIndex(ADDRESS, 1, secondCell)
            args.setAtIndex(ADDRESS, 2, thirdCell)
            args.setAtIndex(ADDRESS, 3, firstBoolCell)
            args.setAtIndex(ADDRESS, 4, secondBoolCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return try {
                BuiltinTypes.readArrayObjects(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, ret)
            }
        }
    }

    fun <T : Any> ptrcallWithThreeIntTwoBoolArgsRetTypedObjectList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Int,
        third: Int,
        firstBool: Boolean,
        secondBool: Boolean,
        wrapper: (MemorySegment) -> T?,
    ): List<T> =
        ptrcallWithThreeIntTwoBoolArgsRetObjectList(methodBind, instance, first, second, third, firstBool, secondBool)
            .mapNotNull { wrapper(it.handle) }

    fun ptrcallWithThreeIntBoolDoubleBoolArgsRetObjectList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Int,
        third: Int,
        firstBool: Boolean,
        value: Double,
        secondBool: Boolean,
    ): List<GodotObject> {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(JAVA_INT)
            val secondCell = arena.allocate(JAVA_INT)
            val thirdCell = arena.allocate(JAVA_INT)
            val firstBoolCell = arena.allocate(JAVA_BYTE)
            val valueCell = arena.allocate(JAVA_DOUBLE)
            val secondBoolCell = arena.allocate(JAVA_BYTE)
            val ret = arena.allocate(8L, 8L)
            firstCell.set(JAVA_INT, 0, first)
            secondCell.set(JAVA_INT, 0, second)
            thirdCell.set(JAVA_INT, 0, third)
            firstBoolCell.set(JAVA_BYTE, 0, if (firstBool) 1.toByte() else 0.toByte())
            valueCell.set(JAVA_DOUBLE, 0, value)
            secondBoolCell.set(JAVA_BYTE, 0, if (secondBool) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 6)
            args.setAtIndex(ADDRESS, 0, firstCell)
            args.setAtIndex(ADDRESS, 1, secondCell)
            args.setAtIndex(ADDRESS, 2, thirdCell)
            args.setAtIndex(ADDRESS, 3, firstBoolCell)
            args.setAtIndex(ADDRESS, 4, valueCell)
            args.setAtIndex(ADDRESS, 5, secondBoolCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return try {
                BuiltinTypes.readArrayObjects(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, ret)
            }
        }
    }

    fun <T : Any> ptrcallWithThreeIntBoolDoubleBoolArgsRetTypedObjectList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Int,
        third: Int,
        firstBool: Boolean,
        value: Double,
        secondBool: Boolean,
        wrapper: (MemorySegment) -> T?,
    ): List<T> =
        ptrcallWithThreeIntBoolDoubleBoolArgsRetObjectList(
            methodBind,
            instance,
            first,
            second,
            third,
            firstBool,
            value,
            secondBool,
        ).mapNotNull { wrapper(it.handle) }

    fun ptrcallWithTwoStringAndTwoBoolArgsRetTypedNodeList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: String,
        second: String,
        firstBool: Boolean,
        secondBool: Boolean,
    ): List<Node> {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(8L, 8L)
            val arg1 = arena.allocate(8L, 8L)
            val arg2 = arena.allocate(JAVA_BYTE)
            val arg3 = arena.allocate(JAVA_BYTE)
            try {
                GodotStrings.initString(arg0, first)
                GodotStrings.initString(arg1, second)
                arg2.set(JAVA_BYTE, 0, if (firstBool) 1.toByte() else 0.toByte())
                arg3.set(JAVA_BYTE, 0, if (secondBool) 1.toByte() else 0.toByte())
                val args = arena.allocate(ADDRESS, 4)
                args.setAtIndex(ADDRESS, 0, arg0)
                args.setAtIndex(ADDRESS, 1, arg1)
                args.setAtIndex(ADDRESS, 2, arg2)
                args.setAtIndex(ADDRESS, 3, arg3)
                return callArrayReturn(methodBind, instance, args) { ret ->
                    BuiltinTypes.readArrayObjects(ret, ::Node)
                }
            } finally {
                GodotStrings.destroyString(arg0)
                GodotStrings.destroyString(arg1)
            }
        }
    }

    /**
     * Calls [methodBind] with (StringName, Variant) and no return value.
     */
    fun ptrcallWithStringNameAndVariantArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
        value: Any?,
    ) {
        Arena.ofConfined().use { arena ->
            val valueVariant = arena.allocate(BuiltinTypes.VARIANT_SIZE, 8L)
            BuiltinTypes.initVariantFromAny(valueVariant, value, arena)
            try {
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(name))
                arr.setAtIndex(ADDRESS, 1, valueVariant)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyVariant(valueVariant)
            }
        }
    }

    fun ptrcallWithStringNameAndVariantArgRetVariantScalar(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
        value: Any?,
    ): Any? {
        Arena.ofConfined().use { arena ->
            val variant = initVariantCell(arena, value)
            try {
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(name))
                arr.setAtIndex(ADDRESS, 1, variant)
                return readVariantReturn(methodBind, instance, arr, arena)
            } finally {
                BuiltinTypes.destroyVariant(variant)
            }
        }
    }

    fun ptrcallWithStringNameStringAndVariantArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
        text: String,
        value: Any?,
    ) {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val variant = initVariantCell(arena, value)
            try {
                GodotStrings.initString(stringCell, text)
                val arr = arena.allocate(ADDRESS, 3)
                arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(name))
                arr.setAtIndex(ADDRESS, 1, stringCell)
                arr.setAtIndex(ADDRESS, 2, variant)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyVariant(variant)
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithUInt32StringNameStringVariantArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        flags: Long,
        group: String,
        method: String,
        value: Any?,
    ) {
        val checkedFlags = BuiltinTypes.requireUInt32(flags)
        Arena.ofConfined().use { arena ->
            val flagsCell = arena.allocate(JAVA_INT)
            val methodCell = arena.allocate(8L, 8L)
            val variant = initVariantCell(arena, value)
            try {
                flagsCell.set(JAVA_INT, 0, checkedFlags)
                GodotStrings.initString(methodCell, method)
                val args = arena.allocate(ADDRESS, 4)
                args.setAtIndex(ADDRESS, 0, flagsCell)
                args.setAtIndex(ADDRESS, 1, GodotStrings.makeStringName(group))
                args.setAtIndex(ADDRESS, 2, methodCell)
                args.setAtIndex(ADDRESS, 3, variant)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyVariant(variant)
                GodotStrings.destroyString(methodCell)
            }
        }
    }

    fun ptrcallWithStringNameVariantBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
        value: Any?,
        boolArg: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val variant = initVariantCell(arena, value)
            try {
                val boolCell = arena.allocate(JAVA_BYTE)
                boolCell.set(JAVA_BYTE, 0, if (boolArg) 1.toByte() else 0.toByte())
                val arr = arena.allocate(ADDRESS, 3)
                arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(name))
                arr.setAtIndex(ADDRESS, 1, variant)
                arr.setAtIndex(ADDRESS, 2, boolCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyVariant(variant)
            }
        }
    }

    fun ptrcallWithStringNameVariantStringNameBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstName: String,
        value: Any?,
        secondName: String,
        boolArg: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val variant = initVariantCell(arena, value)
            try {
                val boolCell = arena.allocate(JAVA_BYTE)
                boolCell.set(JAVA_BYTE, 0, if (boolArg) 1.toByte() else 0.toByte())
                val arr = arena.allocate(ADDRESS, 4)
                arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(firstName))
                arr.setAtIndex(ADDRESS, 1, variant)
                arr.setAtIndex(ADDRESS, 2, GodotStrings.makeStringName(secondName))
                arr.setAtIndex(ADDRESS, 3, boolCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyVariant(variant)
            }
        }
    }

    /**
     * Calls [methodBind] with one Variant argument and no return value.
     */
    fun ptrcallWithVariantArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Any?,
    ) {
        Arena.ofConfined().use { arena ->
            val variant = arena.allocate(BuiltinTypes.VARIANT_SIZE, 8L)
            var initialized = false
            try {
                BuiltinTypes.initVariantFromAny(variant, value, arena)
                initialized = true
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, variant)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                if (initialized) {
                    BuiltinTypes.destroyVariant(variant)
                }
            }
        }
    }

    /**
     * Calls [methodBind] with (Variant, bool) and decodes a Variant return.
     */
    fun ptrcallWithVariantAndBoolArgRetVariantScalar(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Any?,
        boolArg: Boolean,
    ): Any? {
        Arena.ofConfined().use { arena ->
            val variant = initVariantCell(arena, value)
            try {
                val boolCell = arena.allocate(JAVA_BYTE)
                boolCell.set(JAVA_BYTE, 0, if (boolArg) 1.toByte() else 0.toByte())
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, variant)
                arr.setAtIndex(ADDRESS, 1, boolCell)
                return readVariantReturn(methodBind, instance, arr, arena)
            } finally {
                BuiltinTypes.destroyVariant(variant)
            }
        }
    }

    fun ptrcallWithTwoVariantArgsRetDictionary(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstValue: Any?,
        secondValue: Any?,
    ): Map<String, Any?> {
        Arena.ofConfined().use { arena ->
            val firstVariant = initVariantCell(arena, firstValue)
            try {
                val secondVariant = initVariantCell(arena, secondValue)
                try {
                    val arr = arena.allocate(ADDRESS, 2)
                    arr.setAtIndex(ADDRESS, 0, firstVariant)
                    arr.setAtIndex(ADDRESS, 1, secondVariant)
                    val ret = arena.allocate(8L, 8L)
                    objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                    return try {
                        BuiltinTypes.readDictionaryScalars(ret)
                    } finally {
                        BuiltinTypes.destroyTyped(VariantType.DICTIONARY, ret)
                    }
                } finally {
                    BuiltinTypes.destroyVariant(secondVariant)
                }
            } finally {
                BuiltinTypes.destroyVariant(firstVariant)
            }
        }
    }

    fun ptrcallWithTwoVariantTwoDoubleTwoLongArgsRetVariantScalar(
        methodBind: MemorySegment,
        instance: MemorySegment,
        initialValue: Any?,
        deltaValue: Any?,
        elapsedTime: Double,
        duration: Double,
        transition: Long,
        ease: Long,
    ): Any? {
        Arena.ofConfined().use { arena ->
            val initialVariant = initVariantCell(arena, initialValue)
            try {
                val deltaVariant = initVariantCell(arena, deltaValue)
                try {
                    val elapsedCell = arena.allocate(JAVA_DOUBLE)
                    val durationCell = arena.allocate(JAVA_DOUBLE)
                    val transitionCell = arena.allocate(JAVA_LONG)
                    val easeCell = arena.allocate(JAVA_LONG)
                    elapsedCell.set(JAVA_DOUBLE, 0, elapsedTime)
                    durationCell.set(JAVA_DOUBLE, 0, duration)
                    transitionCell.set(JAVA_LONG, 0, transition)
                    easeCell.set(JAVA_LONG, 0, ease)
                    val args = arena.allocate(ADDRESS, 6)
                    args.setAtIndex(ADDRESS, 0, initialVariant)
                    args.setAtIndex(ADDRESS, 1, deltaVariant)
                    args.setAtIndex(ADDRESS, 2, elapsedCell)
                    args.setAtIndex(ADDRESS, 3, durationCell)
                    args.setAtIndex(ADDRESS, 4, transitionCell)
                    args.setAtIndex(ADDRESS, 5, easeCell)
                    return readVariantReturn(methodBind, instance, args, arena)
                } finally {
                    BuiltinTypes.destroyVariant(deltaVariant)
                }
            } finally {
                BuiltinTypes.destroyVariant(initialVariant)
            }
        }
    }

    fun ptrcallWithVariantArgRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Any?,
    ): Int {
        Arena.ofConfined().use { arena ->
            val variant = initVariantCell(arena, value)
            try {
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, variant)
                val ret = arena.allocate(JAVA_INT)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_INT, 0)
            } finally {
                BuiltinTypes.destroyVariant(variant)
            }
        }
    }

    fun ptrcallWithVariantArgRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Any?,
    ): Long {
        Arena.ofConfined().use { arena ->
            val variant = initVariantCell(arena, value)
            try {
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, variant)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                BuiltinTypes.destroyVariant(variant)
            }
        }
    }

    fun ptrcallWithVariantArgRetString(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Any?,
    ): String {
        Arena.ofConfined().use { arena ->
            val variant = initVariantCell(arena, value)
            val ret = arena.allocate(8L, 8L)
            try {
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, variant)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return GodotStrings.readString(ret)
            } finally {
                GodotStrings.destroyString(ret)
                BuiltinTypes.destroyVariant(variant)
            }
        }
    }

    fun ptrcallWithVariantArgRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Any?,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val variant = initVariantCell(arena, value)
            try {
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, variant)
                val ret = arena.allocate(JAVA_BYTE)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_BYTE, 0) != 0.toByte()
            } finally {
                BuiltinTypes.destroyVariant(variant)
            }
        }
    }

    fun ptrcallWithVariantArgRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Any?,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val variant = initVariantCell(arena, value)
            try {
                val args = arena.allocate(ADDRESS, 1)
                args.setAtIndex(ADDRESS, 0, variant)
                val ret = arena.allocate(ADDRESS)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(ADDRESS, 0)
            } finally {
                BuiltinTypes.destroyVariant(variant)
            }
        }
    }

    fun ptrcallWithConstVoidPtrArgRetTransform3D(
        methodBind: MemorySegment,
        instance: MemorySegment,
        pointer: MemorySegment,
    ): Transform3D {
        Arena.ofConfined().use { arena ->
            val pointerCell = arena.allocate(ADDRESS)
            pointerCell.set(ADDRESS, 0, pointer)
            val args = arena.allocate(ADDRESS, 1)
            args.setAtIndex(ADDRESS, 0, pointerCell)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 12, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return readTransform3D(ret)
        }
    }

    fun ptrcallWithConstVoidPtrArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        pointer: MemorySegment,
    ) {
        Arena.ofConfined().use { arena ->
            val pointerCell = arena.allocate(ADDRESS)
            pointerCell.set(ADDRESS, 0, pointer)
            val args = arena.allocate(ADDRESS, 1)
            args.setAtIndex(ADDRESS, 0, pointerCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithStringConstGDExtensionInitializationFunctionPtrArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        path: String,
        initializer: MemorySegment,
    ): Long {
        Arena.ofConfined().use { arena ->
            val pathCell = arena.allocate(8L, 8L)
            val initializerCell = arena.allocate(ADDRESS)
            try {
                GodotStrings.initString(pathCell, path)
                initializerCell.set(ADDRESS, 0, initializer)
                val args = arena.allocate(ADDRESS, 2)
                args.setAtIndex(ADDRESS, 0, pathCell)
                args.setAtIndex(ADDRESS, 1, initializerCell)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                GodotStrings.destroyString(pathCell)
            }
        }
    }

    fun ptrcallWithVariantArgRetRect2(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Any?,
    ): Rect2 {
        Arena.ofConfined().use { arena ->
            val variant = initVariantCell(arena, value)
            try {
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, variant)
                val ret = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return readRect2(ret)
            } finally {
                BuiltinTypes.destroyVariant(variant)
            }
        }
    }

    fun ptrcallWithVariantAndBoolArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Any?,
        boolArg: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val variant = initVariantCell(arena, value)
            try {
                val boolCell = arena.allocate(JAVA_BYTE)
                boolCell.set(JAVA_BYTE, 0, if (boolArg) 1.toByte() else 0.toByte())
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, variant)
                arr.setAtIndex(ADDRESS, 1, boolCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyVariant(variant)
            }
        }
    }

    fun ptrcallWithVariantAndObjectArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Any?,
        objectArg: MemorySegment,
    ) {
        Arena.ofConfined().use { arena ->
            val variant = initVariantCell(arena, value)
            val objectCell = arena.allocate(ADDRESS)
            try {
                objectCell.set(ADDRESS, 0, objectArg)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, variant)
                arr.setAtIndex(ADDRESS, 1, objectCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyVariant(variant)
            }
        }
    }

    fun ptrcallWithVariantAndBoolArgRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Any?,
        boolArg: Boolean,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val variant = initVariantCell(arena, value)
            try {
                val boolCell = arena.allocate(JAVA_BYTE)
                boolCell.set(JAVA_BYTE, 0, if (boolArg) 1.toByte() else 0.toByte())
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, variant)
                arr.setAtIndex(ADDRESS, 1, boolCell)
                val ret = arena.allocate(JAVA_BYTE)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_BYTE, 0) != 0.toByte()
            } finally {
                BuiltinTypes.destroyVariant(variant)
            }
        }
    }

    fun ptrcallWithVariantAndBoolArgRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Any?,
        boolArg: Boolean,
    ): Long {
        Arena.ofConfined().use { arena ->
            val variant = initVariantCell(arena, value)
            try {
                val boolCell = arena.allocate(JAVA_BYTE)
                boolCell.set(JAVA_BYTE, 0, if (boolArg) 1.toByte() else 0.toByte())
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, variant)
                arr.setAtIndex(ADDRESS, 1, boolCell)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                BuiltinTypes.destroyVariant(variant)
            }
        }
    }

    fun ptrcallWithVariantAndBoolArgRetString(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Any?,
        boolArg: Boolean,
    ): String {
        Arena.ofConfined().use { arena ->
            val variant = initVariantCell(arena, value)
            val ret = arena.allocate(8L, 8L)
            try {
                val boolCell = arena.allocate(JAVA_BYTE)
                boolCell.set(JAVA_BYTE, 0, if (boolArg) 1.toByte() else 0.toByte())
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, variant)
                arr.setAtIndex(ADDRESS, 1, boolCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return GodotStrings.readString(ret)
            } finally {
                GodotStrings.destroyString(ret)
                BuiltinTypes.destroyVariant(variant)
            }
        }
    }

    fun ptrcallWithVariantVector2LongIntDoubleArgsRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Any?,
        vector: Vector2,
        longValue: Long,
        intValue: Int,
        doubleValue: Double,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val variant = initVariantCell(arena, value)
            try {
                val vectorCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
                val longCell = arena.allocate(JAVA_LONG)
                val intCell = arena.allocate(JAVA_INT)
                val doubleCell = arena.allocate(JAVA_DOUBLE)
                writeVector2(vectorCell, vector)
                longCell.set(JAVA_LONG, 0, longValue)
                intCell.set(JAVA_INT, 0, intValue)
                doubleCell.set(JAVA_DOUBLE, 0, doubleValue)
                val arr = arena.allocate(ADDRESS, 5)
                arr.setAtIndex(ADDRESS, 0, variant)
                arr.setAtIndex(ADDRESS, 1, vectorCell)
                arr.setAtIndex(ADDRESS, 2, longCell)
                arr.setAtIndex(ADDRESS, 3, intCell)
                arr.setAtIndex(ADDRESS, 4, doubleCell)
                val ret = arena.allocate(JAVA_BYTE)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_BYTE, 0) != 0.toByte()
            } finally {
                BuiltinTypes.destroyVariant(variant)
            }
        }
    }

    fun ptrcallWithVariantVector2LongDoubleArgsRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Any?,
        vector: Vector2,
        longValue: Long,
        doubleValue: Double,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val variant = initVariantCell(arena, value)
            try {
                val vectorCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
                val longCell = arena.allocate(JAVA_LONG)
                val doubleCell = arena.allocate(JAVA_DOUBLE)
                writeVector2(vectorCell, vector)
                longCell.set(JAVA_LONG, 0, longValue)
                doubleCell.set(JAVA_DOUBLE, 0, doubleValue)
                val arr = arena.allocate(ADDRESS, 4)
                arr.setAtIndex(ADDRESS, 0, variant)
                arr.setAtIndex(ADDRESS, 1, vectorCell)
                arr.setAtIndex(ADDRESS, 2, longCell)
                arr.setAtIndex(ADDRESS, 3, doubleCell)
                val ret = arena.allocate(JAVA_BYTE)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_BYTE, 0) != 0.toByte()
            } finally {
                BuiltinTypes.destroyVariant(variant)
            }
        }
    }

    fun ptrcallWithVariantLongObjectTwoIntColorLongRect2BoolStringTwoBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        key: Any?,
        mask: Long,
        image: MemorySegment,
        width: Int,
        height: Int,
        color: Color,
        inlineAlign: Long,
        region: Rect2,
        pad: Boolean,
        tooltip: String,
        sizeInPercent: Boolean,
        altText: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val keyVariant = initVariantCell(arena, key)
            val maskCell = arena.allocate(JAVA_LONG)
            val imageCell = arena.allocate(ADDRESS)
            val widthCell = arena.allocate(JAVA_INT)
            val heightCell = arena.allocate(JAVA_INT)
            val colorCell = arena.allocate(16L, 4L)
            val inlineAlignCell = arena.allocate(JAVA_LONG)
            val regionCell = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
            val padCell = arena.allocate(JAVA_BYTE)
            val tooltipCell = arena.allocate(8L, 8L)
            val sizeInPercentCell = arena.allocate(JAVA_BYTE)
            val altTextCell = arena.allocate(JAVA_BYTE)
            try {
                maskCell.set(JAVA_LONG, 0, mask)
                imageCell.set(ADDRESS, 0, image)
                widthCell.set(JAVA_INT, 0, width)
                heightCell.set(JAVA_INT, 0, height)
                colorCell.set(JAVA_FLOAT, 0, color.r)
                colorCell.set(JAVA_FLOAT, 4, color.g)
                colorCell.set(JAVA_FLOAT, 8, color.b)
                colorCell.set(JAVA_FLOAT, 12, color.a)
                inlineAlignCell.set(JAVA_LONG, 0, inlineAlign)
                writeRect2(regionCell, region)
                padCell.set(JAVA_BYTE, 0, if (pad) 1.toByte() else 0.toByte())
                GodotStrings.initString(tooltipCell, tooltip)
                sizeInPercentCell.set(JAVA_BYTE, 0, if (sizeInPercent) 1.toByte() else 0.toByte())
                altTextCell.set(JAVA_BYTE, 0, if (altText) 1.toByte() else 0.toByte())
                val args = arena.allocate(ADDRESS, 12)
                args.setAtIndex(ADDRESS, 0, keyVariant)
                args.setAtIndex(ADDRESS, 1, maskCell)
                args.setAtIndex(ADDRESS, 2, imageCell)
                args.setAtIndex(ADDRESS, 3, widthCell)
                args.setAtIndex(ADDRESS, 4, heightCell)
                args.setAtIndex(ADDRESS, 5, colorCell)
                args.setAtIndex(ADDRESS, 6, inlineAlignCell)
                args.setAtIndex(ADDRESS, 7, regionCell)
                args.setAtIndex(ADDRESS, 8, padCell)
                args.setAtIndex(ADDRESS, 9, tooltipCell)
                args.setAtIndex(ADDRESS, 10, sizeInPercentCell)
                args.setAtIndex(ADDRESS, 11, altTextCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(tooltipCell)
                BuiltinTypes.destroyVariant(keyVariant)
            }
        }
    }

    fun ptrcallWithVariantLongStringArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Any?,
        longValue: Long,
        text: String,
    ) {
        Arena.ofConfined().use { arena ->
            val variant = initVariantCell(arena, value)
            val stringCell = arena.allocate(8L, 8L)
            try {
                val longCell = arena.allocate(JAVA_LONG)
                GodotStrings.initString(stringCell, text)
                longCell.set(JAVA_LONG, 0, longValue)
                val args = arena.allocate(ADDRESS, 3)
                args.setAtIndex(ADDRESS, 0, variant)
                args.setAtIndex(ADDRESS, 1, longCell)
                args.setAtIndex(ADDRESS, 2, stringCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(stringCell)
                BuiltinTypes.destroyVariant(variant)
            }
        }
    }

    fun ptrcallWithVariantStringTwoBoolArgsRetString(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Any?,
        text: String,
        firstBool: Boolean,
        secondBool: Boolean,
    ): String {
        Arena.ofConfined().use { arena ->
            val variant = initVariantCell(arena, value)
            val stringCell = arena.allocate(8L, 8L)
            val ret = arena.allocate(8L, 8L)
            try {
                val bool0 = arena.allocate(JAVA_BYTE)
                val bool1 = arena.allocate(JAVA_BYTE)
                GodotStrings.initString(stringCell, text)
                bool0.set(JAVA_BYTE, 0, if (firstBool) 1.toByte() else 0.toByte())
                bool1.set(JAVA_BYTE, 0, if (secondBool) 1.toByte() else 0.toByte())
                val args = arena.allocate(ADDRESS, 4)
                args.setAtIndex(ADDRESS, 0, variant)
                args.setAtIndex(ADDRESS, 1, stringCell)
                args.setAtIndex(ADDRESS, 2, bool0)
                args.setAtIndex(ADDRESS, 3, bool1)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return GodotStrings.readString(ret)
            } finally {
                GodotStrings.destroyString(ret)
                GodotStrings.destroyString(stringCell)
                BuiltinTypes.destroyVariant(variant)
            }
        }
    }

    fun ptrcallWithIntAndTwoVariantArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        intValue: Int,
        firstValue: Any?,
        secondValue: Any?,
    ) {
        Arena.ofConfined().use { arena ->
            val firstVariant = initVariantCell(arena, firstValue)
            try {
                val secondVariant = initVariantCell(arena, secondValue)
                try {
                    val intCell = arena.allocate(JAVA_INT)
                    intCell.set(JAVA_INT, 0, intValue)
                    val args = arena.allocate(ADDRESS, 3)
                    args.setAtIndex(ADDRESS, 0, intCell)
                    args.setAtIndex(ADDRESS, 1, firstVariant)
                    args.setAtIndex(ADDRESS, 2, secondVariant)
                    objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
                } finally {
                    BuiltinTypes.destroyVariant(secondVariant)
                }
            } finally {
                BuiltinTypes.destroyVariant(firstVariant)
            }
        }
    }

    /**
     * Calls [methodBind] with (String, String, Variant) and no return value.
     */
    fun ptrcallWithTwoStringAndVariantArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: String,
        second: String,
        value: Any?,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(8L, 8L)
            val arg1 = arena.allocate(8L, 8L)
            val variant = initVariantCell(arena, value)
            try {
                GodotStrings.initString(arg0, first)
                GodotStrings.initString(arg1, second)
                val arr = arena.allocate(ADDRESS, 3)
                arr.setAtIndex(ADDRESS, 0, arg0)
                arr.setAtIndex(ADDRESS, 1, arg1)
                arr.setAtIndex(ADDRESS, 2, variant)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyVariant(variant)
                GodotStrings.destroyString(arg1)
                GodotStrings.destroyString(arg0)
            }
        }
    }

    /**
     * Calls [methodBind] with (String, String, Variant) and decodes a Variant return.
     */
    fun ptrcallWithTwoStringAndVariantArgRetVariantScalar(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: String,
        second: String,
        value: Any?,
    ): Any? {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(8L, 8L)
            val arg1 = arena.allocate(8L, 8L)
            val variant = initVariantCell(arena, value)
            try {
                GodotStrings.initString(arg0, first)
                GodotStrings.initString(arg1, second)
                val arr = arena.allocate(ADDRESS, 3)
                arr.setAtIndex(ADDRESS, 0, arg0)
                arr.setAtIndex(ADDRESS, 1, arg1)
                arr.setAtIndex(ADDRESS, 2, variant)
                return readVariantReturn(methodBind, instance, arr, arena)
            } finally {
                BuiltinTypes.destroyVariant(variant)
                GodotStrings.destroyString(arg1)
                GodotStrings.destroyString(arg0)
            }
        }
    }

    /**
     * Calls [methodBind] with no arguments and decodes a Variant return as a supported Kotlin value.
     */
    fun ptrcallNoArgsRetVariantScalar(
        methodBind: MemorySegment,
        instance: MemorySegment,
    ): Any? {
        Arena.ofConfined().use { arena ->
            return readVariantReturn(methodBind, instance, MemorySegment.NULL, arena)
        }
    }

    /**
     * Calls [methodBind] with one StringName argument and decodes a Variant return.
     */
    fun ptrcallWithStringNameArgRetVariantScalar(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
    ): Any? {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(name))
            return readVariantReturn(methodBind, instance, arr, arena)
        }
    }

    fun ptrcallWithStringNamePackedStringListArgRetVariantScalar(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
        values: List<String>,
    ): Any? {
        Arena.ofConfined().use { arena ->
            val packed = BuiltinTypes.allocatePackedArray(arena)
            try {
                BuiltinTypes.initPackedStringArray(packed, values)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(name))
                arr.setAtIndex(ADDRESS, 1, packed)
                return readVariantReturn(methodBind, instance, arr, arena)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_STRING_ARRAY, packed)
            }
        }
    }

    fun ptrcallWithStringNameAndStringArgRetVariantScalar(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
        text: String,
    ): Any? {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            GodotStrings.initString(stringCell, text)
            try {
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(name))
                arr.setAtIndex(ADDRESS, 1, stringCell)
                return readVariantReturn(methodBind, instance, arr, arena)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    /**
     * Calls [methodBind] with one String argument and decodes a Variant return.
     */
    fun ptrcallWithStringArgRetVariantScalar(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
    ): Any? {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            GodotStrings.initString(stringCell, text)
            try {
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, stringCell)
                return readVariantReturn(methodBind, instance, arr, arena)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithStringAndIntArgRetVariantScalar(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        value: Int,
    ): Any? {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val intCell = arena.allocate(JAVA_INT)
            GodotStrings.initString(stringCell, text)
            try {
                intCell.set(JAVA_INT, 0, value)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, stringCell)
                arr.setAtIndex(ADDRESS, 1, intCell)
                return readVariantReturn(methodBind, instance, arr, arena)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    /**
     * Calls [methodBind] with (String, bool) args and decodes a Variant return.
     */
    fun ptrcallWithStringAndBoolArgRetVariantScalar(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        boolArg: Boolean,
    ): Any? {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            GodotStrings.initString(stringCell, text)
            try {
                val boolCell = arena.allocate(JAVA_BYTE)
                boolCell.set(JAVA_BYTE, 0, if (boolArg) 1.toByte() else 0.toByte())
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, stringCell)
                arr.setAtIndex(ADDRESS, 1, boolCell)
                return readVariantReturn(methodBind, instance, arr, arena)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    /**
     * Calls [methodBind] with one String argument and Color return value.
     */
    fun ptrcallWithStringArgRetColor(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
    ): Color {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            GodotStrings.initString(stringCell, text)
            try {
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, stringCell)
                val ret = arena.allocate(16L, 4L)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return Color(
                    ret.get(JAVA_FLOAT, 0),
                    ret.get(JAVA_FLOAT, 4),
                    ret.get(JAVA_FLOAT, 8),
                    ret.get(JAVA_FLOAT, 12),
                )
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    /**
     * Calls [methodBind] with one int32 argument and decodes a Variant return.
     */
    fun ptrcallWithIntArgRetVariantScalar(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Int,
    ): Any? {
        Arena.ofConfined().use { arena ->
            val intCell = arena.allocate(JAVA_INT)
            intCell.set(JAVA_INT, 0, value)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, intCell)
            return readVariantReturn(methodBind, instance, arr, arena)
        }
    }

    /**
     * Calls [methodBind] with one int32 argument and decodes an Array return as Kotlin values.
     */
    fun ptrcallWithIntArgRetArray(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Int,
    ): List<Any?> {
        Arena.ofConfined().use { arena ->
            val intCell = arena.allocate(JAVA_INT)
            intCell.set(JAVA_INT, 0, value)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, intCell)
            return callArrayReturn(methodBind, instance, arr, BuiltinTypes::readArrayScalars)
        }
    }

    /**
     * Calls [methodBind] with (int32, Variant) and no return value.
     */
    fun ptrcallWithIntAndVariantArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        index: Int,
        value: Any?,
    ) {
        Arena.ofConfined().use { arena ->
            val intCell = arena.allocate(JAVA_INT)
            val variant = initVariantCell(arena, value)
            try {
                intCell.set(JAVA_INT, 0, index)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, intCell)
                arr.setAtIndex(ADDRESS, 1, variant)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyVariant(variant)
            }
        }
    }

    fun ptrcallWithIntAndVariantArgRetRect2(
        methodBind: MemorySegment,
        instance: MemorySegment,
        index: Int,
        value: Any?,
    ): Rect2 {
        Arena.ofConfined().use { arena ->
            val intCell = arena.allocate(JAVA_INT)
            val variant = initVariantCell(arena, value)
            try {
                intCell.set(JAVA_INT, 0, index)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, intCell)
                arr.setAtIndex(ADDRESS, 1, variant)
                val ret = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return readRect2(ret)
            } finally {
                BuiltinTypes.destroyVariant(variant)
            }
        }
    }

    fun ptrcallWithIntAndArrayArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        index: Int,
        values: List<Any?>,
    ) {
        Arena.ofConfined().use { arena ->
            val intCell = arena.allocate(JAVA_INT)
            val array = arena.allocate(8L, 8L)
            try {
                intCell.set(JAVA_INT, 0, index)
                BuiltinTypes.initArray(array, values)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, intCell)
                arr.setAtIndex(ADDRESS, 1, array)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, array)
            }
        }
    }

    fun ptrcallWithIntAndArrayArgRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        index: Int,
        values: List<Any?>,
    ): Long {
        Arena.ofConfined().use { arena ->
            val intCell = arena.allocate(JAVA_INT)
            val array = arena.allocate(8L, 8L)
            try {
                intCell.set(JAVA_INT, 0, index)
                BuiltinTypes.initArray(array, values)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, intCell)
                arr.setAtIndex(ADDRESS, 1, array)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, array)
            }
        }
    }

    /**
     * Calls [methodBind] with (int32, int32, Variant) and no return value.
     */
    fun ptrcallWithTwoIntAndVariantArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Int,
        value: Any?,
    ) {
        Arena.ofConfined().use { arena ->
            val int0 = arena.allocate(JAVA_INT)
            val int1 = arena.allocate(JAVA_INT)
            val variant = initVariantCell(arena, value)
            try {
                int0.set(JAVA_INT, 0, first)
                int1.set(JAVA_INT, 0, second)
                val arr = arena.allocate(ADDRESS, 3)
                arr.setAtIndex(ADDRESS, 0, int0)
                arr.setAtIndex(ADDRESS, 1, int1)
                arr.setAtIndex(ADDRESS, 2, variant)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyVariant(variant)
            }
        }
    }

    /**
     * Calls [methodBind] with (int32, int32, Object*) and no return value.
     */
    fun ptrcallWithTwoIntAndObjectArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Int,
        objectArg: MemorySegment,
    ) {
        Arena.ofConfined().use { arena ->
            val int0 = arena.allocate(JAVA_INT)
            val int1 = arena.allocate(JAVA_INT)
            val objCell = arena.allocate(ADDRESS)
            int0.set(JAVA_INT, 0, first)
            int1.set(JAVA_INT, 0, second)
            objCell.set(ADDRESS, 0, objectArg)
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, int0)
            arr.setAtIndex(ADDRESS, 1, int1)
            arr.setAtIndex(ADDRESS, 2, objCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (int32, int32, PackedByteArray) and no return value.
     */
    fun ptrcallWithTwoIntAndByteArrayArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Int,
        bytes: ByteArray,
    ) {
        Arena.ofConfined().use { arena ->
            val int0 = arena.allocate(JAVA_INT)
            val int1 = arena.allocate(JAVA_INT)
            val byteArray = BuiltinTypes.allocatePackedArray(arena)
            try {
                int0.set(JAVA_INT, 0, first)
                int1.set(JAVA_INT, 0, second)
                BuiltinTypes.initPackedByteArray(byteArray, bytes)
                val arr = arena.allocate(ADDRESS, 3)
                arr.setAtIndex(ADDRESS, 0, int0)
                arr.setAtIndex(ADDRESS, 1, int1)
                arr.setAtIndex(ADDRESS, 2, byteArray)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, byteArray)
            }
        }
    }

    fun ptrcallWithTwoIntAndPackedVector2ListArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Int,
        values: List<Vector2>,
    ) {
        Arena.ofConfined().use { arena ->
            val int0 = arena.allocate(JAVA_INT)
            val int1 = arena.allocate(JAVA_INT)
            val packed = BuiltinTypes.allocatePackedArray(arena)
            try {
                int0.set(JAVA_INT, 0, first)
                int1.set(JAVA_INT, 0, second)
                BuiltinTypes.initPackedVector2Array(packed, values)
                val arr = arena.allocate(ADDRESS, 3)
                arr.setAtIndex(ADDRESS, 0, int0)
                arr.setAtIndex(ADDRESS, 1, int1)
                arr.setAtIndex(ADDRESS, 2, packed)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR2_ARRAY, packed)
            }
        }
    }

    fun ptrcallWithTwoIntArgsRetPackedVector2List(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Int,
    ): List<Vector2> {
        Arena.ofConfined().use { arena ->
            val int0 = arena.allocate(JAVA_INT)
            val int1 = arena.allocate(JAVA_INT)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            int0.set(JAVA_INT, 0, first)
            int1.set(JAVA_INT, 0, second)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, int0)
            arr.setAtIndex(ADDRESS, 1, int1)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return try {
                BuiltinTypes.readPackedVector2Array(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR2_ARRAY, ret)
            }
        }
    }

    fun ptrcallWithIntByteArrayIntArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        bytes: ByteArray,
        second: Int,
    ) {
        Arena.ofConfined().use { arena ->
            val int0 = arena.allocate(JAVA_INT)
            val byteArray = BuiltinTypes.allocatePackedArray(arena)
            val int1 = arena.allocate(JAVA_INT)
            try {
                int0.set(JAVA_INT, 0, first)
                BuiltinTypes.initPackedByteArray(byteArray, bytes)
                int1.set(JAVA_INT, 0, second)
                val arr = arena.allocate(ADDRESS, 3)
                arr.setAtIndex(ADDRESS, 0, int0)
                arr.setAtIndex(ADDRESS, 1, byteArray)
                arr.setAtIndex(ADDRESS, 2, int1)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, byteArray)
            }
        }
    }

    fun ptrcallWithIntByteArrayIntArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        bytes: ByteArray,
        second: Int,
    ): Long {
        Arena.ofConfined().use { arena ->
            val int0 = arena.allocate(JAVA_INT)
            val byteArray = BuiltinTypes.allocatePackedArray(arena)
            val int1 = arena.allocate(JAVA_INT)
            try {
                int0.set(JAVA_INT, 0, first)
                BuiltinTypes.initPackedByteArray(byteArray, bytes)
                int1.set(JAVA_INT, 0, second)
                val arr = arena.allocate(ADDRESS, 3)
                arr.setAtIndex(ADDRESS, 0, int0)
                arr.setAtIndex(ADDRESS, 1, byteArray)
                arr.setAtIndex(ADDRESS, 2, int1)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, byteArray)
            }
        }
    }

    /**
     * Calls [methodBind] with (String, Variant) and no return value.
     */
    fun ptrcallWithStringAndVariantArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        value: Any?,
    ) {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val variant = initVariantCell(arena, value)
            try {
                GodotStrings.initString(stringCell, text)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, stringCell)
                arr.setAtIndex(ADDRESS, 1, variant)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyVariant(variant)
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithStringAndVariantArgRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        value: Any?,
    ): Int {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val variant = initVariantCell(arena, value)
            try {
                GodotStrings.initString(stringCell, text)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, stringCell)
                arr.setAtIndex(ADDRESS, 1, variant)
                val ret = arena.allocate(JAVA_INT)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_INT, 0)
            } finally {
                BuiltinTypes.destroyVariant(variant)
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithStringAndVariantArgRetVariantScalar(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        value: Any?,
    ): Any? {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val variant = initVariantCell(arena, value)
            try {
                GodotStrings.initString(stringCell, text)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, stringCell)
                arr.setAtIndex(ADDRESS, 1, variant)
                return readVariantReturn(methodBind, instance, arr, arena)
            } finally {
                BuiltinTypes.destroyVariant(variant)
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithStringAndVariantArgRetDictionary(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        value: Any?,
    ): Map<String, Any?> {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val variant = initVariantCell(arena, value)
            try {
                GodotStrings.initString(stringCell, text)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, stringCell)
                arr.setAtIndex(ADDRESS, 1, variant)
                val ret = arena.allocate(8L, 8L)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return try {
                    BuiltinTypes.readDictionaryScalars(ret)
                } finally {
                    BuiltinTypes.destroyTyped(VariantType.DICTIONARY, ret)
                }
            } finally {
                BuiltinTypes.destroyVariant(variant)
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithStringAndTwoVariantArgsRetDictionary(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        firstValue: Any?,
        secondValue: Any?,
    ): Map<String, Any?> {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val firstVariant = initVariantCell(arena, firstValue)
            try {
                GodotStrings.initString(stringCell, text)
                val secondVariant = initVariantCell(arena, secondValue)
                try {
                    val arr = arena.allocate(ADDRESS, 3)
                    arr.setAtIndex(ADDRESS, 0, stringCell)
                    arr.setAtIndex(ADDRESS, 1, firstVariant)
                    arr.setAtIndex(ADDRESS, 2, secondVariant)
                    val ret = arena.allocate(8L, 8L)
                    objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                    return try {
                        BuiltinTypes.readDictionaryScalars(ret)
                    } finally {
                        BuiltinTypes.destroyTyped(VariantType.DICTIONARY, ret)
                    }
                } finally {
                    BuiltinTypes.destroyVariant(secondVariant)
                }
            } finally {
                BuiltinTypes.destroyVariant(firstVariant)
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithStringIntAndVariantArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        index: Int,
        value: Any?,
    ) {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val intCell = arena.allocate(JAVA_INT)
            val variant = initVariantCell(arena, value)
            try {
                GodotStrings.initString(stringCell, text)
                intCell.set(JAVA_INT, 0, index)
                val arr = arena.allocate(ADDRESS, 3)
                arr.setAtIndex(ADDRESS, 0, stringCell)
                arr.setAtIndex(ADDRESS, 1, intCell)
                arr.setAtIndex(ADDRESS, 2, variant)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyVariant(variant)
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithIntStringVariantArgsRetDictionary(
        methodBind: MemorySegment,
        instance: MemorySegment,
        intValue: Int,
        text: String,
        value: Any?,
    ): Map<String, Any?> {
        Arena.ofConfined().use { arena ->
            val intCell = arena.allocate(JAVA_INT)
            val stringCell = arena.allocate(8L, 8L)
            val variant = initVariantCell(arena, value)
            try {
                intCell.set(JAVA_INT, 0, intValue)
                GodotStrings.initString(stringCell, text)
                val arr = arena.allocate(ADDRESS, 3)
                arr.setAtIndex(ADDRESS, 0, intCell)
                arr.setAtIndex(ADDRESS, 1, stringCell)
                arr.setAtIndex(ADDRESS, 2, variant)
                val ret = arena.allocate(8L, 8L)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return try {
                    BuiltinTypes.readDictionaryScalars(ret)
                } finally {
                    BuiltinTypes.destroyTyped(VariantType.DICTIONARY, ret)
                }
            } finally {
                BuiltinTypes.destroyVariant(variant)
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    /**
     * Calls [methodBind] with (Object*, StringName, Variant) and no return value.
     */
    fun ptrcallWithObjectStringNameAndVariantArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        name: String,
        value: Any?,
    ) {
        Arena.ofConfined().use { arena ->
            val objCell = arena.allocate(ADDRESS)
            val variant = initVariantCell(arena, value)
            try {
                objCell.set(ADDRESS, 0, objectArg)
                val arr = arena.allocate(ADDRESS, 3)
                arr.setAtIndex(ADDRESS, 0, objCell)
                arr.setAtIndex(ADDRESS, 1, GodotStrings.makeStringName(name))
                arr.setAtIndex(ADDRESS, 2, variant)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyVariant(variant)
            }
        }
    }

    fun ptrcallWithStringObjectStringNameVariantArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        objectArg: MemorySegment,
        name: String,
        value: Any?,
    ) {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val objectCell = arena.allocate(ADDRESS)
            val variant = initVariantCell(arena, value)
            try {
                GodotStrings.initString(stringCell, text)
                objectCell.set(ADDRESS, 0, objectArg)
                val args = arena.allocate(ADDRESS, 4)
                args.setAtIndex(ADDRESS, 0, stringCell)
                args.setAtIndex(ADDRESS, 1, objectCell)
                args.setAtIndex(ADDRESS, 2, GodotStrings.makeStringName(name))
                args.setAtIndex(ADDRESS, 3, variant)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyVariant(variant)
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithTwoObjectStringNameVariantArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstObject: MemorySegment,
        secondObject: MemorySegment,
        name: String,
        value: Any?,
    ) {
        Arena.ofConfined().use { arena ->
            val object0 = arena.allocate(ADDRESS)
            val object1 = arena.allocate(ADDRESS)
            val variant = initVariantCell(arena, value)
            try {
                object0.set(ADDRESS, 0, firstObject)
                object1.set(ADDRESS, 0, secondObject)
                val args = arena.allocate(ADDRESS, 4)
                args.setAtIndex(ADDRESS, 0, object0)
                args.setAtIndex(ADDRESS, 1, object1)
                args.setAtIndex(ADDRESS, 2, GodotStrings.makeStringName(name))
                args.setAtIndex(ADDRESS, 3, variant)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyVariant(variant)
            }
        }
    }

    fun ptrcallWithObjectAndVariantArgRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        value: Any?,
    ): Long {
        Arena.ofConfined().use { arena ->
            val objectCell = arena.allocate(ADDRESS)
            val variant = initVariantCell(arena, value)
            try {
                objectCell.set(ADDRESS, 0, objectArg)
                val args = arena.allocate(ADDRESS, 2)
                args.setAtIndex(ADDRESS, 0, objectCell)
                args.setAtIndex(ADDRESS, 1, variant)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                BuiltinTypes.destroyVariant(variant)
            }
        }
    }

    fun ptrcallWithObjectStringNameArgRetVariantScalar(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        name: String,
    ): Any? {
        Arena.ofConfined().use { arena ->
            val objCell = arena.allocate(ADDRESS)
            objCell.set(ADDRESS, 0, objectArg)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, objCell)
            arr.setAtIndex(ADDRESS, 1, GodotStrings.makeStringName(name))
            return readVariantReturn(methodBind, instance, arr, arena)
        }
    }

    fun ptrcallWithObjectStringNameAndVariantArgRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        name: String,
        value: Any?,
    ): Long {
        Arena.ofConfined().use { arena ->
            val objCell = arena.allocate(ADDRESS)
            val variant = initVariantCell(arena, value)
            try {
                objCell.set(ADDRESS, 0, objectArg)
                val arr = arena.allocate(ADDRESS, 3)
                arr.setAtIndex(ADDRESS, 0, objCell)
                arr.setAtIndex(ADDRESS, 1, GodotStrings.makeStringName(name))
                arr.setAtIndex(ADDRESS, 2, variant)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                BuiltinTypes.destroyVariant(variant)
            }
        }
    }

    /**
     * Calls [methodBind] with one Object* arg and decodes PackedStringArray return.
     */
    fun ptrcallWithObjectArgRetPackedStringList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
    ): List<String> {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 1)
            val objCell = arena.allocate(ADDRESS)
            objCell.set(ADDRESS, 0, objectArg)
            arr.setAtIndex(ADDRESS, 0, objCell)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return try {
                BuiltinTypes.readPackedStringArray(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_STRING_ARRAY, ret)
            }
        }
    }

    /**
     * Calls [methodBind] with one Object* arg and decodes PackedByteArray return.
     */
    fun ptrcallWithObjectArgRetByteArray(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
    ): ByteArray {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 1)
            val objCell = arena.allocate(ADDRESS)
            objCell.set(ADDRESS, 0, objectArg)
            arr.setAtIndex(ADDRESS, 0, objCell)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return try {
                BuiltinTypes.readPackedByteArray(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, ret)
            }
        }
    }

    /**
     * Calls [methodBind] with (Object*, String, int64) args and int64 return value.
     */
    fun ptrcallWithObjectStringLongArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        text: String,
        value: Long,
    ): Long {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 3)
            val objCell = arena.allocate(ADDRESS)
            objCell.set(ADDRESS, 0, objectArg)
            arr.setAtIndex(ADDRESS, 0, objCell)

            val stringArg = arena.allocate(8L, 8L)
            val longArg = arena.allocate(JAVA_LONG)
            try {
                GodotStrings.initString(stringArg, text)
                longArg.set(JAVA_LONG, 0, value)
                arr.setAtIndex(ADDRESS, 1, stringArg)
                arr.setAtIndex(ADDRESS, 2, longArg)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                GodotStrings.destroyString(stringArg)
            }
        }
    }

    /**
     * Calls [methodBind] with no arguments and bool return value.
     */
    fun ptrcallNoArgsRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
    ): Boolean {
        val ret = ptrcallScratch.get().byteRet
        objectMethodBindPtrcall.invoke(methodBind, instance, MemorySegment.NULL, ret)
        return ret.get(JAVA_BYTE, 0) != 0.toByte()
    }

    /**
     * Calls [methodBind] with no arguments and int64 return value.
     */
    fun ptrcallNoArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
    ): Long {
        val ret = ptrcallScratch.get().longRet
        objectMethodBindPtrcall.invoke(methodBind, instance, MemorySegment.NULL, ret)
        return ret.get(JAVA_LONG, 0)
    }

    /**
     * Calls [methodBind] with no arguments and uint32 return value.
     */
    fun ptrcallNoArgsRetUInt32(
        methodBind: MemorySegment,
        instance: MemorySegment,
    ): Long {
        val ret = ptrcallScratch.get().intRet
        objectMethodBindPtrcall.invoke(methodBind, instance, MemorySegment.NULL, ret)
        return ret.get(JAVA_INT, 0).toLong() and 0xffff_ffffL
    }

    /**
     * Calls [methodBind] with no arguments and RID return value.
     */
    fun ptrcallNoArgsRetRID(
        methodBind: MemorySegment,
        instance: MemorySegment,
    ): RID {
        val ret = ptrcallScratch.get().longRet
        objectMethodBindPtrcall.invoke(methodBind, instance, MemorySegment.NULL, ret)
        return RID(ret.get(JAVA_LONG, 0))
    }

    /**
     * Calls [methodBind] with no arguments and int32 return value.
     */
    fun ptrcallNoArgsRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
    ): Int {
        val ret = ptrcallScratch.get().intRet
        objectMethodBindPtrcall.invoke(methodBind, instance, MemorySegment.NULL, ret)
        return ret.get(JAVA_INT, 0)
    }

    fun ptrcallNoArgsRetNodePath(
        methodBind: MemorySegment,
        instance: MemorySegment,
    ): NodePath {
        Arena.ofConfined().use { arena ->
            val ret = arena.allocate(8L, 8L)
            objectMethodBindPtrcall.invoke(methodBind, instance, MemorySegment.NULL, ret)
            return try {
                BuiltinTypes.readTypedNodePath(ret, arena)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.NODE_PATH, ret)
            }
        }
    }

    /**
     * Calls [methodBind] with no arguments and no return value.
     */
    fun ptrcallNoArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
    ) {
        objectMethodBindPtrcall.invoke(methodBind, instance, MemorySegment.NULL, MemorySegment.NULL)
    }

    /**
     * Calls [methodBind] with no arguments and float(double) return value.
     */
    fun ptrcallNoArgsRetDouble(
        methodBind: MemorySegment,
        instance: MemorySegment,
    ): Double {
        val ret = ptrcallScratch.get().doubleRet
        objectMethodBindPtrcall.invoke(methodBind, instance, MemorySegment.NULL, ret)
        return ret.get(JAVA_DOUBLE, 0)
    }

    /**
     * Calls [methodBind] with one RID arg and no return value.
     */
    fun ptrcallWithRIDArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: RID,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, value.value)
            val args = arena.allocate(ADDRESS, 1)
            args.setAtIndex(ADDRESS, 0, ridCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDArgRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: RID,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, value.value)
            val args = arena.allocate(ADDRESS, 1)
            args.setAtIndex(ADDRESS, 0, ridCell)
            val ret = arena.allocate(JAVA_BYTE)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(JAVA_BYTE, 0) != 0.toByte()
        }
    }

    fun ptrcallWithRIDArgRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: RID,
    ): Long {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, value.value)
            val args = arena.allocate(ADDRESS, 1)
            args.setAtIndex(ADDRESS, 0, ridCell)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(JAVA_LONG, 0)
        }
    }

    fun ptrcallWithRIDArgRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: RID,
    ): Int {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, value.value)
            val args = arena.allocate(ADDRESS, 1)
            args.setAtIndex(ADDRESS, 0, ridCell)
            val ret = arena.allocate(JAVA_INT)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(JAVA_INT, 0)
        }
    }

    fun ptrcallWithRIDArgRetUInt32(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: RID,
    ): Long {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, value.value)
            val args = arena.allocate(ADDRESS, 1)
            args.setAtIndex(ADDRESS, 0, ridCell)
            val ret = arena.allocate(JAVA_INT)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(JAVA_INT, 0).toLong() and 0xffff_ffffL
        }
    }

    fun ptrcallWithRIDArgRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: RID,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, value.value)
            val args = arena.allocate(ADDRESS, 1)
            args.setAtIndex(ADDRESS, 0, ridCell)
            val ret = arena.allocate(ADDRESS)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(ADDRESS, 0)
        }
    }

    fun ptrcallWithRIDArgRetDouble(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: RID,
    ): Double {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, value.value)
            val args = arena.allocate(ADDRESS, 1)
            args.setAtIndex(ADDRESS, 0, ridCell)
            val ret = arena.allocate(JAVA_DOUBLE)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(JAVA_DOUBLE, 0)
        }
    }

    fun ptrcallWithRIDArgRetVariantScalar(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: RID,
    ): Any? {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, value.value)
            val args = arena.allocate(ADDRESS, 1)
            args.setAtIndex(ADDRESS, 0, ridCell)
            return readVariantReturn(methodBind, instance, args, arena)
        }
    }

    fun ptrcallWithRIDArgRetDictionaryList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: RID,
    ): List<Map<String, Any?>> {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, value.value)
            val args = arena.allocate(ADDRESS, 1)
            args.setAtIndex(ADDRESS, 0, ridCell)
            return callArrayReturn(methodBind, instance, args, BuiltinTypes::readArrayDictionaries)
        }
    }

    fun ptrcallWithRIDArgRetRID(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: RID,
    ): RID {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, value.value)
            val args = arena.allocate(ADDRESS, 1)
            args.setAtIndex(ADDRESS, 0, ridCell)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return RID(ret.get(JAVA_LONG, 0))
        }
    }

    fun ptrcallWithRIDArgRetString(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: RID,
    ): String {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, value.value)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, ridCell)
            val retString = arena.allocate(8L, 8L)
            try {
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, retString)
                return GodotStrings.readString(retString)
            } finally {
                GodotStrings.destroyString(retString)
            }
        }
    }

    fun ptrcallWithRIDArgRetVector2(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: RID,
    ): Vector2 {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, value.value)
            val args = arena.allocate(ADDRESS, 1)
            args.setAtIndex(ADDRESS, 0, ridCell)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return Vector2(
                x = GodotReal.readIndex(ret, 0),
                y = GodotReal.readIndex(ret, 1),
            )
        }
    }

    fun ptrcallWithRIDArgRetVector3(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: RID,
    ): Vector3 {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, value.value)
            val args = arena.allocate(ADDRESS, 1)
            args.setAtIndex(ADDRESS, 0, ridCell)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return Vector3(
                x = GodotReal.readIndex(ret, 0),
                y = GodotReal.readIndex(ret, 1),
                z = GodotReal.readIndex(ret, 2),
            )
        }
    }

    fun ptrcallWithRIDArgRetVector2i(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: RID,
    ): Vector2i {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, value.value)
            val args = arena.allocate(ADDRESS, 1)
            args.setAtIndex(ADDRESS, 0, ridCell)
            val ret = arena.allocate(8L, 4L)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return Vector2i(
                x = ret.get(JAVA_INT, 0),
                y = ret.get(JAVA_INT, 4),
            )
        }
    }

    fun ptrcallWithRIDArgRetRIDList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: RID,
    ): List<RID> {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, value.value)
            val args = arena.allocate(ADDRESS, 1)
            args.setAtIndex(ADDRESS, 0, ridCell)
            val ret = arena.allocate(8L, 8L)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return try {
                BuiltinTypes.readArrayRids(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, ret)
            }
        }
    }

    fun ptrcallWithRIDArgRetVector2iList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: RID,
    ): List<Vector2i> {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, value.value)
            val args = arena.allocate(ADDRESS, 1)
            args.setAtIndex(ADDRESS, 0, ridCell)
            return callArrayReturn(methodBind, instance, args, BuiltinTypes::readArrayVector2i)
        }
    }

    fun ptrcallWithRIDArgRetObjectList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: RID,
    ): List<GodotObject> {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, value.value)
            val args = arena.allocate(ADDRESS, 1)
            args.setAtIndex(ADDRESS, 0, ridCell)
            return callArrayReturn(methodBind, instance, args, BuiltinTypes::readArrayObjects)
        }
    }

    fun <T : Any> ptrcallWithRIDArgRetTypedObjectList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: RID,
        wrapper: (MemorySegment) -> T?,
    ): List<T> =
        ptrcallWithRIDArgRetObjectList(methodBind, instance, value).mapNotNull { wrapper(it.handle) }

    fun ptrcallWithRIDArgRetPackedInt32List(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: RID,
    ): List<Int> {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, value.value)
            val args = arena.allocate(ADDRESS, 1)
            args.setAtIndex(ADDRESS, 0, ridCell)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return try {
                BuiltinTypes.readPackedInt32Array(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_INT32_ARRAY, ret)
            }
        }
    }

    fun ptrcallWithRIDArgRetAABB(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: RID,
    ): AABB {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, value.value)
            val args = arena.allocate(ADDRESS, 1)
            args.setAtIndex(ADDRESS, 0, ridCell)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 6, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return readAABB(ret)
        }
    }

    fun ptrcallWithRIDArgRetTransform2D(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: RID,
    ): Transform2D {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, value.value)
            val args = arena.allocate(ADDRESS, 1)
            args.setAtIndex(ADDRESS, 0, ridCell)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 6, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return readTransform2D(ret)
        }
    }

    fun ptrcallWithRIDArgRetTransform3D(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: RID,
    ): Transform3D {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, value.value)
            val args = arena.allocate(ADDRESS, 1)
            args.setAtIndex(ADDRESS, 0, ridCell)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 12, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return readTransform3D(ret)
        }
    }

    fun ptrcallWithRIDAndBoolArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            val boolCell = arena.allocate(JAVA_BYTE)
            boolCell.set(JAVA_BYTE, 0, if (value) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, boolCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDBoolRect2Args(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        enabled: Boolean,
        rect: Rect2,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val boolCell = arena.allocate(JAVA_BYTE)
            val rectCell = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
            ridCell.set(JAVA_LONG, 0, rid.value)
            boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
            writeRect2(rectCell, rect)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, boolCell)
            args.setAtIndex(ADDRESS, 2, rectCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDAndLongArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Long,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            val valueCell = arena.allocate(JAVA_LONG)
            valueCell.set(JAVA_LONG, 0, value)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, valueCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDAndUInt32Arg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Long,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            val valueCell = arena.allocate(JAVA_INT)
            valueCell.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(value))
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, valueCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDAndUInt32ArgRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Long,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            val valueCell = arena.allocate(JAVA_INT)
            valueCell.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(value))
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, valueCell)
            val ret = arena.allocate(JAVA_BYTE)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(JAVA_BYTE, 0) != 0.toByte()
        }
    }

    fun ptrcallWithRIDAndUInt32ArgRetString(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Long,
    ): String {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            val valueCell = arena.allocate(JAVA_INT)
            valueCell.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(value))
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, valueCell)
            val retString = arena.allocate(8L, 8L)
            try {
                objectMethodBindPtrcall.invoke(methodBind, instance, args, retString)
                return GodotStrings.readString(retString)
            } finally {
                GodotStrings.destroyString(retString)
            }
        }
    }

    fun ptrcallWithRIDAndUInt32ArgRetTransform3D(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Long,
    ): Transform3D {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            val valueCell = arena.allocate(JAVA_INT)
            valueCell.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(value))
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, valueCell)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 12, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return readTransform3D(ret)
        }
    }

    fun ptrcallWithRIDAndUInt32ArgRetByteArray(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Long,
    ): ByteArray {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            val valueCell = arena.allocate(JAVA_INT)
            valueCell.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(value))
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, valueCell)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return try {
                BuiltinTypes.readPackedByteArray(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, ret)
            }
        }
    }

    fun ptrcallWithRIDAndLongArgRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Long,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val valueCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            valueCell.set(JAVA_LONG, 0, value)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, valueCell)
            val ret = arena.allocate(JAVA_BYTE)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(JAVA_BYTE, 0) != 0.toByte()
        }
    }

    fun ptrcallWithRIDAndVariantArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Any?,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val variant = initVariantCell(arena, value)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                val args = arena.allocate(ADDRESS, 2)
                args.setAtIndex(ADDRESS, 0, ridCell)
                args.setAtIndex(ADDRESS, 1, variant)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyVariant(variant)
            }
        }
    }

    fun ptrcallWithRIDAndVariantArgRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Any?,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val variant = initVariantCell(arena, value)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                val args = arena.allocate(ADDRESS, 2)
                args.setAtIndex(ADDRESS, 0, ridCell)
                args.setAtIndex(ADDRESS, 1, variant)
                val ret = arena.allocate(JAVA_BYTE)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(JAVA_BYTE, 0) != 0.toByte()
            } finally {
                BuiltinTypes.destroyVariant(variant)
            }
        }
    }

    fun ptrcallWithRIDAndVariantArgRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Any?,
    ): Int {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val variant = initVariantCell(arena, value)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                val args = arena.allocate(ADDRESS, 2)
                args.setAtIndex(ADDRESS, 0, ridCell)
                args.setAtIndex(ADDRESS, 1, variant)
                val ret = arena.allocate(JAVA_INT)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(JAVA_INT, 0)
            } finally {
                BuiltinTypes.destroyVariant(variant)
            }
        }
    }

    fun ptrcallWithRIDAndVariantArgRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Any?,
    ): Long {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val variant = initVariantCell(arena, value)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                val args = arena.allocate(ADDRESS, 2)
                args.setAtIndex(ADDRESS, 0, ridCell)
                args.setAtIndex(ADDRESS, 1, variant)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                BuiltinTypes.destroyVariant(variant)
            }
        }
    }

    fun ptrcallWithRIDAndVariantArgRetRect2(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Any?,
    ): Rect2 {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val variant = initVariantCell(arena, value)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                val args = arena.allocate(ADDRESS, 2)
                args.setAtIndex(ADDRESS, 0, ridCell)
                args.setAtIndex(ADDRESS, 1, variant)
                val ret = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return readRect2(ret)
            } finally {
                BuiltinTypes.destroyVariant(variant)
            }
        }
    }

    fun ptrcallWithRIDAndVariantArgRetVector2i(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Any?,
    ): Vector2i {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val variant = initVariantCell(arena, value)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                val args = arena.allocate(ADDRESS, 2)
                args.setAtIndex(ADDRESS, 0, ridCell)
                args.setAtIndex(ADDRESS, 1, variant)
                val ret = arena.allocate(8L, 4L)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return readVector2i(ret)
            } finally {
                BuiltinTypes.destroyVariant(variant)
            }
        }
    }

    fun ptrcallWithRIDVariantVector2LongLongDoubleArgsRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Any?,
        vector: Vector2,
        firstLong: Long,
        secondLong: Long,
        doubleValue: Double,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val variant = initVariantCell(arena, value)
            try {
                val vectorCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
                val long0 = arena.allocate(JAVA_LONG)
                val long1 = arena.allocate(JAVA_LONG)
                val doubleCell = arena.allocate(JAVA_DOUBLE)
                ridCell.set(JAVA_LONG, 0, rid.value)
                writeVector2(vectorCell, vector)
                long0.set(JAVA_LONG, 0, firstLong)
                long1.set(JAVA_LONG, 0, secondLong)
                doubleCell.set(JAVA_DOUBLE, 0, doubleValue)
                val args = arena.allocate(ADDRESS, 6)
                args.setAtIndex(ADDRESS, 0, ridCell)
                args.setAtIndex(ADDRESS, 1, variant)
                args.setAtIndex(ADDRESS, 2, vectorCell)
                args.setAtIndex(ADDRESS, 3, long0)
                args.setAtIndex(ADDRESS, 4, long1)
                args.setAtIndex(ADDRESS, 5, doubleCell)
                val ret = arena.allocate(JAVA_BYTE)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(JAVA_BYTE, 0) != 0.toByte()
            } finally {
                BuiltinTypes.destroyVariant(variant)
            }
        }
    }

    fun ptrcallWithRIDVariantVector2LongDoubleArgsRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Any?,
        vector: Vector2,
        longValue: Long,
        doubleValue: Double,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val variant = initVariantCell(arena, value)
            try {
                val vectorCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
                val longCell = arena.allocate(JAVA_LONG)
                val doubleCell = arena.allocate(JAVA_DOUBLE)
                ridCell.set(JAVA_LONG, 0, rid.value)
                writeVector2(vectorCell, vector)
                longCell.set(JAVA_LONG, 0, longValue)
                doubleCell.set(JAVA_DOUBLE, 0, doubleValue)
                val args = arena.allocate(ADDRESS, 5)
                args.setAtIndex(ADDRESS, 0, ridCell)
                args.setAtIndex(ADDRESS, 1, variant)
                args.setAtIndex(ADDRESS, 2, vectorCell)
                args.setAtIndex(ADDRESS, 3, longCell)
                args.setAtIndex(ADDRESS, 4, doubleCell)
                val ret = arena.allocate(JAVA_BYTE)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(JAVA_BYTE, 0) != 0.toByte()
            } finally {
                BuiltinTypes.destroyVariant(variant)
            }
        }
    }

    fun ptrcallWithRIDLongAndVariantArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        longArg: Long,
        value: Any?,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val longCell = arena.allocate(JAVA_LONG)
            val variant = initVariantCell(arena, value)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                longCell.set(JAVA_LONG, 0, longArg)
                val args = arena.allocate(ADDRESS, 3)
                args.setAtIndex(ADDRESS, 0, ridCell)
                args.setAtIndex(ADDRESS, 1, longCell)
                args.setAtIndex(ADDRESS, 2, variant)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyVariant(variant)
            }
        }
    }

    fun ptrcallWithRIDStringNameAndVariantArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        name: String,
        value: Any?,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val variant = initVariantCell(arena, value)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                val args = arena.allocate(ADDRESS, 3)
                args.setAtIndex(ADDRESS, 0, ridCell)
                args.setAtIndex(ADDRESS, 1, GodotStrings.makeStringName(name))
                args.setAtIndex(ADDRESS, 2, variant)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyVariant(variant)
            }
        }
    }

    fun ptrcallWithRIDAndLongArgRetVector2iList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Long,
    ): List<Vector2i> {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val valueCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            valueCell.set(JAVA_LONG, 0, value)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, valueCell)
            return callArrayReturn(methodBind, instance, args, BuiltinTypes::readArrayVector2i)
        }
    }

    fun ptrcallWithRIDAndIntArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Int,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            val valueCell = arena.allocate(JAVA_INT)
            valueCell.set(JAVA_INT, 0, value)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, valueCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDAndIntArgRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Int,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            val valueCell = arena.allocate(JAVA_INT)
            valueCell.set(JAVA_INT, 0, value)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, valueCell)
            val ret = arena.allocate(JAVA_BYTE)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(JAVA_BYTE, 0) != 0.toByte()
        }
    }

    fun ptrcallWithRIDAndIntArgRetRID(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Int,
    ): RID {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            val valueCell = arena.allocate(JAVA_INT)
            valueCell.set(JAVA_INT, 0, value)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, valueCell)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return RID(ret.get(JAVA_LONG, 0))
        }
    }

    fun ptrcallWithRIDAndIntArgRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Int,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            val valueCell = arena.allocate(JAVA_INT)
            valueCell.set(JAVA_INT, 0, value)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, valueCell)
            val ret = arena.allocate(ADDRESS)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(ADDRESS, 0)
        }
    }

    fun ptrcallWithRIDAndIntArgRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Int,
    ): Int {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val valueCell = arena.allocate(JAVA_INT)
            ridCell.set(JAVA_LONG, 0, rid.value)
            valueCell.set(JAVA_INT, 0, value)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, valueCell)
            val ret = arena.allocate(JAVA_INT)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(JAVA_INT, 0)
        }
    }

    fun ptrcallWithRIDAndIntArgRetVariantScalar(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Int,
    ): Any? {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val valueCell = arena.allocate(JAVA_INT)
            ridCell.set(JAVA_LONG, 0, rid.value)
            valueCell.set(JAVA_INT, 0, value)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, valueCell)
            return readVariantReturn(methodBind, instance, args, arena)
        }
    }

    fun ptrcallWithRIDIntAndVariantArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        index: Int,
        value: Any?,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val indexCell = arena.allocate(JAVA_INT)
            val variant = initVariantCell(arena, value)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                indexCell.set(JAVA_INT, 0, index)
                val args = arena.allocate(ADDRESS, 3)
                args.setAtIndex(ADDRESS, 0, ridCell)
                args.setAtIndex(ADDRESS, 1, indexCell)
                args.setAtIndex(ADDRESS, 2, variant)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyVariant(variant)
            }
        }
    }

    fun ptrcallWithRIDAndIntArgRetDictionary(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Int,
    ): Map<String, Any?> {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val valueCell = arena.allocate(JAVA_INT)
            ridCell.set(JAVA_LONG, 0, rid.value)
            valueCell.set(JAVA_INT, 0, value)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, valueCell)
            val ret = arena.allocate(8L, 8L)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return try {
                BuiltinTypes.readDictionaryScalars(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.DICTIONARY, ret)
            }
        }
    }

    fun ptrcallWithRIDAndIntArgRetTransform2D(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Int,
    ): Transform2D {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val valueCell = arena.allocate(JAVA_INT)
            ridCell.set(JAVA_LONG, 0, rid.value)
            valueCell.set(JAVA_INT, 0, value)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, valueCell)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 6, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return readTransform2D(ret)
        }
    }

    fun ptrcallWithRIDAndIntArgRetTransform3D(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Int,
    ): Transform3D {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val valueCell = arena.allocate(JAVA_INT)
            ridCell.set(JAVA_LONG, 0, rid.value)
            valueCell.set(JAVA_INT, 0, value)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, valueCell)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 12, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return readTransform3D(ret)
        }
    }

    /**
     * Calls [methodBind] with (RID, int32, RID) and no return value.
     */
    fun ptrcallWithRIDIntAndRIDArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Int,
        other: RID,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            val valueCell = arena.allocate(JAVA_INT)
            valueCell.set(JAVA_INT, 0, value)
            val otherCell = arena.allocate(JAVA_LONG)
            otherCell.set(JAVA_LONG, 0, other.value)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, valueCell)
            args.setAtIndex(ADDRESS, 2, otherCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDIntAndTransform2DArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Int,
        transform: Transform2D,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val valueCell = arena.allocate(JAVA_INT)
            val transformCell = arena.allocate(GodotReal.SIZE_BYTES * 6, GodotReal.ALIGN_BYTES)
            ridCell.set(JAVA_LONG, 0, rid.value)
            valueCell.set(JAVA_INT, 0, value)
            writeTransform2D(transformCell, transform)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, valueCell)
            args.setAtIndex(ADDRESS, 2, transformCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDIntAndTransform3DArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Int,
        transform: Transform3D,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val valueCell = arena.allocate(JAVA_INT)
            val transformCell = arena.allocate(GodotReal.SIZE_BYTES * 12, GodotReal.ALIGN_BYTES)
            ridCell.set(JAVA_LONG, 0, rid.value)
            valueCell.set(JAVA_INT, 0, value)
            writeTransform3D(transformCell, transform)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, valueCell)
            args.setAtIndex(ADDRESS, 2, transformCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDIntAndVector3Arg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Int,
        vector: Vector3,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val valueCell = arena.allocate(JAVA_INT)
            val vectorCell = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            ridCell.set(JAVA_LONG, 0, rid.value)
            valueCell.set(JAVA_INT, 0, value)
            GodotReal.writeIndex(vectorCell, 0, vector.x)
            GodotReal.writeIndex(vectorCell, 1, vector.y)
            GodotReal.writeIndex(vectorCell, 2, vector.z)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, valueCell)
            args.setAtIndex(ADDRESS, 2, vectorCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDIntIntAndByteArrayArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        first: Int,
        second: Int,
        bytes: ByteArray,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val int0 = arena.allocate(JAVA_INT)
            val int1 = arena.allocate(JAVA_INT)
            val byteArray = BuiltinTypes.allocatePackedArray(arena)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                int0.set(JAVA_INT, 0, first)
                int1.set(JAVA_INT, 0, second)
                BuiltinTypes.initPackedByteArray(byteArray, bytes)
                val args = arena.allocate(ADDRESS, 4)
                args.setAtIndex(ADDRESS, 0, ridCell)
                args.setAtIndex(ADDRESS, 1, int0)
                args.setAtIndex(ADDRESS, 2, int1)
                args.setAtIndex(ADDRESS, 3, byteArray)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, byteArray)
            }
        }
    }

    fun ptrcallWithRIDAndDoubleArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            val valueCell = arena.allocate(JAVA_DOUBLE)
            valueCell.set(JAVA_DOUBLE, 0, value)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, valueCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDAndDoubleArgRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Double,
    ): Long {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val valueCell = arena.allocate(JAVA_DOUBLE)
            ridCell.set(JAVA_LONG, 0, rid.value)
            valueCell.set(JAVA_DOUBLE, 0, value)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, valueCell)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(JAVA_LONG, 0)
        }
    }

    fun ptrcallWithRIDAndTwoDoubleArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        first: Double,
        second: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            val firstCell = arena.allocate(JAVA_DOUBLE)
            firstCell.set(JAVA_DOUBLE, 0, first)
            val secondCell = arena.allocate(JAVA_DOUBLE)
            secondCell.set(JAVA_DOUBLE, 0, second)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, firstCell)
            args.setAtIndex(ADDRESS, 2, secondCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDAndThreeDoubleArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        first: Double,
        second: Double,
        third: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val firstCell = arena.allocate(JAVA_DOUBLE)
            val secondCell = arena.allocate(JAVA_DOUBLE)
            val thirdCell = arena.allocate(JAVA_DOUBLE)
            ridCell.set(JAVA_LONG, 0, rid.value)
            firstCell.set(JAVA_DOUBLE, 0, first)
            secondCell.set(JAVA_DOUBLE, 0, second)
            thirdCell.set(JAVA_DOUBLE, 0, third)
            val args = arena.allocate(ADDRESS, 4)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, firstCell)
            args.setAtIndex(ADDRESS, 2, secondCell)
            args.setAtIndex(ADDRESS, 3, thirdCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDAndStringArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: String,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val stringCell = arena.allocate(8L, 8L)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                GodotStrings.initString(stringCell, value)
                val args = arena.allocate(ADDRESS, 2)
                args.setAtIndex(ADDRESS, 0, ridCell)
                args.setAtIndex(ADDRESS, 1, stringCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithRIDIntAndStringArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        intArg: Int,
        value: String,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val intCell = arena.allocate(JAVA_INT)
            val stringCell = arena.allocate(8L, 8L)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                intCell.set(JAVA_INT, 0, intArg)
                GodotStrings.initString(stringCell, value)
                val args = arena.allocate(ADDRESS, 3)
                args.setAtIndex(ADDRESS, 0, ridCell)
                args.setAtIndex(ADDRESS, 1, intCell)
                args.setAtIndex(ADDRESS, 2, stringCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithRIDStringRIDVariantIntArgsRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstRid: RID,
        text: String,
        secondRid: RID,
        value: Any?,
        intValue: Int,
    ): Int {
        Arena.ofConfined().use { arena ->
            val firstRidCell = arena.allocate(JAVA_LONG)
            val stringCell = arena.allocate(8L, 8L)
            val secondRidCell = arena.allocate(JAVA_LONG)
            val variant = initVariantCell(arena, value)
            try {
                val intCell = arena.allocate(JAVA_INT)
                firstRidCell.set(JAVA_LONG, 0, firstRid.value)
                GodotStrings.initString(stringCell, text)
                secondRidCell.set(JAVA_LONG, 0, secondRid.value)
                intCell.set(JAVA_INT, 0, intValue)
                val args = arena.allocate(ADDRESS, 5)
                args.setAtIndex(ADDRESS, 0, firstRidCell)
                args.setAtIndex(ADDRESS, 1, stringCell)
                args.setAtIndex(ADDRESS, 2, secondRidCell)
                args.setAtIndex(ADDRESS, 3, variant)
                args.setAtIndex(ADDRESS, 4, intCell)
                val ret = arena.allocate(JAVA_INT)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(JAVA_INT, 0)
            } finally {
                BuiltinTypes.destroyVariant(variant)
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithRIDAndStringArgRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: String,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val stringCell = arena.allocate(8L, 8L)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                GodotStrings.initString(stringCell, value)
                val args = arena.allocate(ADDRESS, 2)
                args.setAtIndex(ADDRESS, 0, ridCell)
                args.setAtIndex(ADDRESS, 1, stringCell)
                val ret = arena.allocate(JAVA_BYTE)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(JAVA_BYTE, 0) != 0.toByte()
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithRIDAndObjectArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        objectArg: MemorySegment,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            val objCell = arena.allocate(ADDRESS)
            objCell.set(ADDRESS, 0, objectArg)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, objCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDAndTwoObjectArgsRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        firstObject: MemorySegment,
        secondObject: MemorySegment,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val firstObjectCell = arena.allocate(ADDRESS)
            val secondObjectCell = arena.allocate(ADDRESS)
            ridCell.set(JAVA_LONG, 0, rid.value)
            firstObjectCell.set(ADDRESS, 0, firstObject)
            secondObjectCell.set(ADDRESS, 0, secondObject)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, firstObjectCell)
            args.setAtIndex(ADDRESS, 2, secondObjectCell)
            val ret = arena.allocate(JAVA_BYTE)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(JAVA_BYTE, 0) != 0.toByte()
        }
    }

    fun ptrcallWithRIDObjectIntArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        objectArg: MemorySegment,
        intValue: Int,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val objCell = arena.allocate(ADDRESS)
            val intCell = arena.allocate(JAVA_INT)
            ridCell.set(JAVA_LONG, 0, rid.value)
            objCell.set(ADDRESS, 0, objectArg)
            intCell.set(JAVA_INT, 0, intValue)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, objCell)
            args.setAtIndex(ADDRESS, 2, intCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDStringAndBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: String,
        enabled: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val stringCell = arena.allocate(8L, 8L)
            val boolCell = arena.allocate(JAVA_BYTE)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                GodotStrings.initString(stringCell, value)
                boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
                val args = arena.allocate(ADDRESS, 3)
                args.setAtIndex(ADDRESS, 0, ridCell)
                args.setAtIndex(ADDRESS, 1, stringCell)
                args.setAtIndex(ADDRESS, 2, boolCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithRIDAndVector3Arg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Vector3,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            val vectorCell = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            GodotReal.writeIndex(vectorCell, 0, value.x)
            GodotReal.writeIndex(vectorCell, 1, value.y)
            GodotReal.writeIndex(vectorCell, 2, value.z)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, vectorCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDAndTwoVector3Args(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        first: Vector3,
        second: Vector3,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val arg0 = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            val arg1 = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            ridCell.set(JAVA_LONG, 0, rid.value)
            GodotReal.writeIndex(arg0, 0, first.x)
            GodotReal.writeIndex(arg0, 1, first.y)
            GodotReal.writeIndex(arg0, 2, first.z)
            GodotReal.writeIndex(arg1, 0, second.x)
            GodotReal.writeIndex(arg1, 1, second.y)
            GodotReal.writeIndex(arg1, 2, second.z)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, arg0)
            args.setAtIndex(ADDRESS, 2, arg1)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDTwoVector3BoolArgsRetVector3(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        first: Vector3,
        second: Vector3,
        enabled: Boolean,
    ): Vector3 {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val arg0 = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            val arg1 = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            val boolCell = arena.allocate(JAVA_BYTE)
            ridCell.set(JAVA_LONG, 0, rid.value)
            GodotReal.writeIndex(arg0, 0, first.x)
            GodotReal.writeIndex(arg0, 1, first.y)
            GodotReal.writeIndex(arg0, 2, first.z)
            GodotReal.writeIndex(arg1, 0, second.x)
            GodotReal.writeIndex(arg1, 1, second.y)
            GodotReal.writeIndex(arg1, 2, second.z)
            boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 4)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, arg0)
            args.setAtIndex(ADDRESS, 2, arg1)
            args.setAtIndex(ADDRESS, 3, boolCell)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return Vector3(
                x = GodotReal.readIndex(ret, 0),
                y = GodotReal.readIndex(ret, 1),
                z = GodotReal.readIndex(ret, 2),
            )
        }
    }

    fun ptrcallWithRIDAndVector3ArgRetVector3(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Vector3,
    ): Vector3 {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val vectorCell = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            ridCell.set(JAVA_LONG, 0, rid.value)
            GodotReal.writeIndex(vectorCell, 0, value.x)
            GodotReal.writeIndex(vectorCell, 1, value.y)
            GodotReal.writeIndex(vectorCell, 2, value.z)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, vectorCell)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return Vector3(
                x = GodotReal.readIndex(ret, 0),
                y = GodotReal.readIndex(ret, 1),
                z = GodotReal.readIndex(ret, 2),
            )
        }
    }

    fun ptrcallWithRIDAndVector3ArgRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Vector3,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val vectorCell = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            ridCell.set(JAVA_LONG, 0, rid.value)
            GodotReal.writeIndex(vectorCell, 0, value.x)
            GodotReal.writeIndex(vectorCell, 1, value.y)
            GodotReal.writeIndex(vectorCell, 2, value.z)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, vectorCell)
            val ret = arena.allocate(JAVA_BYTE)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(JAVA_BYTE, 0) != 0.toByte()
        }
    }

    fun ptrcallWithRIDAndVector3ArgRetRID(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Vector3,
    ): RID {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val vectorCell = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            ridCell.set(JAVA_LONG, 0, rid.value)
            GodotReal.writeIndex(vectorCell, 0, value.x)
            GodotReal.writeIndex(vectorCell, 1, value.y)
            GodotReal.writeIndex(vectorCell, 2, value.z)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, vectorCell)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return RID(ret.get(JAVA_LONG, 0))
        }
    }

    fun ptrcallWithRIDAndVector2Arg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Vector2,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            val vectorCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            GodotReal.writeIndex(vectorCell, 0, value.x)
            GodotReal.writeIndex(vectorCell, 1, value.y)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, vectorCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDAndVector2ArgRetVector2(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Vector2,
    ): Vector2 {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val vectorCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            ridCell.set(JAVA_LONG, 0, rid.value)
            GodotReal.writeIndex(vectorCell, 0, value.x)
            GodotReal.writeIndex(vectorCell, 1, value.y)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, vectorCell)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return Vector2(
                x = GodotReal.readIndex(ret, 0),
                y = GodotReal.readIndex(ret, 1),
            )
        }
    }

    fun ptrcallWithRIDAndVector2ArgRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Vector2,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val vectorCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            ridCell.set(JAVA_LONG, 0, rid.value)
            GodotReal.writeIndex(vectorCell, 0, value.x)
            GodotReal.writeIndex(vectorCell, 1, value.y)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, vectorCell)
            val ret = arena.allocate(JAVA_BYTE)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(JAVA_BYTE, 0) != 0.toByte()
        }
    }

    fun ptrcallWithRIDAndVector2ArgRetRID(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Vector2,
    ): RID {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val vectorCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            ridCell.set(JAVA_LONG, 0, rid.value)
            GodotReal.writeIndex(vectorCell, 0, value.x)
            GodotReal.writeIndex(vectorCell, 1, value.y)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, vectorCell)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return RID(ret.get(JAVA_LONG, 0))
        }
    }

    fun ptrcallWithRIDAndTwoVector2Args(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        first: Vector2,
        second: Vector2,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val arg0 = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val arg1 = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            ridCell.set(JAVA_LONG, 0, rid.value)
            GodotReal.writeIndex(arg0, 0, first.x)
            GodotReal.writeIndex(arg0, 1, first.y)
            GodotReal.writeIndex(arg1, 0, second.x)
            GodotReal.writeIndex(arg1, 1, second.y)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, arg0)
            args.setAtIndex(ADDRESS, 2, arg1)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDVector2TwoRIDArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        anchor: Vector2,
        firstBody: RID,
        secondBody: RID,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val anchorCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val firstBodyCell = arena.allocate(JAVA_LONG)
            val secondBodyCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            writeVector2(anchorCell, anchor)
            firstBodyCell.set(JAVA_LONG, 0, firstBody.value)
            secondBodyCell.set(JAVA_LONG, 0, secondBody.value)
            val args = arena.allocate(ADDRESS, 4)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, anchorCell)
            args.setAtIndex(ADDRESS, 2, firstBodyCell)
            args.setAtIndex(ADDRESS, 3, secondBodyCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithTwoRIDThreeUInt32ArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstRid: RID,
        secondRid: RID,
        first: Long,
        second: Long,
        third: Long,
    ): Long {
        Arena.ofConfined().use { arena ->
            val rid0 = arena.allocate(JAVA_LONG)
            val rid1 = arena.allocate(JAVA_LONG)
            val uint0 = arena.allocate(JAVA_INT)
            val uint1 = arena.allocate(JAVA_INT)
            val uint2 = arena.allocate(JAVA_INT)
            rid0.set(JAVA_LONG, 0, firstRid.value)
            rid1.set(JAVA_LONG, 0, secondRid.value)
            uint0.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(first))
            uint1.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(second))
            uint2.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(third))
            val args = arena.allocate(ADDRESS, 5)
            args.setAtIndex(ADDRESS, 0, rid0)
            args.setAtIndex(ADDRESS, 1, rid1)
            args.setAtIndex(ADDRESS, 2, uint0)
            args.setAtIndex(ADDRESS, 3, uint1)
            args.setAtIndex(ADDRESS, 4, uint2)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(JAVA_LONG, 0)
        }
    }

    fun ptrcallWithTwoRIDThreeVector3FourUInt32ArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstRid: RID,
        secondRid: RID,
        firstVector: Vector3,
        secondVector: Vector3,
        thirdVector: Vector3,
        firstUInt: Long,
        secondUInt: Long,
        thirdUInt: Long,
        fourthUInt: Long,
    ): Long {
        Arena.ofConfined().use { arena ->
            val rid0 = arena.allocate(JAVA_LONG)
            val rid1 = arena.allocate(JAVA_LONG)
            val vector0 = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            val vector1 = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            val vector2 = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            val uint0 = arena.allocate(JAVA_INT)
            val uint1 = arena.allocate(JAVA_INT)
            val uint2 = arena.allocate(JAVA_INT)
            val uint3 = arena.allocate(JAVA_INT)
            rid0.set(JAVA_LONG, 0, firstRid.value)
            rid1.set(JAVA_LONG, 0, secondRid.value)
            writeVector3(vector0, firstVector)
            writeVector3(vector1, secondVector)
            writeVector3(vector2, thirdVector)
            uint0.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(firstUInt))
            uint1.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(secondUInt))
            uint2.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(thirdUInt))
            uint3.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(fourthUInt))
            val args = arena.allocate(ADDRESS, 9)
            args.setAtIndex(ADDRESS, 0, rid0)
            args.setAtIndex(ADDRESS, 1, rid1)
            args.setAtIndex(ADDRESS, 2, vector0)
            args.setAtIndex(ADDRESS, 3, vector1)
            args.setAtIndex(ADDRESS, 4, vector2)
            args.setAtIndex(ADDRESS, 5, uint0)
            args.setAtIndex(ADDRESS, 6, uint1)
            args.setAtIndex(ADDRESS, 7, uint2)
            args.setAtIndex(ADDRESS, 8, uint3)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(JAVA_LONG, 0)
        }
    }

    fun ptrcallWithLongBoolRIDThreeUInt32Args(
        methodBind: MemorySegment,
        instance: MemorySegment,
        longValue: Long,
        enabled: Boolean,
        rid: RID,
        first: Long,
        second: Long,
        third: Long,
    ) {
        Arena.ofConfined().use { arena ->
            val longCell = arena.allocate(JAVA_LONG)
            val boolCell = arena.allocate(JAVA_BYTE)
            val ridCell = arena.allocate(JAVA_LONG)
            val uint0 = arena.allocate(JAVA_INT)
            val uint1 = arena.allocate(JAVA_INT)
            val uint2 = arena.allocate(JAVA_INT)
            longCell.set(JAVA_LONG, 0, longValue)
            boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
            ridCell.set(JAVA_LONG, 0, rid.value)
            uint0.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(first))
            uint1.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(second))
            uint2.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(third))
            val args = arena.allocate(ADDRESS, 6)
            args.setAtIndex(ADDRESS, 0, longCell)
            args.setAtIndex(ADDRESS, 1, boolCell)
            args.setAtIndex(ADDRESS, 2, ridCell)
            args.setAtIndex(ADDRESS, 3, uint0)
            args.setAtIndex(ADDRESS, 4, uint1)
            args.setAtIndex(ADDRESS, 5, uint2)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithTwoRIDVector3RIDVector3Args(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstRid: RID,
        secondRid: RID,
        firstVector: Vector3,
        thirdRid: RID,
        secondVector: Vector3,
    ) {
        Arena.ofConfined().use { arena ->
            val rid0 = arena.allocate(JAVA_LONG)
            val rid1 = arena.allocate(JAVA_LONG)
            val vector0 = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            val rid2 = arena.allocate(JAVA_LONG)
            val vector1 = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            rid0.set(JAVA_LONG, 0, firstRid.value)
            rid1.set(JAVA_LONG, 0, secondRid.value)
            writeVector3(vector0, firstVector)
            rid2.set(JAVA_LONG, 0, thirdRid.value)
            writeVector3(vector1, secondVector)
            val args = arena.allocate(ADDRESS, 5)
            args.setAtIndex(ADDRESS, 0, rid0)
            args.setAtIndex(ADDRESS, 1, rid1)
            args.setAtIndex(ADDRESS, 2, vector0)
            args.setAtIndex(ADDRESS, 3, rid2)
            args.setAtIndex(ADDRESS, 4, vector1)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithTwoRIDLongVector2LongColorDoubleArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstRid: RID,
        secondRid: RID,
        firstLong: Long,
        vector: Vector2,
        secondLong: Long,
        color: Color,
        doubleValue: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val rid0 = arena.allocate(JAVA_LONG)
            val rid1 = arena.allocate(JAVA_LONG)
            val long0 = arena.allocate(JAVA_LONG)
            val vectorCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val long1 = arena.allocate(JAVA_LONG)
            val colorCell = arena.allocate(16L, 4L)
            val doubleCell = arena.allocate(JAVA_DOUBLE)
            rid0.set(JAVA_LONG, 0, firstRid.value)
            rid1.set(JAVA_LONG, 0, secondRid.value)
            long0.set(JAVA_LONG, 0, firstLong)
            writeVector2(vectorCell, vector)
            long1.set(JAVA_LONG, 0, secondLong)
            colorCell.set(JAVA_FLOAT, 0, color.r)
            colorCell.set(JAVA_FLOAT, 4, color.g)
            colorCell.set(JAVA_FLOAT, 8, color.b)
            colorCell.set(JAVA_FLOAT, 12, color.a)
            doubleCell.set(JAVA_DOUBLE, 0, doubleValue)
            val args = arena.allocate(ADDRESS, 7)
            args.setAtIndex(ADDRESS, 0, rid0)
            args.setAtIndex(ADDRESS, 1, rid1)
            args.setAtIndex(ADDRESS, 2, long0)
            args.setAtIndex(ADDRESS, 3, vectorCell)
            args.setAtIndex(ADDRESS, 4, long1)
            args.setAtIndex(ADDRESS, 5, colorCell)
            args.setAtIndex(ADDRESS, 6, doubleCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithTwoRIDTwoLongVector2LongColorDoubleArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstRid: RID,
        secondRid: RID,
        firstLong: Long,
        secondLong: Long,
        vector: Vector2,
        thirdLong: Long,
        color: Color,
        doubleValue: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val rid0 = arena.allocate(JAVA_LONG)
            val rid1 = arena.allocate(JAVA_LONG)
            val long0 = arena.allocate(JAVA_LONG)
            val long1 = arena.allocate(JAVA_LONG)
            val vectorCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val long2 = arena.allocate(JAVA_LONG)
            val colorCell = arena.allocate(16L, 4L)
            val doubleCell = arena.allocate(JAVA_DOUBLE)
            rid0.set(JAVA_LONG, 0, firstRid.value)
            rid1.set(JAVA_LONG, 0, secondRid.value)
            long0.set(JAVA_LONG, 0, firstLong)
            long1.set(JAVA_LONG, 0, secondLong)
            writeVector2(vectorCell, vector)
            long2.set(JAVA_LONG, 0, thirdLong)
            colorCell.set(JAVA_FLOAT, 0, color.r)
            colorCell.set(JAVA_FLOAT, 4, color.g)
            colorCell.set(JAVA_FLOAT, 8, color.b)
            colorCell.set(JAVA_FLOAT, 12, color.a)
            doubleCell.set(JAVA_DOUBLE, 0, doubleValue)
            val args = arena.allocate(ADDRESS, 8)
            args.setAtIndex(ADDRESS, 0, rid0)
            args.setAtIndex(ADDRESS, 1, rid1)
            args.setAtIndex(ADDRESS, 2, long0)
            args.setAtIndex(ADDRESS, 3, long1)
            args.setAtIndex(ADDRESS, 4, vectorCell)
            args.setAtIndex(ADDRESS, 5, long2)
            args.setAtIndex(ADDRESS, 6, colorCell)
            args.setAtIndex(ADDRESS, 7, doubleCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDTransform3DVector3TwoColorUInt32Args(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        transformValue: Transform3D,
        vectorValue: Vector3,
        firstColor: Color,
        secondColor: Color,
        flags: Long,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val transform = arena.allocate(GodotReal.SIZE_BYTES * 12, GodotReal.ALIGN_BYTES)
            val vector = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            val color0 = arena.allocate(16L, 4L)
            val color1 = arena.allocate(16L, 4L)
            val flagsCell = arena.allocate(JAVA_INT)
            ridCell.set(JAVA_LONG, 0, rid.value)
            writeTransform3D(transform, transformValue)
            writeVector3(vector, vectorValue)
            color0.set(JAVA_FLOAT, 0, firstColor.r)
            color0.set(JAVA_FLOAT, 4, firstColor.g)
            color0.set(JAVA_FLOAT, 8, firstColor.b)
            color0.set(JAVA_FLOAT, 12, firstColor.a)
            color1.set(JAVA_FLOAT, 0, secondColor.r)
            color1.set(JAVA_FLOAT, 4, secondColor.g)
            color1.set(JAVA_FLOAT, 8, secondColor.b)
            color1.set(JAVA_FLOAT, 12, secondColor.a)
            flagsCell.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(flags))
            val args = arena.allocate(ADDRESS, 6)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, transform)
            args.setAtIndex(ADDRESS, 2, vector)
            args.setAtIndex(ADDRESS, 3, color0)
            args.setAtIndex(ADDRESS, 4, color1)
            args.setAtIndex(ADDRESS, 5, flagsCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDIntLongThreeBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        intValue: Int,
        longValue: Long,
        firstBool: Boolean,
        secondBool: Boolean,
        thirdBool: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val intCell = arena.allocate(JAVA_INT)
            val longCell = arena.allocate(JAVA_LONG)
            val bool0 = arena.allocate(JAVA_BYTE)
            val bool1 = arena.allocate(JAVA_BYTE)
            val bool2 = arena.allocate(JAVA_BYTE)
            ridCell.set(JAVA_LONG, 0, rid.value)
            intCell.set(JAVA_INT, 0, intValue)
            longCell.set(JAVA_LONG, 0, longValue)
            bool0.set(JAVA_BYTE, 0, if (firstBool) 1.toByte() else 0.toByte())
            bool1.set(JAVA_BYTE, 0, if (secondBool) 1.toByte() else 0.toByte())
            bool2.set(JAVA_BYTE, 0, if (thirdBool) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 6)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, intCell)
            args.setAtIndex(ADDRESS, 2, longCell)
            args.setAtIndex(ADDRESS, 3, bool0)
            args.setAtIndex(ADDRESS, 4, bool1)
            args.setAtIndex(ADDRESS, 5, bool2)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDLongVector2LongColorArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        firstLong: Long,
        vector: Vector2,
        secondLong: Long,
        color: Color,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val long0 = arena.allocate(JAVA_LONG)
            val vectorCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val long1 = arena.allocate(JAVA_LONG)
            val colorCell = arena.allocate(16L, 4L)
            ridCell.set(JAVA_LONG, 0, rid.value)
            long0.set(JAVA_LONG, 0, firstLong)
            writeVector2(vectorCell, vector)
            long1.set(JAVA_LONG, 0, secondLong)
            colorCell.set(JAVA_FLOAT, 0, color.r)
            colorCell.set(JAVA_FLOAT, 4, color.g)
            colorCell.set(JAVA_FLOAT, 8, color.b)
            colorCell.set(JAVA_FLOAT, 12, color.a)
            val args = arena.allocate(ADDRESS, 5)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, long0)
            args.setAtIndex(ADDRESS, 2, vectorCell)
            args.setAtIndex(ADDRESS, 3, long1)
            args.setAtIndex(ADDRESS, 4, colorCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithTwoRIDVector2TwoDoubleColorDoubleArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstRid: RID,
        secondRid: RID,
        vector: Vector2,
        firstDouble: Double,
        secondDouble: Double,
        color: Color,
        thirdDouble: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val rid0 = arena.allocate(JAVA_LONG)
            val rid1 = arena.allocate(JAVA_LONG)
            val vectorCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val double0 = arena.allocate(JAVA_DOUBLE)
            val double1 = arena.allocate(JAVA_DOUBLE)
            val colorCell = arena.allocate(16L, 4L)
            val double2 = arena.allocate(JAVA_DOUBLE)
            rid0.set(JAVA_LONG, 0, firstRid.value)
            rid1.set(JAVA_LONG, 0, secondRid.value)
            writeVector2(vectorCell, vector)
            double0.set(JAVA_DOUBLE, 0, firstDouble)
            double1.set(JAVA_DOUBLE, 0, secondDouble)
            colorCell.set(JAVA_FLOAT, 0, color.r)
            colorCell.set(JAVA_FLOAT, 4, color.g)
            colorCell.set(JAVA_FLOAT, 8, color.b)
            colorCell.set(JAVA_FLOAT, 12, color.a)
            double2.set(JAVA_DOUBLE, 0, thirdDouble)
            val args = arena.allocate(ADDRESS, 7)
            args.setAtIndex(ADDRESS, 0, rid0)
            args.setAtIndex(ADDRESS, 1, rid1)
            args.setAtIndex(ADDRESS, 2, vectorCell)
            args.setAtIndex(ADDRESS, 3, double0)
            args.setAtIndex(ADDRESS, 4, double1)
            args.setAtIndex(ADDRESS, 5, colorCell)
            args.setAtIndex(ADDRESS, 6, double2)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithTwoRIDVector2TwoDoubleLongColorDoubleArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstRid: RID,
        secondRid: RID,
        vector: Vector2,
        firstDouble: Double,
        secondDouble: Double,
        longValue: Long,
        color: Color,
        thirdDouble: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val rid0 = arena.allocate(JAVA_LONG)
            val rid1 = arena.allocate(JAVA_LONG)
            val vectorCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val double0 = arena.allocate(JAVA_DOUBLE)
            val double1 = arena.allocate(JAVA_DOUBLE)
            val longCell = arena.allocate(JAVA_LONG)
            val colorCell = arena.allocate(16L, 4L)
            val double2 = arena.allocate(JAVA_DOUBLE)
            rid0.set(JAVA_LONG, 0, firstRid.value)
            rid1.set(JAVA_LONG, 0, secondRid.value)
            writeVector2(vectorCell, vector)
            double0.set(JAVA_DOUBLE, 0, firstDouble)
            double1.set(JAVA_DOUBLE, 0, secondDouble)
            longCell.set(JAVA_LONG, 0, longValue)
            colorCell.set(JAVA_FLOAT, 0, color.r)
            colorCell.set(JAVA_FLOAT, 4, color.g)
            colorCell.set(JAVA_FLOAT, 8, color.b)
            colorCell.set(JAVA_FLOAT, 12, color.a)
            double2.set(JAVA_DOUBLE, 0, thirdDouble)
            val args = arena.allocate(ADDRESS, 8)
            args.setAtIndex(ADDRESS, 0, rid0)
            args.setAtIndex(ADDRESS, 1, rid1)
            args.setAtIndex(ADDRESS, 2, vectorCell)
            args.setAtIndex(ADDRESS, 3, double0)
            args.setAtIndex(ADDRESS, 4, double1)
            args.setAtIndex(ADDRESS, 5, longCell)
            args.setAtIndex(ADDRESS, 6, colorCell)
            args.setAtIndex(ADDRESS, 7, double2)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDTwoVector2TwoRIDArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        first: Vector2,
        second: Vector2,
        firstBody: RID,
        secondBody: RID,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val firstCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val secondCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val firstBodyCell = arena.allocate(JAVA_LONG)
            val secondBodyCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            writeVector2(firstCell, first)
            writeVector2(secondCell, second)
            firstBodyCell.set(JAVA_LONG, 0, firstBody.value)
            secondBodyCell.set(JAVA_LONG, 0, secondBody.value)
            val args = arena.allocate(ADDRESS, 5)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, firstCell)
            args.setAtIndex(ADDRESS, 2, secondCell)
            args.setAtIndex(ADDRESS, 3, firstBodyCell)
            args.setAtIndex(ADDRESS, 4, secondBodyCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDThreeVector2TwoRIDArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        first: Vector2,
        second: Vector2,
        third: Vector2,
        firstBody: RID,
        secondBody: RID,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val firstCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val secondCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val thirdCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val firstBodyCell = arena.allocate(JAVA_LONG)
            val secondBodyCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            writeVector2(firstCell, first)
            writeVector2(secondCell, second)
            writeVector2(thirdCell, third)
            firstBodyCell.set(JAVA_LONG, 0, firstBody.value)
            secondBodyCell.set(JAVA_LONG, 0, secondBody.value)
            val args = arena.allocate(ADDRESS, 6)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, firstCell)
            args.setAtIndex(ADDRESS, 2, secondCell)
            args.setAtIndex(ADDRESS, 3, thirdCell)
            args.setAtIndex(ADDRESS, 4, firstBodyCell)
            args.setAtIndex(ADDRESS, 5, secondBodyCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDAndVector2iArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Vector2i,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val vectorCell = arena.allocate(8L, 4L)
            ridCell.set(JAVA_LONG, 0, rid.value)
            vectorCell.set(JAVA_INT, 0, value.x)
            vectorCell.set(JAVA_INT, 4, value.y)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, vectorCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDVector2iLongArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        vector: Vector2i,
        longValue: Long,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val vectorCell = arena.allocate(8L, 4L)
            val longCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            vectorCell.set(JAVA_INT, 0, vector.x)
            vectorCell.set(JAVA_INT, 4, vector.y)
            longCell.set(JAVA_LONG, 0, longValue)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, vectorCell)
            args.setAtIndex(ADDRESS, 2, longCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDVector2iLongArgsRetVector2(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        vector: Vector2i,
        longValue: Long,
    ): Vector2 {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val vectorCell = arena.allocate(8L, 4L)
            val longCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            vectorCell.set(JAVA_INT, 0, vector.x)
            vectorCell.set(JAVA_INT, 4, vector.y)
            longCell.set(JAVA_LONG, 0, longValue)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, vectorCell)
            args.setAtIndex(ADDRESS, 2, longCell)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return Vector2(GodotReal.readIndex(ret, 0), GodotReal.readIndex(ret, 1))
        }
    }

    fun ptrcallWithRIDVector2iLongArgsRetPackedInt32List(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        vector: Vector2i,
        longValue: Long,
    ): List<Int> {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val vectorCell = arena.allocate(8L, 4L)
            val longCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            vectorCell.set(JAVA_INT, 0, vector.x)
            vectorCell.set(JAVA_INT, 4, vector.y)
            longCell.set(JAVA_LONG, 0, longValue)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, vectorCell)
            args.setAtIndex(ADDRESS, 2, longCell)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return try {
                BuiltinTypes.readPackedInt32Array(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_INT32_ARRAY, ret)
            }
        }
    }

    fun ptrcallWithRIDVector2iLongArgsRetRect2(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        vector: Vector2i,
        longValue: Long,
    ): Rect2 {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val vectorCell = arena.allocate(8L, 4L)
            val longCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            vectorCell.set(JAVA_INT, 0, vector.x)
            vectorCell.set(JAVA_INT, 4, vector.y)
            longCell.set(JAVA_LONG, 0, longValue)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, vectorCell)
            args.setAtIndex(ADDRESS, 2, longCell)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return readRect2(ret)
        }
    }

    fun ptrcallWithRIDVector2iLongArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        vector: Vector2i,
        longValue: Long,
    ): Long {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val vectorCell = arena.allocate(8L, 4L)
            val longCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            vectorCell.set(JAVA_INT, 0, vector.x)
            vectorCell.set(JAVA_INT, 4, vector.y)
            longCell.set(JAVA_LONG, 0, longValue)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, vectorCell)
            args.setAtIndex(ADDRESS, 2, longCell)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(JAVA_LONG, 0)
        }
    }

    fun ptrcallWithRIDVector2iLongArgsRetRID(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        vector: Vector2i,
        longValue: Long,
    ): RID {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val vectorCell = arena.allocate(8L, 4L)
            val longCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            vectorCell.set(JAVA_INT, 0, vector.x)
            vectorCell.set(JAVA_INT, 4, vector.y)
            longCell.set(JAVA_LONG, 0, longValue)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, vectorCell)
            args.setAtIndex(ADDRESS, 2, longCell)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return RID(ret.get(JAVA_LONG, 0))
        }
    }

    fun ptrcallWithRIDVector2iLongVector2Args(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        vector: Vector2i,
        longValue: Long,
        value: Vector2,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val vectorCell = arena.allocate(8L, 4L)
            val longCell = arena.allocate(JAVA_LONG)
            val valueCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            ridCell.set(JAVA_LONG, 0, rid.value)
            vectorCell.set(JAVA_INT, 0, vector.x)
            vectorCell.set(JAVA_INT, 4, vector.y)
            longCell.set(JAVA_LONG, 0, longValue)
            GodotReal.writeIndex(valueCell, 0, value.x)
            GodotReal.writeIndex(valueCell, 1, value.y)
            val args = arena.allocate(ADDRESS, 4)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, vectorCell)
            args.setAtIndex(ADDRESS, 2, longCell)
            args.setAtIndex(ADDRESS, 3, valueCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDVector2iLongRect2Args(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        vector: Vector2i,
        longValue: Long,
        rect: Rect2,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val vectorCell = arena.allocate(8L, 4L)
            val longCell = arena.allocate(JAVA_LONG)
            val rectCell = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
            ridCell.set(JAVA_LONG, 0, rid.value)
            vectorCell.set(JAVA_INT, 0, vector.x)
            vectorCell.set(JAVA_INT, 4, vector.y)
            longCell.set(JAVA_LONG, 0, longValue)
            writeRect2(rectCell, rect)
            val args = arena.allocate(ADDRESS, 4)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, vectorCell)
            args.setAtIndex(ADDRESS, 2, longCell)
            args.setAtIndex(ADDRESS, 3, rectCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDVector2iTwoLongArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        vector: Vector2i,
        firstLong: Long,
        secondLong: Long,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val vectorCell = arena.allocate(8L, 4L)
            val firstLongCell = arena.allocate(JAVA_LONG)
            val secondLongCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            vectorCell.set(JAVA_INT, 0, vector.x)
            vectorCell.set(JAVA_INT, 4, vector.y)
            firstLongCell.set(JAVA_LONG, 0, firstLong)
            secondLongCell.set(JAVA_LONG, 0, secondLong)
            val args = arena.allocate(ADDRESS, 4)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, vectorCell)
            args.setAtIndex(ADDRESS, 2, firstLongCell)
            args.setAtIndex(ADDRESS, 3, secondLongCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDAndTransform2DArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Transform2D,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            val transformCell = arena.allocate(GodotReal.SIZE_BYTES * 6, GodotReal.ALIGN_BYTES)
            writeTransform2D(transformCell, value)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, transformCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithTwoRIDTransform2DBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstRid: RID,
        secondRid: RID,
        transform: Transform2D,
        enabled: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val rid0 = arena.allocate(JAVA_LONG)
            val rid1 = arena.allocate(JAVA_LONG)
            val transformCell = arena.allocate(GodotReal.SIZE_BYTES * 6, GodotReal.ALIGN_BYTES)
            val boolCell = arena.allocate(JAVA_BYTE)
            rid0.set(JAVA_LONG, 0, firstRid.value)
            rid1.set(JAVA_LONG, 0, secondRid.value)
            writeTransform2D(transformCell, transform)
            boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 4)
            args.setAtIndex(ADDRESS, 0, rid0)
            args.setAtIndex(ADDRESS, 1, rid1)
            args.setAtIndex(ADDRESS, 2, transformCell)
            args.setAtIndex(ADDRESS, 3, boolCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithTwoRIDAndVector2Arg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstRid: RID,
        secondRid: RID,
        value: Vector2,
    ) {
        Arena.ofConfined().use { arena ->
            val rid0 = arena.allocate(JAVA_LONG)
            val rid1 = arena.allocate(JAVA_LONG)
            val vectorCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            rid0.set(JAVA_LONG, 0, firstRid.value)
            rid1.set(JAVA_LONG, 0, secondRid.value)
            GodotReal.writeIndex(vectorCell, 0, value.x)
            GodotReal.writeIndex(vectorCell, 1, value.y)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, rid0)
            args.setAtIndex(ADDRESS, 1, rid1)
            args.setAtIndex(ADDRESS, 2, vectorCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithTwoRIDAndTransform2DArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstRid: RID,
        secondRid: RID,
        transform: Transform2D,
    ) {
        Arena.ofConfined().use { arena ->
            val rid0 = arena.allocate(JAVA_LONG)
            val rid1 = arena.allocate(JAVA_LONG)
            val transformCell = arena.allocate(GodotReal.SIZE_BYTES * 6, GodotReal.ALIGN_BYTES)
            rid0.set(JAVA_LONG, 0, firstRid.value)
            rid1.set(JAVA_LONG, 0, secondRid.value)
            writeTransform2D(transformCell, transform)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, rid0)
            args.setAtIndex(ADDRESS, 1, rid1)
            args.setAtIndex(ADDRESS, 2, transformCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithTwoRIDTransform2DColorRIDArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstRid: RID,
        secondRid: RID,
        transform: Transform2D,
        color: Color,
        thirdRid: RID,
    ) {
        Arena.ofConfined().use { arena ->
            val rid0 = arena.allocate(JAVA_LONG)
            val rid1 = arena.allocate(JAVA_LONG)
            val transformCell = arena.allocate(GodotReal.SIZE_BYTES * 6, GodotReal.ALIGN_BYTES)
            val colorCell = arena.allocate(16L, 4L)
            val rid2 = arena.allocate(JAVA_LONG)
            rid0.set(JAVA_LONG, 0, firstRid.value)
            rid1.set(JAVA_LONG, 0, secondRid.value)
            writeTransform2D(transformCell, transform)
            colorCell.set(JAVA_FLOAT, 0, color.r)
            colorCell.set(JAVA_FLOAT, 4, color.g)
            colorCell.set(JAVA_FLOAT, 8, color.b)
            colorCell.set(JAVA_FLOAT, 12, color.a)
            rid2.set(JAVA_LONG, 0, thirdRid.value)
            val args = arena.allocate(ADDRESS, 5)
            args.setAtIndex(ADDRESS, 0, rid0)
            args.setAtIndex(ADDRESS, 1, rid1)
            args.setAtIndex(ADDRESS, 2, transformCell)
            args.setAtIndex(ADDRESS, 3, colorCell)
            args.setAtIndex(ADDRESS, 4, rid2)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDAndTransform3DArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Transform3D,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            val transformCell = arena.allocate(GodotReal.SIZE_BYTES * 12, GodotReal.ALIGN_BYTES)
            writeTransform3D(transformCell, value)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, transformCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithTwoRIDTransform3DBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstRid: RID,
        secondRid: RID,
        transform: Transform3D,
        enabled: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val rid0 = arena.allocate(JAVA_LONG)
            val rid1 = arena.allocate(JAVA_LONG)
            val transformCell = arena.allocate(GodotReal.SIZE_BYTES * 12, GodotReal.ALIGN_BYTES)
            val boolCell = arena.allocate(JAVA_BYTE)
            rid0.set(JAVA_LONG, 0, firstRid.value)
            rid1.set(JAVA_LONG, 0, secondRid.value)
            writeTransform3D(transformCell, transform)
            boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 4)
            args.setAtIndex(ADDRESS, 0, rid0)
            args.setAtIndex(ADDRESS, 1, rid1)
            args.setAtIndex(ADDRESS, 2, transformCell)
            args.setAtIndex(ADDRESS, 3, boolCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDAndAABBArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: AABB,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            val aabbCell = arena.allocate(GodotReal.SIZE_BYTES * 6, GodotReal.ALIGN_BYTES)
            writeAABB(aabbCell, value)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, aabbCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDAndRect2Arg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Rect2,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val rectCell = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
            ridCell.set(JAVA_LONG, 0, rid.value)
            writeRect2(rectCell, value)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, rectCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDAndColorArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        color: Color,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            val colorCell = arena.allocate(16L, 4L)
            colorCell.set(JAVA_FLOAT, 0, color.r)
            colorCell.set(JAVA_FLOAT, 4, color.g)
            colorCell.set(JAVA_FLOAT, 8, color.b)
            colorCell.set(JAVA_FLOAT, 12, color.a)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, colorCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDVector2IntColorDoubleArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        vector: Vector2,
        intValue: Int,
        color: Color,
        doubleValue: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val vectorCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val intCell = arena.allocate(JAVA_INT)
            val colorCell = arena.allocate(16L, 4L)
            val doubleCell = arena.allocate(JAVA_DOUBLE)
            ridCell.set(JAVA_LONG, 0, rid.value)
            GodotReal.writeIndex(vectorCell, 0, vector.x)
            GodotReal.writeIndex(vectorCell, 1, vector.y)
            intCell.set(JAVA_INT, 0, intValue)
            // Color components are fixed 32-bit floats, unlike scalar float ptrcall slots.
            colorCell.set(JAVA_FLOAT, 0, color.r)
            colorCell.set(JAVA_FLOAT, 4, color.g)
            colorCell.set(JAVA_FLOAT, 8, color.b)
            colorCell.set(JAVA_FLOAT, 12, color.a)
            doubleCell.set(JAVA_DOUBLE, 0, doubleValue)
            val args = arena.allocate(ADDRESS, 5)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, vectorCell)
            args.setAtIndex(ADDRESS, 2, intCell)
            args.setAtIndex(ADDRESS, 3, colorCell)
            args.setAtIndex(ADDRESS, 4, doubleCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDColorFourUInt32ArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        color: Color,
        first: Long,
        second: Long,
        third: Long,
        fourth: Long,
    ): Long {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val colorCell = arena.allocate(16L, 4L)
            val uint0 = arena.allocate(JAVA_INT)
            val uint1 = arena.allocate(JAVA_INT)
            val uint2 = arena.allocate(JAVA_INT)
            val uint3 = arena.allocate(JAVA_INT)
            ridCell.set(JAVA_LONG, 0, rid.value)
            colorCell.set(JAVA_FLOAT, 0, color.r)
            colorCell.set(JAVA_FLOAT, 4, color.g)
            colorCell.set(JAVA_FLOAT, 8, color.b)
            colorCell.set(JAVA_FLOAT, 12, color.a)
            uint0.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(first))
            uint1.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(second))
            uint2.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(third))
            uint3.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(fourth))
            val args = arena.allocate(ADDRESS, 6)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, colorCell)
            args.setAtIndex(ADDRESS, 2, uint0)
            args.setAtIndex(ADDRESS, 3, uint1)
            args.setAtIndex(ADDRESS, 4, uint2)
            args.setAtIndex(ADDRESS, 5, uint3)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(JAVA_LONG, 0)
        }
    }

    fun ptrcallWithRIDColorLongTwoDoubleLongArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        color: Color,
        firstLong: Long,
        firstDouble: Double,
        secondDouble: Double,
        secondLong: Long,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val colorCell = arena.allocate(16L, 4L)
            val long0 = arena.allocate(JAVA_LONG)
            val double0 = arena.allocate(JAVA_DOUBLE)
            val double1 = arena.allocate(JAVA_DOUBLE)
            val long1 = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            colorCell.set(JAVA_FLOAT, 0, color.r)
            colorCell.set(JAVA_FLOAT, 4, color.g)
            colorCell.set(JAVA_FLOAT, 8, color.b)
            colorCell.set(JAVA_FLOAT, 12, color.a)
            long0.set(JAVA_LONG, 0, firstLong)
            double0.set(JAVA_DOUBLE, 0, firstDouble)
            double1.set(JAVA_DOUBLE, 0, secondDouble)
            long1.set(JAVA_LONG, 0, secondLong)
            val args = arena.allocate(ADDRESS, 6)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, colorCell)
            args.setAtIndex(ADDRESS, 2, long0)
            args.setAtIndex(ADDRESS, 3, double0)
            args.setAtIndex(ADDRESS, 4, double1)
            args.setAtIndex(ADDRESS, 5, long1)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDVector2TwoColorDoubleArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        vector: Vector2,
        firstColor: Color,
        secondColor: Color,
        doubleValue: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val vectorCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val color0 = arena.allocate(16L, 4L)
            val color1 = arena.allocate(16L, 4L)
            val doubleCell = arena.allocate(JAVA_DOUBLE)
            ridCell.set(JAVA_LONG, 0, rid.value)
            writeVector2(vectorCell, vector)
            color0.set(JAVA_FLOAT, 0, firstColor.r)
            color0.set(JAVA_FLOAT, 4, firstColor.g)
            color0.set(JAVA_FLOAT, 8, firstColor.b)
            color0.set(JAVA_FLOAT, 12, firstColor.a)
            color1.set(JAVA_FLOAT, 0, secondColor.r)
            color1.set(JAVA_FLOAT, 4, secondColor.g)
            color1.set(JAVA_FLOAT, 8, secondColor.b)
            color1.set(JAVA_FLOAT, 12, secondColor.a)
            doubleCell.set(JAVA_DOUBLE, 0, doubleValue)
            val args = arena.allocate(ADDRESS, 5)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, vectorCell)
            args.setAtIndex(ADDRESS, 2, color0)
            args.setAtIndex(ADDRESS, 3, color1)
            args.setAtIndex(ADDRESS, 4, doubleCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDVector2IntTwoColorDoubleArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        vector: Vector2,
        intValue: Int,
        firstColor: Color,
        secondColor: Color,
        doubleValue: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val vectorCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val intCell = arena.allocate(JAVA_INT)
            val color0 = arena.allocate(16L, 4L)
            val color1 = arena.allocate(16L, 4L)
            val doubleCell = arena.allocate(JAVA_DOUBLE)
            ridCell.set(JAVA_LONG, 0, rid.value)
            writeVector2(vectorCell, vector)
            intCell.set(JAVA_INT, 0, intValue)
            color0.set(JAVA_FLOAT, 0, firstColor.r)
            color0.set(JAVA_FLOAT, 4, firstColor.g)
            color0.set(JAVA_FLOAT, 8, firstColor.b)
            color0.set(JAVA_FLOAT, 12, firstColor.a)
            color1.set(JAVA_FLOAT, 0, secondColor.r)
            color1.set(JAVA_FLOAT, 4, secondColor.g)
            color1.set(JAVA_FLOAT, 8, secondColor.b)
            color1.set(JAVA_FLOAT, 12, secondColor.a)
            doubleCell.set(JAVA_DOUBLE, 0, doubleValue)
            val args = arena.allocate(ADDRESS, 6)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, vectorCell)
            args.setAtIndex(ADDRESS, 2, intCell)
            args.setAtIndex(ADDRESS, 3, color0)
            args.setAtIndex(ADDRESS, 4, color1)
            args.setAtIndex(ADDRESS, 5, doubleCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDVector2TwoIntColorDoubleArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        vector: Vector2,
        firstInt: Int,
        secondInt: Int,
        color: Color,
        doubleValue: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val vectorCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val int0 = arena.allocate(JAVA_INT)
            val int1 = arena.allocate(JAVA_INT)
            val colorCell = arena.allocate(16L, 4L)
            val doubleCell = arena.allocate(JAVA_DOUBLE)
            ridCell.set(JAVA_LONG, 0, rid.value)
            writeVector2(vectorCell, vector)
            int0.set(JAVA_INT, 0, firstInt)
            int1.set(JAVA_INT, 0, secondInt)
            colorCell.set(JAVA_FLOAT, 0, color.r)
            colorCell.set(JAVA_FLOAT, 4, color.g)
            colorCell.set(JAVA_FLOAT, 8, color.b)
            colorCell.set(JAVA_FLOAT, 12, color.a)
            doubleCell.set(JAVA_DOUBLE, 0, doubleValue)
            val args = arena.allocate(ADDRESS, 6)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, vectorCell)
            args.setAtIndex(ADDRESS, 2, int0)
            args.setAtIndex(ADDRESS, 3, int1)
            args.setAtIndex(ADDRESS, 4, colorCell)
            args.setAtIndex(ADDRESS, 5, doubleCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDVector2TwoIntColorDoubleArgsRetDouble(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        position: Vector2,
        char: Int,
        fontSize: Int,
        color: Color,
        oversampling: Double,
    ): Double {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val vectorCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val charCell = arena.allocate(JAVA_INT)
            val fontSizeCell = arena.allocate(JAVA_INT)
            val colorCell = arena.allocate(16L, 4L)
            val oversamplingCell = arena.allocate(JAVA_DOUBLE)
            ridCell.set(JAVA_LONG, 0, rid.value)
            writeVector2(vectorCell, position)
            charCell.set(JAVA_INT, 0, char)
            fontSizeCell.set(JAVA_INT, 0, fontSize)
            colorCell.set(JAVA_FLOAT, 0, color.r)
            colorCell.set(JAVA_FLOAT, 4, color.g)
            colorCell.set(JAVA_FLOAT, 8, color.b)
            colorCell.set(JAVA_FLOAT, 12, color.a)
            oversamplingCell.set(JAVA_DOUBLE, 0, oversampling)
            val args = arena.allocate(ADDRESS, 6)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, vectorCell)
            args.setAtIndex(ADDRESS, 2, charCell)
            args.setAtIndex(ADDRESS, 3, fontSizeCell)
            args.setAtIndex(ADDRESS, 4, colorCell)
            args.setAtIndex(ADDRESS, 5, oversamplingCell)
            val ret = arena.allocate(JAVA_DOUBLE)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(JAVA_DOUBLE, 0)
        }
    }

    fun ptrcallWithRIDVector2ThreeIntColorDoubleArgsRetDouble(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        position: Vector2,
        char: Int,
        fontSize: Int,
        outlineSize: Int,
        color: Color,
        oversampling: Double,
    ): Double {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val vectorCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val charCell = arena.allocate(JAVA_INT)
            val fontSizeCell = arena.allocate(JAVA_INT)
            val outlineSizeCell = arena.allocate(JAVA_INT)
            val colorCell = arena.allocate(16L, 4L)
            val oversamplingCell = arena.allocate(JAVA_DOUBLE)
            ridCell.set(JAVA_LONG, 0, rid.value)
            writeVector2(vectorCell, position)
            charCell.set(JAVA_INT, 0, char)
            fontSizeCell.set(JAVA_INT, 0, fontSize)
            outlineSizeCell.set(JAVA_INT, 0, outlineSize)
            colorCell.set(JAVA_FLOAT, 0, color.r)
            colorCell.set(JAVA_FLOAT, 4, color.g)
            colorCell.set(JAVA_FLOAT, 8, color.b)
            colorCell.set(JAVA_FLOAT, 12, color.a)
            oversamplingCell.set(JAVA_DOUBLE, 0, oversampling)
            val args = arena.allocate(ADDRESS, 7)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, vectorCell)
            args.setAtIndex(ADDRESS, 2, charCell)
            args.setAtIndex(ADDRESS, 3, fontSizeCell)
            args.setAtIndex(ADDRESS, 4, outlineSizeCell)
            args.setAtIndex(ADDRESS, 5, colorCell)
            args.setAtIndex(ADDRESS, 6, oversamplingCell)
            val ret = arena.allocate(JAVA_DOUBLE)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(JAVA_DOUBLE, 0)
        }
    }

    fun ptrcallWithRIDVector2StringLongDoubleIntColorThreeLongDoubleArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        position: Vector2,
        text: String,
        alignment: Long,
        width: Double,
        fontSize: Int,
        color: Color,
        justification: Long,
        direction: Long,
        orientation: Long,
        oversampling: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val vectorCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val stringCell = arena.allocate(8L, 8L)
            val alignmentCell = arena.allocate(JAVA_LONG)
            val widthCell = arena.allocate(JAVA_DOUBLE)
            val fontSizeCell = arena.allocate(JAVA_INT)
            val colorCell = arena.allocate(16L, 4L)
            val justificationCell = arena.allocate(JAVA_LONG)
            val directionCell = arena.allocate(JAVA_LONG)
            val orientationCell = arena.allocate(JAVA_LONG)
            val oversamplingCell = arena.allocate(JAVA_DOUBLE)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                writeVector2(vectorCell, position)
                GodotStrings.initString(stringCell, text)
                alignmentCell.set(JAVA_LONG, 0, alignment)
                widthCell.set(JAVA_DOUBLE, 0, width)
                fontSizeCell.set(JAVA_INT, 0, fontSize)
                colorCell.set(JAVA_FLOAT, 0, color.r)
                colorCell.set(JAVA_FLOAT, 4, color.g)
                colorCell.set(JAVA_FLOAT, 8, color.b)
                colorCell.set(JAVA_FLOAT, 12, color.a)
                justificationCell.set(JAVA_LONG, 0, justification)
                directionCell.set(JAVA_LONG, 0, direction)
                orientationCell.set(JAVA_LONG, 0, orientation)
                oversamplingCell.set(JAVA_DOUBLE, 0, oversampling)
                val args = arena.allocate(ADDRESS, 11)
                args.setAtIndex(ADDRESS, 0, ridCell)
                args.setAtIndex(ADDRESS, 1, vectorCell)
                args.setAtIndex(ADDRESS, 2, stringCell)
                args.setAtIndex(ADDRESS, 3, alignmentCell)
                args.setAtIndex(ADDRESS, 4, widthCell)
                args.setAtIndex(ADDRESS, 5, fontSizeCell)
                args.setAtIndex(ADDRESS, 6, colorCell)
                args.setAtIndex(ADDRESS, 7, justificationCell)
                args.setAtIndex(ADDRESS, 8, directionCell)
                args.setAtIndex(ADDRESS, 9, orientationCell)
                args.setAtIndex(ADDRESS, 10, oversamplingCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithRIDVector2StringLongDoubleTwoIntColorThreeLongDoubleArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        position: Vector2,
        text: String,
        alignment: Long,
        width: Double,
        fontSize: Int,
        outlineSize: Int,
        color: Color,
        justification: Long,
        direction: Long,
        orientation: Long,
        oversampling: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val vectorCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val stringCell = arena.allocate(8L, 8L)
            val alignmentCell = arena.allocate(JAVA_LONG)
            val widthCell = arena.allocate(JAVA_DOUBLE)
            val fontSizeCell = arena.allocate(JAVA_INT)
            val outlineSizeCell = arena.allocate(JAVA_INT)
            val colorCell = arena.allocate(16L, 4L)
            val justificationCell = arena.allocate(JAVA_LONG)
            val directionCell = arena.allocate(JAVA_LONG)
            val orientationCell = arena.allocate(JAVA_LONG)
            val oversamplingCell = arena.allocate(JAVA_DOUBLE)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                writeVector2(vectorCell, position)
                GodotStrings.initString(stringCell, text)
                alignmentCell.set(JAVA_LONG, 0, alignment)
                widthCell.set(JAVA_DOUBLE, 0, width)
                fontSizeCell.set(JAVA_INT, 0, fontSize)
                outlineSizeCell.set(JAVA_INT, 0, outlineSize)
                colorCell.set(JAVA_FLOAT, 0, color.r)
                colorCell.set(JAVA_FLOAT, 4, color.g)
                colorCell.set(JAVA_FLOAT, 8, color.b)
                colorCell.set(JAVA_FLOAT, 12, color.a)
                justificationCell.set(JAVA_LONG, 0, justification)
                directionCell.set(JAVA_LONG, 0, direction)
                orientationCell.set(JAVA_LONG, 0, orientation)
                oversamplingCell.set(JAVA_DOUBLE, 0, oversampling)
                val args = arena.allocate(ADDRESS, 12)
                args.setAtIndex(ADDRESS, 0, ridCell)
                args.setAtIndex(ADDRESS, 1, vectorCell)
                args.setAtIndex(ADDRESS, 2, stringCell)
                args.setAtIndex(ADDRESS, 3, alignmentCell)
                args.setAtIndex(ADDRESS, 4, widthCell)
                args.setAtIndex(ADDRESS, 5, fontSizeCell)
                args.setAtIndex(ADDRESS, 6, outlineSizeCell)
                args.setAtIndex(ADDRESS, 7, colorCell)
                args.setAtIndex(ADDRESS, 8, justificationCell)
                args.setAtIndex(ADDRESS, 9, directionCell)
                args.setAtIndex(ADDRESS, 10, orientationCell)
                args.setAtIndex(ADDRESS, 11, oversamplingCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithRIDVector2StringLongDoubleTwoIntColorFourLongDoubleArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        position: Vector2,
        text: String,
        alignment: Long,
        width: Double,
        fontSize: Int,
        maxLines: Int,
        color: Color,
        breakFlags: Long,
        justification: Long,
        direction: Long,
        orientation: Long,
        oversampling: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val vectorCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val stringCell = arena.allocate(8L, 8L)
            val alignmentCell = arena.allocate(JAVA_LONG)
            val widthCell = arena.allocate(JAVA_DOUBLE)
            val fontSizeCell = arena.allocate(JAVA_INT)
            val maxLinesCell = arena.allocate(JAVA_INT)
            val colorCell = arena.allocate(16L, 4L)
            val breakFlagsCell = arena.allocate(JAVA_LONG)
            val justificationCell = arena.allocate(JAVA_LONG)
            val directionCell = arena.allocate(JAVA_LONG)
            val orientationCell = arena.allocate(JAVA_LONG)
            val oversamplingCell = arena.allocate(JAVA_DOUBLE)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                writeVector2(vectorCell, position)
                GodotStrings.initString(stringCell, text)
                alignmentCell.set(JAVA_LONG, 0, alignment)
                widthCell.set(JAVA_DOUBLE, 0, width)
                fontSizeCell.set(JAVA_INT, 0, fontSize)
                maxLinesCell.set(JAVA_INT, 0, maxLines)
                colorCell.set(JAVA_FLOAT, 0, color.r)
                colorCell.set(JAVA_FLOAT, 4, color.g)
                colorCell.set(JAVA_FLOAT, 8, color.b)
                colorCell.set(JAVA_FLOAT, 12, color.a)
                breakFlagsCell.set(JAVA_LONG, 0, breakFlags)
                justificationCell.set(JAVA_LONG, 0, justification)
                directionCell.set(JAVA_LONG, 0, direction)
                orientationCell.set(JAVA_LONG, 0, orientation)
                oversamplingCell.set(JAVA_DOUBLE, 0, oversampling)
                val args = arena.allocate(ADDRESS, 13)
                args.setAtIndex(ADDRESS, 0, ridCell)
                args.setAtIndex(ADDRESS, 1, vectorCell)
                args.setAtIndex(ADDRESS, 2, stringCell)
                args.setAtIndex(ADDRESS, 3, alignmentCell)
                args.setAtIndex(ADDRESS, 4, widthCell)
                args.setAtIndex(ADDRESS, 5, fontSizeCell)
                args.setAtIndex(ADDRESS, 6, maxLinesCell)
                args.setAtIndex(ADDRESS, 7, colorCell)
                args.setAtIndex(ADDRESS, 8, breakFlagsCell)
                args.setAtIndex(ADDRESS, 9, justificationCell)
                args.setAtIndex(ADDRESS, 10, directionCell)
                args.setAtIndex(ADDRESS, 11, orientationCell)
                args.setAtIndex(ADDRESS, 12, oversamplingCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithRIDVector2StringLongDoubleThreeIntColorFourLongDoubleArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        position: Vector2,
        text: String,
        alignment: Long,
        width: Double,
        fontSize: Int,
        maxLines: Int,
        outlineSize: Int,
        color: Color,
        breakFlags: Long,
        justification: Long,
        direction: Long,
        orientation: Long,
        oversampling: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val vectorCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val stringCell = arena.allocate(8L, 8L)
            val alignmentCell = arena.allocate(JAVA_LONG)
            val widthCell = arena.allocate(JAVA_DOUBLE)
            val fontSizeCell = arena.allocate(JAVA_INT)
            val maxLinesCell = arena.allocate(JAVA_INT)
            val outlineSizeCell = arena.allocate(JAVA_INT)
            val colorCell = arena.allocate(16L, 4L)
            val breakFlagsCell = arena.allocate(JAVA_LONG)
            val justificationCell = arena.allocate(JAVA_LONG)
            val directionCell = arena.allocate(JAVA_LONG)
            val orientationCell = arena.allocate(JAVA_LONG)
            val oversamplingCell = arena.allocate(JAVA_DOUBLE)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                writeVector2(vectorCell, position)
                GodotStrings.initString(stringCell, text)
                alignmentCell.set(JAVA_LONG, 0, alignment)
                widthCell.set(JAVA_DOUBLE, 0, width)
                fontSizeCell.set(JAVA_INT, 0, fontSize)
                maxLinesCell.set(JAVA_INT, 0, maxLines)
                outlineSizeCell.set(JAVA_INT, 0, outlineSize)
                colorCell.set(JAVA_FLOAT, 0, color.r)
                colorCell.set(JAVA_FLOAT, 4, color.g)
                colorCell.set(JAVA_FLOAT, 8, color.b)
                colorCell.set(JAVA_FLOAT, 12, color.a)
                breakFlagsCell.set(JAVA_LONG, 0, breakFlags)
                justificationCell.set(JAVA_LONG, 0, justification)
                directionCell.set(JAVA_LONG, 0, direction)
                orientationCell.set(JAVA_LONG, 0, orientation)
                oversamplingCell.set(JAVA_DOUBLE, 0, oversampling)
                val args = arena.allocate(ADDRESS, 14)
                args.setAtIndex(ADDRESS, 0, ridCell)
                args.setAtIndex(ADDRESS, 1, vectorCell)
                args.setAtIndex(ADDRESS, 2, stringCell)
                args.setAtIndex(ADDRESS, 3, alignmentCell)
                args.setAtIndex(ADDRESS, 4, widthCell)
                args.setAtIndex(ADDRESS, 5, fontSizeCell)
                args.setAtIndex(ADDRESS, 6, maxLinesCell)
                args.setAtIndex(ADDRESS, 7, outlineSizeCell)
                args.setAtIndex(ADDRESS, 8, colorCell)
                args.setAtIndex(ADDRESS, 9, breakFlagsCell)
                args.setAtIndex(ADDRESS, 10, justificationCell)
                args.setAtIndex(ADDRESS, 11, directionCell)
                args.setAtIndex(ADDRESS, 12, orientationCell)
                args.setAtIndex(ADDRESS, 13, oversamplingCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithRIDVector2ColorDoubleArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        vector: Vector2,
        color: Color,
        doubleValue: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val vectorCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val colorCell = arena.allocate(16L, 4L)
            val doubleCell = arena.allocate(JAVA_DOUBLE)
            ridCell.set(JAVA_LONG, 0, rid.value)
            GodotReal.writeIndex(vectorCell, 0, vector.x)
            GodotReal.writeIndex(vectorCell, 1, vector.y)
            // Color components are fixed 32-bit floats, unlike scalar float ptrcall slots.
            colorCell.set(JAVA_FLOAT, 0, color.r)
            colorCell.set(JAVA_FLOAT, 4, color.g)
            colorCell.set(JAVA_FLOAT, 8, color.b)
            colorCell.set(JAVA_FLOAT, 12, color.a)
            doubleCell.set(JAVA_DOUBLE, 0, doubleValue)
            val args = arena.allocate(ADDRESS, 4)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, vectorCell)
            args.setAtIndex(ADDRESS, 2, colorCell)
            args.setAtIndex(ADDRESS, 3, doubleCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDVector2ColorBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        vector: Vector2,
        color: Color,
        transpose: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val vectorCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val colorCell = arena.allocate(16L, 4L)
            val transposeCell = arena.allocate(JAVA_BYTE)
            ridCell.set(JAVA_LONG, 0, rid.value)
            writeVector2(vectorCell, vector)
            // Color components are fixed 32-bit floats, unlike scalar float ptrcall slots.
            colorCell.set(JAVA_FLOAT, 0, color.r)
            colorCell.set(JAVA_FLOAT, 4, color.g)
            colorCell.set(JAVA_FLOAT, 8, color.b)
            colorCell.set(JAVA_FLOAT, 12, color.a)
            transposeCell.set(JAVA_BYTE, 0, if (transpose) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 4)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, vectorCell)
            args.setAtIndex(ADDRESS, 2, colorCell)
            args.setAtIndex(ADDRESS, 3, transposeCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDTwoVector2ColorDoubleBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        first: Vector2,
        second: Vector2,
        color: Color,
        width: Double,
        antialiased: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val firstCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val secondCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val colorCell = arena.allocate(16L, 4L)
            val widthCell = arena.allocate(JAVA_DOUBLE)
            val boolCell = arena.allocate(JAVA_BYTE)
            ridCell.set(JAVA_LONG, 0, rid.value)
            writeVector2(firstCell, first)
            writeVector2(secondCell, second)
            // Color components are fixed 32-bit floats, unlike scalar float ptrcall slots.
            colorCell.set(JAVA_FLOAT, 0, color.r)
            colorCell.set(JAVA_FLOAT, 4, color.g)
            colorCell.set(JAVA_FLOAT, 8, color.b)
            colorCell.set(JAVA_FLOAT, 12, color.a)
            widthCell.set(JAVA_DOUBLE, 0, width)
            boolCell.set(JAVA_BYTE, 0, if (antialiased) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 6)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, firstCell)
            args.setAtIndex(ADDRESS, 2, secondCell)
            args.setAtIndex(ADDRESS, 3, colorCell)
            args.setAtIndex(ADDRESS, 4, widthCell)
            args.setAtIndex(ADDRESS, 5, boolCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDVector2DoubleColorBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        vector: Vector2,
        firstDouble: Double,
        color: Color,
        enabled: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val vectorCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val doubleCell = arena.allocate(JAVA_DOUBLE)
            val colorCell = arena.allocate(16L, 4L)
            val boolCell = arena.allocate(JAVA_BYTE)
            ridCell.set(JAVA_LONG, 0, rid.value)
            writeVector2(vectorCell, vector)
            doubleCell.set(JAVA_DOUBLE, 0, firstDouble)
            // Color components are fixed 32-bit floats, unlike scalar float ptrcall slots.
            colorCell.set(JAVA_FLOAT, 0, color.r)
            colorCell.set(JAVA_FLOAT, 4, color.g)
            colorCell.set(JAVA_FLOAT, 8, color.b)
            colorCell.set(JAVA_FLOAT, 12, color.a)
            boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 5)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, vectorCell)
            args.setAtIndex(ADDRESS, 2, doubleCell)
            args.setAtIndex(ADDRESS, 3, colorCell)
            args.setAtIndex(ADDRESS, 4, boolCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDVector2TwoDoubleColorBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        vector: Vector2,
        firstDouble: Double,
        secondDouble: Double,
        color: Color,
        enabled: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val vectorCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val double0 = arena.allocate(JAVA_DOUBLE)
            val double1 = arena.allocate(JAVA_DOUBLE)
            val colorCell = arena.allocate(16L, 4L)
            val boolCell = arena.allocate(JAVA_BYTE)
            ridCell.set(JAVA_LONG, 0, rid.value)
            writeVector2(vectorCell, vector)
            double0.set(JAVA_DOUBLE, 0, firstDouble)
            double1.set(JAVA_DOUBLE, 0, secondDouble)
            // Color components are fixed 32-bit floats, unlike scalar float ptrcall slots.
            colorCell.set(JAVA_FLOAT, 0, color.r)
            colorCell.set(JAVA_FLOAT, 4, color.g)
            colorCell.set(JAVA_FLOAT, 8, color.b)
            colorCell.set(JAVA_FLOAT, 12, color.a)
            boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 6)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, vectorCell)
            args.setAtIndex(ADDRESS, 2, double0)
            args.setAtIndex(ADDRESS, 3, double1)
            args.setAtIndex(ADDRESS, 4, colorCell)
            args.setAtIndex(ADDRESS, 5, boolCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDRect2BoolColorBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        rect: Rect2,
        tile: Boolean,
        color: Color,
        transpose: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val rectCell = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
            val tileCell = arena.allocate(JAVA_BYTE)
            val colorCell = arena.allocate(16L, 4L)
            val transposeCell = arena.allocate(JAVA_BYTE)
            ridCell.set(JAVA_LONG, 0, rid.value)
            writeRect2(rectCell, rect)
            tileCell.set(JAVA_BYTE, 0, if (tile) 1.toByte() else 0.toByte())
            // Color components are fixed 32-bit floats, unlike scalar float ptrcall slots.
            colorCell.set(JAVA_FLOAT, 0, color.r)
            colorCell.set(JAVA_FLOAT, 4, color.g)
            colorCell.set(JAVA_FLOAT, 8, color.b)
            colorCell.set(JAVA_FLOAT, 12, color.a)
            transposeCell.set(JAVA_BYTE, 0, if (transpose) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 5)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, rectCell)
            args.setAtIndex(ADDRESS, 2, tileCell)
            args.setAtIndex(ADDRESS, 3, colorCell)
            args.setAtIndex(ADDRESS, 4, transposeCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDRect2ColorBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        rect: Rect2,
        color: Color,
        enabled: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val rectCell = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
            val colorCell = arena.allocate(16L, 4L)
            val boolCell = arena.allocate(JAVA_BYTE)
            ridCell.set(JAVA_LONG, 0, rid.value)
            writeRect2(rectCell, rect)
            // Color components are fixed 32-bit floats, unlike scalar float ptrcall slots.
            colorCell.set(JAVA_FLOAT, 0, color.r)
            colorCell.set(JAVA_FLOAT, 4, color.g)
            colorCell.set(JAVA_FLOAT, 8, color.b)
            colorCell.set(JAVA_FLOAT, 12, color.a)
            boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 4)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, rectCell)
            args.setAtIndex(ADDRESS, 2, colorCell)
            args.setAtIndex(ADDRESS, 3, boolCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDRect2RIDBoolColorBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        rect: Rect2,
        texture: RID,
        tile: Boolean,
        color: Color,
        transpose: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val rectCell = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
            val textureCell = arena.allocate(JAVA_LONG)
            val tileCell = arena.allocate(JAVA_BYTE)
            val colorCell = arena.allocate(16L, 4L)
            val transposeCell = arena.allocate(JAVA_BYTE)
            ridCell.set(JAVA_LONG, 0, rid.value)
            writeRect2(rectCell, rect)
            textureCell.set(JAVA_LONG, 0, texture.value)
            tileCell.set(JAVA_BYTE, 0, if (tile) 1.toByte() else 0.toByte())
            // Color components are fixed 32-bit floats, unlike scalar float ptrcall slots.
            colorCell.set(JAVA_FLOAT, 0, color.r)
            colorCell.set(JAVA_FLOAT, 4, color.g)
            colorCell.set(JAVA_FLOAT, 8, color.b)
            colorCell.set(JAVA_FLOAT, 12, color.a)
            transposeCell.set(JAVA_BYTE, 0, if (transpose) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 6)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, rectCell)
            args.setAtIndex(ADDRESS, 2, textureCell)
            args.setAtIndex(ADDRESS, 3, tileCell)
            args.setAtIndex(ADDRESS, 4, colorCell)
            args.setAtIndex(ADDRESS, 5, transposeCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDRect2RIDRect2ColorTwoBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        rect: Rect2,
        texture: RID,
        srcRect: Rect2,
        color: Color,
        tile: Boolean,
        transpose: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val rectCell = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
            val textureCell = arena.allocate(JAVA_LONG)
            val srcRectCell = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
            val colorCell = arena.allocate(16L, 4L)
            val tileCell = arena.allocate(JAVA_BYTE)
            val transposeCell = arena.allocate(JAVA_BYTE)
            ridCell.set(JAVA_LONG, 0, rid.value)
            writeRect2(rectCell, rect)
            textureCell.set(JAVA_LONG, 0, texture.value)
            writeRect2(srcRectCell, srcRect)
            // Color components are fixed 32-bit floats, unlike scalar float ptrcall slots.
            colorCell.set(JAVA_FLOAT, 0, color.r)
            colorCell.set(JAVA_FLOAT, 4, color.g)
            colorCell.set(JAVA_FLOAT, 8, color.b)
            colorCell.set(JAVA_FLOAT, 12, color.a)
            tileCell.set(JAVA_BYTE, 0, if (tile) 1.toByte() else 0.toByte())
            transposeCell.set(JAVA_BYTE, 0, if (transpose) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 7)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, rectCell)
            args.setAtIndex(ADDRESS, 2, textureCell)
            args.setAtIndex(ADDRESS, 3, srcRectCell)
            args.setAtIndex(ADDRESS, 4, colorCell)
            args.setAtIndex(ADDRESS, 5, tileCell)
            args.setAtIndex(ADDRESS, 6, transposeCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDTwoRect2ColorTwoBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        first: Rect2,
        second: Rect2,
        color: Color,
        tile: Boolean,
        transpose: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val firstCell = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
            val secondCell = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
            val colorCell = arena.allocate(16L, 4L)
            val tileCell = arena.allocate(JAVA_BYTE)
            val transposeCell = arena.allocate(JAVA_BYTE)
            ridCell.set(JAVA_LONG, 0, rid.value)
            writeRect2(firstCell, first)
            writeRect2(secondCell, second)
            // Color components are fixed 32-bit floats, unlike scalar float ptrcall slots.
            colorCell.set(JAVA_FLOAT, 0, color.r)
            colorCell.set(JAVA_FLOAT, 4, color.g)
            colorCell.set(JAVA_FLOAT, 8, color.b)
            colorCell.set(JAVA_FLOAT, 12, color.a)
            tileCell.set(JAVA_BYTE, 0, if (tile) 1.toByte() else 0.toByte())
            transposeCell.set(JAVA_BYTE, 0, if (transpose) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 6)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, firstCell)
            args.setAtIndex(ADDRESS, 2, secondCell)
            args.setAtIndex(ADDRESS, 3, colorCell)
            args.setAtIndex(ADDRESS, 4, tileCell)
            args.setAtIndex(ADDRESS, 5, transposeCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDAndLongArgRetDouble(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Long,
    ): Double {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val valueCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            valueCell.set(JAVA_LONG, 0, value)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, valueCell)
            val ret = arena.allocate(JAVA_DOUBLE)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(JAVA_DOUBLE, 0)
        }
    }

    fun ptrcallWithRIDAndLongArgRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Long,
    ): Long {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val valueCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            valueCell.set(JAVA_LONG, 0, value)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, valueCell)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(JAVA_LONG, 0)
        }
    }

    fun ptrcallWithRIDAndLongArgRetVector2i(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Long,
    ): Vector2i {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val valueCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            valueCell.set(JAVA_LONG, 0, value)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, valueCell)
            val ret = arena.allocate(8L, 4L)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return Vector2i(
                x = ret.get(JAVA_INT, 0),
                y = ret.get(JAVA_INT, 4),
            )
        }
    }

    fun ptrcallWithRIDAndLongArgRetDictionary(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Long,
    ): Map<String, Any?> {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val valueCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            valueCell.set(JAVA_LONG, 0, value)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, valueCell)
            val ret = arena.allocate(8L, 8L)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return try {
                BuiltinTypes.readDictionaryScalars(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.DICTIONARY, ret)
            }
        }
    }

    fun ptrcallWithRIDAndTwoLongArgsRetRID(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        first: Long,
        second: Long,
    ): RID {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val arg0 = arena.allocate(JAVA_LONG)
            val arg1 = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            arg0.set(JAVA_LONG, 0, first)
            arg1.set(JAVA_LONG, 0, second)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, arg0)
            args.setAtIndex(ADDRESS, 2, arg1)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return RID(ret.get(JAVA_LONG, 0))
        }
    }

    fun ptrcallWithRIDAndTwoLongArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        first: Long,
        second: Long,
    ): Long {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val arg0 = arena.allocate(JAVA_LONG)
            val arg1 = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            arg0.set(JAVA_LONG, 0, first)
            arg1.set(JAVA_LONG, 0, second)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, arg0)
            args.setAtIndex(ADDRESS, 2, arg1)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(JAVA_LONG, 0)
        }
    }

    fun ptrcallWithRIDAndTwoLongArgsRetDictionary(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        first: Long,
        second: Long,
    ): Map<String, Any?> {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val arg0 = arena.allocate(JAVA_LONG)
            val arg1 = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            arg0.set(JAVA_LONG, 0, first)
            arg1.set(JAVA_LONG, 0, second)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, arg0)
            args.setAtIndex(ADDRESS, 2, arg1)
            val ret = arena.allocate(8L, 8L)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return try {
                BuiltinTypes.readDictionaryScalars(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.DICTIONARY, ret)
            }
        }
    }

    fun ptrcallWithRIDTwoLongArgsRetDouble(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        first: Long,
        second: Long,
    ): Double {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val arg0 = arena.allocate(JAVA_LONG)
            val arg1 = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            arg0.set(JAVA_LONG, 0, first)
            arg1.set(JAVA_LONG, 0, second)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, arg0)
            args.setAtIndex(ADDRESS, 2, arg1)
            val ret = arena.allocate(JAVA_DOUBLE)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(JAVA_DOUBLE, 0)
        }
    }

    fun ptrcallWithRIDTwoLongArgsRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        first: Long,
        second: Long,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val arg0 = arena.allocate(JAVA_LONG)
            val arg1 = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            arg0.set(JAVA_LONG, 0, first)
            arg1.set(JAVA_LONG, 0, second)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, arg0)
            args.setAtIndex(ADDRESS, 2, arg1)
            val ret = arena.allocate(JAVA_BYTE)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(JAVA_BYTE, 0) != 0.toByte()
        }
    }

    fun ptrcallWithRIDAndTwoLongArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        first: Long,
        second: Long,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val arg0 = arena.allocate(JAVA_LONG)
            val arg1 = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            arg0.set(JAVA_LONG, 0, first)
            arg1.set(JAVA_LONG, 0, second)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, arg0)
            args.setAtIndex(ADDRESS, 2, arg1)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (RID, int64/enum, int64/enum, scalar float) args.
     */
    fun ptrcallWithRIDTwoLongDoubleArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        first: Long,
        second: Long,
        value: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val arg0 = arena.allocate(JAVA_LONG)
            val arg1 = arena.allocate(JAVA_LONG)
            val doubleCell = arena.allocate(JAVA_DOUBLE)
            ridCell.set(JAVA_LONG, 0, rid.value)
            arg0.set(JAVA_LONG, 0, first)
            arg1.set(JAVA_LONG, 0, second)
            doubleCell.set(JAVA_DOUBLE, 0, value)
            val args = arena.allocate(ADDRESS, 4)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, arg0)
            args.setAtIndex(ADDRESS, 2, arg1)
            args.setAtIndex(ADDRESS, 3, doubleCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDTwoLongBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        first: Long,
        second: Long,
        enabled: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val arg0 = arena.allocate(JAVA_LONG)
            val arg1 = arena.allocate(JAVA_LONG)
            val boolCell = arena.allocate(JAVA_BYTE)
            ridCell.set(JAVA_LONG, 0, rid.value)
            arg0.set(JAVA_LONG, 0, first)
            arg1.set(JAVA_LONG, 0, second)
            boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 4)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, arg0)
            args.setAtIndex(ADDRESS, 2, arg1)
            args.setAtIndex(ADDRESS, 3, boolCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDAndLongArgRetVariantScalar(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Long,
    ): Any? {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val valueCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            valueCell.set(JAVA_LONG, 0, value)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, valueCell)
            return readVariantReturn(methodBind, instance, args, arena)
        }
    }

    fun ptrcallWithRIDAndStringNameArgRetVariantScalar(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        name: String,
    ): Any? {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, GodotStrings.makeStringName(name))
            return readVariantReturn(methodBind, instance, args, arena)
        }
    }

    fun ptrcallWithRIDAndLongArgRetString(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Long,
    ): String {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val valueCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            valueCell.set(JAVA_LONG, 0, value)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, valueCell)
            val ret = arena.allocate(8L, 8L)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return try {
                GodotStrings.readString(ret)
            } finally {
                GodotStrings.destroyString(ret)
            }
        }
    }

    fun ptrcallWithRIDAndIntArgRetString(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Int,
    ): String {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val valueCell = arena.allocate(JAVA_INT)
            ridCell.set(JAVA_LONG, 0, rid.value)
            valueCell.set(JAVA_INT, 0, value)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, valueCell)
            val ret = arena.allocate(8L, 8L)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return try {
                GodotStrings.readString(ret)
            } finally {
                GodotStrings.destroyString(ret)
            }
        }
    }

    fun ptrcallWithRIDAndLongArgRetRID(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Long,
    ): RID {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val valueCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            valueCell.set(JAVA_LONG, 0, value)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, valueCell)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return RID(ret.get(JAVA_LONG, 0))
        }
    }

    fun ptrcallWithRIDAndLongArgRetByteArray(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Long,
    ): ByteArray {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val valueCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            valueCell.set(JAVA_LONG, 0, value)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, valueCell)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return try {
                BuiltinTypes.readPackedByteArray(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, ret)
            }
        }
    }

    fun ptrcallWithRIDIntAndBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Int,
        enabled: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val intCell = arena.allocate(JAVA_INT)
            val boolCell = arena.allocate(JAVA_BYTE)
            ridCell.set(JAVA_LONG, 0, rid.value)
            intCell.set(JAVA_INT, 0, value)
            boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, intCell)
            args.setAtIndex(ADDRESS, 2, boolCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (RID, int32, bool, scalar float) args and no return value.
     */
    fun ptrcallWithRIDIntBoolDoubleArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Int,
        enabled: Boolean,
        amount: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val intCell = arena.allocate(JAVA_INT)
            val boolCell = arena.allocate(JAVA_BYTE)
            val doubleCell = arena.allocate(JAVA_DOUBLE)
            ridCell.set(JAVA_LONG, 0, rid.value)
            intCell.set(JAVA_INT, 0, value)
            boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
            doubleCell.set(JAVA_DOUBLE, 0, amount)
            val args = arena.allocate(ADDRESS, 4)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, intCell)
            args.setAtIndex(ADDRESS, 2, boolCell)
            args.setAtIndex(ADDRESS, 3, doubleCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDDoubleVector2TwoDoubleArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        firstDouble: Double,
        vector: Vector2,
        secondDouble: Double,
        thirdDouble: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val double0 = arena.allocate(JAVA_DOUBLE)
            val vectorCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val double1 = arena.allocate(JAVA_DOUBLE)
            val double2 = arena.allocate(JAVA_DOUBLE)
            ridCell.set(JAVA_LONG, 0, rid.value)
            double0.set(JAVA_DOUBLE, 0, firstDouble)
            writeVector2(vectorCell, vector)
            double1.set(JAVA_DOUBLE, 0, secondDouble)
            double2.set(JAVA_DOUBLE, 0, thirdDouble)
            val args = arena.allocate(ADDRESS, 5)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, double0)
            args.setAtIndex(ADDRESS, 2, vectorCell)
            args.setAtIndex(ADDRESS, 3, double1)
            args.setAtIndex(ADDRESS, 4, double2)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDLongAndBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Long,
        enabled: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val longCell = arena.allocate(JAVA_LONG)
            val boolCell = arena.allocate(JAVA_BYTE)
            ridCell.set(JAVA_LONG, 0, rid.value)
            longCell.set(JAVA_LONG, 0, value)
            boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, longCell)
            args.setAtIndex(ADDRESS, 2, boolCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDLongAndBoolArgsRetVector2(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Long,
        enabled: Boolean,
    ): Vector2 {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val longCell = arena.allocate(JAVA_LONG)
            val boolCell = arena.allocate(JAVA_BYTE)
            ridCell.set(JAVA_LONG, 0, rid.value)
            longCell.set(JAVA_LONG, 0, value)
            boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, longCell)
            args.setAtIndex(ADDRESS, 2, boolCell)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return Vector2(
                x = GodotReal.readIndex(ret, 0),
                y = GodotReal.readIndex(ret, 1),
            )
        }
    }

    fun ptrcallWithRIDLongAndBoolArgsRetVector3(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Long,
        enabled: Boolean,
    ): Vector3 {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val longCell = arena.allocate(JAVA_LONG)
            val boolCell = arena.allocate(JAVA_BYTE)
            ridCell.set(JAVA_LONG, 0, rid.value)
            longCell.set(JAVA_LONG, 0, value)
            boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, longCell)
            args.setAtIndex(ADDRESS, 2, boolCell)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return Vector3(
                x = GodotReal.readIndex(ret, 0),
                y = GodotReal.readIndex(ret, 1),
                z = GodotReal.readIndex(ret, 2),
            )
        }
    }

    fun ptrcallWithRIDAndTwoIntArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        first: Int,
        second: Int,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val intCell0 = arena.allocate(JAVA_INT)
            val intCell1 = arena.allocate(JAVA_INT)
            ridCell.set(JAVA_LONG, 0, rid.value)
            intCell0.set(JAVA_INT, 0, first)
            intCell1.set(JAVA_INT, 0, second)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, intCell0)
            args.setAtIndex(ADDRESS, 2, intCell1)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDLongAndDoubleArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        longValue: Long,
        doubleValue: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val longCell = arena.allocate(JAVA_LONG)
            val doubleCell = arena.allocate(JAVA_DOUBLE)
            ridCell.set(JAVA_LONG, 0, rid.value)
            longCell.set(JAVA_LONG, 0, longValue)
            doubleCell.set(JAVA_DOUBLE, 0, doubleValue)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, longCell)
            args.setAtIndex(ADDRESS, 2, doubleCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with no arguments and Object return value.
     */
    fun ptrcallNoArgsRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val ret = arena.allocate(ADDRESS)
            objectMethodBindPtrcall.invoke(methodBind, instance, MemorySegment.NULL, ret)
            return ret.get(ADDRESS, 0)
        }
    }

    /**
     * Calls [methodBind] with no args where the method returns a Variant containing an Object.
     */
    fun ptrcallNoArgsRetVariantObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val retVariant = arena.allocate(BuiltinTypes.VARIANT_SIZE, 8L)
            try {
                objectMethodBindPtrcall.invoke(methodBind, instance, MemorySegment.NULL, retVariant)
                val scratch = arena.allocate(ADDRESS)
                VariantConverters.variantToType(VariantType.OBJECT).invoke(scratch, retVariant)
                return scratch.get(ADDRESS, 0)
            } finally {
                BuiltinTypes.destroyVariant(retVariant)
            }
        }
    }

    /**
     * Calls [methodBind] with no arguments and decodes a StringName return value.
     */
    fun ptrcallNoArgsRetStringName(
        methodBind: MemorySegment,
        instance: MemorySegment,
    ): String {
        Arena.ofConfined().use { arena ->
            val ret = arena.allocate(8L, 8L)
            objectMethodBindPtrcall.invoke(methodBind, instance, MemorySegment.NULL, ret)
            return try {
                GodotStrings.readStringName(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.STRING_NAME, ret)
            }
        }
    }

    /**
     * Calls [methodBind] with a single String argument and no return value.
     */
    fun ptrcallWithStringArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: String,
    ) {
        Arena.ofConfined().use { arena ->
            val argString = arena.allocate(8L, 8L)
            try {
                GodotStrings.initString(argString, value)
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, argString)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(argString)
            }
        }
    }

    /**
     * Calls [methodBind] with two String args and no return value.
     */
    fun ptrcallWithTwoStringArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: String,
        second: String,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(8L, 8L)
            val arg1 = arena.allocate(8L, 8L)
            try {
                GodotStrings.initString(arg0, first)
                GodotStrings.initString(arg1, second)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, arg0)
                arr.setAtIndex(ADDRESS, 1, arg1)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(arg0)
                GodotStrings.destroyString(arg1)
            }
        }
    }

    fun ptrcallWithTwoStringAndBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: String,
        second: String,
        value: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(8L, 8L)
            val arg1 = arena.allocate(8L, 8L)
            try {
                GodotStrings.initString(arg0, first)
                GodotStrings.initString(arg1, second)
                val boolCell = arena.allocate(JAVA_BYTE)
                boolCell.set(JAVA_BYTE, 0, if (value) 1.toByte() else 0.toByte())
                val arr = arena.allocate(ADDRESS, 3)
                arr.setAtIndex(ADDRESS, 0, arg0)
                arr.setAtIndex(ADDRESS, 1, arg1)
                arr.setAtIndex(ADDRESS, 2, boolCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(arg1)
                GodotStrings.destroyString(arg0)
            }
        }
    }

    fun ptrcallWithThreeStringArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: String,
        second: String,
        third: String,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(8L, 8L)
            val arg1 = arena.allocate(8L, 8L)
            val arg2 = arena.allocate(8L, 8L)
            try {
                GodotStrings.initString(arg0, first)
                GodotStrings.initString(arg1, second)
                GodotStrings.initString(arg2, third)
                val arr = arena.allocate(ADDRESS, 3)
                arr.setAtIndex(ADDRESS, 0, arg0)
                arr.setAtIndex(ADDRESS, 1, arg1)
                arr.setAtIndex(ADDRESS, 2, arg2)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(arg0)
                GodotStrings.destroyString(arg1)
                GodotStrings.destroyString(arg2)
            }
        }
    }

    fun ptrcallWithThreeStringTwoLongArgsRetDictionary(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: String,
        second: String,
        third: String,
        firstLong: Long,
        secondLong: Long,
    ): Map<String, Any?> {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(8L, 8L)
            val arg1 = arena.allocate(8L, 8L)
            val arg2 = arena.allocate(8L, 8L)
            val arg3 = arena.allocate(JAVA_LONG)
            val arg4 = arena.allocate(JAVA_LONG)
            val ret = arena.allocate(8L, 8L)
            try {
                GodotStrings.initString(arg0, first)
                GodotStrings.initString(arg1, second)
                GodotStrings.initString(arg2, third)
                arg3.set(JAVA_LONG, 0, firstLong)
                arg4.set(JAVA_LONG, 0, secondLong)
                val arr = arena.allocate(ADDRESS, 5)
                arr.setAtIndex(ADDRESS, 0, arg0)
                arr.setAtIndex(ADDRESS, 1, arg1)
                arr.setAtIndex(ADDRESS, 2, arg2)
                arr.setAtIndex(ADDRESS, 3, arg3)
                arr.setAtIndex(ADDRESS, 4, arg4)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return BuiltinTypes.readDictionaryScalars(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.DICTIONARY, ret)
                GodotStrings.destroyString(arg0)
                GodotStrings.destroyString(arg1)
                GodotStrings.destroyString(arg2)
            }
        }
    }

    /**
     * Calls [methodBind] with (String, int64) args and no return value.
     */
    fun ptrcallWithStringAndLongArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        value: Long,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(8L, 8L)
            val arg1 = arena.allocate(JAVA_LONG)
            try {
                GodotStrings.initString(arg0, text)
                arg1.set(JAVA_LONG, 0, value)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, arg0)
                arr.setAtIndex(ADDRESS, 1, arg1)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(arg0)
            }
        }
    }

    fun ptrcallWithStringLongTwoIntArgsRetVector2i(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        longValue: Long,
        firstInt: Int,
        secondInt: Int,
    ): Vector2i {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(8L, 8L)
            val arg1 = arena.allocate(JAVA_LONG)
            val arg2 = arena.allocate(JAVA_INT)
            val arg3 = arena.allocate(JAVA_INT)
            try {
                GodotStrings.initString(arg0, text)
                arg1.set(JAVA_LONG, 0, longValue)
                arg2.set(JAVA_INT, 0, firstInt)
                arg3.set(JAVA_INT, 0, secondInt)
                val arr = arena.allocate(ADDRESS, 4)
                arr.setAtIndex(ADDRESS, 0, arg0)
                arr.setAtIndex(ADDRESS, 1, arg1)
                arr.setAtIndex(ADDRESS, 2, arg2)
                arr.setAtIndex(ADDRESS, 3, arg3)
                val ret = arena.allocate(8L, 4L)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return readVector2i(ret)
            } finally {
                GodotStrings.destroyString(arg0)
            }
        }
    }

    fun ptrcallWithStringLongDoubleIntThreeLongArgsRetVector2(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        alignment: Long,
        width: Double,
        fontSize: Int,
        justification: Long,
        direction: Long,
        orientation: Long,
    ): Vector2 {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val alignmentCell = arena.allocate(JAVA_LONG)
            val widthCell = arena.allocate(JAVA_DOUBLE)
            val fontSizeCell = arena.allocate(JAVA_INT)
            val justificationCell = arena.allocate(JAVA_LONG)
            val directionCell = arena.allocate(JAVA_LONG)
            val orientationCell = arena.allocate(JAVA_LONG)
            try {
                GodotStrings.initString(stringCell, text)
                alignmentCell.set(JAVA_LONG, 0, alignment)
                widthCell.set(JAVA_DOUBLE, 0, width)
                fontSizeCell.set(JAVA_INT, 0, fontSize)
                justificationCell.set(JAVA_LONG, 0, justification)
                directionCell.set(JAVA_LONG, 0, direction)
                orientationCell.set(JAVA_LONG, 0, orientation)
                val args = arena.allocate(ADDRESS, 7)
                args.setAtIndex(ADDRESS, 0, stringCell)
                args.setAtIndex(ADDRESS, 1, alignmentCell)
                args.setAtIndex(ADDRESS, 2, widthCell)
                args.setAtIndex(ADDRESS, 3, fontSizeCell)
                args.setAtIndex(ADDRESS, 4, justificationCell)
                args.setAtIndex(ADDRESS, 5, directionCell)
                args.setAtIndex(ADDRESS, 6, orientationCell)
                val ret = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return Vector2(
                    x = GodotReal.readIndex(ret, 0),
                    y = GodotReal.readIndex(ret, 1),
                )
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithStringLongDoubleTwoIntFourLongArgsRetVector2(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        alignment: Long,
        width: Double,
        fontSize: Int,
        maxLines: Int,
        breakFlags: Long,
        justification: Long,
        direction: Long,
        orientation: Long,
    ): Vector2 {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val alignmentCell = arena.allocate(JAVA_LONG)
            val widthCell = arena.allocate(JAVA_DOUBLE)
            val fontSizeCell = arena.allocate(JAVA_INT)
            val maxLinesCell = arena.allocate(JAVA_INT)
            val breakFlagsCell = arena.allocate(JAVA_LONG)
            val justificationCell = arena.allocate(JAVA_LONG)
            val directionCell = arena.allocate(JAVA_LONG)
            val orientationCell = arena.allocate(JAVA_LONG)
            try {
                GodotStrings.initString(stringCell, text)
                alignmentCell.set(JAVA_LONG, 0, alignment)
                widthCell.set(JAVA_DOUBLE, 0, width)
                fontSizeCell.set(JAVA_INT, 0, fontSize)
                maxLinesCell.set(JAVA_INT, 0, maxLines)
                breakFlagsCell.set(JAVA_LONG, 0, breakFlags)
                justificationCell.set(JAVA_LONG, 0, justification)
                directionCell.set(JAVA_LONG, 0, direction)
                orientationCell.set(JAVA_LONG, 0, orientation)
                val args = arena.allocate(ADDRESS, 9)
                args.setAtIndex(ADDRESS, 0, stringCell)
                args.setAtIndex(ADDRESS, 1, alignmentCell)
                args.setAtIndex(ADDRESS, 2, widthCell)
                args.setAtIndex(ADDRESS, 3, fontSizeCell)
                args.setAtIndex(ADDRESS, 4, maxLinesCell)
                args.setAtIndex(ADDRESS, 5, breakFlagsCell)
                args.setAtIndex(ADDRESS, 6, justificationCell)
                args.setAtIndex(ADDRESS, 7, directionCell)
                args.setAtIndex(ADDRESS, 8, orientationCell)
                val ret = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return Vector2(
                    x = GodotReal.readIndex(ret, 0),
                    y = GodotReal.readIndex(ret, 1),
                )
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    /**
     * Calls [methodBind] with (String, int32) args and no return value.
     */
    fun ptrcallWithStringAndIntArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        value: Int,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(8L, 8L)
            val arg1 = arena.allocate(JAVA_INT)
            try {
                GodotStrings.initString(arg0, text)
                arg1.set(JAVA_INT, 0, value)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, arg0)
                arr.setAtIndex(ADDRESS, 1, arg1)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(arg0)
            }
        }
    }

    fun ptrcallWithStringIntAndStringArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        intValue: Int,
        stringValue: String,
    ) {
        Arena.ofConfined().use { arena ->
            val textCell = arena.allocate(8L, 8L)
            val intCell = arena.allocate(JAVA_INT)
            val stringCell = arena.allocate(8L, 8L)
            try {
                GodotStrings.initString(textCell, text)
                intCell.set(JAVA_INT, 0, intValue)
                GodotStrings.initString(stringCell, stringValue)
                val arr = arena.allocate(ADDRESS, 3)
                arr.setAtIndex(ADDRESS, 0, textCell)
                arr.setAtIndex(ADDRESS, 1, intCell)
                arr.setAtIndex(ADDRESS, 2, stringCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(textCell)
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithStringIntAndIntArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        firstInt: Int,
        secondInt: Int,
    ) {
        Arena.ofConfined().use { arena ->
            val textCell = arena.allocate(8L, 8L)
            val firstIntCell = arena.allocate(JAVA_INT)
            val secondIntCell = arena.allocate(JAVA_INT)
            try {
                GodotStrings.initString(textCell, text)
                firstIntCell.set(JAVA_INT, 0, firstInt)
                secondIntCell.set(JAVA_INT, 0, secondInt)
                val arr = arena.allocate(ADDRESS, 3)
                arr.setAtIndex(ADDRESS, 0, textCell)
                arr.setAtIndex(ADDRESS, 1, firstIntCell)
                arr.setAtIndex(ADDRESS, 2, secondIntCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(textCell)
            }
        }
    }

    fun ptrcallWithStringIntAndLongArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        intValue: Int,
        longValue: Long,
    ) {
        Arena.ofConfined().use { arena ->
            val textCell = arena.allocate(8L, 8L)
            val intCell = arena.allocate(JAVA_INT)
            val longCell = arena.allocate(JAVA_LONG)
            try {
                GodotStrings.initString(textCell, text)
                intCell.set(JAVA_INT, 0, intValue)
                longCell.set(JAVA_LONG, 0, longValue)
                val arr = arena.allocate(ADDRESS, 3)
                arr.setAtIndex(ADDRESS, 0, textCell)
                arr.setAtIndex(ADDRESS, 1, intCell)
                arr.setAtIndex(ADDRESS, 2, longCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(textCell)
            }
        }
    }

    fun ptrcallWithStringAndIntArgRetString(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        value: Int,
    ): String {
        Arena.ofConfined().use { arena ->
            val textCell = arena.allocate(8L, 8L)
            val intCell = arena.allocate(JAVA_INT)
            val retString = arena.allocate(8L, 8L)
            try {
                GodotStrings.initString(textCell, text)
                intCell.set(JAVA_INT, 0, value)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, textCell)
                arr.setAtIndex(ADDRESS, 1, intCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, retString)
                return GodotStrings.readString(retString)
            } finally {
                GodotStrings.destroyString(textCell)
                GodotStrings.destroyString(retString)
            }
        }
    }

    fun ptrcallWithStringAndIntArgRetVector2i(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        value: Int,
    ): Vector2i {
        Arena.ofConfined().use { arena ->
            val textCell = arena.allocate(8L, 8L)
            val intCell = arena.allocate(JAVA_INT)
            try {
                GodotStrings.initString(textCell, text)
                intCell.set(JAVA_INT, 0, value)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, textCell)
                arr.setAtIndex(ADDRESS, 1, intCell)
                val ret = arena.allocate(8L, 4L)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return Vector2i(
                    x = ret.get(JAVA_INT, 0),
                    y = ret.get(JAVA_INT, 4),
                )
            } finally {
                GodotStrings.destroyString(textCell)
            }
        }
    }

    fun ptrcallWithStringIntByteArrayArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        value: Int,
        bytes: ByteArray,
    ) {
        Arena.ofConfined().use { arena ->
            val textCell = arena.allocate(8L, 8L)
            val intCell = arena.allocate(JAVA_INT)
            val byteArray = BuiltinTypes.allocatePackedArray(arena)
            try {
                GodotStrings.initString(textCell, text)
                intCell.set(JAVA_INT, 0, value)
                BuiltinTypes.initPackedByteArray(byteArray, bytes)
                val arr = arena.allocate(ADDRESS, 3)
                arr.setAtIndex(ADDRESS, 0, textCell)
                arr.setAtIndex(ADDRESS, 1, intCell)
                arr.setAtIndex(ADDRESS, 2, byteArray)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, byteArray)
                GodotStrings.destroyString(textCell)
            }
        }
    }

    /**
     * Calls [methodBind] with (String, int64) args and Object return value.
     */
    fun ptrcallWithStringAndLongArgsRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        value: Long,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(8L, 8L)
            val arg1 = arena.allocate(JAVA_LONG)
            try {
                GodotStrings.initString(arg0, text)
                arg1.set(JAVA_LONG, 0, value)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, arg0)
                arr.setAtIndex(ADDRESS, 1, arg1)
                val ret = arena.allocate(ADDRESS)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(ADDRESS, 0)
            } finally {
                GodotStrings.destroyString(arg0)
            }
        }
    }

    fun ptrcallWithStringLongStringArgsRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstString: String,
        value: Long,
        secondString: String,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val firstStringCell = arena.allocate(8L, 8L)
            val valueCell = arena.allocate(JAVA_LONG)
            val secondStringCell = arena.allocate(8L, 8L)
            try {
                GodotStrings.initString(firstStringCell, firstString)
                valueCell.set(JAVA_LONG, 0, value)
                GodotStrings.initString(secondStringCell, secondString)
                val arr = arena.allocate(ADDRESS, 3)
                arr.setAtIndex(ADDRESS, 0, firstStringCell)
                arr.setAtIndex(ADDRESS, 1, valueCell)
                arr.setAtIndex(ADDRESS, 2, secondStringCell)
                val ret = arena.allocate(ADDRESS)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(ADDRESS, 0)
            } finally {
                GodotStrings.destroyString(secondStringCell)
                GodotStrings.destroyString(firstStringCell)
            }
        }
    }

    fun ptrcallWithStringTwoLongArgsRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        firstValue: Long,
        secondValue: Long,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val firstCell = arena.allocate(JAVA_LONG)
            val secondCell = arena.allocate(JAVA_LONG)
            try {
                GodotStrings.initString(stringCell, text)
                firstCell.set(JAVA_LONG, 0, firstValue)
                secondCell.set(JAVA_LONG, 0, secondValue)
                val arr = arena.allocate(ADDRESS, 3)
                arr.setAtIndex(ADDRESS, 0, stringCell)
                arr.setAtIndex(ADDRESS, 1, firstCell)
                arr.setAtIndex(ADDRESS, 2, secondCell)
                val ret = arena.allocate(ADDRESS)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(ADDRESS, 0)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithStringLongByteArrayByteArrayArgsRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        value: Long,
        firstBytes: ByteArray,
        secondBytes: ByteArray,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val valueCell = arena.allocate(JAVA_LONG)
            val firstArray = BuiltinTypes.allocatePackedArray(arena)
            val secondArray = BuiltinTypes.allocatePackedArray(arena)
            try {
                GodotStrings.initString(stringCell, text)
                valueCell.set(JAVA_LONG, 0, value)
                BuiltinTypes.initPackedByteArray(firstArray, firstBytes)
                BuiltinTypes.initPackedByteArray(secondArray, secondBytes)
                val arr = arena.allocate(ADDRESS, 4)
                arr.setAtIndex(ADDRESS, 0, stringCell)
                arr.setAtIndex(ADDRESS, 1, valueCell)
                arr.setAtIndex(ADDRESS, 2, firstArray)
                arr.setAtIndex(ADDRESS, 3, secondArray)
                val ret = arena.allocate(ADDRESS)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(ADDRESS, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, secondArray)
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, firstArray)
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithLongTwoStringBoolArgsRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Long,
        firstString: String,
        secondString: String,
        flag: Boolean,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val valueCell = arena.allocate(JAVA_LONG)
            val firstStringCell = arena.allocate(8L, 8L)
            val secondStringCell = arena.allocate(8L, 8L)
            val flagCell = arena.allocate(JAVA_BYTE)
            try {
                valueCell.set(JAVA_LONG, 0, value)
                GodotStrings.initString(firstStringCell, firstString)
                GodotStrings.initString(secondStringCell, secondString)
                flagCell.set(JAVA_BYTE, 0, if (flag) 1 else 0)
                val arr = arena.allocate(ADDRESS, 4)
                arr.setAtIndex(ADDRESS, 0, valueCell)
                arr.setAtIndex(ADDRESS, 1, firstStringCell)
                arr.setAtIndex(ADDRESS, 2, secondStringCell)
                arr.setAtIndex(ADDRESS, 3, flagCell)
                val ret = arena.allocate(ADDRESS)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(ADDRESS, 0)
            } finally {
                GodotStrings.destroyString(secondStringCell)
                GodotStrings.destroyString(firstStringCell)
            }
        }
    }

    /**
     * Calls [methodBind] with (String, String, int64) args and Object return value.
     */
    fun ptrcallWithTwoStringAndLongArgsRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: String,
        second: String,
        value: Long,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(8L, 8L)
            val arg1 = arena.allocate(8L, 8L)
            val arg2 = arena.allocate(JAVA_LONG)
            try {
                GodotStrings.initString(arg0, first)
                GodotStrings.initString(arg1, second)
                arg2.set(JAVA_LONG, 0, value)
                val arr = arena.allocate(ADDRESS, 3)
                arr.setAtIndex(ADDRESS, 0, arg0)
                arr.setAtIndex(ADDRESS, 1, arg1)
                arr.setAtIndex(ADDRESS, 2, arg2)
                val ret = arena.allocate(ADDRESS)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(ADDRESS, 0)
            } finally {
                GodotStrings.destroyString(arg0)
                GodotStrings.destroyString(arg1)
            }
        }
    }

    /**
     * Calls [methodBind] with (String, bool) args and no return value.
     */
    fun ptrcallWithStringAndBoolArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        value: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(8L, 8L)
            val arg1 = arena.allocate(java.lang.foreign.ValueLayout.JAVA_BYTE)
            try {
                GodotStrings.initString(arg0, text)
                arg1.set(java.lang.foreign.ValueLayout.JAVA_BYTE, 0, if (value) 1.toByte() else 0.toByte())
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, arg0)
                arr.setAtIndex(ADDRESS, 1, arg1)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(arg0)
            }
        }
    }

    /**
     * Calls [methodBind] with one bool arg and no return value.
     */
    fun ptrcallWithBoolArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Boolean,
    ) {
        objectMethodBindPtrcall.invoke(methodBind, instance, ptrcallScratch.get().setBoolArg(value), MemorySegment.NULL)
    }

    fun ptrcallWithBoolArgRetArray(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Boolean,
    ): List<Any?> {
        val scratch = ptrcallScratch.get()
        objectMethodBindPtrcall.invoke(methodBind, instance, scratch.setBoolArg(value), scratch.arrayRet)
        return try {
            BuiltinTypes.readArrayScalars(scratch.arrayRet)
        } finally {
            BuiltinTypes.destroyTyped(VariantType.ARRAY, scratch.arrayRet)
        }
    }

    /**
     * Calls [methodBind] with one bool arg and int64 return value.
     */
    fun ptrcallWithBoolArgRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Boolean,
    ): Long {
        val scratch = ptrcallScratch.get()
        objectMethodBindPtrcall.invoke(methodBind, instance, scratch.setBoolArg(value), scratch.longRet)
        return scratch.longRet.get(JAVA_LONG, 0)
    }

    /**
     * Calls [methodBind] with one bool arg and RID return value.
     */
    fun ptrcallWithBoolArgRetRID(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Boolean,
    ): RID {
        val scratch = ptrcallScratch.get()
        objectMethodBindPtrcall.invoke(methodBind, instance, scratch.setBoolArg(value), scratch.longRet)
        return RID(scratch.longRet.get(JAVA_LONG, 0))
    }

    /**
     * Calls [methodBind] with one bool arg and decodes a Variant return.
     */
    fun ptrcallWithBoolArgRetVariantScalar(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Boolean,
    ): Any? {
        Arena.ofConfined().use { arena ->
            val boolCell = arena.allocate(JAVA_BYTE)
            boolCell.set(JAVA_BYTE, 0, if (value) 1.toByte() else 0.toByte())
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, boolCell)
            return readVariantReturn(methodBind, instance, arr, arena)
        }
    }

    /**
     * Calls [methodBind] with one bool arg and int32 return value.
     */
    fun ptrcallWithBoolArgRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Boolean,
    ): Int {
        val scratch = ptrcallScratch.get()
        objectMethodBindPtrcall.invoke(methodBind, instance, scratch.setBoolArg(value), scratch.intRet)
        return scratch.intRet.get(JAVA_INT, 0)
    }

    /**
     * Calls [methodBind] with one bool arg and bool return value.
     */
    fun ptrcallWithBoolArgRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Boolean,
    ): Boolean {
        val scratch = ptrcallScratch.get()
        objectMethodBindPtrcall.invoke(methodBind, instance, scratch.setBoolArg(value), scratch.byteRet)
        return scratch.byteRet.get(JAVA_BYTE, 0) != 0.toByte()
    }

    /**
     * Calls [methodBind] with one bool arg and decodes PackedByteArray return.
     */
    fun ptrcallWithBoolArgRetByteArray(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Boolean,
    ): ByteArray {
        val scratch = ptrcallScratch.get()
        objectMethodBindPtrcall.invoke(methodBind, instance, scratch.setBoolArg(value), scratch.packedArrayRet)
        return try {
            BuiltinTypes.readPackedByteArray(scratch.packedArrayRet)
        } finally {
            BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, scratch.packedArrayRet)
        }
    }

    /**
     * Calls [methodBind] with one bool arg and decodes PackedInt32Array return.
     */
    fun ptrcallWithBoolArgRetPackedInt32List(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Boolean,
    ): List<Int> {
        val scratch = ptrcallScratch.get()
        objectMethodBindPtrcall.invoke(methodBind, instance, scratch.setBoolArg(value), scratch.packedArrayRet)
        return try {
            BuiltinTypes.readPackedInt32Array(scratch.packedArrayRet)
        } finally {
            BuiltinTypes.destroyTyped(VariantType.PACKED_INT32_ARRAY, scratch.packedArrayRet)
        }
    }

    /**
     * Calls [methodBind] with two bool args and no return value.
     */
    fun ptrcallWithTwoBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Boolean,
        second: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val bool0 = arena.allocate(java.lang.foreign.ValueLayout.JAVA_BYTE)
            val bool1 = arena.allocate(java.lang.foreign.ValueLayout.JAVA_BYTE)
            bool0.set(java.lang.foreign.ValueLayout.JAVA_BYTE, 0, if (first) 1.toByte() else 0.toByte())
            bool1.set(java.lang.foreign.ValueLayout.JAVA_BYTE, 0, if (second) 1.toByte() else 0.toByte())
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, bool0)
            arr.setAtIndex(ADDRESS, 1, bool1)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (Object, bool, bool) and no return value.
     */
    fun ptrcallWithObjectAndTwoBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        first: Boolean,
        second: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 3)
            val objCell = arena.allocate(ADDRESS)
            objCell.set(ADDRESS, 0, objectArg)
            arr.setAtIndex(ADDRESS, 0, objCell)
            val bool0 = arena.allocate(java.lang.foreign.ValueLayout.JAVA_BYTE)
            val bool1 = arena.allocate(java.lang.foreign.ValueLayout.JAVA_BYTE)
            bool0.set(java.lang.foreign.ValueLayout.JAVA_BYTE, 0, if (first) 1.toByte() else 0.toByte())
            bool1.set(java.lang.foreign.ValueLayout.JAVA_BYTE, 0, if (second) 1.toByte() else 0.toByte())
            arr.setAtIndex(ADDRESS, 1, bool0)
            arr.setAtIndex(ADDRESS, 2, bool1)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (Object*, bool, int64) args and no return value.
     */
    fun ptrcallWithObjectBoolLongArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        boolArg: Boolean,
        longArg: Long,
    ) {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 3)
            val objCell = arena.allocate(ADDRESS)
            objCell.set(ADDRESS, 0, objectArg)
            val boolCell = arena.allocate(java.lang.foreign.ValueLayout.JAVA_BYTE)
            boolCell.set(java.lang.foreign.ValueLayout.JAVA_BYTE, 0, if (boolArg) 1.toByte() else 0.toByte())
            val longCell = arena.allocate(JAVA_LONG)
            longCell.set(JAVA_LONG, 0, longArg)
            arr.setAtIndex(ADDRESS, 0, objCell)
            arr.setAtIndex(ADDRESS, 1, boolCell)
            arr.setAtIndex(ADDRESS, 2, longCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithObjectBoolStringArgsRetDictionary(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        boolArg: Boolean,
        text: String,
    ): Map<String, Any?> {
        Arena.ofConfined().use { arena ->
            val objCell = arena.allocate(ADDRESS)
            val boolCell = arena.allocate(JAVA_BYTE)
            val stringCell = arena.allocate(8L, 8L)
            objCell.set(ADDRESS, 0, objectArg)
            boolCell.set(JAVA_BYTE, 0, if (boolArg) 1.toByte() else 0.toByte())
            GodotStrings.initString(stringCell, text)
            try {
                val arr = arena.allocate(ADDRESS, 3)
                arr.setAtIndex(ADDRESS, 0, objCell)
                arr.setAtIndex(ADDRESS, 1, boolCell)
                arr.setAtIndex(ADDRESS, 2, stringCell)
                val ret = arena.allocate(8L, 8L)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return try {
                    BuiltinTypes.readDictionaryScalars(ret)
                } finally {
                    BuiltinTypes.destroyTyped(VariantType.DICTIONARY, ret)
                }
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithObjectBoolStringBoolArgsRetDictionary(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        boolArg: Boolean,
        text: String,
        secondBoolArg: Boolean,
    ): Map<String, Any?> {
        Arena.ofConfined().use { arena ->
            val objCell = arena.allocate(ADDRESS)
            val boolCell = arena.allocate(JAVA_BYTE)
            val stringCell = arena.allocate(8L, 8L)
            val secondBoolCell = arena.allocate(JAVA_BYTE)
            objCell.set(ADDRESS, 0, objectArg)
            boolCell.set(JAVA_BYTE, 0, if (boolArg) 1.toByte() else 0.toByte())
            secondBoolCell.set(JAVA_BYTE, 0, if (secondBoolArg) 1.toByte() else 0.toByte())
            GodotStrings.initString(stringCell, text)
            try {
                val arr = arena.allocate(ADDRESS, 4)
                arr.setAtIndex(ADDRESS, 0, objCell)
                arr.setAtIndex(ADDRESS, 1, boolCell)
                arr.setAtIndex(ADDRESS, 2, stringCell)
                arr.setAtIndex(ADDRESS, 3, secondBoolCell)
                val ret = arena.allocate(8L, 8L)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return try {
                    BuiltinTypes.readDictionaryScalars(ret)
                } finally {
                    BuiltinTypes.destroyTyped(VariantType.DICTIONARY, ret)
                }
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithObjectBoolStringLongArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        boolArg: Boolean,
        text: String,
        longArg: Long,
    ): Long {
        Arena.ofConfined().use { arena ->
            val objCell = arena.allocate(ADDRESS)
            val boolCell = arena.allocate(JAVA_BYTE)
            val stringCell = arena.allocate(8L, 8L)
            val longCell = arena.allocate(JAVA_LONG)
            objCell.set(ADDRESS, 0, objectArg)
            boolCell.set(JAVA_BYTE, 0, if (boolArg) 1.toByte() else 0.toByte())
            longCell.set(JAVA_LONG, 0, longArg)
            GodotStrings.initString(stringCell, text)
            try {
                val arr = arena.allocate(ADDRESS, 4)
                arr.setAtIndex(ADDRESS, 0, objCell)
                arr.setAtIndex(ADDRESS, 1, boolCell)
                arr.setAtIndex(ADDRESS, 2, stringCell)
                arr.setAtIndex(ADDRESS, 3, longCell)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithObjectBoolStringPackedStringListLongArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        boolArg: Boolean,
        text: String,
        values: List<String>,
        longArg: Long,
    ): Long {
        Arena.ofConfined().use { arena ->
            val objCell = arena.allocate(ADDRESS)
            val boolCell = arena.allocate(JAVA_BYTE)
            val stringCell = arena.allocate(8L, 8L)
            val packedCell = BuiltinTypes.allocatePackedArray(arena)
            val longCell = arena.allocate(JAVA_LONG)
            objCell.set(ADDRESS, 0, objectArg)
            boolCell.set(JAVA_BYTE, 0, if (boolArg) 1.toByte() else 0.toByte())
            longCell.set(JAVA_LONG, 0, longArg)
            GodotStrings.initString(stringCell, text)
            BuiltinTypes.initPackedStringArray(packedCell, values)
            try {
                val arr = arena.allocate(ADDRESS, 5)
                arr.setAtIndex(ADDRESS, 0, objCell)
                arr.setAtIndex(ADDRESS, 1, boolCell)
                arr.setAtIndex(ADDRESS, 2, stringCell)
                arr.setAtIndex(ADDRESS, 3, packedCell)
                arr.setAtIndex(ADDRESS, 4, longCell)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_STRING_ARRAY, packedCell)
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    /**
     * Calls [methodBind] with (Object*, int32, bool) args and no return value.
     */
    fun ptrcallWithObjectIntBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        intArg: Int,
        boolArg: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val objCell = arena.allocate(ADDRESS)
            val intCell = arena.allocate(JAVA_INT)
            val boolCell = arena.allocate(JAVA_BYTE)
            objCell.set(ADDRESS, 0, objectArg)
            intCell.set(JAVA_INT, 0, intArg)
            boolCell.set(JAVA_BYTE, 0, if (boolArg) 1.toByte() else 0.toByte())
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, objCell)
            arr.setAtIndex(ADDRESS, 1, intCell)
            arr.setAtIndex(ADDRESS, 2, boolCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (Object*, int32, bool, bool) args and no return value.
     */
    fun ptrcallWithObjectIntTwoBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        intArg: Int,
        first: Boolean,
        second: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val objCell = arena.allocate(ADDRESS)
            val intCell = arena.allocate(JAVA_INT)
            val boolCell0 = arena.allocate(JAVA_BYTE)
            val boolCell1 = arena.allocate(JAVA_BYTE)
            objCell.set(ADDRESS, 0, objectArg)
            intCell.set(JAVA_INT, 0, intArg)
            boolCell0.set(JAVA_BYTE, 0, if (first) 1.toByte() else 0.toByte())
            boolCell1.set(JAVA_BYTE, 0, if (second) 1.toByte() else 0.toByte())
            val arr = arena.allocate(ADDRESS, 4)
            arr.setAtIndex(ADDRESS, 0, objCell)
            arr.setAtIndex(ADDRESS, 1, intCell)
            arr.setAtIndex(ADDRESS, 2, boolCell0)
            arr.setAtIndex(ADDRESS, 3, boolCell1)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (Object*, Object*, int32, bool) args and no return value.
     */
    fun ptrcallWithObjectObjectIntBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstObjectArg: MemorySegment,
        secondObjectArg: MemorySegment,
        intArg: Int,
        boolArg: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val objCell0 = arena.allocate(ADDRESS)
            val objCell1 = arena.allocate(ADDRESS)
            val intCell = arena.allocate(JAVA_INT)
            val boolCell = arena.allocate(JAVA_BYTE)
            objCell0.set(ADDRESS, 0, firstObjectArg)
            objCell1.set(ADDRESS, 0, secondObjectArg)
            intCell.set(JAVA_INT, 0, intArg)
            boolCell.set(JAVA_BYTE, 0, if (boolArg) 1.toByte() else 0.toByte())
            val arr = arena.allocate(ADDRESS, 4)
            arr.setAtIndex(ADDRESS, 0, objCell0)
            arr.setAtIndex(ADDRESS, 1, objCell1)
            arr.setAtIndex(ADDRESS, 2, intCell)
            arr.setAtIndex(ADDRESS, 3, boolCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (Object*, Object*, int32, bool, bool) args and no return value.
     */
    fun ptrcallWithObjectObjectIntTwoBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstObjectArg: MemorySegment,
        secondObjectArg: MemorySegment,
        intArg: Int,
        first: Boolean,
        second: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val objCell0 = arena.allocate(ADDRESS)
            val objCell1 = arena.allocate(ADDRESS)
            val intCell = arena.allocate(JAVA_INT)
            val boolCell0 = arena.allocate(JAVA_BYTE)
            val boolCell1 = arena.allocate(JAVA_BYTE)
            objCell0.set(ADDRESS, 0, firstObjectArg)
            objCell1.set(ADDRESS, 0, secondObjectArg)
            intCell.set(JAVA_INT, 0, intArg)
            boolCell0.set(JAVA_BYTE, 0, if (first) 1.toByte() else 0.toByte())
            boolCell1.set(JAVA_BYTE, 0, if (second) 1.toByte() else 0.toByte())
            val arr = arena.allocate(ADDRESS, 5)
            arr.setAtIndex(ADDRESS, 0, objCell0)
            arr.setAtIndex(ADDRESS, 1, objCell1)
            arr.setAtIndex(ADDRESS, 2, intCell)
            arr.setAtIndex(ADDRESS, 3, boolCell0)
            arr.setAtIndex(ADDRESS, 4, boolCell1)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithTwoObjectIntArgsRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstObjectArg: MemorySegment,
        secondObjectArg: MemorySegment,
        intArg: Int,
    ): Int {
        Arena.ofConfined().use { arena ->
            val objCell0 = arena.allocate(ADDRESS)
            val objCell1 = arena.allocate(ADDRESS)
            val intCell = arena.allocate(JAVA_INT)
            objCell0.set(ADDRESS, 0, firstObjectArg)
            objCell1.set(ADDRESS, 0, secondObjectArg)
            intCell.set(JAVA_INT, 0, intArg)
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, objCell0)
            arr.setAtIndex(ADDRESS, 1, objCell1)
            arr.setAtIndex(ADDRESS, 2, intCell)
            val ret = arena.allocate(JAVA_INT)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_INT, 0)
        }
    }

    /**
     * Calls [methodBind] with (Object*, String, int32, int64) args and no return value.
     */
    fun ptrcallWithObjectStringIntLongArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        text: String,
        intArg: Int,
        longArg: Long,
    ) {
        Arena.ofConfined().use { arena ->
            val objCell = arena.allocate(ADDRESS)
            val stringCell = arena.allocate(8L, 8L)
            val intCell = arena.allocate(JAVA_INT)
            val longCell = arena.allocate(JAVA_LONG)
            try {
                objCell.set(ADDRESS, 0, objectArg)
                GodotStrings.initString(stringCell, text)
                intCell.set(JAVA_INT, 0, intArg)
                longCell.set(JAVA_LONG, 0, longArg)
                val arr = arena.allocate(ADDRESS, 4)
                arr.setAtIndex(ADDRESS, 0, objCell)
                arr.setAtIndex(ADDRESS, 1, stringCell)
                arr.setAtIndex(ADDRESS, 2, intCell)
                arr.setAtIndex(ADDRESS, 3, longCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    /**
     * Calls [methodBind] with (Object*, uint64) args and Object return value.
     */
    fun ptrcallWithObjectAndLongArgsRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        longArg: Long,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 2)
            val objCell = arena.allocate(ADDRESS)
            objCell.set(ADDRESS, 0, objectArg)
            val longCell = arena.allocate(JAVA_LONG)
            longCell.set(JAVA_LONG, 0, longArg)
            arr.setAtIndex(ADDRESS, 0, objCell)
            arr.setAtIndex(ADDRESS, 1, longCell)
            val ret = arena.allocate(ADDRESS)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(ADDRESS, 0)
        }
    }

    /**
     * Calls [methodBind] with two RID args and no return value.
     */
    fun ptrcallWithTwoRIDArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: RID,
        second: RID,
    ) {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(JAVA_LONG)
            firstCell.set(JAVA_LONG, 0, first.value)
            val secondCell = arena.allocate(JAVA_LONG)
            secondCell.set(JAVA_LONG, 0, second.value)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, firstCell)
            arr.setAtIndex(ADDRESS, 1, secondCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithThreeRIDArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: RID,
        second: RID,
        third: RID,
    ) {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(JAVA_LONG)
            val secondCell = arena.allocate(JAVA_LONG)
            val thirdCell = arena.allocate(JAVA_LONG)
            firstCell.set(JAVA_LONG, 0, first.value)
            secondCell.set(JAVA_LONG, 0, second.value)
            thirdCell.set(JAVA_LONG, 0, third.value)
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, firstCell)
            arr.setAtIndex(ADDRESS, 1, secondCell)
            arr.setAtIndex(ADDRESS, 2, thirdCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDRIDTransform3DRIDTransform3DArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstRid: RID,
        secondRid: RID,
        firstTransform: Transform3D,
        thirdRid: RID,
        secondTransform: Transform3D,
    ) {
        Arena.ofConfined().use { arena ->
            val rid0 = arena.allocate(JAVA_LONG)
            val rid1 = arena.allocate(JAVA_LONG)
            val transform0 = arena.allocate(GodotReal.SIZE_BYTES * 12, GodotReal.ALIGN_BYTES)
            val rid2 = arena.allocate(JAVA_LONG)
            val transform1 = arena.allocate(GodotReal.SIZE_BYTES * 12, GodotReal.ALIGN_BYTES)
            rid0.set(JAVA_LONG, 0, firstRid.value)
            rid1.set(JAVA_LONG, 0, secondRid.value)
            writeTransform3D(transform0, firstTransform)
            rid2.set(JAVA_LONG, 0, thirdRid.value)
            writeTransform3D(transform1, secondTransform)
            val args = arena.allocate(ADDRESS, 5)
            args.setAtIndex(ADDRESS, 0, rid0)
            args.setAtIndex(ADDRESS, 1, rid1)
            args.setAtIndex(ADDRESS, 2, transform0)
            args.setAtIndex(ADDRESS, 3, rid2)
            args.setAtIndex(ADDRESS, 4, transform1)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (Object, int64, Vector2) and no return value.
     */
    fun ptrcallWithObjectLongAndVector2Arg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        longArg: Long,
        vector2Arg: Vector2,
    ) {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 3)

            val objCell = arena.allocate(ADDRESS)
            objCell.set(ADDRESS, 0, objectArg)
            arr.setAtIndex(ADDRESS, 0, objCell)

            val intCell = arena.allocate(JAVA_LONG)
            intCell.set(JAVA_LONG, 0, longArg)
            arr.setAtIndex(ADDRESS, 1, intCell)

            val vecCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            GodotReal.writeIndex(vecCell, 0, vector2Arg.x)
            GodotReal.writeIndex(vecCell, 1, vector2Arg.y)
            arr.setAtIndex(ADDRESS, 2, vecCell)

            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithLongObjectVector2IntArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        longArg: Long,
        objectArg: MemorySegment,
        vector2Arg: Vector2,
        intArg: Int,
    ) {
        Arena.ofConfined().use { arena ->
            val longCell = arena.allocate(JAVA_LONG)
            val objCell = arena.allocate(ADDRESS)
            val vecCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val intCell = arena.allocate(JAVA_INT)
            longCell.set(JAVA_LONG, 0, longArg)
            objCell.set(ADDRESS, 0, objectArg)
            writeVector2(vecCell, vector2Arg)
            intCell.set(JAVA_INT, 0, intArg)
            val arr = arena.allocate(ADDRESS, 4)
            arr.setAtIndex(ADDRESS, 0, longCell)
            arr.setAtIndex(ADDRESS, 1, objCell)
            arr.setAtIndex(ADDRESS, 2, vecCell)
            arr.setAtIndex(ADDRESS, 3, intCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithLongAndVector2Arg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Long,
        point: Vector2,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_LONG)
            val vec = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            arg0.set(JAVA_LONG, 0, value)
            GodotReal.writeIndex(vec, 0, point.x)
            GodotReal.writeIndex(vec, 1, point.y)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, arg0)
            args.setAtIndex(ADDRESS, 1, vec)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithLongVector2AndDoubleArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Long,
        point: Vector2,
        weight: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_LONG)
            val vec = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val arg2 = arena.allocate(JAVA_DOUBLE)
            arg0.set(JAVA_LONG, 0, value)
            GodotReal.writeIndex(vec, 0, point.x)
            GodotReal.writeIndex(vec, 1, point.y)
            arg2.set(JAVA_DOUBLE, 0, weight)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, arg0)
            args.setAtIndex(ADDRESS, 1, vec)
            args.setAtIndex(ADDRESS, 2, arg2)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithLongAndVector3Arg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Long,
        point: Vector3,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_LONG)
            val vec = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            arg0.set(JAVA_LONG, 0, value)
            GodotReal.writeIndex(vec, 0, point.x)
            GodotReal.writeIndex(vec, 1, point.y)
            GodotReal.writeIndex(vec, 2, point.z)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, arg0)
            args.setAtIndex(ADDRESS, 1, vec)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithLongAndTransform3DArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Long,
        transform: Transform3D,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_LONG)
            val transformArg = arena.allocate(GodotReal.SIZE_BYTES * 12, GodotReal.ALIGN_BYTES)
            arg0.set(JAVA_LONG, 0, value)
            writeTransform3D(transformArg, transform)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, arg0)
            args.setAtIndex(ADDRESS, 1, transformArg)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithLongAndTransform2DArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Long,
        transform: Transform2D,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_LONG)
            val transformArg = arena.allocate(GodotReal.SIZE_BYTES * 6, GodotReal.ALIGN_BYTES)
            arg0.set(JAVA_LONG, 0, value)
            writeTransform2D(transformArg, transform)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, arg0)
            args.setAtIndex(ADDRESS, 1, transformArg)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithLongAndTransform3DArgRetTransform3D(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Long,
        transform: Transform3D,
    ): Transform3D {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_LONG)
            val transformArg = arena.allocate(GodotReal.SIZE_BYTES * 12, GodotReal.ALIGN_BYTES)
            arg0.set(JAVA_LONG, 0, value)
            writeTransform3D(transformArg, transform)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, arg0)
            args.setAtIndex(ADDRESS, 1, transformArg)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 12, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return readTransform3D(ret)
        }
    }

    fun ptrcallWithLongVector3AndDoubleArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Long,
        point: Vector3,
        weight: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_LONG)
            val vec = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            val arg2 = arena.allocate(JAVA_DOUBLE)
            arg0.set(JAVA_LONG, 0, value)
            GodotReal.writeIndex(vec, 0, point.x)
            GodotReal.writeIndex(vec, 1, point.y)
            GodotReal.writeIndex(vec, 2, point.z)
            arg2.set(JAVA_DOUBLE, 0, weight)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, arg0)
            args.setAtIndex(ADDRESS, 1, vec)
            args.setAtIndex(ADDRESS, 2, arg2)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with one float(double) arg and no return value.
     */
    fun ptrcallWithDoubleArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Double,
    ) {
        val scratch = ptrcallScratch.get()
        scratch.doubleCell.set(JAVA_DOUBLE, 0, value)
        scratch.args1.setAtIndex(ADDRESS, 0, scratch.doubleCell)
        objectMethodBindPtrcall.invoke(methodBind, instance, scratch.args1, MemorySegment.NULL)
    }

    /**
     * Calls [methodBind] with one scalar float arg and scalar float return value.
     *
     * Godot's scalar Variant float ABI uses 64-bit doubles. This is separate from `real_t`
     * storage inside vector and transform built-ins, which may be 32-bit in normal builds.
     */
    fun ptrcallWithFloatArgRetFloat(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Double,
    ): Double {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_DOUBLE)
            arg0.set(JAVA_DOUBLE, 0, value)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, arg0)
            val ret = arena.allocate(JAVA_DOUBLE)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_DOUBLE, 0)
        }
    }

    /**
     * Calls [methodBind] with one scalar float arg and StringName return value.
     */
    fun ptrcallWithFloatArgRetStringName(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Double,
    ): String {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_DOUBLE)
            arg0.set(JAVA_DOUBLE, 0, value)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, arg0)
            val ret = arena.allocate(8L, 8L)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return try {
                GodotStrings.readStringName(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.STRING_NAME, ret)
            }
        }
    }

    /**
     * Calls [methodBind] with one scalar float arg and bool return value.
     */
    fun ptrcallWithDoubleArgRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Double,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(java.lang.foreign.ValueLayout.JAVA_DOUBLE)
            arg0.set(java.lang.foreign.ValueLayout.JAVA_DOUBLE, 0, value)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, arg0)
            val ret = arena.allocate(java.lang.foreign.ValueLayout.JAVA_BYTE)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(java.lang.foreign.ValueLayout.JAVA_BYTE, 0) != 0.toByte()
        }
    }

    /**
     * Calls [methodBind] with one float(double) arg and Object return value.
     */
    fun ptrcallWithDoubleArgRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Double,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_DOUBLE)
            arg0.set(JAVA_DOUBLE, 0, value)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, arg0)
            val ret = arena.allocate(ADDRESS)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(ADDRESS, 0)
        }
    }

    /**
     * Calls [methodBind] with one String arg and bool return value.
     */
    fun ptrcallWithStringArgRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: String,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val argString = arena.allocate(8L, 8L)
            try {
                GodotStrings.initString(argString, value)
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, argString)
                val ret = arena.allocate(java.lang.foreign.ValueLayout.JAVA_BYTE)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(java.lang.foreign.ValueLayout.JAVA_BYTE, 0) != 0.toByte()
            } finally {
                GodotStrings.destroyString(argString)
            }
        }
    }

    /**
     * Calls [methodBind] with one String arg and Object return value.
     */
    fun ptrcallWithStringArgRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: String,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val argString = arena.allocate(8L, 8L)
            try {
                GodotStrings.initString(argString, value)
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, argString)
                val ret = arena.allocate(ADDRESS)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(ADDRESS, 0)
            } finally {
                GodotStrings.destroyString(argString)
            }
        }
    }

    fun ptrcallWithStringAndObjectArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        objectArg: MemorySegment,
    ) {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            GodotStrings.initString(stringCell, text)
            val objCell = arena.allocate(ADDRESS)
            objCell.set(ADDRESS, 0, objectArg)
            try {
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, stringCell)
                arr.setAtIndex(ADDRESS, 1, objCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithStringAndObjectArgRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        objectArg: MemorySegment,
    ): Long {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val objCell = arena.allocate(ADDRESS)
            GodotStrings.initString(stringCell, text)
            objCell.set(ADDRESS, 0, objectArg)
            try {
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, stringCell)
                arr.setAtIndex(ADDRESS, 1, objCell)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithStringAndObjectArgRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        objectArg: MemorySegment,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val objCell = arena.allocate(ADDRESS)
            try {
                GodotStrings.initString(stringCell, text)
                objCell.set(ADDRESS, 0, objectArg)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, stringCell)
                arr.setAtIndex(ADDRESS, 1, objCell)
                val ret = arena.allocate(ADDRESS)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(ADDRESS, 0)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithObjectStringArgRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        text: String,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val objCell = arena.allocate(ADDRESS)
            val stringCell = arena.allocate(8L, 8L)
            objCell.set(ADDRESS, 0, objectArg)
            GodotStrings.initString(stringCell, text)
            try {
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, objCell)
                arr.setAtIndex(ADDRESS, 1, stringCell)
                val ret = arena.allocate(ADDRESS)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(ADDRESS, 0)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithObjectStringAndObjectArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstObjectArg: MemorySegment,
        text: String,
        secondObjectArg: MemorySegment,
    ): Long {
        Arena.ofConfined().use { arena ->
            val firstObjCell = arena.allocate(ADDRESS)
            val stringCell = arena.allocate(8L, 8L)
            val secondObjCell = arena.allocate(ADDRESS)
            firstObjCell.set(ADDRESS, 0, firstObjectArg)
            secondObjCell.set(ADDRESS, 0, secondObjectArg)
            GodotStrings.initString(stringCell, text)
            try {
                val arr = arena.allocate(ADDRESS, 3)
                arr.setAtIndex(ADDRESS, 0, firstObjCell)
                arr.setAtIndex(ADDRESS, 1, stringCell)
                arr.setAtIndex(ADDRESS, 2, secondObjCell)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithObjectIntTransform3DArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        intArg: Int,
        transformArg: Transform3D,
    ) {
        Arena.ofConfined().use { arena ->
            val objCell = arena.allocate(ADDRESS)
            val intCell = arena.allocate(JAVA_INT)
            val transform = arena.allocate(GodotReal.SIZE_BYTES * 12, GodotReal.ALIGN_BYTES)
            objCell.set(ADDRESS, 0, objectArg)
            intCell.set(JAVA_INT, 0, intArg)
            GodotReal.writeIndex(transform, 0, transformArg.basis.x.x)
            GodotReal.writeIndex(transform, 1, transformArg.basis.y.x)
            GodotReal.writeIndex(transform, 2, transformArg.basis.z.x)
            GodotReal.writeIndex(transform, 3, transformArg.basis.x.y)
            GodotReal.writeIndex(transform, 4, transformArg.basis.y.y)
            GodotReal.writeIndex(transform, 5, transformArg.basis.z.y)
            GodotReal.writeIndex(transform, 6, transformArg.basis.x.z)
            GodotReal.writeIndex(transform, 7, transformArg.basis.y.z)
            GodotReal.writeIndex(transform, 8, transformArg.basis.z.z)
            GodotReal.writeIndex(transform, 9, transformArg.origin.x)
            GodotReal.writeIndex(transform, 10, transformArg.origin.y)
            GodotReal.writeIndex(transform, 11, transformArg.origin.z)
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, objCell)
            arr.setAtIndex(ADDRESS, 1, intCell)
            arr.setAtIndex(ADDRESS, 2, transform)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithObjectAndTransform3DArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        transformArg: Transform3D,
    ) {
        Arena.ofConfined().use { arena ->
            val objCell = arena.allocate(ADDRESS)
            val transform = arena.allocate(GodotReal.SIZE_BYTES * 12, GodotReal.ALIGN_BYTES)
            objCell.set(ADDRESS, 0, objectArg)
            writeTransform3D(transform, transformArg)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, objCell)
            arr.setAtIndex(ADDRESS, 1, transform)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithStringAndTwoObjectArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        firstObject: MemorySegment,
        secondObject: MemorySegment,
    ) {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val obj0 = arena.allocate(ADDRESS)
            val obj1 = arena.allocate(ADDRESS)
            GodotStrings.initString(stringCell, text)
            obj0.set(ADDRESS, 0, firstObject)
            obj1.set(ADDRESS, 0, secondObject)
            try {
                val arr = arena.allocate(ADDRESS, 3)
                arr.setAtIndex(ADDRESS, 0, stringCell)
                arr.setAtIndex(ADDRESS, 1, obj0)
                arr.setAtIndex(ADDRESS, 2, obj1)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithStringAndColorArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        color: Color,
    ) {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val colorCell = arena.allocate(16L, 4L)
            try {
                GodotStrings.initString(stringCell, text)
                // Color components use fixed 32-bit storage, unlike scalar float method args.
                colorCell.set(JAVA_FLOAT, 0, color.r)
                colorCell.set(JAVA_FLOAT, 4, color.g)
                colorCell.set(JAVA_FLOAT, 8, color.b)
                colorCell.set(JAVA_FLOAT, 12, color.a)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, stringCell)
                arr.setAtIndex(ADDRESS, 1, colorCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithStringAndIntArgRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        value: Int,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            GodotStrings.initString(stringCell, text)
            val intCell = arena.allocate(JAVA_INT)
            intCell.set(JAVA_INT, 0, value)
            try {
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, stringCell)
                arr.setAtIndex(ADDRESS, 1, intCell)
                val ret = arena.allocate(JAVA_BYTE)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_BYTE, 0) != 0.toByte()
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    /**
     * Calls [methodBind] with two String args and bool return value.
     */
    fun ptrcallWithTwoStringArgsRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: String,
        second: String,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(8L, 8L)
            val arg1 = arena.allocate(8L, 8L)
            try {
                GodotStrings.initString(arg0, first)
                GodotStrings.initString(arg1, second)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, arg0)
                arr.setAtIndex(ADDRESS, 1, arg1)
                val ret = arena.allocate(java.lang.foreign.ValueLayout.JAVA_BYTE)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(java.lang.foreign.ValueLayout.JAVA_BYTE, 0) != 0.toByte()
            } finally {
                GodotStrings.destroyString(arg0)
                GodotStrings.destroyString(arg1)
            }
        }
    }

    /**
     * Calls [methodBind] with two String args and int64 return value.
     */
    fun ptrcallWithTwoStringArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: String,
        second: String,
    ): Long {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(8L, 8L)
            val arg1 = arena.allocate(8L, 8L)
            try {
                GodotStrings.initString(arg0, first)
                GodotStrings.initString(arg1, second)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, arg0)
                arr.setAtIndex(ADDRESS, 1, arg1)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                GodotStrings.destroyString(arg0)
                GodotStrings.destroyString(arg1)
            }
        }
    }

    /**
     * Calls [methodBind] with two String args and String return value.
     */
    fun ptrcallWithTwoStringArgsRetString(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: String,
        second: String,
    ): String {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(8L, 8L)
            val arg1 = arena.allocate(8L, 8L)
            val retString = arena.allocate(8L, 8L)
            try {
                GodotStrings.initString(arg0, first)
                GodotStrings.initString(arg1, second)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, arg0)
                arr.setAtIndex(ADDRESS, 1, arg1)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, retString)
                return GodotStrings.readString(retString)
            } finally {
                GodotStrings.destroyString(retString)
                GodotStrings.destroyString(arg0)
                GodotStrings.destroyString(arg1)
            }
        }
    }

    /**
     * Calls [methodBind] with two String args and decodes PackedByteArray return.
     */
    fun ptrcallWithTwoStringArgsRetByteArray(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: String,
        second: String,
    ): ByteArray {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(8L, 8L)
            val arg1 = arena.allocate(8L, 8L)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            try {
                GodotStrings.initString(arg0, first)
                GodotStrings.initString(arg1, second)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, arg0)
                arr.setAtIndex(ADDRESS, 1, arg1)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return BuiltinTypes.readPackedByteArray(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, ret)
                GodotStrings.destroyString(arg0)
                GodotStrings.destroyString(arg1)
            }
        }
    }

    fun ptrcallWithTwoStringArgsRetDictionary(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: String,
        second: String,
    ): Map<String, Any?> {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(8L, 8L)
            val arg1 = arena.allocate(8L, 8L)
            val ret = arena.allocate(8L, 8L)
            try {
                GodotStrings.initString(arg0, first)
                GodotStrings.initString(arg1, second)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, arg0)
                arr.setAtIndex(ADDRESS, 1, arg1)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return BuiltinTypes.readDictionaryScalars(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.DICTIONARY, ret)
                GodotStrings.destroyString(arg0)
                GodotStrings.destroyString(arg1)
            }
        }
    }

    /**
     * Calls [methodBind] with two String args and decodes PackedInt32Array return.
     */
    fun ptrcallWithTwoStringArgsRetPackedInt32List(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: String,
        second: String,
    ): List<Int> {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(8L, 8L)
            val arg1 = arena.allocate(8L, 8L)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            try {
                GodotStrings.initString(arg0, first)
                GodotStrings.initString(arg1, second)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, arg0)
                arr.setAtIndex(ADDRESS, 1, arg1)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return BuiltinTypes.readPackedInt32Array(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_INT32_ARRAY, ret)
                GodotStrings.destroyString(arg0)
                GodotStrings.destroyString(arg1)
            }
        }
    }

    fun ptrcallWithThreeStringAndIntArgRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: String,
        second: String,
        third: String,
        value: Int,
    ): Int {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(8L, 8L)
            val arg1 = arena.allocate(8L, 8L)
            val arg2 = arena.allocate(8L, 8L)
            val arg3 = arena.allocate(JAVA_INT)
            try {
                GodotStrings.initString(arg0, first)
                GodotStrings.initString(arg1, second)
                GodotStrings.initString(arg2, third)
                arg3.set(JAVA_INT, 0, value)
                val arr = arena.allocate(ADDRESS, 4)
                arr.setAtIndex(ADDRESS, 0, arg0)
                arr.setAtIndex(ADDRESS, 1, arg1)
                arr.setAtIndex(ADDRESS, 2, arg2)
                arr.setAtIndex(ADDRESS, 3, arg3)
                val ret = arena.allocate(JAVA_INT)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_INT, 0)
            } finally {
                GodotStrings.destroyString(arg0)
                GodotStrings.destroyString(arg1)
                GodotStrings.destroyString(arg2)
            }
        }
    }

    fun ptrcallWithStringAndIntArgRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        value: Int,
    ): Int {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val intCell = arena.allocate(JAVA_INT)
            GodotStrings.initString(stringCell, text)
            intCell.set(JAVA_INT, 0, value)
            try {
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, stringCell)
                arr.setAtIndex(ADDRESS, 1, intCell)
                val ret = arena.allocate(JAVA_INT)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_INT, 0)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithStringAndIntArgRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        value: Int,
    ): Long {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val intCell = arena.allocate(JAVA_INT)
            GodotStrings.initString(stringCell, text)
            intCell.set(JAVA_INT, 0, value)
            try {
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, stringCell)
                arr.setAtIndex(ADDRESS, 1, intCell)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    /**
     * Calls [methodBind] with one String arg, one scalar float arg, and int64 return value.
     */
    fun ptrcallWithStringAndDoubleArgRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        value: Double,
    ): Long {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val doubleCell = arena.allocate(JAVA_DOUBLE)
            GodotStrings.initString(stringCell, text)
            doubleCell.set(JAVA_DOUBLE, 0, value)
            try {
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, stringCell)
                arr.setAtIndex(ADDRESS, 1, doubleCell)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    /**
     * Calls [methodBind] with two String args, one int32 arg, and int64 return value.
     */
    fun ptrcallWithTwoStringAndIntArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: String,
        second: String,
        value: Int,
    ): Long {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(8L, 8L)
            val arg1 = arena.allocate(8L, 8L)
            val arg2 = arena.allocate(JAVA_INT)
            try {
                GodotStrings.initString(arg0, first)
                GodotStrings.initString(arg1, second)
                arg2.set(JAVA_INT, 0, value)
                val arr = arena.allocate(ADDRESS, 3)
                arr.setAtIndex(ADDRESS, 0, arg0)
                arr.setAtIndex(ADDRESS, 1, arg1)
                arr.setAtIndex(ADDRESS, 2, arg2)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                GodotStrings.destroyString(arg0)
                GodotStrings.destroyString(arg1)
            }
        }
    }

    fun ptrcallWithTwoStringIntTwoDoubleLongBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: String,
        second: String,
        intValue: Int,
        firstDouble: Double,
        secondDouble: Double,
        longValue: Long,
        enabled: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(8L, 8L)
            val arg1 = arena.allocate(8L, 8L)
            val intCell = arena.allocate(JAVA_INT)
            val double0 = arena.allocate(JAVA_DOUBLE)
            val double1 = arena.allocate(JAVA_DOUBLE)
            val longCell = arena.allocate(JAVA_LONG)
            val boolCell = arena.allocate(JAVA_BYTE)
            try {
                GodotStrings.initString(arg0, first)
                GodotStrings.initString(arg1, second)
                intCell.set(JAVA_INT, 0, intValue)
                double0.set(JAVA_DOUBLE, 0, firstDouble)
                double1.set(JAVA_DOUBLE, 0, secondDouble)
                longCell.set(JAVA_LONG, 0, longValue)
                boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
                val args = arena.allocate(ADDRESS, 7)
                args.setAtIndex(ADDRESS, 0, arg0)
                args.setAtIndex(ADDRESS, 1, arg1)
                args.setAtIndex(ADDRESS, 2, intCell)
                args.setAtIndex(ADDRESS, 3, double0)
                args.setAtIndex(ADDRESS, 4, double1)
                args.setAtIndex(ADDRESS, 5, longCell)
                args.setAtIndex(ADDRESS, 6, boolCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(arg1)
                GodotStrings.destroyString(arg0)
            }
        }
    }

    /**
     * Calls [methodBind] with two String args plus one int64 arg and decodes PackedInt32Array return.
     */
    fun ptrcallWithTwoStringAndLongArgRetPackedInt32List(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: String,
        second: String,
        value: Long,
    ): List<Int> {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(8L, 8L)
            val arg1 = arena.allocate(8L, 8L)
            val arg2 = arena.allocate(JAVA_LONG)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            try {
                GodotStrings.initString(arg0, first)
                GodotStrings.initString(arg1, second)
                arg2.set(JAVA_LONG, 0, value)
                val arr = arena.allocate(ADDRESS, 3)
                arr.setAtIndex(ADDRESS, 0, arg0)
                arr.setAtIndex(ADDRESS, 1, arg1)
                arr.setAtIndex(ADDRESS, 2, arg2)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return BuiltinTypes.readPackedInt32Array(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_INT32_ARRAY, ret)
                GodotStrings.destroyString(arg0)
                GodotStrings.destroyString(arg1)
            }
        }
    }

    /**
     * Calls [methodBind] with one String arg and decodes PackedStringArray return.
     */
    fun ptrcallWithStringArgRetPackedStringList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: String,
    ): List<String> {
        Arena.ofConfined().use { arena ->
            val argString = arena.allocate(8L, 8L)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            try {
                GodotStrings.initString(argString, value)
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, argString)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return BuiltinTypes.readPackedStringArray(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_STRING_ARRAY, ret)
                GodotStrings.destroyString(argString)
            }
        }
    }

    /**
     * Calls [methodBind] with one String arg and decodes PackedByteArray return.
     */
    fun ptrcallWithStringArgRetByteArray(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: String,
    ): ByteArray {
        Arena.ofConfined().use { arena ->
            val argString = arena.allocate(8L, 8L)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            try {
                GodotStrings.initString(argString, value)
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, argString)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return BuiltinTypes.readPackedByteArray(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, ret)
                GodotStrings.destroyString(argString)
            }
        }
    }

    fun ptrcallWithStringAndFiveIntArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        first: Int,
        second: Int,
        third: Int,
        fourth: Int,
        fifth: Int,
    ): Long {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val int0 = arena.allocate(JAVA_INT)
            val int1 = arena.allocate(JAVA_INT)
            val int2 = arena.allocate(JAVA_INT)
            val int3 = arena.allocate(JAVA_INT)
            val int4 = arena.allocate(JAVA_INT)
            GodotStrings.initString(stringCell, text)
            int0.set(JAVA_INT, 0, first)
            int1.set(JAVA_INT, 0, second)
            int2.set(JAVA_INT, 0, third)
            int3.set(JAVA_INT, 0, fourth)
            int4.set(JAVA_INT, 0, fifth)
            try {
                val arr = arena.allocate(ADDRESS, 6)
                arr.setAtIndex(ADDRESS, 0, stringCell)
                arr.setAtIndex(ADDRESS, 1, int0)
                arr.setAtIndex(ADDRESS, 2, int1)
                arr.setAtIndex(ADDRESS, 3, int2)
                arr.setAtIndex(ADDRESS, 4, int3)
                arr.setAtIndex(ADDRESS, 5, int4)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    /**
     * Calls [methodBind] with one int64 arg and decodes PackedByteArray return.
     */
    fun ptrcallWithLongArgRetByteArray(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Long,
    ): ByteArray {
        Arena.ofConfined().use { arena ->
            val arg = arena.allocate(JAVA_LONG)
            arg.set(JAVA_LONG, 0, value)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            try {
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, arg)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return BuiltinTypes.readPackedByteArray(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, ret)
            }
        }
    }

    fun ptrcallWithLongByteArrayLongArgsRetRID(
        methodBind: MemorySegment,
        instance: MemorySegment,
        longArg: Long,
        bytes: ByteArray,
        flags: Long,
    ): RID {
        Arena.ofConfined().use { arena ->
            val longCell = arena.allocate(JAVA_LONG)
            val bytesCell = BuiltinTypes.allocatePackedArray(arena)
            val flagsCell = arena.allocate(JAVA_LONG)
            try {
                longCell.set(JAVA_LONG, 0, longArg)
                BuiltinTypes.initPackedByteArray(bytesCell, bytes)
                flagsCell.set(JAVA_LONG, 0, flags)
                val args = arena.allocate(ADDRESS, 3)
                args.setAtIndex(ADDRESS, 0, longCell)
                args.setAtIndex(ADDRESS, 1, bytesCell)
                args.setAtIndex(ADDRESS, 2, flagsCell)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return RID(ret.get(JAVA_LONG, 0))
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, bytesCell)
            }
        }
    }

    fun ptrcallWithUInt32ByteArrayLongArgsRetRID(
        methodBind: MemorySegment,
        instance: MemorySegment,
        size: Long,
        bytes: ByteArray,
        flags: Long,
    ): RID {
        Arena.ofConfined().use { arena ->
            val sizeCell = arena.allocate(JAVA_INT)
            val bytesCell = BuiltinTypes.allocatePackedArray(arena)
            val flagsCell = arena.allocate(JAVA_LONG)
            try {
                sizeCell.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(size))
                BuiltinTypes.initPackedByteArray(bytesCell, bytes)
                flagsCell.set(JAVA_LONG, 0, flags)
                val args = arena.allocate(ADDRESS, 3)
                args.setAtIndex(ADDRESS, 0, sizeCell)
                args.setAtIndex(ADDRESS, 1, bytesCell)
                args.setAtIndex(ADDRESS, 2, flagsCell)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return RID(ret.get(JAVA_LONG, 0))
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, bytesCell)
            }
        }
    }

    fun ptrcallWithLongByteArrayLongArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        longArg: Long,
        bytes: ByteArray,
        flags: Long,
    ) {
        Arena.ofConfined().use { arena ->
            val longCell = arena.allocate(JAVA_LONG)
            val bytesCell = BuiltinTypes.allocatePackedArray(arena)
            val flagsCell = arena.allocate(JAVA_LONG)
            try {
                longCell.set(JAVA_LONG, 0, longArg)
                BuiltinTypes.initPackedByteArray(bytesCell, bytes)
                flagsCell.set(JAVA_LONG, 0, flags)
                val args = arena.allocate(ADDRESS, 3)
                args.setAtIndex(ADDRESS, 0, longCell)
                args.setAtIndex(ADDRESS, 1, bytesCell)
                args.setAtIndex(ADDRESS, 2, flagsCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, bytesCell)
            }
        }
    }

    fun ptrcallWithLongByteArrayUInt32Args(
        methodBind: MemorySegment,
        instance: MemorySegment,
        longArg: Long,
        bytes: ByteArray,
        size: Long,
    ) {
        Arena.ofConfined().use { arena ->
            val longCell = arena.allocate(JAVA_LONG)
            val bytesCell = BuiltinTypes.allocatePackedArray(arena)
            val sizeCell = arena.allocate(JAVA_INT)
            try {
                longCell.set(JAVA_LONG, 0, longArg)
                BuiltinTypes.initPackedByteArray(bytesCell, bytes)
                sizeCell.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(size))
                val args = arena.allocate(ADDRESS, 3)
                args.setAtIndex(ADDRESS, 0, longCell)
                args.setAtIndex(ADDRESS, 1, bytesCell)
                args.setAtIndex(ADDRESS, 2, sizeCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, bytesCell)
            }
        }
    }

    fun ptrcallWithLongBoolTwoUInt32Args(
        methodBind: MemorySegment,
        instance: MemorySegment,
        longArg: Long,
        boolArg: Boolean,
        first: Long,
        second: Long,
    ) {
        Arena.ofConfined().use { arena ->
            val longCell = arena.allocate(JAVA_LONG)
            val boolCell = arena.allocate(JAVA_BYTE)
            val firstCell = arena.allocate(JAVA_INT)
            val secondCell = arena.allocate(JAVA_INT)
            longCell.set(JAVA_LONG, 0, longArg)
            boolCell.set(JAVA_BYTE, 0, if (boolArg) 1.toByte() else 0.toByte())
            firstCell.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(first))
            secondCell.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(second))
            val args = arena.allocate(ADDRESS, 4)
            args.setAtIndex(ADDRESS, 0, longCell)
            args.setAtIndex(ADDRESS, 1, boolCell)
            args.setAtIndex(ADDRESS, 2, firstCell)
            args.setAtIndex(ADDRESS, 3, secondCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithLongAndThreeUInt32Args(
        methodBind: MemorySegment,
        instance: MemorySegment,
        longArg: Long,
        first: Long,
        second: Long,
        third: Long,
    ) {
        Arena.ofConfined().use { arena ->
            val longCell = arena.allocate(JAVA_LONG)
            val firstCell = arena.allocate(JAVA_INT)
            val secondCell = arena.allocate(JAVA_INT)
            val thirdCell = arena.allocate(JAVA_INT)
            longCell.set(JAVA_LONG, 0, longArg)
            firstCell.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(first))
            secondCell.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(second))
            thirdCell.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(third))
            val args = arena.allocate(ADDRESS, 4)
            args.setAtIndex(ADDRESS, 0, longCell)
            args.setAtIndex(ADDRESS, 1, firstCell)
            args.setAtIndex(ADDRESS, 2, secondCell)
            args.setAtIndex(ADDRESS, 3, thirdCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDUInt32ByteArrayArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        layer: Long,
        bytes: ByteArray,
    ): Long {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val layerCell = arena.allocate(JAVA_INT)
            val bytesCell = BuiltinTypes.allocatePackedArray(arena)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                layerCell.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(layer))
                BuiltinTypes.initPackedByteArray(bytesCell, bytes)
                val args = arena.allocate(ADDRESS, 3)
                args.setAtIndex(ADDRESS, 0, ridCell)
                args.setAtIndex(ADDRESS, 1, layerCell)
                args.setAtIndex(ADDRESS, 2, bytesCell)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, bytesCell)
            }
        }
    }

    fun ptrcallWithUInt32LongByteArrayArgsRetRID(
        methodBind: MemorySegment,
        instance: MemorySegment,
        size: Long,
        format: Long,
        bytes: ByteArray,
    ): RID {
        Arena.ofConfined().use { arena ->
            val sizeCell = arena.allocate(JAVA_INT)
            val formatCell = arena.allocate(JAVA_LONG)
            val bytesCell = BuiltinTypes.allocatePackedArray(arena)
            try {
                sizeCell.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(size))
                formatCell.set(JAVA_LONG, 0, format)
                BuiltinTypes.initPackedByteArray(bytesCell, bytes)
                val args = arena.allocate(ADDRESS, 3)
                args.setAtIndex(ADDRESS, 0, sizeCell)
                args.setAtIndex(ADDRESS, 1, formatCell)
                args.setAtIndex(ADDRESS, 2, bytesCell)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return RID(ret.get(JAVA_LONG, 0))
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, bytesCell)
            }
        }
    }

    /**
     * Calls [methodBind] with one PackedByteArray arg and bool return value.
     */
    fun ptrcallWithByteArrayArgRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: ByteArray,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val argBytes = BuiltinTypes.allocatePackedArray(arena)
            try {
                BuiltinTypes.initPackedByteArray(argBytes, value)
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, argBytes)
                val ret = arena.allocate(java.lang.foreign.ValueLayout.JAVA_BYTE)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(java.lang.foreign.ValueLayout.JAVA_BYTE, 0) != 0.toByte()
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, argBytes)
            }
        }
    }

    /**
     * Calls [methodBind] with one PackedByteArray arg and no return value.
     */
    fun ptrcallWithByteArrayArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: ByteArray,
    ) {
        Arena.ofConfined().use { arena ->
            val argBytes = BuiltinTypes.allocatePackedArray(arena)
            try {
                BuiltinTypes.initPackedByteArray(argBytes, value)
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, argBytes)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, argBytes)
            }
        }
    }

    /**
     * Calls [methodBind] with one PackedByteArray arg and int64/enum return value.
     */
    fun ptrcallWithByteArrayArgRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: ByteArray,
    ): Long {
        Arena.ofConfined().use { arena ->
            val argBytes = BuiltinTypes.allocatePackedArray(arena)
            try {
                BuiltinTypes.initPackedByteArray(argBytes, value)
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, argBytes)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, argBytes)
            }
        }
    }

    fun ptrcallWithByteArrayArgRetArray(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: ByteArray,
    ): List<Any?> {
        Arena.ofConfined().use { arena ->
            val argBytes = BuiltinTypes.allocatePackedArray(arena)
            try {
                BuiltinTypes.initPackedByteArray(argBytes, value)
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, argBytes)
                return callArrayReturn(methodBind, instance, arr, BuiltinTypes::readArrayScalars)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, argBytes)
            }
        }
    }

    /**
     * Calls [methodBind] with one PackedByteArray arg and decodes PackedByteArray return.
     */
    fun ptrcallWithByteArrayArgRetByteArray(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: ByteArray,
    ): ByteArray {
        Arena.ofConfined().use { arena ->
            val argBytes = BuiltinTypes.allocatePackedArray(arena)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            try {
                BuiltinTypes.initPackedByteArray(argBytes, value)
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, argBytes)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return BuiltinTypes.readPackedByteArray(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, ret)
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, argBytes)
            }
        }
    }

    /**
     * Calls [methodBind] with one PackedByteArray arg and Object return value.
     */
    fun ptrcallWithByteArrayArgRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: ByteArray,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val argBytes = BuiltinTypes.allocatePackedArray(arena)
            try {
                BuiltinTypes.initPackedByteArray(argBytes, value)
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, argBytes)
                val ret = arena.allocate(ADDRESS)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(ADDRESS, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, argBytes)
            }
        }
    }

    /**
     * Calls [methodBind] with (PackedByteArray, scalar float) args and int64/enum return value.
     */
    fun ptrcallWithByteArrayAndDoubleArgRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: ByteArray,
        scale: Double,
    ): Long {
        Arena.ofConfined().use { arena ->
            val argBytes = BuiltinTypes.allocatePackedArray(arena)
            val doubleCell = arena.allocate(JAVA_DOUBLE)
            try {
                BuiltinTypes.initPackedByteArray(argBytes, value)
                doubleCell.set(JAVA_DOUBLE, 0, scale)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, argBytes)
                arr.setAtIndex(ADDRESS, 1, doubleCell)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, argBytes)
            }
        }
    }

    fun ptrcallWithByteArrayAndDictionaryArgRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        bytes: ByteArray,
        values: Map<String, Any?>,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val byteArray = BuiltinTypes.allocatePackedArray(arena)
            val dict = arena.allocate(8L, 8L)
            try {
                BuiltinTypes.initPackedByteArray(byteArray, bytes)
                BuiltinTypes.initDictionary(dict, values)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, byteArray)
                arr.setAtIndex(ADDRESS, 1, dict)
                val ret = arena.allocate(ADDRESS)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(ADDRESS, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.DICTIONARY, dict)
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, byteArray)
            }
        }
    }

    /**
     * Calls [methodBind] with (Object, PackedByteArray) args and decodes PackedByteArray return.
     */
    fun ptrcallWithObjectAndByteArrayArgRetByteArray(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        value: ByteArray,
    ): ByteArray {
        Arena.ofConfined().use { arena ->
            val objCell = arena.allocate(ADDRESS)
            val argBytes = BuiltinTypes.allocatePackedArray(arena)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            try {
                objCell.set(ADDRESS, 0, objectArg)
                BuiltinTypes.initPackedByteArray(argBytes, value)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, objCell)
                arr.setAtIndex(ADDRESS, 1, argBytes)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return BuiltinTypes.readPackedByteArray(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, ret)
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, argBytes)
            }
        }
    }

    /**
     * Calls [methodBind] with two PackedByteArray args and bool return value.
     */
    fun ptrcallWithTwoByteArrayArgsRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: ByteArray,
        second: ByteArray,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val firstBytes = BuiltinTypes.allocatePackedArray(arena)
            val secondBytes = BuiltinTypes.allocatePackedArray(arena)
            try {
                BuiltinTypes.initPackedByteArray(firstBytes, first)
                BuiltinTypes.initPackedByteArray(secondBytes, second)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, firstBytes)
                arr.setAtIndex(ADDRESS, 1, secondBytes)
                val ret = arena.allocate(JAVA_BYTE)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_BYTE, 0) != 0.toByte()
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, secondBytes)
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, firstBytes)
            }
        }
    }

    /**
     * Calls [methodBind] with (int64/enum, PackedByteArray, PackedByteArray) args and int64/enum return value.
     */
    fun ptrcallWithLongAndTwoByteArrayArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        mode: Long,
        first: ByteArray,
        second: ByteArray,
    ): Long {
        Arena.ofConfined().use { arena ->
            val modeCell = arena.allocate(JAVA_LONG)
            val firstBytes = BuiltinTypes.allocatePackedArray(arena)
            val secondBytes = BuiltinTypes.allocatePackedArray(arena)
            try {
                modeCell.set(JAVA_LONG, 0, mode)
                BuiltinTypes.initPackedByteArray(firstBytes, first)
                BuiltinTypes.initPackedByteArray(secondBytes, second)
                val arr = arena.allocate(ADDRESS, 3)
                arr.setAtIndex(ADDRESS, 0, modeCell)
                arr.setAtIndex(ADDRESS, 1, firstBytes)
                arr.setAtIndex(ADDRESS, 2, secondBytes)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, secondBytes)
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, firstBytes)
            }
        }
    }

    /**
     * Calls [methodBind] with (int64/enum, PackedByteArray, PackedByteArray) args and decodes PackedByteArray return.
     */
    fun ptrcallWithLongAndTwoByteArrayArgsRetByteArray(
        methodBind: MemorySegment,
        instance: MemorySegment,
        mode: Long,
        first: ByteArray,
        second: ByteArray,
    ): ByteArray {
        Arena.ofConfined().use { arena ->
            val modeCell = arena.allocate(JAVA_LONG)
            val firstBytes = BuiltinTypes.allocatePackedArray(arena)
            val secondBytes = BuiltinTypes.allocatePackedArray(arena)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            try {
                modeCell.set(JAVA_LONG, 0, mode)
                BuiltinTypes.initPackedByteArray(firstBytes, first)
                BuiltinTypes.initPackedByteArray(secondBytes, second)
                val arr = arena.allocate(ADDRESS, 3)
                arr.setAtIndex(ADDRESS, 0, modeCell)
                arr.setAtIndex(ADDRESS, 1, firstBytes)
                arr.setAtIndex(ADDRESS, 2, secondBytes)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return BuiltinTypes.readPackedByteArray(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, ret)
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, secondBytes)
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, firstBytes)
            }
        }
    }

    /**
     * Calls [methodBind] with (int64/enum, PackedByteArray) args and int64/enum return value.
     */
    fun ptrcallWithLongAndByteArrayArgRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        mode: Long,
        value: ByteArray,
    ): Long {
        Arena.ofConfined().use { arena ->
            val modeCell = arena.allocate(JAVA_LONG)
            val argBytes = BuiltinTypes.allocatePackedArray(arena)
            try {
                modeCell.set(JAVA_LONG, 0, mode)
                BuiltinTypes.initPackedByteArray(argBytes, value)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, modeCell)
                arr.setAtIndex(ADDRESS, 1, argBytes)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, argBytes)
            }
        }
    }

    fun ptrcallWithLongAndByteArrayArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        mode: Long,
        value: ByteArray,
    ) {
        Arena.ofConfined().use { arena ->
            val modeCell = arena.allocate(JAVA_LONG)
            val argBytes = BuiltinTypes.allocatePackedArray(arena)
            try {
                modeCell.set(JAVA_LONG, 0, mode)
                BuiltinTypes.initPackedByteArray(argBytes, value)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, modeCell)
                arr.setAtIndex(ADDRESS, 1, argBytes)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, argBytes)
            }
        }
    }

    fun ptrcallWithByteArrayAndLongArgRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: ByteArray,
        mode: Long,
    ): Long {
        Arena.ofConfined().use { arena ->
            val argBytes = BuiltinTypes.allocatePackedArray(arena)
            val modeCell = arena.allocate(JAVA_LONG)
            try {
                BuiltinTypes.initPackedByteArray(argBytes, value)
                modeCell.set(JAVA_LONG, 0, mode)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, argBytes)
                arr.setAtIndex(ADDRESS, 1, modeCell)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, argBytes)
            }
        }
    }

    /**
     * Calls [methodBind] with (PackedStringArray, String) args and bool return value.
     */
    fun ptrcallWithPackedStringListAndStringArgsRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        values: List<String>,
        delimiter: String,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val argPacked = BuiltinTypes.allocatePackedArray(arena)
            val argString = arena.allocate(8L, 8L)
            try {
                BuiltinTypes.initPackedStringArray(argPacked, values)
                GodotStrings.initString(argString, delimiter)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, argPacked)
                arr.setAtIndex(ADDRESS, 1, argString)
                val ret = arena.allocate(java.lang.foreign.ValueLayout.JAVA_BYTE)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(java.lang.foreign.ValueLayout.JAVA_BYTE, 0) != 0.toByte()
            } finally {
                GodotStrings.destroyString(argString)
                BuiltinTypes.destroyTyped(VariantType.PACKED_STRING_ARRAY, argPacked)
            }
        }
    }

    fun ptrcallWithStringAndPackedStringListArgRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        values: List<String>,
    ): Long {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val packedCell = BuiltinTypes.allocatePackedArray(arena)
            try {
                GodotStrings.initString(stringCell, text)
                BuiltinTypes.initPackedStringArray(packedCell, values)
                val args = arena.allocate(ADDRESS, 2)
                args.setAtIndex(ADDRESS, 0, stringCell)
                args.setAtIndex(ADDRESS, 1, packedCell)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_STRING_ARRAY, packedCell)
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    /**
     * Calls [methodBind] with one PackedStringArray arg and no return value.
     */
    fun ptrcallWithPackedStringListArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        values: List<String>,
    ) {
        Arena.ofConfined().use { arena ->
            val argPacked = BuiltinTypes.allocatePackedArray(arena)
            try {
                BuiltinTypes.initPackedStringArray(argPacked, values)
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, argPacked)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_STRING_ARRAY, argPacked)
            }
        }
    }

    /**
     * Calls [methodBind] with one PackedStringArray arg and decodes int32 return.
     */
    fun ptrcallWithPackedStringListArgRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        values: List<String>,
    ): Int {
        Arena.ofConfined().use { arena ->
            val argPacked = BuiltinTypes.allocatePackedArray(arena)
            try {
                BuiltinTypes.initPackedStringArray(argPacked, values)
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, argPacked)
                val ret = arena.allocate(JAVA_INT)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_INT, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_STRING_ARRAY, argPacked)
            }
        }
    }

    fun ptrcallWithPackedStringListArgRetDictionary(
        methodBind: MemorySegment,
        instance: MemorySegment,
        values: List<String>,
    ): Map<String, Any?> {
        Arena.ofConfined().use { arena ->
            val argPacked = BuiltinTypes.allocatePackedArray(arena)
            val ret = arena.allocate(8L, 8L)
            try {
                BuiltinTypes.initPackedStringArray(argPacked, values)
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, argPacked)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return BuiltinTypes.readDictionaryScalars(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.DICTIONARY, ret)
                BuiltinTypes.destroyTyped(VariantType.PACKED_STRING_ARRAY, argPacked)
            }
        }
    }

    fun ptrcallWithRIDListArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        values: List<RID>,
    ) {
        Arena.ofConfined().use { arena ->
            val argArray = arena.allocate(8L, 8L)
            try {
                BuiltinTypes.initArrayOfRids(argArray, values)
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, argArray)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, argArray)
            }
        }
    }

    fun ptrcallWithArrayArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        values: List<Any?>,
    ) {
        Arena.ofConfined().use { arena ->
            val argArray = arena.allocate(8L, 8L)
            try {
                BuiltinTypes.initArray(argArray, values)
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, argArray)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, argArray)
            }
        }
    }

    fun ptrcallWithArrayLongArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        values: List<Any?>,
        mode: Long,
    ) {
        Arena.ofConfined().use { arena ->
            val argArray = arena.allocate(8L, 8L)
            val modeCell = arena.allocate(JAVA_LONG)
            try {
                BuiltinTypes.initArray(argArray, values)
                modeCell.set(JAVA_LONG, 0, mode)
                val args = arena.allocate(ADDRESS, 2)
                args.setAtIndex(ADDRESS, 0, argArray)
                args.setAtIndex(ADDRESS, 1, modeCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, argArray)
            }
        }
    }

    fun ptrcallWithObjectListArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        values: List<*>,
    ) {
        Arena.ofConfined().use { arena ->
            val argArray = arena.allocate(8L, 8L)
            try {
                BuiltinTypes.initArrayOfObjects(argArray, values)
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, argArray)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, argArray)
            }
        }
    }

    fun ptrcallWithObjectListArgRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        values: List<*>,
    ): Long {
        Arena.ofConfined().use { arena ->
            val argArray = arena.allocate(8L, 8L)
            try {
                BuiltinTypes.initArrayOfObjects(argArray, values)
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, argArray)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, argArray)
            }
        }
    }

    fun ptrcallWithObjectListLongArgsRetRID(
        methodBind: MemorySegment,
        instance: MemorySegment,
        values: List<*>,
        type: Long,
    ): RID {
        Arena.ofConfined().use { arena ->
            val argArray = arena.allocate(8L, 8L)
            val typeCell = arena.allocate(JAVA_LONG)
            try {
                BuiltinTypes.initArrayOfObjects(argArray, values)
                typeCell.set(JAVA_LONG, 0, type)
                val args = arena.allocate(ADDRESS, 2)
                args.setAtIndex(ADDRESS, 0, argArray)
                args.setAtIndex(ADDRESS, 1, typeCell)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return RID(ret.get(JAVA_LONG, 0))
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, argArray)
            }
        }
    }

    fun ptrcallWithObjectListUInt32ArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        values: List<*>,
        count: Long,
    ): Long {
        val checkedCount = BuiltinTypes.requireUInt32(count)
        Arena.ofConfined().use { arena ->
            val argArray = arena.allocate(8L, 8L)
            val countCell = arena.allocate(JAVA_INT)
            try {
                BuiltinTypes.initArrayOfObjects(argArray, values)
                countCell.set(JAVA_INT, 0, checkedCount)
                val args = arena.allocate(ADDRESS, 2)
                args.setAtIndex(ADDRESS, 0, argArray)
                args.setAtIndex(ADDRESS, 1, countCell)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, argArray)
            }
        }
    }

    fun ptrcallWithTwoObjectListUInt32ArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstValues: List<*>,
        secondValues: List<*>,
        count: Long,
    ): Long {
        val checkedCount = BuiltinTypes.requireUInt32(count)
        Arena.ofConfined().use { arena ->
            val firstArray = arena.allocate(8L, 8L)
            val secondArray = arena.allocate(8L, 8L)
            val countCell = arena.allocate(JAVA_INT)
            try {
                BuiltinTypes.initArrayOfObjects(firstArray, firstValues)
                BuiltinTypes.initArrayOfObjects(secondArray, secondValues)
                countCell.set(JAVA_INT, 0, checkedCount)
                val args = arena.allocate(ADDRESS, 3)
                args.setAtIndex(ADDRESS, 0, firstArray)
                args.setAtIndex(ADDRESS, 1, secondArray)
                args.setAtIndex(ADDRESS, 2, countCell)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, secondArray)
                BuiltinTypes.destroyTyped(VariantType.ARRAY, firstArray)
            }
        }
    }

    fun ptrcallWithObjectListRIDUInt32ArgsRetRID(
        methodBind: MemorySegment,
        instance: MemorySegment,
        values: List<*>,
        rid: RID,
        set: Long,
    ): RID {
        val checkedSet = BuiltinTypes.requireUInt32(set)
        Arena.ofConfined().use { arena ->
            val objectArray = arena.allocate(8L, 8L)
            val ridCell = arena.allocate(JAVA_LONG)
            val setCell = arena.allocate(JAVA_INT)
            try {
                BuiltinTypes.initArrayOfObjects(objectArray, values)
                ridCell.set(JAVA_LONG, 0, rid.value)
                setCell.set(JAVA_INT, 0, checkedSet)
                val args = arena.allocate(ADDRESS, 3)
                args.setAtIndex(ADDRESS, 0, objectArray)
                args.setAtIndex(ADDRESS, 1, ridCell)
                args.setAtIndex(ADDRESS, 2, setCell)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return RID(ret.get(JAVA_LONG, 0))
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, objectArray)
            }
        }
    }

    fun ptrcallWithRIDUInt32ObjectListArgsRetRID(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        set: Long,
        values: List<*>,
    ): RID {
        val checkedSet = BuiltinTypes.requireUInt32(set)
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val setCell = arena.allocate(JAVA_INT)
            val objectArray = arena.allocate(8L, 8L)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                setCell.set(JAVA_INT, 0, checkedSet)
                BuiltinTypes.initArrayOfObjects(objectArray, values)
                val args = arena.allocate(ADDRESS, 3)
                args.setAtIndex(ADDRESS, 0, ridCell)
                args.setAtIndex(ADDRESS, 1, setCell)
                args.setAtIndex(ADDRESS, 2, objectArray)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return RID(ret.get(JAVA_LONG, 0))
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, objectArray)
            }
        }
    }

    fun ptrcallWithRIDObjectListArgsRetRID(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        values: List<*>,
    ): RID {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val objectArray = arena.allocate(8L, 8L)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                BuiltinTypes.initArrayOfObjects(objectArray, values)
                val args = arena.allocate(ADDRESS, 2)
                args.setAtIndex(ADDRESS, 0, ridCell)
                args.setAtIndex(ADDRESS, 1, objectArray)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return RID(ret.get(JAVA_LONG, 0))
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, objectArray)
            }
        }
    }

    fun ptrcallWithRIDObjectListObjectArgsRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        values: List<*>,
        objectArg: MemorySegment,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val objectArray = arena.allocate(8L, 8L)
            val objectCell = arena.allocate(ADDRESS)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                BuiltinTypes.initArrayOfObjects(objectArray, values)
                objectCell.set(ADDRESS, 0, objectArg)
                val args = arena.allocate(ADDRESS, 3)
                args.setAtIndex(ADDRESS, 0, ridCell)
                args.setAtIndex(ADDRESS, 1, objectArray)
                args.setAtIndex(ADDRESS, 2, objectCell)
                val ret = arena.allocate(JAVA_BYTE)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(JAVA_BYTE, 0) != 0.toByte()
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, objectArray)
            }
        }
    }

    fun ptrcallWithObjectListIntArgsRetObjectList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        values: List<*>,
        size: Int,
    ): List<GodotObject> {
        Arena.ofConfined().use { arena ->
            val argArray = arena.allocate(8L, 8L)
            val sizeCell = arena.allocate(JAVA_INT)
            try {
                BuiltinTypes.initArrayOfObjects(argArray, values)
                sizeCell.set(JAVA_INT, 0, size)
                val args = arena.allocate(ADDRESS, 2)
                args.setAtIndex(ADDRESS, 0, argArray)
                args.setAtIndex(ADDRESS, 1, sizeCell)
                return callArrayReturn(methodBind, instance, args, BuiltinTypes::readArrayObjects)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, argArray)
            }
        }
    }

    fun <T : Any> ptrcallWithObjectListIntArgsRetTypedObjectList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        values: List<*>,
        size: Int,
        wrapper: (MemorySegment) -> T?,
    ): List<T> =
        ptrcallWithObjectListIntArgsRetObjectList(methodBind, instance, values, size)
            .mapNotNull { wrapper(it.handle) }

    fun ptrcallWithStringAndObjectListArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        values: List<*>,
    ) {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val argArray = arena.allocate(8L, 8L)
            try {
                GodotStrings.initString(stringCell, text)
                BuiltinTypes.initArrayOfObjects(argArray, values)
                val args = arena.allocate(ADDRESS, 2)
                args.setAtIndex(ADDRESS, 0, stringCell)
                args.setAtIndex(ADDRESS, 1, argArray)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, argArray)
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithRIDAndObjectListArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        values: List<*>,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val argArray = arena.allocate(8L, 8L)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                BuiltinTypes.initArrayOfObjects(argArray, values)
                val args = arena.allocate(ADDRESS, 2)
                args.setAtIndex(ADDRESS, 0, ridCell)
                args.setAtIndex(ADDRESS, 1, argArray)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, argArray)
            }
        }
    }

    fun ptrcallWithStringAndArrayArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        values: List<Any?>,
    ) {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val argArray = arena.allocate(8L, 8L)
            try {
                GodotStrings.initString(stringCell, text)
                BuiltinTypes.initArray(argArray, values)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, stringCell)
                arr.setAtIndex(ADDRESS, 1, argArray)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, argArray)
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithArrayArgRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        values: List<Any?>,
    ): Long {
        Arena.ofConfined().use { arena ->
            val argArray = arena.allocate(8L, 8L)
            try {
                BuiltinTypes.initArray(argArray, values)
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, argArray)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, argArray)
            }
        }
    }

    fun ptrcallWithStringAndArrayArgRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        values: List<Any?>,
    ): Long {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val argArray = arena.allocate(8L, 8L)
            try {
                GodotStrings.initString(stringCell, text)
                BuiltinTypes.initArray(argArray, values)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, stringCell)
                arr.setAtIndex(ADDRESS, 1, argArray)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, argArray)
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithStringAndArrayArgRetLongAndArray(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        values: List<Any?>,
    ): Pair<Long, List<Any?>> {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val argArray = arena.allocate(8L, 8L)
            try {
                GodotStrings.initString(stringCell, text)
                BuiltinTypes.initArray(argArray, values)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, stringCell)
                arr.setAtIndex(ADDRESS, 1, argArray)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_LONG, 0) to BuiltinTypes.readArrayScalars(argArray)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, argArray)
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithStringBoolArrayArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        enabled: Boolean,
        values: List<Any?>,
    ) {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val boolCell = arena.allocate(JAVA_BYTE)
            val argArray = arena.allocate(8L, 8L)
            try {
                GodotStrings.initString(stringCell, text)
                boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
                BuiltinTypes.initArray(argArray, values)
                val arr = arena.allocate(ADDRESS, 3)
                arr.setAtIndex(ADDRESS, 0, stringCell)
                arr.setAtIndex(ADDRESS, 1, boolCell)
                arr.setAtIndex(ADDRESS, 2, argArray)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, argArray)
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithStringNameArrayArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
        values: List<Any?>,
    ) {
        Arena.ofConfined().use { arena ->
            val argArray = arena.allocate(8L, 8L)
            try {
                BuiltinTypes.initArray(argArray, values)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(name))
                arr.setAtIndex(ADDRESS, 1, argArray)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, argArray)
            }
        }
    }

    fun ptrcallWithStringNameArrayBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
        values: List<Any?>,
        enabled: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val argArray = arena.allocate(8L, 8L)
            val boolCell = arena.allocate(JAVA_BYTE)
            try {
                BuiltinTypes.initArray(argArray, values)
                boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
                val arr = arena.allocate(ADDRESS, 3)
                arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(name))
                arr.setAtIndex(ADDRESS, 1, argArray)
                arr.setAtIndex(ADDRESS, 2, boolCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, argArray)
            }
        }
    }

    fun ptrcallWithStringNameBoolArrayArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
        enabled: Boolean,
        values: List<Any?>,
    ) {
        Arena.ofConfined().use { arena ->
            val boolCell = arena.allocate(JAVA_BYTE)
            val argArray = arena.allocate(8L, 8L)
            try {
                boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
                BuiltinTypes.initArray(argArray, values)
                val arr = arena.allocate(ADDRESS, 3)
                arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(name))
                arr.setAtIndex(ADDRESS, 1, boolCell)
                arr.setAtIndex(ADDRESS, 2, argArray)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, argArray)
            }
        }
    }

    fun ptrcallWithArrayTransform3DArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        values: List<Any?>,
        transform: Transform3D,
    ) {
        Arena.ofConfined().use { arena ->
            val argArray = arena.allocate(8L, 8L)
            val transformCell = arena.allocate(GodotReal.SIZE_BYTES * 12, GodotReal.ALIGN_BYTES)
            try {
                BuiltinTypes.initArray(argArray, values)
                writeTransform3D(transformCell, transform)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, argArray)
                arr.setAtIndex(ADDRESS, 1, transformCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, argArray)
            }
        }
    }

    fun ptrcallWithTwoDoubleArrayArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Double,
        second: Double,
        values: List<Any?>,
    ) {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(JAVA_DOUBLE)
            val secondCell = arena.allocate(JAVA_DOUBLE)
            val argArray = arena.allocate(8L, 8L)
            try {
                firstCell.set(JAVA_DOUBLE, 0, first)
                secondCell.set(JAVA_DOUBLE, 0, second)
                BuiltinTypes.initArray(argArray, values)
                val arr = arena.allocate(ADDRESS, 3)
                arr.setAtIndex(ADDRESS, 0, firstCell)
                arr.setAtIndex(ADDRESS, 1, secondCell)
                arr.setAtIndex(ADDRESS, 2, argArray)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, argArray)
            }
        }
    }

    fun ptrcallWithNodePathArgRetArray(
        methodBind: MemorySegment,
        instance: MemorySegment,
        path: NodePath,
    ): List<Any?> {
        Arena.ofConfined().use { arena ->
            val nodePath = arena.allocate(8L, 8L)
            val pathString = arena.allocate(8L, 8L)
            try {
                GodotStrings.initString(pathString, path.path)
                BuiltinTypes.construct(
                    type = VariantType.NODE_PATH,
                    dest = nodePath,
                    constructorIndex = 2,
                    args = listOf(pathString),
                )
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, nodePath)
                return callArrayReturn(methodBind, instance, arr, BuiltinTypes::readArrayScalars)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.NODE_PATH, nodePath)
                GodotStrings.destroyString(pathString)
            }
        }
    }

    fun ptrcallWithStringAndDictionaryArgRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        values: Map<String, Any?>,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val dict = arena.allocate(8L, 8L)
            try {
                GodotStrings.initString(stringCell, text)
                BuiltinTypes.initDictionary(dict, values)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, stringCell)
                arr.setAtIndex(ADDRESS, 1, dict)
                val ret = arena.allocate(ADDRESS)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(ADDRESS, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.DICTIONARY, dict)
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithObjectThreeStringArgsRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        first: String,
        second: String,
        third: String,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val objectCell = arena.allocate(ADDRESS)
            val firstCell = arena.allocate(8L, 8L)
            val secondCell = arena.allocate(8L, 8L)
            val thirdCell = arena.allocate(8L, 8L)
            var firstInitialized = false
            var secondInitialized = false
            var thirdInitialized = false
            try {
                objectCell.set(ADDRESS, 0, objectArg)
                GodotStrings.initString(firstCell, first)
                firstInitialized = true
                GodotStrings.initString(secondCell, second)
                secondInitialized = true
                GodotStrings.initString(thirdCell, third)
                thirdInitialized = true
                val args = arena.allocate(ADDRESS, 4)
                args.setAtIndex(ADDRESS, 0, objectCell)
                args.setAtIndex(ADDRESS, 1, firstCell)
                args.setAtIndex(ADDRESS, 2, secondCell)
                args.setAtIndex(ADDRESS, 3, thirdCell)
                val ret = arena.allocate(ADDRESS)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(ADDRESS, 0)
            } finally {
                if (thirdInitialized) GodotStrings.destroyString(thirdCell)
                if (secondInitialized) GodotStrings.destroyString(secondCell)
                if (firstInitialized) GodotStrings.destroyString(firstCell)
            }
        }
    }

    fun ptrcallWithStringTwoDoubleDictionaryArgsRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        first: Double,
        second: Double,
        values: Map<String, Any?>,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val firstCell = arena.allocate(JAVA_DOUBLE)
            val secondCell = arena.allocate(JAVA_DOUBLE)
            val dict = arena.allocate(8L, 8L)
            try {
                GodotStrings.initString(stringCell, text)
                firstCell.set(JAVA_DOUBLE, 0, first)
                secondCell.set(JAVA_DOUBLE, 0, second)
                BuiltinTypes.initDictionary(dict, values)
                val args = arena.allocate(ADDRESS, 4)
                args.setAtIndex(ADDRESS, 0, stringCell)
                args.setAtIndex(ADDRESS, 1, firstCell)
                args.setAtIndex(ADDRESS, 2, secondCell)
                args.setAtIndex(ADDRESS, 3, dict)
                val ret = arena.allocate(ADDRESS)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(ADDRESS, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.DICTIONARY, dict)
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithObjectLongStringLongStringUInt32BoolArgsRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        firstLong: Long,
        firstText: String,
        secondLong: Long,
        secondText: String,
        uint32Value: Long,
        enabled: Boolean,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val objectCell = arena.allocate(ADDRESS)
            val firstLongCell = arena.allocate(JAVA_LONG)
            val firstStringCell = arena.allocate(8L, 8L)
            val secondLongCell = arena.allocate(JAVA_LONG)
            val secondStringCell = arena.allocate(8L, 8L)
            val uint32Cell = arena.allocate(JAVA_INT)
            val boolCell = arena.allocate(JAVA_BYTE)
            var firstStringInitialized = false
            var secondStringInitialized = false
            try {
                objectCell.set(ADDRESS, 0, objectArg)
                firstLongCell.set(JAVA_LONG, 0, firstLong)
                GodotStrings.initString(firstStringCell, firstText)
                firstStringInitialized = true
                secondLongCell.set(JAVA_LONG, 0, secondLong)
                GodotStrings.initString(secondStringCell, secondText)
                secondStringInitialized = true
                uint32Cell.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(uint32Value))
                boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
                val args = arena.allocate(ADDRESS, 7)
                args.setAtIndex(ADDRESS, 0, objectCell)
                args.setAtIndex(ADDRESS, 1, firstLongCell)
                args.setAtIndex(ADDRESS, 2, firstStringCell)
                args.setAtIndex(ADDRESS, 3, secondLongCell)
                args.setAtIndex(ADDRESS, 4, secondStringCell)
                args.setAtIndex(ADDRESS, 5, uint32Cell)
                args.setAtIndex(ADDRESS, 6, boolCell)
                val ret = arena.allocate(ADDRESS)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(ADDRESS, 0)
            } finally {
                if (secondStringInitialized) GodotStrings.destroyString(secondStringCell)
                if (firstStringInitialized) GodotStrings.destroyString(firstStringCell)
            }
        }
    }

    fun ptrcallWithObjectNodePathObjectIntArgsRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstObject: MemorySegment,
        path: NodePath,
        secondObject: MemorySegment,
        intValue: Int,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val firstObjectCell = arena.allocate(ADDRESS)
            val nodePath = arena.allocate(8L, 8L)
            val pathString = arena.allocate(8L, 8L)
            val secondObjectCell = arena.allocate(ADDRESS)
            val intCell = arena.allocate(JAVA_INT)
            try {
                firstObjectCell.set(ADDRESS, 0, firstObject)
                GodotStrings.initString(pathString, path.path)
                BuiltinTypes.construct(
                    type = VariantType.NODE_PATH,
                    dest = nodePath,
                    constructorIndex = 2,
                    args = listOf(pathString),
                )
                secondObjectCell.set(ADDRESS, 0, secondObject)
                intCell.set(JAVA_INT, 0, intValue)
                val args = arena.allocate(ADDRESS, 4)
                args.setAtIndex(ADDRESS, 0, firstObjectCell)
                args.setAtIndex(ADDRESS, 1, nodePath)
                args.setAtIndex(ADDRESS, 2, secondObjectCell)
                args.setAtIndex(ADDRESS, 3, intCell)
                val ret = arena.allocate(ADDRESS)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(ADDRESS, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.NODE_PATH, nodePath)
                GodotStrings.destroyString(pathString)
            }
        }
    }

    fun ptrcallWithBoolObjectArgsRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        enabled: Boolean,
        objectArg: MemorySegment,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val boolCell = arena.allocate(JAVA_BYTE)
            val objectCell = arena.allocate(ADDRESS)
            boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
            objectCell.set(ADDRESS, 0, objectArg)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, boolCell)
            args.setAtIndex(ADDRESS, 1, objectCell)
            val ret = arena.allocate(ADDRESS)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(ADDRESS, 0)
        }
    }

    fun ptrcallWithTransform3DRIDArgsRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        transform: Transform3D,
        rid: RID,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val transformCell = arena.allocate(GodotReal.SIZE_BYTES * 12, GodotReal.ALIGN_BYTES)
            val ridCell = arena.allocate(JAVA_LONG)
            writeTransform3D(transformCell, transform)
            ridCell.set(JAVA_LONG, 0, rid.value)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, transformCell)
            args.setAtIndex(ADDRESS, 1, ridCell)
            val ret = arena.allocate(ADDRESS)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(ADDRESS, 0)
        }
    }

    fun ptrcallWithTwoIntTwoBoolDoubleBoolArgsRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Int,
        firstBool: Boolean,
        secondBool: Boolean,
        value: Double,
        thirdBool: Boolean,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(JAVA_INT)
            val secondCell = arena.allocate(JAVA_INT)
            val firstBoolCell = arena.allocate(JAVA_BYTE)
            val secondBoolCell = arena.allocate(JAVA_BYTE)
            val valueCell = arena.allocate(JAVA_DOUBLE)
            val thirdBoolCell = arena.allocate(JAVA_BYTE)
            firstCell.set(JAVA_INT, 0, first)
            secondCell.set(JAVA_INT, 0, second)
            firstBoolCell.set(JAVA_BYTE, 0, if (firstBool) 1.toByte() else 0.toByte())
            secondBoolCell.set(JAVA_BYTE, 0, if (secondBool) 1.toByte() else 0.toByte())
            valueCell.set(JAVA_DOUBLE, 0, value)
            thirdBoolCell.set(JAVA_BYTE, 0, if (thirdBool) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 6)
            args.setAtIndex(ADDRESS, 0, firstCell)
            args.setAtIndex(ADDRESS, 1, secondCell)
            args.setAtIndex(ADDRESS, 2, firstBoolCell)
            args.setAtIndex(ADDRESS, 3, secondBoolCell)
            args.setAtIndex(ADDRESS, 4, valueCell)
            args.setAtIndex(ADDRESS, 5, thirdBoolCell)
            val ret = arena.allocate(ADDRESS)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(ADDRESS, 0)
        }
    }

    fun ptrcallWithTypedStringListArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        values: List<String>,
    ) {
        Arena.ofConfined().use { arena ->
            val argArray = arena.allocate(8L, 8L)
            try {
                BuiltinTypes.initArray(argArray, values)
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, argArray)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, argArray)
            }
        }
    }

    fun ptrcallWithTypedIntListArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        values: List<Long>,
    ) {
        Arena.ofConfined().use { arena ->
            val argArray = arena.allocate(8L, 8L)
            try {
                BuiltinTypes.initArray(argArray, values)
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, argArray)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, argArray)
            }
        }
    }

    fun ptrcallWithDictionaryListArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        values: List<Map<String, Any?>>,
    ) {
        Arena.ofConfined().use { arena ->
            val argArray = arena.allocate(8L, 8L)
            try {
                BuiltinTypes.initArrayOfDictionaries(argArray, values)
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, argArray)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, argArray)
            }
        }
    }

    fun ptrcallWithDictionaryListIntArgsRetRID(
        methodBind: MemorySegment,
        instance: MemorySegment,
        values: List<Map<String, Any?>>,
        compression: Int,
    ): RID {
        Arena.ofConfined().use { arena ->
            val dictionaryArray = arena.allocate(8L, 8L)
            val compressionCell = arena.allocate(JAVA_INT)
            try {
                BuiltinTypes.initArrayOfDictionaries(dictionaryArray, values)
                compressionCell.set(JAVA_INT, 0, compression)
                val args = arena.allocate(ADDRESS, 2)
                args.setAtIndex(ADDRESS, 0, dictionaryArray)
                args.setAtIndex(ADDRESS, 1, compressionCell)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return RID(ret.get(JAVA_LONG, 0))
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, dictionaryArray)
            }
        }
    }

    fun ptrcallWithNodePathListArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        values: List<NodePath>,
    ) {
        Arena.ofConfined().use { arena ->
            val argArray = arena.allocate(8L, 8L)
            try {
                BuiltinTypes.initArray(argArray, values)
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, argArray)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, argArray)
            }
        }
    }

    fun ptrcallWithTypedMaterialListArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        values: List<Material>,
    ) {
        Arena.ofConfined().use { arena ->
            val argArray = arena.allocate(8L, 8L)
            try {
                BuiltinTypes.initArray(argArray, values)
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, argArray)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, argArray)
            }
        }
    }

    fun ptrcallWithVector2iListArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        values: List<Vector2i>,
    ) {
        Arena.ofConfined().use { arena ->
            val argArray = arena.allocate(8L, 8L)
            try {
                BuiltinTypes.initArray(argArray, values)
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, argArray)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, argArray)
            }
        }
    }

    fun ptrcallWithVector2iListTwoIntAndBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        values: List<Vector2i>,
        firstInt: Int,
        secondInt: Int,
        boolArg: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val argArray = arena.allocate(8L, 8L)
            val int0 = arena.allocate(JAVA_INT)
            val int1 = arena.allocate(JAVA_INT)
            val boolCell = arena.allocate(JAVA_BYTE)
            try {
                BuiltinTypes.initArray(argArray, values)
                int0.set(JAVA_INT, 0, firstInt)
                int1.set(JAVA_INT, 0, secondInt)
                boolCell.set(JAVA_BYTE, 0, if (boolArg) 1.toByte() else 0.toByte())
                val arr = arena.allocate(ADDRESS, 4)
                arr.setAtIndex(ADDRESS, 0, argArray)
                arr.setAtIndex(ADDRESS, 1, int0)
                arr.setAtIndex(ADDRESS, 2, int1)
                arr.setAtIndex(ADDRESS, 3, boolCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, argArray)
            }
        }
    }

    fun ptrcallWithIntVector2iListTwoIntAndBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        intArg: Int,
        values: List<Vector2i>,
        firstInt: Int,
        secondInt: Int,
        boolArg: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val int0 = arena.allocate(JAVA_INT)
            val argArray = arena.allocate(8L, 8L)
            val int1 = arena.allocate(JAVA_INT)
            val int2 = arena.allocate(JAVA_INT)
            val boolCell = arena.allocate(JAVA_BYTE)
            try {
                int0.set(JAVA_INT, 0, intArg)
                BuiltinTypes.initArray(argArray, values)
                int1.set(JAVA_INT, 0, firstInt)
                int2.set(JAVA_INT, 0, secondInt)
                boolCell.set(JAVA_BYTE, 0, if (boolArg) 1.toByte() else 0.toByte())
                val arr = arena.allocate(ADDRESS, 5)
                arr.setAtIndex(ADDRESS, 0, int0)
                arr.setAtIndex(ADDRESS, 1, argArray)
                arr.setAtIndex(ADDRESS, 2, int1)
                arr.setAtIndex(ADDRESS, 3, int2)
                arr.setAtIndex(ADDRESS, 4, boolCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, argArray)
            }
        }
    }

    fun ptrcallWithVector3iListArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        values: List<Vector3i>,
    ) {
        Arena.ofConfined().use { arena ->
            val argArray = arena.allocate(8L, 8L)
            try {
                BuiltinTypes.initArray(argArray, values)
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, argArray)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, argArray)
            }
        }
    }

    /**
     * Calls [methodBind] with one int64 arg and String return value.
     */
    fun ptrcallWithLongArgRetString(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Long,
    ): String {
        Arena.ofConfined().use { arena ->
            val arg = arena.allocate(JAVA_LONG)
            arg.set(JAVA_LONG, 0, value)
            val retString = arena.allocate(8L, 8L)
            try {
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, arg)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, retString)
                return GodotStrings.readString(retString)
            } finally {
                GodotStrings.destroyString(retString)
            }
        }
    }

    /**
     * Calls [methodBind] with one String arg and int64 return value.
     */
    fun ptrcallWithStringArgRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: String,
    ): Long {
        Arena.ofConfined().use { arena ->
            val argString = arena.allocate(8L, 8L)
            try {
                GodotStrings.initString(argString, value)
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, argString)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                GodotStrings.destroyString(argString)
            }
        }
    }

    /**
     * Calls [methodBind] with one String arg and int32 return value.
     */
    fun ptrcallWithStringArgRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: String,
    ): Int {
        Arena.ofConfined().use { arena ->
            val argString = arena.allocate(8L, 8L)
            try {
                GodotStrings.initString(argString, value)
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, argString)
                val ret = arena.allocate(JAVA_INT)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_INT, 0)
            } finally {
                GodotStrings.destroyString(argString)
            }
        }
    }

    /**
     * Calls [methodBind] with (String, bool, int64) and bool return value.
     */
    fun ptrcallWithStringBoolLongArgsRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        flag: Boolean,
        number: Long,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val argString = arena.allocate(8L, 8L)
            try {
                GodotStrings.initString(argString, text)
                val argBool = arena.allocate(java.lang.foreign.ValueLayout.JAVA_BYTE)
                argBool.set(java.lang.foreign.ValueLayout.JAVA_BYTE, 0, if (flag) 1.toByte() else 0.toByte())
                val argLong = arena.allocate(JAVA_LONG)
                argLong.set(JAVA_LONG, 0, number)
                val arr = arena.allocate(ADDRESS, 3)
                arr.setAtIndex(ADDRESS, 0, argString)
                arr.setAtIndex(ADDRESS, 1, argBool)
                arr.setAtIndex(ADDRESS, 2, argLong)
                val ret = arena.allocate(java.lang.foreign.ValueLayout.JAVA_BYTE)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(java.lang.foreign.ValueLayout.JAVA_BYTE, 0) != 0.toByte()
            } finally {
                GodotStrings.destroyString(argString)
            }
        }
    }

    /**
     * Calls [methodBind] with (String, bool, int32) and bool return value.
     */
    fun ptrcallWithStringBoolIntArgsRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        flag: Boolean,
        number: Int,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val argString = arena.allocate(8L, 8L)
            try {
                GodotStrings.initString(argString, text)
                val argBool = arena.allocate(JAVA_BYTE)
                argBool.set(JAVA_BYTE, 0, if (flag) 1.toByte() else 0.toByte())
                val argInt = arena.allocate(JAVA_INT)
                argInt.set(JAVA_INT, 0, number)
                val arr = arena.allocate(ADDRESS, 3)
                arr.setAtIndex(ADDRESS, 0, argString)
                arr.setAtIndex(ADDRESS, 1, argBool)
                arr.setAtIndex(ADDRESS, 2, argInt)
                val ret = arena.allocate(JAVA_BYTE)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_BYTE, 0) != 0.toByte()
            } finally {
                GodotStrings.destroyString(argString)
            }
        }
    }

    /**
     * Calls [methodBind] with (String, bool) args and int64 return value.
     */
    fun ptrcallWithStringAndBoolArgRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        value: Boolean,
    ): Long {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(8L, 8L)
            val arg1 = arena.allocate(java.lang.foreign.ValueLayout.JAVA_BYTE)
            try {
                GodotStrings.initString(arg0, text)
                arg1.set(java.lang.foreign.ValueLayout.JAVA_BYTE, 0, if (value) 1.toByte() else 0.toByte())
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, arg0)
                arr.setAtIndex(ADDRESS, 1, arg1)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                GodotStrings.destroyString(arg0)
            }
        }
    }

    /**
     * Calls [methodBind] with (String, bool) args and int32 return value.
     */
    fun ptrcallWithStringAndBoolArgRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        value: Boolean,
    ): Int {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(8L, 8L)
            val arg1 = arena.allocate(JAVA_BYTE)
            try {
                GodotStrings.initString(arg0, text)
                arg1.set(JAVA_BYTE, 0, if (value) 1.toByte() else 0.toByte())
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, arg0)
                arr.setAtIndex(ADDRESS, 1, arg1)
                val ret = arena.allocate(JAVA_INT)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_INT, 0)
            } finally {
                GodotStrings.destroyString(arg0)
            }
        }
    }

    /**
     * Calls [methodBind] with (String, bool) args and decodes PackedByteArray return.
     */
    fun ptrcallWithStringAndBoolArgRetByteArray(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        value: Boolean,
    ): ByteArray {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(8L, 8L)
            val arg1 = arena.allocate(JAVA_BYTE)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            try {
                GodotStrings.initString(arg0, text)
                arg1.set(JAVA_BYTE, 0, if (value) 1.toByte() else 0.toByte())
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, arg0)
                arr.setAtIndex(ADDRESS, 1, arg1)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return BuiltinTypes.readPackedByteArray(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, ret)
                GodotStrings.destroyString(arg0)
            }
        }
    }

    /**
     * Calls [methodBind] with (String, int64) args and int64 return value.
     */
    fun ptrcallWithStringAndLongArgRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        value: Long,
    ): Long {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(8L, 8L)
            val arg1 = arena.allocate(JAVA_LONG)
            try {
                GodotStrings.initString(arg0, text)
                arg1.set(JAVA_LONG, 0, value)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, arg0)
                arr.setAtIndex(ADDRESS, 1, arg1)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                GodotStrings.destroyString(arg0)
            }
        }
    }

    /**
     * Calls [methodBind] with (String, int64) args and String return value.
     */
    fun ptrcallWithStringAndLongArgRetString(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        value: Long,
    ): String {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(8L, 8L)
            val arg1 = arena.allocate(JAVA_LONG)
            val ret = arena.allocate(8L, 8L)
            try {
                GodotStrings.initString(arg0, text)
                arg1.set(JAVA_LONG, 0, value)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, arg0)
                arr.setAtIndex(ADDRESS, 1, arg1)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return GodotStrings.readString(ret)
            } finally {
                GodotStrings.destroyString(ret)
                GodotStrings.destroyString(arg0)
            }
        }
    }

    /**
     * Calls [methodBind] with (String, int64) args and int32 return value.
     */
    fun ptrcallWithStringAndLongArgRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        value: Long,
    ): Int {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(8L, 8L)
            val arg1 = arena.allocate(JAVA_LONG)
            try {
                GodotStrings.initString(arg0, text)
                arg1.set(JAVA_LONG, 0, value)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, arg0)
                arr.setAtIndex(ADDRESS, 1, arg1)
                val ret = arena.allocate(JAVA_INT)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_INT, 0)
            } finally {
                GodotStrings.destroyString(arg0)
            }
        }
    }

    /**
     * Calls [methodBind] with (String, int64) args and decodes PackedStringArray return.
     */
    fun ptrcallWithStringAndLongArgRetPackedStringList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        value: Long,
    ): List<String> {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(8L, 8L)
            val arg1 = arena.allocate(JAVA_LONG)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            try {
                GodotStrings.initString(arg0, text)
                arg1.set(JAVA_LONG, 0, value)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, arg0)
                arr.setAtIndex(ADDRESS, 1, arg1)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return BuiltinTypes.readPackedStringArray(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_STRING_ARRAY, ret)
                GodotStrings.destroyString(arg0)
            }
        }
    }

    /**
     * Calls [methodBind] with (String, String, String) args and int64 return value.
     */
    fun ptrcallWithThreeStringArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: String,
        second: String,
        third: String,
    ): Long {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(8L, 8L)
            val arg1 = arena.allocate(8L, 8L)
            val arg2 = arena.allocate(8L, 8L)
            try {
                GodotStrings.initString(arg0, first)
                GodotStrings.initString(arg1, second)
                GodotStrings.initString(arg2, third)
                val arr = arena.allocate(ADDRESS, 3)
                arr.setAtIndex(ADDRESS, 0, arg0)
                arr.setAtIndex(ADDRESS, 1, arg1)
                arr.setAtIndex(ADDRESS, 2, arg2)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                GodotStrings.destroyString(arg0)
                GodotStrings.destroyString(arg1)
                GodotStrings.destroyString(arg2)
            }
        }
    }

    fun ptrcallWithStringStringStringStringStringLongArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: String,
        second: String,
        third: String,
        fourth: String,
        fifth: String,
        longValue: Long,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(8L, 8L)
            val arg1 = arena.allocate(8L, 8L)
            val arg2 = arena.allocate(8L, 8L)
            val arg3 = arena.allocate(8L, 8L)
            val arg4 = arena.allocate(8L, 8L)
            val longCell = arena.allocate(JAVA_LONG)
            try {
                GodotStrings.initString(arg0, first)
                GodotStrings.initString(arg1, second)
                GodotStrings.initString(arg2, third)
                GodotStrings.initString(arg3, fourth)
                GodotStrings.initString(arg4, fifth)
                longCell.set(JAVA_LONG, 0, longValue)
                val args = arena.allocate(ADDRESS, 6)
                args.setAtIndex(ADDRESS, 0, arg0)
                args.setAtIndex(ADDRESS, 1, arg1)
                args.setAtIndex(ADDRESS, 2, arg2)
                args.setAtIndex(ADDRESS, 3, arg3)
                args.setAtIndex(ADDRESS, 4, arg4)
                args.setAtIndex(ADDRESS, 5, longCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(arg4)
                GodotStrings.destroyString(arg3)
                GodotStrings.destroyString(arg2)
                GodotStrings.destroyString(arg1)
                GodotStrings.destroyString(arg0)
            }
        }
    }

    fun ptrcallWithFourStringTwoIntBoolArgsRetPackedStringList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: String,
        second: String,
        third: String,
        fourth: String,
        firstInt: Int,
        secondInt: Int,
        enabled: Boolean,
    ): List<String> {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(8L, 8L)
            val secondCell = arena.allocate(8L, 8L)
            val thirdCell = arena.allocate(8L, 8L)
            val fourthCell = arena.allocate(8L, 8L)
            val firstIntCell = arena.allocate(JAVA_INT)
            val secondIntCell = arena.allocate(JAVA_INT)
            val boolCell = arena.allocate(JAVA_BYTE)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            var firstInitialized = false
            var secondInitialized = false
            var thirdInitialized = false
            var fourthInitialized = false
            try {
                GodotStrings.initString(firstCell, first)
                firstInitialized = true
                GodotStrings.initString(secondCell, second)
                secondInitialized = true
                GodotStrings.initString(thirdCell, third)
                thirdInitialized = true
                GodotStrings.initString(fourthCell, fourth)
                fourthInitialized = true
                firstIntCell.set(JAVA_INT, 0, firstInt)
                secondIntCell.set(JAVA_INT, 0, secondInt)
                boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
                val args = arena.allocate(ADDRESS, 7)
                args.setAtIndex(ADDRESS, 0, firstCell)
                args.setAtIndex(ADDRESS, 1, secondCell)
                args.setAtIndex(ADDRESS, 2, thirdCell)
                args.setAtIndex(ADDRESS, 3, fourthCell)
                args.setAtIndex(ADDRESS, 4, firstIntCell)
                args.setAtIndex(ADDRESS, 5, secondIntCell)
                args.setAtIndex(ADDRESS, 6, boolCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return BuiltinTypes.readPackedStringArray(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_STRING_ARRAY, ret)
                if (fourthInitialized) GodotStrings.destroyString(fourthCell)
                if (thirdInitialized) GodotStrings.destroyString(thirdCell)
                if (secondInitialized) GodotStrings.destroyString(secondCell)
                if (firstInitialized) GodotStrings.destroyString(firstCell)
            }
        }
    }

    /**
     * Calls [methodBind] with two scalar float args and scalar float return value.
     */
    fun ptrcallWithTwoDoubleArgsRetDouble(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Double,
        second: Double,
    ): Double {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_DOUBLE)
            val arg1 = arena.allocate(JAVA_DOUBLE)
            arg0.set(JAVA_DOUBLE, 0, first)
            arg1.set(JAVA_DOUBLE, 0, second)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            val ret = arena.allocate(JAVA_DOUBLE)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_DOUBLE, 0)
        }
    }

    /**
     * Calls [methodBind] with three scalar float args and no return value.
     */
    fun ptrcallWithThreeDoubleArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Double,
        second: Double,
        third: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_DOUBLE)
            val arg1 = arena.allocate(JAVA_DOUBLE)
            val arg2 = arena.allocate(JAVA_DOUBLE)
            arg0.set(JAVA_DOUBLE, 0, first)
            arg1.set(JAVA_DOUBLE, 0, second)
            arg2.set(JAVA_DOUBLE, 0, third)
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (scalar float, scalar float, int32) args and no return value.
     */
    fun ptrcallWithTwoDoubleAndIntArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Double,
        second: Double,
        intArg: Int,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_DOUBLE)
            val arg1 = arena.allocate(JAVA_DOUBLE)
            val arg2 = arena.allocate(JAVA_INT)
            arg0.set(JAVA_DOUBLE, 0, first)
            arg1.set(JAVA_DOUBLE, 0, second)
            arg2.set(JAVA_INT, 0, intArg)
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with one PackedInt32Array arg and no return value.
     */
    fun ptrcallWithPackedInt32ListArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        values: List<Int>,
    ) {
        Arena.ofConfined().use { arena ->
            val argPacked = BuiltinTypes.allocatePackedArray(arena)
            try {
                BuiltinTypes.initPackedInt32Array(argPacked, values)
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, argPacked)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_INT32_ARRAY, argPacked)
            }
        }
    }

    /**
     * Calls [methodBind] with one PackedInt64Array arg and no return value.
     */
    fun ptrcallWithPackedInt64ListArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        values: List<Long>,
    ) {
        Arena.ofConfined().use { arena ->
            val argPacked = BuiltinTypes.allocatePackedArray(arena)
            try {
                BuiltinTypes.initPackedInt64Array(argPacked, values)
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, argPacked)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_INT64_ARRAY, argPacked)
            }
        }
    }

    /**
     * Calls [methodBind] with one PackedFloat32Array arg and no return value.
     * Elements use Godot's scalar float ABI slot and are narrowed to packed float32 storage by Godot.
     */
    fun ptrcallWithPackedFloat32ListArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        values: List<Float>,
    ) {
        Arena.ofConfined().use { arena ->
            val argPacked = BuiltinTypes.allocatePackedArray(arena)
            try {
                BuiltinTypes.initPackedFloat32Array(argPacked, values)
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, argPacked)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_FLOAT32_ARRAY, argPacked)
            }
        }
    }

    fun ptrcallWithTwoPackedFloat32ListArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: List<Float>,
        second: List<Float>,
    ) {
        Arena.ofConfined().use { arena ->
            val firstPacked = BuiltinTypes.allocatePackedArray(arena)
            val secondPacked = BuiltinTypes.allocatePackedArray(arena)
            try {
                BuiltinTypes.initPackedFloat32Array(firstPacked, first)
                BuiltinTypes.initPackedFloat32Array(secondPacked, second)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, firstPacked)
                arr.setAtIndex(ADDRESS, 1, secondPacked)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_FLOAT32_ARRAY, secondPacked)
                BuiltinTypes.destroyTyped(VariantType.PACKED_FLOAT32_ARRAY, firstPacked)
            }
        }
    }

    fun ptrcallWithPackedFloat32ListAndPackedInt32ListArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        floats: List<Float>,
        ints: List<Int>,
    ) {
        Arena.ofConfined().use { arena ->
            val floatPacked = BuiltinTypes.allocatePackedArray(arena)
            val intPacked = BuiltinTypes.allocatePackedArray(arena)
            try {
                BuiltinTypes.initPackedFloat32Array(floatPacked, floats)
                BuiltinTypes.initPackedInt32Array(intPacked, ints)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, floatPacked)
                arr.setAtIndex(ADDRESS, 1, intPacked)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_INT32_ARRAY, intPacked)
                BuiltinTypes.destroyTyped(VariantType.PACKED_FLOAT32_ARRAY, floatPacked)
            }
        }
    }

    /**
     * Calls [methodBind] with (PackedFloat32Array, bool) args and scalar float return value.
     * Packed elements are Float, while the scalar return uses the 64-bit Godot float ABI slot.
     */
    fun ptrcallWithPackedFloat32ListAndBoolArgRetDouble(
        methodBind: MemorySegment,
        instance: MemorySegment,
        values: List<Float>,
        boolArg: Boolean,
    ): Double {
        Arena.ofConfined().use { arena ->
            val argPacked = BuiltinTypes.allocatePackedArray(arena)
            val boolCell = arena.allocate(JAVA_BYTE)
            try {
                BuiltinTypes.initPackedFloat32Array(argPacked, values)
                boolCell.set(JAVA_BYTE, 0, if (boolArg) 1.toByte() else 0.toByte())
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, argPacked)
                arr.setAtIndex(ADDRESS, 1, boolCell)
                val ret = arena.allocate(JAVA_DOUBLE)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_DOUBLE, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_FLOAT32_ARRAY, argPacked)
            }
        }
    }

    /**
     * Calls [methodBind] with one int32 arg and one PackedFloat32Array arg.
     * Elements use Godot's scalar float ABI slot and are narrowed to packed float32 storage by Godot.
     */
    fun ptrcallWithIntAndPackedFloat32ListArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        index: Int,
        values: List<Float>,
    ) {
        Arena.ofConfined().use { arena ->
            val indexCell = arena.allocate(JAVA_INT)
            val argPacked = BuiltinTypes.allocatePackedArray(arena)
            try {
                indexCell.set(JAVA_INT, 0, index)
                BuiltinTypes.initPackedFloat32Array(argPacked, values)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, indexCell)
                arr.setAtIndex(ADDRESS, 1, argPacked)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_FLOAT32_ARRAY, argPacked)
            }
        }
    }

    /**
     * Calls [methodBind] with one PackedFloat64Array arg and no return value.
     * Elements use Godot's scalar float ABI slot and stay packed float64 storage.
     */
    fun ptrcallWithPackedFloat64ListArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        values: List<Double>,
    ) {
        Arena.ofConfined().use { arena ->
            val argPacked = BuiltinTypes.allocatePackedArray(arena)
            try {
                BuiltinTypes.initPackedFloat64Array(argPacked, values)
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, argPacked)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_FLOAT64_ARRAY, argPacked)
            }
        }
    }

    /**
     * Calls [methodBind] with one PackedVector2Array arg and no return value.
     * Elements use GodotReal component storage, matching Vector2 ptrcall layout.
     */
    fun ptrcallWithPackedVector2ListArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        values: List<Vector2>,
    ) {
        Arena.ofConfined().use { arena ->
            val argPacked = BuiltinTypes.allocatePackedArray(arena)
            try {
                BuiltinTypes.initPackedVector2Array(argPacked, values)
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, argPacked)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR2_ARRAY, argPacked)
            }
        }
    }

    fun ptrcallWithPackedVector2ListArgRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        values: List<Vector2>,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val argPacked = BuiltinTypes.allocatePackedArray(arena)
            try {
                BuiltinTypes.initPackedVector2Array(argPacked, values)
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, argPacked)
                val ret = arena.allocate(JAVA_BYTE)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_BYTE, 0) != 0.toByte()
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR2_ARRAY, argPacked)
            }
        }
    }

    fun ptrcallWithPackedVector2ListArgRetDictionary(
        methodBind: MemorySegment,
        instance: MemorySegment,
        values: List<Vector2>,
    ): Map<String, Any?> {
        Arena.ofConfined().use { arena ->
            val argPacked = BuiltinTypes.allocatePackedArray(arena)
            val ret = arena.allocate(8L, 8L)
            try {
                BuiltinTypes.initPackedVector2Array(argPacked, values)
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, argPacked)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return BuiltinTypes.readDictionaryScalars(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.DICTIONARY, ret)
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR2_ARRAY, argPacked)
            }
        }
    }

    fun ptrcallWithPackedVector2ListAndBoolArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        values: List<Vector2>,
        enabled: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val argPacked = BuiltinTypes.allocatePackedArray(arena)
            val boolCell = arena.allocate(JAVA_BYTE)
            try {
                BuiltinTypes.initPackedVector2Array(argPacked, values)
                boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, argPacked)
                arr.setAtIndex(ADDRESS, 1, boolCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR2_ARRAY, argPacked)
            }
        }
    }

    fun ptrcallWithPackedVector2ListAndDoubleArgRetPackedVector2List(
        methodBind: MemorySegment,
        instance: MemorySegment,
        values: List<Vector2>,
        tolerance: Double,
    ): List<Vector2> {
        Arena.ofConfined().use { arena ->
            val argPacked = BuiltinTypes.allocatePackedArray(arena)
            val doubleCell = arena.allocate(JAVA_DOUBLE)
            try {
                BuiltinTypes.initPackedVector2Array(argPacked, values)
                doubleCell.set(JAVA_DOUBLE, 0, tolerance)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, argPacked)
                arr.setAtIndex(ADDRESS, 1, doubleCell)
                val ret = BuiltinTypes.allocatePackedArray(arena)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return try {
                    BuiltinTypes.readPackedVector2Array(ret)
                } finally {
                    BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR2_ARRAY, ret)
                }
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR2_ARRAY, argPacked)
            }
        }
    }

    fun ptrcallWithPackedVector2ListAndPackedInt32ListArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        points: List<Vector2>,
        indices: List<Int>,
    ) {
        Arena.ofConfined().use { arena ->
            val pointsPacked = BuiltinTypes.allocatePackedArray(arena)
            val indicesPacked = BuiltinTypes.allocatePackedArray(arena)
            try {
                BuiltinTypes.initPackedVector2Array(pointsPacked, points)
                BuiltinTypes.initPackedInt32Array(indicesPacked, indices)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, pointsPacked)
                arr.setAtIndex(ADDRESS, 1, indicesPacked)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_INT32_ARRAY, indicesPacked)
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR2_ARRAY, pointsPacked)
            }
        }
    }

    fun ptrcallWithPackedVector2ListArgRetPackedInt32List(
        methodBind: MemorySegment,
        instance: MemorySegment,
        values: List<Vector2>,
    ): List<Int> {
        Arena.ofConfined().use { arena ->
            val argPacked = BuiltinTypes.allocatePackedArray(arena)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            try {
                BuiltinTypes.initPackedVector2Array(argPacked, values)
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, argPacked)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return BuiltinTypes.readPackedInt32Array(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_INT32_ARRAY, ret)
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR2_ARRAY, argPacked)
            }
        }
    }

    fun ptrcallWithPackedVector2ListArgRetPackedVector2ListList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        values: List<Vector2>,
    ): List<List<Vector2>> {
        Arena.ofConfined().use { arena ->
            val argPacked = BuiltinTypes.allocatePackedArray(arena)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            try {
                BuiltinTypes.initPackedVector2Array(argPacked, values)
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, argPacked)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return BuiltinTypes.readArrayPackedVector2Arrays(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, ret)
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR2_ARRAY, argPacked)
            }
        }
    }

    fun ptrcallWithPackedVector2ListAndIntArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        values: List<Vector2>,
        intArg: Int,
    ) {
        Arena.ofConfined().use { arena ->
            val argPacked = BuiltinTypes.allocatePackedArray(arena)
            val intCell = arena.allocate(JAVA_INT)
            try {
                BuiltinTypes.initPackedVector2Array(argPacked, values)
                intCell.set(JAVA_INT, 0, intArg)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, argPacked)
                arr.setAtIndex(ADDRESS, 1, intCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR2_ARRAY, argPacked)
            }
        }
    }

    fun ptrcallWithPackedVector2ListListArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        values: List<List<Vector2>>,
    ) {
        Arena.ofConfined().use { arena ->
            val argArray = arena.allocate(8L, 8L)
            try {
                BuiltinTypes.initArrayOfPackedVector2Arrays(argArray, values)
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, argArray)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, argArray)
            }
        }
    }

    fun ptrcallWithTwoPackedVector2ListArgsRetPackedVector2ListList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: List<Vector2>,
        second: List<Vector2>,
    ): List<List<Vector2>> {
        Arena.ofConfined().use { arena ->
            val firstPacked = BuiltinTypes.allocatePackedArray(arena)
            val secondPacked = BuiltinTypes.allocatePackedArray(arena)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            try {
                BuiltinTypes.initPackedVector2Array(firstPacked, first)
                BuiltinTypes.initPackedVector2Array(secondPacked, second)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, firstPacked)
                arr.setAtIndex(ADDRESS, 1, secondPacked)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return BuiltinTypes.readArrayPackedVector2Arrays(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, ret)
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR2_ARRAY, secondPacked)
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR2_ARRAY, firstPacked)
            }
        }
    }

    fun ptrcallWithPackedVector2ListDoubleAndLongArgsRetPackedVector2ListList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        values: List<Vector2>,
        amount: Double,
        mode: Long,
    ): List<List<Vector2>> {
        Arena.ofConfined().use { arena ->
            val argPacked = BuiltinTypes.allocatePackedArray(arena)
            val amountCell = arena.allocate(JAVA_DOUBLE)
            val modeCell = arena.allocate(JAVA_LONG)
            val ret = arena.allocate(8L, 8L)
            try {
                BuiltinTypes.initPackedVector2Array(argPacked, values)
                amountCell.set(JAVA_DOUBLE, 0, amount)
                modeCell.set(JAVA_LONG, 0, mode)
                val arr = arena.allocate(ADDRESS, 3)
                arr.setAtIndex(ADDRESS, 0, argPacked)
                arr.setAtIndex(ADDRESS, 1, amountCell)
                arr.setAtIndex(ADDRESS, 2, modeCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return BuiltinTypes.readArrayPackedVector2Arrays(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, ret)
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR2_ARRAY, argPacked)
            }
        }
    }

    fun ptrcallWithPackedVector2ListDoubleAndTwoLongArgsRetPackedVector2ListList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        values: List<Vector2>,
        amount: Double,
        joinType: Long,
        endType: Long,
    ): List<List<Vector2>> {
        Arena.ofConfined().use { arena ->
            val argPacked = BuiltinTypes.allocatePackedArray(arena)
            val amountCell = arena.allocate(JAVA_DOUBLE)
            val joinTypeCell = arena.allocate(JAVA_LONG)
            val endTypeCell = arena.allocate(JAVA_LONG)
            val ret = arena.allocate(8L, 8L)
            try {
                BuiltinTypes.initPackedVector2Array(argPacked, values)
                amountCell.set(JAVA_DOUBLE, 0, amount)
                joinTypeCell.set(JAVA_LONG, 0, joinType)
                endTypeCell.set(JAVA_LONG, 0, endType)
                val arr = arena.allocate(ADDRESS, 4)
                arr.setAtIndex(ADDRESS, 0, argPacked)
                arr.setAtIndex(ADDRESS, 1, amountCell)
                arr.setAtIndex(ADDRESS, 2, joinTypeCell)
                arr.setAtIndex(ADDRESS, 3, endTypeCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return BuiltinTypes.readArrayPackedVector2Arrays(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, ret)
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR2_ARRAY, argPacked)
            }
        }
    }

    fun ptrcallWithPackedVector2ListColorDoubleAndBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        points: List<Vector2>,
        color: Color,
        width: Double,
        antialiased: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val pointsPacked = BuiltinTypes.allocatePackedArray(arena)
            val colorCell = arena.allocate(16L, 4L)
            val widthCell = arena.allocate(JAVA_DOUBLE)
            val antialiasedCell = arena.allocate(JAVA_BYTE)
            try {
                BuiltinTypes.initPackedVector2Array(pointsPacked, points)
                // Color component storage is fixed 32-bit, unlike scalar method float args.
                colorCell.set(JAVA_FLOAT, 0, color.r)
                colorCell.set(JAVA_FLOAT, 4, color.g)
                colorCell.set(JAVA_FLOAT, 8, color.b)
                colorCell.set(JAVA_FLOAT, 12, color.a)
                widthCell.set(JAVA_DOUBLE, 0, width)
                antialiasedCell.set(JAVA_BYTE, 0, if (antialiased) 1.toByte() else 0.toByte())
                val arr = arena.allocate(ADDRESS, 4)
                arr.setAtIndex(ADDRESS, 0, pointsPacked)
                arr.setAtIndex(ADDRESS, 1, colorCell)
                arr.setAtIndex(ADDRESS, 2, widthCell)
                arr.setAtIndex(ADDRESS, 3, antialiasedCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR2_ARRAY, pointsPacked)
            }
        }
    }

    fun ptrcallWithPackedVector2ListPackedColorListDoubleAndBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        points: List<Vector2>,
        colors: List<Color>,
        width: Double,
        antialiased: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val pointsPacked = BuiltinTypes.allocatePackedArray(arena)
            val colorsPacked = BuiltinTypes.allocatePackedArray(arena)
            val widthCell = arena.allocate(JAVA_DOUBLE)
            val antialiasedCell = arena.allocate(JAVA_BYTE)
            try {
                BuiltinTypes.initPackedVector2Array(pointsPacked, points)
                BuiltinTypes.initPackedColorArray(colorsPacked, colors)
                widthCell.set(JAVA_DOUBLE, 0, width)
                antialiasedCell.set(JAVA_BYTE, 0, if (antialiased) 1.toByte() else 0.toByte())
                val arr = arena.allocate(ADDRESS, 4)
                arr.setAtIndex(ADDRESS, 0, pointsPacked)
                arr.setAtIndex(ADDRESS, 1, colorsPacked)
                arr.setAtIndex(ADDRESS, 2, widthCell)
                arr.setAtIndex(ADDRESS, 3, antialiasedCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_COLOR_ARRAY, colorsPacked)
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR2_ARRAY, pointsPacked)
            }
        }
    }

    fun ptrcallWithRIDPackedVector2ListPackedColorListDoubleAndBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        points: List<Vector2>,
        colors: List<Color>,
        width: Double,
        antialiased: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val pointsPacked = BuiltinTypes.allocatePackedArray(arena)
            val colorsPacked = BuiltinTypes.allocatePackedArray(arena)
            val widthCell = arena.allocate(JAVA_DOUBLE)
            val antialiasedCell = arena.allocate(JAVA_BYTE)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                BuiltinTypes.initPackedVector2Array(pointsPacked, points)
                BuiltinTypes.initPackedColorArray(colorsPacked, colors)
                widthCell.set(JAVA_DOUBLE, 0, width)
                antialiasedCell.set(JAVA_BYTE, 0, if (antialiased) 1.toByte() else 0.toByte())
                val arr = arena.allocate(ADDRESS, 5)
                arr.setAtIndex(ADDRESS, 0, ridCell)
                arr.setAtIndex(ADDRESS, 1, pointsPacked)
                arr.setAtIndex(ADDRESS, 2, colorsPacked)
                arr.setAtIndex(ADDRESS, 3, widthCell)
                arr.setAtIndex(ADDRESS, 4, antialiasedCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_COLOR_ARRAY, colorsPacked)
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR2_ARRAY, pointsPacked)
            }
        }
    }

    fun ptrcallWithPackedVector2ListColorPackedVector2ListAndObjectArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        points: List<Vector2>,
        color: Color,
        uvs: List<Vector2>,
        texture: MemorySegment,
    ) {
        Arena.ofConfined().use { arena ->
            val pointsPacked = BuiltinTypes.allocatePackedArray(arena)
            val colorCell = arena.allocate(16L, 4L)
            val uvsPacked = BuiltinTypes.allocatePackedArray(arena)
            val textureCell = arena.allocate(ADDRESS)
            try {
                BuiltinTypes.initPackedVector2Array(pointsPacked, points)
                // Color component storage is fixed 32-bit, unlike scalar method float args.
                colorCell.set(JAVA_FLOAT, 0, color.r)
                colorCell.set(JAVA_FLOAT, 4, color.g)
                colorCell.set(JAVA_FLOAT, 8, color.b)
                colorCell.set(JAVA_FLOAT, 12, color.a)
                BuiltinTypes.initPackedVector2Array(uvsPacked, uvs)
                textureCell.set(ADDRESS, 0, texture)
                val arr = arena.allocate(ADDRESS, 4)
                arr.setAtIndex(ADDRESS, 0, pointsPacked)
                arr.setAtIndex(ADDRESS, 1, colorCell)
                arr.setAtIndex(ADDRESS, 2, uvsPacked)
                arr.setAtIndex(ADDRESS, 3, textureCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR2_ARRAY, uvsPacked)
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR2_ARRAY, pointsPacked)
            }
        }
    }

    fun ptrcallWithPackedVector2ListPackedColorListPackedVector2ListAndObjectArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        points: List<Vector2>,
        colors: List<Color>,
        uvs: List<Vector2>,
        texture: MemorySegment,
    ) {
        Arena.ofConfined().use { arena ->
            val pointsPacked = BuiltinTypes.allocatePackedArray(arena)
            val colorsPacked = BuiltinTypes.allocatePackedArray(arena)
            val uvsPacked = BuiltinTypes.allocatePackedArray(arena)
            val textureCell = arena.allocate(ADDRESS)
            try {
                BuiltinTypes.initPackedVector2Array(pointsPacked, points)
                BuiltinTypes.initPackedColorArray(colorsPacked, colors)
                BuiltinTypes.initPackedVector2Array(uvsPacked, uvs)
                textureCell.set(ADDRESS, 0, texture)
                val arr = arena.allocate(ADDRESS, 4)
                arr.setAtIndex(ADDRESS, 0, pointsPacked)
                arr.setAtIndex(ADDRESS, 1, colorsPacked)
                arr.setAtIndex(ADDRESS, 2, uvsPacked)
                arr.setAtIndex(ADDRESS, 3, textureCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR2_ARRAY, uvsPacked)
                BuiltinTypes.destroyTyped(VariantType.PACKED_COLOR_ARRAY, colorsPacked)
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR2_ARRAY, pointsPacked)
            }
        }
    }

    fun ptrcallWithRIDPackedVector2ListPackedColorListPackedVector2ListAndRIDArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        points: List<Vector2>,
        colors: List<Color>,
        uvs: List<Vector2>,
        textureRid: RID,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val pointsPacked = BuiltinTypes.allocatePackedArray(arena)
            val colorsPacked = BuiltinTypes.allocatePackedArray(arena)
            val uvsPacked = BuiltinTypes.allocatePackedArray(arena)
            val textureRidCell = arena.allocate(JAVA_LONG)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                BuiltinTypes.initPackedVector2Array(pointsPacked, points)
                BuiltinTypes.initPackedColorArray(colorsPacked, colors)
                BuiltinTypes.initPackedVector2Array(uvsPacked, uvs)
                textureRidCell.set(JAVA_LONG, 0, textureRid.value)
                val arr = arena.allocate(ADDRESS, 5)
                arr.setAtIndex(ADDRESS, 0, ridCell)
                arr.setAtIndex(ADDRESS, 1, pointsPacked)
                arr.setAtIndex(ADDRESS, 2, colorsPacked)
                arr.setAtIndex(ADDRESS, 3, uvsPacked)
                arr.setAtIndex(ADDRESS, 4, textureRidCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR2_ARRAY, uvsPacked)
                BuiltinTypes.destroyTyped(VariantType.PACKED_COLOR_ARRAY, colorsPacked)
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR2_ARRAY, pointsPacked)
            }
        }
    }

    /**
     * Calls [methodBind] with one PackedVector3Array arg and no return value.
     * Elements use GodotReal component storage, matching Vector3 ptrcall layout.
     */
    fun ptrcallWithPackedVector3ListArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        values: List<Vector3>,
    ) {
        Arena.ofConfined().use { arena ->
            val argPacked = BuiltinTypes.allocatePackedArray(arena)
            try {
                BuiltinTypes.initPackedVector3Array(argPacked, values)
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, argPacked)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR3_ARRAY, argPacked)
            }
        }
    }

    fun ptrcallWithPackedVector3ListArgRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        values: List<Vector3>,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val argPacked = BuiltinTypes.allocatePackedArray(arena)
            try {
                BuiltinTypes.initPackedVector3Array(argPacked, values)
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, argPacked)
                val ret = arena.allocate(JAVA_BYTE)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_BYTE, 0) != 0.toByte()
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR3_ARRAY, argPacked)
            }
        }
    }

    fun ptrcallWithPackedVector3ListAndDoubleArgRetPackedVector3List(
        methodBind: MemorySegment,
        instance: MemorySegment,
        values: List<Vector3>,
        tolerance: Double,
    ): List<Vector3> {
        Arena.ofConfined().use { arena ->
            val argPacked = BuiltinTypes.allocatePackedArray(arena)
            val doubleCell = arena.allocate(JAVA_DOUBLE)
            try {
                BuiltinTypes.initPackedVector3Array(argPacked, values)
                doubleCell.set(JAVA_DOUBLE, 0, tolerance)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, argPacked)
                arr.setAtIndex(ADDRESS, 1, doubleCell)
                val ret = BuiltinTypes.allocatePackedArray(arena)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return try {
                    BuiltinTypes.readPackedVector3Array(ret)
                } finally {
                    BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR3_ARRAY, ret)
                }
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR3_ARRAY, argPacked)
            }
        }
    }

    fun ptrcallWithPackedVector3ListAndPackedInt32ListArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        points: List<Vector3>,
        indices: List<Int>,
    ) {
        Arena.ofConfined().use { arena ->
            val pointsPacked = BuiltinTypes.allocatePackedArray(arena)
            val indicesPacked = BuiltinTypes.allocatePackedArray(arena)
            try {
                BuiltinTypes.initPackedVector3Array(pointsPacked, points)
                BuiltinTypes.initPackedInt32Array(indicesPacked, indices)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, pointsPacked)
                arr.setAtIndex(ADDRESS, 1, indicesPacked)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_INT32_ARRAY, indicesPacked)
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR3_ARRAY, pointsPacked)
            }
        }
    }

    fun ptrcallWithPackedVector3ListObjectBoolColorArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        points: List<Vector3>,
        objectArg: MemorySegment,
        enabled: Boolean,
        color: Color,
    ) {
        Arena.ofConfined().use { arena ->
            val pointsPacked = BuiltinTypes.allocatePackedArray(arena)
            val objectCell = arena.allocate(ADDRESS)
            val boolCell = arena.allocate(JAVA_BYTE)
            val colorCell = arena.allocate(16L, 4L)
            try {
                BuiltinTypes.initPackedVector3Array(pointsPacked, points)
                objectCell.set(ADDRESS, 0, objectArg)
                boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
                colorCell.set(JAVA_FLOAT, 0, color.r)
                colorCell.set(JAVA_FLOAT, 4, color.g)
                colorCell.set(JAVA_FLOAT, 8, color.b)
                colorCell.set(JAVA_FLOAT, 12, color.a)
                val args = arena.allocate(ADDRESS, 4)
                args.setAtIndex(ADDRESS, 0, pointsPacked)
                args.setAtIndex(ADDRESS, 1, objectCell)
                args.setAtIndex(ADDRESS, 2, boolCell)
                args.setAtIndex(ADDRESS, 3, colorCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR3_ARRAY, pointsPacked)
            }
        }
    }

    fun ptrcallWithPackedVector3ListObjectPackedInt32ListTwoBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        points: List<Vector3>,
        objectArg: MemorySegment,
        ids: List<Int>,
        first: Boolean,
        second: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val pointsPacked = BuiltinTypes.allocatePackedArray(arena)
            val objectCell = arena.allocate(ADDRESS)
            val idsPacked = BuiltinTypes.allocatePackedArray(arena)
            val bool0 = arena.allocate(JAVA_BYTE)
            val bool1 = arena.allocate(JAVA_BYTE)
            try {
                BuiltinTypes.initPackedVector3Array(pointsPacked, points)
                objectCell.set(ADDRESS, 0, objectArg)
                BuiltinTypes.initPackedInt32Array(idsPacked, ids)
                bool0.set(JAVA_BYTE, 0, if (first) 1.toByte() else 0.toByte())
                bool1.set(JAVA_BYTE, 0, if (second) 1.toByte() else 0.toByte())
                val args = arena.allocate(ADDRESS, 5)
                args.setAtIndex(ADDRESS, 0, pointsPacked)
                args.setAtIndex(ADDRESS, 1, objectCell)
                args.setAtIndex(ADDRESS, 2, idsPacked)
                args.setAtIndex(ADDRESS, 3, bool0)
                args.setAtIndex(ADDRESS, 4, bool1)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_INT32_ARRAY, idsPacked)
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR3_ARRAY, pointsPacked)
            }
        }
    }

    fun ptrcallWithPackedVector3ListAndTransform3DArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        values: List<Vector3>,
        transform: Transform3D,
    ) {
        Arena.ofConfined().use { arena ->
            val argPacked = BuiltinTypes.allocatePackedArray(arena)
            val transformCell = arena.allocate(GodotReal.SIZE_BYTES * 12, GodotReal.ALIGN_BYTES)
            try {
                BuiltinTypes.initPackedVector3Array(argPacked, values)
                writeTransform3D(transformCell, transform)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, argPacked)
                arr.setAtIndex(ADDRESS, 1, transformCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR3_ARRAY, argPacked)
            }
        }
    }

    fun ptrcallWithPackedVector3ListTwoDoubleAndBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        values: List<Vector3>,
        first: Double,
        second: Double,
        enabled: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val argPacked = BuiltinTypes.allocatePackedArray(arena)
            val arg1 = arena.allocate(JAVA_DOUBLE)
            val arg2 = arena.allocate(JAVA_DOUBLE)
            val arg3 = arena.allocate(JAVA_BYTE)
            try {
                BuiltinTypes.initPackedVector3Array(argPacked, values)
                arg1.set(JAVA_DOUBLE, 0, first)
                arg2.set(JAVA_DOUBLE, 0, second)
                arg3.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
                val arr = arena.allocate(ADDRESS, 4)
                arr.setAtIndex(ADDRESS, 0, argPacked)
                arr.setAtIndex(ADDRESS, 1, arg1)
                arr.setAtIndex(ADDRESS, 2, arg2)
                arr.setAtIndex(ADDRESS, 3, arg3)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR3_ARRAY, argPacked)
            }
        }
    }

    /**
     * Calls [methodBind] with one PackedColorArray arg and no return value.
     * Elements use fixed 32-bit Color component storage.
     */
    fun ptrcallWithPackedColorListArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        values: List<Color>,
    ) {
        Arena.ofConfined().use { arena ->
            val argPacked = BuiltinTypes.allocatePackedArray(arena)
            try {
                BuiltinTypes.initPackedColorArray(argPacked, values)
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, argPacked)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_COLOR_ARRAY, argPacked)
            }
        }
    }

    /**
     * Calls [methodBind] with one PackedVector4Array arg and no return value.
     * Elements use GodotReal component storage, matching Vector4 ptrcall layout.
     */
    fun ptrcallWithPackedVector4ListArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        values: List<Vector4>,
    ) {
        Arena.ofConfined().use { arena ->
            val argPacked = BuiltinTypes.allocatePackedArray(arena)
            try {
                BuiltinTypes.initPackedVector4Array(argPacked, values)
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, argPacked)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR4_ARRAY, argPacked)
            }
        }
    }

    /**
     * Calls [methodBind] with (String, String, PackedByteArray) args and int64 return value.
     */
    fun ptrcallWithTwoStringAndByteArrayArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: String,
        second: String,
        third: ByteArray,
    ): Long {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(8L, 8L)
            val arg1 = arena.allocate(8L, 8L)
            val arg2 = BuiltinTypes.allocatePackedArray(arena)
            try {
                GodotStrings.initString(arg0, first)
                GodotStrings.initString(arg1, second)
                BuiltinTypes.initPackedByteArray(arg2, third)
                val arr = arena.allocate(ADDRESS, 3)
                arr.setAtIndex(ADDRESS, 0, arg0)
                arr.setAtIndex(ADDRESS, 1, arg1)
                arr.setAtIndex(ADDRESS, 2, arg2)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, arg2)
                GodotStrings.destroyString(arg0)
                GodotStrings.destroyString(arg1)
            }
        }
    }

    fun ptrcallWithStringAndByteArrayArgRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        bytes: ByteArray,
    ): Long {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val byteArrayCell = BuiltinTypes.allocatePackedArray(arena)
            try {
                GodotStrings.initString(stringCell, text)
                BuiltinTypes.initPackedByteArray(byteArrayCell, bytes)
                val args = arena.allocate(ADDRESS, 2)
                args.setAtIndex(ADDRESS, 0, stringCell)
                args.setAtIndex(ADDRESS, 1, byteArrayCell)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, byteArrayCell)
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    /**
     * Calls [methodBind] with one int64 arg and no return value.
     */
    fun ptrcallWithLongArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Long,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_LONG)
            arg0.set(JAVA_LONG, 0, value)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, arg0)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with one uint32 arg and no return value.
     */
    fun ptrcallWithUInt32Arg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Long,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            arg0.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(value))
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, arg0)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (int64, bool) arguments.
     */
    fun ptrcallWithLongAndBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        longArg: Long,
        boolArg: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_LONG)
            val arg1 = arena.allocate(JAVA_BYTE)
            arg0.set(JAVA_LONG, 0, longArg)
            arg1.set(JAVA_BYTE, 0, if (boolArg) 1.toByte() else 0.toByte())
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithLongBoolIntArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        longArg: Long,
        boolArg: Boolean,
        intArg: Int,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_LONG)
            val arg1 = arena.allocate(JAVA_BYTE)
            val arg2 = arena.allocate(JAVA_INT)
            arg0.set(JAVA_LONG, 0, longArg)
            arg1.set(JAVA_BYTE, 0, if (boolArg) 1.toByte() else 0.toByte())
            arg2.set(JAVA_INT, 0, intArg)
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (int64/enum, Object*) args and no return value.
     */
    fun ptrcallWithLongAndObjectArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        longArg: Long,
        objectArg: MemorySegment,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_LONG)
            arg0.set(JAVA_LONG, 0, longArg)
            val objCell = arena.allocate(ADDRESS)
            objCell.set(ADDRESS, 0, objectArg)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, objCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithLongAndRIDArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        longArg: Long,
        rid: RID,
    ) {
        Arena.ofConfined().use { arena ->
            val longCell = arena.allocate(JAVA_LONG)
            val ridCell = arena.allocate(JAVA_LONG)
            longCell.set(JAVA_LONG, 0, longArg)
            ridCell.set(JAVA_LONG, 0, rid.value)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, longCell)
            arr.setAtIndex(ADDRESS, 1, ridCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithLongAndUInt32ArgRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        longArg: Long,
        uintArg: Long,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val longCell = arena.allocate(JAVA_LONG)
            val uintCell = arena.allocate(JAVA_INT)
            longCell.set(JAVA_LONG, 0, longArg)
            uintCell.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(uintArg))
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, longCell)
            args.setAtIndex(ADDRESS, 1, uintCell)
            val ret = arena.allocate(JAVA_BYTE)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(JAVA_BYTE, 0) != 0.toByte()
        }
    }

    fun ptrcallWithLongAndUInt32ArgRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        longArg: Long,
        uintArg: Long,
    ): Long {
        Arena.ofConfined().use { arena ->
            val longCell = arena.allocate(JAVA_LONG)
            val uintCell = arena.allocate(JAVA_INT)
            longCell.set(JAVA_LONG, 0, longArg)
            uintCell.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(uintArg))
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, longCell)
            args.setAtIndex(ADDRESS, 1, uintCell)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(JAVA_LONG, 0)
        }
    }

    fun ptrcallWithThreeLongFourUInt32ArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstLong: Long,
        secondLong: Long,
        thirdLong: Long,
        firstUInt: Long,
        secondUInt: Long,
        thirdUInt: Long,
        fourthUInt: Long,
    ): Long {
        Arena.ofConfined().use { arena ->
            val long0 = arena.allocate(JAVA_LONG)
            val long1 = arena.allocate(JAVA_LONG)
            val long2 = arena.allocate(JAVA_LONG)
            val uint0 = arena.allocate(JAVA_INT)
            val uint1 = arena.allocate(JAVA_INT)
            val uint2 = arena.allocate(JAVA_INT)
            val uint3 = arena.allocate(JAVA_INT)
            long0.set(JAVA_LONG, 0, firstLong)
            long1.set(JAVA_LONG, 0, secondLong)
            long2.set(JAVA_LONG, 0, thirdLong)
            uint0.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(firstUInt))
            uint1.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(secondUInt))
            uint2.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(thirdUInt))
            uint3.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(fourthUInt))
            val args = arena.allocate(ADDRESS, 7)
            args.setAtIndex(ADDRESS, 0, long0)
            args.setAtIndex(ADDRESS, 1, long1)
            args.setAtIndex(ADDRESS, 2, long2)
            args.setAtIndex(ADDRESS, 3, uint0)
            args.setAtIndex(ADDRESS, 4, uint1)
            args.setAtIndex(ADDRESS, 5, uint2)
            args.setAtIndex(ADDRESS, 6, uint3)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(JAVA_LONG, 0)
        }
    }

    fun ptrcallWithThreeLongFourIntLongArgsRetRID(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstLong: Long,
        secondLong: Long,
        thirdLong: Long,
        firstInt: Int,
        secondInt: Int,
        thirdInt: Int,
        fourthInt: Int,
        fourthLong: Long,
    ): RID {
        Arena.ofConfined().use { arena ->
            val long0 = arena.allocate(JAVA_LONG)
            val long1 = arena.allocate(JAVA_LONG)
            val long2 = arena.allocate(JAVA_LONG)
            val int0 = arena.allocate(JAVA_INT)
            val int1 = arena.allocate(JAVA_INT)
            val int2 = arena.allocate(JAVA_INT)
            val int3 = arena.allocate(JAVA_INT)
            val long3 = arena.allocate(JAVA_LONG)
            long0.set(JAVA_LONG, 0, firstLong)
            long1.set(JAVA_LONG, 0, secondLong)
            long2.set(JAVA_LONG, 0, thirdLong)
            int0.set(JAVA_INT, 0, firstInt)
            int1.set(JAVA_INT, 0, secondInt)
            int2.set(JAVA_INT, 0, thirdInt)
            int3.set(JAVA_INT, 0, fourthInt)
            long3.set(JAVA_LONG, 0, fourthLong)
            val args = arena.allocate(ADDRESS, 8)
            args.setAtIndex(ADDRESS, 0, long0)
            args.setAtIndex(ADDRESS, 1, long1)
            args.setAtIndex(ADDRESS, 2, long2)
            args.setAtIndex(ADDRESS, 3, int0)
            args.setAtIndex(ADDRESS, 4, int1)
            args.setAtIndex(ADDRESS, 5, int2)
            args.setAtIndex(ADDRESS, 6, int3)
            args.setAtIndex(ADDRESS, 7, long3)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return RID(ret.get(JAVA_LONG, 0))
        }
    }

    fun ptrcallWithFourLongLongLongLongLongLongArgsRetRID(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstLong: Long,
        secondLong: Long,
        thirdLong: Long,
        fourthLong: Long,
        fifthLong: Long,
        sixthLong: Long,
        seventhLong: Long,
        eighthLong: Long,
        ninthLong: Long,
        tenthLong: Long,
    ): RID {
        Arena.ofConfined().use { arena ->
            val long0 = arena.allocate(JAVA_LONG)
            val long1 = arena.allocate(JAVA_LONG)
            val long2 = arena.allocate(JAVA_LONG)
            val long3 = arena.allocate(JAVA_LONG)
            val long4 = arena.allocate(JAVA_LONG)
            val long5 = arena.allocate(JAVA_LONG)
            val long6 = arena.allocate(JAVA_LONG)
            val long7 = arena.allocate(JAVA_LONG)
            val long8 = arena.allocate(JAVA_LONG)
            val long9 = arena.allocate(JAVA_LONG)
            long0.set(JAVA_LONG, 0, firstLong)
            long1.set(JAVA_LONG, 0, secondLong)
            long2.set(JAVA_LONG, 0, thirdLong)
            long3.set(JAVA_LONG, 0, fourthLong)
            long4.set(JAVA_LONG, 0, fifthLong)
            long5.set(JAVA_LONG, 0, sixthLong)
            long6.set(JAVA_LONG, 0, seventhLong)
            long7.set(JAVA_LONG, 0, eighthLong)
            long8.set(JAVA_LONG, 0, ninthLong)
            long9.set(JAVA_LONG, 0, tenthLong)
            val args = arena.allocate(ADDRESS, 10)
            args.setAtIndex(ADDRESS, 0, long0)
            args.setAtIndex(ADDRESS, 1, long1)
            args.setAtIndex(ADDRESS, 2, long2)
            args.setAtIndex(ADDRESS, 3, long3)
            args.setAtIndex(ADDRESS, 4, long4)
            args.setAtIndex(ADDRESS, 5, long5)
            args.setAtIndex(ADDRESS, 6, long6)
            args.setAtIndex(ADDRESS, 7, long7)
            args.setAtIndex(ADDRESS, 8, long8)
            args.setAtIndex(ADDRESS, 9, long9)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return RID(ret.get(JAVA_LONG, 0))
        }
    }

    fun ptrcallWithLongRIDAndLongArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Long,
        rid: RID,
        second: Long,
    ) {
        Arena.ofConfined().use { arena ->
            val long0 = arena.allocate(JAVA_LONG)
            val ridCell = arena.allocate(JAVA_LONG)
            val long1 = arena.allocate(JAVA_LONG)
            long0.set(JAVA_LONG, 0, first)
            ridCell.set(JAVA_LONG, 0, rid.value)
            long1.set(JAVA_LONG, 0, second)
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, long0)
            arr.setAtIndex(ADDRESS, 1, ridCell)
            arr.setAtIndex(ADDRESS, 2, long1)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithLongRIDLongArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Long,
        rid: RID,
        second: Long,
    ): Long {
        Arena.ofConfined().use { arena ->
            val long0 = arena.allocate(JAVA_LONG)
            val ridCell = arena.allocate(JAVA_LONG)
            val long1 = arena.allocate(JAVA_LONG)
            long0.set(JAVA_LONG, 0, first)
            ridCell.set(JAVA_LONG, 0, rid.value)
            long1.set(JAVA_LONG, 0, second)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, long0)
            args.setAtIndex(ADDRESS, 1, ridCell)
            args.setAtIndex(ADDRESS, 2, long1)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(JAVA_LONG, 0)
        }
    }

    fun ptrcallWithLongRIDAndUInt32Args(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Long,
        rid: RID,
        second: Long,
    ) {
        Arena.ofConfined().use { arena ->
            val longCell = arena.allocate(JAVA_LONG)
            val ridCell = arena.allocate(JAVA_LONG)
            val uintCell = arena.allocate(JAVA_INT)
            longCell.set(JAVA_LONG, 0, first)
            ridCell.set(JAVA_LONG, 0, rid.value)
            uintCell.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(second))
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, longCell)
            arr.setAtIndex(ADDRESS, 1, ridCell)
            arr.setAtIndex(ADDRESS, 2, uintCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with one int32 arg and no return value.
     */
    fun ptrcallWithIntArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Int,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            arg0.set(JAVA_INT, 0, value)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, arg0)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with one int32 arg and bool return value.
     */
    fun ptrcallWithIntArgRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Int,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val arg = arena.allocate(JAVA_INT)
            arg.set(JAVA_INT, 0, value)
            val ret = arena.allocate(JAVA_BYTE)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, arg)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_BYTE, 0) != 0.toByte()
        }
    }

    /**
     * Calls [methodBind] with one int32 arg and float(double) return value.
     */
    fun ptrcallWithIntArgRetDouble(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Int,
    ): Double {
        Arena.ofConfined().use { arena ->
            val arg = arena.allocate(JAVA_INT)
            arg.set(JAVA_INT, 0, value)
            val ret = arena.allocate(JAVA_DOUBLE)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, arg)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_DOUBLE, 0)
        }
    }

    /**
     * Calls [methodBind] with one int32 arg and int32 return value.
     */
    fun ptrcallWithIntArgRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Int,
    ): Int {
        Arena.ofConfined().use { arena ->
            val arg = arena.allocate(JAVA_INT)
            arg.set(JAVA_INT, 0, value)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, arg)
            val ret = arena.allocate(JAVA_INT)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_INT, 0)
        }
    }

    /**
     * Calls [methodBind] with one int32 arg and int64 return value.
     */
    fun ptrcallWithIntArgRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Int,
    ): Long {
        Arena.ofConfined().use { arena ->
            val arg = arena.allocate(JAVA_INT)
            arg.set(JAVA_INT, 0, value)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, arg)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_LONG, 0)
        }
    }

    /**
     * Calls [methodBind] with one int32 arg and uint32 return value.
     */
    fun ptrcallWithIntArgRetUInt32(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Int,
    ): Long {
        Arena.ofConfined().use { arena ->
            val arg = arena.allocate(JAVA_INT)
            arg.set(JAVA_INT, 0, value)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, arg)
            val ret = arena.allocate(JAVA_INT)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_INT, 0).toLong() and 0xffff_ffffL
        }
    }

    /**
     * Calls [methodBind] with one int32 arg and RID return value.
     */
    fun ptrcallWithIntArgRetRID(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Int,
    ): RID {
        Arena.ofConfined().use { arena ->
            val arg = arena.allocate(JAVA_INT)
            arg.set(JAVA_INT, 0, value)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, arg)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return RID(ret.get(JAVA_LONG, 0))
        }
    }

    /**
     * Calls [methodBind] with one int32 arg and String return value.
     */
    fun ptrcallWithIntArgRetString(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Int,
    ): String {
        Arena.ofConfined().use { arena ->
            val arg = arena.allocate(JAVA_INT)
            arg.set(JAVA_INT, 0, value)
            val retString = arena.allocate(8L, 8L)
            try {
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, arg)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, retString)
                return GodotStrings.readString(retString)
            } finally {
                GodotStrings.destroyString(retString)
            }
        }
    }

    /**
     * Calls [methodBind] with one int32 arg and Vector2 return value.
     */
    fun ptrcallWithIntArgRetVector2(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Int,
    ): Vector2 {
        Arena.ofConfined().use { arena ->
            val arg = arena.allocate(JAVA_INT)
            arg.set(JAVA_INT, 0, value)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, arg)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return Vector2(
                x = GodotReal.readIndex(ret, 0),
                y = GodotReal.readIndex(ret, 1),
            )
        }
    }

    /**
     * Calls [methodBind] with one int32 arg and Vector2i return value.
     */
    fun ptrcallWithIntArgRetVector2i(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Int,
    ): Vector2i {
        Arena.ofConfined().use { arena ->
            val arg = arena.allocate(JAVA_INT)
            arg.set(JAVA_INT, 0, value)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, arg)
            val ret = arena.allocate(8L, 4L)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return Vector2i(
                x = ret.get(JAVA_INT, 0),
                y = ret.get(JAVA_INT, 4),
            )
        }
    }

    /**
     * Calls [methodBind] with one int32 arg and StringName return value.
     */
    fun ptrcallWithIntArgRetStringName(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Int,
    ): String {
        Arena.ofConfined().use { arena ->
            val arg = arena.allocate(JAVA_INT)
            arg.set(JAVA_INT, 0, value)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, arg)
            val ret = arena.allocate(8L, 8L)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return try {
                GodotStrings.readStringName(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.STRING_NAME, ret)
            }
        }
    }

    /**
     * Calls [methodBind] with one int32 arg and NodePath return value.
     */
    fun ptrcallWithIntArgRetNodePath(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Int,
    ): NodePath {
        Arena.ofConfined().use { arena ->
            val arg = arena.allocate(JAVA_INT)
            arg.set(JAVA_INT, 0, value)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, arg)
            val ret = arena.allocate(8L, 8L)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return try {
                BuiltinTypes.readTypedNodePath(ret, arena)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.NODE_PATH, ret)
            }
        }
    }

    fun ptrcallWithIntArgRetByteArray(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Int,
    ): ByteArray {
        Arena.ofConfined().use { arena ->
            val arg = arena.allocate(JAVA_INT)
            arg.set(JAVA_INT, 0, value)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, arg)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return try {
                BuiltinTypes.readPackedByteArray(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, ret)
            }
        }
    }

    /**
     * Calls [methodBind] with one int64 arg and bool return value.
     */
    fun ptrcallWithLongArgRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Long,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val arg = arena.allocate(JAVA_LONG)
            arg.set(JAVA_LONG, 0, value)
            val ret = arena.allocate(java.lang.foreign.ValueLayout.JAVA_BYTE)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, arg)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(java.lang.foreign.ValueLayout.JAVA_BYTE, 0) != 0.toByte()
        }
    }

    /**
     * Calls [methodBind] with one uint32 arg and bool return value.
     */
    fun ptrcallWithUInt32ArgRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Long,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val arg = arena.allocate(JAVA_INT)
            arg.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(value))
            val ret = arena.allocate(JAVA_BYTE)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, arg)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_BYTE, 0) != 0.toByte()
        }
    }

    /**
     * Calls [methodBind] with one uint32 arg and int64 return value.
     */
    fun ptrcallWithUInt32ArgRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Long,
    ): Long {
        Arena.ofConfined().use { arena ->
            val arg = arena.allocate(JAVA_INT)
            arg.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(value))
            val ret = arena.allocate(JAVA_LONG)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, arg)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_LONG, 0)
        }
    }

    /**
     * Calls [methodBind] with one uint32 arg and int32 return value.
     */
    fun ptrcallWithUInt32ArgRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Long,
    ): Int {
        Arena.ofConfined().use { arena ->
            val arg = arena.allocate(JAVA_INT)
            arg.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(value))
            val ret = arena.allocate(JAVA_INT)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, arg)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_INT, 0)
        }
    }

    /**
     * Calls [methodBind] with one uint32 arg and Object* return value.
     */
    fun ptrcallWithUInt32ArgRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Long,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val arg = arena.allocate(JAVA_INT)
            arg.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(value))
            val ret = arena.allocate(ADDRESS)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, arg)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(ADDRESS, 0)
        }
    }

    /**
     * Calls [methodBind] with one uint32 arg and String return value.
     */
    fun ptrcallWithUInt32ArgRetString(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Long,
    ): String {
        Arena.ofConfined().use { arena ->
            val arg = arena.allocate(JAVA_INT)
            arg.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(value))
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, arg)
            val retString = arena.allocate(8L, 8L)
            try {
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, retString)
                return GodotStrings.readString(retString)
            } finally {
                GodotStrings.destroyString(retString)
            }
        }
    }

    /**
     * Calls [methodBind] with one uint32 arg and float(double) return value.
     */
    fun ptrcallWithUInt32ArgRetDouble(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Long,
    ): Double {
        Arena.ofConfined().use { arena ->
            val arg = arena.allocate(JAVA_INT)
            arg.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(value))
            val ret = arena.allocate(JAVA_DOUBLE)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, arg)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_DOUBLE, 0)
        }
    }

    /**
     * Calls [methodBind] with one uint32 arg and Vector3 return value.
     */
    fun ptrcallWithUInt32ArgRetVector3(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Long,
    ): Vector3 {
        Arena.ofConfined().use { arena ->
            val arg = arena.allocate(JAVA_INT)
            arg.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(value))
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, arg)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return Vector3(
                x = GodotReal.readIndex(ret, 0),
                y = GodotReal.readIndex(ret, 1),
                z = GodotReal.readIndex(ret, 2),
            )
        }
    }

    /**
     * Calls [methodBind] with one uint32 arg and Transform2D return value.
     */
    fun ptrcallWithUInt32ArgRetTransform2D(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Long,
    ): Transform2D {
        Arena.ofConfined().use { arena ->
            val arg = arena.allocate(JAVA_INT)
            arg.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(value))
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, arg)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 6, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return readTransform2D(ret)
        }
    }

    /**
     * Calls [methodBind] with one uint32 arg and Transform3D return value.
     */
    fun ptrcallWithUInt32ArgRetTransform3D(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Long,
    ): Transform3D {
        Arena.ofConfined().use { arena ->
            val arg = arena.allocate(JAVA_INT)
            arg.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(value))
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, arg)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 12, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return readTransform3D(ret)
        }
    }

    /**
     * Calls [methodBind] with one uint32 arg and Projection return value.
     */
    fun ptrcallWithUInt32ArgRetProjection(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Long,
    ): Projection {
        Arena.ofConfined().use { arena ->
            val arg = arena.allocate(JAVA_INT)
            arg.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(value))
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, arg)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 16, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return readProjection(ret)
        }
    }

    fun ptrcallWithUInt32ArgRetPackedInt64List(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Long,
    ): List<Long> {
        Arena.ofConfined().use { arena ->
            val arg = arena.allocate(JAVA_INT)
            arg.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(value))
            val args = arena.allocate(ADDRESS, 1)
            args.setAtIndex(ADDRESS, 0, arg)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return try {
                BuiltinTypes.readPackedInt64Array(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_INT64_ARRAY, ret)
            }
        }
    }

    fun ptrcallWithUInt32Transform3DArgsRetTransform3D(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Long,
        transform: Transform3D,
    ): Transform3D {
        Arena.ofConfined().use { arena ->
            val uintCell = arena.allocate(JAVA_INT)
            val transformCell = arena.allocate(GodotReal.SIZE_BYTES * 12, GodotReal.ALIGN_BYTES)
            uintCell.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(value))
            writeTransform3D(transformCell, transform)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, uintCell)
            args.setAtIndex(ADDRESS, 1, transformCell)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 12, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return readTransform3D(ret)
        }
    }

    fun ptrcallWithUInt32ThreeDoubleArgsRetProjection(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Long,
        first: Double,
        second: Double,
        third: Double,
    ): Projection {
        Arena.ofConfined().use { arena ->
            val uintCell = arena.allocate(JAVA_INT)
            val double0 = arena.allocate(JAVA_DOUBLE)
            val double1 = arena.allocate(JAVA_DOUBLE)
            val double2 = arena.allocate(JAVA_DOUBLE)
            uintCell.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(value))
            double0.set(JAVA_DOUBLE, 0, first)
            double1.set(JAVA_DOUBLE, 0, second)
            double2.set(JAVA_DOUBLE, 0, third)
            val args = arena.allocate(ADDRESS, 4)
            args.setAtIndex(ADDRESS, 0, uintCell)
            args.setAtIndex(ADDRESS, 1, double0)
            args.setAtIndex(ADDRESS, 2, double1)
            args.setAtIndex(ADDRESS, 3, double2)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 16, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return readProjection(ret)
        }
    }

    /**
     * Calls [methodBind] with one int64 arg and float(double) return value.
     */
    fun ptrcallWithLongArgRetDouble(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Long,
    ): Double {
        Arena.ofConfined().use { arena ->
            val arg = arena.allocate(JAVA_LONG)
            arg.set(JAVA_LONG, 0, value)
            val ret = arena.allocate(java.lang.foreign.ValueLayout.JAVA_DOUBLE)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, arg)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(java.lang.foreign.ValueLayout.JAVA_DOUBLE, 0)
        }
    }

    /**
     * Calls [methodBind] with one int64 arg and int64 return value.
     */
    fun ptrcallWithLongArgRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Long,
    ): Long {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_LONG)
            arg0.set(JAVA_LONG, 0, value)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, arg0)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_LONG, 0)
        }
    }

    /**
     * Calls [methodBind] with one int64 arg and int32 return value.
     */
    fun ptrcallWithLongArgRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Long,
    ): Int {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_LONG)
            arg0.set(JAVA_LONG, 0, value)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, arg0)
            val ret = arena.allocate(JAVA_INT)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_INT, 0)
        }
    }

    /**
     * Calls [methodBind] with one int64 arg and uint32 return value.
     */
    fun ptrcallWithLongArgRetUInt32(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Long,
    ): Long {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_LONG)
            arg0.set(JAVA_LONG, 0, value)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, arg0)
            val ret = arena.allocate(JAVA_INT)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_INT, 0).toLong() and 0xffff_ffffL
        }
    }

    fun ptrcallWithLongArgRetRID(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Long,
    ): RID {
        Arena.ofConfined().use { arena ->
            val arg = arena.allocate(JAVA_LONG)
            arg.set(JAVA_LONG, 0, value)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, arg)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return RID(ret.get(JAVA_LONG, 0))
        }
    }

    /**
     * Calls [methodBind] with one int64 arg and Vector2 return value.
     */
    fun ptrcallWithLongArgRetVector2(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Long,
    ): Vector2 {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_LONG)
            arg0.set(JAVA_LONG, 0, value)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, arg0)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return Vector2(
                x = GodotReal.readIndex(ret, 0),
                y = GodotReal.readIndex(ret, 1),
            )
        }
    }

    /**
     * Calls [methodBind] with one int64 arg and Vector3 return value.
     */
    fun ptrcallWithLongArgRetVector3(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Long,
    ): Vector3 {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_LONG)
            arg0.set(JAVA_LONG, 0, value)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, arg0)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return Vector3(
                x = GodotReal.readIndex(ret, 0),
                y = GodotReal.readIndex(ret, 1),
                z = GodotReal.readIndex(ret, 2),
            )
        }
    }

    /**
     * Calls [methodBind] with one int64 arg and Vector2i return value.
     */
    fun ptrcallWithLongArgRetVector2i(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Long,
    ): Vector2i {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_LONG)
            arg0.set(JAVA_LONG, 0, value)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, arg0)
            val ret = arena.allocate(8L, 4L)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return Vector2i(
                x = ret.get(JAVA_INT, 0),
                y = ret.get(JAVA_INT, 4),
            )
        }
    }

    fun ptrcallWithLongArgRetPackedInt64List(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Long,
    ): List<Long> {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_LONG)
            arg0.set(JAVA_LONG, 0, value)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, arg0)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return try {
                BuiltinTypes.readPackedInt64Array(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_INT64_ARRAY, ret)
            }
        }
    }

    fun ptrcallWithLongArgRetPackedInt32List(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Long,
    ): List<Int> {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_LONG)
            arg0.set(JAVA_LONG, 0, value)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, arg0)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return try {
                BuiltinTypes.readPackedInt32Array(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_INT32_ARRAY, ret)
            }
        }
    }

    fun ptrcallWithLongArgRetTransform3D(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Long,
    ): Transform3D {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_LONG)
            arg0.set(JAVA_LONG, 0, value)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, arg0)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 12, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return readTransform3D(ret)
        }
    }

    fun ptrcallWithLongArgRetProjection(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Long,
    ): Projection {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_LONG)
            arg0.set(JAVA_LONG, 0, value)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, arg0)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 16, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return readProjection(ret)
        }
    }

    /**
     * Calls [methodBind] with one int64 arg and StringName return value.
     */
    fun ptrcallWithLongArgRetStringName(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Long,
    ): String {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_LONG)
            arg0.set(JAVA_LONG, 0, value)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, arg0)
            val ret = arena.allocate(8L, 8L)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return try {
                GodotStrings.readStringName(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.STRING_NAME, ret)
            }
        }
    }

    /**
     * Calls [methodBind] with no arguments and Vector2i return value.
     */
    fun ptrcallNoArgsRetVector2i(
        methodBind: MemorySegment,
        instance: MemorySegment,
    ): Vector2i {
        val ret = ptrcallScratch.get().vector2iRet
        objectMethodBindPtrcall.invoke(methodBind, instance, MemorySegment.NULL, ret)
        return Vector2i(
            x = ret.get(JAVA_INT, 0),
            y = ret.get(JAVA_INT, 4),
        )
    }

    fun ptrcallNoArgsRetVector3i(
        methodBind: MemorySegment,
        instance: MemorySegment,
    ): Vector3i {
        val ret = ptrcallScratch.get().vector3iRet
        objectMethodBindPtrcall.invoke(methodBind, instance, MemorySegment.NULL, ret)
        return readVector3i(ret)
    }

    fun ptrcallNoArgsRetRect2i(
        methodBind: MemorySegment,
        instance: MemorySegment,
    ): Rect2i {
        Arena.ofConfined().use { arena ->
            val ret = arena.allocate(16L, 4L)
            objectMethodBindPtrcall.invoke(methodBind, instance, MemorySegment.NULL, ret)
            return Rect2i(
                position = Vector2i(
                    x = ret.get(JAVA_INT, 0),
                    y = ret.get(JAVA_INT, 4),
                ),
                size = Vector2i(
                    x = ret.get(JAVA_INT, 8),
                    y = ret.get(JAVA_INT, 12),
                ),
            )
        }
    }

    fun ptrcallWithVector2iArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Vector2i,
    ) {
        Arena.ofConfined().use { arena ->
            val arg = arena.allocate(8L, 4L)
            arg.set(JAVA_INT, 0, value.x)
            arg.set(JAVA_INT, 4, value.y)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, arg)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRect2iArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Rect2i,
    ) {
        Arena.ofConfined().use { arena ->
            val rect = arena.allocate(16L, 4L)
            rect.set(JAVA_INT, 0, value.position.x)
            rect.set(JAVA_INT, 4, value.position.y)
            rect.set(JAVA_INT, 8, value.size.x)
            rect.set(JAVA_INT, 12, value.size.y)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, rect)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRect2iArgRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Rect2i,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val rect = arena.allocate(16L, 4L)
            rect.set(JAVA_INT, 0, value.position.x)
            rect.set(JAVA_INT, 4, value.position.y)
            rect.set(JAVA_INT, 8, value.size.x)
            rect.set(JAVA_INT, 12, value.size.y)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, rect)
            val ret = arena.allocate(ADDRESS)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(ADDRESS, 0)
        }
    }

    fun ptrcallWithRect2iArgRetDictionaryList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Rect2i,
    ): List<Map<String, Any?>> {
        Arena.ofConfined().use { arena ->
            val rect = arena.allocate(16L, 4L)
            rect.set(JAVA_INT, 0, value.position.x)
            rect.set(JAVA_INT, 4, value.position.y)
            rect.set(JAVA_INT, 8, value.size.x)
            rect.set(JAVA_INT, 12, value.size.y)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, rect)
            return callArrayReturn(methodBind, instance, arr, BuiltinTypes::readArrayDictionaries)
        }
    }

    fun ptrcallWithRect2iAndColorArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rectValue: Rect2i,
        color: Color,
    ) {
        Arena.ofConfined().use { arena ->
            val rect = arena.allocate(16L, 4L)
            rect.set(JAVA_INT, 0, rectValue.position.x)
            rect.set(JAVA_INT, 4, rectValue.position.y)
            rect.set(JAVA_INT, 8, rectValue.size.x)
            rect.set(JAVA_INT, 12, rectValue.size.y)
            val colorCell = arena.allocate(16L, 4L)
            // Color components use fixed 32-bit storage, unlike scalar float method args.
            colorCell.set(JAVA_FLOAT, 0, color.r)
            colorCell.set(JAVA_FLOAT, 4, color.g)
            colorCell.set(JAVA_FLOAT, 8, color.b)
            colorCell.set(JAVA_FLOAT, 12, color.a)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, rect)
            arr.setAtIndex(ADDRESS, 1, colorCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRect2iAndBoolArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Rect2i,
        flag: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val rect = arena.allocate(16L, 4L)
            rect.set(JAVA_INT, 0, value.position.x)
            rect.set(JAVA_INT, 4, value.position.y)
            rect.set(JAVA_INT, 8, value.size.x)
            rect.set(JAVA_INT, 12, value.size.y)
            val boolCell = arena.allocate(JAVA_BYTE)
            boolCell.set(JAVA_BYTE, 0, if (flag) 1.toByte() else 0.toByte())
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, rect)
            arr.setAtIndex(ADDRESS, 1, boolCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRect2iAndDoubleArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Rect2i,
        amount: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val rect = arena.allocate(16L, 4L)
            rect.set(JAVA_INT, 0, value.position.x)
            rect.set(JAVA_INT, 4, value.position.y)
            rect.set(JAVA_INT, 8, value.size.x)
            rect.set(JAVA_INT, 12, value.size.y)
            val amountCell = arena.allocate(JAVA_DOUBLE)
            amountCell.set(JAVA_DOUBLE, 0, amount)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, rect)
            arr.setAtIndex(ADDRESS, 1, amountCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRect2iAndDoubleArgsRetPackedVector2ListList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Rect2i,
        amount: Double,
    ): List<List<Vector2>> {
        Arena.ofConfined().use { arena ->
            val rect = arena.allocate(16L, 4L)
            rect.set(JAVA_INT, 0, value.position.x)
            rect.set(JAVA_INT, 4, value.position.y)
            rect.set(JAVA_INT, 8, value.size.x)
            rect.set(JAVA_INT, 12, value.size.y)
            val amountCell = arena.allocate(JAVA_DOUBLE)
            amountCell.set(JAVA_DOUBLE, 0, amount)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, rect)
            arr.setAtIndex(ADDRESS, 1, amountCell)
            val ret = arena.allocate(8L, 8L)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return try {
                BuiltinTypes.readArrayPackedVector2Arrays(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, ret)
            }
        }
    }

    /**
     * Calls [methodBind] with one int64 arg and Object return value.
     */
    fun ptrcallWithLongArgRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Long,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_LONG)
            arg0.set(JAVA_LONG, 0, value)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, arg0)
            val ret = arena.allocate(ADDRESS)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(ADDRESS, 0)
        }
    }

    /**
     * Calls [methodBind] with one int32 arg and Object return value.
     */
    fun ptrcallWithIntArgRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Int,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val arg = arena.allocate(JAVA_INT)
            arg.set(JAVA_INT, 0, value)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, arg)
            val ret = arena.allocate(ADDRESS)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(ADDRESS, 0)
        }
    }

    fun ptrcallWithIntArgRetRect2i(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Int,
    ): Rect2i {
        Arena.ofConfined().use { arena ->
            val arg = arena.allocate(JAVA_INT)
            arg.set(JAVA_INT, 0, value)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, arg)
            val ret = arena.allocate(16L, 4L)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return Rect2i(
                position = Vector2i(
                    x = ret.get(JAVA_INT, 0),
                    y = ret.get(JAVA_INT, 4),
                ),
                size = Vector2i(
                    x = ret.get(JAVA_INT, 8),
                    y = ret.get(JAVA_INT, 12),
                ),
            )
        }
    }

    fun ptrcallWithIntArgRetVector2iList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Int,
    ): List<Vector2i> {
        Arena.ofConfined().use { arena ->
            val argCell = arena.allocate(JAVA_INT)
            argCell.set(JAVA_INT, 0, value)
            val args = arena.allocate(ADDRESS, 1)
            args.setAtIndex(ADDRESS, 0, argCell)
            return callArrayReturn(methodBind, instance, args, BuiltinTypes::readArrayVector2i)
        }
    }

    fun ptrcallWithIntArgRetPackedVector2List(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Int,
    ): List<Vector2> {
        Arena.ofConfined().use { arena ->
            val argCell = arena.allocate(JAVA_INT)
            argCell.set(JAVA_INT, 0, value)
            val args = arena.allocate(ADDRESS, 1)
            args.setAtIndex(ADDRESS, 0, argCell)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return try {
                BuiltinTypes.readPackedVector2Array(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR2_ARRAY, ret)
            }
        }
    }

    fun ptrcallWithIntArgRetVector3iList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Int,
    ): List<Vector3i> {
        Arena.ofConfined().use { arena ->
            val argCell = arena.allocate(JAVA_INT)
            argCell.set(JAVA_INT, 0, value)
            val args = arena.allocate(ADDRESS, 1)
            args.setAtIndex(ADDRESS, 0, argCell)
            return callArrayReturn(methodBind, instance, args, BuiltinTypes::readArrayVector3i)
        }
    }

    fun ptrcallWithIntArgRetStringNameList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Int,
    ): List<String> {
        Arena.ofConfined().use { arena ->
            val argCell = arena.allocate(JAVA_INT)
            argCell.set(JAVA_INT, 0, value)
            val args = arena.allocate(ADDRESS, 1)
            args.setAtIndex(ADDRESS, 0, argCell)
            return callArrayReturn(methodBind, instance, args, BuiltinTypes::readArrayStringNames)
        }
    }

    /**
     * Calls [methodBind] with one int32 arg and Rect2 return value.
     */
    fun ptrcallWithIntArgRetRect2(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Int,
    ): Rect2 {
        Arena.ofConfined().use { arena ->
            val arg = arena.allocate(JAVA_INT)
            arg.set(JAVA_INT, 0, value)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, arg)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return Rect2(
                position = Vector2(
                    x = GodotReal.readIndex(ret, 0),
                    y = GodotReal.readIndex(ret, 1),
                ),
                size = Vector2(
                    x = GodotReal.readIndex(ret, 2),
                    y = GodotReal.readIndex(ret, 3),
                ),
            )
        }
    }

    /**
     * Calls [methodBind] with one int32 arg, one Rect2 arg, and no return value.
     */
    fun ptrcallWithIntAndRect2Arg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        index: Int,
        value: Rect2,
    ) {
        Arena.ofConfined().use { arena ->
            val indexCell = arena.allocate(JAVA_INT)
            val rectCell = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
            indexCell.set(JAVA_INT, 0, index)
            writeRect2(rectCell, value)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, indexCell)
            arr.setAtIndex(ADDRESS, 1, rectCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithIntArgRetTransform2D(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Int,
    ): Transform2D {
        Arena.ofConfined().use { arena ->
            val arg = arena.allocate(JAVA_INT)
            arg.set(JAVA_INT, 0, value)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, arg)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 6, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return readTransform2D(ret)
        }
    }

    fun ptrcallWithIntAndTransform2DArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Int,
        transform: Transform2D,
    ) {
        Arena.ofConfined().use { arena ->
            val intCell = arena.allocate(JAVA_INT)
            val transformCell = arena.allocate(GodotReal.SIZE_BYTES * 6, GodotReal.ALIGN_BYTES)
            intCell.set(JAVA_INT, 0, value)
            writeTransform2D(transformCell, transform)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, intCell)
            arr.setAtIndex(ADDRESS, 1, transformCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithIntArgRetTransform3D(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Int,
    ): Transform3D {
        Arena.ofConfined().use { arena ->
            val arg = arena.allocate(JAVA_INT)
            arg.set(JAVA_INT, 0, value)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, arg)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 12, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return readTransform3D(ret)
        }
    }

    /**
     * Calls [methodBind] with one int32 arg and Color return value.
     */
    fun ptrcallWithIntArgRetColor(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Int,
    ): Color {
        Arena.ofConfined().use { arena ->
            val arg = arena.allocate(JAVA_INT)
            arg.set(JAVA_INT, 0, value)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, arg)
            val ret = arena.allocate(16L, 4L)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return Color(
                ret.get(JAVA_FLOAT, 0),
                ret.get(JAVA_FLOAT, 4),
                ret.get(JAVA_FLOAT, 8),
                ret.get(JAVA_FLOAT, 12),
            )
        }
    }

    /**
     * Calls [methodBind] with (int32, int32) args and no return value.
     */
    fun ptrcallWithTwoIntArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Int,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_INT)
            arg0.set(JAVA_INT, 0, first)
            arg1.set(JAVA_INT, 0, second)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (int32, uint32) args and no return value.
     */
    fun ptrcallWithIntAndUInt32Args(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Long,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_INT)
            arg0.set(JAVA_INT, 0, first)
            arg1.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(second))
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (uint32, int32) args and no return value.
     */
    fun ptrcallWithUInt32AndIntArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Long,
        second: Int,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_INT)
            arg0.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(first))
            arg1.set(JAVA_INT, 0, second)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (uint32, int32) args and int32 return value.
     */
    fun ptrcallWithUInt32AndIntArgRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Long,
        second: Int,
    ): Int {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_INT)
            arg0.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(first))
            arg1.set(JAVA_INT, 0, second)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            val ret = arena.allocate(JAVA_INT)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_INT, 0)
        }
    }

    /**
     * Calls [methodBind] with (uint32, int32) args and Object* return value.
     */
    fun ptrcallWithUInt32AndIntArgRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Long,
        second: Int,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_INT)
            arg0.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(first))
            arg1.set(JAVA_INT, 0, second)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            val ret = arena.allocate(ADDRESS)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(ADDRESS, 0)
        }
    }

    /**
     * Calls [methodBind] with (uint32, uint32, float) args and no return value.
     */
    fun ptrcallWithTwoUInt32AndDoubleArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Long,
        second: Long,
        third: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_INT)
            val arg2 = arena.allocate(JAVA_DOUBLE)
            arg0.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(first))
            arg1.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(second))
            arg2.set(JAVA_DOUBLE, 0, third)
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (int32, int32) args and int64 return value.
     */
    fun ptrcallWithTwoIntArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Int,
    ): Long {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_INT)
            arg0.set(JAVA_INT, 0, first)
            arg1.set(JAVA_INT, 0, second)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_LONG, 0)
        }
    }

    /**
     * Calls [methodBind] with (int32, int32) args and int32 return value.
     */
    fun ptrcallWithTwoIntArgsRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Int,
    ): Int {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_INT)
            arg0.set(JAVA_INT, 0, first)
            arg1.set(JAVA_INT, 0, second)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            val ret = arena.allocate(JAVA_INT)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_INT, 0)
        }
    }

    /**
     * Calls [methodBind] with (int32, int32) args and bool return value.
     */
    fun ptrcallWithTwoIntArgsRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Int,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_INT)
            arg0.set(JAVA_INT, 0, first)
            arg1.set(JAVA_INT, 0, second)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            val ret = arena.allocate(JAVA_BYTE)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_BYTE, 0) != 0.toByte()
        }
    }

    /**
     * Calls [methodBind] with (int32, int32, bool) args and no return value.
     */
    fun ptrcallWithTwoIntAndBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Int,
        boolArg: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_INT)
            val arg2 = arena.allocate(JAVA_BYTE)
            arg0.set(JAVA_INT, 0, first)
            arg1.set(JAVA_INT, 0, second)
            arg2.set(JAVA_BYTE, 0, if (boolArg) 1.toByte() else 0.toByte())
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (int32, int32, int64, int64, int64, scalar float, bool, int32, bool).
     */
    fun ptrcallWithTwoIntThreeLongDoubleBoolIntBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Int,
        firstLong: Long,
        secondLong: Long,
        thirdLong: Long,
        value: Double,
        firstBool: Boolean,
        thirdInt: Int,
        secondBool: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val int0 = arena.allocate(JAVA_INT)
            val int1 = arena.allocate(JAVA_INT)
            val long0 = arena.allocate(JAVA_LONG)
            val long1 = arena.allocate(JAVA_LONG)
            val long2 = arena.allocate(JAVA_LONG)
            val doubleCell = arena.allocate(JAVA_DOUBLE)
            val bool0 = arena.allocate(JAVA_BYTE)
            val int2 = arena.allocate(JAVA_INT)
            val bool1 = arena.allocate(JAVA_BYTE)
            int0.set(JAVA_INT, 0, first)
            int1.set(JAVA_INT, 0, second)
            long0.set(JAVA_LONG, 0, firstLong)
            long1.set(JAVA_LONG, 0, secondLong)
            long2.set(JAVA_LONG, 0, thirdLong)
            doubleCell.set(JAVA_DOUBLE, 0, value)
            bool0.set(JAVA_BYTE, 0, if (firstBool) 1.toByte() else 0.toByte())
            int2.set(JAVA_INT, 0, thirdInt)
            bool1.set(JAVA_BYTE, 0, if (secondBool) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 9)
            args.setAtIndex(ADDRESS, 0, int0)
            args.setAtIndex(ADDRESS, 1, int1)
            args.setAtIndex(ADDRESS, 2, long0)
            args.setAtIndex(ADDRESS, 3, long1)
            args.setAtIndex(ADDRESS, 4, long2)
            args.setAtIndex(ADDRESS, 5, doubleCell)
            args.setAtIndex(ADDRESS, 6, bool0)
            args.setAtIndex(ADDRESS, 7, int2)
            args.setAtIndex(ADDRESS, 8, bool1)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (int32, int32, bool, int64/enum, PackedByteArray) args and no return value.
     */
    fun ptrcallWithTwoIntBoolLongByteArrayArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Int,
        boolArg: Boolean,
        mode: Long,
        bytes: ByteArray,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_INT)
            val arg2 = arena.allocate(JAVA_BYTE)
            val arg3 = arena.allocate(JAVA_LONG)
            val arg4 = BuiltinTypes.allocatePackedArray(arena)
            try {
                arg0.set(JAVA_INT, 0, first)
                arg1.set(JAVA_INT, 0, second)
                arg2.set(JAVA_BYTE, 0, if (boolArg) 1.toByte() else 0.toByte())
                arg3.set(JAVA_LONG, 0, mode)
                BuiltinTypes.initPackedByteArray(arg4, bytes)
                val arr = arena.allocate(ADDRESS, 5)
                arr.setAtIndex(ADDRESS, 0, arg0)
                arr.setAtIndex(ADDRESS, 1, arg1)
                arr.setAtIndex(ADDRESS, 2, arg2)
                arr.setAtIndex(ADDRESS, 3, arg3)
                arr.setAtIndex(ADDRESS, 4, arg4)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, arg4)
            }
        }
    }

    fun ptrcallWithThreeIntArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Int,
        third: Int,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_INT)
            val arg2 = arena.allocate(JAVA_INT)
            arg0.set(JAVA_INT, 0, first)
            arg1.set(JAVA_INT, 0, second)
            arg2.set(JAVA_INT, 0, third)
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithThreeIntArgsRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Int,
        third: Int,
    ): Int {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_INT)
            val arg2 = arena.allocate(JAVA_INT)
            arg0.set(JAVA_INT, 0, first)
            arg1.set(JAVA_INT, 0, second)
            arg2.set(JAVA_INT, 0, third)
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            val ret = arena.allocate(JAVA_INT)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_INT, 0)
        }
    }

    fun ptrcallWithThreeIntArgsRetVector2i(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Int,
        third: Int,
    ): Vector2i {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_INT)
            val arg2 = arena.allocate(JAVA_INT)
            arg0.set(JAVA_INT, 0, first)
            arg1.set(JAVA_INT, 0, second)
            arg2.set(JAVA_INT, 0, third)
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            val ret = arena.allocate(8L, 4L)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return readVector2i(ret)
        }
    }

    fun ptrcallWithThreeIntArgsRetVector2(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Int,
        third: Int,
    ): Vector2 {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_INT)
            val arg2 = arena.allocate(JAVA_INT)
            arg0.set(JAVA_INT, 0, first)
            arg1.set(JAVA_INT, 0, second)
            arg2.set(JAVA_INT, 0, third)
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return Vector2(
                x = GodotReal.readIndex(ret, 0),
                y = GodotReal.readIndex(ret, 1),
            )
        }
    }

    fun ptrcallWithThreeIntAndVector2Arg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Int,
        third: Int,
        vector: Vector2,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_INT)
            val arg2 = arena.allocate(JAVA_INT)
            val arg3 = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            arg0.set(JAVA_INT, 0, first)
            arg1.set(JAVA_INT, 0, second)
            arg2.set(JAVA_INT, 0, third)
            writeVector2(arg3, vector)
            val arr = arena.allocate(ADDRESS, 4)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            arr.setAtIndex(ADDRESS, 3, arg3)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithFourIntArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Int,
        third: Int,
        fourth: Int,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_INT)
            val arg2 = arena.allocate(JAVA_INT)
            val arg3 = arena.allocate(JAVA_INT)
            arg0.set(JAVA_INT, 0, first)
            arg1.set(JAVA_INT, 0, second)
            arg2.set(JAVA_INT, 0, third)
            arg3.set(JAVA_INT, 0, fourth)
            val arr = arena.allocate(ADDRESS, 4)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            arr.setAtIndex(ADDRESS, 3, arg3)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithFourIntArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Int,
        third: Int,
        fourth: Int,
    ): Long {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_INT)
            val arg2 = arena.allocate(JAVA_INT)
            val arg3 = arena.allocate(JAVA_INT)
            arg0.set(JAVA_INT, 0, first)
            arg1.set(JAVA_INT, 0, second)
            arg2.set(JAVA_INT, 0, third)
            arg3.set(JAVA_INT, 0, fourth)
            val arr = arena.allocate(ADDRESS, 4)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            arr.setAtIndex(ADDRESS, 3, arg3)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_LONG, 0)
        }
    }

    fun ptrcallWithFourIntArgsRetDictionary(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Int,
        third: Int,
        fourth: Int,
    ): Map<String, Any?> {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_INT)
            val arg2 = arena.allocate(JAVA_INT)
            val arg3 = arena.allocate(JAVA_INT)
            arg0.set(JAVA_INT, 0, first)
            arg1.set(JAVA_INT, 0, second)
            arg2.set(JAVA_INT, 0, third)
            arg3.set(JAVA_INT, 0, fourth)
            val arr = arena.allocate(ADDRESS, 4)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            arr.setAtIndex(ADDRESS, 3, arg3)
            val ret = arena.allocate(8L, 8L)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return try {
                BuiltinTypes.readDictionaryScalars(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.DICTIONARY, ret)
            }
        }
    }

    fun ptrcallWithFourIntAndBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Int,
        third: Int,
        fourth: Int,
        boolValue: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_INT)
            val arg2 = arena.allocate(JAVA_INT)
            val arg3 = arena.allocate(JAVA_INT)
            val arg4 = arena.allocate(JAVA_BYTE)
            arg0.set(JAVA_INT, 0, first)
            arg1.set(JAVA_INT, 0, second)
            arg2.set(JAVA_INT, 0, third)
            arg3.set(JAVA_INT, 0, fourth)
            arg4.set(JAVA_BYTE, 0, if (boolValue) 1.toByte() else 0.toByte())
            val arr = arena.allocate(ADDRESS, 5)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            arr.setAtIndex(ADDRESS, 3, arg3)
            arr.setAtIndex(ADDRESS, 4, arg4)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithFiveIntArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Int,
        third: Int,
        fourth: Int,
        fifth: Int,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_INT)
            val arg2 = arena.allocate(JAVA_INT)
            val arg3 = arena.allocate(JAVA_INT)
            val arg4 = arena.allocate(JAVA_INT)
            arg0.set(JAVA_INT, 0, first)
            arg1.set(JAVA_INT, 0, second)
            arg2.set(JAVA_INT, 0, third)
            arg3.set(JAVA_INT, 0, fourth)
            arg4.set(JAVA_INT, 0, fifth)
            val arr = arena.allocate(ADDRESS, 5)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            arr.setAtIndex(ADDRESS, 3, arg3)
            arr.setAtIndex(ADDRESS, 4, arg4)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithFiveIntArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Int,
        third: Int,
        fourth: Int,
        fifth: Int,
    ): Long {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_INT)
            val arg2 = arena.allocate(JAVA_INT)
            val arg3 = arena.allocate(JAVA_INT)
            val arg4 = arena.allocate(JAVA_INT)
            arg0.set(JAVA_INT, 0, first)
            arg1.set(JAVA_INT, 0, second)
            arg2.set(JAVA_INT, 0, third)
            arg3.set(JAVA_INT, 0, fourth)
            arg4.set(JAVA_INT, 0, fifth)
            val arr = arena.allocate(ADDRESS, 5)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            arr.setAtIndex(ADDRESS, 3, arg3)
            arr.setAtIndex(ADDRESS, 4, arg4)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_LONG, 0)
        }
    }

    fun ptrcallWithTwoIntAndLongArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Int,
        third: Long,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_INT)
            val arg2 = arena.allocate(JAVA_LONG)
            arg0.set(JAVA_INT, 0, first)
            arg1.set(JAVA_INT, 0, second)
            arg2.set(JAVA_LONG, 0, third)
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithTwoIntAndStringArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Int,
        text: String,
    ) {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(JAVA_INT)
            val secondCell = arena.allocate(JAVA_INT)
            val stringCell = arena.allocate(8L, 8L)
            firstCell.set(JAVA_INT, 0, first)
            secondCell.set(JAVA_INT, 0, second)
            GodotStrings.initString(stringCell, text)
            try {
                val arr = arena.allocate(ADDRESS, 3)
                arr.setAtIndex(ADDRESS, 0, firstCell)
                arr.setAtIndex(ADDRESS, 1, secondCell)
                arr.setAtIndex(ADDRESS, 2, stringCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithTwoIntAndColorArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Int,
        color: Color,
    ) {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(JAVA_INT)
            val secondCell = arena.allocate(JAVA_INT)
            firstCell.set(JAVA_INT, 0, first)
            secondCell.set(JAVA_INT, 0, second)
            // Color components use fixed 32-bit storage, unlike scalar float method args.
            val colorCell = arena.allocate(16L, 4L)
            colorCell.set(JAVA_FLOAT, 0, color.r)
            colorCell.set(JAVA_FLOAT, 4, color.g)
            colorCell.set(JAVA_FLOAT, 8, color.b)
            colorCell.set(JAVA_FLOAT, 12, color.a)
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, firstCell)
            arr.setAtIndex(ADDRESS, 1, secondCell)
            arr.setAtIndex(ADDRESS, 2, colorCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithTwoIntAndVector3Arg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Int,
        value: Vector3,
    ) {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(JAVA_INT)
            val secondCell = arena.allocate(JAVA_INT)
            val vec = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            firstCell.set(JAVA_INT, 0, first)
            secondCell.set(JAVA_INT, 0, second)
            GodotReal.writeIndex(vec, 0, value.x)
            GodotReal.writeIndex(vec, 1, value.y)
            GodotReal.writeIndex(vec, 2, value.z)
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, firstCell)
            arr.setAtIndex(ADDRESS, 1, secondCell)
            arr.setAtIndex(ADDRESS, 2, vec)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (int32, int32) args and float(double) return value.
     */
    fun ptrcallWithTwoIntArgsRetDouble(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Int,
    ): Double {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_INT)
            arg0.set(JAVA_INT, 0, first)
            arg1.set(JAVA_INT, 0, second)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            val ret = arena.allocate(JAVA_DOUBLE)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_DOUBLE, 0)
        }
    }

    fun ptrcallWithTwoIntArgsRetColor(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Int,
    ): Color {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_INT)
            arg0.set(JAVA_INT, 0, first)
            arg1.set(JAVA_INT, 0, second)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            val ret = arena.allocate(16L, 4L)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return Color(
                ret.get(JAVA_FLOAT, 0),
                ret.get(JAVA_FLOAT, 4),
                ret.get(JAVA_FLOAT, 8),
                ret.get(JAVA_FLOAT, 12),
            )
        }
    }

    fun ptrcallWithTwoIntArgsRetVector3(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Int,
    ): Vector3 {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_INT)
            arg0.set(JAVA_INT, 0, first)
            arg1.set(JAVA_INT, 0, second)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return Vector3(
                x = GodotReal.readIndex(ret, 0),
                y = GodotReal.readIndex(ret, 1),
                z = GodotReal.readIndex(ret, 2),
            )
        }
    }

    /**
     * Calls [methodBind] with (int32, int32, float) args and no return value.
     */
    fun ptrcallWithTwoIntAndDoubleArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Int,
        value: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_INT)
            val arg2 = arena.allocate(JAVA_DOUBLE)
            arg0.set(JAVA_INT, 0, first)
            arg1.set(JAVA_INT, 0, second)
            arg2.set(JAVA_DOUBLE, 0, value)
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (int32, int32, StringName) args and no return value.
     */
    fun ptrcallWithTwoIntAndStringNameArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Int,
        name: String,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_INT)
            arg0.set(JAVA_INT, 0, first)
            arg1.set(JAVA_INT, 0, second)
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, GodotStrings.makeStringName(name))
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithTwoIntVector2DoubleArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Int,
        vector: Vector2,
        value: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_INT)
            val arg2 = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val arg3 = arena.allocate(JAVA_DOUBLE)
            arg0.set(JAVA_INT, 0, first)
            arg1.set(JAVA_INT, 0, second)
            GodotReal.writeIndex(arg2, 0, vector.x)
            GodotReal.writeIndex(arg2, 1, vector.y)
            arg3.set(JAVA_DOUBLE, 0, value)
            val arr = arena.allocate(ADDRESS, 4)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            arr.setAtIndex(ADDRESS, 3, arg3)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithTwoIntAndVector2iArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Int,
        vector: Vector2i,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_INT)
            val arg2 = arena.allocate(8L, 4L)
            arg0.set(JAVA_INT, 0, first)
            arg1.set(JAVA_INT, 0, second)
            writeVector2i(arg2, vector)
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithTwoIntVector2iArgRetVector2(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Int,
        vector: Vector2i,
    ): Vector2 {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_INT)
            val arg2 = arena.allocate(8L, 4L)
            arg0.set(JAVA_INT, 0, first)
            arg1.set(JAVA_INT, 0, second)
            writeVector2i(arg2, vector)
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return Vector2(
                x = GodotReal.readIndex(ret, 0),
                y = GodotReal.readIndex(ret, 1),
            )
        }
    }

    fun ptrcallWithTwoIntVector2iVector2Args(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Int,
        vector2i: Vector2i,
        vector2: Vector2,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_INT)
            val arg2 = arena.allocate(8L, 4L)
            val arg3 = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            arg0.set(JAVA_INT, 0, first)
            arg1.set(JAVA_INT, 0, second)
            writeVector2i(arg2, vector2i)
            writeVector2(arg3, vector2)
            val arr = arena.allocate(ADDRESS, 4)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            arr.setAtIndex(ADDRESS, 3, arg3)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (int32, int32) args and StringName return value.
     */
    fun ptrcallWithTwoIntArgsRetStringName(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Int,
    ): String {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_INT)
            arg0.set(JAVA_INT, 0, first)
            arg1.set(JAVA_INT, 0, second)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            val ret = arena.allocate(8L, 8L)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return try {
                GodotStrings.readStringName(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.STRING_NAME, ret)
            }
        }
    }

    fun ptrcallWithTwoIntArgsRetVector2iList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Int,
    ): List<Vector2i> {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_INT)
            arg0.set(JAVA_INT, 0, first)
            arg1.set(JAVA_INT, 0, second)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            return callArrayReturn(methodBind, instance, arr, BuiltinTypes::readArrayVector2i)
        }
    }

    fun ptrcallWithTwoIntArgsRetNodePath(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Int,
    ): NodePath {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_INT)
            arg0.set(JAVA_INT, 0, first)
            arg1.set(JAVA_INT, 0, second)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            val ret = arena.allocate(8L, 8L)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return try {
                BuiltinTypes.readTypedNodePath(ret, arena)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.NODE_PATH, ret)
            }
        }
    }

    fun ptrcallWithTwoIntArgsRetVector2i(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Int,
    ): Vector2i {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_INT)
            arg0.set(JAVA_INT, 0, first)
            arg1.set(JAVA_INT, 0, second)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            val ret = arena.allocate(8L, 4L)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return readVector2i(ret)
        }
    }

    fun ptrcallWithTwoIntArgsRetRect2i(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Int,
    ): Rect2i {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_INT)
            arg0.set(JAVA_INT, 0, first)
            arg1.set(JAVA_INT, 0, second)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            val ret = arena.allocate(16L, 4L)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return readRect2i(ret)
        }
    }

    fun ptrcallWithTwoIntTwoBoolArgsRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Int,
        firstBool: Boolean,
        secondBool: Boolean,
    ): Int {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_INT)
            val arg2 = arena.allocate(JAVA_BYTE)
            val arg3 = arena.allocate(JAVA_BYTE)
            arg0.set(JAVA_INT, 0, first)
            arg1.set(JAVA_INT, 0, second)
            arg2.set(JAVA_BYTE, 0, if (firstBool) 1.toByte() else 0.toByte())
            arg3.set(JAVA_BYTE, 0, if (secondBool) 1.toByte() else 0.toByte())
            val arr = arena.allocate(ADDRESS, 4)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            arr.setAtIndex(ADDRESS, 3, arg3)
            val ret = arena.allocate(JAVA_INT)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_INT, 0)
        }
    }

    fun ptrcallWithTwoIntAndNodePathArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Int,
        path: NodePath,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_INT)
            val nodePath = arena.allocate(8L, 8L)
            val pathString = arena.allocate(8L, 8L)
            try {
                arg0.set(JAVA_INT, 0, first)
                arg1.set(JAVA_INT, 0, second)
                GodotStrings.initString(pathString, path.path)
                BuiltinTypes.construct(
                    type = VariantType.NODE_PATH,
                    dest = nodePath,
                    constructorIndex = 2,
                    args = listOf(pathString),
                )
                val arr = arena.allocate(ADDRESS, 3)
                arr.setAtIndex(ADDRESS, 0, arg0)
                arr.setAtIndex(ADDRESS, 1, arg1)
                arr.setAtIndex(ADDRESS, 2, nodePath)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.NODE_PATH, nodePath)
                GodotStrings.destroyString(pathString)
            }
        }
    }

    fun ptrcallWithTwoIntArgsRetVector2(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Int,
    ): Vector2 {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_INT)
            arg0.set(JAVA_INT, 0, first)
            arg1.set(JAVA_INT, 0, second)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return Vector2(
                x = GodotReal.readIndex(ret, 0),
                y = GodotReal.readIndex(ret, 1),
            )
        }
    }

    /**
     * Calls [methodBind] with (int32, int32) args and String return value.
     */
    fun ptrcallWithTwoIntArgsRetString(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Int,
    ): String {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_INT)
            val retString = arena.allocate(8L, 8L)
            try {
                arg0.set(JAVA_INT, 0, first)
                arg1.set(JAVA_INT, 0, second)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, arg0)
                arr.setAtIndex(ADDRESS, 1, arg1)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, retString)
                return GodotStrings.readString(retString)
            } finally {
                GodotStrings.destroyString(retString)
            }
        }
    }

    /**
     * Calls [methodBind] with two int32 args and Variant return value.
     */
    fun ptrcallWithTwoIntArgsRetVariantScalar(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Int,
    ): Any? {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_INT)
            arg0.set(JAVA_INT, 0, first)
            arg1.set(JAVA_INT, 0, second)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            val retVariant = arena.allocate(BuiltinTypes.VARIANT_SIZE, 8L)
            try {
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, retVariant)
                return BuiltinTypes.readVariantScalar(retVariant, arena)
            } finally {
                BuiltinTypes.destroyVariant(retVariant)
            }
        }
    }

    /**
     * Calls [methodBind] with two int32 args and Object* return value.
     */
    fun ptrcallWithTwoIntArgsRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Int,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_INT)
            arg0.set(JAVA_INT, 0, first)
            arg1.set(JAVA_INT, 0, second)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            val ret = arena.allocate(ADDRESS)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(ADDRESS, 0)
        }
    }

    fun ptrcallWithTwoIntBoolLongArgsRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Int,
        boolArg: Boolean,
        longArg: Long,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_INT)
            val arg2 = arena.allocate(JAVA_BYTE)
            val arg3 = arena.allocate(JAVA_LONG)
            arg0.set(JAVA_INT, 0, first)
            arg1.set(JAVA_INT, 0, second)
            arg2.set(JAVA_BYTE, 0, if (boolArg) 1.toByte() else 0.toByte())
            arg3.set(JAVA_LONG, 0, longArg)
            val arr = arena.allocate(ADDRESS, 4)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            arr.setAtIndex(ADDRESS, 3, arg3)
            val ret = arena.allocate(ADDRESS)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(ADDRESS, 0)
        }
    }

    fun ptrcallWithIntAndThreeBoolArgsRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        intArg: Int,
        firstBool: Boolean,
        secondBool: Boolean,
        thirdBool: Boolean,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val intCell = arena.allocate(JAVA_INT)
            val bool0 = arena.allocate(JAVA_BYTE)
            val bool1 = arena.allocate(JAVA_BYTE)
            val bool2 = arena.allocate(JAVA_BYTE)
            intCell.set(JAVA_INT, 0, intArg)
            bool0.set(JAVA_BYTE, 0, if (firstBool) 1.toByte() else 0.toByte())
            bool1.set(JAVA_BYTE, 0, if (secondBool) 1.toByte() else 0.toByte())
            bool2.set(JAVA_BYTE, 0, if (thirdBool) 1.toByte() else 0.toByte())
            val arr = arena.allocate(ADDRESS, 4)
            arr.setAtIndex(ADDRESS, 0, intCell)
            arr.setAtIndex(ADDRESS, 1, bool0)
            arr.setAtIndex(ADDRESS, 2, bool1)
            arr.setAtIndex(ADDRESS, 3, bool2)
            val ret = arena.allocate(ADDRESS)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(ADDRESS, 0)
        }
    }

    fun ptrcallWithTwoIntAndThreeBoolArgsRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Int,
        firstBool: Boolean,
        secondBool: Boolean,
        thirdBool: Boolean,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val int0 = arena.allocate(JAVA_INT)
            val int1 = arena.allocate(JAVA_INT)
            val bool0 = arena.allocate(JAVA_BYTE)
            val bool1 = arena.allocate(JAVA_BYTE)
            val bool2 = arena.allocate(JAVA_BYTE)
            int0.set(JAVA_INT, 0, first)
            int1.set(JAVA_INT, 0, second)
            bool0.set(JAVA_BYTE, 0, if (firstBool) 1.toByte() else 0.toByte())
            bool1.set(JAVA_BYTE, 0, if (secondBool) 1.toByte() else 0.toByte())
            bool2.set(JAVA_BYTE, 0, if (thirdBool) 1.toByte() else 0.toByte())
            val arr = arena.allocate(ADDRESS, 5)
            arr.setAtIndex(ADDRESS, 0, int0)
            arr.setAtIndex(ADDRESS, 1, int1)
            arr.setAtIndex(ADDRESS, 2, bool0)
            arr.setAtIndex(ADDRESS, 3, bool1)
            arr.setAtIndex(ADDRESS, 4, bool2)
            val ret = arena.allocate(ADDRESS)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(ADDRESS, 0)
        }
    }

    fun ptrcallWithThreeIntArgsRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Int,
        third: Int,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val int0 = arena.allocate(JAVA_INT)
            val int1 = arena.allocate(JAVA_INT)
            val int2 = arena.allocate(JAVA_INT)
            int0.set(JAVA_INT, 0, first)
            int1.set(JAVA_INT, 0, second)
            int2.set(JAVA_INT, 0, third)
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, int0)
            arr.setAtIndex(ADDRESS, 1, int1)
            arr.setAtIndex(ADDRESS, 2, int2)
            val ret = arena.allocate(ADDRESS)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(ADDRESS, 0)
        }
    }

    fun ptrcallWithLongThreeIntBoolObjectListArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        type: Long,
        width: Int,
        height: Int,
        depth: Int,
        useMipmaps: Boolean,
        images: List<*>,
    ): Long {
        Arena.ofConfined().use { arena ->
            val typeCell = arena.allocate(JAVA_LONG)
            val widthCell = arena.allocate(JAVA_INT)
            val heightCell = arena.allocate(JAVA_INT)
            val depthCell = arena.allocate(JAVA_INT)
            val useMipmapsCell = arena.allocate(JAVA_BYTE)
            val imageArray = arena.allocate(8L, 8L)
            try {
                typeCell.set(JAVA_LONG, 0, type)
                widthCell.set(JAVA_INT, 0, width)
                heightCell.set(JAVA_INT, 0, height)
                depthCell.set(JAVA_INT, 0, depth)
                useMipmapsCell.set(JAVA_BYTE, 0, if (useMipmaps) 1.toByte() else 0.toByte())
                BuiltinTypes.initArrayOfObjects(imageArray, images)
                val args = arena.allocate(ADDRESS, 6)
                args.setAtIndex(ADDRESS, 0, typeCell)
                args.setAtIndex(ADDRESS, 1, widthCell)
                args.setAtIndex(ADDRESS, 2, heightCell)
                args.setAtIndex(ADDRESS, 3, depthCell)
                args.setAtIndex(ADDRESS, 4, useMipmapsCell)
                args.setAtIndex(ADDRESS, 5, imageArray)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, imageArray)
            }
        }
    }

    fun ptrcallWithLongThreeIntBoolObjectListArgsRetRID(
        methodBind: MemorySegment,
        instance: MemorySegment,
        type: Long,
        width: Int,
        height: Int,
        depth: Int,
        useMipmaps: Boolean,
        images: List<*>,
    ): RID {
        Arena.ofConfined().use { arena ->
            val typeCell = arena.allocate(JAVA_LONG)
            val widthCell = arena.allocate(JAVA_INT)
            val heightCell = arena.allocate(JAVA_INT)
            val depthCell = arena.allocate(JAVA_INT)
            val useMipmapsCell = arena.allocate(JAVA_BYTE)
            val imageArray = arena.allocate(8L, 8L)
            try {
                typeCell.set(JAVA_LONG, 0, type)
                widthCell.set(JAVA_INT, 0, width)
                heightCell.set(JAVA_INT, 0, height)
                depthCell.set(JAVA_INT, 0, depth)
                useMipmapsCell.set(JAVA_BYTE, 0, if (useMipmaps) 1.toByte() else 0.toByte())
                BuiltinTypes.initArrayOfObjects(imageArray, images)
                val args = arena.allocate(ADDRESS, 6)
                args.setAtIndex(ADDRESS, 0, typeCell)
                args.setAtIndex(ADDRESS, 1, widthCell)
                args.setAtIndex(ADDRESS, 2, heightCell)
                args.setAtIndex(ADDRESS, 3, depthCell)
                args.setAtIndex(ADDRESS, 4, useMipmapsCell)
                args.setAtIndex(ADDRESS, 5, imageArray)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return RID(ret.get(JAVA_LONG, 0))
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, imageArray)
            }
        }
    }

    fun ptrcallWithLongAndFourIntArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        longArg: Long,
        first: Int,
        second: Int,
        third: Int,
        fourth: Int,
    ) {
        Arena.ofConfined().use { arena ->
            val longCell = arena.allocate(JAVA_LONG)
            val int0 = arena.allocate(JAVA_INT)
            val int1 = arena.allocate(JAVA_INT)
            val int2 = arena.allocate(JAVA_INT)
            val int3 = arena.allocate(JAVA_INT)
            longCell.set(JAVA_LONG, 0, longArg)
            int0.set(JAVA_INT, 0, first)
            int1.set(JAVA_INT, 0, second)
            int2.set(JAVA_INT, 0, third)
            int3.set(JAVA_INT, 0, fourth)
            val arr = arena.allocate(ADDRESS, 5)
            arr.setAtIndex(ADDRESS, 0, longCell)
            arr.setAtIndex(ADDRESS, 1, int0)
            arr.setAtIndex(ADDRESS, 2, int1)
            arr.setAtIndex(ADDRESS, 3, int2)
            arr.setAtIndex(ADDRESS, 4, int3)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithLongAndFourIntArgsRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        longArg: Long,
        first: Int,
        second: Int,
        third: Int,
        fourth: Int,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val longCell = arena.allocate(JAVA_LONG)
            val int0 = arena.allocate(JAVA_INT)
            val int1 = arena.allocate(JAVA_INT)
            val int2 = arena.allocate(JAVA_INT)
            val int3 = arena.allocate(JAVA_INT)
            longCell.set(JAVA_LONG, 0, longArg)
            int0.set(JAVA_INT, 0, first)
            int1.set(JAVA_INT, 0, second)
            int2.set(JAVA_INT, 0, third)
            int3.set(JAVA_INT, 0, fourth)
            val arr = arena.allocate(ADDRESS, 5)
            arr.setAtIndex(ADDRESS, 0, longCell)
            arr.setAtIndex(ADDRESS, 1, int0)
            arr.setAtIndex(ADDRESS, 2, int1)
            arr.setAtIndex(ADDRESS, 3, int2)
            arr.setAtIndex(ADDRESS, 4, int3)
            val ret = arena.allocate(JAVA_BYTE)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_BYTE, 0) != 0.toByte()
        }
    }

    fun ptrcallWithLongBoolDoubleIntAndTwoDoubleArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        longArg: Long,
        boolArg: Boolean,
        firstDouble: Double,
        intArg: Int,
        secondDouble: Double,
        thirdDouble: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_LONG)
            val arg1 = arena.allocate(JAVA_BYTE)
            val arg2 = arena.allocate(JAVA_DOUBLE)
            val arg3 = arena.allocate(JAVA_INT)
            val arg4 = arena.allocate(JAVA_DOUBLE)
            val arg5 = arena.allocate(JAVA_DOUBLE)
            arg0.set(JAVA_LONG, 0, longArg)
            arg1.set(JAVA_BYTE, 0, if (boolArg) 1.toByte() else 0.toByte())
            arg2.set(JAVA_DOUBLE, 0, firstDouble)
            arg3.set(JAVA_INT, 0, intArg)
            arg4.set(JAVA_DOUBLE, 0, secondDouble)
            arg5.set(JAVA_DOUBLE, 0, thirdDouble)
            val arr = arena.allocate(ADDRESS, 6)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            arr.setAtIndex(ADDRESS, 3, arg3)
            arr.setAtIndex(ADDRESS, 4, arg4)
            arr.setAtIndex(ADDRESS, 5, arg5)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with two int32 args and decodes an Array return as Kotlin values.
     */
    fun ptrcallWithTwoIntArgsRetArray(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Int,
    ): List<Any?> {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_INT)
            arg0.set(JAVA_INT, 0, first)
            arg1.set(JAVA_INT, 0, second)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            return callArrayReturn(methodBind, instance, arr, BuiltinTypes::readArrayScalars)
        }
    }

    /**
     * Calls [methodBind] with (Object*, int32) args and int64 return value.
     */
    fun ptrcallWithObjectAndIntArgRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        intArg: Int,
    ): Long {
        Arena.ofConfined().use { arena ->
            val objCell = arena.allocate(ADDRESS)
            val intCell = arena.allocate(JAVA_INT)
            objCell.set(ADDRESS, 0, objectArg)
            intCell.set(JAVA_INT, 0, intArg)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, objCell)
            arr.setAtIndex(ADDRESS, 1, intCell)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_LONG, 0)
        }
    }

    /**
     * Calls [methodBind] with (Object*, int32) args and no return value.
     */
    fun ptrcallWithObjectAndIntArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        intArg: Int,
    ) {
        Arena.ofConfined().use { arena ->
            val objCell = arena.allocate(ADDRESS)
            val intCell = arena.allocate(JAVA_INT)
            objCell.set(ADDRESS, 0, objectArg)
            intCell.set(JAVA_INT, 0, intArg)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, objCell)
            arr.setAtIndex(ADDRESS, 1, intCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (Object*, int32) args and int32 return value.
     */
    fun ptrcallWithObjectAndIntArgRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        intArg: Int,
    ): Int {
        Arena.ofConfined().use { arena ->
            val objCell = arena.allocate(ADDRESS)
            val intCell = arena.allocate(JAVA_INT)
            objCell.set(ADDRESS, 0, objectArg)
            intCell.set(JAVA_INT, 0, intArg)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, objCell)
            arr.setAtIndex(ADDRESS, 1, intCell)
            val ret = arena.allocate(JAVA_INT)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_INT, 0)
        }
    }

    fun ptrcallWithIntObjectAndIntArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstInt: Int,
        objectArg: MemorySegment,
        secondInt: Int,
    ) {
        Arena.ofConfined().use { arena ->
            val int0 = arena.allocate(JAVA_INT)
            val objCell = arena.allocate(ADDRESS)
            val int1 = arena.allocate(JAVA_INT)
            int0.set(JAVA_INT, 0, firstInt)
            objCell.set(ADDRESS, 0, objectArg)
            int1.set(JAVA_INT, 0, secondInt)
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, int0)
            arr.setAtIndex(ADDRESS, 1, objCell)
            arr.setAtIndex(ADDRESS, 2, int1)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithIntObjectBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        intValue: Int,
        objectArg: MemorySegment,
        boolValue: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val intCell = arena.allocate(JAVA_INT)
            val objCell = arena.allocate(ADDRESS)
            val boolCell = arena.allocate(JAVA_BYTE)
            intCell.set(JAVA_INT, 0, intValue)
            objCell.set(ADDRESS, 0, objectArg)
            boolCell.set(JAVA_BYTE, 0, if (boolValue) 1.toByte() else 0.toByte())
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, intCell)
            arr.setAtIndex(ADDRESS, 1, objCell)
            arr.setAtIndex(ADDRESS, 2, boolCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithObjectDoubleAndIntArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        doubleArg: Double,
        intArg: Int,
    ) {
        Arena.ofConfined().use { arena ->
            val objCell = arena.allocate(ADDRESS)
            val doubleCell = arena.allocate(JAVA_DOUBLE)
            val intCell = arena.allocate(JAVA_INT)
            objCell.set(ADDRESS, 0, objectArg)
            doubleCell.set(JAVA_DOUBLE, 0, doubleArg)
            intCell.set(JAVA_INT, 0, intArg)
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, objCell)
            arr.setAtIndex(ADDRESS, 1, doubleCell)
            arr.setAtIndex(ADDRESS, 2, intCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithObjectVector2AndIntArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        vector: Vector2,
        intArg: Int,
    ) {
        Arena.ofConfined().use { arena ->
            val objCell = arena.allocate(ADDRESS)
            val vectorCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val intCell = arena.allocate(JAVA_INT)
            objCell.set(ADDRESS, 0, objectArg)
            GodotReal.writeIndex(vectorCell, 0, vector.x)
            GodotReal.writeIndex(vectorCell, 1, vector.y)
            intCell.set(JAVA_INT, 0, intArg)
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, objCell)
            arr.setAtIndex(ADDRESS, 1, vectorCell)
            arr.setAtIndex(ADDRESS, 2, intCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with one Object arg and Object return value.
     */
    fun ptrcallWithObjectArgRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val objCell = arena.allocate(ADDRESS)
            objCell.set(ADDRESS, 0, objectArg)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, objCell)
            val ret = arena.allocate(ADDRESS)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(ADDRESS, 0)
        }
    }

    /**
     * Calls [methodBind] with (int64, bool) and String return value.
     */
    fun ptrcallWithLongAndBoolArgRetString(
        methodBind: MemorySegment,
        instance: MemorySegment,
        longValue: Long,
        boolValue: Boolean,
    ): String {
        Arena.ofConfined().use { arena ->
            val longCell = arena.allocate(JAVA_LONG)
            longCell.set(JAVA_LONG, 0, longValue)
            val boolCell = arena.allocate(java.lang.foreign.ValueLayout.JAVA_BYTE)
            boolCell.set(java.lang.foreign.ValueLayout.JAVA_BYTE, 0, if (boolValue) 1.toByte() else 0.toByte())
            val retString = arena.allocate(8L, 8L)
            try {
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, longCell)
                arr.setAtIndex(ADDRESS, 1, boolCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, retString)
                return GodotStrings.readString(retString)
            } finally {
                GodotStrings.destroyString(retString)
            }
        }
    }

    /**
     * Calls [methodBind] with (int32, bool) args and no return value.
     */
    fun ptrcallWithIntAndBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        intValue: Int,
        boolValue: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val intCell = arena.allocate(JAVA_INT)
            intCell.set(JAVA_INT, 0, intValue)
            val boolCell = arena.allocate(java.lang.foreign.ValueLayout.JAVA_BYTE)
            boolCell.set(java.lang.foreign.ValueLayout.JAVA_BYTE, 0, if (boolValue) 1.toByte() else 0.toByte())
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, intCell)
            arr.setAtIndex(ADDRESS, 1, boolCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (uint32, bool) args and no return value.
     */
    fun ptrcallWithUInt32AndBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        intValue: Long,
        boolValue: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val intCell = arena.allocate(JAVA_INT)
            intCell.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(intValue))
            val boolCell = arena.allocate(JAVA_BYTE)
            boolCell.set(JAVA_BYTE, 0, if (boolValue) 1.toByte() else 0.toByte())
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, intCell)
            arr.setAtIndex(ADDRESS, 1, boolCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (uint32, bool) args and RID return value.
     */
    fun ptrcallWithUInt32AndBoolArgRetRID(
        methodBind: MemorySegment,
        instance: MemorySegment,
        intValue: Long,
        boolValue: Boolean,
    ): RID {
        Arena.ofConfined().use { arena ->
            val intCell = arena.allocate(JAVA_INT)
            intCell.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(intValue))
            val boolCell = arena.allocate(JAVA_BYTE)
            boolCell.set(JAVA_BYTE, 0, if (boolValue) 1.toByte() else 0.toByte())
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, intCell)
            arr.setAtIndex(ADDRESS, 1, boolCell)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return RID(ret.get(JAVA_LONG, 0))
        }
    }

    fun ptrcallWithRIDUInt32BoolArgsRetVector2(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        intValue: Long,
        boolValue: Boolean,
    ): Vector2 {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            val intCell = arena.allocate(JAVA_INT)
            intCell.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(intValue))
            val boolCell = arena.allocate(JAVA_BYTE)
            boolCell.set(JAVA_BYTE, 0, if (boolValue) 1.toByte() else 0.toByte())
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, ridCell)
            arr.setAtIndex(ADDRESS, 1, intCell)
            arr.setAtIndex(ADDRESS, 2, boolCell)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return Vector2(
                GodotReal.readIndex(ret, 0),
                GodotReal.readIndex(ret, 1),
            )
        }
    }

    fun ptrcallWithRIDUInt32BoolArgsRetVector3(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        intValue: Long,
        boolValue: Boolean,
    ): Vector3 {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            val intCell = arena.allocate(JAVA_INT)
            intCell.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(intValue))
            val boolCell = arena.allocate(JAVA_BYTE)
            boolCell.set(JAVA_BYTE, 0, if (boolValue) 1.toByte() else 0.toByte())
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, ridCell)
            arr.setAtIndex(ADDRESS, 1, intCell)
            arr.setAtIndex(ADDRESS, 2, boolCell)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return Vector3(
                GodotReal.readIndex(ret, 0),
                GodotReal.readIndex(ret, 1),
                GodotReal.readIndex(ret, 2),
            )
        }
    }

    fun ptrcallWithIntAndBoolArgsRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        intValue: Int,
        boolValue: Boolean,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val intCell = arena.allocate(JAVA_INT)
            val boolCell = arena.allocate(JAVA_BYTE)
            intCell.set(JAVA_INT, 0, intValue)
            boolCell.set(JAVA_BYTE, 0, if (boolValue) 1.toByte() else 0.toByte())
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, intCell)
            arr.setAtIndex(ADDRESS, 1, boolCell)
            val ret = arena.allocate(JAVA_BYTE)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_BYTE, 0) != 0.toByte()
        }
    }

    fun ptrcallWithIntAndBoolArgRetNodePath(
        methodBind: MemorySegment,
        instance: MemorySegment,
        intValue: Int,
        boolValue: Boolean,
    ): NodePath {
        Arena.ofConfined().use { arena ->
            val intCell = arena.allocate(JAVA_INT)
            val boolCell = arena.allocate(JAVA_BYTE)
            intCell.set(JAVA_INT, 0, intValue)
            boolCell.set(JAVA_BYTE, 0, if (boolValue) 1.toByte() else 0.toByte())
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, intCell)
            arr.setAtIndex(ADDRESS, 1, boolCell)
            val ret = arena.allocate(8L, 8L)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return try {
                BuiltinTypes.readTypedNodePath(ret, arena)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.NODE_PATH, ret)
            }
        }
    }

    fun ptrcallWithIntAndBoolArgRetRect2(
        methodBind: MemorySegment,
        instance: MemorySegment,
        intValue: Int,
        boolValue: Boolean,
    ): Rect2 {
        Arena.ofConfined().use { arena ->
            val intCell = arena.allocate(JAVA_INT)
            val boolCell = arena.allocate(JAVA_BYTE)
            intCell.set(JAVA_INT, 0, intValue)
            boolCell.set(JAVA_BYTE, 0, if (boolValue) 1.toByte() else 0.toByte())
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, intCell)
            arr.setAtIndex(ADDRESS, 1, boolCell)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return readRect2(ret)
        }
    }

    fun ptrcallWithIntBoolIntArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        intValue: Int,
        boolValue: Boolean,
        secondInt: Int,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_BYTE)
            val arg2 = arena.allocate(JAVA_INT)
            arg0.set(JAVA_INT, 0, intValue)
            arg1.set(JAVA_BYTE, 0, if (boolValue) 1.toByte() else 0.toByte())
            arg2.set(JAVA_INT, 0, secondInt)
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithIntBoolIntBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        intValue: Int,
        boolValue: Boolean,
        secondInt: Int,
        secondBool: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_BYTE)
            val arg2 = arena.allocate(JAVA_INT)
            val arg3 = arena.allocate(JAVA_BYTE)
            arg0.set(JAVA_INT, 0, intValue)
            arg1.set(JAVA_BYTE, 0, if (boolValue) 1.toByte() else 0.toByte())
            arg2.set(JAVA_INT, 0, secondInt)
            arg3.set(JAVA_BYTE, 0, if (secondBool) 1.toByte() else 0.toByte())
            val arr = arena.allocate(ADDRESS, 4)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            arr.setAtIndex(ADDRESS, 3, arg3)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithIntBoolTwoIntArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        intValue: Int,
        boolValue: Boolean,
        secondInt: Int,
        thirdInt: Int,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_BYTE)
            val arg2 = arena.allocate(JAVA_INT)
            val arg3 = arena.allocate(JAVA_INT)
            arg0.set(JAVA_INT, 0, intValue)
            arg1.set(JAVA_BYTE, 0, if (boolValue) 1.toByte() else 0.toByte())
            arg2.set(JAVA_INT, 0, secondInt)
            arg3.set(JAVA_INT, 0, thirdInt)
            val arr = arena.allocate(ADDRESS, 4)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            arr.setAtIndex(ADDRESS, 3, arg3)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithIntTwoBoolTwoIntArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        intValue: Int,
        firstBool: Boolean,
        secondBool: Boolean,
        secondInt: Int,
        thirdInt: Int,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_BYTE)
            val arg2 = arena.allocate(JAVA_BYTE)
            val arg3 = arena.allocate(JAVA_INT)
            val arg4 = arena.allocate(JAVA_INT)
            arg0.set(JAVA_INT, 0, intValue)
            arg1.set(JAVA_BYTE, 0, if (firstBool) 1.toByte() else 0.toByte())
            arg2.set(JAVA_BYTE, 0, if (secondBool) 1.toByte() else 0.toByte())
            arg3.set(JAVA_INT, 0, secondInt)
            arg4.set(JAVA_INT, 0, thirdInt)
            val arr = arena.allocate(ADDRESS, 5)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            arr.setAtIndex(ADDRESS, 3, arg3)
            arr.setAtIndex(ADDRESS, 4, arg4)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (int32, int64) args and no return value.
     */
    fun ptrcallWithIntAndLongArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Long,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_LONG)
            arg0.set(JAVA_INT, 0, first)
            arg1.set(JAVA_LONG, 0, second)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithIntTwoLongArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Long,
        third: Long,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_LONG)
            val arg2 = arena.allocate(JAVA_LONG)
            arg0.set(JAVA_INT, 0, first)
            arg1.set(JAVA_LONG, 0, second)
            arg2.set(JAVA_LONG, 0, third)
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithIntTwoLongArgsRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Long,
        third: Long,
    ): Int {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_LONG)
            val arg2 = arena.allocate(JAVA_LONG)
            arg0.set(JAVA_INT, 0, first)
            arg1.set(JAVA_LONG, 0, second)
            arg2.set(JAVA_LONG, 0, third)
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            val ret = arena.allocate(JAVA_INT)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_INT, 0)
        }
    }

    fun ptrcallWithIntAndLongArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Long,
    ): Long {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_LONG)
            arg0.set(JAVA_INT, 0, first)
            arg1.set(JAVA_LONG, 0, second)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_LONG, 0)
        }
    }

    fun ptrcallWithIntAndLongArgsRetRID(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Long,
    ): RID {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_LONG)
            arg0.set(JAVA_INT, 0, first)
            arg1.set(JAVA_LONG, 0, second)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return RID(ret.get(JAVA_LONG, 0))
        }
    }

    /**
     * Calls [methodBind] with (int32, String) args and no return value.
     */
    fun ptrcallWithIntAndStringArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        intValue: Int,
        stringValue: String,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(8L, 8L)
            arg0.set(JAVA_INT, 0, intValue)
            GodotStrings.initString(arg1, stringValue)
            try {
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, arg0)
                arr.setAtIndex(ADDRESS, 1, arg1)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(arg1)
            }
        }
    }

    /**
     * Calls [methodBind] with (int32, String) args and int32 return value.
     */
    fun ptrcallWithIntAndStringArgRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        intValue: Int,
        stringValue: String,
    ): Int {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(8L, 8L)
            arg0.set(JAVA_INT, 0, intValue)
            GodotStrings.initString(arg1, stringValue)
            try {
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, arg0)
                arr.setAtIndex(ADDRESS, 1, arg1)
                val ret = arena.allocate(JAVA_INT)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_INT, 0)
            } finally {
                GodotStrings.destroyString(arg1)
            }
        }
    }

    fun ptrcallWithTwoIntStringArgsRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Int,
        value: String,
    ): Int {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(JAVA_INT)
            val secondCell = arena.allocate(JAVA_INT)
            val stringCell = arena.allocate(8L, 8L)
            try {
                firstCell.set(JAVA_INT, 0, first)
                secondCell.set(JAVA_INT, 0, second)
                GodotStrings.initString(stringCell, value)
                val arr = arena.allocate(ADDRESS, 3)
                arr.setAtIndex(ADDRESS, 0, firstCell)
                arr.setAtIndex(ADDRESS, 1, secondCell)
                arr.setAtIndex(ADDRESS, 2, stringCell)
                val ret = arena.allocate(JAVA_INT)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_INT, 0)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithIntAndStringArgRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        intValue: Int,
        stringValue: String,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(8L, 8L)
            arg0.set(JAVA_INT, 0, intValue)
            GodotStrings.initString(arg1, stringValue)
            try {
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, arg0)
                arr.setAtIndex(ADDRESS, 1, arg1)
                val ret = arena.allocate(JAVA_BYTE)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_BYTE, 0) != 0.toByte()
            } finally {
                GodotStrings.destroyString(arg1)
            }
        }
    }

    /**
     * Calls [methodBind] with (int32, int32, String, String, int32) args and int32 return value.
     */
    fun ptrcallWithTwoIntTwoStringAndIntArgsRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstInt: Int,
        secondInt: Int,
        firstString: String,
        secondString: String,
        thirdInt: Int,
    ): Int {
        Arena.ofConfined().use { arena ->
            val int0 = arena.allocate(JAVA_INT)
            val int1 = arena.allocate(JAVA_INT)
            val string0 = arena.allocate(8L, 8L)
            val string1 = arena.allocate(8L, 8L)
            val int2 = arena.allocate(JAVA_INT)
            int0.set(JAVA_INT, 0, firstInt)
            int1.set(JAVA_INT, 0, secondInt)
            int2.set(JAVA_INT, 0, thirdInt)
            GodotStrings.initString(string0, firstString)
            GodotStrings.initString(string1, secondString)
            try {
                val arr = arena.allocate(ADDRESS, 5)
                arr.setAtIndex(ADDRESS, 0, int0)
                arr.setAtIndex(ADDRESS, 1, int1)
                arr.setAtIndex(ADDRESS, 2, string0)
                arr.setAtIndex(ADDRESS, 3, string1)
                arr.setAtIndex(ADDRESS, 4, int2)
                val ret = arena.allocate(JAVA_INT)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_INT, 0)
            } finally {
                GodotStrings.destroyString(string1)
                GodotStrings.destroyString(string0)
            }
        }
    }

    fun ptrcallWithTwoIntTwoStringArgsRetDictionary(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstInt: Int,
        secondInt: Int,
        firstString: String,
        secondString: String,
    ): Map<String, Any?> {
        Arena.ofConfined().use { arena ->
            val int0 = arena.allocate(JAVA_INT)
            val int1 = arena.allocate(JAVA_INT)
            val string0 = arena.allocate(8L, 8L)
            val string1 = arena.allocate(8L, 8L)
            val ret = arena.allocate(8L, 8L)
            int0.set(JAVA_INT, 0, firstInt)
            int1.set(JAVA_INT, 0, secondInt)
            GodotStrings.initString(string0, firstString)
            GodotStrings.initString(string1, secondString)
            try {
                val arr = arena.allocate(ADDRESS, 4)
                arr.setAtIndex(ADDRESS, 0, int0)
                arr.setAtIndex(ADDRESS, 1, int1)
                arr.setAtIndex(ADDRESS, 2, string0)
                arr.setAtIndex(ADDRESS, 3, string1)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return BuiltinTypes.readDictionaryScalars(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.DICTIONARY, ret)
                GodotStrings.destroyString(string1)
                GodotStrings.destroyString(string0)
            }
        }
    }

    /**
     * Calls [methodBind] with (bool, bool) and String return value.
     */
    fun ptrcallWithTwoBoolArgsRetString(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Boolean,
        second: Boolean,
    ): String {
        Arena.ofConfined().use { arena ->
            val bool0 = arena.allocate(java.lang.foreign.ValueLayout.JAVA_BYTE)
            val bool1 = arena.allocate(java.lang.foreign.ValueLayout.JAVA_BYTE)
            bool0.set(java.lang.foreign.ValueLayout.JAVA_BYTE, 0, if (first) 1.toByte() else 0.toByte())
            bool1.set(java.lang.foreign.ValueLayout.JAVA_BYTE, 0, if (second) 1.toByte() else 0.toByte())
            val retString = arena.allocate(8L, 8L)
            try {
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, bool0)
                arr.setAtIndex(ADDRESS, 1, bool1)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, retString)
                return GodotStrings.readString(retString)
            } finally {
                GodotStrings.destroyString(retString)
            }
        }
    }

    fun ptrcallWithDoubleAndBoolArgRetVector3(
        methodBind: MemorySegment,
        instance: MemorySegment,
        doubleArg: Double,
        boolArg: Boolean,
    ): Vector3 {
        Arena.ofConfined().use { arena ->
            val doubleCell = arena.allocate(JAVA_DOUBLE)
            val boolCell = arena.allocate(JAVA_BYTE)
            doubleCell.set(JAVA_DOUBLE, 0, doubleArg)
            boolCell.set(JAVA_BYTE, 0, if (boolArg) 1.toByte() else 0.toByte())
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, doubleCell)
            arr.setAtIndex(ADDRESS, 1, boolCell)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return Vector3(
                x = GodotReal.readIndex(ret, 0),
                y = GodotReal.readIndex(ret, 1),
                z = GodotReal.readIndex(ret, 2),
            )
        }
    }

    /**
     * Calls [methodBind] with one scalar float arg, one bool arg, and no return value.
     */
    fun ptrcallWithDoubleAndBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        doubleArg: Double,
        boolArg: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val doubleCell = arena.allocate(JAVA_DOUBLE)
            val boolCell = arena.allocate(JAVA_BYTE)
            doubleCell.set(JAVA_DOUBLE, 0, doubleArg)
            boolCell.set(JAVA_BYTE, 0, if (boolArg) 1.toByte() else 0.toByte())
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, doubleCell)
            arr.setAtIndex(ADDRESS, 1, boolCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithDoubleAndIntArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        doubleArg: Double,
        intArg: Int,
    ) {
        Arena.ofConfined().use { arena ->
            val doubleCell = arena.allocate(JAVA_DOUBLE)
            val intCell = arena.allocate(JAVA_INT)
            // Scalar Godot float method args use the ABI's 64-bit Variant float slot.
            doubleCell.set(JAVA_DOUBLE, 0, doubleArg)
            intCell.set(JAVA_INT, 0, intArg)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, doubleCell)
            arr.setAtIndex(ADDRESS, 1, intCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithDoubleAndIntArgsRetPackedInt32List(
        methodBind: MemorySegment,
        instance: MemorySegment,
        doubleArg: Double,
        intArg: Int,
    ): List<Int> {
        Arena.ofConfined().use { arena ->
            val doubleCell = arena.allocate(JAVA_DOUBLE)
            val intCell = arena.allocate(JAVA_INT)
            // Scalar Godot float method args use the ABI's 64-bit Variant float slot.
            doubleCell.set(JAVA_DOUBLE, 0, doubleArg)
            intCell.set(JAVA_INT, 0, intArg)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, doubleCell)
            arr.setAtIndex(ADDRESS, 1, intCell)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return try {
                BuiltinTypes.readPackedInt32Array(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_INT32_ARRAY, ret)
            }
        }
    }

    fun ptrcallWithDoubleAndIntArgsRetPackedVector2List(
        methodBind: MemorySegment,
        instance: MemorySegment,
        doubleArg: Double,
        intArg: Int,
    ): List<Vector2> {
        Arena.ofConfined().use { arena ->
            val doubleCell = arena.allocate(JAVA_DOUBLE)
            val intCell = arena.allocate(JAVA_INT)
            // Scalar Godot float method args use the ABI's 64-bit Variant float slot.
            doubleCell.set(JAVA_DOUBLE, 0, doubleArg)
            intCell.set(JAVA_INT, 0, intArg)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, doubleCell)
            arr.setAtIndex(ADDRESS, 1, intCell)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return try {
                BuiltinTypes.readPackedVector2Array(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR2_ARRAY, ret)
            }
        }
    }

    fun ptrcallWithTwoDoubleAndLongArgsRetVector2(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstDouble: Double,
        secondDouble: Double,
        longArg: Long,
    ): Vector2 {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(JAVA_DOUBLE)
            val secondCell = arena.allocate(JAVA_DOUBLE)
            val longCell = arena.allocate(JAVA_LONG)
            firstCell.set(JAVA_DOUBLE, 0, firstDouble)
            secondCell.set(JAVA_DOUBLE, 0, secondDouble)
            longCell.set(JAVA_LONG, 0, longArg)
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, firstCell)
            arr.setAtIndex(ADDRESS, 1, secondCell)
            arr.setAtIndex(ADDRESS, 2, longCell)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return Vector2(
                x = GodotReal.readIndex(ret, 0),
                y = GodotReal.readIndex(ret, 1),
            )
        }
    }

    /**
     * Calls [methodBind] with one scalar float arg, one bool arg, and Vector2 return value.
     */
    fun ptrcallWithDoubleAndBoolArgRetVector2(
        methodBind: MemorySegment,
        instance: MemorySegment,
        doubleArg: Double,
        boolArg: Boolean,
    ): Vector2 {
        Arena.ofConfined().use { arena ->
            val doubleCell = arena.allocate(JAVA_DOUBLE)
            val boolCell = arena.allocate(JAVA_BYTE)
            doubleCell.set(JAVA_DOUBLE, 0, doubleArg)
            boolCell.set(JAVA_BYTE, 0, if (boolArg) 1.toByte() else 0.toByte())
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, doubleCell)
            arr.setAtIndex(ADDRESS, 1, boolCell)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return Vector2(
                x = GodotReal.readIndex(ret, 0),
                y = GodotReal.readIndex(ret, 1),
            )
        }
    }

    /**
     * Calls [methodBind] with one scalar float arg, one bool arg, and Transform2D return value.
     */
    fun ptrcallWithDoubleAndBoolArgRetTransform2D(
        methodBind: MemorySegment,
        instance: MemorySegment,
        doubleArg: Double,
        boolArg: Boolean,
    ): Transform2D {
        Arena.ofConfined().use { arena ->
            val doubleCell = arena.allocate(JAVA_DOUBLE)
            val boolCell = arena.allocate(JAVA_BYTE)
            doubleCell.set(JAVA_DOUBLE, 0, doubleArg)
            boolCell.set(JAVA_BYTE, 0, if (boolArg) 1.toByte() else 0.toByte())
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, doubleCell)
            arr.setAtIndex(ADDRESS, 1, boolCell)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 6, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return readTransform2D(ret)
        }
    }

    /**
     * Calls [methodBind] with one bool arg, one scalar float arg, and no return value.
     */
    fun ptrcallWithBoolAndDoubleArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        boolArg: Boolean,
        doubleArg: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val boolCell = arena.allocate(JAVA_BYTE)
            val doubleCell = arena.allocate(JAVA_DOUBLE)
            boolCell.set(JAVA_BYTE, 0, if (boolArg) 1.toByte() else 0.toByte())
            doubleCell.set(JAVA_DOUBLE, 0, doubleArg)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, boolCell)
            arr.setAtIndex(ADDRESS, 1, doubleCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithBoolAndIntArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        boolArg: Boolean,
        intArg: Int,
    ) {
        Arena.ofConfined().use { arena ->
            val boolCell = arena.allocate(JAVA_BYTE)
            val intCell = arena.allocate(JAVA_INT)
            boolCell.set(JAVA_BYTE, 0, if (boolArg) 1.toByte() else 0.toByte())
            intCell.set(JAVA_INT, 0, intArg)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, boolCell)
            arr.setAtIndex(ADDRESS, 1, intCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithBoolAndIntArgsRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        boolArg: Boolean,
        intArg: Int,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val boolCell = arena.allocate(JAVA_BYTE)
            val intCell = arena.allocate(JAVA_INT)
            boolCell.set(JAVA_BYTE, 0, if (boolArg) 1.toByte() else 0.toByte())
            intCell.set(JAVA_INT, 0, intArg)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, boolCell)
            arr.setAtIndex(ADDRESS, 1, intCell)
            val ret = arena.allocate(JAVA_BYTE)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_BYTE, 0) != 0.toByte()
        }
    }

    fun ptrcallWithBoolAndIntArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        boolArg: Boolean,
        intArg: Int,
    ): Long {
        Arena.ofConfined().use { arena ->
            val boolCell = arena.allocate(JAVA_BYTE)
            val intCell = arena.allocate(JAVA_INT)
            boolCell.set(JAVA_BYTE, 0, if (boolArg) 1.toByte() else 0.toByte())
            intCell.set(JAVA_INT, 0, intArg)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, boolCell)
            arr.setAtIndex(ADDRESS, 1, intCell)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_LONG, 0)
        }
    }

    /**
     * Calls [methodBind] with one bool arg, one scalar float arg, and decodes PackedByteArray return.
     */
    fun ptrcallWithBoolAndDoubleArgRetByteArray(
        methodBind: MemorySegment,
        instance: MemorySegment,
        boolArg: Boolean,
        doubleArg: Double,
    ): ByteArray {
        Arena.ofConfined().use { arena ->
            val boolCell = arena.allocate(JAVA_BYTE)
            val doubleCell = arena.allocate(JAVA_DOUBLE)
            boolCell.set(JAVA_BYTE, 0, if (boolArg) 1.toByte() else 0.toByte())
            doubleCell.set(JAVA_DOUBLE, 0, doubleArg)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, boolCell)
            arr.setAtIndex(ADDRESS, 1, doubleCell)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return try {
                BuiltinTypes.readPackedByteArray(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, ret)
            }
        }
    }

    fun ptrcallWithTwoBoolArgsRetVector2iList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Boolean,
        second: Boolean,
    ): List<Vector2i> {
        Arena.ofConfined().use { arena ->
            val bool0 = arena.allocate(JAVA_BYTE)
            val bool1 = arena.allocate(JAVA_BYTE)
            bool0.set(JAVA_BYTE, 0, if (first) 1.toByte() else 0.toByte())
            bool1.set(JAVA_BYTE, 0, if (second) 1.toByte() else 0.toByte())
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, bool0)
            arr.setAtIndex(ADDRESS, 1, bool1)
            return callArrayReturn(methodBind, instance, arr, BuiltinTypes::readArrayVector2i)
        }
    }

    /**
     * Calls [methodBind] with (int64, int64) and bool return value.
     */
    fun ptrcallWithTwoLongArgsRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Long,
        second: Long,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_LONG)
            val arg1 = arena.allocate(JAVA_LONG)
            arg0.set(JAVA_LONG, 0, first)
            arg1.set(JAVA_LONG, 0, second)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            val ret = arena.allocate(java.lang.foreign.ValueLayout.JAVA_BYTE)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(java.lang.foreign.ValueLayout.JAVA_BYTE, 0) != 0.toByte()
        }
    }

    fun ptrcallWithTwoLongArgsRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Long,
        second: Long,
    ): Int {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_LONG)
            val arg1 = arena.allocate(JAVA_LONG)
            arg0.set(JAVA_LONG, 0, first)
            arg1.set(JAVA_LONG, 0, second)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            val ret = arena.allocate(JAVA_INT)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_INT, 0)
        }
    }

    fun ptrcallWithTwoLongArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Long,
        second: Long,
    ): Long {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_LONG)
            val arg1 = arena.allocate(JAVA_LONG)
            arg0.set(JAVA_LONG, 0, first)
            arg1.set(JAVA_LONG, 0, second)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_LONG, 0)
        }
    }

    fun ptrcallWithTwoLongArgsRetVector2(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Long,
        second: Long,
    ): Vector2 {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_LONG)
            val arg1 = arena.allocate(JAVA_LONG)
            arg0.set(JAVA_LONG, 0, first)
            arg1.set(JAVA_LONG, 0, second)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return Vector2(GodotReal.readIndex(ret, 0), GodotReal.readIndex(ret, 1))
        }
    }

    fun ptrcallWithTwoBoolArgsRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Boolean,
        second: Boolean,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(JAVA_BYTE)
            val secondCell = arena.allocate(JAVA_BYTE)
            firstCell.set(JAVA_BYTE, 0, if (first) 1.toByte() else 0.toByte())
            secondCell.set(JAVA_BYTE, 0, if (second) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, firstCell)
            args.setAtIndex(ADDRESS, 1, secondCell)
            val ret = arena.allocate(ADDRESS)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(ADDRESS, 0)
        }
    }

    fun ptrcallWithTwoLongArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Long,
        second: Long,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_LONG)
            val arg1 = arena.allocate(JAVA_LONG)
            arg0.set(JAVA_LONG, 0, first)
            arg1.set(JAVA_LONG, 0, second)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithThreeLongArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Long,
        second: Long,
        third: Long,
    ): Long {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_LONG)
            val arg1 = arena.allocate(JAVA_LONG)
            val arg2 = arena.allocate(JAVA_LONG)
            arg0.set(JAVA_LONG, 0, first)
            arg1.set(JAVA_LONG, 0, second)
            arg2.set(JAVA_LONG, 0, third)
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_LONG, 0)
        }
    }

    fun ptrcallWithTwoLongAndBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Long,
        second: Long,
        enabled: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_LONG)
            val arg1 = arena.allocate(JAVA_LONG)
            val arg2 = arena.allocate(JAVA_BYTE)
            arg0.set(JAVA_LONG, 0, first)
            arg1.set(JAVA_LONG, 0, second)
            arg2.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithTwoLongAndIntArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Long,
        second: Long,
        third: Int,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_LONG)
            val arg1 = arena.allocate(JAVA_LONG)
            val arg2 = arena.allocate(JAVA_INT)
            arg0.set(JAVA_LONG, 0, first)
            arg1.set(JAVA_LONG, 0, second)
            arg2.set(JAVA_INT, 0, third)
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithTwoLongAndBoolArgsRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Long,
        second: Long,
        enabled: Boolean,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_LONG)
            val arg1 = arena.allocate(JAVA_LONG)
            val arg2 = arena.allocate(JAVA_BYTE)
            arg0.set(JAVA_LONG, 0, first)
            arg1.set(JAVA_LONG, 0, second)
            arg2.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            val ret = arena.allocate(JAVA_BYTE)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_BYTE, 0) != 0.toByte()
        }
    }

    fun ptrcallWithTwoLongAndBoolArgsRetPackedInt64List(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Long,
        second: Long,
        enabled: Boolean,
    ): List<Long> {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_LONG)
            val arg1 = arena.allocate(JAVA_LONG)
            val arg2 = arena.allocate(JAVA_BYTE)
            arg0.set(JAVA_LONG, 0, first)
            arg1.set(JAVA_LONG, 0, second)
            arg2.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return try {
                BuiltinTypes.readPackedInt64Array(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_INT64_ARRAY, ret)
            }
        }
    }

    fun ptrcallWithTwoLongAndBoolArgsRetPackedVector2List(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Long,
        second: Long,
        enabled: Boolean,
    ): List<Vector2> {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_LONG)
            val arg1 = arena.allocate(JAVA_LONG)
            val arg2 = arena.allocate(JAVA_BYTE)
            arg0.set(JAVA_LONG, 0, first)
            arg1.set(JAVA_LONG, 0, second)
            arg2.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return try {
                BuiltinTypes.readPackedVector2Array(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR2_ARRAY, ret)
            }
        }
    }

    fun ptrcallWithTwoLongAndBoolArgsRetPackedVector3List(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Long,
        second: Long,
        enabled: Boolean,
    ): List<Vector3> {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_LONG)
            val arg1 = arena.allocate(JAVA_LONG)
            val arg2 = arena.allocate(JAVA_BYTE)
            arg0.set(JAVA_LONG, 0, first)
            arg1.set(JAVA_LONG, 0, second)
            arg2.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return try {
                BuiltinTypes.readPackedVector3Array(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR3_ARRAY, ret)
            }
        }
    }

    /**
     * Calls [methodBind] with (int32, int64) and bool return value.
     */
    fun ptrcallWithIntAndLongArgsRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Long,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_LONG)
            arg0.set(JAVA_INT, 0, first)
            arg1.set(JAVA_LONG, 0, second)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            val ret = arena.allocate(JAVA_BYTE)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_BYTE, 0) != 0.toByte()
        }
    }

    /**
     * Calls [methodBind] with (int64, int64) and float(double) return value.
     */
    fun ptrcallWithTwoLongArgsRetDouble(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Long,
        second: Long,
    ): Double {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_LONG)
            val arg1 = arena.allocate(JAVA_LONG)
            arg0.set(JAVA_LONG, 0, first)
            arg1.set(JAVA_LONG, 0, second)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            val ret = arena.allocate(java.lang.foreign.ValueLayout.JAVA_DOUBLE)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(java.lang.foreign.ValueLayout.JAVA_DOUBLE, 0)
        }
    }

    fun ptrcallWithTwoLongArgsRetQuaternion(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Long,
        second: Long,
    ): Quaternion {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_LONG)
            val arg1 = arena.allocate(JAVA_LONG)
            arg0.set(JAVA_LONG, 0, first)
            arg1.set(JAVA_LONG, 0, second)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return Quaternion(
                x = GodotReal.readIndex(ret, 0),
                y = GodotReal.readIndex(ret, 1),
                z = GodotReal.readIndex(ret, 2),
                w = GodotReal.readIndex(ret, 3),
            )
        }
    }

    /**
     * Calls [methodBind] with two int64 args and Vector3 return value.
     */
    fun ptrcallWithTwoLongArgsRetVector3(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Long,
        second: Long,
    ): Vector3 {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_LONG)
            val arg1 = arena.allocate(JAVA_LONG)
            arg0.set(JAVA_LONG, 0, first)
            arg1.set(JAVA_LONG, 0, second)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return Vector3(
                x = GodotReal.readIndex(ret, 0),
                y = GodotReal.readIndex(ret, 1),
                z = GodotReal.readIndex(ret, 2),
            )
        }
    }

    /**
     * Calls [methodBind] with (int32, int64) and float(double) return value.
     */
    fun ptrcallWithIntAndLongArgsRetDouble(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Long,
    ): Double {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_LONG)
            arg0.set(JAVA_INT, 0, first)
            arg1.set(JAVA_LONG, 0, second)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            val ret = arena.allocate(JAVA_DOUBLE)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_DOUBLE, 0)
        }
    }

    /**
     * Calls [methodBind] with (int64, float(double)) and no return value.
     */
    fun ptrcallWithLongAndDoubleArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        longArg: Long,
        doubleArg: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_LONG)
            val arg1 = arena.allocate(java.lang.foreign.ValueLayout.JAVA_DOUBLE)
            arg0.set(JAVA_LONG, 0, longArg)
            arg1.set(java.lang.foreign.ValueLayout.JAVA_DOUBLE, 0, doubleArg)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (int64, int64, scalar float) args and no return value.
     */
    fun ptrcallWithTwoLongAndDoubleArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Long,
        second: Long,
        doubleArg: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_LONG)
            val arg1 = arena.allocate(JAVA_LONG)
            val arg2 = arena.allocate(JAVA_DOUBLE)
            arg0.set(JAVA_LONG, 0, first)
            arg1.set(JAVA_LONG, 0, second)
            arg2.set(JAVA_DOUBLE, 0, doubleArg)
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (int32, float(double)) and no return value.
     */
    fun ptrcallWithIntAndDoubleArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        intArg: Int,
        doubleArg: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_DOUBLE)
            arg0.set(JAVA_INT, 0, intArg)
            arg1.set(JAVA_DOUBLE, 0, doubleArg)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (int32, scalar float) args and scalar float return value.
     */
    fun ptrcallWithIntAndDoubleArgRetDouble(
        methodBind: MemorySegment,
        instance: MemorySegment,
        intArg: Int,
        doubleArg: Double,
    ): Double {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_DOUBLE)
            arg0.set(JAVA_INT, 0, intArg)
            arg1.set(JAVA_DOUBLE, 0, doubleArg)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            val ret = arena.allocate(JAVA_DOUBLE)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_DOUBLE, 0)
        }
    }

    fun ptrcallWithIntAndDoubleArgRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        intArg: Int,
        doubleArg: Double,
    ): Int {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_DOUBLE)
            arg0.set(JAVA_INT, 0, intArg)
            arg1.set(JAVA_DOUBLE, 0, doubleArg)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            val ret = arena.allocate(JAVA_INT)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_INT, 0)
        }
    }

    fun ptrcallWithIntAndDoubleArgRetVector2(
        methodBind: MemorySegment,
        instance: MemorySegment,
        intArg: Int,
        doubleArg: Double,
    ): Vector2 {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_DOUBLE)
            arg0.set(JAVA_INT, 0, intArg)
            arg1.set(JAVA_DOUBLE, 0, doubleArg)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return Vector2(
                x = GodotReal.readIndex(ret, 0),
                y = GodotReal.readIndex(ret, 1),
            )
        }
    }

    fun ptrcallWithIntAndDoubleArgRetVector3(
        methodBind: MemorySegment,
        instance: MemorySegment,
        intArg: Int,
        doubleArg: Double,
    ): Vector3 {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_DOUBLE)
            arg0.set(JAVA_INT, 0, intArg)
            arg1.set(JAVA_DOUBLE, 0, doubleArg)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return Vector3(
                x = GodotReal.readIndex(ret, 0),
                y = GodotReal.readIndex(ret, 1),
                z = GodotReal.readIndex(ret, 2),
            )
        }
    }

    /**
     * Calls [methodBind] with (int32, scalar float, scalar float) args and int32 return value.
     */
    fun ptrcallWithIntAndTwoDoubleArgsRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        intArg: Int,
        firstDouble: Double,
        secondDouble: Double,
    ): Int {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_DOUBLE)
            val arg2 = arena.allocate(JAVA_DOUBLE)
            arg0.set(JAVA_INT, 0, intArg)
            arg1.set(JAVA_DOUBLE, 0, firstDouble)
            arg2.set(JAVA_DOUBLE, 0, secondDouble)
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            val ret = arena.allocate(JAVA_INT)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_INT, 0)
        }
    }

    /**
     * Calls [methodBind] with (int32, scalar float, Quaternion) args and int32 return value.
     */
    fun ptrcallWithIntDoubleQuaternionArgsRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        intArg: Int,
        doubleArg: Double,
        quaternion: Quaternion,
    ): Int {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_DOUBLE)
            val arg2 = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
            arg0.set(JAVA_INT, 0, intArg)
            arg1.set(JAVA_DOUBLE, 0, doubleArg)
            GodotReal.writeIndex(arg2, 0, quaternion.x)
            GodotReal.writeIndex(arg2, 1, quaternion.y)
            GodotReal.writeIndex(arg2, 2, quaternion.z)
            GodotReal.writeIndex(arg2, 3, quaternion.w)
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            val ret = arena.allocate(JAVA_INT)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_INT, 0)
        }
    }

    /**
     * Calls [methodBind] with (int32, scalar float, bool) args and scalar float return value.
     */
    fun ptrcallWithIntDoubleBoolArgsRetDouble(
        methodBind: MemorySegment,
        instance: MemorySegment,
        intArg: Int,
        doubleArg: Double,
        boolArg: Boolean,
    ): Double {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_DOUBLE)
            val arg2 = arena.allocate(JAVA_BYTE)
            arg0.set(JAVA_INT, 0, intArg)
            arg1.set(JAVA_DOUBLE, 0, doubleArg)
            arg2.set(JAVA_BYTE, 0, if (boolArg) 1.toByte() else 0.toByte())
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            val ret = arena.allocate(JAVA_DOUBLE)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_DOUBLE, 0)
        }
    }

    /**
     * Calls [methodBind] with (int32, scalar float, bool) args and decodes a Variant return.
     */
    fun ptrcallWithIntDoubleBoolArgsRetVariantScalar(
        methodBind: MemorySegment,
        instance: MemorySegment,
        intArg: Int,
        doubleArg: Double,
        boolArg: Boolean,
    ): Any? {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_DOUBLE)
            val arg2 = arena.allocate(JAVA_BYTE)
            arg0.set(JAVA_INT, 0, intArg)
            arg1.set(JAVA_DOUBLE, 0, doubleArg)
            arg2.set(JAVA_BYTE, 0, if (boolArg) 1.toByte() else 0.toByte())
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            return readVariantReturn(methodBind, instance, arr, arena)
        }
    }

    /**
     * Calls [methodBind] with (int32, scalar float, Variant, scalar float) args and int32 return value.
     */
    fun ptrcallWithIntDoubleVariantDoubleArgsRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        intArg: Int,
        firstDouble: Double,
        variantValue: Any?,
        secondDouble: Double,
    ): Int {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_DOUBLE)
            val variant = initVariantCell(arena, variantValue)
            val arg3 = arena.allocate(JAVA_DOUBLE)
            try {
                arg0.set(JAVA_INT, 0, intArg)
                arg1.set(JAVA_DOUBLE, 0, firstDouble)
                arg3.set(JAVA_DOUBLE, 0, secondDouble)
                val arr = arena.allocate(ADDRESS, 4)
                arr.setAtIndex(ADDRESS, 0, arg0)
                arr.setAtIndex(ADDRESS, 1, arg1)
                arr.setAtIndex(ADDRESS, 2, variant)
                arr.setAtIndex(ADDRESS, 3, arg3)
                val ret = arena.allocate(JAVA_INT)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_INT, 0)
            } finally {
                BuiltinTypes.destroyVariant(variant)
            }
        }
    }

    /**
     * Calls [methodBind] with (int32, scalar float, int64, bool, bool) args and int32 return value.
     */
    fun ptrcallWithIntDoubleLongTwoBoolArgsRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        intArg: Int,
        doubleArg: Double,
        longArg: Long,
        firstBool: Boolean,
        secondBool: Boolean,
    ): Int {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_DOUBLE)
            val arg2 = arena.allocate(JAVA_LONG)
            val arg3 = arena.allocate(JAVA_BYTE)
            val arg4 = arena.allocate(JAVA_BYTE)
            arg0.set(JAVA_INT, 0, intArg)
            arg1.set(JAVA_DOUBLE, 0, doubleArg)
            arg2.set(JAVA_LONG, 0, longArg)
            arg3.set(JAVA_BYTE, 0, if (firstBool) 1.toByte() else 0.toByte())
            arg4.set(JAVA_BYTE, 0, if (secondBool) 1.toByte() else 0.toByte())
            val arr = arena.allocate(ADDRESS, 5)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            arr.setAtIndex(ADDRESS, 3, arg3)
            arr.setAtIndex(ADDRESS, 4, arg4)
            val ret = arena.allocate(JAVA_INT)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_INT, 0)
        }
    }

    /**
     * Calls [methodBind] with (int32, scalar float, scalar float, Vector2, Vector2) args and int32 return value.
     */
    fun ptrcallWithIntTwoDoubleTwoVector2ArgsRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        intArg: Int,
        firstDouble: Double,
        secondDouble: Double,
        firstVector: Vector2,
        secondVector: Vector2,
    ): Int {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_DOUBLE)
            val arg2 = arena.allocate(JAVA_DOUBLE)
            val arg3 = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val arg4 = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            arg0.set(JAVA_INT, 0, intArg)
            arg1.set(JAVA_DOUBLE, 0, firstDouble)
            arg2.set(JAVA_DOUBLE, 0, secondDouble)
            GodotReal.writeIndex(arg3, 0, firstVector.x)
            GodotReal.writeIndex(arg3, 1, firstVector.y)
            GodotReal.writeIndex(arg4, 0, secondVector.x)
            GodotReal.writeIndex(arg4, 1, secondVector.y)
            val arr = arena.allocate(ADDRESS, 5)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            arr.setAtIndex(ADDRESS, 3, arg3)
            arr.setAtIndex(ADDRESS, 4, arg4)
            val ret = arena.allocate(JAVA_INT)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_INT, 0)
        }
    }

    /**
     * Calls [methodBind] with (int32, scalar float, Object*, scalar float, scalar float) args and int32 return value.
     */
    fun ptrcallWithIntDoubleObjectTwoDoubleArgsRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        intArg: Int,
        firstDouble: Double,
        objectArg: MemorySegment,
        secondDouble: Double,
        thirdDouble: Double,
    ): Int {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_DOUBLE)
            val objectCell = arena.allocate(ADDRESS)
            val arg3 = arena.allocate(JAVA_DOUBLE)
            val arg4 = arena.allocate(JAVA_DOUBLE)
            arg0.set(JAVA_INT, 0, intArg)
            arg1.set(JAVA_DOUBLE, 0, firstDouble)
            objectCell.set(ADDRESS, 0, objectArg)
            arg3.set(JAVA_DOUBLE, 0, secondDouble)
            arg4.set(JAVA_DOUBLE, 0, thirdDouble)
            val arr = arena.allocate(ADDRESS, 5)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, objectCell)
            arr.setAtIndex(ADDRESS, 3, arg3)
            arr.setAtIndex(ADDRESS, 4, arg4)
            val ret = arena.allocate(JAVA_INT)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_INT, 0)
        }
    }

    /**
     * Calls [methodBind] with (int32, scalar float, bool) args and Quaternion return value.
     */
    fun ptrcallWithIntDoubleBoolArgsRetQuaternion(
        methodBind: MemorySegment,
        instance: MemorySegment,
        intArg: Int,
        doubleArg: Double,
        boolArg: Boolean,
    ): Quaternion {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_DOUBLE)
            val arg2 = arena.allocate(JAVA_BYTE)
            arg0.set(JAVA_INT, 0, intArg)
            arg1.set(JAVA_DOUBLE, 0, doubleArg)
            arg2.set(JAVA_BYTE, 0, if (boolArg) 1.toByte() else 0.toByte())
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return Quaternion(
                x = GodotReal.readIndex(ret, 0),
                y = GodotReal.readIndex(ret, 1),
                z = GodotReal.readIndex(ret, 2),
                w = GodotReal.readIndex(ret, 3),
            )
        }
    }

    /**
     * Calls [methodBind] with (int32, scalar float, StringName) args and int32 return value.
     */
    fun ptrcallWithIntDoubleStringNameArgsRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        intArg: Int,
        doubleArg: Double,
        name: String,
    ): Int {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_DOUBLE)
            arg0.set(JAVA_INT, 0, intArg)
            arg1.set(JAVA_DOUBLE, 0, doubleArg)
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, GodotStrings.makeStringName(name))
            val ret = arena.allocate(JAVA_INT)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_INT, 0)
        }
    }

    fun ptrcallWithIntDoubleVector3ArgsRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        intArg: Int,
        doubleArg: Double,
        vector: Vector3,
    ): Int {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_DOUBLE)
            val arg2 = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            arg0.set(JAVA_INT, 0, intArg)
            arg1.set(JAVA_DOUBLE, 0, doubleArg)
            GodotReal.writeIndex(arg2, 0, vector.x)
            GodotReal.writeIndex(arg2, 1, vector.y)
            GodotReal.writeIndex(arg2, 2, vector.z)
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            val ret = arena.allocate(JAVA_INT)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_INT, 0)
        }
    }

    fun ptrcallWithIntDoubleBoolArgsRetVector3(
        methodBind: MemorySegment,
        instance: MemorySegment,
        intArg: Int,
        doubleArg: Double,
        boolArg: Boolean,
    ): Vector3 {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_DOUBLE)
            val arg2 = arena.allocate(JAVA_BYTE)
            arg0.set(JAVA_INT, 0, intArg)
            arg1.set(JAVA_DOUBLE, 0, doubleArg)
            arg2.set(JAVA_BYTE, 0, if (boolArg) 1.toByte() else 0.toByte())
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return Vector3(
                GodotReal.readIndex(ret, 0),
                GodotReal.readIndex(ret, 1),
                GodotReal.readIndex(ret, 2),
            )
        }
    }

    fun ptrcallWithIntAndDoubleArgRetPackedVector2List(
        methodBind: MemorySegment,
        instance: MemorySegment,
        intArg: Int,
        doubleArg: Double,
    ): List<Vector2> {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_DOUBLE)
            arg0.set(JAVA_INT, 0, intArg)
            arg1.set(JAVA_DOUBLE, 0, doubleArg)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return try {
                BuiltinTypes.readPackedVector2Array(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR2_ARRAY, ret)
            }
        }
    }

    fun ptrcallWithIntAndDoubleArgRetPackedVector3List(
        methodBind: MemorySegment,
        instance: MemorySegment,
        intArg: Int,
        doubleArg: Double,
    ): List<Vector3> {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_DOUBLE)
            arg0.set(JAVA_INT, 0, intArg)
            arg1.set(JAVA_DOUBLE, 0, doubleArg)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return try {
                BuiltinTypes.readPackedVector3Array(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR3_ARRAY, ret)
            }
        }
    }

    /**
     * Calls [methodBind] with (int32, Object*) args and no return value.
     */
    fun ptrcallWithIntAndObjectArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        intArg: Int,
        objectArg: MemorySegment,
    ) {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 2)
            val intCell = arena.allocate(JAVA_INT)
            intCell.set(JAVA_INT, 0, intArg)
            val objCell = arena.allocate(ADDRESS)
            objCell.set(ADDRESS, 0, objectArg)
            arr.setAtIndex(ADDRESS, 0, intCell)
            arr.setAtIndex(ADDRESS, 1, objCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (uint32, Object*) args and no return value.
     */
    fun ptrcallWithUInt32AndObjectArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        intArg: Long,
        objectArg: MemorySegment,
    ) {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 2)
            val intCell = arena.allocate(JAVA_INT)
            intCell.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(intArg))
            val objCell = arena.allocate(ADDRESS)
            objCell.set(ADDRESS, 0, objectArg)
            arr.setAtIndex(ADDRESS, 0, intCell)
            arr.setAtIndex(ADDRESS, 1, objCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (uint32, float) args and no return value.
     */
    fun ptrcallWithUInt32AndDoubleArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        intArg: Long,
        doubleArg: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val intCell = arena.allocate(JAVA_INT)
            intCell.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(intArg))
            val doubleCell = arena.allocate(JAVA_DOUBLE)
            doubleCell.set(JAVA_DOUBLE, 0, doubleArg)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, intCell)
            arr.setAtIndex(ADDRESS, 1, doubleCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (uint32, Transform2D) args and no return value.
     */
    fun ptrcallWithUInt32AndTransform2DArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        intArg: Long,
        transform: Transform2D,
    ) {
        Arena.ofConfined().use { arena ->
            val intCell = arena.allocate(JAVA_INT)
            intCell.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(intArg))
            val transformCell = arena.allocate(GodotReal.SIZE_BYTES * 6, GodotReal.ALIGN_BYTES)
            writeTransform2D(transformCell, transform)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, intCell)
            arr.setAtIndex(ADDRESS, 1, transformCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (uint32, Transform3D) args and no return value.
     */
    fun ptrcallWithUInt32AndTransform3DArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        intArg: Long,
        transform: Transform3D,
    ) {
        Arena.ofConfined().use { arena ->
            val intCell = arena.allocate(JAVA_INT)
            intCell.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(intArg))
            val transformCell = arena.allocate(GodotReal.SIZE_BYTES * 12, GodotReal.ALIGN_BYTES)
            writeTransform3D(transformCell, transform)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, intCell)
            arr.setAtIndex(ADDRESS, 1, transformCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (int32, Transform3D) args and no return value.
     */
    fun ptrcallWithIntAndTransform3DArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        intArg: Int,
        transformArg: Transform3D,
    ) {
        Arena.ofConfined().use { arena ->
            val intCell = arena.allocate(JAVA_INT)
            intCell.set(JAVA_INT, 0, intArg)
            val transform = arena.allocate(GodotReal.SIZE_BYTES * 12, GodotReal.ALIGN_BYTES)
            GodotReal.writeIndex(transform, 0, transformArg.basis.x.x)
            GodotReal.writeIndex(transform, 1, transformArg.basis.y.x)
            GodotReal.writeIndex(transform, 2, transformArg.basis.z.x)
            GodotReal.writeIndex(transform, 3, transformArg.basis.x.y)
            GodotReal.writeIndex(transform, 4, transformArg.basis.y.y)
            GodotReal.writeIndex(transform, 5, transformArg.basis.z.y)
            GodotReal.writeIndex(transform, 6, transformArg.basis.x.z)
            GodotReal.writeIndex(transform, 7, transformArg.basis.y.z)
            GodotReal.writeIndex(transform, 8, transformArg.basis.z.z)
            GodotReal.writeIndex(transform, 9, transformArg.origin.x)
            GodotReal.writeIndex(transform, 10, transformArg.origin.y)
            GodotReal.writeIndex(transform, 11, transformArg.origin.z)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, intCell)
            arr.setAtIndex(ADDRESS, 1, transform)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with two int64 args and StringName return value.
     */
    fun ptrcallWithTwoLongArgsRetStringName(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Long,
        second: Long,
    ): String {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_LONG)
            val arg1 = arena.allocate(JAVA_LONG)
            arg0.set(JAVA_LONG, 0, first)
            arg1.set(JAVA_LONG, 0, second)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            val ret = arena.allocate(8L, 8L)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return try {
                GodotStrings.readStringName(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.STRING_NAME, ret)
            }
        }
    }

    /**
     * Calls [methodBind] with two int64 args and Variant return value.
     */
    fun ptrcallWithTwoLongArgsRetVariantScalar(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Long,
        second: Long,
    ): Any? {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_LONG)
            val arg1 = arena.allocate(JAVA_LONG)
            arg0.set(JAVA_LONG, 0, first)
            arg1.set(JAVA_LONG, 0, second)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            val retVariant = arena.allocate(BuiltinTypes.VARIANT_SIZE, 8L)
            try {
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, retVariant)
                return BuiltinTypes.readVariantScalar(retVariant, arena)
            } finally {
                BuiltinTypes.destroyVariant(retVariant)
            }
        }
    }

    /**
     * Calls [methodBind] with (int64, float, float, float) and no return value.
     */
    fun ptrcallWithLongAndThreeDoubleArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        longArg: Long,
        a: Double,
        b: Double,
        c: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_LONG)
            val arg1 = arena.allocate(java.lang.foreign.ValueLayout.JAVA_DOUBLE)
            val arg2 = arena.allocate(java.lang.foreign.ValueLayout.JAVA_DOUBLE)
            val arg3 = arena.allocate(java.lang.foreign.ValueLayout.JAVA_DOUBLE)
            arg0.set(JAVA_LONG, 0, longArg)
            arg1.set(java.lang.foreign.ValueLayout.JAVA_DOUBLE, 0, a)
            arg2.set(java.lang.foreign.ValueLayout.JAVA_DOUBLE, 0, b)
            arg3.set(java.lang.foreign.ValueLayout.JAVA_DOUBLE, 0, c)
            val arr = arena.allocate(ADDRESS, 4)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            arr.setAtIndex(ADDRESS, 3, arg3)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (int32, float, float, float) and no return value.
     */
    fun ptrcallWithIntAndThreeDoubleArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        intArg: Int,
        a: Double,
        b: Double,
        c: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_DOUBLE)
            val arg2 = arena.allocate(JAVA_DOUBLE)
            val arg3 = arena.allocate(JAVA_DOUBLE)
            arg0.set(JAVA_INT, 0, intArg)
            arg1.set(JAVA_DOUBLE, 0, a)
            arg2.set(JAVA_DOUBLE, 0, b)
            arg3.set(JAVA_DOUBLE, 0, c)
            val arr = arena.allocate(ADDRESS, 4)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            arr.setAtIndex(ADDRESS, 3, arg3)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (float, bool, bool, bool) and Object return value.
     */
    fun ptrcallWithDoubleAndThreeBoolArgsRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Double,
        first: Boolean,
        second: Boolean,
        third: Boolean,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 4)
            val doubleCell = arena.allocate(java.lang.foreign.ValueLayout.JAVA_DOUBLE)
            doubleCell.set(java.lang.foreign.ValueLayout.JAVA_DOUBLE, 0, value)
            val bool0 = arena.allocate(java.lang.foreign.ValueLayout.JAVA_BYTE)
            val bool1 = arena.allocate(java.lang.foreign.ValueLayout.JAVA_BYTE)
            val bool2 = arena.allocate(java.lang.foreign.ValueLayout.JAVA_BYTE)
            bool0.set(java.lang.foreign.ValueLayout.JAVA_BYTE, 0, if (first) 1.toByte() else 0.toByte())
            bool1.set(java.lang.foreign.ValueLayout.JAVA_BYTE, 0, if (second) 1.toByte() else 0.toByte())
            bool2.set(java.lang.foreign.ValueLayout.JAVA_BYTE, 0, if (third) 1.toByte() else 0.toByte())
            arr.setAtIndex(ADDRESS, 0, doubleCell)
            arr.setAtIndex(ADDRESS, 1, bool0)
            arr.setAtIndex(ADDRESS, 2, bool1)
            arr.setAtIndex(ADDRESS, 3, bool2)
            val ret = arena.allocate(ADDRESS)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(ADDRESS, 0)
        }
    }

    /**
     * Calls [methodBind] with one bool arg and String return value.
     */
    fun ptrcallWithBoolArgRetString(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Boolean,
    ): String {
        Arena.ofConfined().use { arena ->
            val boolCell = arena.allocate(java.lang.foreign.ValueLayout.JAVA_BYTE)
            boolCell.set(java.lang.foreign.ValueLayout.JAVA_BYTE, 0, if (value) 1.toByte() else 0.toByte())
            val retString = arena.allocate(8L, 8L)
            try {
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, boolCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, retString)
                return GodotStrings.readString(retString)
            } finally {
                GodotStrings.destroyString(retString)
            }
        }
    }

    /**
     * Calls [methodBind] with one StringName arg and no return value.
     */
    fun ptrcallWithStringNameArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
    ) {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(name))
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithStringNameArgRetString(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
    ): String {
        Arena.ofConfined().use { arena ->
            val ret = arena.allocate(8L, 8L)
            try {
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(name))
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return GodotStrings.readString(ret)
            } finally {
                GodotStrings.destroyString(ret)
            }
        }
    }

    /**
     * Calls [methodBind] with (StringName, float, float, bool) args.
     */
    fun ptrcallWithStringNameDoubleDoubleBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
        firstFloat: Double,
        secondFloat: Double,
        boolArg: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 4)
            arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(name))

            val firstCell = arena.allocate(java.lang.foreign.ValueLayout.JAVA_DOUBLE)
            firstCell.set(java.lang.foreign.ValueLayout.JAVA_DOUBLE, 0, firstFloat)
            arr.setAtIndex(ADDRESS, 1, firstCell)

            val secondCell = arena.allocate(java.lang.foreign.ValueLayout.JAVA_DOUBLE)
            secondCell.set(java.lang.foreign.ValueLayout.JAVA_DOUBLE, 0, secondFloat)
            arr.setAtIndex(ADDRESS, 2, secondCell)

            val boolCell = arena.allocate(java.lang.foreign.ValueLayout.JAVA_BYTE)
            boolCell.set(java.lang.foreign.ValueLayout.JAVA_BYTE, 0, if (boolArg) 1.toByte() else 0.toByte())
            arr.setAtIndex(ADDRESS, 3, boolCell)

            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (StringName, scalar float, scalar float, bool, bool, scalar float, int64).
     */
    fun ptrcallWithStringNameTwoDoubleTwoBoolDoubleLongArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
        firstDouble: Double,
        secondDouble: Double,
        firstBool: Boolean,
        secondBool: Boolean,
        thirdDouble: Double,
        longArg: Long,
    ) {
        Arena.ofConfined().use { arena ->
            val double0 = arena.allocate(JAVA_DOUBLE)
            val double1 = arena.allocate(JAVA_DOUBLE)
            val bool0 = arena.allocate(JAVA_BYTE)
            val bool1 = arena.allocate(JAVA_BYTE)
            val double2 = arena.allocate(JAVA_DOUBLE)
            val longCell = arena.allocate(JAVA_LONG)
            double0.set(JAVA_DOUBLE, 0, firstDouble)
            double1.set(JAVA_DOUBLE, 0, secondDouble)
            bool0.set(JAVA_BYTE, 0, if (firstBool) 1.toByte() else 0.toByte())
            bool1.set(JAVA_BYTE, 0, if (secondBool) 1.toByte() else 0.toByte())
            double2.set(JAVA_DOUBLE, 0, thirdDouble)
            longCell.set(JAVA_LONG, 0, longArg)
            val args = arena.allocate(ADDRESS, 7)
            args.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(name))
            args.setAtIndex(ADDRESS, 1, double0)
            args.setAtIndex(ADDRESS, 2, double1)
            args.setAtIndex(ADDRESS, 3, bool0)
            args.setAtIndex(ADDRESS, 4, bool1)
            args.setAtIndex(ADDRESS, 5, double2)
            args.setAtIndex(ADDRESS, 6, longCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (StringName, Object*, scalar float, bool, bool, scalar float, int64, bool, bool).
     */
    fun ptrcallWithStringNameObjectDoubleTwoBoolDoubleLongTwoBoolArgsRetDouble(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
        objectArg: MemorySegment,
        firstDouble: Double,
        firstBool: Boolean,
        secondBool: Boolean,
        secondDouble: Double,
        longArg: Long,
        thirdBool: Boolean,
        fourthBool: Boolean,
    ): Double {
        Arena.ofConfined().use { arena ->
            val objectCell = arena.allocate(ADDRESS)
            val double0 = arena.allocate(JAVA_DOUBLE)
            val bool0 = arena.allocate(JAVA_BYTE)
            val bool1 = arena.allocate(JAVA_BYTE)
            val double1 = arena.allocate(JAVA_DOUBLE)
            val longCell = arena.allocate(JAVA_LONG)
            val bool2 = arena.allocate(JAVA_BYTE)
            val bool3 = arena.allocate(JAVA_BYTE)
            objectCell.set(ADDRESS, 0, objectArg)
            double0.set(JAVA_DOUBLE, 0, firstDouble)
            bool0.set(JAVA_BYTE, 0, if (firstBool) 1.toByte() else 0.toByte())
            bool1.set(JAVA_BYTE, 0, if (secondBool) 1.toByte() else 0.toByte())
            double1.set(JAVA_DOUBLE, 0, secondDouble)
            longCell.set(JAVA_LONG, 0, longArg)
            bool2.set(JAVA_BYTE, 0, if (thirdBool) 1.toByte() else 0.toByte())
            bool3.set(JAVA_BYTE, 0, if (fourthBool) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 9)
            args.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(name))
            args.setAtIndex(ADDRESS, 1, objectCell)
            args.setAtIndex(ADDRESS, 2, double0)
            args.setAtIndex(ADDRESS, 3, bool0)
            args.setAtIndex(ADDRESS, 4, bool1)
            args.setAtIndex(ADDRESS, 5, double1)
            args.setAtIndex(ADDRESS, 6, longCell)
            args.setAtIndex(ADDRESS, 7, bool2)
            args.setAtIndex(ADDRESS, 8, bool3)
            val ret = arena.allocate(JAVA_DOUBLE)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(JAVA_DOUBLE, 0)
        }
    }

    /**
     * Calls [methodBind] with (StringName, float, bool) args.
     */
    fun ptrcallWithStringNameDoubleBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
        floatArg: Double,
        boolArg: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(name))

            val floatCell = arena.allocate(java.lang.foreign.ValueLayout.JAVA_DOUBLE)
            floatCell.set(java.lang.foreign.ValueLayout.JAVA_DOUBLE, 0, floatArg)
            arr.setAtIndex(ADDRESS, 1, floatCell)

            val boolCell = arena.allocate(java.lang.foreign.ValueLayout.JAVA_BYTE)
            boolCell.set(java.lang.foreign.ValueLayout.JAVA_BYTE, 0, if (boolArg) 1.toByte() else 0.toByte())
            arr.setAtIndex(ADDRESS, 2, boolCell)

            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (StringName, float) args.
     */
    fun ptrcallWithStringNameAndDoubleArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
        floatArg: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(name))
            val floatCell = arena.allocate(java.lang.foreign.ValueLayout.JAVA_DOUBLE)
            floatCell.set(java.lang.foreign.ValueLayout.JAVA_DOUBLE, 0, floatArg)
            arr.setAtIndex(ADDRESS, 1, floatCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithStringNameAndColorArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
        color: Color,
    ) {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(name))
            val colorCell = arena.allocate(16L, 4L)
            colorCell.set(JAVA_FLOAT, 0, color.r)
            colorCell.set(JAVA_FLOAT, 4, color.g)
            colorCell.set(JAVA_FLOAT, 8, color.b)
            colorCell.set(JAVA_FLOAT, 12, color.a)
            arr.setAtIndex(ADDRESS, 1, colorCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithStringNameAndVector2Arg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
        value: Vector2,
    ) {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(name))
            val vec = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            GodotReal.writeIndex(vec, 0, value.x)
            GodotReal.writeIndex(vec, 1, value.y)
            arr.setAtIndex(ADDRESS, 1, vec)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (float, bool, bool) args.
     */
    fun ptrcallWithDoubleBoolBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        floatArg: Double,
        firstBool: Boolean,
        secondBool: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 3)
            val floatCell = arena.allocate(java.lang.foreign.ValueLayout.JAVA_DOUBLE)
            floatCell.set(java.lang.foreign.ValueLayout.JAVA_DOUBLE, 0, floatArg)
            arr.setAtIndex(ADDRESS, 0, floatCell)

            val firstBoolCell = arena.allocate(java.lang.foreign.ValueLayout.JAVA_BYTE)
            firstBoolCell.set(java.lang.foreign.ValueLayout.JAVA_BYTE, 0, if (firstBool) 1.toByte() else 0.toByte())
            arr.setAtIndex(ADDRESS, 1, firstBoolCell)

            val secondBoolCell = arena.allocate(java.lang.foreign.ValueLayout.JAVA_BYTE)
            secondBoolCell.set(java.lang.foreign.ValueLayout.JAVA_BYTE, 0, if (secondBool) 1.toByte() else 0.toByte())
            arr.setAtIndex(ADDRESS, 2, secondBoolCell)

            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with two StringName args and no return value.
     */
    fun ptrcallWithTwoStringNameArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: String,
        second: String,
    ) {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(first))
            arr.setAtIndex(ADDRESS, 1, GodotStrings.makeStringName(second))
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithTwoStringNameArgsRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: String,
        second: String,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(first))
            arr.setAtIndex(ADDRESS, 1, GodotStrings.makeStringName(second))
            val ret = arena.allocate(JAVA_BYTE)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_BYTE, 0) != 0.toByte()
        }
    }

    fun ptrcallWithTwoStringNameArgsRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: String,
        second: String,
    ): Int {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(first))
            arr.setAtIndex(ADDRESS, 1, GodotStrings.makeStringName(second))
            val ret = arena.allocate(JAVA_INT)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_INT, 0)
        }
    }

    fun ptrcallWithTwoStringNameArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: String,
        second: String,
    ): Long {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(first))
            arr.setAtIndex(ADDRESS, 1, GodotStrings.makeStringName(second))
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_LONG, 0)
        }
    }

    fun ptrcallWithTwoStringNameAndIntArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: String,
        second: String,
        value: Int,
    ) {
        Arena.ofConfined().use { arena ->
            val intCell = arena.allocate(JAVA_INT)
            intCell.set(JAVA_INT, 0, value)
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(first))
            arr.setAtIndex(ADDRESS, 1, GodotStrings.makeStringName(second))
            arr.setAtIndex(ADDRESS, 2, intCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithTwoStringNameArgsRetStringName(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: String,
        second: String,
    ): String {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(first))
            arr.setAtIndex(ADDRESS, 1, GodotStrings.makeStringName(second))
            val ret = arena.allocate(8L, 8L)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return try {
                GodotStrings.readStringName(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.STRING_NAME, ret)
            }
        }
    }

    fun ptrcallWithTwoStringNameArgsRetRID(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: String,
        second: String,
    ): RID {
        Arena.ofConfined().use { arena ->
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(first))
            args.setAtIndex(ADDRESS, 1, GodotStrings.makeStringName(second))
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return RID(ret.get(JAVA_LONG, 0))
        }
    }

    fun ptrcallWithTwoStringNameAndBoolArgsRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: String,
        second: String,
        enabled: Boolean,
    ): Int {
        Arena.ofConfined().use { arena ->
            val boolCell = arena.allocate(JAVA_BYTE)
            boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(first))
            arr.setAtIndex(ADDRESS, 1, GodotStrings.makeStringName(second))
            arr.setAtIndex(ADDRESS, 2, boolCell)
            val ret = arena.allocate(JAVA_INT)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_INT, 0)
        }
    }

    fun ptrcallWithTwoStringNameAndBoolArgsRetStringName(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: String,
        second: String,
        enabled: Boolean,
    ): String {
        Arena.ofConfined().use { arena ->
            val boolCell = arena.allocate(JAVA_BYTE)
            boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(first))
            arr.setAtIndex(ADDRESS, 1, GodotStrings.makeStringName(second))
            arr.setAtIndex(ADDRESS, 2, boolCell)
            val ret = arena.allocate(8L, 8L)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return try {
                GodotStrings.readStringName(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.STRING_NAME, ret)
            }
        }
    }

    fun ptrcallWithTwoStringNameArgsRetString(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: String,
        second: String,
    ): String {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 2)
            val retString = arena.allocate(8L, 8L)
            try {
                arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(first))
                arr.setAtIndex(ADDRESS, 1, GodotStrings.makeStringName(second))
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, retString)
                return GodotStrings.readString(retString)
            } finally {
                GodotStrings.destroyString(retString)
            }
        }
    }

    fun ptrcallWithTwoStringNameIntStringNameArgsRetStringName(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: String,
        second: String,
        intValue: Int,
        third: String,
    ): String {
        Arena.ofConfined().use { arena ->
            val intCell = arena.allocate(JAVA_INT)
            intCell.set(JAVA_INT, 0, intValue)
            val arr = arena.allocate(ADDRESS, 4)
            arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(first))
            arr.setAtIndex(ADDRESS, 1, GodotStrings.makeStringName(second))
            arr.setAtIndex(ADDRESS, 2, intCell)
            arr.setAtIndex(ADDRESS, 3, GodotStrings.makeStringName(third))
            val ret = arena.allocate(8L, 8L)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return try {
                GodotStrings.readStringName(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.STRING_NAME, ret)
            }
        }
    }

    fun ptrcallWithThreeStringNameArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: String,
        second: String,
        third: String,
    ) {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(first))
            arr.setAtIndex(ADDRESS, 1, GodotStrings.makeStringName(second))
            arr.setAtIndex(ADDRESS, 2, GodotStrings.makeStringName(third))
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithStringNameIntAndStringNameArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: String,
        value: Int,
        second: String,
    ) {
        Arena.ofConfined().use { arena ->
            val intCell = arena.allocate(JAVA_INT)
            intCell.set(JAVA_INT, 0, value)
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(first))
            arr.setAtIndex(ADDRESS, 1, intCell)
            arr.setAtIndex(ADDRESS, 2, GodotStrings.makeStringName(second))
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithStringNameIntStringNameIntArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: String,
        firstValue: Int,
        second: String,
        secondValue: Int,
    ) {
        Arena.ofConfined().use { arena ->
            val firstIntCell = arena.allocate(JAVA_INT)
            val secondIntCell = arena.allocate(JAVA_INT)
            firstIntCell.set(JAVA_INT, 0, firstValue)
            secondIntCell.set(JAVA_INT, 0, secondValue)
            val arr = arena.allocate(ADDRESS, 4)
            arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(first))
            arr.setAtIndex(ADDRESS, 1, firstIntCell)
            arr.setAtIndex(ADDRESS, 2, GodotStrings.makeStringName(second))
            arr.setAtIndex(ADDRESS, 3, secondIntCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithStringNameIntStringNameIntArgsRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: String,
        firstValue: Int,
        second: String,
        secondValue: Int,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val firstIntCell = arena.allocate(JAVA_INT)
            val secondIntCell = arena.allocate(JAVA_INT)
            firstIntCell.set(JAVA_INT, 0, firstValue)
            secondIntCell.set(JAVA_INT, 0, secondValue)
            val arr = arena.allocate(ADDRESS, 4)
            arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(first))
            arr.setAtIndex(ADDRESS, 1, firstIntCell)
            arr.setAtIndex(ADDRESS, 2, GodotStrings.makeStringName(second))
            arr.setAtIndex(ADDRESS, 3, secondIntCell)
            val ret = arena.allocate(JAVA_BYTE)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_BYTE, 0) != 0.toByte()
        }
    }

    fun ptrcallWithStringNameIntStringNameIntBoolArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: String,
        firstValue: Int,
        second: String,
        secondValue: Int,
        enabled: Boolean,
    ): Long {
        Arena.ofConfined().use { arena ->
            val firstIntCell = arena.allocate(JAVA_INT)
            val secondIntCell = arena.allocate(JAVA_INT)
            val boolCell = arena.allocate(JAVA_BYTE)
            firstIntCell.set(JAVA_INT, 0, firstValue)
            secondIntCell.set(JAVA_INT, 0, secondValue)
            boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
            val arr = arena.allocate(ADDRESS, 5)
            arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(first))
            arr.setAtIndex(ADDRESS, 1, firstIntCell)
            arr.setAtIndex(ADDRESS, 2, GodotStrings.makeStringName(second))
            arr.setAtIndex(ADDRESS, 3, secondIntCell)
            arr.setAtIndex(ADDRESS, 4, boolCell)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_LONG, 0)
        }
    }

    fun ptrcallWithStringNameIntStringNameIntDoubleArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: String,
        firstValue: Int,
        second: String,
        secondValue: Int,
        value: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val firstIntCell = arena.allocate(JAVA_INT)
            val secondIntCell = arena.allocate(JAVA_INT)
            val doubleCell = arena.allocate(JAVA_DOUBLE)
            firstIntCell.set(JAVA_INT, 0, firstValue)
            secondIntCell.set(JAVA_INT, 0, secondValue)
            doubleCell.set(JAVA_DOUBLE, 0, value)
            val arr = arena.allocate(ADDRESS, 5)
            arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(first))
            arr.setAtIndex(ADDRESS, 1, firstIntCell)
            arr.setAtIndex(ADDRESS, 2, GodotStrings.makeStringName(second))
            arr.setAtIndex(ADDRESS, 3, secondIntCell)
            arr.setAtIndex(ADDRESS, 4, doubleCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with one StringName arg and bool return value.
     */
    fun ptrcallWithStringNameArgRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(name))
            val ret = arena.allocate(java.lang.foreign.ValueLayout.JAVA_BYTE)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(java.lang.foreign.ValueLayout.JAVA_BYTE, 0) != 0.toByte()
        }
    }

    /**
     * Calls [methodBind] with one StringName arg and int64 return value.
     */
    fun ptrcallWithStringNameArgRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
    ): Long {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(name))
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_LONG, 0)
        }
    }

    fun ptrcallWithStringNameArgRetStringName(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
    ): String {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(name))
            val ret = arena.allocate(8L, 8L)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return try {
                GodotStrings.readStringName(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.STRING_NAME, ret)
            }
        }
    }

    /**
     * Calls [methodBind] with one StringName arg and int32 return value.
     */
    fun ptrcallWithStringNameArgRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
    ): Int {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(name))
            val ret = arena.allocate(JAVA_INT)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_INT, 0)
        }
    }

    /**
     * Calls [methodBind] with one StringName arg and float return value.
     */
    fun ptrcallWithStringNameArgRetDouble(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
    ): Double {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(name))
            val ret = arena.allocate(JAVA_DOUBLE)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_DOUBLE, 0)
        }
    }

    fun ptrcallWithStringNameArgRetColor(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
    ): Color {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(name))
            val ret = arena.allocate(16L, 4L)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return Color(
                ret.get(JAVA_FLOAT, 0),
                ret.get(JAVA_FLOAT, 4),
                ret.get(JAVA_FLOAT, 8),
                ret.get(JAVA_FLOAT, 12),
            )
        }
    }

    fun ptrcallWithStringNameArgRetVector2(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
    ): Vector2 {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(name))
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return Vector2(
                GodotReal.readIndex(ret, 0),
                GodotReal.readIndex(ret, 1),
            )
        }
    }

    /**
     * Calls [methodBind] with one StringName arg and Object return value.
     */
    fun ptrcallWithStringNameArgRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(name))
            val ret = arena.allocate(ADDRESS)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(ADDRESS, 0)
        }
    }

    /**
     * Calls [methodBind] with one StringName argument and decodes an Array of Object values as
     * non-owning wrappers.
     */
    fun ptrcallWithStringNameArgRetObjectList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
    ): List<GodotObject> {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(name))
            val ret = arena.allocate(8L, 8L)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return try {
                BuiltinTypes.readArrayObjects(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, ret)
            }
        }
    }

    fun <T : Any> ptrcallWithStringNameArgRetTypedObjectList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
        wrapper: (MemorySegment) -> T?,
    ): List<T> =
        ptrcallWithStringNameArgRetObjectList(methodBind, instance, name).mapNotNull { wrapper(it.handle) }

    /**
     * Calls [methodBind] with one StringName argument and decodes Array[Node].
     */
    fun ptrcallWithStringNameArgRetTypedNodeList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
    ): List<Node> =
        ptrcallWithStringNameArgRetObjectList(methodBind, instance, name).map { Node(it.handle) }

    /**
     * Calls [methodBind] with one NodePath arg and Object return value.
     *
     * The NodePath is constructed from [path] for the duration of the ptrcall.
     */
    fun ptrcallWithNodePathArgRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        path: String,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val nodePath = arena.allocate(8L, 8L)
            val pathString = arena.allocate(8L, 8L)
            try {
                GodotStrings.initString(pathString, path)
                BuiltinTypes.construct(
                    type = VariantType.NODE_PATH,
                    dest = nodePath,
                    constructorIndex = 2,
                    args = listOf(pathString),
                )
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, nodePath)
                val ret = arena.allocate(ADDRESS)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(ADDRESS, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.NODE_PATH, nodePath)
                GodotStrings.destroyString(pathString)
            }
        }
    }

    fun ptrcallWithNodePathArgRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        path: NodePath,
    ): MemorySegment = ptrcallWithNodePathArgRetObject(methodBind, instance, path.path)

    fun ptrcallWithNodePathArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        path: NodePath,
    ) {
        Arena.ofConfined().use { arena ->
            val nodePath = arena.allocate(8L, 8L)
            val pathString = arena.allocate(8L, 8L)
            try {
                GodotStrings.initString(pathString, path.path)
                BuiltinTypes.construct(
                    type = VariantType.NODE_PATH,
                    dest = nodePath,
                    constructorIndex = 2,
                    args = listOf(pathString),
                )
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, nodePath)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.NODE_PATH, nodePath)
                GodotStrings.destroyString(pathString)
            }
        }
    }

    fun ptrcallWithNodePathArgRetVariantScalar(
        methodBind: MemorySegment,
        instance: MemorySegment,
        path: NodePath,
    ): Any? {
        Arena.ofConfined().use { arena ->
            val nodePath = arena.allocate(8L, 8L)
            val pathString = arena.allocate(8L, 8L)
            try {
                GodotStrings.initString(pathString, path.path)
                BuiltinTypes.construct(
                    type = VariantType.NODE_PATH,
                    dest = nodePath,
                    constructorIndex = 2,
                    args = listOf(pathString),
                )
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, nodePath)
                return readVariantReturn(methodBind, instance, arr, arena)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.NODE_PATH, nodePath)
                GodotStrings.destroyString(pathString)
            }
        }
    }

    fun ptrcallWithNodePathAndVariantArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        path: NodePath,
        value: Any?,
    ) {
        Arena.ofConfined().use { arena ->
            val nodePath = arena.allocate(8L, 8L)
            val pathString = arena.allocate(8L, 8L)
            val variant = initVariantCell(arena, value)
            try {
                GodotStrings.initString(pathString, path.path)
                BuiltinTypes.construct(
                    type = VariantType.NODE_PATH,
                    dest = nodePath,
                    constructorIndex = 2,
                    args = listOf(pathString),
                )
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, nodePath)
                arr.setAtIndex(ADDRESS, 1, variant)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyVariant(variant)
                BuiltinTypes.destroyTyped(VariantType.NODE_PATH, nodePath)
                GodotStrings.destroyString(pathString)
            }
        }
    }

    /**
     * Calls [methodBind] with one NodePath arg and bool return value.
     */
    fun ptrcallWithNodePathArgRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        path: String,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val nodePath = arena.allocate(8L, 8L)
            val pathString = arena.allocate(8L, 8L)
            try {
                GodotStrings.initString(pathString, path)
                BuiltinTypes.construct(
                    type = VariantType.NODE_PATH,
                    dest = nodePath,
                    constructorIndex = 2,
                    args = listOf(pathString),
                )
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, nodePath)
                val ret = arena.allocate(java.lang.foreign.ValueLayout.JAVA_BYTE)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(java.lang.foreign.ValueLayout.JAVA_BYTE, 0) != 0.toByte()
            } finally {
                BuiltinTypes.destroyTyped(VariantType.NODE_PATH, nodePath)
                GodotStrings.destroyString(pathString)
            }
        }
    }

    fun ptrcallWithNodePathArgRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        path: NodePath,
    ): Boolean = ptrcallWithNodePathArgRetBool(methodBind, instance, path.path)

    fun ptrcallWithNodePathArgRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        path: NodePath,
    ): Int {
        Arena.ofConfined().use { arena ->
            val nodePath = arena.allocate(8L, 8L)
            val pathString = arena.allocate(8L, 8L)
            try {
                GodotStrings.initString(pathString, path.path)
                BuiltinTypes.construct(
                    type = VariantType.NODE_PATH,
                    dest = nodePath,
                    constructorIndex = 2,
                    args = listOf(pathString),
                )
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, nodePath)
                val ret = arena.allocate(JAVA_INT)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_INT, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.NODE_PATH, nodePath)
                GodotStrings.destroyString(pathString)
            }
        }
    }

    fun ptrcallWithNodePathArgRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        path: NodePath,
    ): Long {
        Arena.ofConfined().use { arena ->
            val nodePath = arena.allocate(8L, 8L)
            val pathString = arena.allocate(8L, 8L)
            try {
                GodotStrings.initString(pathString, path.path)
                BuiltinTypes.construct(
                    type = VariantType.NODE_PATH,
                    dest = nodePath,
                    constructorIndex = 2,
                    args = listOf(pathString),
                )
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, nodePath)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.NODE_PATH, nodePath)
                GodotStrings.destroyString(pathString)
            }
        }
    }

    /**
     * Calls [methodBind] with (NodePath, int64/enum) args and int32 return value.
     */
    fun ptrcallWithNodePathAndLongArgRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        path: NodePath,
        value: Long,
    ): Int {
        Arena.ofConfined().use { arena ->
            val nodePath = arena.allocate(8L, 8L)
            val pathString = arena.allocate(8L, 8L)
            val longCell = arena.allocate(JAVA_LONG)
            try {
                GodotStrings.initString(pathString, path.path)
                BuiltinTypes.construct(
                    type = VariantType.NODE_PATH,
                    dest = nodePath,
                    constructorIndex = 2,
                    args = listOf(pathString),
                )
                longCell.set(JAVA_LONG, 0, value)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, nodePath)
                arr.setAtIndex(ADDRESS, 1, longCell)
                val ret = arena.allocate(JAVA_INT)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_INT, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.NODE_PATH, nodePath)
                GodotStrings.destroyString(pathString)
            }
        }
    }

    fun ptrcallWithNodePathStringNameArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        path: NodePath,
        name: String,
    ) {
        Arena.ofConfined().use { arena ->
            val nodePath = arena.allocate(8L, 8L)
            val pathString = arena.allocate(8L, 8L)
            try {
                GodotStrings.initString(pathString, path.path)
                BuiltinTypes.construct(
                    type = VariantType.NODE_PATH,
                    dest = nodePath,
                    constructorIndex = 2,
                    args = listOf(pathString),
                )
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, nodePath)
                arr.setAtIndex(ADDRESS, 1, GodotStrings.makeStringName(name))
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.NODE_PATH, nodePath)
                GodotStrings.destroyString(pathString)
            }
        }
    }

    fun ptrcallWithNodePathAndLongArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        path: NodePath,
        value: Long,
    ) {
        Arena.ofConfined().use { arena ->
            val nodePath = arena.allocate(8L, 8L)
            val pathString = arena.allocate(8L, 8L)
            val longCell = arena.allocate(JAVA_LONG)
            try {
                GodotStrings.initString(pathString, path.path)
                BuiltinTypes.construct(
                    type = VariantType.NODE_PATH,
                    dest = nodePath,
                    constructorIndex = 2,
                    args = listOf(pathString),
                )
                longCell.set(JAVA_LONG, 0, value)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, nodePath)
                arr.setAtIndex(ADDRESS, 1, longCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.NODE_PATH, nodePath)
                GodotStrings.destroyString(pathString)
            }
        }
    }

    fun ptrcallWithLongAndNodePathArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Long,
        path: NodePath,
    ) {
        Arena.ofConfined().use { arena ->
            val longCell = arena.allocate(JAVA_LONG)
            val nodePath = arena.allocate(8L, 8L)
            val pathString = arena.allocate(8L, 8L)
            try {
                longCell.set(JAVA_LONG, 0, value)
                GodotStrings.initString(pathString, path.path)
                BuiltinTypes.construct(
                    type = VariantType.NODE_PATH,
                    dest = nodePath,
                    constructorIndex = 2,
                    args = listOf(pathString),
                )
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, longCell)
                arr.setAtIndex(ADDRESS, 1, nodePath)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.NODE_PATH, nodePath)
                GodotStrings.destroyString(pathString)
            }
        }
    }

    fun ptrcallWithNodePathAndIntArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        path: NodePath,
        value: Int,
    ) {
        Arena.ofConfined().use { arena ->
            val nodePath = arena.allocate(8L, 8L)
            val pathString = arena.allocate(8L, 8L)
            val intArg = arena.allocate(JAVA_INT)
            try {
                GodotStrings.initString(pathString, path.path)
                BuiltinTypes.construct(
                    type = VariantType.NODE_PATH,
                    dest = nodePath,
                    constructorIndex = 2,
                    args = listOf(pathString),
                )
                intArg.set(JAVA_INT, 0, value)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, nodePath)
                arr.setAtIndex(ADDRESS, 1, intArg)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.NODE_PATH, nodePath)
                GodotStrings.destroyString(pathString)
            }
        }
    }

    fun ptrcallWithNodePathAndBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        path: NodePath,
        value: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val nodePath = arena.allocate(8L, 8L)
            val pathString = arena.allocate(8L, 8L)
            val boolArg = arena.allocate(JAVA_BYTE)
            try {
                GodotStrings.initString(pathString, path.path)
                BuiltinTypes.construct(
                    type = VariantType.NODE_PATH,
                    dest = nodePath,
                    constructorIndex = 2,
                    args = listOf(pathString),
                )
                boolArg.set(JAVA_BYTE, 0, if (value) 1.toByte() else 0.toByte())
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, nodePath)
                arr.setAtIndex(ADDRESS, 1, boolArg)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.NODE_PATH, nodePath)
                GodotStrings.destroyString(pathString)
            }
        }
    }

    fun ptrcallWithIntBoolNodePathIntArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        enabled: Boolean,
        path: NodePath,
        second: Int,
    ) {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(JAVA_INT)
            val boolCell = arena.allocate(JAVA_BYTE)
            val nodePath = arena.allocate(8L, 8L)
            val pathString = arena.allocate(8L, 8L)
            val secondCell = arena.allocate(JAVA_INT)
            try {
                firstCell.set(JAVA_INT, 0, first)
                boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
                GodotStrings.initString(pathString, path.path)
                BuiltinTypes.construct(
                    type = VariantType.NODE_PATH,
                    dest = nodePath,
                    constructorIndex = 2,
                    args = listOf(pathString),
                )
                secondCell.set(JAVA_INT, 0, second)
                val arr = arena.allocate(ADDRESS, 4)
                arr.setAtIndex(ADDRESS, 0, firstCell)
                arr.setAtIndex(ADDRESS, 1, boolCell)
                arr.setAtIndex(ADDRESS, 2, nodePath)
                arr.setAtIndex(ADDRESS, 3, secondCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.NODE_PATH, nodePath)
                GodotStrings.destroyString(pathString)
            }
        }
    }

    fun ptrcallWithNodePathRect2TwoIntArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        path: NodePath,
        rect: Rect2,
        first: Int,
        second: Int,
    ) {
        Arena.ofConfined().use { arena ->
            val nodePath = arena.allocate(8L, 8L)
            val pathString = arena.allocate(8L, 8L)
            val rectCell = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
            val firstCell = arena.allocate(JAVA_INT)
            val secondCell = arena.allocate(JAVA_INT)
            try {
                GodotStrings.initString(pathString, path.path)
                BuiltinTypes.construct(
                    type = VariantType.NODE_PATH,
                    dest = nodePath,
                    constructorIndex = 2,
                    args = listOf(pathString),
                )
                writeRect2(rectCell, rect)
                firstCell.set(JAVA_INT, 0, first)
                secondCell.set(JAVA_INT, 0, second)
                val arr = arena.allocate(ADDRESS, 4)
                arr.setAtIndex(ADDRESS, 0, nodePath)
                arr.setAtIndex(ADDRESS, 1, rectCell)
                arr.setAtIndex(ADDRESS, 2, firstCell)
                arr.setAtIndex(ADDRESS, 3, secondCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.NODE_PATH, nodePath)
                GodotStrings.destroyString(pathString)
            }
        }
    }

    /**
     * Calls [methodBind] with one Object* arg and one NodePath arg.
     */
    fun ptrcallWithObjectAndNodePathArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        path: String,
    ) {
        Arena.ofConfined().use { arena ->
            val objCell = arena.allocate(ADDRESS)
            objCell.set(ADDRESS, 0, objectArg)

            val nodePath = arena.allocate(8L, 8L)
            val pathString = arena.allocate(8L, 8L)
            try {
                GodotStrings.initString(pathString, path)
                BuiltinTypes.construct(
                    type = VariantType.NODE_PATH,
                    dest = nodePath,
                    constructorIndex = 2,
                    args = listOf(pathString),
                )

                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, objCell)
                arr.setAtIndex(ADDRESS, 1, nodePath)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.NODE_PATH, nodePath)
                GodotStrings.destroyString(pathString)
            }
        }
    }

    /**
     * Calls [methodBind] with (Object, NodePath, Variant, float) and Object return value.
     */
    fun ptrcallWithObjectNodePathVariantDoubleArgsRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        path: String,
        variantArg: Any?,
        doubleArg: Double,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val objCell = arena.allocate(ADDRESS)
            objCell.set(ADDRESS, 0, objectArg)

            val nodePath = arena.allocate(8L, 8L)
            val pathString = arena.allocate(8L, 8L)
            val variant = arena.allocate(BuiltinTypes.VARIANT_SIZE, 8L)
            val doubleCell = arena.allocate(JAVA_DOUBLE)
            var variantInitialized = false
            try {
                GodotStrings.initString(pathString, path)
                BuiltinTypes.construct(
                    type = VariantType.NODE_PATH,
                    dest = nodePath,
                    constructorIndex = 2,
                    args = listOf(pathString),
                )
                BuiltinTypes.initVariantFromAny(variant, variantArg, arena)
                variantInitialized = true
                doubleCell.set(JAVA_DOUBLE, 0, doubleArg)

                val arr = arena.allocate(ADDRESS, 4)
                arr.setAtIndex(ADDRESS, 0, objCell)
                arr.setAtIndex(ADDRESS, 1, nodePath)
                arr.setAtIndex(ADDRESS, 2, variant)
                arr.setAtIndex(ADDRESS, 3, doubleCell)
                val ret = arena.allocate(ADDRESS)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(ADDRESS, 0)
            } finally {
                if (variantInitialized) {
                    BuiltinTypes.destroyVariant(variant)
                }
                BuiltinTypes.destroyTyped(VariantType.NODE_PATH, nodePath)
                GodotStrings.destroyString(pathString)
            }
        }
    }

    fun ptrcallWithObjectNodePathVariantDoubleArgsRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        path: NodePath,
        variantArg: Any?,
        doubleArg: Double,
    ): MemorySegment =
        ptrcallWithObjectNodePathVariantDoubleArgsRetObject(
            methodBind,
            instance,
            objectArg,
            path.path,
            variantArg,
            doubleArg,
        )

    /**
     * Calls [methodBind] with (StringName, scalar float) and no return value.
     */
    fun ptrcallWithStringNameAndFloatArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
        value: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(name))
            val floatCell = arena.allocate(java.lang.foreign.ValueLayout.JAVA_DOUBLE)
            floatCell.set(java.lang.foreign.ValueLayout.JAVA_DOUBLE, 0, value)
            arr.setAtIndex(ADDRESS, 1, floatCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (StringName, scalar float, int64/enum, int64/enum) args and no return value.
     */
    fun ptrcallWithStringNameDoubleTwoLongArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
        doubleArg: Double,
        firstLong: Long,
        secondLong: Long,
    ) {
        Arena.ofConfined().use { arena ->
            val doubleCell = arena.allocate(JAVA_DOUBLE)
            val longCell0 = arena.allocate(JAVA_LONG)
            val longCell1 = arena.allocate(JAVA_LONG)
            doubleCell.set(JAVA_DOUBLE, 0, doubleArg)
            longCell0.set(JAVA_LONG, 0, firstLong)
            longCell1.set(JAVA_LONG, 0, secondLong)
            val arr = arena.allocate(ADDRESS, 4)
            arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(name))
            arr.setAtIndex(ADDRESS, 1, doubleCell)
            arr.setAtIndex(ADDRESS, 2, longCell0)
            arr.setAtIndex(ADDRESS, 3, longCell1)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (StringName, bool) and bool return value.
     */
    fun ptrcallWithStringNameAndBoolArgRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
        boolArg: Boolean,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 2)
            val stringName = GodotStrings.makeStringName(name)
            arr.setAtIndex(ADDRESS, 0, stringName)
            val boolCell = arena.allocate(java.lang.foreign.ValueLayout.JAVA_BYTE)
            boolCell.set(java.lang.foreign.ValueLayout.JAVA_BYTE, 0, if (boolArg) 1.toByte() else 0.toByte())
            arr.setAtIndex(ADDRESS, 1, boolCell)
            val ret = arena.allocate(java.lang.foreign.ValueLayout.JAVA_BYTE)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(java.lang.foreign.ValueLayout.JAVA_BYTE, 0) != 0.toByte()
        }
    }

    /**
     * Calls [methodBind] with (StringName, bool, bool) and bool return value.
     */
    fun ptrcallWithStringNameAndTwoBoolArgsRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
        firstBoolArg: Boolean,
        secondBoolArg: Boolean,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(name))
            val firstBoolCell = arena.allocate(java.lang.foreign.ValueLayout.JAVA_BYTE)
            firstBoolCell.set(
                java.lang.foreign.ValueLayout.JAVA_BYTE,
                0,
                if (firstBoolArg) 1.toByte() else 0.toByte(),
            )
            arr.setAtIndex(ADDRESS, 1, firstBoolCell)
            val secondBoolCell = arena.allocate(java.lang.foreign.ValueLayout.JAVA_BYTE)
            secondBoolCell.set(
                java.lang.foreign.ValueLayout.JAVA_BYTE,
                0,
                if (secondBoolArg) 1.toByte() else 0.toByte(),
            )
            arr.setAtIndex(ADDRESS, 2, secondBoolCell)
            val ret = arena.allocate(java.lang.foreign.ValueLayout.JAVA_BYTE)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(java.lang.foreign.ValueLayout.JAVA_BYTE, 0) != 0.toByte()
        }
    }

    /**
     * Calls [methodBind] with (StringName, bool) and no return value.
     */
    fun ptrcallWithStringNameAndBoolArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
        boolArg: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(name))
            val boolCell = arena.allocate(java.lang.foreign.ValueLayout.JAVA_BYTE)
            boolCell.set(java.lang.foreign.ValueLayout.JAVA_BYTE, 0, if (boolArg) 1.toByte() else 0.toByte())
            arr.setAtIndex(ADDRESS, 1, boolCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (String, int32, bool) and no return value.
     */
    fun ptrcallWithStringIntAndBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        intArg: Int,
        boolArg: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            GodotStrings.initString(stringCell, text)
            val intCell = arena.allocate(JAVA_INT)
            intCell.set(JAVA_INT, 0, intArg)
            val boolCell = arena.allocate(JAVA_BYTE)
            boolCell.set(JAVA_BYTE, 0, if (boolArg) 1.toByte() else 0.toByte())
            try {
                val arr = arena.allocate(ADDRESS, 3)
                arr.setAtIndex(ADDRESS, 0, stringCell)
                arr.setAtIndex(ADDRESS, 1, intCell)
                arr.setAtIndex(ADDRESS, 2, boolCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    /**
     * Calls [methodBind] with (StringName, bool) and float(double) return value.
     */
    fun ptrcallWithStringNameAndBoolArgRetDouble(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
        boolArg: Boolean,
    ): Double {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 2)
            val stringName = GodotStrings.makeStringName(name)
            arr.setAtIndex(ADDRESS, 0, stringName)
            val boolCell = arena.allocate(java.lang.foreign.ValueLayout.JAVA_BYTE)
            boolCell.set(java.lang.foreign.ValueLayout.JAVA_BYTE, 0, if (boolArg) 1.toByte() else 0.toByte())
            arr.setAtIndex(ADDRESS, 1, boolCell)
            val ret = arena.allocate(java.lang.foreign.ValueLayout.JAVA_DOUBLE)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(java.lang.foreign.ValueLayout.JAVA_DOUBLE, 0)
        }
    }

    /**
     * Calls [methodBind] with one bool argument and Object return value.
     */
    fun ptrcallWithBoolArgRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        boolArg: Boolean,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 1)
            val boolCell = arena.allocate(java.lang.foreign.ValueLayout.JAVA_BYTE)
            boolCell.set(java.lang.foreign.ValueLayout.JAVA_BYTE, 0, if (boolArg) 1.toByte() else 0.toByte())
            arr.setAtIndex(ADDRESS, 0, boolCell)
            val ret = arena.allocate(ADDRESS)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(ADDRESS, 0)
        }
    }

    /**
     * Calls [methodBind] with (Dictionary, bool) and String return value.
     */
    fun ptrcallWithDictionaryAndBoolArgRetString(
        methodBind: MemorySegment,
        instance: MemorySegment,
        values: Map<String, Any?>,
        boolArg: Boolean,
    ): String {
        Arena.ofConfined().use { arena ->
            val dict = arena.allocate(8L, 8L)
            BuiltinTypes.initDictionary(dict, values)
            val retString = arena.allocate(8L, 8L)
            try {
                val boolCell = arena.allocate(java.lang.foreign.ValueLayout.JAVA_BYTE)
                boolCell.set(java.lang.foreign.ValueLayout.JAVA_BYTE, 0, if (boolArg) 1.toByte() else 0.toByte())
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, dict)
                arr.setAtIndex(ADDRESS, 1, boolCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, retString)
                return GodotStrings.readString(retString)
            } finally {
                GodotStrings.destroyString(retString)
                BuiltinTypes.destroyTyped(VariantType.DICTIONARY, dict)
            }
        }
    }

    fun ptrcallWithDictionaryIntDoubleTransform2DFourIntDoubleArgsRetRID(
        methodBind: MemorySegment,
        instance: MemorySegment,
        variationCoordinates: Map<String, Any?>,
        faceIndex: Int,
        strength: Double,
        transform: Transform2D,
        spacingTop: Int,
        spacingBottom: Int,
        spacingSpace: Int,
        spacingGlyph: Int,
        baselineOffset: Double,
    ): RID {
        Arena.ofConfined().use { arena ->
            val variationCoordinatesCell = arena.allocate(8L, 8L)
            val faceIndexCell = arena.allocate(JAVA_INT)
            val strengthCell = arena.allocate(JAVA_DOUBLE)
            val transformCell = arena.allocate(GodotReal.SIZE_BYTES * 6, GodotReal.ALIGN_BYTES)
            val spacingTopCell = arena.allocate(JAVA_INT)
            val spacingBottomCell = arena.allocate(JAVA_INT)
            val spacingSpaceCell = arena.allocate(JAVA_INT)
            val spacingGlyphCell = arena.allocate(JAVA_INT)
            val baselineOffsetCell = arena.allocate(JAVA_DOUBLE)
            try {
                BuiltinTypes.initDictionary(variationCoordinatesCell, variationCoordinates)
                faceIndexCell.set(JAVA_INT, 0, faceIndex)
                strengthCell.set(JAVA_DOUBLE, 0, strength)
                writeTransform2D(transformCell, transform)
                spacingTopCell.set(JAVA_INT, 0, spacingTop)
                spacingBottomCell.set(JAVA_INT, 0, spacingBottom)
                spacingSpaceCell.set(JAVA_INT, 0, spacingSpace)
                spacingGlyphCell.set(JAVA_INT, 0, spacingGlyph)
                baselineOffsetCell.set(JAVA_DOUBLE, 0, baselineOffset)
                val args = arena.allocate(ADDRESS, 9)
                args.setAtIndex(ADDRESS, 0, variationCoordinatesCell)
                args.setAtIndex(ADDRESS, 1, faceIndexCell)
                args.setAtIndex(ADDRESS, 2, strengthCell)
                args.setAtIndex(ADDRESS, 3, transformCell)
                args.setAtIndex(ADDRESS, 4, spacingTopCell)
                args.setAtIndex(ADDRESS, 5, spacingBottomCell)
                args.setAtIndex(ADDRESS, 6, spacingSpaceCell)
                args.setAtIndex(ADDRESS, 7, spacingGlyphCell)
                args.setAtIndex(ADDRESS, 8, baselineOffsetCell)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return RID(ret.get(JAVA_LONG, 0))
            } finally {
                BuiltinTypes.destroyTyped(VariantType.DICTIONARY, variationCoordinatesCell)
            }
        }
    }

    fun ptrcallWithDictionaryDictionaryListArgsRetDictionary(
        methodBind: MemorySegment,
        instance: MemorySegment,
        dictionary: Map<String, Any?>,
        dictionaries: List<Map<String, Any?>>,
    ): Map<String, Any?> {
        Arena.ofConfined().use { arena ->
            val dictionaryCell = arena.allocate(8L, 8L)
            val dictionaryArray = arena.allocate(8L, 8L)
            val ret = arena.allocate(8L, 8L)
            try {
                BuiltinTypes.initDictionary(dictionaryCell, dictionary)
                BuiltinTypes.initArrayOfDictionaries(dictionaryArray, dictionaries)
                val args = arena.allocate(ADDRESS, 2)
                args.setAtIndex(ADDRESS, 0, dictionaryCell)
                args.setAtIndex(ADDRESS, 1, dictionaryArray)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return BuiltinTypes.readDictionaryScalars(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.DICTIONARY, ret)
                BuiltinTypes.destroyTyped(VariantType.ARRAY, dictionaryArray)
                BuiltinTypes.destroyTyped(VariantType.DICTIONARY, dictionaryCell)
            }
        }
    }

    /**
     * Calls [methodBind] with one Dictionary arg and int64 return value.
     */
    fun ptrcallWithDictionaryArgRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        values: Map<String, Any?>,
    ): Long {
        Arena.ofConfined().use { arena ->
            val dict = arena.allocate(8L, 8L)
            BuiltinTypes.initDictionary(dict, values)
            try {
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, dict)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.DICTIONARY, dict)
            }
        }
    }

    /**
     * Calls [methodBind] with one Dictionary arg and no return value.
     */
    fun ptrcallWithDictionaryArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        values: Map<String, Any?>,
    ) {
        Arena.ofConfined().use { arena ->
            val dict = arena.allocate(8L, 8L)
            try {
                BuiltinTypes.initDictionary(dict, values)
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, dict)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.DICTIONARY, dict)
            }
        }
    }

    /**
     * Calls [methodBind] with one Dictionary arg and decodes a Dictionary return as scalar key/value pairs.
     */
    fun ptrcallWithDictionaryArgRetDictionary(
        methodBind: MemorySegment,
        instance: MemorySegment,
        values: Map<String, Any?>,
    ): Map<String, Any?> {
        Arena.ofConfined().use { arena ->
            val dict = arena.allocate(8L, 8L)
            val ret = arena.allocate(8L, 8L)
            try {
                BuiltinTypes.initDictionary(dict, values)
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, dict)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return BuiltinTypes.readDictionaryScalars(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.DICTIONARY, ret)
                BuiltinTypes.destroyTyped(VariantType.DICTIONARY, dict)
            }
        }
    }

    /**
     * Calls [methodBind] with (int32, Dictionary) and no return value.
     */
    fun ptrcallWithIntAndDictionaryArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        intValue: Int,
        values: Map<String, Any?>,
    ) {
        Arena.ofConfined().use { arena ->
            val intCell = arena.allocate(JAVA_INT)
            val dict = arena.allocate(8L, 8L)
            try {
                intCell.set(JAVA_INT, 0, intValue)
                BuiltinTypes.initDictionary(dict, values)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, intCell)
                arr.setAtIndex(ADDRESS, 1, dict)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.DICTIONARY, dict)
            }
        }
    }

    /**
     * Calls [methodBind] with (int32, Dictionary) and bool return value.
     */
    fun ptrcallWithIntAndDictionaryArgRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        intValue: Int,
        values: Map<String, Any?>,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val intCell = arena.allocate(JAVA_INT)
            val dict = arena.allocate(8L, 8L)
            try {
                intCell.set(JAVA_INT, 0, intValue)
                BuiltinTypes.initDictionary(dict, values)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, intCell)
                arr.setAtIndex(ADDRESS, 1, dict)
                val ret = arena.allocate(JAVA_BYTE)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_BYTE, 0) != 0.toByte()
            } finally {
                BuiltinTypes.destroyTyped(VariantType.DICTIONARY, dict)
            }
        }
    }

    /**
     * Calls [methodBind] with (RID, Dictionary) and no return value.
     */
    fun ptrcallWithRIDAndDictionaryArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        values: Map<String, Any?>,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val dict = arena.allocate(8L, 8L)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                BuiltinTypes.initDictionary(dict, values)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, ridCell)
                arr.setAtIndex(ADDRESS, 1, dict)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.DICTIONARY, dict)
            }
        }
    }

    fun ptrcallWithRIDLongTwoArrayDictionaryLongArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        primitive: Long,
        arrays: List<Any?>,
        blendShapes: List<Any?>,
        lods: Map<String, Any?>,
        flags: Long,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val primitiveCell = arena.allocate(JAVA_LONG)
            val arraysCell = arena.allocate(8L, 8L)
            val blendShapesCell = arena.allocate(8L, 8L)
            val lodsCell = arena.allocate(8L, 8L)
            val flagsCell = arena.allocate(JAVA_LONG)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                primitiveCell.set(JAVA_LONG, 0, primitive)
                BuiltinTypes.initArray(arraysCell, arrays)
                BuiltinTypes.initArray(blendShapesCell, blendShapes)
                BuiltinTypes.initDictionary(lodsCell, lods)
                flagsCell.set(JAVA_LONG, 0, flags)
                val args = arena.allocate(ADDRESS, 6)
                args.setAtIndex(ADDRESS, 0, ridCell)
                args.setAtIndex(ADDRESS, 1, primitiveCell)
                args.setAtIndex(ADDRESS, 2, arraysCell)
                args.setAtIndex(ADDRESS, 3, blendShapesCell)
                args.setAtIndex(ADDRESS, 4, lodsCell)
                args.setAtIndex(ADDRESS, 5, flagsCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.DICTIONARY, lodsCell)
                BuiltinTypes.destroyTyped(VariantType.ARRAY, blendShapesCell)
                BuiltinTypes.destroyTyped(VariantType.ARRAY, arraysCell)
            }
        }
    }

    fun ptrcallWithRIDStringRIDListLongDictionaryStringVariantArgsRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        text: String,
        fonts: List<RID>,
        size: Long,
        features: Map<String, Any?>,
        language: String,
        meta: Any?,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val textCell = arena.allocate(8L, 8L)
            val fontsArray = arena.allocate(8L, 8L)
            val sizeCell = arena.allocate(JAVA_LONG)
            val featuresCell = arena.allocate(8L, 8L)
            val languageCell = arena.allocate(8L, 8L)
            val metaVariant = initVariantCell(arena, meta)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                GodotStrings.initString(textCell, text)
                BuiltinTypes.initArrayOfRids(fontsArray, fonts)
                sizeCell.set(JAVA_LONG, 0, size)
                BuiltinTypes.initDictionary(featuresCell, features)
                GodotStrings.initString(languageCell, language)
                val args = arena.allocate(ADDRESS, 7)
                args.setAtIndex(ADDRESS, 0, ridCell)
                args.setAtIndex(ADDRESS, 1, textCell)
                args.setAtIndex(ADDRESS, 2, fontsArray)
                args.setAtIndex(ADDRESS, 3, sizeCell)
                args.setAtIndex(ADDRESS, 4, featuresCell)
                args.setAtIndex(ADDRESS, 5, languageCell)
                args.setAtIndex(ADDRESS, 6, metaVariant)
                val ret = arena.allocate(JAVA_BYTE)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(JAVA_BYTE, 0) != 0.toByte()
            } finally {
                BuiltinTypes.destroyVariant(metaVariant)
                GodotStrings.destroyString(languageCell)
                BuiltinTypes.destroyTyped(VariantType.DICTIONARY, featuresCell)
                BuiltinTypes.destroyTyped(VariantType.ARRAY, fontsArray)
                GodotStrings.destroyString(textCell)
            }
        }
    }

    fun ptrcallWithRIDLongRIDListLongDictionaryArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        start: Long,
        fonts: List<RID>,
        size: Long,
        features: Map<String, Any?>,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val startCell = arena.allocate(JAVA_LONG)
            val fontsArray = arena.allocate(8L, 8L)
            val sizeCell = arena.allocate(JAVA_LONG)
            val featuresCell = arena.allocate(8L, 8L)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                startCell.set(JAVA_LONG, 0, start)
                BuiltinTypes.initArrayOfRids(fontsArray, fonts)
                sizeCell.set(JAVA_LONG, 0, size)
                BuiltinTypes.initDictionary(featuresCell, features)
                val args = arena.allocate(ADDRESS, 5)
                args.setAtIndex(ADDRESS, 0, ridCell)
                args.setAtIndex(ADDRESS, 1, startCell)
                args.setAtIndex(ADDRESS, 2, fontsArray)
                args.setAtIndex(ADDRESS, 3, sizeCell)
                args.setAtIndex(ADDRESS, 4, featuresCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.DICTIONARY, featuresCell)
                BuiltinTypes.destroyTyped(VariantType.ARRAY, fontsArray)
            }
        }
    }

    /**
     * Calls [methodBind] with no arguments and decodes a Dictionary return as scalar key/value pairs.
     */
    fun ptrcallNoArgsRetDictionary(
        methodBind: MemorySegment,
        instance: MemorySegment,
    ): Map<String, Any?> {
        Arena.ofConfined().use { arena ->
            val ret = arena.allocate(8L, 8L)
            objectMethodBindPtrcall.invoke(methodBind, instance, MemorySegment.NULL, ret)
            return try {
                BuiltinTypes.readDictionaryScalars(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.DICTIONARY, ret)
            }
        }
    }

    /**
     * Calls [methodBind] with one int32 argument and decodes a Dictionary return as scalar key/value pairs.
     */
    fun ptrcallWithIntArgRetDictionary(
        methodBind: MemorySegment,
        instance: MemorySegment,
        intArg: Int,
    ): Map<String, Any?> {
        Arena.ofConfined().use { arena ->
            val intCell = arena.allocate(JAVA_INT)
            intCell.set(JAVA_INT, 0, intArg)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, intCell)
            val ret = arena.allocate(8L, 8L)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return try {
                BuiltinTypes.readDictionaryScalars(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.DICTIONARY, ret)
            }
        }
    }

    /**
     * Calls [methodBind] with one RID argument and decodes a Dictionary return as scalar key/value pairs.
     */
    fun ptrcallWithRIDArgRetDictionary(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
    ): Map<String, Any?> {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, ridCell)
            val ret = arena.allocate(8L, 8L)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return try {
                BuiltinTypes.readDictionaryScalars(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.DICTIONARY, ret)
            }
        }
    }

    fun ptrcallWithRIDArgRetArray(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
    ): List<Any?> {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, ridCell)
            return callArrayReturn(methodBind, instance, arr, BuiltinTypes::readArrayScalars)
        }
    }

    fun ptrcallWithRIDAndArrayArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        values: List<Any?>,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val argArray = arena.allocate(8L, 8L)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                BuiltinTypes.initArray(argArray, values)
                val args = arena.allocate(ADDRESS, 2)
                args.setAtIndex(ADDRESS, 0, ridCell)
                args.setAtIndex(ADDRESS, 1, argArray)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, argArray)
            }
        }
    }

    /**
     * Calls [methodBind] with one bool argument and decodes a Dictionary return as scalar key/value pairs.
     */
    fun ptrcallWithBoolArgRetDictionary(
        methodBind: MemorySegment,
        instance: MemorySegment,
        boolArg: Boolean,
    ): Map<String, Any?> {
        Arena.ofConfined().use { arena ->
            val boolCell = arena.allocate(java.lang.foreign.ValueLayout.JAVA_BYTE)
            boolCell.set(java.lang.foreign.ValueLayout.JAVA_BYTE, 0, if (boolArg) 1.toByte() else 0.toByte())
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, boolCell)
            val ret = arena.allocate(8L, 8L)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return try {
                BuiltinTypes.readDictionaryScalars(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.DICTIONARY, ret)
            }
        }
    }

    /**
     * Calls [methodBind] with one int64 argument and decodes a Dictionary return as scalar key/value pairs.
     */
    fun ptrcallWithLongArgRetDictionary(
        methodBind: MemorySegment,
        instance: MemorySegment,
        longArg: Long,
    ): Map<String, Any?> {
        Arena.ofConfined().use { arena ->
            val longCell = arena.allocate(JAVA_LONG)
            longCell.set(JAVA_LONG, 0, longArg)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, longCell)
            val ret = arena.allocate(8L, 8L)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return try {
                BuiltinTypes.readDictionaryScalars(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.DICTIONARY, ret)
            }
        }
    }

    /**
     * Calls [methodBind] with (String, bool) and decodes a Dictionary return as scalar key/value pairs.
     */
    fun ptrcallWithStringAndBoolArgRetDictionary(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: String,
        boolArg: Boolean,
    ): Map<String, Any?> {
        Arena.ofConfined().use { arena ->
            val argString = arena.allocate(8L, 8L)
            GodotStrings.initString(argString, value)
            val ret = arena.allocate(8L, 8L)
            try {
                val boolCell = arena.allocate(java.lang.foreign.ValueLayout.JAVA_BYTE)
                boolCell.set(java.lang.foreign.ValueLayout.JAVA_BYTE, 0, if (boolArg) 1.toByte() else 0.toByte())
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, argString)
                arr.setAtIndex(ADDRESS, 1, boolCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return BuiltinTypes.readDictionaryScalars(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.DICTIONARY, ret)
                GodotStrings.destroyString(argString)
            }
        }
    }

    /**
     * Calls [methodBind] with (StringName, bool) and decodes Array[Dictionary] return values.
     */
    fun ptrcallWithStringNameAndBoolArgRetDictionaryList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
        boolArg: Boolean,
    ): List<Map<String, Any?>> {
        Arena.ofConfined().use { arena ->
            val boolCell = arena.allocate(JAVA_BYTE)
            boolCell.set(JAVA_BYTE, 0, if (boolArg) 1.toByte() else 0.toByte())
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(name))
            arr.setAtIndex(ADDRESS, 1, boolCell)
            return callArrayReturn(methodBind, instance, arr, BuiltinTypes::readArrayDictionaries)
        }
    }

    /**
     * Calls [methodBind] with one StringName argument and decodes Array[Dictionary] return values.
     */
    fun ptrcallWithStringNameArgRetDictionaryList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
    ): List<Map<String, Any?>> {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(name))
            return callArrayReturn(methodBind, instance, arr, BuiltinTypes::readArrayDictionaries)
        }
    }

    fun ptrcallWithStringNameArgRetStringNameList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
    ): List<String> {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(name))
            return callArrayReturn(methodBind, instance, arr, BuiltinTypes::readArrayStringNames)
        }
    }

    /**
     * Calls [methodBind] with (StringName, Object, bool) and bool return value.
     */
    fun ptrcallWithStringNameObjectAndBoolArgRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
        objectArg: MemorySegment,
        boolArg: Boolean,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(name))

            val objCell = arena.allocate(ADDRESS)
            objCell.set(ADDRESS, 0, objectArg)
            arr.setAtIndex(ADDRESS, 1, objCell)

            val boolCell = arena.allocate(java.lang.foreign.ValueLayout.JAVA_BYTE)
            boolCell.set(java.lang.foreign.ValueLayout.JAVA_BYTE, 0, if (boolArg) 1.toByte() else 0.toByte())
            arr.setAtIndex(ADDRESS, 2, boolCell)

            val ret = arena.allocate(java.lang.foreign.ValueLayout.JAVA_BYTE)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(java.lang.foreign.ValueLayout.JAVA_BYTE, 0) != 0.toByte()
        }
    }

    /**
     * Calls [methodBind] with (int64, StringName) and no return value.
     */
    fun ptrcallWithLongAndStringNameArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Long,
        name: String,
    ) {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 2)
            val intCell = arena.allocate(JAVA_LONG)
            intCell.set(JAVA_LONG, 0, value)
            arr.setAtIndex(ADDRESS, 0, intCell)
            arr.setAtIndex(ADDRESS, 1, GodotStrings.makeStringName(name))
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (int64/enum, int32) and no return value.
     */
    fun ptrcallWithLongAndIntArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Long,
        second: Int,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_LONG)
            val arg1 = arena.allocate(JAVA_INT)
            arg0.set(JAVA_LONG, 0, first)
            arg1.set(JAVA_INT, 0, second)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (int64/enum, int32) and bool return value.
     */
    fun ptrcallWithLongAndIntArgsRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Long,
        second: Int,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_LONG)
            val arg1 = arena.allocate(JAVA_INT)
            arg0.set(JAVA_LONG, 0, first)
            arg1.set(JAVA_INT, 0, second)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            val ret = arena.allocate(JAVA_BYTE)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_BYTE, 0) != 0.toByte()
        }
    }

    /**
     * Calls [methodBind] with (int64/enum, int32) and int64 return value.
     */
    fun ptrcallWithLongAndIntArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Long,
        second: Int,
    ): Long {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_LONG)
            val arg1 = arena.allocate(JAVA_INT)
            arg0.set(JAVA_LONG, 0, first)
            arg1.set(JAVA_INT, 0, second)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_LONG, 0)
        }
    }

    /**
     * Calls [methodBind] with (int64/enum, int32) and Object* return value.
     */
    fun ptrcallWithLongAndIntArgsRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Long,
        second: Int,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_LONG)
            val arg1 = arena.allocate(JAVA_INT)
            arg0.set(JAVA_LONG, 0, first)
            arg1.set(JAVA_INT, 0, second)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            val ret = arena.allocate(ADDRESS)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(ADDRESS, 0)
        }
    }

    /**
     * Calls [methodBind] with (int64/enum, int32) and int32 return value.
     */
    fun ptrcallWithLongAndIntArgsRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Long,
        second: Int,
    ): Int {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_LONG)
            val arg1 = arena.allocate(JAVA_INT)
            arg0.set(JAVA_LONG, 0, first)
            arg1.set(JAVA_INT, 0, second)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            val ret = arena.allocate(JAVA_INT)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_INT, 0)
        }
    }

    /**
     * Calls [methodBind] with (int64/enum/bitfield, int32) and uint32 return value.
     */
    fun ptrcallWithLongAndIntArgsRetUInt32(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Long,
        second: Int,
    ): Long {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_LONG)
            val arg1 = arena.allocate(JAVA_INT)
            arg0.set(JAVA_LONG, 0, first)
            arg1.set(JAVA_INT, 0, second)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            val ret = arena.allocate(JAVA_INT)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_INT, 0).toLong() and 0xffff_ffffL
        }
    }

    /**
     * Calls [methodBind] with (int64/enum/bitfield, int32, int32) and uint32 return value.
     */
    fun ptrcallWithLongAndTwoIntArgsRetUInt32(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Long,
        second: Int,
        third: Int,
    ): Long {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_LONG)
            val arg1 = arena.allocate(JAVA_INT)
            val arg2 = arena.allocate(JAVA_INT)
            arg0.set(JAVA_LONG, 0, first)
            arg1.set(JAVA_INT, 0, second)
            arg2.set(JAVA_INT, 0, third)
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            val ret = arena.allocate(JAVA_INT)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_INT, 0).toLong() and 0xffff_ffffL
        }
    }

    /**
     * Calls [methodBind] with (int32, StringName) and no return value.
     */
    fun ptrcallWithIntAndStringNameArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Int,
        name: String,
    ) {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 2)
            val intCell = arena.allocate(JAVA_INT)
            intCell.set(JAVA_INT, 0, value)
            arr.setAtIndex(ADDRESS, 0, intCell)
            arr.setAtIndex(ADDRESS, 1, GodotStrings.makeStringName(name))
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithIntAndStringNameArgRetVariantScalar(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Int,
        name: String,
    ): Any? {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 2)
            val intCell = arena.allocate(JAVA_INT)
            intCell.set(JAVA_INT, 0, value)
            arr.setAtIndex(ADDRESS, 0, intCell)
            arr.setAtIndex(ADDRESS, 1, GodotStrings.makeStringName(name))
            return readVariantReturn(methodBind, instance, arr, arena)
        }
    }

    fun ptrcallWithIntStringNameAndVariantArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Int,
        name: String,
        variantValue: Any?,
    ) {
        Arena.ofConfined().use { arena ->
            val intCell = arena.allocate(JAVA_INT)
            val variant = initVariantCell(arena, variantValue)
            try {
                intCell.set(JAVA_INT, 0, value)
                val arr = arena.allocate(ADDRESS, 3)
                arr.setAtIndex(ADDRESS, 0, intCell)
                arr.setAtIndex(ADDRESS, 1, GodotStrings.makeStringName(name))
                arr.setAtIndex(ADDRESS, 2, variant)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyVariant(variant)
            }
        }
    }

    /**
     * Calls [methodBind] with (int32, NodePath) and no return value.
     */
    fun ptrcallWithIntAndNodePathArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Int,
        path: NodePath,
    ) {
        Arena.ofConfined().use { arena ->
            val intCell = arena.allocate(JAVA_INT)
            val nodePath = arena.allocate(8L, 8L)
            val pathString = arena.allocate(8L, 8L)
            try {
                intCell.set(JAVA_INT, 0, value)
                GodotStrings.initString(pathString, path.path)
                BuiltinTypes.construct(
                    type = VariantType.NODE_PATH,
                    dest = nodePath,
                    constructorIndex = 2,
                    args = listOf(pathString),
                )
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, intCell)
                arr.setAtIndex(ADDRESS, 1, nodePath)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.NODE_PATH, nodePath)
                GodotStrings.destroyString(pathString)
            }
        }
    }

    /**
     * Calls [methodBind] with (int32, Vector2) and no return value.
     */
    fun ptrcallWithIntAndVector2Arg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Int,
        vector: Vector2,
    ) {
        Arena.ofConfined().use { arena ->
            val intCell = arena.allocate(JAVA_INT)
            val vectorCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            intCell.set(JAVA_INT, 0, value)
            GodotReal.writeIndex(vectorCell, 0, vector.x)
            GodotReal.writeIndex(vectorCell, 1, vector.y)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, intCell)
            arr.setAtIndex(ADDRESS, 1, vectorCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithIntAndVector2iArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Int,
        vector: Vector2i,
    ) {
        Arena.ofConfined().use { arena ->
            val intCell = arena.allocate(JAVA_INT)
            intCell.set(JAVA_INT, 0, value)
            val vecCell = arena.allocate(8L, 4L)
            vecCell.set(JAVA_INT, 0, vector.x)
            vecCell.set(JAVA_INT, 4, vector.y)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, intCell)
            arr.setAtIndex(ADDRESS, 1, vecCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithIntAndVector2iArgRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Int,
        vector: Vector2i,
    ): Int {
        Arena.ofConfined().use { arena ->
            val intCell = arena.allocate(JAVA_INT)
            val vecCell = arena.allocate(8L, 4L)
            intCell.set(JAVA_INT, 0, value)
            writeVector2i(vecCell, vector)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, intCell)
            arr.setAtIndex(ADDRESS, 1, vecCell)
            val ret = arena.allocate(JAVA_INT)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_INT, 0)
        }
    }

    fun ptrcallWithIntAndVector2iArgRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Int,
        vector: Vector2i,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val intCell = arena.allocate(JAVA_INT)
            val vecCell = arena.allocate(8L, 4L)
            intCell.set(JAVA_INT, 0, value)
            writeVector2i(vecCell, vector)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, intCell)
            arr.setAtIndex(ADDRESS, 1, vecCell)
            val ret = arena.allocate(JAVA_BYTE)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_BYTE, 0) != 0.toByte()
        }
    }

    fun ptrcallWithIntVector2iArgsRetPackedInt32List(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Int,
        vector: Vector2i,
    ): List<Int> {
        Arena.ofConfined().use { arena ->
            val intCell = arena.allocate(JAVA_INT)
            val vecCell = arena.allocate(8L, 4L)
            intCell.set(JAVA_INT, 0, value)
            writeVector2i(vecCell, vector)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, intCell)
            arr.setAtIndex(ADDRESS, 1, vecCell)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return try {
                BuiltinTypes.readPackedInt32Array(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_INT32_ARRAY, ret)
            }
        }
    }

    fun ptrcallWithIntVector2iArgsRetArray(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Int,
        vector: Vector2i,
    ): List<Any?> {
        Arena.ofConfined().use { arena ->
            val intCell = arena.allocate(JAVA_INT)
            val vecCell = arena.allocate(8L, 4L)
            intCell.set(JAVA_INT, 0, value)
            writeVector2i(vecCell, vector)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, intCell)
            arr.setAtIndex(ADDRESS, 1, vecCell)
            return callArrayReturn(methodBind, instance, arr, BuiltinTypes::readArrayScalars)
        }
    }

    /**
     * Calls [methodBind] with (StringName, int64) and no return value.
     */
    fun ptrcallWithStringNameAndLongArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
        value: Long,
    ) {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(name))
            val intCell = arena.allocate(JAVA_LONG)
            intCell.set(JAVA_LONG, 0, value)
            arr.setAtIndex(ADDRESS, 1, intCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (StringName, int32) and no return value.
     */
    fun ptrcallWithStringNameAndIntArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
        value: Int,
    ) {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(name))
            val intCell = arena.allocate(JAVA_INT)
            intCell.set(JAVA_INT, 0, value)
            arr.setAtIndex(ADDRESS, 1, intCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithStringNameAndIntArgRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
        value: Int,
    ): Int {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(name))
            val intCell = arena.allocate(JAVA_INT)
            intCell.set(JAVA_INT, 0, value)
            arr.setAtIndex(ADDRESS, 1, intCell)
            val ret = arena.allocate(JAVA_INT)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_INT, 0)
        }
    }

    fun ptrcallWithStringNameAndIntArgRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
        value: Int,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val intCell = arena.allocate(JAVA_INT)
            intCell.set(JAVA_INT, 0, value)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(name))
            arr.setAtIndex(ADDRESS, 1, intCell)
            val ret = arena.allocate(ADDRESS)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(ADDRESS, 0)
        }
    }

    /**
     * Calls [methodBind] with (int64, StringName, StringName) and no return value.
     */
    fun ptrcallWithLongAndTwoStringNameArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Long,
        first: String,
        second: String,
    ) {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 3)
            val intCell = arena.allocate(JAVA_LONG)
            intCell.set(JAVA_LONG, 0, value)
            arr.setAtIndex(ADDRESS, 0, intCell)
            arr.setAtIndex(ADDRESS, 1, GodotStrings.makeStringName(first))
            arr.setAtIndex(ADDRESS, 2, GodotStrings.makeStringName(second))
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithLongAndTwoStringNameArgsRetVariantScalar(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Long,
        first: String,
        second: String,
    ): Any? {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 3)
            val intCell = arena.allocate(JAVA_LONG)
            intCell.set(JAVA_LONG, 0, value)
            arr.setAtIndex(ADDRESS, 0, intCell)
            arr.setAtIndex(ADDRESS, 1, GodotStrings.makeStringName(first))
            arr.setAtIndex(ADDRESS, 2, GodotStrings.makeStringName(second))
            return readVariantReturn(methodBind, instance, arr, arena)
        }
    }

    fun ptrcallWithLongAndTwoStringNameAndVariantArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Long,
        first: String,
        second: String,
        variantValue: Any?,
    ) {
        Arena.ofConfined().use { arena ->
            val intCell = arena.allocate(JAVA_LONG)
            val variant = initVariantCell(arena, variantValue)
            try {
                intCell.set(JAVA_LONG, 0, value)
                val arr = arena.allocate(ADDRESS, 4)
                arr.setAtIndex(ADDRESS, 0, intCell)
                arr.setAtIndex(ADDRESS, 1, GodotStrings.makeStringName(first))
                arr.setAtIndex(ADDRESS, 2, GodotStrings.makeStringName(second))
                arr.setAtIndex(ADDRESS, 3, variant)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyVariant(variant)
            }
        }
    }

    /**
     * Calls [methodBind] with (int64, StringName, int64) and no return value.
     */
    fun ptrcallWithLongStringNameAndLongArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Long,
        name: String,
        second: Long,
    ) {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 3)
            val int0 = arena.allocate(JAVA_LONG)
            val int1 = arena.allocate(JAVA_LONG)
            int0.set(JAVA_LONG, 0, first)
            int1.set(JAVA_LONG, 0, second)
            arr.setAtIndex(ADDRESS, 0, int0)
            arr.setAtIndex(ADDRESS, 1, GodotStrings.makeStringName(name))
            arr.setAtIndex(ADDRESS, 2, int1)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (int64, StringName, int32) and no return value.
     */
    fun ptrcallWithLongStringNameAndIntArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Long,
        name: String,
        second: Int,
    ) {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 3)
            val int0 = arena.allocate(JAVA_LONG)
            val int1 = arena.allocate(JAVA_INT)
            int0.set(JAVA_LONG, 0, first)
            int1.set(JAVA_INT, 0, second)
            arr.setAtIndex(ADDRESS, 0, int0)
            arr.setAtIndex(ADDRESS, 1, GodotStrings.makeStringName(name))
            arr.setAtIndex(ADDRESS, 2, int1)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithLongStringVariantLongStringIntArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstLong: Long,
        firstText: String,
        value: Any?,
        secondLong: Long,
        secondText: String,
        intValue: Int,
    ) {
        Arena.ofConfined().use { arena ->
            val firstLongCell = arena.allocate(JAVA_LONG)
            val firstStringCell = arena.allocate(8L, 8L)
            val variant = initVariantCell(arena, value)
            val secondLongCell = arena.allocate(JAVA_LONG)
            val secondStringCell = arena.allocate(8L, 8L)
            val intCell = arena.allocate(JAVA_INT)
            try {
                firstLongCell.set(JAVA_LONG, 0, firstLong)
                GodotStrings.initString(firstStringCell, firstText)
                secondLongCell.set(JAVA_LONG, 0, secondLong)
                GodotStrings.initString(secondStringCell, secondText)
                intCell.set(JAVA_INT, 0, intValue)
                val args = arena.allocate(ADDRESS, 6)
                args.setAtIndex(ADDRESS, 0, firstLongCell)
                args.setAtIndex(ADDRESS, 1, firstStringCell)
                args.setAtIndex(ADDRESS, 2, variant)
                args.setAtIndex(ADDRESS, 3, secondLongCell)
                args.setAtIndex(ADDRESS, 4, secondStringCell)
                args.setAtIndex(ADDRESS, 5, intCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyVariant(variant)
                GodotStrings.destroyString(secondStringCell)
                GodotStrings.destroyString(firstStringCell)
            }
        }
    }

    fun ptrcallWithLongTwoStringColorObjectVariantIntArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        longValue: Long,
        firstText: String,
        secondText: String,
        color: Color,
        objectArg: MemorySegment,
        variantValue: Any?,
        intValue: Int,
    ) {
        Arena.ofConfined().use { arena ->
            val longCell = arena.allocate(JAVA_LONG)
            val firstStringCell = arena.allocate(8L, 8L)
            val secondStringCell = arena.allocate(8L, 8L)
            val colorCell = arena.allocate(16L, 4L)
            val objectCell = arena.allocate(ADDRESS)
            val variant = initVariantCell(arena, variantValue)
            val intCell = arena.allocate(JAVA_INT)
            try {
                longCell.set(JAVA_LONG, 0, longValue)
                GodotStrings.initString(firstStringCell, firstText)
                GodotStrings.initString(secondStringCell, secondText)
                colorCell.set(JAVA_FLOAT, 0, color.r)
                colorCell.set(JAVA_FLOAT, 4, color.g)
                colorCell.set(JAVA_FLOAT, 8, color.b)
                colorCell.set(JAVA_FLOAT, 12, color.a)
                objectCell.set(ADDRESS, 0, objectArg)
                intCell.set(JAVA_INT, 0, intValue)
                val args = arena.allocate(ADDRESS, 7)
                args.setAtIndex(ADDRESS, 0, longCell)
                args.setAtIndex(ADDRESS, 1, firstStringCell)
                args.setAtIndex(ADDRESS, 2, secondStringCell)
                args.setAtIndex(ADDRESS, 3, colorCell)
                args.setAtIndex(ADDRESS, 4, objectCell)
                args.setAtIndex(ADDRESS, 5, variant)
                args.setAtIndex(ADDRESS, 6, intCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyVariant(variant)
                GodotStrings.destroyString(secondStringCell)
                GodotStrings.destroyString(firstStringCell)
            }
        }
    }

    /**
     * Calls [methodBind] with (uint32, StringName, int32) and no return value.
     */
    fun ptrcallWithUInt32StringNameAndIntArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Long,
        name: String,
        second: Int,
    ) {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 3)
            val int0 = arena.allocate(JAVA_INT)
            val int1 = arena.allocate(JAVA_INT)
            int0.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(first))
            int1.set(JAVA_INT, 0, second)
            arr.setAtIndex(ADDRESS, 0, int0)
            arr.setAtIndex(ADDRESS, 1, GodotStrings.makeStringName(name))
            arr.setAtIndex(ADDRESS, 2, int1)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (int32, StringName, int32) and no return value.
     */
    fun ptrcallWithIntStringNameAndIntArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        name: String,
        second: Int,
    ) {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 3)
            val int0 = arena.allocate(JAVA_INT)
            val int1 = arena.allocate(JAVA_INT)
            int0.set(JAVA_INT, 0, first)
            int1.set(JAVA_INT, 0, second)
            arr.setAtIndex(ADDRESS, 0, int0)
            arr.setAtIndex(ADDRESS, 1, GodotStrings.makeStringName(name))
            arr.setAtIndex(ADDRESS, 2, int1)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (StringName, Object) and no return value.
     */
    fun ptrcallWithStringNameAndObjectArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
        objectArg: MemorySegment,
    ) {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(name))
            val objCell = arena.allocate(ADDRESS)
            objCell.set(ADDRESS, 0, objectArg)
            arr.setAtIndex(ADDRESS, 1, objCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (StringName, Object) and int64/enum return value.
     */
    fun ptrcallWithStringNameAndObjectArgRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
        objectArg: MemorySegment,
    ): Long {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(name))
            val objCell = arena.allocate(ADDRESS)
            objCell.set(ADDRESS, 0, objectArg)
            arr.setAtIndex(ADDRESS, 1, objCell)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_LONG, 0)
        }
    }

    /**
     * Calls [methodBind] with (StringName, Object, Vector2) and no return value.
     */
    fun ptrcallWithStringNameObjectAndVector2Arg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
        objectArg: MemorySegment,
        vector: Vector2,
    ) {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(name))

            val objCell = arena.allocate(ADDRESS)
            objCell.set(ADDRESS, 0, objectArg)
            arr.setAtIndex(ADDRESS, 1, objCell)

            val vectorCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            GodotReal.writeIndex(vectorCell, 0, vector.x)
            GodotReal.writeIndex(vectorCell, 1, vector.y)
            arr.setAtIndex(ADDRESS, 2, vectorCell)

            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (StringName, StringName, Object) and no return value.
     */
    fun ptrcallWithTwoStringNameAndObjectArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstName: String,
        secondName: String,
        objectArg: MemorySegment,
    ) {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(firstName))
            arr.setAtIndex(ADDRESS, 1, GodotStrings.makeStringName(secondName))
            val objCell = arena.allocate(ADDRESS)
            objCell.set(ADDRESS, 0, objectArg)
            arr.setAtIndex(ADDRESS, 2, objCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (StringName, Callable) and no return value.
     */
    fun ptrcallWithStringNameAndCallableArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
        callableObject: MemorySegment,
        callableMethod: String,
    ) {
        Arena.ofConfined().use { arena ->
            val callable = BuiltinTypes.allocateCallable(arena)
            try {
                BuiltinTypes.initCallable(callable, callableObject, callableMethod)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(name))
                arr.setAtIndex(ADDRESS, 1, callable)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, callable)
            }
        }
    }

    /**
     * Calls [methodBind] with (StringName, Callable, int64) and int64 return value.
     */
    fun ptrcallWithStringNameCallableAndLongArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
        callableObject: MemorySegment,
        callableMethod: String,
        flags: Long,
    ): Long {
        Arena.ofConfined().use { arena ->
            val callable = BuiltinTypes.allocateCallable(arena)
            val flagsArg = arena.allocate(JAVA_LONG)
            try {
                BuiltinTypes.initCallable(callable, callableObject, callableMethod)
                flagsArg.set(JAVA_LONG, 0, flags)
                val arr = arena.allocate(ADDRESS, 3)
                arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(name))
                arr.setAtIndex(ADDRESS, 1, callable)
                arr.setAtIndex(ADDRESS, 2, flagsArg)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, callable)
            }
        }
    }

    /**
     * Calls [methodBind] with (StringName, Callable, uint32) and int64 return value.
     */
    fun ptrcallWithStringNameCallableAndUInt32ArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
        callableObject: MemorySegment,
        callableMethod: String,
        flags: Long,
    ): Long {
        Arena.ofConfined().use { arena ->
            val callable = BuiltinTypes.allocateCallable(arena)
            val flagsArg = arena.allocate(JAVA_INT)
            try {
                BuiltinTypes.initCallable(callable, callableObject, callableMethod)
                flagsArg.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(flags))
                val arr = arena.allocate(ADDRESS, 3)
                arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(name))
                arr.setAtIndex(ADDRESS, 1, callable)
                arr.setAtIndex(ADDRESS, 2, flagsArg)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, callable)
            }
        }
    }

    /**
     * Calls [methodBind] with (StringName, Callable.bindv(boundArgs), uint32)
     * and int64 return value.
     */
    fun ptrcallWithStringNameBoundCallableAndUInt32ArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
        callableObject: MemorySegment,
        callableMethod: String,
        boundArgs: List<Any?>,
        flags: Long,
    ): Long {
        val bindvHash = 3564560322L
        Arena.ofConfined().use { arena ->
            val callable = BuiltinTypes.allocateCallable(arena)
            val boundArray = arena.allocate(8L, 8L)
            val boundCallable = BuiltinTypes.allocateCallable(arena)
            val flagsArg = arena.allocate(JAVA_INT)
            try {
                BuiltinTypes.initCallable(callable, callableObject, callableMethod)
                BuiltinTypes.initArray(boundArray, boundArgs)
                BuiltinTypes.call(
                    type = VariantType.CALLABLE,
                    method = "bindv",
                    hash = bindvHash,
                    base = callable,
                    args = listOf(boundArray),
                    rReturn = boundCallable,
                )
                flagsArg.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(flags))
                val arr = arena.allocate(ADDRESS, 3)
                arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(name))
                arr.setAtIndex(ADDRESS, 1, boundCallable)
                arr.setAtIndex(ADDRESS, 2, flagsArg)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, boundCallable)
                BuiltinTypes.destroyTyped(VariantType.ARRAY, boundArray)
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, callable)
            }
        }
    }

    /**
     * Calls [methodBind] with (StringName, Callable) and no return value.
     */
    fun ptrcallWithStringNameAndCallableArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
        callableObject: MemorySegment,
        callableMethod: String,
    ) {
        Arena.ofConfined().use { arena ->
            val callable = BuiltinTypes.allocateCallable(arena)
            try {
                BuiltinTypes.initCallable(callable, callableObject, callableMethod)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(name))
                arr.setAtIndex(ADDRESS, 1, callable)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, callable)
            }
        }
    }

    /**
     * Calls [methodBind] with (StringName, Callable) and bool return value.
     */
    fun ptrcallWithStringNameAndCallableArgsRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
        callableObject: MemorySegment,
        callableMethod: String,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val callable = BuiltinTypes.allocateCallable(arena)
            try {
                BuiltinTypes.initCallable(callable, callableObject, callableMethod)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(name))
                arr.setAtIndex(ADDRESS, 1, callable)
                val ret = arena.allocate(JAVA_BYTE)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_BYTE, 0) != 0.toByte()
            } finally {
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, callable)
            }
        }
    }

    fun ptrcallWithCallableArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        callableObject: MemorySegment,
        callableMethod: String,
    ) {
        Arena.ofConfined().use { arena ->
            val callable = BuiltinTypes.allocateCallable(arena)
            try {
                BuiltinTypes.initCallable(callable, callableObject, callableMethod)
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, callable)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, callable)
            }
        }
    }

    fun ptrcallWithCallableArgRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        callableObject: MemorySegment,
        callableMethod: String,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val callable = BuiltinTypes.allocateCallable(arena)
            try {
                BuiltinTypes.initCallable(callable, callableObject, callableMethod)
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, callable)
                val ret = arena.allocate(JAVA_BYTE)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_BYTE, 0) != 0.toByte()
            } finally {
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, callable)
            }
        }
    }

    fun ptrcallWithCallableIntArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        callableObject: MemorySegment,
        callableMethod: String,
        value: Int,
    ) {
        Arena.ofConfined().use { arena ->
            val callable = BuiltinTypes.allocateCallable(arena)
            val valueCell = arena.allocate(JAVA_INT)
            try {
                BuiltinTypes.initCallable(callable, callableObject, callableMethod)
                valueCell.set(JAVA_INT, 0, value)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, callable)
                arr.setAtIndex(ADDRESS, 1, valueCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, callable)
            }
        }
    }

    fun ptrcallWithCallableLongArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        callableObject: MemorySegment,
        callableMethod: String,
        value: Long,
    ): Long {
        Arena.ofConfined().use { arena ->
            val callable = BuiltinTypes.allocateCallable(arena)
            val valueCell = arena.allocate(JAVA_LONG)
            try {
                BuiltinTypes.initCallable(callable, callableObject, callableMethod)
                valueCell.set(JAVA_LONG, 0, value)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, callable)
                arr.setAtIndex(ADDRESS, 1, valueCell)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, callable)
            }
        }
    }

    fun ptrcallWithIntCallableArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Int,
        callableObject: MemorySegment,
        callableMethod: String,
    ) {
        Arena.ofConfined().use { arena ->
            val valueCell = arena.allocate(JAVA_INT)
            val callable = BuiltinTypes.allocateCallable(arena)
            try {
                valueCell.set(JAVA_INT, 0, value)
                BuiltinTypes.initCallable(callable, callableObject, callableMethod)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, valueCell)
                arr.setAtIndex(ADDRESS, 1, callable)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, callable)
            }
        }
    }

    fun ptrcallWithLongCallableArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Long,
        callableObject: MemorySegment,
        callableMethod: String,
    ) {
        Arena.ofConfined().use { arena ->
            val valueCell = arena.allocate(JAVA_LONG)
            val callable = BuiltinTypes.allocateCallable(arena)
            try {
                valueCell.set(JAVA_LONG, 0, value)
                BuiltinTypes.initCallable(callable, callableObject, callableMethod)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, valueCell)
                arr.setAtIndex(ADDRESS, 1, callable)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, callable)
            }
        }
    }

    fun ptrcallWithRIDCallableArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        callableObject: MemorySegment,
        callableMethod: String,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val callable = BuiltinTypes.allocateCallable(arena)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                BuiltinTypes.initCallable(callable, callableObject, callableMethod)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, ridCell)
                arr.setAtIndex(ADDRESS, 1, callable)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, callable)
            }
        }
    }

    fun ptrcallWithRIDIntCallableArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Int,
        callableObject: MemorySegment,
        callableMethod: String,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val valueCell = arena.allocate(JAVA_INT)
            val callable = BuiltinTypes.allocateCallable(arena)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                valueCell.set(JAVA_INT, 0, value)
                BuiltinTypes.initCallable(callable, callableObject, callableMethod)
                val arr = arena.allocate(ADDRESS, 3)
                arr.setAtIndex(ADDRESS, 0, ridCell)
                arr.setAtIndex(ADDRESS, 1, valueCell)
                arr.setAtIndex(ADDRESS, 2, callable)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, callable)
            }
        }
    }

    fun ptrcallWithRIDLongCallableArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Long,
        callableObject: MemorySegment,
        callableMethod: String,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val valueCell = arena.allocate(JAVA_LONG)
            val callable = BuiltinTypes.allocateCallable(arena)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                valueCell.set(JAVA_LONG, 0, value)
                BuiltinTypes.initCallable(callable, callableObject, callableMethod)
                val arr = arena.allocate(ADDRESS, 3)
                arr.setAtIndex(ADDRESS, 0, ridCell)
                arr.setAtIndex(ADDRESS, 1, valueCell)
                arr.setAtIndex(ADDRESS, 2, callable)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, callable)
            }
        }
    }

    fun ptrcallWithStringCallableArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: String,
        callableObject: MemorySegment,
        callableMethod: String,
    ) {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val callable = BuiltinTypes.allocateCallable(arena)
            try {
                GodotStrings.initString(stringCell, value)
                BuiltinTypes.initCallable(callable, callableObject, callableMethod)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, stringCell)
                arr.setAtIndex(ADDRESS, 1, callable)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, callable)
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithStringIntCallableArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: String,
        index: Int,
        callableObject: MemorySegment,
        callableMethod: String,
    ) {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val indexCell = arena.allocate(JAVA_INT)
            val callable = BuiltinTypes.allocateCallable(arena)
            try {
                GodotStrings.initString(stringCell, value)
                indexCell.set(JAVA_INT, 0, index)
                BuiltinTypes.initCallable(callable, callableObject, callableMethod)
                val arr = arena.allocate(ADDRESS, 3)
                arr.setAtIndex(ADDRESS, 0, stringCell)
                arr.setAtIndex(ADDRESS, 1, indexCell)
                arr.setAtIndex(ADDRESS, 2, callable)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, callable)
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithObjectCallableArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        callableObject: MemorySegment,
        callableMethod: String,
    ) {
        Arena.ofConfined().use { arena ->
            val objectCell = arena.allocate(ADDRESS)
            val callable = BuiltinTypes.allocateCallable(arena)
            try {
                objectCell.set(ADDRESS, 0, objectArg)
                BuiltinTypes.initCallable(callable, callableObject, callableMethod)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, objectCell)
                arr.setAtIndex(ADDRESS, 1, callable)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, callable)
            }
        }
    }

    fun ptrcallWithObjectCallableStringArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        callableObject: MemorySegment,
        callableMethod: String,
        value: String,
    ) {
        Arena.ofConfined().use { arena ->
            val objectCell = arena.allocate(ADDRESS)
            val callable = BuiltinTypes.allocateCallable(arena)
            val stringCell = arena.allocate(8L, 8L)
            try {
                objectCell.set(ADDRESS, 0, objectArg)
                BuiltinTypes.initCallable(callable, callableObject, callableMethod)
                GodotStrings.initString(stringCell, value)
                val arr = arena.allocate(ADDRESS, 3)
                arr.setAtIndex(ADDRESS, 0, objectCell)
                arr.setAtIndex(ADDRESS, 1, callable)
                arr.setAtIndex(ADDRESS, 2, stringCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(stringCell)
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, callable)
            }
        }
    }

    fun ptrcallWithObjectRIDCallableArgsRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        rid: RID,
        callableObject: MemorySegment,
        callableMethod: String,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val objectCell = arena.allocate(ADDRESS)
            val ridCell = arena.allocate(JAVA_LONG)
            val callable = BuiltinTypes.allocateCallable(arena)
            try {
                objectCell.set(ADDRESS, 0, objectArg)
                ridCell.set(JAVA_LONG, 0, rid.value)
                BuiltinTypes.initCallable(callable, callableObject, callableMethod)
                val arr = arena.allocate(ADDRESS, 3)
                arr.setAtIndex(ADDRESS, 0, objectCell)
                arr.setAtIndex(ADDRESS, 1, ridCell)
                arr.setAtIndex(ADDRESS, 2, callable)
                val ret = arena.allocate(ADDRESS)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(ADDRESS, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, callable)
            }
        }
    }

    fun ptrcallWithStringCallableObjectArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: String,
        callableObject: MemorySegment,
        callableMethod: String,
        objectArg: MemorySegment,
    ) {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val callable = BuiltinTypes.allocateCallable(arena)
            val objectCell = arena.allocate(ADDRESS)
            try {
                GodotStrings.initString(stringCell, value)
                BuiltinTypes.initCallable(callable, callableObject, callableMethod)
                objectCell.set(ADDRESS, 0, objectArg)
                val arr = arena.allocate(ADDRESS, 3)
                arr.setAtIndex(ADDRESS, 0, stringCell)
                arr.setAtIndex(ADDRESS, 1, callable)
                arr.setAtIndex(ADDRESS, 2, objectCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, callable)
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithTwoObjectCallableArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstObject: MemorySegment,
        secondObject: MemorySegment,
        callableObject: MemorySegment,
        callableMethod: String,
    ) {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(ADDRESS)
            val secondCell = arena.allocate(ADDRESS)
            val callable = BuiltinTypes.allocateCallable(arena)
            try {
                firstCell.set(ADDRESS, 0, firstObject)
                secondCell.set(ADDRESS, 0, secondObject)
                BuiltinTypes.initCallable(callable, callableObject, callableMethod)
                val arr = arena.allocate(ADDRESS, 3)
                arr.setAtIndex(ADDRESS, 0, firstCell)
                arr.setAtIndex(ADDRESS, 1, secondCell)
                arr.setAtIndex(ADDRESS, 2, callable)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, callable)
            }
        }
    }

    fun ptrcallWithThreeObjectCallableArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstObject: MemorySegment,
        secondObject: MemorySegment,
        thirdObject: MemorySegment,
        callableObject: MemorySegment,
        callableMethod: String,
    ) {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(ADDRESS)
            val secondCell = arena.allocate(ADDRESS)
            val thirdCell = arena.allocate(ADDRESS)
            val callable = BuiltinTypes.allocateCallable(arena)
            try {
                firstCell.set(ADDRESS, 0, firstObject)
                secondCell.set(ADDRESS, 0, secondObject)
                thirdCell.set(ADDRESS, 0, thirdObject)
                BuiltinTypes.initCallable(callable, callableObject, callableMethod)
                val arr = arena.allocate(ADDRESS, 4)
                arr.setAtIndex(ADDRESS, 0, firstCell)
                arr.setAtIndex(ADDRESS, 1, secondCell)
                arr.setAtIndex(ADDRESS, 2, thirdCell)
                arr.setAtIndex(ADDRESS, 3, callable)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, callable)
            }
        }
    }

    fun ptrcallWithTwoStringTwoCallableVariantLongIntArgsRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstString: String,
        secondString: String,
        firstCallableObject: MemorySegment,
        firstCallableMethod: String,
        secondCallableObject: MemorySegment,
        secondCallableMethod: String,
        data: Any?,
        flags: Long,
        index: Int,
    ): Int {
        Arena.ofConfined().use { arena ->
            val firstStringCell = arena.allocate(8L, 8L)
            val secondStringCell = arena.allocate(8L, 8L)
            val firstCallable = BuiltinTypes.allocateCallable(arena)
            val secondCallable = BuiltinTypes.allocateCallable(arena)
            val variantCell = arena.allocate(BuiltinTypes.VARIANT_SIZE, 8L)
            val flagsCell = arena.allocate(JAVA_LONG)
            val indexCell = arena.allocate(JAVA_INT)
            var variantInitialized = false
            try {
                GodotStrings.initString(firstStringCell, firstString)
                GodotStrings.initString(secondStringCell, secondString)
                BuiltinTypes.initCallable(firstCallable, firstCallableObject, firstCallableMethod)
                BuiltinTypes.initCallable(secondCallable, secondCallableObject, secondCallableMethod)
                BuiltinTypes.initVariantFromAny(variantCell, data, arena)
                variantInitialized = true
                flagsCell.set(JAVA_LONG, 0, flags)
                indexCell.set(JAVA_INT, 0, index)
                val arr = arena.allocate(ADDRESS, 7)
                arr.setAtIndex(ADDRESS, 0, firstStringCell)
                arr.setAtIndex(ADDRESS, 1, secondStringCell)
                arr.setAtIndex(ADDRESS, 2, firstCallable)
                arr.setAtIndex(ADDRESS, 3, secondCallable)
                arr.setAtIndex(ADDRESS, 4, variantCell)
                arr.setAtIndex(ADDRESS, 5, flagsCell)
                arr.setAtIndex(ADDRESS, 6, indexCell)
                val ret = arena.allocate(JAVA_INT)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_INT, 0)
            } finally {
                if (variantInitialized) {
                    BuiltinTypes.destroyVariant(variantCell)
                }
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, secondCallable)
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, firstCallable)
                GodotStrings.destroyString(secondStringCell)
                GodotStrings.destroyString(firstStringCell)
            }
        }
    }

    fun ptrcallWithStringObjectStringTwoCallableVariantLongIntArgsRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstString: String,
        objectArg: MemorySegment,
        secondString: String,
        firstCallableObject: MemorySegment,
        firstCallableMethod: String,
        secondCallableObject: MemorySegment,
        secondCallableMethod: String,
        data: Any?,
        flags: Long,
        index: Int,
    ): Int {
        Arena.ofConfined().use { arena ->
            val firstStringCell = arena.allocate(8L, 8L)
            val objectCell = arena.allocate(ADDRESS)
            val secondStringCell = arena.allocate(8L, 8L)
            val firstCallable = BuiltinTypes.allocateCallable(arena)
            val secondCallable = BuiltinTypes.allocateCallable(arena)
            val variantCell = arena.allocate(BuiltinTypes.VARIANT_SIZE, 8L)
            val flagsCell = arena.allocate(JAVA_LONG)
            val indexCell = arena.allocate(JAVA_INT)
            var variantInitialized = false
            try {
                GodotStrings.initString(firstStringCell, firstString)
                objectCell.set(ADDRESS, 0, objectArg)
                GodotStrings.initString(secondStringCell, secondString)
                BuiltinTypes.initCallable(firstCallable, firstCallableObject, firstCallableMethod)
                BuiltinTypes.initCallable(secondCallable, secondCallableObject, secondCallableMethod)
                BuiltinTypes.initVariantFromAny(variantCell, data, arena)
                variantInitialized = true
                flagsCell.set(JAVA_LONG, 0, flags)
                indexCell.set(JAVA_INT, 0, index)
                val arr = arena.allocate(ADDRESS, 8)
                arr.setAtIndex(ADDRESS, 0, firstStringCell)
                arr.setAtIndex(ADDRESS, 1, objectCell)
                arr.setAtIndex(ADDRESS, 2, secondStringCell)
                arr.setAtIndex(ADDRESS, 3, firstCallable)
                arr.setAtIndex(ADDRESS, 4, secondCallable)
                arr.setAtIndex(ADDRESS, 5, variantCell)
                arr.setAtIndex(ADDRESS, 6, flagsCell)
                arr.setAtIndex(ADDRESS, 7, indexCell)
                val ret = arena.allocate(JAVA_INT)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_INT, 0)
            } finally {
                if (variantInitialized) {
                    BuiltinTypes.destroyVariant(variantCell)
                }
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, secondCallable)
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, firstCallable)
                GodotStrings.destroyString(secondStringCell)
                GodotStrings.destroyString(firstStringCell)
            }
        }
    }

    fun ptrcallWithRIDStringTwoCallableVariantLongIntArgsRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: String,
        firstCallableObject: MemorySegment,
        firstCallableMethod: String,
        secondCallableObject: MemorySegment,
        secondCallableMethod: String,
        data: Any?,
        flags: Long,
        index: Int,
    ): Int {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val stringCell = arena.allocate(8L, 8L)
            val firstCallable = BuiltinTypes.allocateCallable(arena)
            val secondCallable = BuiltinTypes.allocateCallable(arena)
            val variantCell = arena.allocate(BuiltinTypes.VARIANT_SIZE, 8L)
            val flagsCell = arena.allocate(JAVA_LONG)
            val indexCell = arena.allocate(JAVA_INT)
            var variantInitialized = false
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                GodotStrings.initString(stringCell, value)
                BuiltinTypes.initCallable(firstCallable, firstCallableObject, firstCallableMethod)
                BuiltinTypes.initCallable(secondCallable, secondCallableObject, secondCallableMethod)
                BuiltinTypes.initVariantFromAny(variantCell, data, arena)
                variantInitialized = true
                flagsCell.set(JAVA_LONG, 0, flags)
                indexCell.set(JAVA_INT, 0, index)
                val arr = arena.allocate(ADDRESS, 7)
                arr.setAtIndex(ADDRESS, 0, ridCell)
                arr.setAtIndex(ADDRESS, 1, stringCell)
                arr.setAtIndex(ADDRESS, 2, firstCallable)
                arr.setAtIndex(ADDRESS, 3, secondCallable)
                arr.setAtIndex(ADDRESS, 4, variantCell)
                arr.setAtIndex(ADDRESS, 5, flagsCell)
                arr.setAtIndex(ADDRESS, 6, indexCell)
                val ret = arena.allocate(JAVA_INT)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_INT, 0)
            } finally {
                if (variantInitialized) {
                    BuiltinTypes.destroyVariant(variantCell)
                }
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, secondCallable)
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, firstCallable)
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithRIDObjectStringTwoCallableVariantLongIntArgsRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        objectArg: MemorySegment,
        value: String,
        firstCallableObject: MemorySegment,
        firstCallableMethod: String,
        secondCallableObject: MemorySegment,
        secondCallableMethod: String,
        data: Any?,
        flags: Long,
        index: Int,
    ): Int {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val objectCell = arena.allocate(ADDRESS)
            val stringCell = arena.allocate(8L, 8L)
            val firstCallable = BuiltinTypes.allocateCallable(arena)
            val secondCallable = BuiltinTypes.allocateCallable(arena)
            val variantCell = arena.allocate(BuiltinTypes.VARIANT_SIZE, 8L)
            val flagsCell = arena.allocate(JAVA_LONG)
            val indexCell = arena.allocate(JAVA_INT)
            var variantInitialized = false
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                objectCell.set(ADDRESS, 0, objectArg)
                GodotStrings.initString(stringCell, value)
                BuiltinTypes.initCallable(firstCallable, firstCallableObject, firstCallableMethod)
                BuiltinTypes.initCallable(secondCallable, secondCallableObject, secondCallableMethod)
                BuiltinTypes.initVariantFromAny(variantCell, data, arena)
                variantInitialized = true
                flagsCell.set(JAVA_LONG, 0, flags)
                indexCell.set(JAVA_INT, 0, index)
                val arr = arena.allocate(ADDRESS, 8)
                arr.setAtIndex(ADDRESS, 0, ridCell)
                arr.setAtIndex(ADDRESS, 1, objectCell)
                arr.setAtIndex(ADDRESS, 2, stringCell)
                arr.setAtIndex(ADDRESS, 3, firstCallable)
                arr.setAtIndex(ADDRESS, 4, secondCallable)
                arr.setAtIndex(ADDRESS, 5, variantCell)
                arr.setAtIndex(ADDRESS, 6, flagsCell)
                arr.setAtIndex(ADDRESS, 7, indexCell)
                val ret = arena.allocate(JAVA_INT)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_INT, 0)
            } finally {
                if (variantInitialized) {
                    BuiltinTypes.destroyVariant(variantCell)
                }
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, secondCallable)
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, firstCallable)
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithTwoCallableArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstCallableObject: MemorySegment,
        firstCallableMethod: String,
        secondCallableObject: MemorySegment,
        secondCallableMethod: String,
    ) {
        Arena.ofConfined().use { arena ->
            val firstCallable = BuiltinTypes.allocateCallable(arena)
            val secondCallable = BuiltinTypes.allocateCallable(arena)
            try {
                BuiltinTypes.initCallable(firstCallable, firstCallableObject, firstCallableMethod)
                BuiltinTypes.initCallable(secondCallable, secondCallableObject, secondCallableMethod)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, firstCallable)
                arr.setAtIndex(ADDRESS, 1, secondCallable)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, secondCallable)
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, firstCallable)
            }
        }
    }

    fun ptrcallWithThreeCallableArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstCallableObject: MemorySegment,
        firstCallableMethod: String,
        secondCallableObject: MemorySegment,
        secondCallableMethod: String,
        thirdCallableObject: MemorySegment,
        thirdCallableMethod: String,
    ) {
        Arena.ofConfined().use { arena ->
            val firstCallable = BuiltinTypes.allocateCallable(arena)
            val secondCallable = BuiltinTypes.allocateCallable(arena)
            val thirdCallable = BuiltinTypes.allocateCallable(arena)
            try {
                BuiltinTypes.initCallable(firstCallable, firstCallableObject, firstCallableMethod)
                BuiltinTypes.initCallable(secondCallable, secondCallableObject, secondCallableMethod)
                BuiltinTypes.initCallable(thirdCallable, thirdCallableObject, thirdCallableMethod)
                val arr = arena.allocate(ADDRESS, 3)
                arr.setAtIndex(ADDRESS, 0, firstCallable)
                arr.setAtIndex(ADDRESS, 1, secondCallable)
                arr.setAtIndex(ADDRESS, 2, thirdCallable)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, thirdCallable)
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, secondCallable)
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, firstCallable)
            }
        }
    }

    fun ptrcallWithStringTwoCallableArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: String,
        firstCallableObject: MemorySegment,
        firstCallableMethod: String,
        secondCallableObject: MemorySegment,
        secondCallableMethod: String,
    ) {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val firstCallable = BuiltinTypes.allocateCallable(arena)
            val secondCallable = BuiltinTypes.allocateCallable(arena)
            try {
                GodotStrings.initString(stringCell, value)
                BuiltinTypes.initCallable(firstCallable, firstCallableObject, firstCallableMethod)
                BuiltinTypes.initCallable(secondCallable, secondCallableObject, secondCallableMethod)
                val arr = arena.allocate(ADDRESS, 3)
                arr.setAtIndex(ADDRESS, 0, stringCell)
                arr.setAtIndex(ADDRESS, 1, firstCallable)
                arr.setAtIndex(ADDRESS, 2, secondCallable)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, secondCallable)
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, firstCallable)
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithRIDTwoCallableArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        firstCallableObject: MemorySegment,
        firstCallableMethod: String,
        secondCallableObject: MemorySegment,
        secondCallableMethod: String,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val firstCallable = BuiltinTypes.allocateCallable(arena)
            val secondCallable = BuiltinTypes.allocateCallable(arena)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                BuiltinTypes.initCallable(firstCallable, firstCallableObject, firstCallableMethod)
                BuiltinTypes.initCallable(secondCallable, secondCallableObject, secondCallableMethod)
                val arr = arena.allocate(ADDRESS, 3)
                arr.setAtIndex(ADDRESS, 0, ridCell)
                arr.setAtIndex(ADDRESS, 1, firstCallable)
                arr.setAtIndex(ADDRESS, 2, secondCallable)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, secondCallable)
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, firstCallable)
            }
        }
    }

    fun ptrcallWithRIDCallableVariantArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        callableObject: MemorySegment,
        callableMethod: String,
        data: Any?,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val callable = BuiltinTypes.allocateCallable(arena)
            val variantCell = arena.allocate(BuiltinTypes.VARIANT_SIZE, 8L)
            var variantInitialized = false
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                BuiltinTypes.initCallable(callable, callableObject, callableMethod)
                BuiltinTypes.initVariantFromAny(variantCell, data, arena)
                variantInitialized = true
                val arr = arena.allocate(ADDRESS, 3)
                arr.setAtIndex(ADDRESS, 0, ridCell)
                arr.setAtIndex(ADDRESS, 1, callable)
                arr.setAtIndex(ADDRESS, 2, variantCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                if (variantInitialized) {
                    BuiltinTypes.destroyVariant(variantCell)
                }
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, callable)
            }
        }
    }

    fun ptrcallWithTwoStringCallableStringArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstString: String,
        secondString: String,
        callableObject: MemorySegment,
        callableMethod: String,
        thirdString: String,
    ) {
        Arena.ofConfined().use { arena ->
            val firstStringCell = arena.allocate(8L, 8L)
            val secondStringCell = arena.allocate(8L, 8L)
            val callable = BuiltinTypes.allocateCallable(arena)
            val thirdStringCell = arena.allocate(8L, 8L)
            try {
                GodotStrings.initString(firstStringCell, firstString)
                GodotStrings.initString(secondStringCell, secondString)
                BuiltinTypes.initCallable(callable, callableObject, callableMethod)
                GodotStrings.initString(thirdStringCell, thirdString)
                val arr = arena.allocate(ADDRESS, 4)
                arr.setAtIndex(ADDRESS, 0, firstStringCell)
                arr.setAtIndex(ADDRESS, 1, secondStringCell)
                arr.setAtIndex(ADDRESS, 2, callable)
                arr.setAtIndex(ADDRESS, 3, thirdStringCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(thirdStringCell)
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, callable)
                GodotStrings.destroyString(secondStringCell)
                GodotStrings.destroyString(firstStringCell)
            }
        }
    }

    fun ptrcallWithCallableBoolStringArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        callableObject: MemorySegment,
        callableMethod: String,
        value: Boolean,
        text: String,
    ): Long {
        Arena.ofConfined().use { arena ->
            val callable = BuiltinTypes.allocateCallable(arena)
            val boolCell = arena.allocate(JAVA_BYTE)
            val stringCell = arena.allocate(8L, 8L)
            try {
                BuiltinTypes.initCallable(callable, callableObject, callableMethod)
                boolCell.set(JAVA_BYTE, 0, if (value) 1 else 0)
                GodotStrings.initString(stringCell, text)
                val arr = arena.allocate(ADDRESS, 3)
                arr.setAtIndex(ADDRESS, 0, callable)
                arr.setAtIndex(ADDRESS, 1, boolCell)
                arr.setAtIndex(ADDRESS, 2, stringCell)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                GodotStrings.destroyString(stringCell)
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, callable)
            }
        }
    }

    fun ptrcallWithCallableTwoIntBoolStringArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        callableObject: MemorySegment,
        callableMethod: String,
        first: Int,
        second: Int,
        value: Boolean,
        text: String,
    ): Long {
        Arena.ofConfined().use { arena ->
            val callable = BuiltinTypes.allocateCallable(arena)
            val firstCell = arena.allocate(JAVA_INT)
            val secondCell = arena.allocate(JAVA_INT)
            val boolCell = arena.allocate(JAVA_BYTE)
            val stringCell = arena.allocate(8L, 8L)
            try {
                BuiltinTypes.initCallable(callable, callableObject, callableMethod)
                firstCell.set(JAVA_INT, 0, first)
                secondCell.set(JAVA_INT, 0, second)
                boolCell.set(JAVA_BYTE, 0, if (value) 1 else 0)
                GodotStrings.initString(stringCell, text)
                val arr = arena.allocate(ADDRESS, 5)
                arr.setAtIndex(ADDRESS, 0, callable)
                arr.setAtIndex(ADDRESS, 1, firstCell)
                arr.setAtIndex(ADDRESS, 2, secondCell)
                arr.setAtIndex(ADDRESS, 3, boolCell)
                arr.setAtIndex(ADDRESS, 4, stringCell)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                GodotStrings.destroyString(stringCell)
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, callable)
            }
        }
    }

    fun ptrcallWithObjectStringCallableArgsRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        text: String,
        callableObject: MemorySegment,
        callableMethod: String,
    ): Int {
        Arena.ofConfined().use { arena ->
            val objectCell = arena.allocate(ADDRESS)
            val stringCell = arena.allocate(8L, 8L)
            val callable = BuiltinTypes.allocateCallable(arena)
            try {
                objectCell.set(ADDRESS, 0, objectArg)
                GodotStrings.initString(stringCell, text)
                BuiltinTypes.initCallable(callable, callableObject, callableMethod)
                val arr = arena.allocate(ADDRESS, 3)
                arr.setAtIndex(ADDRESS, 0, objectCell)
                arr.setAtIndex(ADDRESS, 1, stringCell)
                arr.setAtIndex(ADDRESS, 2, callable)
                val ret = arena.allocate(JAVA_INT)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_INT, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, callable)
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithRIDUInt32CallableArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Long,
        callableObject: MemorySegment,
        callableMethod: String,
    ): Long {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val valueCell = arena.allocate(JAVA_INT)
            val callable = BuiltinTypes.allocateCallable(arena)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                valueCell.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(value))
                BuiltinTypes.initCallable(callable, callableObject, callableMethod)
                val arr = arena.allocate(ADDRESS, 3)
                arr.setAtIndex(ADDRESS, 0, ridCell)
                arr.setAtIndex(ADDRESS, 1, valueCell)
                arr.setAtIndex(ADDRESS, 2, callable)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, callable)
            }
        }
    }

    fun ptrcallWithRIDCallableTwoUInt32ArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        callableObject: MemorySegment,
        callableMethod: String,
        first: Long,
        second: Long,
    ): Long {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val callable = BuiltinTypes.allocateCallable(arena)
            val firstCell = arena.allocate(JAVA_INT)
            val secondCell = arena.allocate(JAVA_INT)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                BuiltinTypes.initCallable(callable, callableObject, callableMethod)
                firstCell.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(first))
                secondCell.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(second))
                val arr = arena.allocate(ADDRESS, 4)
                arr.setAtIndex(ADDRESS, 0, ridCell)
                arr.setAtIndex(ADDRESS, 1, callable)
                arr.setAtIndex(ADDRESS, 2, firstCell)
                arr.setAtIndex(ADDRESS, 3, secondCell)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, callable)
            }
        }
    }

    fun ptrcallWithObjectBoolTwoCallableArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        value: Boolean,
        firstCallableObject: MemorySegment,
        firstCallableMethod: String,
        secondCallableObject: MemorySegment,
        secondCallableMethod: String,
    ): Long {
        Arena.ofConfined().use { arena ->
            val objectCell = arena.allocate(ADDRESS)
            val boolCell = arena.allocate(JAVA_BYTE)
            val firstCallable = BuiltinTypes.allocateCallable(arena)
            val secondCallable = BuiltinTypes.allocateCallable(arena)
            try {
                objectCell.set(ADDRESS, 0, objectArg)
                boolCell.set(JAVA_BYTE, 0, if (value) 1 else 0)
                BuiltinTypes.initCallable(firstCallable, firstCallableObject, firstCallableMethod)
                BuiltinTypes.initCallable(secondCallable, secondCallableObject, secondCallableMethod)
                val arr = arena.allocate(ADDRESS, 4)
                arr.setAtIndex(ADDRESS, 0, objectCell)
                arr.setAtIndex(ADDRESS, 1, boolCell)
                arr.setAtIndex(ADDRESS, 2, firstCallable)
                arr.setAtIndex(ADDRESS, 3, secondCallable)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, secondCallable)
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, firstCallable)
            }
        }
    }

    fun ptrcallWithRIDBoolRect2TwoCallableArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Boolean,
        rect: Rect2,
        firstCallableObject: MemorySegment,
        firstCallableMethod: String,
        secondCallableObject: MemorySegment,
        secondCallableMethod: String,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val boolCell = arena.allocate(JAVA_BYTE)
            val rectCell = arena.allocate(16L, 4L)
            val firstCallable = BuiltinTypes.allocateCallable(arena)
            val secondCallable = BuiltinTypes.allocateCallable(arena)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                boolCell.set(JAVA_BYTE, 0, if (value) 1 else 0)
                writeRect2(rectCell, rect)
                BuiltinTypes.initCallable(firstCallable, firstCallableObject, firstCallableMethod)
                BuiltinTypes.initCallable(secondCallable, secondCallableObject, secondCallableMethod)
                val arr = arena.allocate(ADDRESS, 5)
                arr.setAtIndex(ADDRESS, 0, ridCell)
                arr.setAtIndex(ADDRESS, 1, boolCell)
                arr.setAtIndex(ADDRESS, 2, rectCell)
                arr.setAtIndex(ADDRESS, 3, firstCallable)
                arr.setAtIndex(ADDRESS, 4, secondCallable)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, secondCallable)
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, firstCallable)
            }
        }
    }

    fun ptrcallWithStringNameCallableArrayLongArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
        callableObject: MemorySegment,
        callableMethod: String,
        values: List<Any?>,
        value: Long,
    ) {
        Arena.ofConfined().use { arena ->
            val callable = BuiltinTypes.allocateCallable(arena)
            val arrayCell = arena.allocate(8L, 8L)
            val valueCell = arena.allocate(JAVA_LONG)
            try {
                BuiltinTypes.initCallable(callable, callableObject, callableMethod)
                BuiltinTypes.initArray(arrayCell, values)
                valueCell.set(JAVA_LONG, 0, value)
                val arr = arena.allocate(ADDRESS, 4)
                arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(name))
                arr.setAtIndex(ADDRESS, 1, callable)
                arr.setAtIndex(ADDRESS, 2, arrayCell)
                arr.setAtIndex(ADDRESS, 3, valueCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, arrayCell)
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, callable)
            }
        }
    }

    fun ptrcallWithCallableStringNameListArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        callableObject: MemorySegment,
        callableMethod: String,
        values: List<String>,
    ) {
        Arena.ofConfined().use { arena ->
            val callable = BuiltinTypes.allocateCallable(arena)
            val arrayCell = arena.allocate(8L, 8L)
            try {
                BuiltinTypes.initCallable(callable, callableObject, callableMethod)
                BuiltinTypes.initArrayOfStringNames(arrayCell, values)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, callable)
                arr.setAtIndex(ADDRESS, 1, arrayCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, arrayCell)
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, callable)
            }
        }
    }

    fun ptrcallWithCallableStringNameListObjectArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        callableObject: MemorySegment,
        callableMethod: String,
        values: List<String>,
        objectArg: MemorySegment,
    ) {
        Arena.ofConfined().use { arena ->
            val callable = BuiltinTypes.allocateCallable(arena)
            val arrayCell = arena.allocate(8L, 8L)
            val objectCell = arena.allocate(ADDRESS)
            try {
                BuiltinTypes.initCallable(callable, callableObject, callableMethod)
                BuiltinTypes.initArrayOfStringNames(arrayCell, values)
                objectCell.set(ADDRESS, 0, objectArg)
                val arr = arena.allocate(ADDRESS, 3)
                arr.setAtIndex(ADDRESS, 0, callable)
                arr.setAtIndex(ADDRESS, 1, arrayCell)
                arr.setAtIndex(ADDRESS, 2, objectCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, arrayCell)
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, callable)
            }
        }
    }

    fun ptrcallWithCallableStringNameTwoStringStringNameListArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        callableObject: MemorySegment,
        callableMethod: String,
        name: String,
        firstString: String,
        secondString: String,
        values: List<String>,
    ) {
        Arena.ofConfined().use { arena ->
            val callable = BuiltinTypes.allocateCallable(arena)
            val firstStringCell = arena.allocate(8L, 8L)
            val secondStringCell = arena.allocate(8L, 8L)
            val arrayCell = arena.allocate(8L, 8L)
            try {
                BuiltinTypes.initCallable(callable, callableObject, callableMethod)
                GodotStrings.initString(firstStringCell, firstString)
                GodotStrings.initString(secondStringCell, secondString)
                BuiltinTypes.initArrayOfStringNames(arrayCell, values)
                val arr = arena.allocate(ADDRESS, 5)
                arr.setAtIndex(ADDRESS, 0, callable)
                arr.setAtIndex(ADDRESS, 1, GodotStrings.makeStringName(name))
                arr.setAtIndex(ADDRESS, 2, firstStringCell)
                arr.setAtIndex(ADDRESS, 3, secondStringCell)
                arr.setAtIndex(ADDRESS, 4, arrayCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, arrayCell)
                GodotStrings.destroyString(secondStringCell)
                GodotStrings.destroyString(firstStringCell)
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, callable)
            }
        }
    }

    fun ptrcallWithObjectCallablePackedInt32ListStringArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        callableObject: MemorySegment,
        callableMethod: String,
        values: List<Int>,
        text: String,
    ) {
        Arena.ofConfined().use { arena ->
            val objectCell = arena.allocate(ADDRESS)
            val callable = BuiltinTypes.allocateCallable(arena)
            val packedCell = BuiltinTypes.allocatePackedArray(arena)
            val stringCell = arena.allocate(8L, 8L)
            try {
                objectCell.set(ADDRESS, 0, objectArg)
                BuiltinTypes.initCallable(callable, callableObject, callableMethod)
                BuiltinTypes.initPackedInt32Array(packedCell, values)
                GodotStrings.initString(stringCell, text)
                val arr = arena.allocate(ADDRESS, 4)
                arr.setAtIndex(ADDRESS, 0, objectCell)
                arr.setAtIndex(ADDRESS, 1, callable)
                arr.setAtIndex(ADDRESS, 2, packedCell)
                arr.setAtIndex(ADDRESS, 3, stringCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(stringCell)
                BuiltinTypes.destroyTyped(VariantType.PACKED_INT32_ARRAY, packedCell)
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, callable)
            }
        }
    }

    fun ptrcallWithTwoStringPackedStringListCallableArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstString: String,
        secondString: String,
        values: List<String>,
        callableObject: MemorySegment,
        callableMethod: String,
    ): Long {
        Arena.ofConfined().use { arena ->
            val firstStringCell = arena.allocate(8L, 8L)
            val secondStringCell = arena.allocate(8L, 8L)
            val packedCell = BuiltinTypes.allocatePackedArray(arena)
            val callable = BuiltinTypes.allocateCallable(arena)
            try {
                GodotStrings.initString(firstStringCell, firstString)
                GodotStrings.initString(secondStringCell, secondString)
                BuiltinTypes.initPackedStringArray(packedCell, values)
                BuiltinTypes.initCallable(callable, callableObject, callableMethod)
                val arr = arena.allocate(ADDRESS, 4)
                arr.setAtIndex(ADDRESS, 0, firstStringCell)
                arr.setAtIndex(ADDRESS, 1, secondStringCell)
                arr.setAtIndex(ADDRESS, 2, packedCell)
                arr.setAtIndex(ADDRESS, 3, callable)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, callable)
                BuiltinTypes.destroyTyped(VariantType.PACKED_STRING_ARRAY, packedCell)
                GodotStrings.destroyString(secondStringCell)
                GodotStrings.destroyString(firstStringCell)
            }
        }
    }

    fun ptrcallWithThreeStringCallableArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstString: String,
        secondString: String,
        thirdString: String,
        callableObject: MemorySegment,
        callableMethod: String,
    ): Long {
        Arena.ofConfined().use { arena ->
            val firstStringCell = arena.allocate(8L, 8L)
            val secondStringCell = arena.allocate(8L, 8L)
            val thirdStringCell = arena.allocate(8L, 8L)
            val callable = BuiltinTypes.allocateCallable(arena)
            try {
                GodotStrings.initString(firstStringCell, firstString)
                GodotStrings.initString(secondStringCell, secondString)
                GodotStrings.initString(thirdStringCell, thirdString)
                BuiltinTypes.initCallable(callable, callableObject, callableMethod)
                val arr = arena.allocate(ADDRESS, 4)
                arr.setAtIndex(ADDRESS, 0, firstStringCell)
                arr.setAtIndex(ADDRESS, 1, secondStringCell)
                arr.setAtIndex(ADDRESS, 2, thirdStringCell)
                arr.setAtIndex(ADDRESS, 3, callable)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, callable)
                GodotStrings.destroyString(thirdStringCell)
                GodotStrings.destroyString(secondStringCell)
                GodotStrings.destroyString(firstStringCell)
            }
        }
    }

    fun ptrcallWithThreeStringBoolLongPackedStringListCallableIntArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstString: String,
        secondString: String,
        thirdString: String,
        flag: Boolean,
        mode: Long,
        values: List<String>,
        callableObject: MemorySegment,
        callableMethod: String,
        index: Int,
    ): Long {
        Arena.ofConfined().use { arena ->
            val firstStringCell = arena.allocate(8L, 8L)
            val secondStringCell = arena.allocate(8L, 8L)
            val thirdStringCell = arena.allocate(8L, 8L)
            val flagCell = arena.allocate(JAVA_BYTE)
            val modeCell = arena.allocate(JAVA_LONG)
            val packedCell = BuiltinTypes.allocatePackedArray(arena)
            val callable = BuiltinTypes.allocateCallable(arena)
            val indexCell = arena.allocate(JAVA_INT)
            try {
                GodotStrings.initString(firstStringCell, firstString)
                GodotStrings.initString(secondStringCell, secondString)
                GodotStrings.initString(thirdStringCell, thirdString)
                flagCell.set(JAVA_BYTE, 0, if (flag) 1 else 0)
                modeCell.set(JAVA_LONG, 0, mode)
                BuiltinTypes.initPackedStringArray(packedCell, values)
                BuiltinTypes.initCallable(callable, callableObject, callableMethod)
                indexCell.set(JAVA_INT, 0, index)
                val arr = arena.allocate(ADDRESS, 8)
                arr.setAtIndex(ADDRESS, 0, firstStringCell)
                arr.setAtIndex(ADDRESS, 1, secondStringCell)
                arr.setAtIndex(ADDRESS, 2, thirdStringCell)
                arr.setAtIndex(ADDRESS, 3, flagCell)
                arr.setAtIndex(ADDRESS, 4, modeCell)
                arr.setAtIndex(ADDRESS, 5, packedCell)
                arr.setAtIndex(ADDRESS, 6, callable)
                arr.setAtIndex(ADDRESS, 7, indexCell)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, callable)
                BuiltinTypes.destroyTyped(VariantType.PACKED_STRING_ARRAY, packedCell)
                GodotStrings.destroyString(thirdStringCell)
                GodotStrings.destroyString(secondStringCell)
                GodotStrings.destroyString(firstStringCell)
            }
        }
    }

    fun ptrcallWithFourStringBoolLongPackedStringListDictionaryListCallableIntArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstString: String,
        secondString: String,
        thirdString: String,
        fourthString: String,
        flag: Boolean,
        mode: Long,
        values: List<String>,
        dictionaries: List<Map<String, Any?>>,
        callableObject: MemorySegment,
        callableMethod: String,
        index: Int,
    ): Long {
        Arena.ofConfined().use { arena ->
            val firstStringCell = arena.allocate(8L, 8L)
            val secondStringCell = arena.allocate(8L, 8L)
            val thirdStringCell = arena.allocate(8L, 8L)
            val fourthStringCell = arena.allocate(8L, 8L)
            val flagCell = arena.allocate(JAVA_BYTE)
            val modeCell = arena.allocate(JAVA_LONG)
            val packedCell = BuiltinTypes.allocatePackedArray(arena)
            val dictionaryArray = arena.allocate(8L, 8L)
            val callable = BuiltinTypes.allocateCallable(arena)
            val indexCell = arena.allocate(JAVA_INT)
            try {
                GodotStrings.initString(firstStringCell, firstString)
                GodotStrings.initString(secondStringCell, secondString)
                GodotStrings.initString(thirdStringCell, thirdString)
                GodotStrings.initString(fourthStringCell, fourthString)
                flagCell.set(JAVA_BYTE, 0, if (flag) 1 else 0)
                modeCell.set(JAVA_LONG, 0, mode)
                BuiltinTypes.initPackedStringArray(packedCell, values)
                BuiltinTypes.initArrayOfDictionaries(dictionaryArray, dictionaries)
                BuiltinTypes.initCallable(callable, callableObject, callableMethod)
                indexCell.set(JAVA_INT, 0, index)
                val arr = arena.allocate(ADDRESS, 10)
                arr.setAtIndex(ADDRESS, 0, firstStringCell)
                arr.setAtIndex(ADDRESS, 1, secondStringCell)
                arr.setAtIndex(ADDRESS, 2, thirdStringCell)
                arr.setAtIndex(ADDRESS, 3, fourthStringCell)
                arr.setAtIndex(ADDRESS, 4, flagCell)
                arr.setAtIndex(ADDRESS, 5, modeCell)
                arr.setAtIndex(ADDRESS, 6, packedCell)
                arr.setAtIndex(ADDRESS, 7, dictionaryArray)
                arr.setAtIndex(ADDRESS, 8, callable)
                arr.setAtIndex(ADDRESS, 9, indexCell)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, callable)
                BuiltinTypes.destroyTyped(VariantType.ARRAY, dictionaryArray)
                BuiltinTypes.destroyTyped(VariantType.PACKED_STRING_ARRAY, packedCell)
                GodotStrings.destroyString(fourthStringCell)
                GodotStrings.destroyString(thirdStringCell)
                GodotStrings.destroyString(secondStringCell)
                GodotStrings.destroyString(firstStringCell)
            }
        }
    }

    fun ptrcallWithTwoStringTwoIntTwoCallableVariantLongIntArgsRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstString: String,
        secondString: String,
        firstInt: Int,
        secondInt: Int,
        firstCallableObject: MemorySegment,
        firstCallableMethod: String,
        secondCallableObject: MemorySegment,
        secondCallableMethod: String,
        data: Any?,
        flags: Long,
        index: Int,
    ): Int {
        Arena.ofConfined().use { arena ->
            val firstStringCell = arena.allocate(8L, 8L)
            val secondStringCell = arena.allocate(8L, 8L)
            val firstIntCell = arena.allocate(JAVA_INT)
            val secondIntCell = arena.allocate(JAVA_INT)
            val firstCallable = BuiltinTypes.allocateCallable(arena)
            val secondCallable = BuiltinTypes.allocateCallable(arena)
            val variantCell = arena.allocate(BuiltinTypes.VARIANT_SIZE, 8L)
            val flagsCell = arena.allocate(JAVA_LONG)
            val indexCell = arena.allocate(JAVA_INT)
            var variantInitialized = false
            try {
                GodotStrings.initString(firstStringCell, firstString)
                GodotStrings.initString(secondStringCell, secondString)
                firstIntCell.set(JAVA_INT, 0, firstInt)
                secondIntCell.set(JAVA_INT, 0, secondInt)
                BuiltinTypes.initCallable(firstCallable, firstCallableObject, firstCallableMethod)
                BuiltinTypes.initCallable(secondCallable, secondCallableObject, secondCallableMethod)
                BuiltinTypes.initVariantFromAny(variantCell, data, arena)
                variantInitialized = true
                flagsCell.set(JAVA_LONG, 0, flags)
                indexCell.set(JAVA_INT, 0, index)
                val arr = arena.allocate(ADDRESS, 9)
                arr.setAtIndex(ADDRESS, 0, firstStringCell)
                arr.setAtIndex(ADDRESS, 1, secondStringCell)
                arr.setAtIndex(ADDRESS, 2, firstIntCell)
                arr.setAtIndex(ADDRESS, 3, secondIntCell)
                arr.setAtIndex(ADDRESS, 4, firstCallable)
                arr.setAtIndex(ADDRESS, 5, secondCallable)
                arr.setAtIndex(ADDRESS, 6, variantCell)
                arr.setAtIndex(ADDRESS, 7, flagsCell)
                arr.setAtIndex(ADDRESS, 8, indexCell)
                val ret = arena.allocate(JAVA_INT)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_INT, 0)
            } finally {
                if (variantInitialized) {
                    BuiltinTypes.destroyVariant(variantCell)
                }
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, secondCallable)
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, firstCallable)
                GodotStrings.destroyString(secondStringCell)
                GodotStrings.destroyString(firstStringCell)
            }
        }
    }

    fun ptrcallWithRIDStringTwoIntTwoCallableVariantLongIntArgsRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: String,
        firstInt: Int,
        secondInt: Int,
        firstCallableObject: MemorySegment,
        firstCallableMethod: String,
        secondCallableObject: MemorySegment,
        secondCallableMethod: String,
        data: Any?,
        flags: Long,
        index: Int,
    ): Int {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val stringCell = arena.allocate(8L, 8L)
            val firstIntCell = arena.allocate(JAVA_INT)
            val secondIntCell = arena.allocate(JAVA_INT)
            val firstCallable = BuiltinTypes.allocateCallable(arena)
            val secondCallable = BuiltinTypes.allocateCallable(arena)
            val variantCell = arena.allocate(BuiltinTypes.VARIANT_SIZE, 8L)
            val flagsCell = arena.allocate(JAVA_LONG)
            val indexCell = arena.allocate(JAVA_INT)
            var variantInitialized = false
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                GodotStrings.initString(stringCell, value)
                firstIntCell.set(JAVA_INT, 0, firstInt)
                secondIntCell.set(JAVA_INT, 0, secondInt)
                BuiltinTypes.initCallable(firstCallable, firstCallableObject, firstCallableMethod)
                BuiltinTypes.initCallable(secondCallable, secondCallableObject, secondCallableMethod)
                BuiltinTypes.initVariantFromAny(variantCell, data, arena)
                variantInitialized = true
                flagsCell.set(JAVA_LONG, 0, flags)
                indexCell.set(JAVA_INT, 0, index)
                val arr = arena.allocate(ADDRESS, 9)
                arr.setAtIndex(ADDRESS, 0, ridCell)
                arr.setAtIndex(ADDRESS, 1, stringCell)
                arr.setAtIndex(ADDRESS, 2, firstIntCell)
                arr.setAtIndex(ADDRESS, 3, secondIntCell)
                arr.setAtIndex(ADDRESS, 4, firstCallable)
                arr.setAtIndex(ADDRESS, 5, secondCallable)
                arr.setAtIndex(ADDRESS, 6, variantCell)
                arr.setAtIndex(ADDRESS, 7, flagsCell)
                arr.setAtIndex(ADDRESS, 8, indexCell)
                val ret = arena.allocate(JAVA_INT)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_INT, 0)
            } finally {
                if (variantInitialized) {
                    BuiltinTypes.destroyVariant(variantCell)
                }
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, secondCallable)
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, firstCallable)
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithObjectListObjectCallableArgsRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        values: List<*>,
        objectArg: MemorySegment,
        callableObject: MemorySegment,
        callableMethod: String,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val arrayCell = arena.allocate(8L, 8L)
            val objectCell = arena.allocate(ADDRESS)
            val callable = BuiltinTypes.allocateCallable(arena)
            try {
                BuiltinTypes.initArrayOfObjects(arrayCell, values)
                objectCell.set(ADDRESS, 0, objectArg)
                BuiltinTypes.initCallable(callable, callableObject, callableMethod)
                val arr = arena.allocate(ADDRESS, 3)
                arr.setAtIndex(ADDRESS, 0, arrayCell)
                arr.setAtIndex(ADDRESS, 1, objectCell)
                arr.setAtIndex(ADDRESS, 2, callable)
                val ret = arena.allocate(ADDRESS)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(ADDRESS, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, callable)
                BuiltinTypes.destroyTyped(VariantType.ARRAY, arrayCell)
            }
        }
    }

    fun ptrcallWithRIDPackedInt64ListObjectCallableArgsRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        values: List<Long>,
        objectArg: MemorySegment,
        callableObject: MemorySegment,
        callableMethod: String,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val packedCell = BuiltinTypes.allocatePackedArray(arena)
            val objectCell = arena.allocate(ADDRESS)
            val callable = BuiltinTypes.allocateCallable(arena)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                BuiltinTypes.initPackedInt64Array(packedCell, values)
                objectCell.set(ADDRESS, 0, objectArg)
                BuiltinTypes.initCallable(callable, callableObject, callableMethod)
                val arr = arena.allocate(ADDRESS, 4)
                arr.setAtIndex(ADDRESS, 0, ridCell)
                arr.setAtIndex(ADDRESS, 1, packedCell)
                arr.setAtIndex(ADDRESS, 2, objectCell)
                arr.setAtIndex(ADDRESS, 3, callable)
                val ret = arena.allocate(ADDRESS)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(ADDRESS, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, callable)
                BuiltinTypes.destroyTyped(VariantType.PACKED_INT64_ARRAY, packedCell)
            }
        }
    }

    fun ptrcallWithStringCallableArgsRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: String,
        callableObject: MemorySegment,
        callableMethod: String,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val callable = BuiltinTypes.allocateCallable(arena)
            try {
                GodotStrings.initString(stringCell, value)
                BuiltinTypes.initCallable(callable, callableObject, callableMethod)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, stringCell)
                arr.setAtIndex(ADDRESS, 1, callable)
                val ret = arena.allocate(ADDRESS)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(ADDRESS, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, callable)
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithRIDObjectListObjectCallableArgsRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        values: List<*>,
        objectArg: MemorySegment,
        callableObject: MemorySegment,
        callableMethod: String,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val arrayCell = arena.allocate(8L, 8L)
            val objectCell = arena.allocate(ADDRESS)
            val callable = BuiltinTypes.allocateCallable(arena)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                BuiltinTypes.initArrayOfObjects(arrayCell, values)
                objectCell.set(ADDRESS, 0, objectArg)
                BuiltinTypes.initCallable(callable, callableObject, callableMethod)
                val arr = arena.allocate(ADDRESS, 4)
                arr.setAtIndex(ADDRESS, 0, ridCell)
                arr.setAtIndex(ADDRESS, 1, arrayCell)
                arr.setAtIndex(ADDRESS, 2, objectCell)
                arr.setAtIndex(ADDRESS, 3, callable)
                val ret = arena.allocate(ADDRESS)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(ADDRESS, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, callable)
                BuiltinTypes.destroyTyped(VariantType.ARRAY, arrayCell)
            }
        }
    }

    fun ptrcallWithRIDObjectListTwoObjectCallableArgsRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        values: List<*>,
        firstObjectArg: MemorySegment,
        secondObjectArg: MemorySegment,
        callableObject: MemorySegment,
        callableMethod: String,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val arrayCell = arena.allocate(8L, 8L)
            val firstObjectCell = arena.allocate(ADDRESS)
            val secondObjectCell = arena.allocate(ADDRESS)
            val callable = BuiltinTypes.allocateCallable(arena)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                BuiltinTypes.initArrayOfObjects(arrayCell, values)
                firstObjectCell.set(ADDRESS, 0, firstObjectArg)
                secondObjectCell.set(ADDRESS, 0, secondObjectArg)
                BuiltinTypes.initCallable(callable, callableObject, callableMethod)
                val arr = arena.allocate(ADDRESS, 5)
                arr.setAtIndex(ADDRESS, 0, ridCell)
                arr.setAtIndex(ADDRESS, 1, arrayCell)
                arr.setAtIndex(ADDRESS, 2, firstObjectCell)
                arr.setAtIndex(ADDRESS, 3, secondObjectCell)
                arr.setAtIndex(ADDRESS, 4, callable)
                val ret = arena.allocate(ADDRESS)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(ADDRESS, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, callable)
                BuiltinTypes.destroyTyped(VariantType.ARRAY, arrayCell)
            }
        }
    }

    fun ptrcallNoArgsRetCallable(
        methodBind: MemorySegment,
        instance: MemorySegment,
    ): GodotCallable? {
        Arena.ofConfined().use { arena ->
            val callable = BuiltinTypes.allocateCallable(arena)
            try {
                objectMethodBindPtrcall.invoke(methodBind, instance, MemorySegment.NULL, callable)
                return BuiltinTypes.readCallable(callable)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, callable)
            }
        }
    }

    fun ptrcallWithIntArgRetCallable(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Int,
    ): GodotCallable? {
        Arena.ofConfined().use { arena ->
            val valueCell = arena.allocate(JAVA_INT)
            val callable = BuiltinTypes.allocateCallable(arena)
            try {
                valueCell.set(JAVA_INT, 0, value)
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, valueCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, callable)
                return BuiltinTypes.readCallable(callable)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, callable)
            }
        }
    }

    fun ptrcallWithRIDArgRetCallable(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
    ): GodotCallable? {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val callable = BuiltinTypes.allocateCallable(arena)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, ridCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, callable)
                return BuiltinTypes.readCallable(callable)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, callable)
            }
        }
    }

    fun ptrcallWithRIDIntArgsRetCallable(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Int,
    ): GodotCallable? {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val valueCell = arena.allocate(JAVA_INT)
            val callable = BuiltinTypes.allocateCallable(arena)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                valueCell.set(JAVA_INT, 0, value)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, ridCell)
                arr.setAtIndex(ADDRESS, 1, valueCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, callable)
                return BuiltinTypes.readCallable(callable)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, callable)
            }
        }
    }

    fun ptrcallWithStringIntArgsRetCallable(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: String,
        index: Int,
    ): GodotCallable? {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val indexCell = arena.allocate(JAVA_INT)
            val callable = BuiltinTypes.allocateCallable(arena)
            try {
                GodotStrings.initString(stringCell, value)
                indexCell.set(JAVA_INT, 0, index)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, stringCell)
                arr.setAtIndex(ADDRESS, 1, indexCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, callable)
                return BuiltinTypes.readCallable(callable)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, callable)
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    /**
     * Calls [methodBind] with one Callable arg and Object return value.
     */
    fun ptrcallWithCallableArgRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        callableObject: MemorySegment,
        callableMethod: String,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val callable = BuiltinTypes.allocateCallable(arena)
            try {
                BuiltinTypes.initCallable(callable, callableObject, callableMethod)
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, callable)
                val ret = arena.allocate(ADDRESS)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(ADDRESS, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, callable)
            }
        }
    }

    /**
     * Calls [methodBind] with (int64/enum, Callable) and Object return value.
     */
    fun ptrcallWithLongCallableArgsRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Long,
        callableObject: MemorySegment,
        callableMethod: String,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val valueCell = arena.allocate(JAVA_LONG)
            val callable = BuiltinTypes.allocateCallable(arena)
            try {
                valueCell.set(JAVA_LONG, 0, value)
                BuiltinTypes.initCallable(callable, callableObject, callableMethod)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, valueCell)
                arr.setAtIndex(ADDRESS, 1, callable)
                val ret = arena.allocate(ADDRESS)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(ADDRESS, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, callable)
            }
        }
    }

    /**
     * Calls [methodBind] with (Callable, Variant, Variant, float) and Object return value.
     */
    fun ptrcallWithCallableVariantVariantDoubleArgsRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        callableObject: MemorySegment,
        callableMethod: String,
        from: Any?,
        to: Any?,
        duration: Double,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val callable = BuiltinTypes.allocateCallable(arena)
            val fromVariant = arena.allocate(BuiltinTypes.VARIANT_SIZE, 8L)
            val toVariant = arena.allocate(BuiltinTypes.VARIANT_SIZE, 8L)
            val durationCell = arena.allocate(JAVA_DOUBLE)
            var fromInitialized = false
            var toInitialized = false
            try {
                BuiltinTypes.initCallable(callable, callableObject, callableMethod)
                BuiltinTypes.initVariantFromAny(fromVariant, from, arena)
                fromInitialized = true
                BuiltinTypes.initVariantFromAny(toVariant, to, arena)
                toInitialized = true
                durationCell.set(JAVA_DOUBLE, 0, duration)

                val arr = arena.allocate(ADDRESS, 4)
                arr.setAtIndex(ADDRESS, 0, callable)
                arr.setAtIndex(ADDRESS, 1, fromVariant)
                arr.setAtIndex(ADDRESS, 2, toVariant)
                arr.setAtIndex(ADDRESS, 3, durationCell)
                val ret = arena.allocate(ADDRESS)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(ADDRESS, 0)
            } finally {
                if (toInitialized) {
                    BuiltinTypes.destroyVariant(toVariant)
                }
                if (fromInitialized) {
                    BuiltinTypes.destroyVariant(fromVariant)
                }
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, callable)
            }
        }
    }

    /**
     * Calls [methodBind] with (StringName, Callable.bindv(boundArgs)) and no
     * return value.
     */
    fun ptrcallWithStringNameAndBoundCallableArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
        callableObject: MemorySegment,
        callableMethod: String,
        boundArgs: List<Any?>,
    ) {
        val bindvHash = 3564560322L
        Arena.ofConfined().use { arena ->
            val callable = BuiltinTypes.allocateCallable(arena)
            val boundArray = arena.allocate(8L, 8L)
            val boundCallable = BuiltinTypes.allocateCallable(arena)
            try {
                BuiltinTypes.initCallable(callable, callableObject, callableMethod)
                BuiltinTypes.initArray(boundArray, boundArgs)
                BuiltinTypes.call(
                    type = VariantType.CALLABLE,
                    method = "bindv",
                    hash = bindvHash,
                    base = callable,
                    args = listOf(boundArray),
                    rReturn = boundCallable,
                )
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(name))
                arr.setAtIndex(ADDRESS, 1, boundCallable)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, boundCallable)
                BuiltinTypes.destroyTyped(VariantType.ARRAY, boundArray)
                BuiltinTypes.destroyTyped(VariantType.CALLABLE, callable)
            }
        }
    }

    /**
     * Calls [methodBind] with (String, Array[Dictionary]) and no return value.
     */
    fun ptrcallWithStringAndArrayOfDictionariesArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        message: String,
        data: List<Map<String, Any>>,
    ) {
        Arena.ofConfined().use { arena ->
            val stringArg = arena.allocate(8L, 8L)
            val arrayArg = arena.allocate(8L, 8L)
            try {
                GodotStrings.initString(stringArg, message)
                BuiltinTypes.initArrayOfDictionaries(arrayArg, data)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, stringArg)
                arr.setAtIndex(ADDRESS, 1, arrayArg)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, arrayArg)
                GodotStrings.destroyString(stringArg)
            }
        }
    }

    /**
     * Calls [methodBind] with (StringName, Array[Dictionary]) and no return value.
     */
    fun ptrcallWithStringNameAndArrayOfDictionariesArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
        data: List<Map<String, Any>>,
    ) {
        Arena.ofConfined().use { arena ->
            val arrayArg = arena.allocate(8L, 8L)
            try {
                BuiltinTypes.initArrayOfDictionaries(arrayArg, data)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(name))
                arr.setAtIndex(ADDRESS, 1, arrayArg)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, arrayArg)
            }
        }
    }

    /**
     * Calls [methodBind] with (StringName, bool, Array[Dictionary]) and no return value.
     */
    fun ptrcallWithStringNameBoolAndArrayOfDictionariesArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
        enabled: Boolean,
        data: List<Map<String, Any>>,
    ) {
        Arena.ofConfined().use { arena ->
            val boolArg = arena.allocate(java.lang.foreign.ValueLayout.JAVA_BYTE)
            val arrayArg = arena.allocate(8L, 8L)
            try {
                boolArg.set(java.lang.foreign.ValueLayout.JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
                BuiltinTypes.initArrayOfDictionaries(arrayArg, data)
                val arr = arena.allocate(ADDRESS, 3)
                arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(name))
                arr.setAtIndex(ADDRESS, 1, boolArg)
                arr.setAtIndex(ADDRESS, 2, arrayArg)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, arrayArg)
            }
        }
    }

    /**
     * Calls [methodBind] with (bool, PackedStringArray) and no return value.
     */
    fun ptrcallWithBoolAndPackedStringArrayArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        enabled: Boolean,
        values: List<String>,
    ) {
        Arena.ofConfined().use { arena ->
            val boolArg = arena.allocate(java.lang.foreign.ValueLayout.JAVA_BYTE)
            val packedArg = BuiltinTypes.allocatePackedArray(arena)
            try {
                boolArg.set(java.lang.foreign.ValueLayout.JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
                BuiltinTypes.initPackedStringArray(packedArg, values)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, boolArg)
                arr.setAtIndex(ADDRESS, 1, packedArg)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_STRING_ARRAY, packedArg)
            }
        }
    }

    /**
     * Calls [methodBind] with (int64, StringName) and bool return value.
     */
    fun ptrcallWithLongAndStringNameArgRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Long,
        name: String,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 2)
            val intCell = arena.allocate(JAVA_LONG)
            intCell.set(JAVA_LONG, 0, value)
            arr.setAtIndex(ADDRESS, 0, intCell)
            arr.setAtIndex(ADDRESS, 1, GodotStrings.makeStringName(name))
            val ret = arena.allocate(java.lang.foreign.ValueLayout.JAVA_BYTE)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(java.lang.foreign.ValueLayout.JAVA_BYTE, 0) != 0.toByte()
        }
    }

    /**
     * Calls [methodBind] with (int32, StringName) and bool return value.
     */
    fun ptrcallWithIntAndStringNameArgRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Int,
        name: String,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 2)
            val intCell = arena.allocate(JAVA_INT)
            intCell.set(JAVA_INT, 0, value)
            arr.setAtIndex(ADDRESS, 0, intCell)
            arr.setAtIndex(ADDRESS, 1, GodotStrings.makeStringName(name))
            val ret = arena.allocate(JAVA_BYTE)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_BYTE, 0) != 0.toByte()
        }
    }

    /**
     * Calls [methodBind] with (StringName, StringName) and float(double) return value.
     */
    fun ptrcallWithTwoStringNameArgsRetDouble(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: String,
        second: String,
    ): Double {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(first))
            arr.setAtIndex(ADDRESS, 1, GodotStrings.makeStringName(second))
            val ret = arena.allocate(java.lang.foreign.ValueLayout.JAVA_DOUBLE)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(java.lang.foreign.ValueLayout.JAVA_DOUBLE, 0)
        }
    }

    fun ptrcallWithTwoStringNameArgsRetVariantScalar(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: String,
        second: String,
    ): Any? {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(first))
            arr.setAtIndex(ADDRESS, 1, GodotStrings.makeStringName(second))
            return readVariantReturn(methodBind, instance, arr, arena)
        }
    }

    fun ptrcallWithTwoStringNameArgsRetDictionary(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: String,
        second: String,
    ): Map<String, Any?> {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(first))
            arr.setAtIndex(ADDRESS, 1, GodotStrings.makeStringName(second))
            val ret = arena.allocate(8L, 8L)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return try {
                BuiltinTypes.readDictionaryScalars(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.DICTIONARY, ret)
            }
        }
    }

    fun ptrcallWithTwoStringNameArgsRetColor(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: String,
        second: String,
    ): Color {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(first))
            arr.setAtIndex(ADDRESS, 1, GodotStrings.makeStringName(second))
            val ret = arena.allocate(16L, 4L)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return Color(
                ret.get(JAVA_FLOAT, 0),
                ret.get(JAVA_FLOAT, 4),
                ret.get(JAVA_FLOAT, 8),
                ret.get(JAVA_FLOAT, 12),
            )
        }
    }

    fun ptrcallWithTwoStringNameAndColorArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: String,
        second: String,
        color: Color,
    ) {
        Arena.ofConfined().use { arena ->
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(first))
            arr.setAtIndex(ADDRESS, 1, GodotStrings.makeStringName(second))
            // Color components use fixed 32-bit storage, unlike scalar float method args.
            val colorCell = arena.allocate(16L, 4L)
            colorCell.set(JAVA_FLOAT, 0, color.r)
            colorCell.set(JAVA_FLOAT, 4, color.g)
            colorCell.set(JAVA_FLOAT, 8, color.b)
            colorCell.set(JAVA_FLOAT, 12, color.a)
            arr.setAtIndex(ADDRESS, 2, colorCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithTwoStringNameAndDoubleArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: String,
        second: String,
        value: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val doubleCell = arena.allocate(JAVA_DOUBLE)
            doubleCell.set(JAVA_DOUBLE, 0, value)
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(first))
            arr.setAtIndex(ADDRESS, 1, GodotStrings.makeStringName(second))
            arr.setAtIndex(ADDRESS, 2, doubleCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithTwoStringNameUInt32ArgRetVector2i(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: String,
        second: String,
        index: Long,
    ): Vector2i {
        Arena.ofConfined().use { arena ->
            val indexCell = arena.allocate(JAVA_INT)
            indexCell.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(index))
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(first))
            args.setAtIndex(ADDRESS, 1, GodotStrings.makeStringName(second))
            args.setAtIndex(ADDRESS, 2, indexCell)
            val ret = arena.allocate(8L, 4L)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return readVector2i(ret)
        }
    }

    fun ptrcallWithTwoStringNameFourUInt32ArgsRetRID(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: String,
        second: String,
        a: Long,
        b: Long,
        c: Long,
        d: Long,
    ): RID {
        Arena.ofConfined().use { arena ->
            val aCell = arena.allocate(JAVA_INT)
            val bCell = arena.allocate(JAVA_INT)
            val cCell = arena.allocate(JAVA_INT)
            val dCell = arena.allocate(JAVA_INT)
            aCell.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(a))
            bCell.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(b))
            cCell.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(c))
            dCell.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(d))
            val args = arena.allocate(ADDRESS, 6)
            args.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(first))
            args.setAtIndex(ADDRESS, 1, GodotStrings.makeStringName(second))
            args.setAtIndex(ADDRESS, 2, aCell)
            args.setAtIndex(ADDRESS, 3, bCell)
            args.setAtIndex(ADDRESS, 4, cCell)
            args.setAtIndex(ADDRESS, 5, dCell)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return RID(ret.get(JAVA_LONG, 0))
        }
    }

    fun ptrcallWithTwoStringNameLongUInt32LongVector2iTwoUInt32TwoBoolArgsRetRID(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: String,
        second: String,
        firstLong: Long,
        firstUInt: Long,
        secondLong: Long,
        vector: Vector2i,
        secondUInt: Long,
        thirdUInt: Long,
        firstBool: Boolean,
        secondBool: Boolean,
    ): RID {
        Arena.ofConfined().use { arena ->
            val long0 = arena.allocate(JAVA_LONG)
            val uint0 = arena.allocate(JAVA_INT)
            val long1 = arena.allocate(JAVA_LONG)
            val vectorCell = arena.allocate(8L, 4L)
            val uint1 = arena.allocate(JAVA_INT)
            val uint2 = arena.allocate(JAVA_INT)
            val bool0 = arena.allocate(JAVA_BYTE)
            val bool1 = arena.allocate(JAVA_BYTE)
            long0.set(JAVA_LONG, 0, firstLong)
            uint0.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(firstUInt))
            long1.set(JAVA_LONG, 0, secondLong)
            writeVector2i(vectorCell, vector)
            uint1.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(secondUInt))
            uint2.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(thirdUInt))
            bool0.set(JAVA_BYTE, 0, if (firstBool) 1.toByte() else 0.toByte())
            bool1.set(JAVA_BYTE, 0, if (secondBool) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 10)
            args.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(first))
            args.setAtIndex(ADDRESS, 1, GodotStrings.makeStringName(second))
            args.setAtIndex(ADDRESS, 2, long0)
            args.setAtIndex(ADDRESS, 3, uint0)
            args.setAtIndex(ADDRESS, 4, long1)
            args.setAtIndex(ADDRESS, 5, vectorCell)
            args.setAtIndex(ADDRESS, 6, uint1)
            args.setAtIndex(ADDRESS, 7, uint2)
            args.setAtIndex(ADDRESS, 8, bool0)
            args.setAtIndex(ADDRESS, 9, bool1)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return RID(ret.get(JAVA_LONG, 0))
        }
    }

    /**
     * Calls [methodBind] with (StringName, StringName, StringName, StringName, scalar float)
     * and returns Vector2.
     */
    fun ptrcallWithFourStringNameAndFloatArgRetVector2(
        methodBind: MemorySegment,
        instance: MemorySegment,
        negativeX: String,
        positiveX: String,
        negativeY: String,
        positiveY: String,
        deadzone: Double,
    ): Vector2 {
        Arena.ofConfined().use { arena ->
            val args = arena.allocate(ADDRESS, 5)
            args.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(negativeX))
            args.setAtIndex(ADDRESS, 1, GodotStrings.makeStringName(positiveX))
            args.setAtIndex(ADDRESS, 2, GodotStrings.makeStringName(negativeY))
            args.setAtIndex(ADDRESS, 3, GodotStrings.makeStringName(positiveY))

            val deadzoneCell = arena.allocate(java.lang.foreign.ValueLayout.JAVA_DOUBLE)
            deadzoneCell.set(java.lang.foreign.ValueLayout.JAVA_DOUBLE, 0, deadzone)
            args.setAtIndex(ADDRESS, 4, deadzoneCell)

            val ret = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return Vector2(
                x = GodotReal.readIndex(ret, 0),
                y = GodotReal.readIndex(ret, 1),
            )
        }
    }

    /**
     * Calls [methodBind] with no args and returns Vector2.
     */
    fun ptrcallNoArgsRetVector2(
        methodBind: MemorySegment,
        instance: MemorySegment,
    ): Vector2 {
        val scratch = ptrcallScratch.get()
        objectMethodBindPtrcall.invoke(methodBind, instance, MemorySegment.NULL, scratch.vector2Ret)
        return Vector2(
            x = GodotReal.readIndex(scratch.vector2Ret, 0),
            y = GodotReal.readIndex(scratch.vector2Ret, 1),
        )
    }

    /**
     * Calls [methodBind] with no args and returns Rect2.
     */
    fun ptrcallNoArgsRetRect2(
        methodBind: MemorySegment,
        instance: MemorySegment,
    ): Rect2 {
        val ret = ptrcallScratch.get().rect2Ret
        objectMethodBindPtrcall.invoke(methodBind, instance, MemorySegment.NULL, ret)
        return Rect2(
            position = Vector2(
                x = GodotReal.readIndex(ret, 0),
                y = GodotReal.readIndex(ret, 1),
            ),
            size = Vector2(
                x = GodotReal.readIndex(ret, 2),
                y = GodotReal.readIndex(ret, 3),
            ),
        )
    }

    fun ptrcallWithRect2Arg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Rect2,
    ) {
        Arena.ofConfined().use { arena ->
            val rect = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
            writeRect2(rect, value)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, rect)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallNoArgsRetAABB(
        methodBind: MemorySegment,
        instance: MemorySegment,
    ): AABB {
        Arena.ofConfined().use { arena ->
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 6, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, MemorySegment.NULL, ret)
            return readAABB(ret)
        }
    }

    fun ptrcallWithAABBArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: AABB,
    ) {
        Arena.ofConfined().use { arena ->
            val aabb = arena.allocate(GodotReal.SIZE_BYTES * 6, GodotReal.ALIGN_BYTES)
            writeAABB(aabb, value)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, aabb)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallNoArgsRetPlane(
        methodBind: MemorySegment,
        instance: MemorySegment,
    ): Plane {
        Arena.ofConfined().use { arena ->
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, MemorySegment.NULL, ret)
            return Plane(
                normal = Vector3(
                    x = GodotReal.readIndex(ret, 0),
                    y = GodotReal.readIndex(ret, 1),
                    z = GodotReal.readIndex(ret, 2),
                ),
                d = GodotReal.readIndex(ret, 3),
            )
        }
    }

    fun ptrcallWithPlaneArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Plane,
    ) {
        Arena.ofConfined().use { arena ->
            val plane = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
            GodotReal.writeIndex(plane, 0, value.normal.x)
            GodotReal.writeIndex(plane, 1, value.normal.y)
            GodotReal.writeIndex(plane, 2, value.normal.z)
            GodotReal.writeIndex(plane, 3, value.d)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, plane)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithIntAndPlaneArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        intValue: Int,
        value: Plane,
    ) {
        Arena.ofConfined().use { arena ->
            val intCell = arena.allocate(JAVA_INT)
            val plane = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
            intCell.set(JAVA_INT, 0, intValue)
            GodotReal.writeIndex(plane, 0, value.normal.x)
            GodotReal.writeIndex(plane, 1, value.normal.y)
            GodotReal.writeIndex(plane, 2, value.normal.z)
            GodotReal.writeIndex(plane, 3, value.d)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, intCell)
            arr.setAtIndex(ADDRESS, 1, plane)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallNoArgsRetTransform2D(
        methodBind: MemorySegment,
        instance: MemorySegment,
    ): Transform2D {
        Arena.ofConfined().use { arena ->
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 6, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, MemorySegment.NULL, ret)
            return readTransform2D(ret)
        }
    }

    fun ptrcallWithTransform2DArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Transform2D,
    ) {
        Arena.ofConfined().use { arena ->
            val transform = arena.allocate(GodotReal.SIZE_BYTES * 6, GodotReal.ALIGN_BYTES)
            writeTransform2D(transform, value)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, transform)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithTransform2DVector2TwoColorLongArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        transformValue: Transform2D,
        vectorValue: Vector2,
        firstColor: Color,
        secondColor: Color,
        flags: Long,
    ) {
        Arena.ofConfined().use { arena ->
            val transform = arena.allocate(GodotReal.SIZE_BYTES * 6, GodotReal.ALIGN_BYTES)
            val vector = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val color0 = arena.allocate(16L, 4L)
            val color1 = arena.allocate(16L, 4L)
            val flagsCell = arena.allocate(JAVA_LONG)
            writeTransform2D(transform, transformValue)
            GodotReal.writeIndex(vector, 0, vectorValue.x)
            GodotReal.writeIndex(vector, 1, vectorValue.y)
            color0.set(JAVA_FLOAT, 0, firstColor.r)
            color0.set(JAVA_FLOAT, 4, firstColor.g)
            color0.set(JAVA_FLOAT, 8, firstColor.b)
            color0.set(JAVA_FLOAT, 12, firstColor.a)
            color1.set(JAVA_FLOAT, 0, secondColor.r)
            color1.set(JAVA_FLOAT, 4, secondColor.g)
            color1.set(JAVA_FLOAT, 8, secondColor.b)
            color1.set(JAVA_FLOAT, 12, secondColor.a)
            flagsCell.set(JAVA_LONG, 0, flags)
            val arr = arena.allocate(ADDRESS, 5)
            arr.setAtIndex(ADDRESS, 0, transform)
            arr.setAtIndex(ADDRESS, 1, vector)
            arr.setAtIndex(ADDRESS, 2, color0)
            arr.setAtIndex(ADDRESS, 3, color1)
            arr.setAtIndex(ADDRESS, 4, flagsCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithTransform2DVector2TwoColorUInt32Args(
        methodBind: MemorySegment,
        instance: MemorySegment,
        transformValue: Transform2D,
        vectorValue: Vector2,
        firstColor: Color,
        secondColor: Color,
        flags: Long,
    ) {
        Arena.ofConfined().use { arena ->
            val transform = arena.allocate(GodotReal.SIZE_BYTES * 6, GodotReal.ALIGN_BYTES)
            val vector = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val color0 = arena.allocate(16L, 4L)
            val color1 = arena.allocate(16L, 4L)
            val flagsCell = arena.allocate(JAVA_INT)
            writeTransform2D(transform, transformValue)
            writeVector2(vector, vectorValue)
            color0.set(JAVA_FLOAT, 0, firstColor.r)
            color0.set(JAVA_FLOAT, 4, firstColor.g)
            color0.set(JAVA_FLOAT, 8, firstColor.b)
            color0.set(JAVA_FLOAT, 12, firstColor.a)
            color1.set(JAVA_FLOAT, 0, secondColor.r)
            color1.set(JAVA_FLOAT, 4, secondColor.g)
            color1.set(JAVA_FLOAT, 8, secondColor.b)
            color1.set(JAVA_FLOAT, 12, secondColor.a)
            flagsCell.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(flags))
            val arr = arena.allocate(ADDRESS, 5)
            arr.setAtIndex(ADDRESS, 0, transform)
            arr.setAtIndex(ADDRESS, 1, vector)
            arr.setAtIndex(ADDRESS, 2, color0)
            arr.setAtIndex(ADDRESS, 3, color1)
            arr.setAtIndex(ADDRESS, 4, flagsCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (Transform2D, Vector2, Object*, scalar float, bool) and bool return value.
     */
    fun ptrcallWithTransform2DVector2ObjectDoubleBoolArgsRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        transformValue: Transform2D,
        vectorValue: Vector2,
        objectArg: MemorySegment,
        margin: Double,
        recoveryAsCollision: Boolean,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val transform = arena.allocate(GodotReal.SIZE_BYTES * 6, GodotReal.ALIGN_BYTES)
            val vector = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val objectCell = arena.allocate(ADDRESS)
            val marginCell = arena.allocate(JAVA_DOUBLE)
            val boolCell = arena.allocate(JAVA_BYTE)
            writeTransform2D(transform, transformValue)
            writeVector2(vector, vectorValue)
            objectCell.set(ADDRESS, 0, objectArg)
            marginCell.set(JAVA_DOUBLE, 0, margin)
            boolCell.set(JAVA_BYTE, 0, if (recoveryAsCollision) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 5)
            args.setAtIndex(ADDRESS, 0, transform)
            args.setAtIndex(ADDRESS, 1, vector)
            args.setAtIndex(ADDRESS, 2, objectCell)
            args.setAtIndex(ADDRESS, 3, marginCell)
            args.setAtIndex(ADDRESS, 4, boolCell)
            val ret = arena.allocate(JAVA_BYTE)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(JAVA_BYTE, 0) != 0.toByte()
        }
    }

    /**
     * Calls [methodBind] with Vector2 arg and int32 return value.
     */
    fun ptrcallWithVector2ArgRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Vector2,
    ): Int {
        Arena.ofConfined().use { arena ->
            val arg = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            GodotReal.writeIndex(arg, 0, value.x)
            GodotReal.writeIndex(arg, 1, value.y)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, arg)
            val ret = arena.allocate(JAVA_INT)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_INT, 0)
        }
    }

    /**
     * Calls [methodBind] with no args and returns Vector3.
     */
    fun ptrcallNoArgsRetVector3(
        methodBind: MemorySegment,
        instance: MemorySegment,
    ): Vector3 {
        val ret = ptrcallScratch.get().vector3Ret
        objectMethodBindPtrcall.invoke(methodBind, instance, MemorySegment.NULL, ret)
        return Vector3(
            x = GodotReal.readIndex(ret, 0),
            y = GodotReal.readIndex(ret, 1),
            z = GodotReal.readIndex(ret, 2),
        )
    }

    fun ptrcallNoArgsRetVector4(
        methodBind: MemorySegment,
        instance: MemorySegment,
    ): Vector4 {
        Arena.ofConfined().use { arena ->
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, MemorySegment.NULL, ret)
            return readVector4(ret)
        }
    }

    fun ptrcallWithVector4Arg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Vector4,
    ) {
        Arena.ofConfined().use { arena ->
            val vec = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
            GodotReal.writeIndex(vec, 0, value.x)
            GodotReal.writeIndex(vec, 1, value.y)
            GodotReal.writeIndex(vec, 2, value.z)
            GodotReal.writeIndex(vec, 3, value.w)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, vec)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with one Vector3 arg and no return value.
     */
    fun ptrcallWithVector3Arg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Vector3,
    ) {
        Arena.ofConfined().use { arena ->
            val vec = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            GodotReal.writeIndex(vec, 0, value.x)
            GodotReal.writeIndex(vec, 1, value.y)
            GodotReal.writeIndex(vec, 2, value.z)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, vec)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithIntAndVector3Arg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        intArg: Int,
        value: Vector3,
    ) {
        Arena.ofConfined().use { arena ->
            val intCell = arena.allocate(JAVA_INT)
            intCell.set(JAVA_INT, 0, intArg)
            val vec = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            GodotReal.writeIndex(vec, 0, value.x)
            GodotReal.writeIndex(vec, 1, value.y)
            GodotReal.writeIndex(vec, 2, value.z)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, intCell)
            arr.setAtIndex(ADDRESS, 1, vec)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithIntVector3ArgsRetDouble(
        methodBind: MemorySegment,
        instance: MemorySegment,
        intArg: Int,
        value: Vector3,
    ): Double {
        Arena.ofConfined().use { arena ->
            val intCell = arena.allocate(JAVA_INT)
            val vec = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            intCell.set(JAVA_INT, 0, intArg)
            GodotReal.writeIndex(vec, 0, value.x)
            GodotReal.writeIndex(vec, 1, value.y)
            GodotReal.writeIndex(vec, 2, value.z)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, intCell)
            arr.setAtIndex(ADDRESS, 1, vec)
            val ret = arena.allocate(JAVA_DOUBLE)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_DOUBLE, 0)
        }
    }

    fun ptrcallWithIntAndRect2iArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        intArg: Int,
        value: Rect2i,
    ) {
        Arena.ofConfined().use { arena ->
            val intCell = arena.allocate(JAVA_INT)
            val rect = arena.allocate(16L, 4L)
            intCell.set(JAVA_INT, 0, intArg)
            rect.set(JAVA_INT, 0, value.position.x)
            rect.set(JAVA_INT, 4, value.position.y)
            rect.set(JAVA_INT, 8, value.size.x)
            rect.set(JAVA_INT, 12, value.size.y)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, intCell)
            arr.setAtIndex(ADDRESS, 1, rect)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with one Vector3 arg and Vector3 return value.
     */
    fun ptrcallWithVector3ArgRetVector3(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Vector3,
    ): Vector3 {
        Arena.ofConfined().use { arena ->
            val vec = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            GodotReal.writeIndex(vec, 0, value.x)
            GodotReal.writeIndex(vec, 1, value.y)
            GodotReal.writeIndex(vec, 2, value.z)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, vec)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return Vector3(
                x = GodotReal.readIndex(ret, 0),
                y = GodotReal.readIndex(ret, 1),
                z = GodotReal.readIndex(ret, 2),
            )
        }
    }

    fun ptrcallWithVector3ArgRetDouble(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Vector3,
    ): Double {
        Arena.ofConfined().use { arena ->
            val vec = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            GodotReal.writeIndex(vec, 0, value.x)
            GodotReal.writeIndex(vec, 1, value.y)
            GodotReal.writeIndex(vec, 2, value.z)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, vec)
            val ret = arena.allocate(JAVA_DOUBLE)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_DOUBLE, 0)
        }
    }

    fun ptrcallWithVector3ArgRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Vector3,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val vec = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            GodotReal.writeIndex(vec, 0, value.x)
            GodotReal.writeIndex(vec, 1, value.y)
            GodotReal.writeIndex(vec, 2, value.z)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, vec)
            val ret = arena.allocate(JAVA_BYTE)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_BYTE, 0) != 0.toByte()
        }
    }

    fun ptrcallWithVector3ArgRetVector2(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Vector3,
    ): Vector2 {
        Arena.ofConfined().use { arena ->
            val vec = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            GodotReal.writeIndex(vec, 0, value.x)
            GodotReal.writeIndex(vec, 1, value.y)
            GodotReal.writeIndex(vec, 2, value.z)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, vec)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return Vector2(
                x = GodotReal.readIndex(ret, 0),
                y = GodotReal.readIndex(ret, 1),
            )
        }
    }

    fun ptrcallWithVector3ArgRetVector3i(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Vector3,
    ): Vector3i {
        Arena.ofConfined().use { arena ->
            val vec = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            GodotReal.writeIndex(vec, 0, value.x)
            GodotReal.writeIndex(vec, 1, value.y)
            GodotReal.writeIndex(vec, 2, value.z)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, vec)
            val ret = arena.allocate(12L, 4L)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return readVector3i(ret)
        }
    }

    fun ptrcallWithVector3AndDoubleArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Vector3,
        amount: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val vec = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            val amountCell = arena.allocate(JAVA_DOUBLE)
            GodotReal.writeIndex(vec, 0, value.x)
            GodotReal.writeIndex(vec, 1, value.y)
            GodotReal.writeIndex(vec, 2, value.z)
            amountCell.set(JAVA_DOUBLE, 0, amount)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, vec)
            arr.setAtIndex(ADDRESS, 1, amountCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (Vector3, scalar float) args and no return value.
     */
    fun ptrcallWithVector3FloatArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        vector: Vector3,
        floatValue: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val vectorCell = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            GodotReal.writeIndex(vectorCell, 0, vector.x)
            GodotReal.writeIndex(vectorCell, 1, vector.y)
            GodotReal.writeIndex(vectorCell, 2, vector.z)

            val floatCell = arena.allocate(JAVA_DOUBLE)
            floatCell.set(JAVA_DOUBLE, 0, floatValue)

            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, vectorCell)
            arr.setAtIndex(ADDRESS, 1, floatCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (Vector3, bool, scalar float, bool, int32) and Object return value.
     */
    fun ptrcallWithVector3BoolFloatBoolIntArgsRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        vector: Vector3,
        firstBool: Boolean,
        floatValue: Double,
        secondBool: Boolean,
        intValue: Int,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val vectorCell = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            GodotReal.writeIndex(vectorCell, 0, vector.x)
            GodotReal.writeIndex(vectorCell, 1, vector.y)
            GodotReal.writeIndex(vectorCell, 2, vector.z)

            val firstBoolCell = arena.allocate(JAVA_BYTE)
            firstBoolCell.set(JAVA_BYTE, 0, if (firstBool) 1.toByte() else 0.toByte())

            val floatCell = arena.allocate(JAVA_DOUBLE)
            floatCell.set(JAVA_DOUBLE, 0, floatValue)

            val secondBoolCell = arena.allocate(JAVA_BYTE)
            secondBoolCell.set(JAVA_BYTE, 0, if (secondBool) 1.toByte() else 0.toByte())

            val intCell = arena.allocate(JAVA_INT)
            intCell.set(JAVA_INT, 0, intValue)

            val arr = arena.allocate(ADDRESS, 5)
            arr.setAtIndex(ADDRESS, 0, vectorCell)
            arr.setAtIndex(ADDRESS, 1, firstBoolCell)
            arr.setAtIndex(ADDRESS, 2, floatCell)
            arr.setAtIndex(ADDRESS, 3, secondBoolCell)
            arr.setAtIndex(ADDRESS, 4, intCell)

            val ret = arena.allocate(ADDRESS)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(ADDRESS, 0)
        }
    }

    /**
     * Calls [methodBind] with one int32 arg and Vector3 return value.
     */
    fun ptrcallWithIntArgRetVector3(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Long,
    ): Vector3 {
        Arena.ofConfined().use { arena ->
            val arg = arena.allocate(JAVA_INT)
            arg.set(JAVA_INT, 0, value.toInt())
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, arg)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return Vector3(
                x = GodotReal.readIndex(ret, 0),
                y = GodotReal.readIndex(ret, 1),
                z = GodotReal.readIndex(ret, 2),
            )
        }
    }

    fun ptrcallWithIntArgRetVector3(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Int,
    ): Vector3 = ptrcallWithIntArgRetVector3(methodBind, instance, value.toLong())

    fun ptrcallWithIntArgRetBasis(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Int,
    ): Basis {
        Arena.ofConfined().use { arena ->
            val arg = arena.allocate(JAVA_INT)
            arg.set(JAVA_INT, 0, value)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, arg)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 9, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return readBasis(ret)
        }
    }

    fun ptrcallWithIntArgRetPlane(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Int,
    ): Plane {
        Arena.ofConfined().use { arena ->
            val arg = arena.allocate(JAVA_INT)
            arg.set(JAVA_INT, 0, value)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, arg)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return readPlane(ret)
        }
    }

    fun ptrcallNoArgsRetBasis(
        methodBind: MemorySegment,
        instance: MemorySegment,
    ): Basis {
        val ret = ptrcallScratch.get().basisRet
        objectMethodBindPtrcall.invoke(methodBind, instance, MemorySegment.NULL, ret)
        return Basis(
            x = Vector3(GodotReal.readIndex(ret, 0), GodotReal.readIndex(ret, 3), GodotReal.readIndex(ret, 6)),
            y = Vector3(GodotReal.readIndex(ret, 1), GodotReal.readIndex(ret, 4), GodotReal.readIndex(ret, 7)),
            z = Vector3(GodotReal.readIndex(ret, 2), GodotReal.readIndex(ret, 5), GodotReal.readIndex(ret, 8)),
        )
    }

    fun ptrcallWithBasisArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Basis,
    ) {
        Arena.ofConfined().use { arena ->
            val basis = arena.allocate(GodotReal.SIZE_BYTES * 9, GodotReal.ALIGN_BYTES)
            GodotReal.writeIndex(basis, 0, value.x.x)
            GodotReal.writeIndex(basis, 1, value.y.x)
            GodotReal.writeIndex(basis, 2, value.z.x)
            GodotReal.writeIndex(basis, 3, value.x.y)
            GodotReal.writeIndex(basis, 4, value.y.y)
            GodotReal.writeIndex(basis, 5, value.z.y)
            GodotReal.writeIndex(basis, 6, value.x.z)
            GodotReal.writeIndex(basis, 7, value.y.z)
            GodotReal.writeIndex(basis, 8, value.z.z)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, basis)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDAndBasisArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Basis,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val basis = arena.allocate(GodotReal.SIZE_BYTES * 9, GodotReal.ALIGN_BYTES)
            ridCell.set(JAVA_LONG, 0, rid.value)
            GodotReal.writeIndex(basis, 0, value.x.x)
            GodotReal.writeIndex(basis, 1, value.y.x)
            GodotReal.writeIndex(basis, 2, value.z.x)
            GodotReal.writeIndex(basis, 3, value.x.y)
            GodotReal.writeIndex(basis, 4, value.y.y)
            GodotReal.writeIndex(basis, 5, value.z.y)
            GodotReal.writeIndex(basis, 6, value.x.z)
            GodotReal.writeIndex(basis, 7, value.y.z)
            GodotReal.writeIndex(basis, 8, value.z.z)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, ridCell)
            arr.setAtIndex(ADDRESS, 1, basis)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithBasisArgRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Basis,
    ): Long {
        Arena.ofConfined().use { arena ->
            val basis = arena.allocate(GodotReal.SIZE_BYTES * 9, GodotReal.ALIGN_BYTES)
            GodotReal.writeIndex(basis, 0, value.x.x)
            GodotReal.writeIndex(basis, 1, value.y.x)
            GodotReal.writeIndex(basis, 2, value.z.x)
            GodotReal.writeIndex(basis, 3, value.x.y)
            GodotReal.writeIndex(basis, 4, value.y.y)
            GodotReal.writeIndex(basis, 5, value.z.y)
            GodotReal.writeIndex(basis, 6, value.x.z)
            GodotReal.writeIndex(basis, 7, value.y.z)
            GodotReal.writeIndex(basis, 8, value.z.z)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, basis)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_LONG, 0)
        }
    }

    fun ptrcallWithBasisArgRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Basis,
    ): Int {
        Arena.ofConfined().use { arena ->
            val basis = arena.allocate(GodotReal.SIZE_BYTES * 9, GodotReal.ALIGN_BYTES)
            GodotReal.writeIndex(basis, 0, value.x.x)
            GodotReal.writeIndex(basis, 1, value.y.x)
            GodotReal.writeIndex(basis, 2, value.z.x)
            GodotReal.writeIndex(basis, 3, value.x.y)
            GodotReal.writeIndex(basis, 4, value.y.y)
            GodotReal.writeIndex(basis, 5, value.z.y)
            GodotReal.writeIndex(basis, 6, value.x.z)
            GodotReal.writeIndex(basis, 7, value.y.z)
            GodotReal.writeIndex(basis, 8, value.z.z)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, basis)
            val ret = arena.allocate(JAVA_INT)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_INT, 0)
        }
    }

    fun ptrcallNoArgsRetTransform3D(
        methodBind: MemorySegment,
        instance: MemorySegment,
    ): Transform3D {
        val ret = ptrcallScratch.get().transform3dRet
        objectMethodBindPtrcall.invoke(methodBind, instance, MemorySegment.NULL, ret)
        return Transform3D(
            basis = Basis(
                x = Vector3(GodotReal.readIndex(ret, 0), GodotReal.readIndex(ret, 3), GodotReal.readIndex(ret, 6)),
                y = Vector3(GodotReal.readIndex(ret, 1), GodotReal.readIndex(ret, 4), GodotReal.readIndex(ret, 7)),
                z = Vector3(GodotReal.readIndex(ret, 2), GodotReal.readIndex(ret, 5), GodotReal.readIndex(ret, 8)),
            ),
            origin = Vector3(GodotReal.readIndex(ret, 9), GodotReal.readIndex(ret, 10), GodotReal.readIndex(ret, 11)),
        )
    }

    fun ptrcallWithTransform3DArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Transform3D,
    ) {
        Arena.ofConfined().use { arena ->
            val transform = arena.allocate(GodotReal.SIZE_BYTES * 12, GodotReal.ALIGN_BYTES)
            GodotReal.writeIndex(transform, 0, value.basis.x.x)
            GodotReal.writeIndex(transform, 1, value.basis.y.x)
            GodotReal.writeIndex(transform, 2, value.basis.z.x)
            GodotReal.writeIndex(transform, 3, value.basis.x.y)
            GodotReal.writeIndex(transform, 4, value.basis.y.y)
            GodotReal.writeIndex(transform, 5, value.basis.z.y)
            GodotReal.writeIndex(transform, 6, value.basis.x.z)
            GodotReal.writeIndex(transform, 7, value.basis.y.z)
            GodotReal.writeIndex(transform, 8, value.basis.z.z)
            GodotReal.writeIndex(transform, 9, value.origin.x)
            GodotReal.writeIndex(transform, 10, value.origin.y)
            GodotReal.writeIndex(transform, 11, value.origin.z)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, transform)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithTransform3DVector3TwoColorLongArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        transformValue: Transform3D,
        vectorValue: Vector3,
        firstColor: Color,
        secondColor: Color,
        flags: Long,
    ) {
        Arena.ofConfined().use { arena ->
            val transform = arena.allocate(GodotReal.SIZE_BYTES * 12, GodotReal.ALIGN_BYTES)
            val vector = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            val color0 = arena.allocate(16L, 4L)
            val color1 = arena.allocate(16L, 4L)
            val flagsCell = arena.allocate(JAVA_LONG)
            writeTransform3D(transform, transformValue)
            GodotReal.writeIndex(vector, 0, vectorValue.x)
            GodotReal.writeIndex(vector, 1, vectorValue.y)
            GodotReal.writeIndex(vector, 2, vectorValue.z)
            color0.set(JAVA_FLOAT, 0, firstColor.r)
            color0.set(JAVA_FLOAT, 4, firstColor.g)
            color0.set(JAVA_FLOAT, 8, firstColor.b)
            color0.set(JAVA_FLOAT, 12, firstColor.a)
            color1.set(JAVA_FLOAT, 0, secondColor.r)
            color1.set(JAVA_FLOAT, 4, secondColor.g)
            color1.set(JAVA_FLOAT, 8, secondColor.b)
            color1.set(JAVA_FLOAT, 12, secondColor.a)
            flagsCell.set(JAVA_LONG, 0, flags)
            val arr = arena.allocate(ADDRESS, 5)
            arr.setAtIndex(ADDRESS, 0, transform)
            arr.setAtIndex(ADDRESS, 1, vector)
            arr.setAtIndex(ADDRESS, 2, color0)
            arr.setAtIndex(ADDRESS, 3, color1)
            arr.setAtIndex(ADDRESS, 4, flagsCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithTransform3DVector3TwoColorUInt32Args(
        methodBind: MemorySegment,
        instance: MemorySegment,
        transformValue: Transform3D,
        vectorValue: Vector3,
        firstColor: Color,
        secondColor: Color,
        flags: Long,
    ) {
        Arena.ofConfined().use { arena ->
            val transform = arena.allocate(GodotReal.SIZE_BYTES * 12, GodotReal.ALIGN_BYTES)
            val vector = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            val color0 = arena.allocate(16L, 4L)
            val color1 = arena.allocate(16L, 4L)
            val flagsCell = arena.allocate(JAVA_INT)
            writeTransform3D(transform, transformValue)
            writeVector3(vector, vectorValue)
            color0.set(JAVA_FLOAT, 0, firstColor.r)
            color0.set(JAVA_FLOAT, 4, firstColor.g)
            color0.set(JAVA_FLOAT, 8, firstColor.b)
            color0.set(JAVA_FLOAT, 12, firstColor.a)
            color1.set(JAVA_FLOAT, 0, secondColor.r)
            color1.set(JAVA_FLOAT, 4, secondColor.g)
            color1.set(JAVA_FLOAT, 8, secondColor.b)
            color1.set(JAVA_FLOAT, 12, secondColor.a)
            flagsCell.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(flags))
            val arr = arena.allocate(ADDRESS, 5)
            arr.setAtIndex(ADDRESS, 0, transform)
            arr.setAtIndex(ADDRESS, 1, vector)
            arr.setAtIndex(ADDRESS, 2, color0)
            arr.setAtIndex(ADDRESS, 3, color1)
            arr.setAtIndex(ADDRESS, 4, flagsCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (Transform3D, Vector3, Object*, scalar float, bool, int32) and bool return value.
     */
    fun ptrcallWithTransform3DVector3ObjectDoubleBoolIntArgsRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        transformValue: Transform3D,
        vectorValue: Vector3,
        objectArg: MemorySegment,
        margin: Double,
        recoveryAsCollision: Boolean,
        maxCollisions: Int,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val transform = arena.allocate(GodotReal.SIZE_BYTES * 12, GodotReal.ALIGN_BYTES)
            val vector = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            val objectCell = arena.allocate(ADDRESS)
            val marginCell = arena.allocate(JAVA_DOUBLE)
            val boolCell = arena.allocate(JAVA_BYTE)
            val maxCollisionsCell = arena.allocate(JAVA_INT)
            writeTransform3D(transform, transformValue)
            writeVector3(vector, vectorValue)
            objectCell.set(ADDRESS, 0, objectArg)
            marginCell.set(JAVA_DOUBLE, 0, margin)
            boolCell.set(JAVA_BYTE, 0, if (recoveryAsCollision) 1.toByte() else 0.toByte())
            maxCollisionsCell.set(JAVA_INT, 0, maxCollisions)
            val args = arena.allocate(ADDRESS, 6)
            args.setAtIndex(ADDRESS, 0, transform)
            args.setAtIndex(ADDRESS, 1, vector)
            args.setAtIndex(ADDRESS, 2, objectCell)
            args.setAtIndex(ADDRESS, 3, marginCell)
            args.setAtIndex(ADDRESS, 4, boolCell)
            args.setAtIndex(ADDRESS, 5, maxCollisionsCell)
            val ret = arena.allocate(JAVA_BYTE)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(JAVA_BYTE, 0) != 0.toByte()
        }
    }

    fun ptrcallWithTransform3DAndLongArgsRetTransform3D(
        methodBind: MemorySegment,
        instance: MemorySegment,
        transformValue: Transform3D,
        longValue: Long,
    ): Transform3D {
        Arena.ofConfined().use { arena ->
            val transform = arena.allocate(GodotReal.SIZE_BYTES * 12, GodotReal.ALIGN_BYTES)
            GodotReal.writeIndex(transform, 0, transformValue.basis.x.x)
            GodotReal.writeIndex(transform, 1, transformValue.basis.y.x)
            GodotReal.writeIndex(transform, 2, transformValue.basis.z.x)
            GodotReal.writeIndex(transform, 3, transformValue.basis.x.y)
            GodotReal.writeIndex(transform, 4, transformValue.basis.y.y)
            GodotReal.writeIndex(transform, 5, transformValue.basis.z.y)
            GodotReal.writeIndex(transform, 6, transformValue.basis.x.z)
            GodotReal.writeIndex(transform, 7, transformValue.basis.y.z)
            GodotReal.writeIndex(transform, 8, transformValue.basis.z.z)
            GodotReal.writeIndex(transform, 9, transformValue.origin.x)
            GodotReal.writeIndex(transform, 10, transformValue.origin.y)
            GodotReal.writeIndex(transform, 11, transformValue.origin.z)
            val longArg = arena.allocate(JAVA_LONG)
            longArg.set(JAVA_LONG, 0, longValue)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, transform)
            arr.setAtIndex(ADDRESS, 1, longArg)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 12, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return readTransform3D(ret)
        }
    }

    fun ptrcallWithTransform3DAndDoubleArgRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        transformValue: Transform3D,
        doubleValue: Double,
    ): Long {
        Arena.ofConfined().use { arena ->
            val transform = arena.allocate(GodotReal.SIZE_BYTES * 12, GodotReal.ALIGN_BYTES)
            val doubleArg = arena.allocate(JAVA_DOUBLE)
            writeTransform3D(transform, transformValue)
            doubleArg.set(JAVA_DOUBLE, 0, doubleValue)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, transform)
            arr.setAtIndex(ADDRESS, 1, doubleArg)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_LONG, 0)
        }
    }

    fun ptrcallNoArgsRetProjection(
        methodBind: MemorySegment,
        instance: MemorySegment,
    ): Projection {
        Arena.ofConfined().use { arena ->
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 16, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, MemorySegment.NULL, ret)
            return Projection(
                x = Vector4(
                    GodotReal.readIndex(ret, 0),
                    GodotReal.readIndex(ret, 1),
                    GodotReal.readIndex(ret, 2),
                    GodotReal.readIndex(ret, 3),
                ),
                y = Vector4(
                    GodotReal.readIndex(ret, 4),
                    GodotReal.readIndex(ret, 5),
                    GodotReal.readIndex(ret, 6),
                    GodotReal.readIndex(ret, 7),
                ),
                z = Vector4(
                    GodotReal.readIndex(ret, 8),
                    GodotReal.readIndex(ret, 9),
                    GodotReal.readIndex(ret, 10),
                    GodotReal.readIndex(ret, 11),
                ),
                w = Vector4(
                    GodotReal.readIndex(ret, 12),
                    GodotReal.readIndex(ret, 13),
                    GodotReal.readIndex(ret, 14),
                    GodotReal.readIndex(ret, 15),
                ),
            )
        }
    }

    /**
     * Calls [methodBind] with (Vector3, Vector3, bool) args and no return value.
     */
    fun ptrcallWithTwoVector3AndBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Vector3,
        second: Vector3,
        boolArg: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            GodotReal.writeIndex(arg0, 0, first.x)
            GodotReal.writeIndex(arg0, 1, first.y)
            GodotReal.writeIndex(arg0, 2, first.z)

            val arg1 = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            GodotReal.writeIndex(arg1, 0, second.x)
            GodotReal.writeIndex(arg1, 1, second.y)
            GodotReal.writeIndex(arg1, 2, second.z)

            val arg2 = arena.allocate(JAVA_BYTE)
            arg2.set(JAVA_BYTE, 0, if (boolArg) 1.toByte() else 0.toByte())

            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (Vector3, Vector3, Vector3, bool) args and no return value.
     */
    fun ptrcallWithThreeVector3AndBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Vector3,
        second: Vector3,
        third: Vector3,
        boolArg: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            GodotReal.writeIndex(arg0, 0, first.x)
            GodotReal.writeIndex(arg0, 1, first.y)
            GodotReal.writeIndex(arg0, 2, first.z)

            val arg1 = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            GodotReal.writeIndex(arg1, 0, second.x)
            GodotReal.writeIndex(arg1, 1, second.y)
            GodotReal.writeIndex(arg1, 2, second.z)

            val arg2 = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            GodotReal.writeIndex(arg2, 0, third.x)
            GodotReal.writeIndex(arg2, 1, third.y)
            GodotReal.writeIndex(arg2, 2, third.z)

            val arg3 = arena.allocate(JAVA_BYTE)
            arg3.set(JAVA_BYTE, 0, if (boolArg) 1.toByte() else 0.toByte())

            val arr = arena.allocate(ADDRESS, 4)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            arr.setAtIndex(ADDRESS, 3, arg3)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (Vector3, Vector3) args and no return value.
     */
    fun ptrcallWithTwoVector3Args(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Vector3,
        second: Vector3,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            GodotReal.writeIndex(arg0, 0, first.x)
            GodotReal.writeIndex(arg0, 1, first.y)
            GodotReal.writeIndex(arg0, 2, first.z)

            val arg1 = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            GodotReal.writeIndex(arg1, 0, second.x)
            GodotReal.writeIndex(arg1, 1, second.y)
            GodotReal.writeIndex(arg1, 2, second.z)

            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithTwoVector3ArgsRetVector2(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Vector3,
        second: Vector3,
    ): Vector2 {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            val arg1 = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            GodotReal.writeIndex(arg0, 0, first.x)
            GodotReal.writeIndex(arg0, 1, first.y)
            GodotReal.writeIndex(arg0, 2, first.z)
            GodotReal.writeIndex(arg1, 0, second.x)
            GodotReal.writeIndex(arg1, 1, second.y)
            GodotReal.writeIndex(arg1, 2, second.z)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return Vector2(
                x = GodotReal.readIndex(ret, 0),
                y = GodotReal.readIndex(ret, 1),
            )
        }
    }

    fun ptrcallWithThreeVector3ArgsRetVector3(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Vector3,
        second: Vector3,
        third: Vector3,
    ): Vector3 {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            val arg1 = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            val arg2 = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            GodotReal.writeIndex(arg0, 0, first.x)
            GodotReal.writeIndex(arg0, 1, first.y)
            GodotReal.writeIndex(arg0, 2, first.z)
            GodotReal.writeIndex(arg1, 0, second.x)
            GodotReal.writeIndex(arg1, 1, second.y)
            GodotReal.writeIndex(arg1, 2, second.z)
            GodotReal.writeIndex(arg2, 0, third.x)
            GodotReal.writeIndex(arg2, 1, third.y)
            GodotReal.writeIndex(arg2, 2, third.z)
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return Vector3(
                GodotReal.readIndex(ret, 0),
                GodotReal.readIndex(ret, 1),
                GodotReal.readIndex(ret, 2),
            )
        }
    }

    fun ptrcallWithThreeVector3AndIntArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Vector3,
        second: Vector3,
        third: Vector3,
        value: Int,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            val arg1 = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            val arg2 = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            val arg3 = arena.allocate(JAVA_INT)
            writeVector3(arg0, first)
            writeVector3(arg1, second)
            writeVector3(arg2, third)
            arg3.set(JAVA_INT, 0, value)
            val arr = arena.allocate(ADDRESS, 4)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            arr.setAtIndex(ADDRESS, 3, arg3)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithTwoVector3ArgsRetDictionary(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Vector3,
        second: Vector3,
    ): Map<String, Any?> {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            val arg1 = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            GodotReal.writeIndex(arg0, 0, first.x)
            GodotReal.writeIndex(arg0, 1, first.y)
            GodotReal.writeIndex(arg0, 2, first.z)
            GodotReal.writeIndex(arg1, 0, second.x)
            GodotReal.writeIndex(arg1, 1, second.y)
            GodotReal.writeIndex(arg1, 2, second.z)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            val ret = arena.allocate(8L, 8L)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return try {
                BuiltinTypes.readDictionaryScalars(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.DICTIONARY, ret)
            }
        }
    }

    /**
     * Calls [methodBind] with no args and returns Quaternion.
     */
    fun ptrcallNoArgsRetQuaternion(
        methodBind: MemorySegment,
        instance: MemorySegment,
    ): Quaternion {
        val ret = ptrcallScratch.get().quaternionRet
        objectMethodBindPtrcall.invoke(methodBind, instance, MemorySegment.NULL, ret)
        return Quaternion(
            x = GodotReal.readIndex(ret, 0),
            y = GodotReal.readIndex(ret, 1),
            z = GodotReal.readIndex(ret, 2),
            w = GodotReal.readIndex(ret, 3),
        )
    }

    fun ptrcallWithIntArgRetQuaternion(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Int,
    ): Quaternion {
        Arena.ofConfined().use { arena ->
            val intCell = arena.allocate(JAVA_INT)
            intCell.set(JAVA_INT, 0, value)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, intCell)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return Quaternion(
                x = GodotReal.readIndex(ret, 0),
                y = GodotReal.readIndex(ret, 1),
                z = GodotReal.readIndex(ret, 2),
                w = GodotReal.readIndex(ret, 3),
            )
        }
    }

    /**
     * Calls [methodBind] with one Quaternion arg and no return value.
     */
    fun ptrcallWithQuaternionArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Quaternion,
    ) {
        Arena.ofConfined().use { arena ->
            val q = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
            GodotReal.writeIndex(q, 0, value.x)
            GodotReal.writeIndex(q, 1, value.y)
            GodotReal.writeIndex(q, 2, value.z)
            GodotReal.writeIndex(q, 3, value.w)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, q)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithIntAndQuaternionArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        intArg: Int,
        value: Quaternion,
    ) {
        Arena.ofConfined().use { arena ->
            val intCell = arena.allocate(JAVA_INT)
            val q = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
            intCell.set(JAVA_INT, 0, intArg)
            GodotReal.writeIndex(q, 0, value.x)
            GodotReal.writeIndex(q, 1, value.y)
            GodotReal.writeIndex(q, 2, value.z)
            GodotReal.writeIndex(q, 3, value.w)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, intCell)
            arr.setAtIndex(ADDRESS, 1, q)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithTwoIntArgsRetQuaternion(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Int,
    ): Quaternion {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_INT)
            arg0.set(JAVA_INT, 0, first)
            arg1.set(JAVA_INT, 0, second)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return Quaternion(
                x = GodotReal.readIndex(ret, 0),
                y = GodotReal.readIndex(ret, 1),
                z = GodotReal.readIndex(ret, 2),
                w = GodotReal.readIndex(ret, 3),
            )
        }
    }

    fun ptrcallWithTwoIntAndQuaternionArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Int,
        value: Quaternion,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_INT)
            val q = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
            arg0.set(JAVA_INT, 0, first)
            arg1.set(JAVA_INT, 0, second)
            GodotReal.writeIndex(q, 0, value.x)
            GodotReal.writeIndex(q, 1, value.y)
            GodotReal.writeIndex(q, 2, value.z)
            GodotReal.writeIndex(q, 3, value.w)
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, q)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with one Vector2 arg and no return value.
     */
    fun ptrcallWithVector2Arg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Vector2,
    ) {
        val scratch = ptrcallScratch.get()
        GodotReal.writeIndex(scratch.vector2Cell, 0, value.x)
        GodotReal.writeIndex(scratch.vector2Cell, 1, value.y)
        scratch.args1.setAtIndex(ADDRESS, 0, scratch.vector2Cell)
        objectMethodBindPtrcall.invoke(methodBind, instance, scratch.args1, MemorySegment.NULL)
    }

    fun ptrcallWithVector2ArgRetVector2(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Vector2,
    ): Vector2 {
        Arena.ofConfined().use { arena ->
            val vec = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            GodotReal.writeIndex(vec, 0, value.x)
            GodotReal.writeIndex(vec, 1, value.y)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, vec)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return Vector2(
                x = GodotReal.readIndex(ret, 0),
                y = GodotReal.readIndex(ret, 1),
            )
        }
    }

    fun ptrcallWithVector2ArgRetVector2i(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Vector2,
    ): Vector2i {
        Arena.ofConfined().use { arena ->
            val vec = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            GodotReal.writeIndex(vec, 0, value.x)
            GodotReal.writeIndex(vec, 1, value.y)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, vec)
            val ret = arena.allocate(8L, 4L)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return Vector2i(
                x = ret.get(JAVA_INT, 0),
                y = ret.get(JAVA_INT, 4),
            )
        }
    }

    fun ptrcallWithVector2ArgRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Vector2,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val vec = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            GodotReal.writeIndex(vec, 0, value.x)
            GodotReal.writeIndex(vec, 1, value.y)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, vec)
            val ret = arena.allocate(JAVA_BYTE)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_BYTE, 0) != 0.toByte()
        }
    }

    fun ptrcallWithVector2ArgRetDouble(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Vector2,
    ): Double {
        Arena.ofConfined().use { arena ->
            val vec = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            GodotReal.writeIndex(vec, 0, value.x)
            GodotReal.writeIndex(vec, 1, value.y)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, vec)
            val ret = arena.allocate(JAVA_DOUBLE)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_DOUBLE, 0)
        }
    }

    fun ptrcallWithVector2ArgRetVector3(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Vector2,
    ): Vector3 {
        Arena.ofConfined().use { arena ->
            val vec = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            GodotReal.writeIndex(vec, 0, value.x)
            GodotReal.writeIndex(vec, 1, value.y)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, vec)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return Vector3(
                x = GodotReal.readIndex(ret, 0),
                y = GodotReal.readIndex(ret, 1),
                z = GodotReal.readIndex(ret, 2),
            )
        }
    }

    fun ptrcallWithVector2AndDoubleArgRetVector3(
        methodBind: MemorySegment,
        instance: MemorySegment,
        vector: Vector2,
        value: Double,
    ): Vector3 {
        Arena.ofConfined().use { arena ->
            val vectorCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val doubleCell = arena.allocate(JAVA_DOUBLE)
            GodotReal.writeIndex(vectorCell, 0, vector.x)
            GodotReal.writeIndex(vectorCell, 1, vector.y)
            doubleCell.set(JAVA_DOUBLE, 0, value)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, vectorCell)
            arr.setAtIndex(ADDRESS, 1, doubleCell)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return Vector3(
                x = GodotReal.readIndex(ret, 0),
                y = GodotReal.readIndex(ret, 1),
                z = GodotReal.readIndex(ret, 2),
            )
        }
    }

    fun ptrcallWithVector2AndDoubleArgRetDictionary(
        methodBind: MemorySegment,
        instance: MemorySegment,
        vector: Vector2,
        value: Double,
    ): Map<String, Any?> {
        Arena.ofConfined().use { arena ->
            val vectorCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val doubleCell = arena.allocate(JAVA_DOUBLE)
            GodotReal.writeIndex(vectorCell, 0, vector.x)
            GodotReal.writeIndex(vectorCell, 1, vector.y)
            doubleCell.set(JAVA_DOUBLE, 0, value)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, vectorCell)
            arr.setAtIndex(ADDRESS, 1, doubleCell)
            val ret = arena.allocate(8L, 8L)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return try {
                BuiltinTypes.readDictionaryScalars(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.DICTIONARY, ret)
            }
        }
    }

    /**
     * Calls [methodBind] with (Vector2, bool, scalar float, bool) args and an Object return.
     */
    fun ptrcallWithVector2BoolFloatBoolArgsRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        vector: Vector2,
        firstBool: Boolean,
        floatValue: Double,
        secondBool: Boolean,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val vectorCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val firstBoolCell = arena.allocate(JAVA_BYTE)
            val floatCell = arena.allocate(JAVA_DOUBLE)
            val secondBoolCell = arena.allocate(JAVA_BYTE)
            GodotReal.writeIndex(vectorCell, 0, vector.x)
            GodotReal.writeIndex(vectorCell, 1, vector.y)
            firstBoolCell.set(JAVA_BYTE, 0, if (firstBool) 1.toByte() else 0.toByte())
            floatCell.set(JAVA_DOUBLE, 0, floatValue)
            secondBoolCell.set(JAVA_BYTE, 0, if (secondBool) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 4)
            args.setAtIndex(ADDRESS, 0, vectorCell)
            args.setAtIndex(ADDRESS, 1, firstBoolCell)
            args.setAtIndex(ADDRESS, 2, floatCell)
            args.setAtIndex(ADDRESS, 3, secondBoolCell)
            val ret = arena.allocate(ADDRESS)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(ADDRESS, 0)
        }
    }

    fun ptrcallWithVector2ArgRetString(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Vector2,
    ): String {
        Arena.ofConfined().use { arena ->
            val vec = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            GodotReal.writeIndex(vec, 0, value.x)
            GodotReal.writeIndex(vec, 1, value.y)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, vec)
            val ret = arena.allocate(8L, 8L)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return try {
                GodotStrings.readString(ret)
            } finally {
                GodotStrings.destroyString(ret)
            }
        }
    }

    /**
     * Calls [methodBind] with (Vector2, Vector2) args and no return value.
     */
    fun ptrcallWithTwoVector2Args(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Vector2,
        second: Vector2,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            GodotReal.writeIndex(arg0, 0, first.x)
            GodotReal.writeIndex(arg0, 1, first.y)

            val arg1 = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            GodotReal.writeIndex(arg1, 0, second.x)
            GodotReal.writeIndex(arg1, 1, second.y)

            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithTwoVector2ArgsRetPackedVector2List(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Vector2,
        second: Vector2,
    ): List<Vector2> {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            GodotReal.writeIndex(arg0, 0, first.x)
            GodotReal.writeIndex(arg0, 1, first.y)
            val arg1 = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            GodotReal.writeIndex(arg1, 0, second.x)
            GodotReal.writeIndex(arg1, 1, second.y)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return try {
                BuiltinTypes.readPackedVector2Array(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR2_ARRAY, ret)
            }
        }
    }

    fun ptrcallWithThreeVector2ArgsRetVector2(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Vector2,
        second: Vector2,
        third: Vector2,
    ): Vector2 {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val arg1 = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val arg2 = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            GodotReal.writeIndex(arg0, 0, first.x)
            GodotReal.writeIndex(arg0, 1, first.y)
            GodotReal.writeIndex(arg1, 0, second.x)
            GodotReal.writeIndex(arg1, 1, second.y)
            GodotReal.writeIndex(arg2, 0, third.x)
            GodotReal.writeIndex(arg2, 1, third.y)
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return Vector2(GodotReal.readIndex(ret, 0), GodotReal.readIndex(ret, 1))
        }
    }

    fun ptrcallWithVector2TwoDoubleTwoLongArgsRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        vector: Vector2,
        firstDouble: Double,
        secondDouble: Double,
        firstLong: Long,
        secondLong: Long,
    ): Int {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val arg1 = arena.allocate(JAVA_DOUBLE)
            val arg2 = arena.allocate(JAVA_DOUBLE)
            val arg3 = arena.allocate(JAVA_LONG)
            val arg4 = arena.allocate(JAVA_LONG)
            writeVector2(arg0, vector)
            arg1.set(JAVA_DOUBLE, 0, firstDouble)
            arg2.set(JAVA_DOUBLE, 0, secondDouble)
            arg3.set(JAVA_LONG, 0, firstLong)
            arg4.set(JAVA_LONG, 0, secondLong)
            val arr = arena.allocate(ADDRESS, 5)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            arr.setAtIndex(ADDRESS, 3, arg3)
            arr.setAtIndex(ADDRESS, 4, arg4)
            val ret = arena.allocate(JAVA_INT)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_INT, 0)
        }
    }

    fun ptrcallWithThreeVector2AndIntArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Vector2,
        second: Vector2,
        third: Vector2,
        value: Int,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val arg1 = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val arg2 = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val arg3 = arena.allocate(JAVA_INT)
            writeVector2(arg0, first)
            writeVector2(arg1, second)
            writeVector2(arg2, third)
            arg3.set(JAVA_INT, 0, value)
            val arr = arena.allocate(ADDRESS, 4)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            arr.setAtIndex(ADDRESS, 3, arg3)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithVector2iArgRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Vector2i,
    ): Int {
        Arena.ofConfined().use { arena ->
            val vec = arena.allocate(8L, 4L)
            vec.set(JAVA_INT, 0, value.x)
            vec.set(JAVA_INT, 4, value.y)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, vec)
            val ret = arena.allocate(JAVA_INT)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_INT, 0)
        }
    }

    fun ptrcallWithVector2iArgRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Vector2i,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val vec = arena.allocate(8L, 4L)
            vec.set(JAVA_INT, 0, value.x)
            vec.set(JAVA_INT, 4, value.y)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, vec)
            val ret = arena.allocate(JAVA_BYTE)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_BYTE, 0) != 0.toByte()
        }
    }

    fun ptrcallWithVector2iArgRetVector2i(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Vector2i,
    ): Vector2i {
        Arena.ofConfined().use { arena ->
            val vec = arena.allocate(8L, 4L)
            vec.set(JAVA_INT, 0, value.x)
            vec.set(JAVA_INT, 4, value.y)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, vec)
            val ret = arena.allocate(8L, 4L)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return Vector2i(
                x = ret.get(JAVA_INT, 0),
                y = ret.get(JAVA_INT, 4),
            )
        }
    }

    fun ptrcallWithVector2iArgRetDouble(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Vector2i,
    ): Double {
        Arena.ofConfined().use { arena ->
            val vec = arena.allocate(8L, 4L)
            vec.set(JAVA_INT, 0, value.x)
            vec.set(JAVA_INT, 4, value.y)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, vec)
            val ret = arena.allocate(JAVA_DOUBLE)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_DOUBLE, 0)
        }
    }

    fun ptrcallWithVector2iArgRetVector2(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Vector2i,
    ): Vector2 {
        Arena.ofConfined().use { arena ->
            val vec = arena.allocate(8L, 4L)
            vec.set(JAVA_INT, 0, value.x)
            vec.set(JAVA_INT, 4, value.y)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, vec)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return Vector2(
                x = GodotReal.readIndex(ret, 0),
                y = GodotReal.readIndex(ret, 1),
            )
        }
    }

    fun ptrcallWithVector2iArgRetColor(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Vector2i,
    ): Color {
        Arena.ofConfined().use { arena ->
            val vec = arena.allocate(8L, 4L)
            vec.set(JAVA_INT, 0, value.x)
            vec.set(JAVA_INT, 4, value.y)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, vec)
            val ret = arena.allocate(16L, 4L)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return Color(
                ret.get(JAVA_FLOAT, 0),
                ret.get(JAVA_FLOAT, 4),
                ret.get(JAVA_FLOAT, 8),
                ret.get(JAVA_FLOAT, 12),
            )
        }
    }

    fun ptrcallWithVector2iArgRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Vector2i,
    ): Long {
        Arena.ofConfined().use { arena ->
            val vec = arena.allocate(8L, 4L)
            writeVector2i(vec, value)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, vec)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_LONG, 0)
        }
    }

    fun ptrcallWithVector2iArgRetVector2iList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Vector2i,
    ): List<Vector2i> {
        Arena.ofConfined().use { arena ->
            val vec = arena.allocate(8L, 4L)
            vec.set(JAVA_INT, 0, value.x)
            vec.set(JAVA_INT, 4, value.y)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, vec)
            return callArrayReturn(methodBind, instance, arr, BuiltinTypes::readArrayVector2i)
        }
    }

    fun ptrcallWithVector2iAndLongArgRetVector2i(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Vector2i,
        kind: Long,
    ): Vector2i {
        Arena.ofConfined().use { arena ->
            val vec = arena.allocate(8L, 4L)
            val longCell = arena.allocate(JAVA_LONG)
            vec.set(JAVA_INT, 0, value.x)
            vec.set(JAVA_INT, 4, value.y)
            longCell.set(JAVA_LONG, 0, kind)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, vec)
            arr.setAtIndex(ADDRESS, 1, longCell)
            val ret = arena.allocate(8L, 4L)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return Vector2i(
                x = ret.get(JAVA_INT, 0),
                y = ret.get(JAVA_INT, 4),
            )
        }
    }

    fun ptrcallWithVector2iAndLongArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Vector2i,
        kind: Long,
    ) {
        Arena.ofConfined().use { arena ->
            val vec = arena.allocate(8L, 4L)
            val longCell = arena.allocate(JAVA_LONG)
            writeVector2i(vec, value)
            longCell.set(JAVA_LONG, 0, kind)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, vec)
            arr.setAtIndex(ADDRESS, 1, longCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithTwoVector2iArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Vector2i,
        second: Vector2i,
    ) {
        Arena.ofConfined().use { arena ->
            val firstVec = arena.allocate(8L, 4L)
            val secondVec = arena.allocate(8L, 4L)
            firstVec.set(JAVA_INT, 0, first.x)
            firstVec.set(JAVA_INT, 4, first.y)
            secondVec.set(JAVA_INT, 0, second.x)
            secondVec.set(JAVA_INT, 4, second.y)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, firstVec)
            arr.setAtIndex(ADDRESS, 1, secondVec)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithThreeVector2iArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Vector2i,
        second: Vector2i,
        third: Vector2i,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(8L, 4L)
            val arg1 = arena.allocate(8L, 4L)
            val arg2 = arena.allocate(8L, 4L)
            writeVector2i(arg0, first)
            writeVector2i(arg1, second)
            writeVector2i(arg2, third)
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithTwoVector2iIntVector2iIntVector2iArgsRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Vector2i,
        second: Vector2i,
        firstInt: Int,
        third: Vector2i,
        secondInt: Int,
        fourth: Vector2i,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(8L, 4L)
            val arg1 = arena.allocate(8L, 4L)
            val arg2 = arena.allocate(JAVA_INT)
            val arg3 = arena.allocate(8L, 4L)
            val arg4 = arena.allocate(JAVA_INT)
            val arg5 = arena.allocate(8L, 4L)
            writeVector2i(arg0, first)
            writeVector2i(arg1, second)
            arg2.set(JAVA_INT, 0, firstInt)
            writeVector2i(arg3, third)
            arg4.set(JAVA_INT, 0, secondInt)
            writeVector2i(arg5, fourth)
            val arr = arena.allocate(ADDRESS, 6)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            arr.setAtIndex(ADDRESS, 3, arg3)
            arr.setAtIndex(ADDRESS, 4, arg4)
            arr.setAtIndex(ADDRESS, 5, arg5)
            val ret = arena.allocate(JAVA_BYTE)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_BYTE, 0) != 0.toByte()
        }
    }

    fun ptrcallWithTwoVector2iArgsRetVector2iList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Vector2i,
        second: Vector2i,
    ): List<Vector2i> {
        Arena.ofConfined().use { arena ->
            val firstVec = arena.allocate(8L, 4L)
            val secondVec = arena.allocate(8L, 4L)
            firstVec.set(JAVA_INT, 0, first.x)
            firstVec.set(JAVA_INT, 4, first.y)
            secondVec.set(JAVA_INT, 0, second.x)
            secondVec.set(JAVA_INT, 4, second.y)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, firstVec)
            arr.setAtIndex(ADDRESS, 1, secondVec)
            return callArrayReturn(methodBind, instance, arr, BuiltinTypes::readArrayVector2i)
        }
    }

    fun ptrcallWithTwoVector2iAndObjectArgRetVector2i(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Vector2i,
        second: Vector2i,
        objectArg: MemorySegment,
    ): Vector2i {
        Arena.ofConfined().use { arena ->
            val firstVec = arena.allocate(8L, 4L)
            val secondVec = arena.allocate(8L, 4L)
            val objCell = arena.allocate(ADDRESS)
            firstVec.set(JAVA_INT, 0, first.x)
            firstVec.set(JAVA_INT, 4, first.y)
            secondVec.set(JAVA_INT, 0, second.x)
            secondVec.set(JAVA_INT, 4, second.y)
            objCell.set(ADDRESS, 0, objectArg)
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, firstVec)
            arr.setAtIndex(ADDRESS, 1, secondVec)
            arr.setAtIndex(ADDRESS, 2, objCell)
            val ret = arena.allocate(8L, 4L)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return Vector2i(
                x = ret.get(JAVA_INT, 0),
                y = ret.get(JAVA_INT, 4),
            )
        }
    }

    fun ptrcallWithTwoVector2iAndBoolArgsRetPackedVector2List(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Vector2i,
        second: Vector2i,
        flag: Boolean,
    ): List<Vector2> {
        Arena.ofConfined().use { arena ->
            val firstVec = arena.allocate(8L, 4L)
            val secondVec = arena.allocate(8L, 4L)
            val boolCell = arena.allocate(JAVA_BYTE)
            firstVec.set(JAVA_INT, 0, first.x)
            firstVec.set(JAVA_INT, 4, first.y)
            secondVec.set(JAVA_INT, 0, second.x)
            secondVec.set(JAVA_INT, 4, second.y)
            boolCell.set(JAVA_BYTE, 0, if (flag) 1.toByte() else 0.toByte())
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, firstVec)
            arr.setAtIndex(ADDRESS, 1, secondVec)
            arr.setAtIndex(ADDRESS, 2, boolCell)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return try {
                BuiltinTypes.readPackedVector2Array(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR2_ARRAY, ret)
            }
        }
    }

    fun ptrcallWithVector2iAndBoolArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Vector2i,
        flag: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val vec = arena.allocate(8L, 4L)
            val boolCell = arena.allocate(JAVA_BYTE)
            vec.set(JAVA_INT, 0, value.x)
            vec.set(JAVA_INT, 4, value.y)
            boolCell.set(JAVA_BYTE, 0, if (flag) 1.toByte() else 0.toByte())
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, vec)
            arr.setAtIndex(ADDRESS, 1, boolCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithVector2iAndTwoBoolArgsRetVector2i(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Vector2i,
        firstBool: Boolean,
        secondBool: Boolean,
    ): Vector2i {
        Arena.ofConfined().use { arena ->
            val vec = arena.allocate(8L, 4L)
            val bool0 = arena.allocate(JAVA_BYTE)
            val bool1 = arena.allocate(JAVA_BYTE)
            vec.set(JAVA_INT, 0, value.x)
            vec.set(JAVA_INT, 4, value.y)
            bool0.set(JAVA_BYTE, 0, if (firstBool) 1.toByte() else 0.toByte())
            bool1.set(JAVA_BYTE, 0, if (secondBool) 1.toByte() else 0.toByte())
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, vec)
            arr.setAtIndex(ADDRESS, 1, bool0)
            arr.setAtIndex(ADDRESS, 2, bool1)
            val ret = arena.allocate(8L, 4L)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return readVector2i(ret)
        }
    }

    fun ptrcallWithVector2iAndDoubleArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Vector2i,
        amount: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val vec = arena.allocate(8L, 4L)
            val amountCell = arena.allocate(JAVA_DOUBLE)
            vec.set(JAVA_INT, 0, value.x)
            vec.set(JAVA_INT, 4, value.y)
            amountCell.set(JAVA_DOUBLE, 0, amount)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, vec)
            arr.setAtIndex(ADDRESS, 1, amountCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithVector2iIntDoubleArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Vector2i,
        intValue: Int,
        doubleValue: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(8L, 4L)
            val arg1 = arena.allocate(JAVA_INT)
            val arg2 = arena.allocate(JAVA_DOUBLE)
            writeVector2i(arg0, value)
            arg1.set(JAVA_INT, 0, intValue)
            arg2.set(JAVA_DOUBLE, 0, doubleValue)
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithVector2iAndTwoIntArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Vector2i,
        firstInt: Int,
        secondInt: Int,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(8L, 4L)
            val arg1 = arena.allocate(JAVA_INT)
            val arg2 = arena.allocate(JAVA_INT)
            writeVector2i(arg0, value)
            arg1.set(JAVA_INT, 0, firstInt)
            arg2.set(JAVA_INT, 0, secondInt)
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithVector2iAndIntArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Vector2i,
        intValue: Int,
    ) {
        Arena.ofConfined().use { arena ->
            val vec = arena.allocate(8L, 4L)
            val intCell = arena.allocate(JAVA_INT)
            vec.set(JAVA_INT, 0, value.x)
            vec.set(JAVA_INT, 4, value.y)
            intCell.set(JAVA_INT, 0, intValue)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, vec)
            arr.setAtIndex(ADDRESS, 1, intCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithVector2iAndIntArgRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Vector2i,
        intValue: Int,
    ): Int {
        Arena.ofConfined().use { arena ->
            val vec = arena.allocate(8L, 4L)
            val intCell = arena.allocate(JAVA_INT)
            vec.set(JAVA_INT, 0, value.x)
            vec.set(JAVA_INT, 4, value.y)
            intCell.set(JAVA_INT, 0, intValue)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, vec)
            arr.setAtIndex(ADDRESS, 1, intCell)
            val ret = arena.allocate(JAVA_INT)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_INT, 0)
        }
    }

    fun ptrcallWithVector2iAndIntArgRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Vector2i,
        intValue: Int,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val vec = arena.allocate(8L, 4L)
            val intCell = arena.allocate(JAVA_INT)
            vec.set(JAVA_INT, 0, value.x)
            vec.set(JAVA_INT, 4, value.y)
            intCell.set(JAVA_INT, 0, intValue)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, vec)
            arr.setAtIndex(ADDRESS, 1, intCell)
            val ret = arena.allocate(JAVA_BYTE)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_BYTE, 0) != 0.toByte()
        }
    }

    fun ptrcallWithVector2iAndIntArgRetDouble(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Vector2i,
        intValue: Int,
    ): Double {
        Arena.ofConfined().use { arena ->
            val vec = arena.allocate(8L, 4L)
            val intCell = arena.allocate(JAVA_INT)
            vec.set(JAVA_INT, 0, value.x)
            vec.set(JAVA_INT, 4, value.y)
            intCell.set(JAVA_INT, 0, intValue)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, vec)
            arr.setAtIndex(ADDRESS, 1, intCell)
            val ret = arena.allocate(JAVA_DOUBLE)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_DOUBLE, 0)
        }
    }

    fun ptrcallWithVector2iAndIntArgRetRect2i(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Vector2i,
        intValue: Int,
    ): Rect2i {
        Arena.ofConfined().use { arena ->
            val vec = arena.allocate(8L, 4L)
            val intCell = arena.allocate(JAVA_INT)
            vec.set(JAVA_INT, 0, value.x)
            vec.set(JAVA_INT, 4, value.y)
            intCell.set(JAVA_INT, 0, intValue)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, vec)
            arr.setAtIndex(ADDRESS, 1, intCell)
            val ret = arena.allocate(16L, 4L)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return Rect2i(
                position = Vector2i(ret.get(JAVA_INT, 0), ret.get(JAVA_INT, 4)),
                size = Vector2i(ret.get(JAVA_INT, 8), ret.get(JAVA_INT, 12)),
            )
        }
    }

    fun ptrcallWithIntVector2iAndBoolArgRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        intValue: Int,
        value: Vector2i,
        flag: Boolean,
    ): Int {
        Arena.ofConfined().use { arena ->
            val intCell = arena.allocate(JAVA_INT)
            val vec = arena.allocate(8L, 4L)
            val boolCell = arena.allocate(JAVA_BYTE)
            intCell.set(JAVA_INT, 0, intValue)
            vec.set(JAVA_INT, 0, value.x)
            vec.set(JAVA_INT, 4, value.y)
            boolCell.set(JAVA_BYTE, 0, if (flag) 1.toByte() else 0.toByte())
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, intCell)
            arr.setAtIndex(ADDRESS, 1, vec)
            arr.setAtIndex(ADDRESS, 2, boolCell)
            val ret = arena.allocate(JAVA_INT)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_INT, 0)
        }
    }

    fun ptrcallWithIntVector2iAndBoolArgRetVector2i(
        methodBind: MemorySegment,
        instance: MemorySegment,
        intValue: Int,
        value: Vector2i,
        flag: Boolean,
    ): Vector2i {
        Arena.ofConfined().use { arena ->
            val intCell = arena.allocate(JAVA_INT)
            val vec = arena.allocate(8L, 4L)
            val boolCell = arena.allocate(JAVA_BYTE)
            intCell.set(JAVA_INT, 0, intValue)
            vec.set(JAVA_INT, 0, value.x)
            vec.set(JAVA_INT, 4, value.y)
            boolCell.set(JAVA_BYTE, 0, if (flag) 1.toByte() else 0.toByte())
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, intCell)
            arr.setAtIndex(ADDRESS, 1, vec)
            arr.setAtIndex(ADDRESS, 2, boolCell)
            val ret = arena.allocate(8L, 4L)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return Vector2i(ret.get(JAVA_INT, 0), ret.get(JAVA_INT, 4))
        }
    }

    fun ptrcallWithIntVector2iAndBoolArgRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        intValue: Int,
        value: Vector2i,
        flag: Boolean,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val intCell = arena.allocate(JAVA_INT)
            val vec = arena.allocate(8L, 4L)
            val boolCell = arena.allocate(JAVA_BYTE)
            intCell.set(JAVA_INT, 0, intValue)
            vec.set(JAVA_INT, 0, value.x)
            vec.set(JAVA_INT, 4, value.y)
            boolCell.set(JAVA_BYTE, 0, if (flag) 1.toByte() else 0.toByte())
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, intCell)
            arr.setAtIndex(ADDRESS, 1, vec)
            arr.setAtIndex(ADDRESS, 2, boolCell)
            val ret = arena.allocate(JAVA_BYTE)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_BYTE, 0) != 0.toByte()
        }
    }

    fun ptrcallWithIntVector2iAndIntArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstInt: Int,
        value: Vector2i,
        secondInt: Int,
    ) {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(JAVA_INT)
            val vec = arena.allocate(8L, 4L)
            val secondCell = arena.allocate(JAVA_INT)
            firstCell.set(JAVA_INT, 0, firstInt)
            vec.set(JAVA_INT, 0, value.x)
            vec.set(JAVA_INT, 4, value.y)
            secondCell.set(JAVA_INT, 0, secondInt)
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, firstCell)
            arr.setAtIndex(ADDRESS, 1, vec)
            arr.setAtIndex(ADDRESS, 2, secondCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithIntVector2iIntArgsRetVector2(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstInt: Int,
        value: Vector2i,
        secondInt: Int,
    ): Vector2 {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(JAVA_INT)
            val vec = arena.allocate(8L, 4L)
            val secondCell = arena.allocate(JAVA_INT)
            firstCell.set(JAVA_INT, 0, firstInt)
            vec.set(JAVA_INT, 0, value.x)
            vec.set(JAVA_INT, 4, value.y)
            secondCell.set(JAVA_INT, 0, secondInt)
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, firstCell)
            arr.setAtIndex(ADDRESS, 1, vec)
            arr.setAtIndex(ADDRESS, 2, secondCell)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return Vector2(
                x = GodotReal.readIndex(ret, 0),
                y = GodotReal.readIndex(ret, 1),
            )
        }
    }

    fun ptrcallWithIntVector2iIntArgsRetPackedInt32List(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstInt: Int,
        value: Vector2i,
        secondInt: Int,
    ): List<Int> {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(8L, 4L)
            val arg2 = arena.allocate(JAVA_INT)
            arg0.set(JAVA_INT, 0, firstInt)
            writeVector2i(arg1, value)
            arg2.set(JAVA_INT, 0, secondInt)
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return try {
                BuiltinTypes.readPackedInt32Array(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_INT32_ARRAY, ret)
            }
        }
    }

    fun ptrcallWithIntVector2iIntArgsRetArray(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstInt: Int,
        value: Vector2i,
        secondInt: Int,
    ): List<Any?> {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(8L, 4L)
            val arg2 = arena.allocate(JAVA_INT)
            arg0.set(JAVA_INT, 0, firstInt)
            writeVector2i(arg1, value)
            arg2.set(JAVA_INT, 0, secondInt)
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            return callArrayReturn(methodBind, instance, arr, BuiltinTypes::readArrayScalars)
        }
    }

    fun ptrcallWithIntVector2iIntPackedInt32ListArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstInt: Int,
        value: Vector2i,
        secondInt: Int,
        values: List<Int>,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(8L, 4L)
            val arg2 = arena.allocate(JAVA_INT)
            val arg3 = BuiltinTypes.allocatePackedArray(arena)
            arg0.set(JAVA_INT, 0, firstInt)
            writeVector2i(arg1, value)
            arg2.set(JAVA_INT, 0, secondInt)
            try {
                BuiltinTypes.initPackedInt32Array(arg3, values)
                val arr = arena.allocate(ADDRESS, 4)
                arr.setAtIndex(ADDRESS, 0, arg0)
                arr.setAtIndex(ADDRESS, 1, arg1)
                arr.setAtIndex(ADDRESS, 2, arg2)
                arr.setAtIndex(ADDRESS, 3, arg3)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_INT32_ARRAY, arg3)
            }
        }
    }

    fun ptrcallWithIntVector2iIntVector2Args(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstInt: Int,
        value: Vector2i,
        secondInt: Int,
        vector: Vector2,
    ) {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(JAVA_INT)
            val vec2i = arena.allocate(8L, 4L)
            val secondCell = arena.allocate(JAVA_INT)
            val vec2 = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            firstCell.set(JAVA_INT, 0, firstInt)
            vec2i.set(JAVA_INT, 0, value.x)
            vec2i.set(JAVA_INT, 4, value.y)
            secondCell.set(JAVA_INT, 0, secondInt)
            GodotReal.writeIndex(vec2, 0, vector.x)
            GodotReal.writeIndex(vec2, 1, vector.y)
            val arr = arena.allocate(ADDRESS, 4)
            arr.setAtIndex(ADDRESS, 0, firstCell)
            arr.setAtIndex(ADDRESS, 1, vec2i)
            arr.setAtIndex(ADDRESS, 2, secondCell)
            arr.setAtIndex(ADDRESS, 3, vec2)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithVector2iIntVector2iIntArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstVector: Vector2i,
        firstInt: Int,
        secondVector: Vector2i,
        secondInt: Int,
    ) {
        Arena.ofConfined().use { arena ->
            val vec0 = arena.allocate(8L, 4L)
            val int0 = arena.allocate(JAVA_INT)
            val vec1 = arena.allocate(8L, 4L)
            val int1 = arena.allocate(JAVA_INT)
            vec0.set(JAVA_INT, 0, firstVector.x)
            vec0.set(JAVA_INT, 4, firstVector.y)
            int0.set(JAVA_INT, 0, firstInt)
            vec1.set(JAVA_INT, 0, secondVector.x)
            vec1.set(JAVA_INT, 4, secondVector.y)
            int1.set(JAVA_INT, 0, secondInt)
            val arr = arena.allocate(ADDRESS, 4)
            arr.setAtIndex(ADDRESS, 0, vec0)
            arr.setAtIndex(ADDRESS, 1, int0)
            arr.setAtIndex(ADDRESS, 2, vec1)
            arr.setAtIndex(ADDRESS, 3, int1)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithTwoVector2iAndBoolArgsRetVector2iList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Vector2i,
        second: Vector2i,
        flag: Boolean,
    ): List<Vector2i> {
        Arena.ofConfined().use { arena ->
            val firstVec = arena.allocate(8L, 4L)
            val secondVec = arena.allocate(8L, 4L)
            val boolCell = arena.allocate(JAVA_BYTE)
            firstVec.set(JAVA_INT, 0, first.x)
            firstVec.set(JAVA_INT, 4, first.y)
            secondVec.set(JAVA_INT, 0, second.x)
            secondVec.set(JAVA_INT, 4, second.y)
            boolCell.set(JAVA_BYTE, 0, if (flag) 1.toByte() else 0.toByte())
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, firstVec)
            arr.setAtIndex(ADDRESS, 1, secondVec)
            arr.setAtIndex(ADDRESS, 2, boolCell)
            return callArrayReturn(methodBind, instance, arr, BuiltinTypes::readArrayVector2i)
        }
    }

    fun ptrcallWithVector3iArgRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Vector3i,
    ): Int {
        Arena.ofConfined().use { arena ->
            val vec = arena.allocate(12L, 4L)
            writeVector3i(vec, value)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, vec)
            val ret = arena.allocate(JAVA_INT)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_INT, 0)
        }
    }

    fun ptrcallWithVector3iArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Vector3i,
    ) {
        Arena.ofConfined().use { arena ->
            val vec = arena.allocate(12L, 4L)
            writeVector3i(vec, value)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, vec)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithVector3iArgRetVector3(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Vector3i,
    ): Vector3 {
        Arena.ofConfined().use { arena ->
            val vec = arena.allocate(12L, 4L)
            writeVector3i(vec, value)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, vec)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return Vector3(
                x = GodotReal.readIndex(ret, 0),
                y = GodotReal.readIndex(ret, 1),
                z = GodotReal.readIndex(ret, 2),
            )
        }
    }

    fun ptrcallWithVector3iArgRetBasis(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Vector3i,
    ): Basis {
        Arena.ofConfined().use { arena ->
            val vec = arena.allocate(12L, 4L)
            writeVector3i(vec, value)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, vec)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 9, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return readBasis(ret)
        }
    }

    fun ptrcallWithVector3iArgRetVector3iList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Vector3i,
    ): List<Vector3i> {
        Arena.ofConfined().use { arena ->
            val vec = arena.allocate(12L, 4L)
            writeVector3i(vec, value)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, vec)
            return callArrayReturn(methodBind, instance, arr, BuiltinTypes::readArrayVector3i)
        }
    }

    fun ptrcallWithVector3iAndIntArgRetVector3iList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Vector3i,
        intArg: Int,
    ): List<Vector3i> {
        Arena.ofConfined().use { arena ->
            val vec = arena.allocate(12L, 4L)
            writeVector3i(vec, value)
            val intCell = arena.allocate(JAVA_INT)
            intCell.set(JAVA_INT, 0, intArg)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, vec)
            arr.setAtIndex(ADDRESS, 1, intCell)
            return callArrayReturn(methodBind, instance, arr, BuiltinTypes::readArrayVector3i)
        }
    }

    fun ptrcallWithAABBArgRetVector3iList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: AABB,
    ): List<Vector3i> {
        Arena.ofConfined().use { arena ->
            val aabb = arena.allocate(GodotReal.SIZE_BYTES * 6, GodotReal.ALIGN_BYTES)
            writeAABB(aabb, value)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, aabb)
            return callArrayReturn(methodBind, instance, arr, BuiltinTypes::readArrayVector3i)
        }
    }

    fun ptrcallWithLongArgRetPackedColorList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Long,
    ): List<Color> {
        Arena.ofConfined().use { arena ->
            val longCell = arena.allocate(JAVA_LONG)
            longCell.set(JAVA_LONG, 0, value)
            val args = arena.allocate(ADDRESS, 1)
            args.setAtIndex(ADDRESS, 0, longCell)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return try {
                BuiltinTypes.readPackedColorArray(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_COLOR_ARRAY, ret)
            }
        }
    }

    fun ptrcallWithRIDAndLongArgRetPackedColorList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Long,
    ): List<Color> {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val valueCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            valueCell.set(JAVA_LONG, 0, value)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, valueCell)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return try {
                BuiltinTypes.readPackedColorArray(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_COLOR_ARRAY, ret)
            }
        }
    }

    fun ptrcallWithDictionaryArgRetArray(
        methodBind: MemorySegment,
        instance: MemorySegment,
        values: Map<String, Any?>,
    ): List<Any?> {
        Arena.ofConfined().use { arena ->
            val dict = arena.allocate(8L, 8L)
            try {
                BuiltinTypes.initDictionary(dict, values)
                val args = arena.allocate(ADDRESS, 1)
                args.setAtIndex(ADDRESS, 0, dict)
                val ret = arena.allocate(8L, 8L)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return try {
                    BuiltinTypes.readArrayScalars(ret)
                } finally {
                    BuiltinTypes.destroyTyped(VariantType.ARRAY, ret)
                }
            } finally {
                BuiltinTypes.destroyTyped(VariantType.DICTIONARY, dict)
            }
        }
    }

    fun ptrcallWithTwoVector3iArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Vector3i,
        second: Vector3i,
    ) {
        Arena.ofConfined().use { arena ->
            val firstVec = arena.allocate(12L, 4L)
            val secondVec = arena.allocate(12L, 4L)
            writeVector3i(firstVec, first)
            writeVector3i(secondVec, second)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, firstVec)
            arr.setAtIndex(ADDRESS, 1, secondVec)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithVector3iIntIntArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        vectorArg: Vector3i,
        firstInt: Int,
        secondInt: Int,
    ) {
        Arena.ofConfined().use { arena ->
            val vec = arena.allocate(12L, 4L)
            writeVector3i(vec, vectorArg)
            val arg1 = arena.allocate(JAVA_INT)
            val arg2 = arena.allocate(JAVA_INT)
            arg1.set(JAVA_INT, 0, firstInt)
            arg2.set(JAVA_INT, 0, secondInt)
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, vec)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithIntVector2iIntArgsRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstInt: Int,
        value: Vector2i,
        secondInt: Int,
    ): Int {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(8L, 4L)
            val arg2 = arena.allocate(JAVA_INT)
            arg0.set(JAVA_INT, 0, firstInt)
            writeVector2i(arg1, value)
            arg2.set(JAVA_INT, 0, secondInt)
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            val ret = arena.allocate(JAVA_INT)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_INT, 0)
        }
    }

    fun ptrcallWithIntVector2iIntArgsRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstInt: Int,
        value: Vector2i,
        secondInt: Int,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(8L, 4L)
            val arg2 = arena.allocate(JAVA_INT)
            arg0.set(JAVA_INT, 0, firstInt)
            writeVector2i(arg1, value)
            arg2.set(JAVA_INT, 0, secondInt)
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            val ret = arena.allocate(JAVA_BYTE)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_BYTE, 0) != 0.toByte()
        }
    }

    fun ptrcallWithIntVector2iIntArgsRetRect2(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstInt: Int,
        value: Vector2i,
        secondInt: Int,
    ): Rect2 {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(8L, 4L)
            val arg2 = arena.allocate(JAVA_INT)
            arg0.set(JAVA_INT, 0, firstInt)
            writeVector2i(arg1, value)
            arg2.set(JAVA_INT, 0, secondInt)
            val arr = arena.allocate(ADDRESS, 3)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return readRect2(ret)
        }
    }

    fun ptrcallWithIntVector2iIntRect2Args(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstInt: Int,
        value: Vector2i,
        secondInt: Int,
        rect: Rect2,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(8L, 4L)
            val arg2 = arena.allocate(JAVA_INT)
            val arg3 = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
            arg0.set(JAVA_INT, 0, firstInt)
            writeVector2i(arg1, value)
            arg2.set(JAVA_INT, 0, secondInt)
            writeRect2(arg3, rect)
            val arr = arena.allocate(ADDRESS, 4)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            arr.setAtIndex(ADDRESS, 3, arg3)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithIntVector2iTwoIntArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstInt: Int,
        value: Vector2i,
        secondInt: Int,
        thirdInt: Int,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(8L, 4L)
            val arg2 = arena.allocate(JAVA_INT)
            val arg3 = arena.allocate(JAVA_INT)
            arg0.set(JAVA_INT, 0, firstInt)
            writeVector2i(arg1, value)
            arg2.set(JAVA_INT, 0, secondInt)
            arg3.set(JAVA_INT, 0, thirdInt)
            val arr = arena.allocate(ADDRESS, 4)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            arr.setAtIndex(ADDRESS, 3, arg3)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithIntVector2iTwoLongArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        intValue: Int,
        value: Vector2i,
        firstLong: Long,
        secondLong: Long,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(8L, 4L)
            val arg2 = arena.allocate(JAVA_LONG)
            val arg3 = arena.allocate(JAVA_LONG)
            arg0.set(JAVA_INT, 0, intValue)
            writeVector2i(arg1, value)
            arg2.set(JAVA_LONG, 0, firstLong)
            arg3.set(JAVA_LONG, 0, secondLong)
            val arr = arena.allocate(ADDRESS, 4)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            arr.setAtIndex(ADDRESS, 3, arg3)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithIntVector2iIntVector2iArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstInt: Int,
        firstVector: Vector2i,
        secondInt: Int,
        secondVector: Vector2i,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(8L, 4L)
            val arg2 = arena.allocate(JAVA_INT)
            val arg3 = arena.allocate(8L, 4L)
            arg0.set(JAVA_INT, 0, firstInt)
            writeVector2i(arg1, firstVector)
            arg2.set(JAVA_INT, 0, secondInt)
            writeVector2i(arg3, secondVector)
            val arr = arena.allocate(ADDRESS, 4)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            arr.setAtIndex(ADDRESS, 3, arg3)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithIntVector2iIntVector2iIntArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstInt: Int,
        firstVector: Vector2i,
        secondInt: Int,
        secondVector: Vector2i,
        thirdInt: Int,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(8L, 4L)
            val arg2 = arena.allocate(JAVA_INT)
            val arg3 = arena.allocate(8L, 4L)
            val arg4 = arena.allocate(JAVA_INT)
            arg0.set(JAVA_INT, 0, firstInt)
            writeVector2i(arg1, firstVector)
            arg2.set(JAVA_INT, 0, secondInt)
            writeVector2i(arg3, secondVector)
            arg4.set(JAVA_INT, 0, thirdInt)
            val arr = arena.allocate(ADDRESS, 5)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            arr.setAtIndex(ADDRESS, 3, arg3)
            arr.setAtIndex(ADDRESS, 4, arg4)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithIntVector2iTwoIntVector2iIntArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstInt: Int,
        firstVector: Vector2i,
        secondInt: Int,
        thirdInt: Int,
        secondVector: Vector2i,
        fourthInt: Int,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(8L, 4L)
            val arg2 = arena.allocate(JAVA_INT)
            val arg3 = arena.allocate(JAVA_INT)
            val arg4 = arena.allocate(8L, 4L)
            val arg5 = arena.allocate(JAVA_INT)
            arg0.set(JAVA_INT, 0, firstInt)
            writeVector2i(arg1, firstVector)
            arg2.set(JAVA_INT, 0, secondInt)
            arg3.set(JAVA_INT, 0, thirdInt)
            writeVector2i(arg4, secondVector)
            arg5.set(JAVA_INT, 0, fourthInt)
            val arr = arena.allocate(ADDRESS, 6)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, arg1)
            arr.setAtIndex(ADDRESS, 2, arg2)
            arr.setAtIndex(ADDRESS, 3, arg3)
            arr.setAtIndex(ADDRESS, 4, arg4)
            arr.setAtIndex(ADDRESS, 5, arg5)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (Vector2, bool) and no return value.
     */
    fun ptrcallWithVector2AndBoolArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Vector2,
        boolArg: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val vec = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            GodotReal.writeIndex(vec, 0, value.x)
            GodotReal.writeIndex(vec, 1, value.y)
            val boolCell = arena.allocate(java.lang.foreign.ValueLayout.JAVA_BYTE)
            boolCell.set(java.lang.foreign.ValueLayout.JAVA_BYTE, 0, if (boolArg) 1.toByte() else 0.toByte())
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, vec)
            arr.setAtIndex(ADDRESS, 1, boolCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithVector2AndBoolArgRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Vector2,
        boolArg: Boolean,
    ): Long {
        Arena.ofConfined().use { arena ->
            val vec = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            GodotReal.writeIndex(vec, 0, value.x)
            GodotReal.writeIndex(vec, 1, value.y)
            val boolCell = arena.allocate(JAVA_BYTE)
            boolCell.set(JAVA_BYTE, 0, if (boolArg) 1.toByte() else 0.toByte())
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, vec)
            arr.setAtIndex(ADDRESS, 1, boolCell)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_LONG, 0)
        }
    }

    fun ptrcallWithVector2AndBoolArgRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Vector2,
        boolArg: Boolean,
    ): Int {
        Arena.ofConfined().use { arena ->
            val vec = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            GodotReal.writeIndex(vec, 0, value.x)
            GodotReal.writeIndex(vec, 1, value.y)
            val boolCell = arena.allocate(JAVA_BYTE)
            boolCell.set(JAVA_BYTE, 0, if (boolArg) 1.toByte() else 0.toByte())
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, vec)
            arr.setAtIndex(ADDRESS, 1, boolCell)
            val ret = arena.allocate(JAVA_INT)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_INT, 0)
        }
    }

    fun ptrcallWithVector3AndBoolArgRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Vector3,
        boolArg: Boolean,
    ): Long {
        Arena.ofConfined().use { arena ->
            val vec = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            GodotReal.writeIndex(vec, 0, value.x)
            GodotReal.writeIndex(vec, 1, value.y)
            GodotReal.writeIndex(vec, 2, value.z)
            val boolCell = arena.allocate(JAVA_BYTE)
            boolCell.set(JAVA_BYTE, 0, if (boolArg) 1.toByte() else 0.toByte())
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, vec)
            arr.setAtIndex(ADDRESS, 1, boolCell)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return ret.get(JAVA_LONG, 0)
        }
    }

    /**
     * Calls [methodBind] with (int64, Color) args and no return value.
     */
    fun ptrcallWithLongAndColorArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Long,
        color: Color,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_LONG)
            arg0.set(JAVA_LONG, 0, value)
            val colorCell = arena.allocate(16L, 4L)
            colorCell.set(JAVA_FLOAT, 0, color.r)
            colorCell.set(JAVA_FLOAT, 4, color.g)
            colorCell.set(JAVA_FLOAT, 8, color.b)
            colorCell.set(JAVA_FLOAT, 12, color.a)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, colorCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (int32, Color) args and no return value.
     */
    fun ptrcallWithIntAndColorArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Int,
        color: Color,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            arg0.set(JAVA_INT, 0, value)
            val colorCell = arena.allocate(16L, 4L)
            colorCell.set(JAVA_FLOAT, 0, color.r)
            colorCell.set(JAVA_FLOAT, 4, color.g)
            colorCell.set(JAVA_FLOAT, 8, color.b)
            colorCell.set(JAVA_FLOAT, 12, color.a)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, arg0)
            arr.setAtIndex(ADDRESS, 1, colorCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithIntColorArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Int,
        color: Color,
    ): Long {
        Arena.ofConfined().use { arena ->
            val intCell = arena.allocate(JAVA_INT)
            val colorCell = arena.allocate(16L, 4L)
            intCell.set(JAVA_INT, 0, value)
            colorCell.set(JAVA_FLOAT, 0, color.r)
            colorCell.set(JAVA_FLOAT, 4, color.g)
            colorCell.set(JAVA_FLOAT, 8, color.b)
            colorCell.set(JAVA_FLOAT, 12, color.a)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, intCell)
            args.setAtIndex(ADDRESS, 1, colorCell)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(JAVA_LONG, 0)
        }
    }

    fun ptrcallWithTwoIntColorLongTwoBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstInt: Int,
        secondInt: Int,
        color: Color,
        longValue: Long,
        firstBool: Boolean,
        secondBool: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val int0 = arena.allocate(JAVA_INT)
            val int1 = arena.allocate(JAVA_INT)
            val colorCell = arena.allocate(16L, 4L)
            val longCell = arena.allocate(JAVA_LONG)
            val bool0 = arena.allocate(JAVA_BYTE)
            val bool1 = arena.allocate(JAVA_BYTE)
            int0.set(JAVA_INT, 0, firstInt)
            int1.set(JAVA_INT, 0, secondInt)
            colorCell.set(JAVA_FLOAT, 0, color.r)
            colorCell.set(JAVA_FLOAT, 4, color.g)
            colorCell.set(JAVA_FLOAT, 8, color.b)
            colorCell.set(JAVA_FLOAT, 12, color.a)
            longCell.set(JAVA_LONG, 0, longValue)
            bool0.set(JAVA_BYTE, 0, if (firstBool) 1.toByte() else 0.toByte())
            bool1.set(JAVA_BYTE, 0, if (secondBool) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 6)
            args.setAtIndex(ADDRESS, 0, int0)
            args.setAtIndex(ADDRESS, 1, int1)
            args.setAtIndex(ADDRESS, 2, colorCell)
            args.setAtIndex(ADDRESS, 3, longCell)
            args.setAtIndex(ADDRESS, 4, bool0)
            args.setAtIndex(ADDRESS, 5, bool1)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with one Color arg and no return value.
     */
    fun ptrcallWithColorArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        color: Color,
    ) {
        Arena.ofConfined().use { arena ->
            val colorCell = arena.allocate(16L, 4L)
            colorCell.set(JAVA_FLOAT, 0, color.r)
            colorCell.set(JAVA_FLOAT, 4, color.g)
            colorCell.set(JAVA_FLOAT, 8, color.b)
            colorCell.set(JAVA_FLOAT, 12, color.a)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, colorCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    fun ptrcallWithTwoColorArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Color,
        second: Color,
    ) {
        Arena.ofConfined().use { arena ->
            val color0 = arena.allocate(16L, 4L)
            val color1 = arena.allocate(16L, 4L)
            // Color uses fixed 32-bit component storage, unlike scalar float ptrcalls.
            color0.set(JAVA_FLOAT, 0, first.r)
            color0.set(JAVA_FLOAT, 4, first.g)
            color0.set(JAVA_FLOAT, 8, first.b)
            color0.set(JAVA_FLOAT, 12, first.a)
            color1.set(JAVA_FLOAT, 0, second.r)
            color1.set(JAVA_FLOAT, 4, second.g)
            color1.set(JAVA_FLOAT, 8, second.b)
            color1.set(JAVA_FLOAT, 12, second.a)
            val arr = arena.allocate(ADDRESS, 2)
            arr.setAtIndex(ADDRESS, 0, color0)
            arr.setAtIndex(ADDRESS, 1, color1)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with no arguments and Color return value.
     */
    fun ptrcallNoArgsRetColor(
        methodBind: MemorySegment,
        instance: MemorySegment,
    ): Color {
        Arena.ofConfined().use { arena ->
            val ret = arena.allocate(16L, 4L)
            objectMethodBindPtrcall.invoke(methodBind, instance, MemorySegment.NULL, ret)
            return Color(
                ret.get(JAVA_FLOAT, 0),
                ret.get(JAVA_FLOAT, 4),
                ret.get(JAVA_FLOAT, 8),
                ret.get(JAVA_FLOAT, 12),
            )
        }
    }

    fun ptrcallWithObjectArgRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
    ): Long {
        Arena.ofConfined().use { arena ->
            val objCell = arena.allocate(ADDRESS)
            objCell.set(ADDRESS, 0, objectArg)
            val args = arena.allocate(ADDRESS, 1)
            args.setAtIndex(ADDRESS, 0, objCell)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(JAVA_LONG, 0)
        }
    }

    fun ptrcallWithObjectArgRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
    ): Int {
        Arena.ofConfined().use { arena ->
            val objCell = arena.allocate(ADDRESS)
            objCell.set(ADDRESS, 0, objectArg)
            val args = arena.allocate(ADDRESS, 1)
            args.setAtIndex(ADDRESS, 0, objCell)
            val ret = arena.allocate(JAVA_INT)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(JAVA_INT, 0)
        }
    }

    fun ptrcallWithObjectArgRetUInt32(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
    ): Long {
        Arena.ofConfined().use { arena ->
            val objCell = arena.allocate(ADDRESS)
            objCell.set(ADDRESS, 0, objectArg)
            val args = arena.allocate(ADDRESS, 1)
            args.setAtIndex(ADDRESS, 0, objCell)
            val ret = arena.allocate(JAVA_INT)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(JAVA_INT, 0).toLong() and 0xffff_ffffL
        }
    }

    fun ptrcallWithObjectArgRetStringName(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
    ): String {
        Arena.ofConfined().use { arena ->
            val objCell = arena.allocate(ADDRESS)
            objCell.set(ADDRESS, 0, objectArg)
            val args = arena.allocate(ADDRESS, 1)
            args.setAtIndex(ADDRESS, 0, objCell)
            val ret = arena.allocate(8L, 8L)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return try {
                GodotStrings.readStringName(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.STRING_NAME, ret)
            }
        }
    }

    fun ptrcallWithObjectArgRetRID(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
    ): RID {
        Arena.ofConfined().use { arena ->
            val objCell = arena.allocate(ADDRESS)
            objCell.set(ADDRESS, 0, objectArg)
            val args = arena.allocate(ADDRESS, 1)
            args.setAtIndex(ADDRESS, 0, objCell)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return RID(ret.get(JAVA_LONG, 0))
        }
    }

    fun ptrcallWithObjectArgRetDictionary(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
    ): Map<String, Any?> {
        Arena.ofConfined().use { arena ->
            val objCell = arena.allocate(ADDRESS)
            objCell.set(ADDRESS, 0, objectArg)
            val args = arena.allocate(ADDRESS, 1)
            args.setAtIndex(ADDRESS, 0, objCell)
            val ret = arena.allocate(8L, 8L)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return try {
                BuiltinTypes.readDictionaryScalars(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.DICTIONARY, ret)
            }
        }
    }

    fun ptrcallWithObjectAndIntArgRetDictionaryList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        intArg: Int,
    ): List<Map<String, Any?>> {
        Arena.ofConfined().use { arena ->
            val objCell = arena.allocate(ADDRESS)
            objCell.set(ADDRESS, 0, objectArg)
            val intCell = arena.allocate(JAVA_INT)
            intCell.set(JAVA_INT, 0, intArg)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, objCell)
            args.setAtIndex(ADDRESS, 1, intCell)
            return callArrayReturn(methodBind, instance, args, BuiltinTypes::readArrayDictionaries)
        }
    }

    /**
     * Calls [methodBind] with one Object arg and decodes a PackedFloat32Array return.
     * Elements are read through Godot's scalar float ABI slot, then narrowed to Float.
     */
    fun ptrcallWithObjectArgRetPackedFloat32List(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
    ): List<Float> {
        Arena.ofConfined().use { arena ->
            val objCell = arena.allocate(ADDRESS)
            objCell.set(ADDRESS, 0, objectArg)
            val args = arena.allocate(ADDRESS, 1)
            args.setAtIndex(ADDRESS, 0, objCell)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return try {
                BuiltinTypes.readPackedFloat32Array(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_FLOAT32_ARRAY, ret)
            }
        }
    }

    fun ptrcallWithTwoStringArgsRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: String,
        second: String,
    ): Int {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(8L, 8L)
            val arg1 = arena.allocate(8L, 8L)
            try {
                GodotStrings.initString(arg0, first)
                GodotStrings.initString(arg1, second)
                val args = arena.allocate(ADDRESS, 2)
                args.setAtIndex(ADDRESS, 0, arg0)
                args.setAtIndex(ADDRESS, 1, arg1)
                val ret = arena.allocate(JAVA_INT)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(JAVA_INT, 0)
            } finally {
                GodotStrings.destroyString(arg0)
                GodotStrings.destroyString(arg1)
            }
        }
    }

    fun ptrcallWithStringAndBoolArgRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        enabled: Boolean,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val boolCell = arena.allocate(JAVA_BYTE)
            boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
            try {
                GodotStrings.initString(stringCell, text)
                val args = arena.allocate(ADDRESS, 2)
                args.setAtIndex(ADDRESS, 0, stringCell)
                args.setAtIndex(ADDRESS, 1, boolCell)
                val ret = arena.allocate(JAVA_BYTE)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(JAVA_BYTE, 0) != 0.toByte()
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithStringNameArgRetPackedStringList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
    ): List<String> {
        Arena.ofConfined().use { arena ->
            val args = arena.allocate(ADDRESS, 1)
            args.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(name))
            val ret = BuiltinTypes.allocatePackedArray(arena)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return try {
                BuiltinTypes.readPackedStringArray(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_STRING_ARRAY, ret)
            }
        }
    }

    fun ptrcallWithTwoStringNameArgsRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: String,
        second: String,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(first))
            args.setAtIndex(ADDRESS, 1, GodotStrings.makeStringName(second))
            val ret = arena.allocate(ADDRESS)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(ADDRESS, 0)
        }
    }

    fun ptrcallWithTwoStringNameAndBoolArgsRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: String,
        second: String,
        enabled: Boolean,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val boolCell = arena.allocate(JAVA_BYTE)
            boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(first))
            args.setAtIndex(ADDRESS, 1, GodotStrings.makeStringName(second))
            args.setAtIndex(ADDRESS, 2, boolCell)
            val ret = arena.allocate(JAVA_BYTE)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(JAVA_BYTE, 0) != 0.toByte()
        }
    }

    fun ptrcallWithTwoStringNameAndBoolArgsRetPackedStringList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: String,
        second: String,
        enabled: Boolean,
    ): List<String> {
        Arena.ofConfined().use { arena ->
            val boolCell = arena.allocate(JAVA_BYTE)
            boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(first))
            args.setAtIndex(ADDRESS, 1, GodotStrings.makeStringName(second))
            args.setAtIndex(ADDRESS, 2, boolCell)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return try {
                BuiltinTypes.readPackedStringArray(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_STRING_ARRAY, ret)
            }
        }
    }

    fun ptrcallWithStringNameAndBoolArgRetPackedStringList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
        enabled: Boolean,
    ): List<String> {
        Arena.ofConfined().use { arena ->
            val boolCell = arena.allocate(JAVA_BYTE)
            boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(name))
            args.setAtIndex(ADDRESS, 1, boolCell)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return try {
                BuiltinTypes.readPackedStringArray(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_STRING_ARRAY, ret)
            }
        }
    }

    fun ptrcallWithIntArgRetPackedStringList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Int,
    ): List<String> {
        Arena.ofConfined().use { arena ->
            val arg = arena.allocate(JAVA_INT)
            arg.set(JAVA_INT, 0, value)
            val args = arena.allocate(ADDRESS, 1)
            args.setAtIndex(ADDRESS, 0, arg)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return try {
                BuiltinTypes.readPackedStringArray(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_STRING_ARRAY, ret)
            }
        }
    }

    fun ptrcallWithLongAndBoolArgRetRID(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Long,
        enabled: Boolean,
    ): RID {
        Arena.ofConfined().use { arena ->
            val longCell = arena.allocate(JAVA_LONG)
            val boolCell = arena.allocate(JAVA_BYTE)
            longCell.set(JAVA_LONG, 0, value)
            boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, longCell)
            args.setAtIndex(ADDRESS, 1, boolCell)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return RID(ret.get(JAVA_LONG, 0))
        }
    }

    fun ptrcallWithLongAndStringArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Long,
        text: String,
    ) {
        Arena.ofConfined().use { arena ->
            val longCell = arena.allocate(JAVA_LONG)
            val stringCell = arena.allocate(8L, 8L)
            longCell.set(JAVA_LONG, 0, value)
            try {
                GodotStrings.initString(stringCell, text)
                val args = arena.allocate(ADDRESS, 2)
                args.setAtIndex(ADDRESS, 0, longCell)
                args.setAtIndex(ADDRESS, 1, stringCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithTwoLongStringArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Long,
        second: Long,
        text: String,
    ) {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(JAVA_LONG)
            val secondCell = arena.allocate(JAVA_LONG)
            val stringCell = arena.allocate(8L, 8L)
            firstCell.set(JAVA_LONG, 0, first)
            secondCell.set(JAVA_LONG, 0, second)
            try {
                GodotStrings.initString(stringCell, text)
                val args = arena.allocate(ADDRESS, 3)
                args.setAtIndex(ADDRESS, 0, firstCell)
                args.setAtIndex(ADDRESS, 1, secondCell)
                args.setAtIndex(ADDRESS, 2, stringCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithLongAndStringArgRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Long,
        text: String,
    ): Long {
        Arena.ofConfined().use { arena ->
            val longCell = arena.allocate(JAVA_LONG)
            val stringCell = arena.allocate(8L, 8L)
            longCell.set(JAVA_LONG, 0, value)
            try {
                GodotStrings.initString(stringCell, text)
                val args = arena.allocate(ADDRESS, 2)
                args.setAtIndex(ADDRESS, 0, longCell)
                args.setAtIndex(ADDRESS, 1, stringCell)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithLongAndStringArgRetPackedStringList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Long,
        text: String,
    ): List<String> {
        Arena.ofConfined().use { arena ->
            val longCell = arena.allocate(JAVA_LONG)
            val stringCell = arena.allocate(8L, 8L)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            longCell.set(JAVA_LONG, 0, value)
            try {
                GodotStrings.initString(stringCell, text)
                val args = arena.allocate(ADDRESS, 2)
                args.setAtIndex(ADDRESS, 0, longCell)
                args.setAtIndex(ADDRESS, 1, stringCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return BuiltinTypes.readPackedStringArray(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_STRING_ARRAY, ret)
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithRIDArgRetByteArray(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
    ): ByteArray {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            val args = arena.allocate(ADDRESS, 1)
            args.setAtIndex(ADDRESS, 0, ridCell)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return try {
                BuiltinTypes.readPackedByteArray(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, ret)
            }
        }
    }

    fun ptrcallWithRIDArgRetPackedStringList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
    ): List<String> {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            val args = arena.allocate(ADDRESS, 1)
            args.setAtIndex(ADDRESS, 0, ridCell)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return try {
                BuiltinTypes.readPackedStringArray(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_STRING_ARRAY, ret)
            }
        }
    }

    fun ptrcallWithRIDArgRetPackedVector2List(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
    ): List<Vector2> {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            val args = arena.allocate(ADDRESS, 1)
            args.setAtIndex(ADDRESS, 0, ridCell)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return try {
                BuiltinTypes.readPackedVector2Array(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR2_ARRAY, ret)
            }
        }
    }

    fun ptrcallWithRIDArgRetRect2(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
    ): Rect2 {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            val args = arena.allocate(ADDRESS, 1)
            args.setAtIndex(ADDRESS, 0, ridCell)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return readRect2(ret)
        }
    }

    fun ptrcallWithRIDArgRetPackedVector3List(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
    ): List<Vector3> {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            val args = arena.allocate(ADDRESS, 1)
            args.setAtIndex(ADDRESS, 0, ridCell)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return try {
                BuiltinTypes.readPackedVector3Array(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR3_ARRAY, ret)
            }
        }
    }

    fun ptrcallWithRIDAndLongArgRetPackedFloat32List(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Long,
    ): List<Float> {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val valueCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            valueCell.set(JAVA_LONG, 0, value)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, valueCell)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return try {
                BuiltinTypes.readPackedFloat32Array(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_FLOAT32_ARRAY, ret)
            }
        }
    }

    fun ptrcallWithRIDAndLongArgRetPackedInt32List(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Long,
    ): List<Int> {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val valueCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            valueCell.set(JAVA_LONG, 0, value)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, valueCell)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return try {
                BuiltinTypes.readPackedInt32Array(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_INT32_ARRAY, ret)
            }
        }
    }

    fun ptrcallWithRIDAndLongArgRetPackedVector3List(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Long,
    ): List<Vector3> {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val valueCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            valueCell.set(JAVA_LONG, 0, value)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, valueCell)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return try {
                BuiltinTypes.readPackedVector3Array(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR3_ARRAY, ret)
            }
        }
    }

    fun ptrcallWithRIDAndLongArgRetPackedVector2List(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Long,
    ): List<Vector2> {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val valueCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            valueCell.set(JAVA_LONG, 0, value)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, valueCell)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return try {
                BuiltinTypes.readPackedVector2Array(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR2_ARRAY, ret)
            }
        }
    }

    fun ptrcallWithRIDAndIntArgRetVector3(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Int,
    ): Vector3 {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val valueCell = arena.allocate(JAVA_INT)
            ridCell.set(JAVA_LONG, 0, rid.value)
            valueCell.set(JAVA_INT, 0, value)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, valueCell)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return Vector3(
                x = GodotReal.readIndex(ret, 0),
                y = GodotReal.readIndex(ret, 1),
                z = GodotReal.readIndex(ret, 2),
            )
        }
    }

    fun ptrcallWithRIDAndIntArgRetVector2(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Int,
    ): Vector2 {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val valueCell = arena.allocate(JAVA_INT)
            ridCell.set(JAVA_LONG, 0, rid.value)
            valueCell.set(JAVA_INT, 0, value)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, valueCell)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return Vector2(
                x = GodotReal.readIndex(ret, 0),
                y = GodotReal.readIndex(ret, 1),
            )
        }
    }

    fun ptrcallWithRIDAndIntArgRetColor(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Int,
    ): Color {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val valueCell = arena.allocate(JAVA_INT)
            ridCell.set(JAVA_LONG, 0, rid.value)
            valueCell.set(JAVA_INT, 0, value)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, valueCell)
            // Color components use fixed 32-bit storage, unlike scalar float method args.
            val ret = arena.allocate(16L, 4L)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return Color(
                ret.get(JAVA_FLOAT, 0),
                ret.get(JAVA_FLOAT, 4),
                ret.get(JAVA_FLOAT, 8),
                ret.get(JAVA_FLOAT, 12),
            )
        }
    }

    fun ptrcallWithRIDIntAndColorArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Int,
        color: Color,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val valueCell = arena.allocate(JAVA_INT)
            val colorCell = arena.allocate(16L, 4L)
            ridCell.set(JAVA_LONG, 0, rid.value)
            valueCell.set(JAVA_INT, 0, value)
            // Color components are fixed 32-bit floats, unlike scalar float ptrcall slots.
            colorCell.set(JAVA_FLOAT, 0, color.r)
            colorCell.set(JAVA_FLOAT, 4, color.g)
            colorCell.set(JAVA_FLOAT, 8, color.b)
            colorCell.set(JAVA_FLOAT, 12, color.a)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, valueCell)
            args.setAtIndex(ADDRESS, 2, colorCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDLongAndRIDArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Long,
        secondRid: RID,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val valueCell = arena.allocate(JAVA_LONG)
            val secondRidCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            valueCell.set(JAVA_LONG, 0, value)
            secondRidCell.set(JAVA_LONG, 0, secondRid.value)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, valueCell)
            args.setAtIndex(ADDRESS, 2, secondRidCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithIntAndRIDArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Int,
        rid: RID,
    ) {
        Arena.ofConfined().use { arena ->
            val intCell = arena.allocate(JAVA_INT)
            val ridCell = arena.allocate(JAVA_LONG)
            intCell.set(JAVA_INT, 0, value)
            ridCell.set(JAVA_LONG, 0, rid.value)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, intCell)
            args.setAtIndex(ADDRESS, 1, ridCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithStringNameListArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        values: List<String>,
    ) {
        Arena.ofConfined().use { arena ->
            val array = arena.allocate(8L, 8L)
            try {
                BuiltinTypes.initArrayOfStringNames(array, values)
                val args = arena.allocate(ADDRESS, 1)
                args.setAtIndex(ADDRESS, 0, array)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, array)
            }
        }
    }

    fun ptrcallWithArrayListArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        values: List<List<Any?>>,
    ) {
        Arena.ofConfined().use { arena ->
            val array = arena.allocate(8L, 8L)
            try {
                BuiltinTypes.initArray(array, values.map { it as Any? })
                val args = arena.allocate(ADDRESS, 1)
                args.setAtIndex(ADDRESS, 0, array)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, array)
            }
        }
    }

    fun ptrcallWithLongArrayArrayListDictionaryLongArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        primitive: Long,
        arrays: List<Any?>,
        blendShapes: List<List<Any?>>,
        lods: Map<String, Any?>,
        flags: Long,
    ) {
        Arena.ofConfined().use { arena ->
            val primitiveCell = arena.allocate(JAVA_LONG)
            val arraysCell = arena.allocate(8L, 8L)
            val blendShapesCell = arena.allocate(8L, 8L)
            val lodsCell = arena.allocate(8L, 8L)
            val flagsCell = arena.allocate(JAVA_LONG)
            try {
                primitiveCell.set(JAVA_LONG, 0, primitive)
                BuiltinTypes.initArray(arraysCell, arrays)
                BuiltinTypes.initArray(blendShapesCell, blendShapes.map { it as Any? })
                BuiltinTypes.initDictionary(lodsCell, lods)
                flagsCell.set(JAVA_LONG, 0, flags)
                val args = arena.allocate(ADDRESS, 5)
                args.setAtIndex(ADDRESS, 0, primitiveCell)
                args.setAtIndex(ADDRESS, 1, arraysCell)
                args.setAtIndex(ADDRESS, 2, blendShapesCell)
                args.setAtIndex(ADDRESS, 3, lodsCell)
                args.setAtIndex(ADDRESS, 4, flagsCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.DICTIONARY, lodsCell)
                BuiltinTypes.destroyTyped(VariantType.ARRAY, blendShapesCell)
                BuiltinTypes.destroyTyped(VariantType.ARRAY, arraysCell)
            }
        }
    }

    fun ptrcallWithLongArrayArrayListDictionaryObjectStringLongArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        primitive: Long,
        arrays: List<Any?>,
        blendShapes: List<List<Any?>>,
        lods: Map<String, Any?>,
        material: MemorySegment,
        name: String,
        flags: Long,
    ) {
        Arena.ofConfined().use { arena ->
            val primitiveCell = arena.allocate(JAVA_LONG)
            val arraysCell = arena.allocate(8L, 8L)
            val blendShapesCell = arena.allocate(8L, 8L)
            val lodsCell = arena.allocate(8L, 8L)
            val materialCell = arena.allocate(ADDRESS)
            val nameCell = arena.allocate(8L, 8L)
            val flagsCell = arena.allocate(JAVA_LONG)
            try {
                primitiveCell.set(JAVA_LONG, 0, primitive)
                BuiltinTypes.initArray(arraysCell, arrays)
                BuiltinTypes.initArray(blendShapesCell, blendShapes.map { it as Any? })
                BuiltinTypes.initDictionary(lodsCell, lods)
                materialCell.set(ADDRESS, 0, material)
                GodotStrings.initString(nameCell, name)
                flagsCell.set(JAVA_LONG, 0, flags)
                val args = arena.allocate(ADDRESS, 7)
                args.setAtIndex(ADDRESS, 0, primitiveCell)
                args.setAtIndex(ADDRESS, 1, arraysCell)
                args.setAtIndex(ADDRESS, 2, blendShapesCell)
                args.setAtIndex(ADDRESS, 3, lodsCell)
                args.setAtIndex(ADDRESS, 4, materialCell)
                args.setAtIndex(ADDRESS, 5, nameCell)
                args.setAtIndex(ADDRESS, 6, flagsCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(nameCell)
                BuiltinTypes.destroyTyped(VariantType.DICTIONARY, lodsCell)
                BuiltinTypes.destroyTyped(VariantType.ARRAY, blendShapesCell)
                BuiltinTypes.destroyTyped(VariantType.ARRAY, arraysCell)
            }
        }
    }

    fun ptrcallWithByteArrayListArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        values: List<ByteArray>,
    ) {
        Arena.ofConfined().use { arena ->
            val array = arena.allocate(8L, 8L)
            try {
                BuiltinTypes.initArray(array, values.map { it as Any? })
                val args = arena.allocate(ADDRESS, 1)
                args.setAtIndex(ADDRESS, 0, array)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, array)
            }
        }
    }

    fun ptrcallWithPackedStringListListArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        values: List<List<String>>,
    ) {
        Arena.ofConfined().use { arena ->
            val array = arena.allocate(8L, 8L)
            try {
                BuiltinTypes.initArrayOfPackedStringArrays(array, values)
                val args = arena.allocate(ADDRESS, 1)
                args.setAtIndex(ADDRESS, 0, array)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, array)
            }
        }
    }

    fun ptrcallWithPlaneListArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        values: List<Plane>,
    ) {
        Arena.ofConfined().use { arena ->
            val array = arena.allocate(8L, 8L)
            try {
                BuiltinTypes.initArray(array, values.map { it as Any? })
                val args = arena.allocate(ADDRESS, 1)
                args.setAtIndex(ADDRESS, 0, array)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, array)
            }
        }
    }

    fun ptrcallWithTransform3DListArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        values: List<Transform3D>,
    ) {
        Arena.ofConfined().use { arena ->
            val array = arena.allocate(8L, 8L)
            try {
                BuiltinTypes.initArray(array, values.map { it as Any? })
                val args = arena.allocate(ADDRESS, 1)
                args.setAtIndex(ADDRESS, 0, array)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, array)
            }
        }
    }

    fun ptrcallWithPlaneListArgRetPackedVector3List(
        methodBind: MemorySegment,
        instance: MemorySegment,
        values: List<Plane>,
    ): List<Vector3> {
        Arena.ofConfined().use { arena ->
            val array = arena.allocate(8L, 8L)
            try {
                BuiltinTypes.initArray(array, values.map { it as Any? })
                val args = arena.allocate(ADDRESS, 1)
                args.setAtIndex(ADDRESS, 0, array)
                val ret = BuiltinTypes.allocatePackedArray(arena)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return try {
                    BuiltinTypes.readPackedVector3Array(ret)
                } finally {
                    BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR3_ARRAY, ret)
                }
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, array)
            }
        }
    }

    fun ptrcallWithPlaneListAndRIDArgsRetPackedInt64List(
        methodBind: MemorySegment,
        instance: MemorySegment,
        values: List<Plane>,
        rid: RID,
    ): List<Long> {
        Arena.ofConfined().use { arena ->
            val array = arena.allocate(8L, 8L)
            try {
                BuiltinTypes.initArray(array, values.map { it as Any? })
                val ridCell = arena.allocate(JAVA_LONG)
                ridCell.set(JAVA_LONG, 0, rid.value)
                val args = arena.allocate(ADDRESS, 2)
                args.setAtIndex(ADDRESS, 0, array)
                args.setAtIndex(ADDRESS, 1, ridCell)
                val ret = BuiltinTypes.allocatePackedArray(arena)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return try {
                    BuiltinTypes.readPackedInt64Array(ret)
                } finally {
                    BuiltinTypes.destroyTyped(VariantType.PACKED_INT64_ARRAY, ret)
                }
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, array)
            }
        }
    }

    fun ptrcallWithRIDAndTransform3DListArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        values: List<Transform3D>,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val array = arena.allocate(8L, 8L)
            ridCell.set(JAVA_LONG, 0, rid.value)
            try {
                BuiltinTypes.initArray(array, values.map { it as Any? })
                val args = arena.allocate(ADDRESS, 2)
                args.setAtIndex(ADDRESS, 0, ridCell)
                args.setAtIndex(ADDRESS, 1, array)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, array)
            }
        }
    }

    fun ptrcallWithTwoDoubleArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Double,
        second: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_DOUBLE)
            val arg1 = arena.allocate(JAVA_DOUBLE)
            arg0.set(JAVA_DOUBLE, 0, first)
            arg1.set(JAVA_DOUBLE, 0, second)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, arg0)
            args.setAtIndex(ADDRESS, 1, arg1)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithDoubleAndTwoBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Double,
        firstBool: Boolean,
        secondBool: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val doubleCell = arena.allocate(JAVA_DOUBLE)
            val bool0 = arena.allocate(JAVA_BYTE)
            val bool1 = arena.allocate(JAVA_BYTE)
            doubleCell.set(JAVA_DOUBLE, 0, value)
            bool0.set(JAVA_BYTE, 0, if (firstBool) 1.toByte() else 0.toByte())
            bool1.set(JAVA_BYTE, 0, if (secondBool) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, doubleCell)
            args.setAtIndex(ADDRESS, 1, bool0)
            args.setAtIndex(ADDRESS, 2, bool1)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithDoubleAndTwoBoolArgsRetTransform3D(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Double,
        firstBool: Boolean,
        secondBool: Boolean,
    ): Transform3D {
        Arena.ofConfined().use { arena ->
            val doubleCell = arena.allocate(JAVA_DOUBLE)
            val bool0 = arena.allocate(JAVA_BYTE)
            val bool1 = arena.allocate(JAVA_BYTE)
            doubleCell.set(JAVA_DOUBLE, 0, value)
            bool0.set(JAVA_BYTE, 0, if (firstBool) 1.toByte() else 0.toByte())
            bool1.set(JAVA_BYTE, 0, if (secondBool) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, doubleCell)
            args.setAtIndex(ADDRESS, 1, bool0)
            args.setAtIndex(ADDRESS, 2, bool1)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 12, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return readTransform3D(ret)
        }
    }

    fun ptrcallWithDoubleVector2TwoDoubleArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstDouble: Double,
        vector: Vector2,
        secondDouble: Double,
        thirdDouble: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_DOUBLE)
            val arg1 = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val arg2 = arena.allocate(JAVA_DOUBLE)
            val arg3 = arena.allocate(JAVA_DOUBLE)
            arg0.set(JAVA_DOUBLE, 0, firstDouble)
            writeVector2(arg1, vector)
            arg2.set(JAVA_DOUBLE, 0, secondDouble)
            arg3.set(JAVA_DOUBLE, 0, thirdDouble)
            val args = arena.allocate(ADDRESS, 4)
            args.setAtIndex(ADDRESS, 0, arg0)
            args.setAtIndex(ADDRESS, 1, arg1)
            args.setAtIndex(ADDRESS, 2, arg2)
            args.setAtIndex(ADDRESS, 3, arg3)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithLongDoubleTwoBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Long,
        doubleValue: Double,
        firstBool: Boolean,
        secondBool: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_LONG)
            val arg1 = arena.allocate(JAVA_DOUBLE)
            val arg2 = arena.allocate(JAVA_BYTE)
            val arg3 = arena.allocate(JAVA_BYTE)
            arg0.set(JAVA_LONG, 0, value)
            arg1.set(JAVA_DOUBLE, 0, doubleValue)
            arg2.set(JAVA_BYTE, 0, if (firstBool) 1.toByte() else 0.toByte())
            arg3.set(JAVA_BYTE, 0, if (secondBool) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 4)
            args.setAtIndex(ADDRESS, 0, arg0)
            args.setAtIndex(ADDRESS, 1, arg1)
            args.setAtIndex(ADDRESS, 2, arg2)
            args.setAtIndex(ADDRESS, 3, arg3)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithLongTwoDoubleAndBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Long,
        firstDouble: Double,
        secondDouble: Double,
        boolValue: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_LONG)
            val arg1 = arena.allocate(JAVA_DOUBLE)
            val arg2 = arena.allocate(JAVA_DOUBLE)
            val arg3 = arena.allocate(JAVA_BYTE)
            arg0.set(JAVA_LONG, 0, value)
            arg1.set(JAVA_DOUBLE, 0, firstDouble)
            arg2.set(JAVA_DOUBLE, 0, secondDouble)
            arg3.set(JAVA_BYTE, 0, if (boolValue) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 4)
            args.setAtIndex(ADDRESS, 0, arg0)
            args.setAtIndex(ADDRESS, 1, arg1)
            args.setAtIndex(ADDRESS, 2, arg2)
            args.setAtIndex(ADDRESS, 3, arg3)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithLongArgRetPackedStringList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Long,
    ): List<String> {
        Arena.ofConfined().use { arena ->
            val longCell = arena.allocate(JAVA_LONG)
            longCell.set(JAVA_LONG, 0, value)
            val args = arena.allocate(ADDRESS, 1)
            args.setAtIndex(ADDRESS, 0, longCell)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return try {
                BuiltinTypes.readPackedStringArray(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_STRING_ARRAY, ret)
            }
        }
    }

    fun ptrcallWithLongArgRetNodePath(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Long,
    ): NodePath {
        Arena.ofConfined().use { arena ->
            val longCell = arena.allocate(JAVA_LONG)
            longCell.set(JAVA_LONG, 0, value)
            val args = arena.allocate(ADDRESS, 1)
            args.setAtIndex(ADDRESS, 0, longCell)
            val ret = arena.allocate(8L, 8L)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return try {
                BuiltinTypes.readTypedNodePath(ret, arena)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.NODE_PATH, ret)
            }
        }
    }

    fun ptrcallWithLongArgRetTransform2D(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Long,
    ): Transform2D {
        Arena.ofConfined().use { arena ->
            val longCell = arena.allocate(JAVA_LONG)
            longCell.set(JAVA_LONG, 0, value)
            val args = arena.allocate(ADDRESS, 1)
            args.setAtIndex(ADDRESS, 0, longCell)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 6, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return readTransform2D(ret)
        }
    }

    fun ptrcallWithDoubleArgRetVector2(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Double,
    ): Vector2 {
        Arena.ofConfined().use { arena ->
            val doubleCell = arena.allocate(JAVA_DOUBLE)
            doubleCell.set(JAVA_DOUBLE, 0, value)
            val args = arena.allocate(ADDRESS, 1)
            args.setAtIndex(ADDRESS, 0, doubleCell)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return Vector2(
                x = GodotReal.readIndex(ret, 0),
                y = GodotReal.readIndex(ret, 1),
            )
        }
    }

    fun ptrcallWithDoubleArgRetVector3(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Double,
    ): Vector3 {
        Arena.ofConfined().use { arena ->
            val doubleCell = arena.allocate(JAVA_DOUBLE)
            doubleCell.set(JAVA_DOUBLE, 0, value)
            val args = arena.allocate(ADDRESS, 1)
            args.setAtIndex(ADDRESS, 0, doubleCell)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return Vector3(
                x = GodotReal.readIndex(ret, 0),
                y = GodotReal.readIndex(ret, 1),
                z = GodotReal.readIndex(ret, 2),
            )
        }
    }

    fun ptrcallWithDoubleArgRetColor(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Double,
    ): Color {
        Arena.ofConfined().use { arena ->
            val doubleCell = arena.allocate(JAVA_DOUBLE)
            doubleCell.set(JAVA_DOUBLE, 0, value)
            val args = arena.allocate(ADDRESS, 1)
            args.setAtIndex(ADDRESS, 0, doubleCell)
            val ret = arena.allocate(16L, 4L)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return Color(
                ret.get(JAVA_FLOAT, 0),
                ret.get(JAVA_FLOAT, 4),
                ret.get(JAVA_FLOAT, 8),
                ret.get(JAVA_FLOAT, 12),
            )
        }
    }

    fun ptrcallWithDoubleAndColorArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Double,
        color: Color,
    ) {
        Arena.ofConfined().use { arena ->
            val doubleCell = arena.allocate(JAVA_DOUBLE)
            val colorCell = arena.allocate(16L, 4L)
            doubleCell.set(JAVA_DOUBLE, 0, value)
            colorCell.set(JAVA_FLOAT, 0, color.r)
            colorCell.set(JAVA_FLOAT, 4, color.g)
            colorCell.set(JAVA_FLOAT, 8, color.b)
            colorCell.set(JAVA_FLOAT, 12, color.a)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, doubleCell)
            args.setAtIndex(ADDRESS, 1, colorCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithDoubleArgRetByteArray(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Double,
    ): ByteArray {
        Arena.ofConfined().use { arena ->
            val doubleCell = arena.allocate(JAVA_DOUBLE)
            doubleCell.set(JAVA_DOUBLE, 0, value)
            val args = arena.allocate(ADDRESS, 1)
            args.setAtIndex(ADDRESS, 0, doubleCell)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return try {
                BuiltinTypes.readPackedByteArray(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, ret)
            }
        }
    }

    fun ptrcallWithVector2ArgRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Vector2,
    ): Long {
        Arena.ofConfined().use { arena ->
            val vectorCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            GodotReal.writeIndex(vectorCell, 0, value.x)
            GodotReal.writeIndex(vectorCell, 1, value.y)
            val args = arena.allocate(ADDRESS, 1)
            args.setAtIndex(ADDRESS, 0, vectorCell)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(JAVA_LONG, 0)
        }
    }

    fun ptrcallWithRect2ArgRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Rect2,
    ): Int {
        Arena.ofConfined().use { arena ->
            val rectCell = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
            writeRect2(rectCell, value)
            val args = arena.allocate(ADDRESS, 1)
            args.setAtIndex(ADDRESS, 0, rectCell)
            val ret = arena.allocate(JAVA_INT)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(JAVA_INT, 0)
        }
    }

    fun ptrcallWithIntArgRetVector3i(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Int,
    ): Vector3i {
        Arena.ofConfined().use { arena ->
            val intCell = arena.allocate(JAVA_INT)
            intCell.set(JAVA_INT, 0, value)
            val args = arena.allocate(ADDRESS, 1)
            args.setAtIndex(ADDRESS, 0, intCell)
            val ret = arena.allocate(12L, 4L)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return Vector3i(
                x = ret.get(JAVA_INT, 0),
                y = ret.get(JAVA_INT, 4),
                z = ret.get(JAVA_INT, 8),
            )
        }
    }

    fun ptrcallWithStringArgRetDictionary(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
    ): Map<String, Any?> {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val ret = arena.allocate(8L, 8L)
            try {
                GodotStrings.initString(stringCell, text)
                val args = arena.allocate(ADDRESS, 1)
                args.setAtIndex(ADDRESS, 0, stringCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return BuiltinTypes.readDictionaryScalars(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.DICTIONARY, ret)
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithDictionaryArgRetString(
        methodBind: MemorySegment,
        instance: MemorySegment,
        values: Map<String, Any?>,
    ): String {
        Arena.ofConfined().use { arena ->
            val dict = arena.allocate(8L, 8L)
            val ret = arena.allocate(8L, 8L)
            try {
                BuiltinTypes.initDictionary(dict, values)
                val args = arena.allocate(ADDRESS, 1)
                args.setAtIndex(ADDRESS, 0, dict)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return GodotStrings.readString(ret)
            } finally {
                GodotStrings.destroyString(ret)
                BuiltinTypes.destroyTyped(VariantType.DICTIONARY, dict)
            }
        }
    }

    fun ptrcallWithDictionaryArgRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        values: Map<String, Any?>,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val dict = arena.allocate(8L, 8L)
            try {
                BuiltinTypes.initDictionary(dict, values)
                val args = arena.allocate(ADDRESS, 1)
                args.setAtIndex(ADDRESS, 0, dict)
                val ret = arena.allocate(ADDRESS)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(ADDRESS, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.DICTIONARY, dict)
            }
        }
    }

    fun ptrcallWithPackedFloat32ListArgRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        values: List<Float>,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val argPacked = BuiltinTypes.allocatePackedArray(arena)
            try {
                BuiltinTypes.initPackedFloat32Array(argPacked, values)
                val args = arena.allocate(ADDRESS, 1)
                args.setAtIndex(ADDRESS, 0, argPacked)
                val ret = arena.allocate(JAVA_BYTE)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(JAVA_BYTE, 0) != 0.toByte()
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_FLOAT32_ARRAY, argPacked)
            }
        }
    }

    fun ptrcallWithPackedFloat32ListArgRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        values: List<Float>,
    ): Long {
        Arena.ofConfined().use { arena ->
            val argPacked = BuiltinTypes.allocatePackedArray(arena)
            try {
                BuiltinTypes.initPackedFloat32Array(argPacked, values)
                val args = arena.allocate(ADDRESS, 1)
                args.setAtIndex(ADDRESS, 0, argPacked)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_FLOAT32_ARRAY, argPacked)
            }
        }
    }

    fun ptrcallWithByteArrayArgRetString(
        methodBind: MemorySegment,
        instance: MemorySegment,
        bytes: ByteArray,
    ): String {
        Arena.ofConfined().use { arena ->
            val byteArray = BuiltinTypes.allocatePackedArray(arena)
            val ret = arena.allocate(8L, 8L)
            try {
                BuiltinTypes.initPackedByteArray(byteArray, bytes)
                val args = arena.allocate(ADDRESS, 1)
                args.setAtIndex(ADDRESS, 0, byteArray)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return GodotStrings.readString(ret)
            } finally {
                GodotStrings.destroyString(ret)
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, byteArray)
            }
        }
    }

    fun ptrcallWithRIDArgRetPackedFloat32List(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
    ): List<Float> {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            val args = arena.allocate(ADDRESS, 1)
            args.setAtIndex(ADDRESS, 0, ridCell)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return try {
                BuiltinTypes.readPackedFloat32Array(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_FLOAT32_ARRAY, ret)
            }
        }
    }

    fun ptrcallWithRIDAndPackedFloat32ListArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        values: List<Float>,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val packed = BuiltinTypes.allocatePackedArray(arena)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                BuiltinTypes.initPackedFloat32Array(packed, values)
                val args = arena.allocate(ADDRESS, 2)
                args.setAtIndex(ADDRESS, 0, ridCell)
                args.setAtIndex(ADDRESS, 1, packed)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_FLOAT32_ARRAY, packed)
            }
        }
    }

    fun ptrcallWithRIDAndPackedFloat32ListArgRetDouble(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        values: List<Float>,
    ): Double {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val packed = BuiltinTypes.allocatePackedArray(arena)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                BuiltinTypes.initPackedFloat32Array(packed, values)
                val args = arena.allocate(ADDRESS, 2)
                args.setAtIndex(ADDRESS, 0, ridCell)
                args.setAtIndex(ADDRESS, 1, packed)
                val ret = arena.allocate(JAVA_DOUBLE)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(JAVA_DOUBLE, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_FLOAT32_ARRAY, packed)
            }
        }
    }

    fun ptrcallWithRIDAndTwoPackedFloat32ListArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        first: List<Float>,
        second: List<Float>,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val firstPacked = BuiltinTypes.allocatePackedArray(arena)
            val secondPacked = BuiltinTypes.allocatePackedArray(arena)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                BuiltinTypes.initPackedFloat32Array(firstPacked, first)
                BuiltinTypes.initPackedFloat32Array(secondPacked, second)
                val args = arena.allocate(ADDRESS, 3)
                args.setAtIndex(ADDRESS, 0, ridCell)
                args.setAtIndex(ADDRESS, 1, firstPacked)
                args.setAtIndex(ADDRESS, 2, secondPacked)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_FLOAT32_ARRAY, secondPacked)
                BuiltinTypes.destroyTyped(VariantType.PACKED_FLOAT32_ARRAY, firstPacked)
            }
        }
    }

    fun ptrcallWithRIDPackedFloat32ListLongBoolLongArgsRetPackedInt32List(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        values: List<Float>,
        value: Long,
        enabled: Boolean,
        flags: Long,
    ): List<Int> {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val packed = BuiltinTypes.allocatePackedArray(arena)
            val valueCell = arena.allocate(JAVA_LONG)
            val boolCell = arena.allocate(JAVA_BYTE)
            val flagsCell = arena.allocate(JAVA_LONG)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                BuiltinTypes.initPackedFloat32Array(packed, values)
                valueCell.set(JAVA_LONG, 0, value)
                boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
                flagsCell.set(JAVA_LONG, 0, flags)
                val args = arena.allocate(ADDRESS, 5)
                args.setAtIndex(ADDRESS, 0, ridCell)
                args.setAtIndex(ADDRESS, 1, packed)
                args.setAtIndex(ADDRESS, 2, valueCell)
                args.setAtIndex(ADDRESS, 3, boolCell)
                args.setAtIndex(ADDRESS, 4, flagsCell)
                val ret = BuiltinTypes.allocatePackedArray(arena)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return try {
                    BuiltinTypes.readPackedInt32Array(ret)
                } finally {
                    BuiltinTypes.destroyTyped(VariantType.PACKED_INT32_ARRAY, ret)
                }
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_FLOAT32_ARRAY, packed)
            }
        }
    }

    fun ptrcallWithRIDAndPackedVector2ListArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        values: List<Vector2>,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val packed = BuiltinTypes.allocatePackedArray(arena)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                BuiltinTypes.initPackedVector2Array(packed, values)
                val args = arena.allocate(ADDRESS, 2)
                args.setAtIndex(ADDRESS, 0, ridCell)
                args.setAtIndex(ADDRESS, 1, packed)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR2_ARRAY, packed)
            }
        }
    }

    fun ptrcallWithRIDPackedVector2ListAndBoolArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        values: List<Vector2>,
        enabled: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val packed = BuiltinTypes.allocatePackedArray(arena)
            val boolCell = arena.allocate(JAVA_BYTE)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                BuiltinTypes.initPackedVector2Array(packed, values)
                boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
                val args = arena.allocate(ADDRESS, 3)
                args.setAtIndex(ADDRESS, 0, ridCell)
                args.setAtIndex(ADDRESS, 1, packed)
                args.setAtIndex(ADDRESS, 2, boolCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR2_ARRAY, packed)
            }
        }
    }

    fun ptrcallWithRIDAndPackedVector3ListArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        values: List<Vector3>,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val packed = BuiltinTypes.allocatePackedArray(arena)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                BuiltinTypes.initPackedVector3Array(packed, values)
                val args = arena.allocate(ADDRESS, 2)
                args.setAtIndex(ADDRESS, 0, ridCell)
                args.setAtIndex(ADDRESS, 1, packed)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR3_ARRAY, packed)
            }
        }
    }

    fun ptrcallWithRIDAndByteArrayArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        bytes: ByteArray,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val byteArray = BuiltinTypes.allocatePackedArray(arena)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                BuiltinTypes.initPackedByteArray(byteArray, bytes)
                val args = arena.allocate(ADDRESS, 2)
                args.setAtIndex(ADDRESS, 0, ridCell)
                args.setAtIndex(ADDRESS, 1, byteArray)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, byteArray)
            }
        }
    }

    fun ptrcallWithRIDAndVector2iArgRetPackedInt32List(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Vector2i,
    ): List<Int> {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val vectorCell = arena.allocate(8L, 4L)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            ridCell.set(JAVA_LONG, 0, rid.value)
            writeVector2i(vectorCell, value)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, vectorCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return try {
                BuiltinTypes.readPackedInt32Array(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_INT32_ARRAY, ret)
            }
        }
    }

    fun ptrcallWithRIDVector2iLongPackedInt32ListArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Vector2i,
        index: Long,
        values: List<Int>,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val vectorCell = arena.allocate(8L, 4L)
            val indexCell = arena.allocate(JAVA_LONG)
            val packed = BuiltinTypes.allocatePackedArray(arena)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                writeVector2i(vectorCell, value)
                indexCell.set(JAVA_LONG, 0, index)
                BuiltinTypes.initPackedInt32Array(packed, values)
                val args = arena.allocate(ADDRESS, 4)
                args.setAtIndex(ADDRESS, 0, ridCell)
                args.setAtIndex(ADDRESS, 1, vectorCell)
                args.setAtIndex(ADDRESS, 2, indexCell)
                args.setAtIndex(ADDRESS, 3, packed)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_INT32_ARRAY, packed)
            }
        }
    }

    fun ptrcallWithRIDArgRetVector3i(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
    ): Vector3i {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            val args = arena.allocate(ADDRESS, 1)
            args.setAtIndex(ADDRESS, 0, ridCell)
            val ret = arena.allocate(12L, 4L)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return Vector3i(
                x = ret.get(JAVA_INT, 0),
                y = ret.get(JAVA_INT, 4),
                z = ret.get(JAVA_INT, 8),
            )
        }
    }

    fun ptrcallWithRIDArgRetPackedColorList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
    ): List<Color> {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            val args = arena.allocate(ADDRESS, 1)
            args.setAtIndex(ADDRESS, 0, ridCell)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return try {
                BuiltinTypes.readPackedColorArray(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_COLOR_ARRAY, ret)
            }
        }
    }

    fun ptrcallWithLongArgRetDictionaryList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Long,
    ): List<Map<String, Any?>> {
        Arena.ofConfined().use { arena ->
            val longCell = arena.allocate(JAVA_LONG)
            longCell.set(JAVA_LONG, 0, value)
            val args = arena.allocate(ADDRESS, 1)
            args.setAtIndex(ADDRESS, 0, longCell)
            return callArrayReturn(methodBind, instance, args, BuiltinTypes::readArrayDictionaries)
        }
    }

    fun ptrcallWithVector3ArgRetPlaneList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Vector3,
    ): List<Plane> {
        Arena.ofConfined().use { arena ->
            val vectorCell = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            writeVector3(vectorCell, value)
            val args = arena.allocate(ADDRESS, 1)
            args.setAtIndex(ADDRESS, 0, vectorCell)
            return callArrayReturn(methodBind, instance, args, BuiltinTypes::readArrayPlanes)
        }
    }

    fun ptrcallWithRect2ArgRetDictionaryList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Rect2,
    ): List<Map<String, Any?>> {
        Arena.ofConfined().use { arena ->
            val rectCell = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
            writeRect2(rectCell, value)
            val args = arena.allocate(ADDRESS, 1)
            args.setAtIndex(ADDRESS, 0, rectCell)
            return callArrayReturn(methodBind, instance, args, BuiltinTypes::readArrayDictionaries)
        }
    }

    fun ptrcallWithIntArgRetArrayList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Int,
    ): List<List<Any?>> {
        Arena.ofConfined().use { arena ->
            val intCell = arena.allocate(JAVA_INT)
            intCell.set(JAVA_INT, 0, value)
            val args = arena.allocate(ADDRESS, 1)
            args.setAtIndex(ADDRESS, 0, intCell)
            return callArrayReturn(methodBind, instance, args, BuiltinTypes::readArrayArrays)
        }
    }

    fun ptrcallWithRIDAndIntArgRetArrayList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Int,
    ): List<List<Any?>> {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val intCell = arena.allocate(JAVA_INT)
            ridCell.set(JAVA_LONG, 0, rid.value)
            intCell.set(JAVA_INT, 0, value)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, intCell)
            return callArrayReturn(methodBind, instance, args, BuiltinTypes::readArrayArrays)
        }
    }

    fun ptrcallWithRIDAndIntArgRetArray(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Int,
    ): List<Any?> {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val intCell = arena.allocate(JAVA_INT)
            ridCell.set(JAVA_LONG, 0, rid.value)
            intCell.set(JAVA_INT, 0, value)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, intCell)
            return callArrayReturn(methodBind, instance, args, BuiltinTypes::readArrayScalars)
        }
    }

    fun ptrcallWithVector2iListArgRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        values: List<Vector2i>,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val array = arena.allocate(8L, 8L)
            try {
                BuiltinTypes.initArray(array, values.map { it as Any? })
                val args = arena.allocate(ADDRESS, 1)
                args.setAtIndex(ADDRESS, 0, array)
                val ret = arena.allocate(ADDRESS)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(ADDRESS, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, array)
            }
        }
    }

    fun ptrcallWithIntAndVector2iListArgsRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Int,
        values: List<Vector2i>,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val intCell = arena.allocate(JAVA_INT)
            val array = arena.allocate(8L, 8L)
            intCell.set(JAVA_INT, 0, value)
            try {
                BuiltinTypes.initArray(array, values.map { it as Any? })
                val args = arena.allocate(ADDRESS, 2)
                args.setAtIndex(ADDRESS, 0, intCell)
                args.setAtIndex(ADDRESS, 1, array)
                val ret = arena.allocate(ADDRESS)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(ADDRESS, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, array)
            }
        }
    }

    fun ptrcallWithIntVector2iAndIntArgsRetVector2iList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        vector: Vector2i,
        second: Int,
    ): List<Vector2i> {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(JAVA_INT)
            val vectorCell = arena.allocate(JAVA_INT, 2)
            val secondCell = arena.allocate(JAVA_INT)
            firstCell.set(JAVA_INT, 0, first)
            vectorCell.set(JAVA_INT, 0, vector.x)
            vectorCell.set(JAVA_INT, 4, vector.y)
            secondCell.set(JAVA_INT, 0, second)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, firstCell)
            args.setAtIndex(ADDRESS, 1, vectorCell)
            args.setAtIndex(ADDRESS, 2, secondCell)
            return callArrayReturn(methodBind, instance, args, BuiltinTypes::readArrayVector2i)
        }
    }

    fun ptrcallWithTwoIntVector2iAndIntArgsRetVector2iList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Int,
        vector: Vector2i,
        third: Int,
    ): List<Vector2i> {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(JAVA_INT)
            val secondCell = arena.allocate(JAVA_INT)
            val vectorCell = arena.allocate(JAVA_INT, 2)
            val thirdCell = arena.allocate(JAVA_INT)
            firstCell.set(JAVA_INT, 0, first)
            secondCell.set(JAVA_INT, 0, second)
            vectorCell.set(JAVA_INT, 0, vector.x)
            vectorCell.set(JAVA_INT, 4, vector.y)
            thirdCell.set(JAVA_INT, 0, third)
            val args = arena.allocate(ADDRESS, 4)
            args.setAtIndex(ADDRESS, 0, firstCell)
            args.setAtIndex(ADDRESS, 1, secondCell)
            args.setAtIndex(ADDRESS, 2, vectorCell)
            args.setAtIndex(ADDRESS, 3, thirdCell)
            return callArrayReturn(methodBind, instance, args, BuiltinTypes::readArrayVector2i)
        }
    }

    fun ptrcallWithIntAndByteArrayArgRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Int,
        bytes: ByteArray,
    ): Long {
        Arena.ofConfined().use { arena ->
            val intCell = arena.allocate(JAVA_INT)
            val byteArray = BuiltinTypes.allocatePackedArray(arena)
            try {
                intCell.set(JAVA_INT, 0, value)
                BuiltinTypes.initPackedByteArray(byteArray, bytes)
                val args = arena.allocate(ADDRESS, 2)
                args.setAtIndex(ADDRESS, 0, intCell)
                args.setAtIndex(ADDRESS, 1, byteArray)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, byteArray)
            }
        }
    }

    fun ptrcallWithByteArrayAndBoolArgRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        bytes: ByteArray,
        enabled: Boolean,
    ): Int {
        Arena.ofConfined().use { arena ->
            val byteArray = BuiltinTypes.allocatePackedArray(arena)
            val boolCell = arena.allocate(JAVA_BYTE)
            try {
                BuiltinTypes.initPackedByteArray(byteArray, bytes)
                boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
                val args = arena.allocate(ADDRESS, 2)
                args.setAtIndex(ADDRESS, 0, byteArray)
                args.setAtIndex(ADDRESS, 1, boolCell)
                val ret = arena.allocate(JAVA_INT)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(JAVA_INT, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, byteArray)
            }
        }
    }

    fun ptrcallWithByteArrayIntLongIntArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        bytes: ByteArray,
        peerId: Int,
        mode: Long,
        channel: Int,
    ): Long {
        Arena.ofConfined().use { arena ->
            val byteArray = BuiltinTypes.allocatePackedArray(arena)
            val peerCell = arena.allocate(JAVA_INT)
            val modeCell = arena.allocate(JAVA_LONG)
            val channelCell = arena.allocate(JAVA_INT)
            try {
                BuiltinTypes.initPackedByteArray(byteArray, bytes)
                peerCell.set(JAVA_INT, 0, peerId)
                modeCell.set(JAVA_LONG, 0, mode)
                channelCell.set(JAVA_INT, 0, channel)
                val args = arena.allocate(ADDRESS, 4)
                args.setAtIndex(ADDRESS, 0, byteArray)
                args.setAtIndex(ADDRESS, 1, peerCell)
                args.setAtIndex(ADDRESS, 2, modeCell)
                args.setAtIndex(ADDRESS, 3, channelCell)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, byteArray)
            }
        }
    }

    fun ptrcallWithIntAndPackedStringListArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        index: Int,
        values: List<String>,
    ) {
        Arena.ofConfined().use { arena ->
            val intCell = arena.allocate(JAVA_INT)
            val packed = BuiltinTypes.allocatePackedArray(arena)
            try {
                intCell.set(JAVA_INT, 0, index)
                BuiltinTypes.initPackedStringArray(packed, values)
                val args = arena.allocate(ADDRESS, 2)
                args.setAtIndex(ADDRESS, 0, intCell)
                args.setAtIndex(ADDRESS, 1, packed)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_STRING_ARRAY, packed)
            }
        }
    }

    fun ptrcallWithStringPackedStringListAndIntArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
        values: List<String>,
        index: Int,
    ) {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val packed = BuiltinTypes.allocatePackedArray(arena)
            val intCell = arena.allocate(JAVA_INT)
            try {
                GodotStrings.initString(stringCell, name)
                BuiltinTypes.initPackedStringArray(packed, values)
                intCell.set(JAVA_INT, 0, index)
                val args = arena.allocate(ADDRESS, 3)
                args.setAtIndex(ADDRESS, 0, stringCell)
                args.setAtIndex(ADDRESS, 1, packed)
                args.setAtIndex(ADDRESS, 2, intCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_STRING_ARRAY, packed)
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithTwoIntArgsRetPackedInt32List(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Int,
    ): List<Int> {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(JAVA_INT)
            val secondCell = arena.allocate(JAVA_INT)
            firstCell.set(JAVA_INT, 0, first)
            secondCell.set(JAVA_INT, 0, second)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, firstCell)
            args.setAtIndex(ADDRESS, 1, secondCell)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return try {
                BuiltinTypes.readPackedInt32Array(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_INT32_ARRAY, ret)
            }
        }
    }

    fun ptrcallWithIntAndPackedInt32ListArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        index: Int,
        values: List<Int>,
    ) {
        Arena.ofConfined().use { arena ->
            val intCell = arena.allocate(JAVA_INT)
            val packed = BuiltinTypes.allocatePackedArray(arena)
            try {
                intCell.set(JAVA_INT, 0, index)
                BuiltinTypes.initPackedInt32Array(packed, values)
                val args = arena.allocate(ADDRESS, 2)
                args.setAtIndex(ADDRESS, 0, intCell)
                args.setAtIndex(ADDRESS, 1, packed)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_INT32_ARRAY, packed)
            }
        }
    }

    fun ptrcallWithIntAndPackedVector2ListArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        index: Int,
        values: List<Vector2>,
    ) {
        Arena.ofConfined().use { arena ->
            val intCell = arena.allocate(JAVA_INT)
            val packed = BuiltinTypes.allocatePackedArray(arena)
            try {
                intCell.set(JAVA_INT, 0, index)
                BuiltinTypes.initPackedVector2Array(packed, values)
                val args = arena.allocate(ADDRESS, 2)
                args.setAtIndex(ADDRESS, 0, intCell)
                args.setAtIndex(ADDRESS, 1, packed)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR2_ARRAY, packed)
            }
        }
    }

    fun ptrcallWithIntPackedFloat32ListAndIntArgsRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        frameCount: Int,
        buffer: List<Float>,
        offset: Int,
    ): Int {
        Arena.ofConfined().use { arena ->
            val frameCell = arena.allocate(JAVA_INT)
            val packed = BuiltinTypes.allocatePackedArray(arena)
            val offsetCell = arena.allocate(JAVA_INT)
            try {
                frameCell.set(JAVA_INT, 0, frameCount)
                BuiltinTypes.initPackedFloat32Array(packed, buffer)
                offsetCell.set(JAVA_INT, 0, offset)
                val args = arena.allocate(ADDRESS, 3)
                args.setAtIndex(ADDRESS, 0, frameCell)
                args.setAtIndex(ADDRESS, 1, packed)
                args.setAtIndex(ADDRESS, 2, offsetCell)
                val ret = arena.allocate(JAVA_INT)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(JAVA_INT, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_FLOAT32_ARRAY, packed)
            }
        }
    }

    fun ptrcallWithStringNamePackedStringListAndStringNameArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
        values: List<String>,
        context: String,
    ) {
        Arena.ofConfined().use { arena ->
            val packed = BuiltinTypes.allocatePackedArray(arena)
            try {
                BuiltinTypes.initPackedStringArray(packed, values)
                val args = arena.allocate(ADDRESS, 3)
                args.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(name))
                args.setAtIndex(ADDRESS, 1, packed)
                args.setAtIndex(ADDRESS, 2, GodotStrings.makeStringName(context))
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_STRING_ARRAY, packed)
            }
        }
    }

    fun ptrcallWithRIDAndLongArgRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Long,
    ): Int {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val valueCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            valueCell.set(JAVA_LONG, 0, value)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, valueCell)
            val ret = arena.allocate(JAVA_INT)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(JAVA_INT, 0)
        }
    }

    fun ptrcallWithRIDAndLongArgRetVector2(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Long,
    ): Vector2 {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val valueCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            valueCell.set(JAVA_LONG, 0, value)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, valueCell)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return Vector2(GodotReal.readIndex(ret, 0), GodotReal.readIndex(ret, 1))
        }
    }

    fun ptrcallWithRIDAndLongArgRetTransform3D(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Long,
    ): Transform3D {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val valueCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            valueCell.set(JAVA_LONG, 0, value)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, valueCell)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 12, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return readTransform3D(ret)
        }
    }

    fun ptrcallWithRIDAndVector2iArgRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        vector: Vector2i,
    ): Long {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val vectorCell = arena.allocate(JAVA_INT, 2)
            ridCell.set(JAVA_LONG, 0, rid.value)
            writeVector2i(vectorCell, vector)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, vectorCell)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(JAVA_LONG, 0)
        }
    }

    fun ptrcallWithRIDAndTwoLongArgsRetVector2(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        first: Long,
        second: Long,
    ): Vector2 {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val firstCell = arena.allocate(JAVA_LONG)
            val secondCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            firstCell.set(JAVA_LONG, 0, first)
            secondCell.set(JAVA_LONG, 0, second)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, firstCell)
            args.setAtIndex(ADDRESS, 2, secondCell)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return Vector2(GodotReal.readIndex(ret, 0), GodotReal.readIndex(ret, 1))
        }
    }

    fun ptrcallWithRIDAndTwoLongArgsRetPackedVector2List(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        first: Long,
        second: Long,
    ): List<Vector2> {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val firstCell = arena.allocate(JAVA_LONG)
            val secondCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            firstCell.set(JAVA_LONG, 0, first)
            secondCell.set(JAVA_LONG, 0, second)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, firstCell)
            args.setAtIndex(ADDRESS, 2, secondCell)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return try {
                BuiltinTypes.readPackedVector2Array(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR2_ARRAY, ret)
            }
        }
    }

    fun ptrcallWithRIDLongVector2iArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Long,
        vector: Vector2i,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val valueCell = arena.allocate(JAVA_LONG)
            val vectorCell = arena.allocate(JAVA_INT, 2)
            ridCell.set(JAVA_LONG, 0, rid.value)
            valueCell.set(JAVA_LONG, 0, value)
            writeVector2i(vectorCell, vector)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, valueCell)
            args.setAtIndex(ADDRESS, 2, vectorCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDLongVector2iArgsRetVector2(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Long,
        vector: Vector2i,
    ): Vector2 {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val valueCell = arena.allocate(JAVA_LONG)
            val vectorCell = arena.allocate(JAVA_INT, 2)
            ridCell.set(JAVA_LONG, 0, rid.value)
            valueCell.set(JAVA_LONG, 0, value)
            writeVector2i(vectorCell, vector)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, valueCell)
            args.setAtIndex(ADDRESS, 2, vectorCell)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return Vector2(GodotReal.readIndex(ret, 0), GodotReal.readIndex(ret, 1))
        }
    }

    fun ptrcallWithRIDLongVector2iAndVector2Args(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Long,
        vector: Vector2i,
        advance: Vector2,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val valueCell = arena.allocate(JAVA_LONG)
            val vectorCell = arena.allocate(JAVA_INT, 2)
            val advanceCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            ridCell.set(JAVA_LONG, 0, rid.value)
            valueCell.set(JAVA_LONG, 0, value)
            writeVector2i(vectorCell, vector)
            writeVector2(advanceCell, advance)
            val args = arena.allocate(ADDRESS, 4)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, valueCell)
            args.setAtIndex(ADDRESS, 2, vectorCell)
            args.setAtIndex(ADDRESS, 3, advanceCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDTwoLongAndVector2Args(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        first: Long,
        second: Long,
        vector: Vector2,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val firstCell = arena.allocate(JAVA_LONG)
            val secondCell = arena.allocate(JAVA_LONG)
            val vectorCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            ridCell.set(JAVA_LONG, 0, rid.value)
            firstCell.set(JAVA_LONG, 0, first)
            secondCell.set(JAVA_LONG, 0, second)
            writeVector2(vectorCell, vector)
            val args = arena.allocate(ADDRESS, 4)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, firstCell)
            args.setAtIndex(ADDRESS, 2, secondCell)
            args.setAtIndex(ADDRESS, 3, vectorCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDDoubleAndLongArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        width: Double,
        flags: Long,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val widthCell = arena.allocate(JAVA_DOUBLE)
            val flagsCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            widthCell.set(JAVA_DOUBLE, 0, width)
            flagsCell.set(JAVA_LONG, 0, flags)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, widthCell)
            args.setAtIndex(ADDRESS, 2, flagsCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDDoubleAndLongArgsRetDouble(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        width: Double,
        flags: Long,
    ): Double {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val widthCell = arena.allocate(JAVA_DOUBLE)
            val flagsCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            widthCell.set(JAVA_DOUBLE, 0, width)
            flagsCell.set(JAVA_LONG, 0, flags)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, widthCell)
            args.setAtIndex(ADDRESS, 2, flagsCell)
            val ret = arena.allocate(JAVA_DOUBLE)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(JAVA_DOUBLE, 0)
        }
    }

    fun ptrcallWithRIDDoubleTwoLongArgsRetPackedInt32List(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        width: Double,
        start: Long,
        flags: Long,
    ): List<Int> {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val widthCell = arena.allocate(JAVA_DOUBLE)
            val startCell = arena.allocate(JAVA_LONG)
            val flagsCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            widthCell.set(JAVA_DOUBLE, 0, width)
            startCell.set(JAVA_LONG, 0, start)
            flagsCell.set(JAVA_LONG, 0, flags)
            val args = arena.allocate(ADDRESS, 4)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, widthCell)
            args.setAtIndex(ADDRESS, 2, startCell)
            args.setAtIndex(ADDRESS, 3, flagsCell)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return try {
                BuiltinTypes.readPackedInt32Array(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_INT32_ARRAY, ret)
            }
        }
    }

    fun ptrcallWithRIDAndTwoLongArgsRetPackedInt32List(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        first: Long,
        second: Long,
    ): List<Int> {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val firstCell = arena.allocate(JAVA_LONG)
            val secondCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            firstCell.set(JAVA_LONG, 0, first)
            secondCell.set(JAVA_LONG, 0, second)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, firstCell)
            args.setAtIndex(ADDRESS, 2, secondCell)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return try {
                BuiltinTypes.readPackedInt32Array(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_INT32_ARRAY, ret)
            }
        }
    }

    fun ptrcallWithTwoLongArgsRetRID(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Long,
        second: Long,
    ): RID {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(JAVA_LONG)
            val secondCell = arena.allocate(JAVA_LONG)
            firstCell.set(JAVA_LONG, 0, first)
            secondCell.set(JAVA_LONG, 0, second)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, firstCell)
            args.setAtIndex(ADDRESS, 1, secondCell)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return RID(ret.get(JAVA_LONG, 0))
        }
    }

    fun ptrcallWithLongAndIntArgsRetVector2(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Long,
        second: Int,
    ): Vector2 {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(JAVA_LONG)
            val secondCell = arena.allocate(JAVA_INT)
            firstCell.set(JAVA_LONG, 0, first)
            secondCell.set(JAVA_INT, 0, second)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, firstCell)
            args.setAtIndex(ADDRESS, 1, secondCell)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return Vector2(GodotReal.readIndex(ret, 0), GodotReal.readIndex(ret, 1))
        }
    }

    fun ptrcallWithLongAndTwoIntArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Long,
        second: Int,
        third: Int,
    ) {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(JAVA_LONG)
            val secondCell = arena.allocate(JAVA_INT)
            val thirdCell = arena.allocate(JAVA_INT)
            firstCell.set(JAVA_LONG, 0, first)
            secondCell.set(JAVA_INT, 0, second)
            thirdCell.set(JAVA_INT, 0, third)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, firstCell)
            args.setAtIndex(ADDRESS, 1, secondCell)
            args.setAtIndex(ADDRESS, 2, thirdCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithLongIntVector2Args(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Long,
        second: Int,
        vector: Vector2,
    ) {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(JAVA_LONG)
            val secondCell = arena.allocate(JAVA_INT)
            val vectorCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            firstCell.set(JAVA_LONG, 0, first)
            secondCell.set(JAVA_INT, 0, second)
            writeVector2(vectorCell, vector)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, firstCell)
            args.setAtIndex(ADDRESS, 1, secondCell)
            args.setAtIndex(ADDRESS, 2, vectorCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithLongIntStringNameArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Long,
        second: Int,
        name: String,
    ) {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(JAVA_LONG)
            val secondCell = arena.allocate(JAVA_INT)
            firstCell.set(JAVA_LONG, 0, first)
            secondCell.set(JAVA_INT, 0, second)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, firstCell)
            args.setAtIndex(ADDRESS, 1, secondCell)
            args.setAtIndex(ADDRESS, 2, GodotStrings.makeStringName(name))
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithLongAndFourIntArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Long,
        second: Int,
        third: Int,
        fourth: Int,
        fifth: Int,
    ): Long {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(JAVA_LONG)
            val secondCell = arena.allocate(JAVA_INT)
            val thirdCell = arena.allocate(JAVA_INT)
            val fourthCell = arena.allocate(JAVA_INT)
            val fifthCell = arena.allocate(JAVA_INT)
            firstCell.set(JAVA_LONG, 0, first)
            secondCell.set(JAVA_INT, 0, second)
            thirdCell.set(JAVA_INT, 0, third)
            fourthCell.set(JAVA_INT, 0, fourth)
            fifthCell.set(JAVA_INT, 0, fifth)
            val args = arena.allocate(ADDRESS, 5)
            args.setAtIndex(ADDRESS, 0, firstCell)
            args.setAtIndex(ADDRESS, 1, secondCell)
            args.setAtIndex(ADDRESS, 2, thirdCell)
            args.setAtIndex(ADDRESS, 3, fourthCell)
            args.setAtIndex(ADDRESS, 4, fifthCell)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(JAVA_LONG, 0)
        }
    }

    fun ptrcallWithIntAndBoolArgsRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Int,
        includeInternal: Boolean,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val valueCell = arena.allocate(JAVA_INT)
            val boolCell = arena.allocate(JAVA_BYTE)
            valueCell.set(JAVA_INT, 0, value)
            boolCell.set(JAVA_BYTE, 0, if (includeInternal) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, valueCell)
            args.setAtIndex(ADDRESS, 1, boolCell)
            val ret = arena.allocate(ADDRESS)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(ADDRESS, 0)
        }
    }

    fun ptrcallWithStringAndTwoBoolArgsRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        pattern: String,
        recursive: Boolean,
        owned: Boolean,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val recursiveCell = arena.allocate(JAVA_BYTE)
            val ownedCell = arena.allocate(JAVA_BYTE)
            try {
                GodotStrings.initString(stringCell, pattern)
                recursiveCell.set(JAVA_BYTE, 0, if (recursive) 1.toByte() else 0.toByte())
                ownedCell.set(JAVA_BYTE, 0, if (owned) 1.toByte() else 0.toByte())
                val args = arena.allocate(ADDRESS, 3)
                args.setAtIndex(ADDRESS, 0, stringCell)
                args.setAtIndex(ADDRESS, 1, recursiveCell)
                args.setAtIndex(ADDRESS, 2, ownedCell)
                val ret = arena.allocate(ADDRESS)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(ADDRESS, 0)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithObjectAndIntArgRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        index: Int,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val objectCell = arena.allocate(ADDRESS)
            val indexCell = arena.allocate(JAVA_INT)
            objectCell.set(ADDRESS, 0, objectArg)
            indexCell.set(JAVA_INT, 0, index)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, objectCell)
            args.setAtIndex(ADDRESS, 1, indexCell)
            val ret = arena.allocate(ADDRESS)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(ADDRESS, 0)
        }
    }

    fun ptrcallWithObjectAndTwoIntArgsRetRect2(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        column: Int,
        buttonIndex: Int,
    ): Rect2 {
        Arena.ofConfined().use { arena ->
            val objectCell = arena.allocate(ADDRESS)
            val columnCell = arena.allocate(JAVA_INT)
            val buttonCell = arena.allocate(JAVA_INT)
            objectCell.set(ADDRESS, 0, objectArg)
            columnCell.set(JAVA_INT, 0, column)
            buttonCell.set(JAVA_INT, 0, buttonIndex)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, objectCell)
            args.setAtIndex(ADDRESS, 1, columnCell)
            args.setAtIndex(ADDRESS, 2, buttonCell)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return Rect2(
                position = Vector2(GodotReal.readIndex(ret, 0), GodotReal.readIndex(ret, 1)),
                size = Vector2(GodotReal.readIndex(ret, 2), GodotReal.readIndex(ret, 3)),
            )
        }
    }

    fun ptrcallWithVector2ArgRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        vector: Vector2,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val vectorCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            writeVector2(vectorCell, vector)
            val args = arena.allocate(ADDRESS, 1)
            args.setAtIndex(ADDRESS, 0, vectorCell)
            val ret = arena.allocate(ADDRESS)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(ADDRESS, 0)
        }
    }

    fun ptrcallWithIntVector2iAndBoolArgRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        layer: Int,
        coords: Vector2i,
        useProxies: Boolean,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val layerCell = arena.allocate(JAVA_INT)
            val coordsCell = arena.allocate(JAVA_INT, 2)
            val boolCell = arena.allocate(JAVA_BYTE)
            layerCell.set(JAVA_INT, 0, layer)
            writeVector2i(coordsCell, coords)
            boolCell.set(JAVA_BYTE, 0, if (useProxies) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, layerCell)
            args.setAtIndex(ADDRESS, 1, coordsCell)
            args.setAtIndex(ADDRESS, 2, boolCell)
            val ret = arena.allocate(ADDRESS)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(ADDRESS, 0)
        }
    }

    fun ptrcallWithIntVector2iAndObjectArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        layer: Int,
        coords: Vector2i,
        pattern: MemorySegment,
    ) {
        Arena.ofConfined().use { arena ->
            val layerCell = arena.allocate(JAVA_INT)
            val coordsCell = arena.allocate(JAVA_INT, 2)
            val objectCell = arena.allocate(ADDRESS)
            layerCell.set(JAVA_INT, 0, layer)
            writeVector2i(coordsCell, coords)
            objectCell.set(ADDRESS, 0, pattern)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, layerCell)
            args.setAtIndex(ADDRESS, 1, coordsCell)
            args.setAtIndex(ADDRESS, 2, objectCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithIntVector2iIntObjectArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstInt: Int,
        coords: Vector2i,
        secondInt: Int,
        objectArg: MemorySegment,
    ) {
        Arena.ofConfined().use { arena ->
            val int0 = arena.allocate(JAVA_INT)
            val coordsCell = arena.allocate(JAVA_INT, 2)
            val int1 = arena.allocate(JAVA_INT)
            val objCell = arena.allocate(ADDRESS)
            int0.set(JAVA_INT, 0, firstInt)
            writeVector2i(coordsCell, coords)
            int1.set(JAVA_INT, 0, secondInt)
            objCell.set(ADDRESS, 0, objectArg)
            val args = arena.allocate(ADDRESS, 4)
            args.setAtIndex(ADDRESS, 0, int0)
            args.setAtIndex(ADDRESS, 1, coordsCell)
            args.setAtIndex(ADDRESS, 2, int1)
            args.setAtIndex(ADDRESS, 3, objCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithVector2iArgRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        coords: Vector2i,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val coordsCell = arena.allocate(JAVA_INT, 2)
            writeVector2i(coordsCell, coords)
            val args = arena.allocate(ADDRESS, 1)
            args.setAtIndex(ADDRESS, 0, coordsCell)
            val ret = arena.allocate(ADDRESS)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(ADDRESS, 0)
        }
    }

    fun ptrcallWithVector2iAndObjectArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        coords: Vector2i,
        pattern: MemorySegment,
    ) {
        Arena.ofConfined().use { arena ->
            val coordsCell = arena.allocate(JAVA_INT, 2)
            val objectCell = arena.allocate(ADDRESS)
            writeVector2i(coordsCell, coords)
            objectCell.set(ADDRESS, 0, pattern)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, coordsCell)
            args.setAtIndex(ADDRESS, 1, objectCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithVector2iAndIntArgRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        coords: Vector2i,
        alternativeTile: Int,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val coordsCell = arena.allocate(JAVA_INT, 2)
            val intCell = arena.allocate(JAVA_INT)
            writeVector2i(coordsCell, coords)
            intCell.set(JAVA_INT, 0, alternativeTile)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, coordsCell)
            args.setAtIndex(ADDRESS, 1, intCell)
            val ret = arena.allocate(ADDRESS)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(ADDRESS, 0)
        }
    }

    fun ptrcallWithStringNameAndObjectArgRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
        objectArg: MemorySegment,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val objectCell = arena.allocate(ADDRESS)
            objectCell.set(ADDRESS, 0, objectArg)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(name))
            args.setAtIndex(ADDRESS, 1, objectCell)
            val ret = arena.allocate(JAVA_BYTE)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(JAVA_BYTE, 0) != 0.toByte()
        }
    }

    fun ptrcallWithStringNameObjectIntArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
        objectArg: MemorySegment,
        intValue: Int,
    ) {
        Arena.ofConfined().use { arena ->
            val objectCell = arena.allocate(ADDRESS)
            val intCell = arena.allocate(JAVA_INT)
            objectCell.set(ADDRESS, 0, objectArg)
            intCell.set(JAVA_INT, 0, intValue)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(name))
            args.setAtIndex(ADDRESS, 1, objectCell)
            args.setAtIndex(ADDRESS, 2, intCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithStringNameLongVariantArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
        longValue: Long,
        value: Any?,
    ) {
        Arena.ofConfined().use { arena ->
            val variant = initVariantCell(arena, value)
            try {
                val longCell = arena.allocate(JAVA_LONG)
                longCell.set(JAVA_LONG, 0, longValue)
                val args = arena.allocate(ADDRESS, 3)
                args.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(name))
                args.setAtIndex(ADDRESS, 1, longCell)
                args.setAtIndex(ADDRESS, 2, variant)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyVariant(variant)
            }
        }
    }

    fun ptrcallWithObjectStringNameAndBoolArgRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        action: String,
        exactMatch: Boolean,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val objectCell = arena.allocate(ADDRESS)
            val boolCell = arena.allocate(JAVA_BYTE)
            objectCell.set(ADDRESS, 0, objectArg)
            boolCell.set(JAVA_BYTE, 0, if (exactMatch) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, objectCell)
            args.setAtIndex(ADDRESS, 1, GodotStrings.makeStringName(action))
            args.setAtIndex(ADDRESS, 2, boolCell)
            val ret = arena.allocate(JAVA_BYTE)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(JAVA_BYTE, 0) != 0.toByte()
        }
    }

    fun ptrcallWithStringAndBoolArgRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        showError: Boolean,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val boolCell = arena.allocate(JAVA_BYTE)
            try {
                GodotStrings.initString(stringCell, text)
                boolCell.set(JAVA_BYTE, 0, if (showError) 1.toByte() else 0.toByte())
                val args = arena.allocate(ADDRESS, 2)
                args.setAtIndex(ADDRESS, 0, stringCell)
                args.setAtIndex(ADDRESS, 1, boolCell)
                val ret = arena.allocate(ADDRESS)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(ADDRESS, 0)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithStringAndTwoIntArgsRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        offset: Int,
        end: Int,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val offsetCell = arena.allocate(JAVA_INT)
            val endCell = arena.allocate(JAVA_INT)
            try {
                GodotStrings.initString(stringCell, text)
                offsetCell.set(JAVA_INT, 0, offset)
                endCell.set(JAVA_INT, 0, end)
                val args = arena.allocate(ADDRESS, 3)
                args.setAtIndex(ADDRESS, 0, stringCell)
                args.setAtIndex(ADDRESS, 1, offsetCell)
                args.setAtIndex(ADDRESS, 2, endCell)
                val ret = arena.allocate(ADDRESS)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(ADDRESS, 0)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithTwoStringBoolTwoIntArgsRetString(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        replacement: String,
        all: Boolean,
        offset: Int,
        end: Int,
    ): String {
        Arena.ofConfined().use { arena ->
            val textCell = arena.allocate(8L, 8L)
            val replacementCell = arena.allocate(8L, 8L)
            val boolCell = arena.allocate(JAVA_BYTE)
            val offsetCell = arena.allocate(JAVA_INT)
            val endCell = arena.allocate(JAVA_INT)
            val ret = arena.allocate(8L, 8L)
            try {
                GodotStrings.initString(textCell, text)
                GodotStrings.initString(replacementCell, replacement)
                boolCell.set(JAVA_BYTE, 0, if (all) 1.toByte() else 0.toByte())
                offsetCell.set(JAVA_INT, 0, offset)
                endCell.set(JAVA_INT, 0, end)
                val args = arena.allocate(ADDRESS, 5)
                args.setAtIndex(ADDRESS, 0, textCell)
                args.setAtIndex(ADDRESS, 1, replacementCell)
                args.setAtIndex(ADDRESS, 2, boolCell)
                args.setAtIndex(ADDRESS, 3, offsetCell)
                args.setAtIndex(ADDRESS, 4, endCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return GodotStrings.readString(ret)
            } finally {
                GodotStrings.destroyString(ret)
                GodotStrings.destroyString(replacementCell)
                GodotStrings.destroyString(textCell)
            }
        }
    }

    fun ptrcallWithObjectAndStringArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        text: String,
    ) {
        Arena.ofConfined().use { arena ->
            val objectCell = arena.allocate(ADDRESS)
            val stringCell = arena.allocate(8L, 8L)
            try {
                objectCell.set(ADDRESS, 0, objectArg)
                GodotStrings.initString(stringCell, text)
                val args = arena.allocate(ADDRESS, 2)
                args.setAtIndex(ADDRESS, 0, objectCell)
                args.setAtIndex(ADDRESS, 1, stringCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithObjectStringBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        text: String,
        enabled: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val objectCell = arena.allocate(ADDRESS)
            val stringCell = arena.allocate(8L, 8L)
            val boolCell = arena.allocate(JAVA_BYTE)
            try {
                objectCell.set(ADDRESS, 0, objectArg)
                GodotStrings.initString(stringCell, text)
                boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
                val args = arena.allocate(ADDRESS, 3)
                args.setAtIndex(ADDRESS, 0, objectCell)
                args.setAtIndex(ADDRESS, 1, stringCell)
                args.setAtIndex(ADDRESS, 2, boolCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithObjectTwoIntBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        first: Int,
        second: Int,
        enabled: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val objectCell = arena.allocate(ADDRESS)
            val firstCell = arena.allocate(JAVA_INT)
            val secondCell = arena.allocate(JAVA_INT)
            val boolCell = arena.allocate(JAVA_BYTE)
            objectCell.set(ADDRESS, 0, objectArg)
            firstCell.set(JAVA_INT, 0, first)
            secondCell.set(JAVA_INT, 0, second)
            boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 4)
            args.setAtIndex(ADDRESS, 0, objectCell)
            args.setAtIndex(ADDRESS, 1, firstCell)
            args.setAtIndex(ADDRESS, 2, secondCell)
            args.setAtIndex(ADDRESS, 3, boolCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithTwoStringNameAndBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: String,
        second: String,
        enabled: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val boolCell = arena.allocate(JAVA_BYTE)
            boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(first))
            args.setAtIndex(ADDRESS, 1, GodotStrings.makeStringName(second))
            args.setAtIndex(ADDRESS, 2, boolCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithStringNameAndBoolArgRetString(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
        enabled: Boolean,
    ): String {
        Arena.ofConfined().use { arena ->
            val boolCell = arena.allocate(JAVA_BYTE)
            val ret = arena.allocate(8L, 8L)
            try {
                boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
                val args = arena.allocate(ADDRESS, 2)
                args.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(name))
                args.setAtIndex(ADDRESS, 1, boolCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return GodotStrings.readString(ret)
            } finally {
                GodotStrings.destroyString(ret)
            }
        }
    }

    fun ptrcallWithTwoStringPackedStringListStringIntArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: String,
        second: String,
        values: List<String>,
        third: String,
        index: Int,
    ): Long {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(8L, 8L)
            val secondCell = arena.allocate(8L, 8L)
            val packed = BuiltinTypes.allocatePackedArray(arena)
            val thirdCell = arena.allocate(8L, 8L)
            val indexCell = arena.allocate(JAVA_INT)
            try {
                GodotStrings.initString(firstCell, first)
                GodotStrings.initString(secondCell, second)
                BuiltinTypes.initPackedStringArray(packed, values)
                GodotStrings.initString(thirdCell, third)
                indexCell.set(JAVA_INT, 0, index)
                val args = arena.allocate(ADDRESS, 5)
                args.setAtIndex(ADDRESS, 0, firstCell)
                args.setAtIndex(ADDRESS, 1, secondCell)
                args.setAtIndex(ADDRESS, 2, packed)
                args.setAtIndex(ADDRESS, 3, thirdCell)
                args.setAtIndex(ADDRESS, 4, indexCell)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                GodotStrings.destroyString(thirdCell)
                BuiltinTypes.destroyTyped(VariantType.PACKED_STRING_ARRAY, packed)
                GodotStrings.destroyString(secondCell)
                GodotStrings.destroyString(firstCell)
            }
        }
    }

    fun ptrcallWithTwoStringPackedStringListAndTwoStringArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: String,
        second: String,
        values: List<String>,
        third: String,
        fourth: String,
    ): Long {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(8L, 8L)
            val secondCell = arena.allocate(8L, 8L)
            val packed = BuiltinTypes.allocatePackedArray(arena)
            val thirdCell = arena.allocate(8L, 8L)
            val fourthCell = arena.allocate(8L, 8L)
            try {
                GodotStrings.initString(firstCell, first)
                GodotStrings.initString(secondCell, second)
                BuiltinTypes.initPackedStringArray(packed, values)
                GodotStrings.initString(thirdCell, third)
                GodotStrings.initString(fourthCell, fourth)
                val args = arena.allocate(ADDRESS, 5)
                args.setAtIndex(ADDRESS, 0, firstCell)
                args.setAtIndex(ADDRESS, 1, secondCell)
                args.setAtIndex(ADDRESS, 2, packed)
                args.setAtIndex(ADDRESS, 3, thirdCell)
                args.setAtIndex(ADDRESS, 4, fourthCell)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                GodotStrings.destroyString(fourthCell)
                GodotStrings.destroyString(thirdCell)
                BuiltinTypes.destroyTyped(VariantType.PACKED_STRING_ARRAY, packed)
                GodotStrings.destroyString(secondCell)
                GodotStrings.destroyString(firstCell)
            }
        }
    }

    fun ptrcallWithStringPackedStringListAndStringArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        values: List<String>,
        second: String,
    ) {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val packed = BuiltinTypes.allocatePackedArray(arena)
            val secondCell = arena.allocate(8L, 8L)
            try {
                GodotStrings.initString(stringCell, text)
                BuiltinTypes.initPackedStringArray(packed, values)
                GodotStrings.initString(secondCell, second)
                val args = arena.allocate(ADDRESS, 3)
                args.setAtIndex(ADDRESS, 0, stringCell)
                args.setAtIndex(ADDRESS, 1, packed)
                args.setAtIndex(ADDRESS, 2, secondCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(secondCell)
                BuiltinTypes.destroyTyped(VariantType.PACKED_STRING_ARRAY, packed)
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithStringPackedStringListAndObjectArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        values: List<String>,
        objectArg: MemorySegment,
    ) {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val packed = BuiltinTypes.allocatePackedArray(arena)
            val objectCell = arena.allocate(ADDRESS)
            try {
                GodotStrings.initString(stringCell, text)
                BuiltinTypes.initPackedStringArray(packed, values)
                objectCell.set(ADDRESS, 0, objectArg)
                val args = arena.allocate(ADDRESS, 3)
                args.setAtIndex(ADDRESS, 0, stringCell)
                args.setAtIndex(ADDRESS, 1, packed)
                args.setAtIndex(ADDRESS, 2, objectCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_STRING_ARRAY, packed)
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithStringByteArrayBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        path: String,
        bytes: ByteArray,
        enabled: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val packed = BuiltinTypes.allocatePackedArray(arena)
            val boolCell = arena.allocate(JAVA_BYTE)
            try {
                GodotStrings.initString(stringCell, path)
                BuiltinTypes.initPackedByteArray(packed, bytes)
                boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
                val args = arena.allocate(ADDRESS, 3)
                args.setAtIndex(ADDRESS, 0, stringCell)
                args.setAtIndex(ADDRESS, 1, packed)
                args.setAtIndex(ADDRESS, 2, boolCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, packed)
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithStringAndRIDArgRetRID(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        rid: RID,
    ): RID {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val ridCell = arena.allocate(JAVA_LONG)
            try {
                GodotStrings.initString(stringCell, text)
                ridCell.set(JAVA_LONG, 0, rid.value)
                val args = arena.allocate(ADDRESS, 2)
                args.setAtIndex(ADDRESS, 0, stringCell)
                args.setAtIndex(ADDRESS, 1, ridCell)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return RID(ret.get(JAVA_LONG, 0))
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithStringAndIntArgRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        index: Int,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val indexCell = arena.allocate(JAVA_INT)
            try {
                GodotStrings.initString(stringCell, text)
                indexCell.set(JAVA_INT, 0, index)
                val args = arena.allocate(ADDRESS, 2)
                args.setAtIndex(ADDRESS, 0, stringCell)
                args.setAtIndex(ADDRESS, 1, indexCell)
                val ret = arena.allocate(ADDRESS)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(ADDRESS, 0)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithStringIntAndObjectArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        index: Int,
        objectArg: MemorySegment,
    ) {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val indexCell = arena.allocate(JAVA_INT)
            val objectCell = arena.allocate(ADDRESS)
            try {
                GodotStrings.initString(stringCell, text)
                indexCell.set(JAVA_INT, 0, index)
                objectCell.set(ADDRESS, 0, objectArg)
                val args = arena.allocate(ADDRESS, 3)
                args.setAtIndex(ADDRESS, 0, stringCell)
                args.setAtIndex(ADDRESS, 1, indexCell)
                args.setAtIndex(ADDRESS, 2, objectCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithStringAndThreeIntArgsRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        first: Int,
        second: Int,
        third: Int,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val firstCell = arena.allocate(JAVA_INT)
            val secondCell = arena.allocate(JAVA_INT)
            val thirdCell = arena.allocate(JAVA_INT)
            try {
                GodotStrings.initString(stringCell, text)
                firstCell.set(JAVA_INT, 0, first)
                secondCell.set(JAVA_INT, 0, second)
                thirdCell.set(JAVA_INT, 0, third)
                val args = arena.allocate(ADDRESS, 4)
                args.setAtIndex(ADDRESS, 0, stringCell)
                args.setAtIndex(ADDRESS, 1, firstCell)
                args.setAtIndex(ADDRESS, 2, secondCell)
                args.setAtIndex(ADDRESS, 3, thirdCell)
                val ret = arena.allocate(ADDRESS)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(ADDRESS, 0)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithStringAndObjectArgRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        objectArg: MemorySegment,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val objectCell = arena.allocate(ADDRESS)
            try {
                GodotStrings.initString(stringCell, text)
                objectCell.set(ADDRESS, 0, objectArg)
                val args = arena.allocate(ADDRESS, 2)
                args.setAtIndex(ADDRESS, 0, stringCell)
                args.setAtIndex(ADDRESS, 1, objectCell)
                val ret = arena.allocate(JAVA_BYTE)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(JAVA_BYTE, 0) != 0.toByte()
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithStringObjectBoolStringArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        objectArg: MemorySegment,
        enabled: Boolean,
        suffix: String,
    ) {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val objectCell = arena.allocate(ADDRESS)
            val boolCell = arena.allocate(JAVA_BYTE)
            val suffixCell = arena.allocate(8L, 8L)
            try {
                GodotStrings.initString(stringCell, text)
                objectCell.set(ADDRESS, 0, objectArg)
                boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
                GodotStrings.initString(suffixCell, suffix)
                val args = arena.allocate(ADDRESS, 4)
                args.setAtIndex(ADDRESS, 0, stringCell)
                args.setAtIndex(ADDRESS, 1, objectCell)
                args.setAtIndex(ADDRESS, 2, boolCell)
                args.setAtIndex(ADDRESS, 3, suffixCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(suffixCell)
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithIntAndObjectArgRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Int,
        objectArg: MemorySegment,
    ): Long {
        Arena.ofConfined().use { arena ->
            val valueCell = arena.allocate(JAVA_INT)
            val objectCell = arena.allocate(ADDRESS)
            valueCell.set(JAVA_INT, 0, value)
            objectCell.set(ADDRESS, 0, objectArg)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, valueCell)
            args.setAtIndex(ADDRESS, 1, objectCell)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(JAVA_LONG, 0)
        }
    }

    fun ptrcallWithStringAndBoolArgRetString(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        enabled: Boolean,
    ): String {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val boolCell = arena.allocate(JAVA_BYTE)
            val ret = arena.allocate(8L, 8L)
            try {
                GodotStrings.initString(stringCell, text)
                boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
                val args = arena.allocate(ADDRESS, 2)
                args.setAtIndex(ADDRESS, 0, stringCell)
                args.setAtIndex(ADDRESS, 1, boolCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return GodotStrings.readString(ret)
            } finally {
                GodotStrings.destroyString(ret)
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithIntObjectDoubleArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        index: Int,
        objectArg: MemorySegment,
        value: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val indexCell = arena.allocate(JAVA_INT)
            val objectCell = arena.allocate(ADDRESS)
            val valueCell = arena.allocate(JAVA_DOUBLE)
            indexCell.set(JAVA_INT, 0, index)
            objectCell.set(ADDRESS, 0, objectArg)
            valueCell.set(JAVA_DOUBLE, 0, value)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, indexCell)
            args.setAtIndex(ADDRESS, 1, objectCell)
            args.setAtIndex(ADDRESS, 2, valueCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithIntObjectStringNameArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        index: Int,
        objectArg: MemorySegment,
        name: String,
    ) {
        Arena.ofConfined().use { arena ->
            val indexCell = arena.allocate(JAVA_INT)
            val objectCell = arena.allocate(ADDRESS)
            indexCell.set(JAVA_INT, 0, index)
            objectCell.set(ADDRESS, 0, objectArg)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, indexCell)
            args.setAtIndex(ADDRESS, 1, objectCell)
            args.setAtIndex(ADDRESS, 2, GodotStrings.makeStringName(name))
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithIntColorBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        index: Int,
        color: Color,
        enabled: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val indexCell = arena.allocate(JAVA_INT)
            val colorCell = arena.allocate(16L, 4L)
            val boolCell = arena.allocate(JAVA_BYTE)
            indexCell.set(JAVA_INT, 0, index)
            // Color component storage is fixed 32-bit, unlike scalar Godot floats.
            colorCell.set(JAVA_FLOAT, 0, color.r)
            colorCell.set(JAVA_FLOAT, 4, color.g)
            colorCell.set(JAVA_FLOAT, 8, color.b)
            colorCell.set(JAVA_FLOAT, 12, color.a)
            boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, indexCell)
            args.setAtIndex(ADDRESS, 1, colorCell)
            args.setAtIndex(ADDRESS, 2, boolCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithIntThreeDoubleBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        index: Int,
        min: Double,
        max: Double,
        step: Double,
        expression: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val indexCell = arena.allocate(JAVA_INT)
            val minCell = arena.allocate(JAVA_DOUBLE)
            val maxCell = arena.allocate(JAVA_DOUBLE)
            val stepCell = arena.allocate(JAVA_DOUBLE)
            val boolCell = arena.allocate(JAVA_BYTE)
            indexCell.set(JAVA_INT, 0, index)
            minCell.set(JAVA_DOUBLE, 0, min)
            maxCell.set(JAVA_DOUBLE, 0, max)
            stepCell.set(JAVA_DOUBLE, 0, step)
            boolCell.set(JAVA_BYTE, 0, if (expression) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 5)
            args.setAtIndex(ADDRESS, 0, indexCell)
            args.setAtIndex(ADDRESS, 1, minCell)
            args.setAtIndex(ADDRESS, 2, maxCell)
            args.setAtIndex(ADDRESS, 3, stepCell)
            args.setAtIndex(ADDRESS, 4, boolCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (int32, scalar float, bool, bool, scalar float, int64, bool, bool).
     */
    fun ptrcallWithIntDoubleTwoBoolDoubleLongTwoBoolArgsRetDouble(
        methodBind: MemorySegment,
        instance: MemorySegment,
        index: Int,
        firstDouble: Double,
        firstBool: Boolean,
        secondBool: Boolean,
        secondDouble: Double,
        longArg: Long,
        thirdBool: Boolean,
        fourthBool: Boolean,
    ): Double {
        Arena.ofConfined().use { arena ->
            val indexCell = arena.allocate(JAVA_INT)
            val double0 = arena.allocate(JAVA_DOUBLE)
            val bool0 = arena.allocate(JAVA_BYTE)
            val bool1 = arena.allocate(JAVA_BYTE)
            val double1 = arena.allocate(JAVA_DOUBLE)
            val longCell = arena.allocate(JAVA_LONG)
            val bool2 = arena.allocate(JAVA_BYTE)
            val bool3 = arena.allocate(JAVA_BYTE)
            indexCell.set(JAVA_INT, 0, index)
            double0.set(JAVA_DOUBLE, 0, firstDouble)
            bool0.set(JAVA_BYTE, 0, if (firstBool) 1.toByte() else 0.toByte())
            bool1.set(JAVA_BYTE, 0, if (secondBool) 1.toByte() else 0.toByte())
            double1.set(JAVA_DOUBLE, 0, secondDouble)
            longCell.set(JAVA_LONG, 0, longArg)
            bool2.set(JAVA_BYTE, 0, if (thirdBool) 1.toByte() else 0.toByte())
            bool3.set(JAVA_BYTE, 0, if (fourthBool) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 8)
            args.setAtIndex(ADDRESS, 0, indexCell)
            args.setAtIndex(ADDRESS, 1, double0)
            args.setAtIndex(ADDRESS, 2, bool0)
            args.setAtIndex(ADDRESS, 3, bool1)
            args.setAtIndex(ADDRESS, 4, double1)
            args.setAtIndex(ADDRESS, 5, longCell)
            args.setAtIndex(ADDRESS, 6, bool2)
            args.setAtIndex(ADDRESS, 7, bool3)
            val ret = arena.allocate(JAVA_DOUBLE)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(JAVA_DOUBLE, 0)
        }
    }

    fun ptrcallWithIntObjectIntBoolTwoStringArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        column: Int,
        texture: MemorySegment,
        id: Int,
        disabled: Boolean,
        tooltip: String,
        description: String,
    ) {
        Arena.ofConfined().use { arena ->
            val columnCell = arena.allocate(JAVA_INT)
            val objectCell = arena.allocate(ADDRESS)
            val idCell = arena.allocate(JAVA_INT)
            val boolCell = arena.allocate(JAVA_BYTE)
            val tooltipCell = arena.allocate(8L, 8L)
            val descriptionCell = arena.allocate(8L, 8L)
            try {
                columnCell.set(JAVA_INT, 0, column)
                objectCell.set(ADDRESS, 0, texture)
                idCell.set(JAVA_INT, 0, id)
                boolCell.set(JAVA_BYTE, 0, if (disabled) 1.toByte() else 0.toByte())
                GodotStrings.initString(tooltipCell, tooltip)
                GodotStrings.initString(descriptionCell, description)
                val args = arena.allocate(ADDRESS, 6)
                args.setAtIndex(ADDRESS, 0, columnCell)
                args.setAtIndex(ADDRESS, 1, objectCell)
                args.setAtIndex(ADDRESS, 2, idCell)
                args.setAtIndex(ADDRESS, 3, boolCell)
                args.setAtIndex(ADDRESS, 4, tooltipCell)
                args.setAtIndex(ADDRESS, 5, descriptionCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(descriptionCell)
                GodotStrings.destroyString(tooltipCell)
            }
        }
    }

    fun ptrcallWithRIDLongIntArgsRetRID(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        kind: Long,
        index: Int,
    ): RID {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val kindCell = arena.allocate(JAVA_LONG)
            val indexCell = arena.allocate(JAVA_INT)
            ridCell.set(JAVA_LONG, 0, rid.value)
            kindCell.set(JAVA_LONG, 0, kind)
            indexCell.set(JAVA_INT, 0, index)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, kindCell)
            args.setAtIndex(ADDRESS, 2, indexCell)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return RID(ret.get(JAVA_LONG, 0))
        }
    }

    fun ptrcallWithTwoRIDDoubleIntBoolArgsRetRID(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstRid: RID,
        secondRid: RID,
        value: Double,
        index: Int,
        enabled: Boolean,
    ): RID {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(JAVA_LONG)
            val secondCell = arena.allocate(JAVA_LONG)
            val valueCell = arena.allocate(JAVA_DOUBLE)
            val indexCell = arena.allocate(JAVA_INT)
            val boolCell = arena.allocate(JAVA_BYTE)
            firstCell.set(JAVA_LONG, 0, firstRid.value)
            secondCell.set(JAVA_LONG, 0, secondRid.value)
            valueCell.set(JAVA_DOUBLE, 0, value)
            indexCell.set(JAVA_INT, 0, index)
            boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 5)
            args.setAtIndex(ADDRESS, 0, firstCell)
            args.setAtIndex(ADDRESS, 1, secondCell)
            args.setAtIndex(ADDRESS, 2, valueCell)
            args.setAtIndex(ADDRESS, 3, indexCell)
            args.setAtIndex(ADDRESS, 4, boolCell)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return RID(ret.get(JAVA_LONG, 0))
        }
    }

    fun ptrcallWithIntRect2Rect2Args(
        methodBind: MemorySegment,
        instance: MemorySegment,
        index: Int,
        first: Rect2,
        second: Rect2,
    ) {
        Arena.ofConfined().use { arena ->
            val indexCell = arena.allocate(JAVA_INT)
            val firstCell = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
            val secondCell = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
            indexCell.set(JAVA_INT, 0, index)
            writeRect2(firstCell, first)
            writeRect2(secondCell, second)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, indexCell)
            args.setAtIndex(ADDRESS, 1, firstCell)
            args.setAtIndex(ADDRESS, 2, secondCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDAndThreeBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        first: Boolean,
        second: Boolean,
        third: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val firstCell = arena.allocate(JAVA_BYTE)
            val secondCell = arena.allocate(JAVA_BYTE)
            val thirdCell = arena.allocate(JAVA_BYTE)
            ridCell.set(JAVA_LONG, 0, rid.value)
            firstCell.set(JAVA_BYTE, 0, if (first) 1.toByte() else 0.toByte())
            secondCell.set(JAVA_BYTE, 0, if (second) 1.toByte() else 0.toByte())
            thirdCell.set(JAVA_BYTE, 0, if (third) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 4)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, firstCell)
            args.setAtIndex(ADDRESS, 2, secondCell)
            args.setAtIndex(ADDRESS, 3, thirdCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithTwoRIDIntRIDIntArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstRid: RID,
        secondRid: RID,
        firstIndex: Int,
        thirdRid: RID,
        secondIndex: Int,
    ) {
        Arena.ofConfined().use { arena ->
            val firstRidCell = arena.allocate(JAVA_LONG)
            val secondRidCell = arena.allocate(JAVA_LONG)
            val firstIndexCell = arena.allocate(JAVA_INT)
            val thirdRidCell = arena.allocate(JAVA_LONG)
            val secondIndexCell = arena.allocate(JAVA_INT)
            firstRidCell.set(JAVA_LONG, 0, firstRid.value)
            secondRidCell.set(JAVA_LONG, 0, secondRid.value)
            firstIndexCell.set(JAVA_INT, 0, firstIndex)
            thirdRidCell.set(JAVA_LONG, 0, thirdRid.value)
            secondIndexCell.set(JAVA_INT, 0, secondIndex)
            val args = arena.allocate(ADDRESS, 5)
            args.setAtIndex(ADDRESS, 0, firstRidCell)
            args.setAtIndex(ADDRESS, 1, secondRidCell)
            args.setAtIndex(ADDRESS, 2, firstIndexCell)
            args.setAtIndex(ADDRESS, 3, thirdRidCell)
            args.setAtIndex(ADDRESS, 4, secondIndexCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithStringRect2LongThreeIntArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        rect: Rect2,
        type: Long,
        first: Int,
        second: Int,
        third: Int,
    ) {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val rectCell = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
            val typeCell = arena.allocate(JAVA_LONG)
            val firstCell = arena.allocate(JAVA_INT)
            val secondCell = arena.allocate(JAVA_INT)
            val thirdCell = arena.allocate(JAVA_INT)
            try {
                GodotStrings.initString(stringCell, text)
                writeRect2(rectCell, rect)
                typeCell.set(JAVA_LONG, 0, type)
                firstCell.set(JAVA_INT, 0, first)
                secondCell.set(JAVA_INT, 0, second)
                thirdCell.set(JAVA_INT, 0, third)
                val args = arena.allocate(ADDRESS, 6)
                args.setAtIndex(ADDRESS, 0, stringCell)
                args.setAtIndex(ADDRESS, 1, rectCell)
                args.setAtIndex(ADDRESS, 2, typeCell)
                args.setAtIndex(ADDRESS, 3, firstCell)
                args.setAtIndex(ADDRESS, 4, secondCell)
                args.setAtIndex(ADDRESS, 5, thirdCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithObjectAndThreeVector2iArgsRetPackedVector2List(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        first: Vector2i,
        second: Vector2i,
        third: Vector2i,
    ): List<Vector2> {
        Arena.ofConfined().use { arena ->
            val objectCell = arena.allocate(ADDRESS)
            val firstCell = arena.allocate(JAVA_INT, 2)
            val secondCell = arena.allocate(JAVA_INT, 2)
            val thirdCell = arena.allocate(JAVA_INT, 2)
            objectCell.set(ADDRESS, 0, objectArg)
            writeVector2i(firstCell, first)
            writeVector2i(secondCell, second)
            writeVector2i(thirdCell, third)
            val args = arena.allocate(ADDRESS, 4)
            args.setAtIndex(ADDRESS, 0, objectCell)
            args.setAtIndex(ADDRESS, 1, firstCell)
            args.setAtIndex(ADDRESS, 2, secondCell)
            args.setAtIndex(ADDRESS, 3, thirdCell)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return try {
                BuiltinTypes.readPackedVector2Array(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR2_ARRAY, ret)
            }
        }
    }

    fun ptrcallWithFourDoubleArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Double,
        second: Double,
        third: Double,
        fourth: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(JAVA_DOUBLE)
            val secondCell = arena.allocate(JAVA_DOUBLE)
            val thirdCell = arena.allocate(JAVA_DOUBLE)
            val fourthCell = arena.allocate(JAVA_DOUBLE)
            firstCell.set(JAVA_DOUBLE, 0, first)
            secondCell.set(JAVA_DOUBLE, 0, second)
            thirdCell.set(JAVA_DOUBLE, 0, third)
            fourthCell.set(JAVA_DOUBLE, 0, fourth)
            val args = arena.allocate(ADDRESS, 4)
            args.setAtIndex(ADDRESS, 0, firstCell)
            args.setAtIndex(ADDRESS, 1, secondCell)
            args.setAtIndex(ADDRESS, 2, thirdCell)
            args.setAtIndex(ADDRESS, 3, fourthCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithObjectVector2AndColorArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        vector: Vector2,
        color: Color,
    ) {
        val scratch = ptrcallScratch.get()
        scratch.objectCell.set(ADDRESS, 0, objectArg)
        writeVector2(scratch.vector2Cell, vector)
        scratch.colorCell.set(JAVA_FLOAT, 0, color.r)
        scratch.colorCell.set(JAVA_FLOAT, 4, color.g)
        scratch.colorCell.set(JAVA_FLOAT, 8, color.b)
        scratch.colorCell.set(JAVA_FLOAT, 12, color.a)
        scratch.args3.setAtIndex(ADDRESS, 0, scratch.objectCell)
        scratch.args3.setAtIndex(ADDRESS, 1, scratch.vector2Cell)
        scratch.args3.setAtIndex(ADDRESS, 2, scratch.colorCell)
        objectMethodBindPtrcall.invoke(methodBind, instance, scratch.args3, MemorySegment.NULL)
    }

    /**
     * Calls [methodBind] with (Object*, scalar float, Color) and no return value.
     */
    fun ptrcallWithObjectDoubleColorArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        value: Double,
        color: Color,
    ) {
        Arena.ofConfined().use { arena ->
            val objectCell = arena.allocate(ADDRESS)
            val valueCell = arena.allocate(JAVA_DOUBLE)
            val colorCell = arena.allocate(16L, 4L)
            objectCell.set(ADDRESS, 0, objectArg)
            valueCell.set(JAVA_DOUBLE, 0, value)
            colorCell.set(JAVA_FLOAT, 0, color.r)
            colorCell.set(JAVA_FLOAT, 4, color.g)
            colorCell.set(JAVA_FLOAT, 8, color.b)
            colorCell.set(JAVA_FLOAT, 12, color.a)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, objectCell)
            args.setAtIndex(ADDRESS, 1, valueCell)
            args.setAtIndex(ADDRESS, 2, colorCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithObjectColorTwoBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        color: Color,
        firstBool: Boolean,
        secondBool: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val objectCell = arena.allocate(ADDRESS)
            val colorCell = arena.allocate(16L, 4L)
            val bool0 = arena.allocate(JAVA_BYTE)
            val bool1 = arena.allocate(JAVA_BYTE)
            objectCell.set(ADDRESS, 0, objectArg)
            colorCell.set(JAVA_FLOAT, 0, color.r)
            colorCell.set(JAVA_FLOAT, 4, color.g)
            colorCell.set(JAVA_FLOAT, 8, color.b)
            colorCell.set(JAVA_FLOAT, 12, color.a)
            bool0.set(JAVA_BYTE, 0, if (firstBool) 1.toByte() else 0.toByte())
            bool1.set(JAVA_BYTE, 0, if (secondBool) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 4)
            args.setAtIndex(ADDRESS, 0, objectCell)
            args.setAtIndex(ADDRESS, 1, colorCell)
            args.setAtIndex(ADDRESS, 2, bool0)
            args.setAtIndex(ADDRESS, 3, bool1)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithObjectColorLongBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        color: Color,
        longValue: Long,
        boolValue: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val objectCell = arena.allocate(ADDRESS)
            val colorCell = arena.allocate(16L, 4L)
            val longCell = arena.allocate(JAVA_LONG)
            val boolCell = arena.allocate(JAVA_BYTE)
            objectCell.set(ADDRESS, 0, objectArg)
            colorCell.set(JAVA_FLOAT, 0, color.r)
            colorCell.set(JAVA_FLOAT, 4, color.g)
            colorCell.set(JAVA_FLOAT, 8, color.b)
            colorCell.set(JAVA_FLOAT, 12, color.a)
            longCell.set(JAVA_LONG, 0, longValue)
            boolCell.set(JAVA_BYTE, 0, if (boolValue) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 4)
            args.setAtIndex(ADDRESS, 0, objectCell)
            args.setAtIndex(ADDRESS, 1, colorCell)
            args.setAtIndex(ADDRESS, 2, longCell)
            args.setAtIndex(ADDRESS, 3, boolCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithObjectVector2StringIntColorDoubleArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        position: Vector2,
        text: String,
        index: Int,
        color: Color,
        size: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val objectCell = arena.allocate(ADDRESS)
            val vectorCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val stringCell = arena.allocate(8L, 8L)
            val indexCell = arena.allocate(JAVA_INT)
            val colorCell = arena.allocate(16L, 4L)
            val sizeCell = arena.allocate(JAVA_DOUBLE)
            try {
                objectCell.set(ADDRESS, 0, objectArg)
                writeVector2(vectorCell, position)
                GodotStrings.initString(stringCell, text)
                indexCell.set(JAVA_INT, 0, index)
                colorCell.set(JAVA_FLOAT, 0, color.r)
                colorCell.set(JAVA_FLOAT, 4, color.g)
                colorCell.set(JAVA_FLOAT, 8, color.b)
                colorCell.set(JAVA_FLOAT, 12, color.a)
                sizeCell.set(JAVA_DOUBLE, 0, size)
                val args = arena.allocate(ADDRESS, 6)
                args.setAtIndex(ADDRESS, 0, objectCell)
                args.setAtIndex(ADDRESS, 1, vectorCell)
                args.setAtIndex(ADDRESS, 2, stringCell)
                args.setAtIndex(ADDRESS, 3, indexCell)
                args.setAtIndex(ADDRESS, 4, colorCell)
                args.setAtIndex(ADDRESS, 5, sizeCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithObjectVector2StringTwoIntColorDoubleArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        position: Vector2,
        text: String,
        firstIndex: Int,
        secondIndex: Int,
        color: Color,
        size: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val objectCell = arena.allocate(ADDRESS)
            val vectorCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val stringCell = arena.allocate(8L, 8L)
            val int0 = arena.allocate(JAVA_INT)
            val int1 = arena.allocate(JAVA_INT)
            val colorCell = arena.allocate(16L, 4L)
            val sizeCell = arena.allocate(JAVA_DOUBLE)
            try {
                objectCell.set(ADDRESS, 0, objectArg)
                writeVector2(vectorCell, position)
                GodotStrings.initString(stringCell, text)
                int0.set(JAVA_INT, 0, firstIndex)
                int1.set(JAVA_INT, 0, secondIndex)
                colorCell.set(JAVA_FLOAT, 0, color.r)
                colorCell.set(JAVA_FLOAT, 4, color.g)
                colorCell.set(JAVA_FLOAT, 8, color.b)
                colorCell.set(JAVA_FLOAT, 12, color.a)
                sizeCell.set(JAVA_DOUBLE, 0, size)
                val args = arena.allocate(ADDRESS, 7)
                args.setAtIndex(ADDRESS, 0, objectCell)
                args.setAtIndex(ADDRESS, 1, vectorCell)
                args.setAtIndex(ADDRESS, 2, stringCell)
                args.setAtIndex(ADDRESS, 3, int0)
                args.setAtIndex(ADDRESS, 4, int1)
                args.setAtIndex(ADDRESS, 5, colorCell)
                args.setAtIndex(ADDRESS, 6, sizeCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithObjectVector2StringLongDoubleIntColorThreeLongDoubleArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        position: Vector2,
        text: String,
        alignment: Long,
        width: Double,
        fontSize: Int,
        color: Color,
        justification: Long,
        direction: Long,
        orientation: Long,
        oversampling: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val objectCell = arena.allocate(ADDRESS)
            val vectorCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val stringCell = arena.allocate(8L, 8L)
            val alignmentCell = arena.allocate(JAVA_LONG)
            val widthCell = arena.allocate(JAVA_DOUBLE)
            val fontSizeCell = arena.allocate(JAVA_INT)
            val colorCell = arena.allocate(16L, 4L)
            val justificationCell = arena.allocate(JAVA_LONG)
            val directionCell = arena.allocate(JAVA_LONG)
            val orientationCell = arena.allocate(JAVA_LONG)
            val oversamplingCell = arena.allocate(JAVA_DOUBLE)
            try {
                objectCell.set(ADDRESS, 0, objectArg)
                writeVector2(vectorCell, position)
                GodotStrings.initString(stringCell, text)
                alignmentCell.set(JAVA_LONG, 0, alignment)
                widthCell.set(JAVA_DOUBLE, 0, width)
                fontSizeCell.set(JAVA_INT, 0, fontSize)
                colorCell.set(JAVA_FLOAT, 0, color.r)
                colorCell.set(JAVA_FLOAT, 4, color.g)
                colorCell.set(JAVA_FLOAT, 8, color.b)
                colorCell.set(JAVA_FLOAT, 12, color.a)
                justificationCell.set(JAVA_LONG, 0, justification)
                directionCell.set(JAVA_LONG, 0, direction)
                orientationCell.set(JAVA_LONG, 0, orientation)
                oversamplingCell.set(JAVA_DOUBLE, 0, oversampling)
                val args = arena.allocate(ADDRESS, 11)
                args.setAtIndex(ADDRESS, 0, objectCell)
                args.setAtIndex(ADDRESS, 1, vectorCell)
                args.setAtIndex(ADDRESS, 2, stringCell)
                args.setAtIndex(ADDRESS, 3, alignmentCell)
                args.setAtIndex(ADDRESS, 4, widthCell)
                args.setAtIndex(ADDRESS, 5, fontSizeCell)
                args.setAtIndex(ADDRESS, 6, colorCell)
                args.setAtIndex(ADDRESS, 7, justificationCell)
                args.setAtIndex(ADDRESS, 8, directionCell)
                args.setAtIndex(ADDRESS, 9, orientationCell)
                args.setAtIndex(ADDRESS, 10, oversamplingCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithObjectVector2StringLongDoubleTwoIntColorThreeLongDoubleArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        position: Vector2,
        text: String,
        alignment: Long,
        width: Double,
        fontSize: Int,
        outlineSize: Int,
        color: Color,
        justification: Long,
        direction: Long,
        orientation: Long,
        oversampling: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val objectCell = arena.allocate(ADDRESS)
            val vectorCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val stringCell = arena.allocate(8L, 8L)
            val alignmentCell = arena.allocate(JAVA_LONG)
            val widthCell = arena.allocate(JAVA_DOUBLE)
            val fontSizeCell = arena.allocate(JAVA_INT)
            val outlineSizeCell = arena.allocate(JAVA_INT)
            val colorCell = arena.allocate(16L, 4L)
            val justificationCell = arena.allocate(JAVA_LONG)
            val directionCell = arena.allocate(JAVA_LONG)
            val orientationCell = arena.allocate(JAVA_LONG)
            val oversamplingCell = arena.allocate(JAVA_DOUBLE)
            try {
                objectCell.set(ADDRESS, 0, objectArg)
                writeVector2(vectorCell, position)
                GodotStrings.initString(stringCell, text)
                alignmentCell.set(JAVA_LONG, 0, alignment)
                widthCell.set(JAVA_DOUBLE, 0, width)
                fontSizeCell.set(JAVA_INT, 0, fontSize)
                outlineSizeCell.set(JAVA_INT, 0, outlineSize)
                colorCell.set(JAVA_FLOAT, 0, color.r)
                colorCell.set(JAVA_FLOAT, 4, color.g)
                colorCell.set(JAVA_FLOAT, 8, color.b)
                colorCell.set(JAVA_FLOAT, 12, color.a)
                justificationCell.set(JAVA_LONG, 0, justification)
                directionCell.set(JAVA_LONG, 0, direction)
                orientationCell.set(JAVA_LONG, 0, orientation)
                oversamplingCell.set(JAVA_DOUBLE, 0, oversampling)
                val args = arena.allocate(ADDRESS, 12)
                args.setAtIndex(ADDRESS, 0, objectCell)
                args.setAtIndex(ADDRESS, 1, vectorCell)
                args.setAtIndex(ADDRESS, 2, stringCell)
                args.setAtIndex(ADDRESS, 3, alignmentCell)
                args.setAtIndex(ADDRESS, 4, widthCell)
                args.setAtIndex(ADDRESS, 5, fontSizeCell)
                args.setAtIndex(ADDRESS, 6, outlineSizeCell)
                args.setAtIndex(ADDRESS, 7, colorCell)
                args.setAtIndex(ADDRESS, 8, justificationCell)
                args.setAtIndex(ADDRESS, 9, directionCell)
                args.setAtIndex(ADDRESS, 10, orientationCell)
                args.setAtIndex(ADDRESS, 11, oversamplingCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithObjectVector2StringLongDoubleTwoIntColorFourLongDoubleArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        position: Vector2,
        text: String,
        alignment: Long,
        width: Double,
        fontSize: Int,
        maxLines: Int,
        color: Color,
        breakFlags: Long,
        justification: Long,
        direction: Long,
        orientation: Long,
        oversampling: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val objectCell = arena.allocate(ADDRESS)
            val vectorCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val stringCell = arena.allocate(8L, 8L)
            val alignmentCell = arena.allocate(JAVA_LONG)
            val widthCell = arena.allocate(JAVA_DOUBLE)
            val fontSizeCell = arena.allocate(JAVA_INT)
            val maxLinesCell = arena.allocate(JAVA_INT)
            val colorCell = arena.allocate(16L, 4L)
            val breakFlagsCell = arena.allocate(JAVA_LONG)
            val justificationCell = arena.allocate(JAVA_LONG)
            val directionCell = arena.allocate(JAVA_LONG)
            val orientationCell = arena.allocate(JAVA_LONG)
            val oversamplingCell = arena.allocate(JAVA_DOUBLE)
            try {
                objectCell.set(ADDRESS, 0, objectArg)
                writeVector2(vectorCell, position)
                GodotStrings.initString(stringCell, text)
                alignmentCell.set(JAVA_LONG, 0, alignment)
                widthCell.set(JAVA_DOUBLE, 0, width)
                fontSizeCell.set(JAVA_INT, 0, fontSize)
                maxLinesCell.set(JAVA_INT, 0, maxLines)
                colorCell.set(JAVA_FLOAT, 0, color.r)
                colorCell.set(JAVA_FLOAT, 4, color.g)
                colorCell.set(JAVA_FLOAT, 8, color.b)
                colorCell.set(JAVA_FLOAT, 12, color.a)
                breakFlagsCell.set(JAVA_LONG, 0, breakFlags)
                justificationCell.set(JAVA_LONG, 0, justification)
                directionCell.set(JAVA_LONG, 0, direction)
                orientationCell.set(JAVA_LONG, 0, orientation)
                oversamplingCell.set(JAVA_DOUBLE, 0, oversampling)
                val args = arena.allocate(ADDRESS, 13)
                args.setAtIndex(ADDRESS, 0, objectCell)
                args.setAtIndex(ADDRESS, 1, vectorCell)
                args.setAtIndex(ADDRESS, 2, stringCell)
                args.setAtIndex(ADDRESS, 3, alignmentCell)
                args.setAtIndex(ADDRESS, 4, widthCell)
                args.setAtIndex(ADDRESS, 5, fontSizeCell)
                args.setAtIndex(ADDRESS, 6, maxLinesCell)
                args.setAtIndex(ADDRESS, 7, colorCell)
                args.setAtIndex(ADDRESS, 8, breakFlagsCell)
                args.setAtIndex(ADDRESS, 9, justificationCell)
                args.setAtIndex(ADDRESS, 10, directionCell)
                args.setAtIndex(ADDRESS, 11, orientationCell)
                args.setAtIndex(ADDRESS, 12, oversamplingCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithObjectVector2StringLongDoubleThreeIntColorFourLongDoubleArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        position: Vector2,
        text: String,
        alignment: Long,
        width: Double,
        fontSize: Int,
        maxLines: Int,
        outlineSize: Int,
        color: Color,
        breakFlags: Long,
        justification: Long,
        direction: Long,
        orientation: Long,
        oversampling: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val objectCell = arena.allocate(ADDRESS)
            val vectorCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val stringCell = arena.allocate(8L, 8L)
            val alignmentCell = arena.allocate(JAVA_LONG)
            val widthCell = arena.allocate(JAVA_DOUBLE)
            val fontSizeCell = arena.allocate(JAVA_INT)
            val maxLinesCell = arena.allocate(JAVA_INT)
            val outlineSizeCell = arena.allocate(JAVA_INT)
            val colorCell = arena.allocate(16L, 4L)
            val breakFlagsCell = arena.allocate(JAVA_LONG)
            val justificationCell = arena.allocate(JAVA_LONG)
            val directionCell = arena.allocate(JAVA_LONG)
            val orientationCell = arena.allocate(JAVA_LONG)
            val oversamplingCell = arena.allocate(JAVA_DOUBLE)
            try {
                objectCell.set(ADDRESS, 0, objectArg)
                writeVector2(vectorCell, position)
                GodotStrings.initString(stringCell, text)
                alignmentCell.set(JAVA_LONG, 0, alignment)
                widthCell.set(JAVA_DOUBLE, 0, width)
                fontSizeCell.set(JAVA_INT, 0, fontSize)
                maxLinesCell.set(JAVA_INT, 0, maxLines)
                outlineSizeCell.set(JAVA_INT, 0, outlineSize)
                colorCell.set(JAVA_FLOAT, 0, color.r)
                colorCell.set(JAVA_FLOAT, 4, color.g)
                colorCell.set(JAVA_FLOAT, 8, color.b)
                colorCell.set(JAVA_FLOAT, 12, color.a)
                breakFlagsCell.set(JAVA_LONG, 0, breakFlags)
                justificationCell.set(JAVA_LONG, 0, justification)
                directionCell.set(JAVA_LONG, 0, direction)
                orientationCell.set(JAVA_LONG, 0, orientation)
                oversamplingCell.set(JAVA_DOUBLE, 0, oversampling)
                val args = arena.allocate(ADDRESS, 14)
                args.setAtIndex(ADDRESS, 0, objectCell)
                args.setAtIndex(ADDRESS, 1, vectorCell)
                args.setAtIndex(ADDRESS, 2, stringCell)
                args.setAtIndex(ADDRESS, 3, alignmentCell)
                args.setAtIndex(ADDRESS, 4, widthCell)
                args.setAtIndex(ADDRESS, 5, fontSizeCell)
                args.setAtIndex(ADDRESS, 6, maxLinesCell)
                args.setAtIndex(ADDRESS, 7, outlineSizeCell)
                args.setAtIndex(ADDRESS, 8, colorCell)
                args.setAtIndex(ADDRESS, 9, breakFlagsCell)
                args.setAtIndex(ADDRESS, 10, justificationCell)
                args.setAtIndex(ADDRESS, 11, directionCell)
                args.setAtIndex(ADDRESS, 12, orientationCell)
                args.setAtIndex(ADDRESS, 13, oversamplingCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithObjectTwoRect2AndColorArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        first: Rect2,
        second: Rect2,
        color: Color,
    ) {
        Arena.ofConfined().use { arena ->
            val objectCell = arena.allocate(ADDRESS)
            val firstCell = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
            val secondCell = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
            val colorCell = arena.allocate(16L, 4L)
            objectCell.set(ADDRESS, 0, objectArg)
            writeRect2(firstCell, first)
            writeRect2(secondCell, second)
            colorCell.set(JAVA_FLOAT, 0, color.r)
            colorCell.set(JAVA_FLOAT, 4, color.g)
            colorCell.set(JAVA_FLOAT, 8, color.b)
            colorCell.set(JAVA_FLOAT, 12, color.a)
            val args = arena.allocate(ADDRESS, 4)
            args.setAtIndex(ADDRESS, 0, objectCell)
            args.setAtIndex(ADDRESS, 1, firstCell)
            args.setAtIndex(ADDRESS, 2, secondCell)
            args.setAtIndex(ADDRESS, 3, colorCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithTwoObjectTransform2DColorArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstObject: MemorySegment,
        secondObject: MemorySegment,
        transform: Transform2D,
        color: Color,
    ) {
        Arena.ofConfined().use { arena ->
            val firstObjectCell = arena.allocate(ADDRESS)
            val secondObjectCell = arena.allocate(ADDRESS)
            val transformCell = arena.allocate(GodotReal.SIZE_BYTES * 6, GodotReal.ALIGN_BYTES)
            val colorCell = arena.allocate(16L, 4L)
            firstObjectCell.set(ADDRESS, 0, firstObject)
            secondObjectCell.set(ADDRESS, 0, secondObject)
            writeTransform2D(transformCell, transform)
            colorCell.set(JAVA_FLOAT, 0, color.r)
            colorCell.set(JAVA_FLOAT, 4, color.g)
            colorCell.set(JAVA_FLOAT, 8, color.b)
            colorCell.set(JAVA_FLOAT, 12, color.a)
            val args = arena.allocate(ADDRESS, 4)
            args.setAtIndex(ADDRESS, 0, firstObjectCell)
            args.setAtIndex(ADDRESS, 1, secondObjectCell)
            args.setAtIndex(ADDRESS, 2, transformCell)
            args.setAtIndex(ADDRESS, 3, colorCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithTwoObjectTransform3DObjectArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstObject: MemorySegment,
        secondObject: MemorySegment,
        transform: Transform3D,
        thirdObject: MemorySegment,
    ) {
        Arena.ofConfined().use { arena ->
            val firstObjectCell = arena.allocate(ADDRESS)
            val secondObjectCell = arena.allocate(ADDRESS)
            val transformCell = arena.allocate(GodotReal.SIZE_BYTES * 12, GodotReal.ALIGN_BYTES)
            val thirdObjectCell = arena.allocate(ADDRESS)
            firstObjectCell.set(ADDRESS, 0, firstObject)
            secondObjectCell.set(ADDRESS, 0, secondObject)
            writeTransform3D(transformCell, transform)
            thirdObjectCell.set(ADDRESS, 0, thirdObject)
            val args = arena.allocate(ADDRESS, 4)
            args.setAtIndex(ADDRESS, 0, firstObjectCell)
            args.setAtIndex(ADDRESS, 1, secondObjectCell)
            args.setAtIndex(ADDRESS, 2, transformCell)
            args.setAtIndex(ADDRESS, 3, thirdObjectCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithTwoVector2ColorDoubleBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Vector2,
        second: Vector2,
        color: Color,
        width: Double,
        antialiased: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val secondCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val colorCell = arena.allocate(16L, 4L)
            val widthCell = arena.allocate(JAVA_DOUBLE)
            val antialiasedCell = arena.allocate(JAVA_BYTE)
            writeVector2(firstCell, first)
            writeVector2(secondCell, second)
            colorCell.set(JAVA_FLOAT, 0, color.r)
            colorCell.set(JAVA_FLOAT, 4, color.g)
            colorCell.set(JAVA_FLOAT, 8, color.b)
            colorCell.set(JAVA_FLOAT, 12, color.a)
            widthCell.set(JAVA_DOUBLE, 0, width)
            antialiasedCell.set(JAVA_BYTE, 0, if (antialiased) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 5)
            args.setAtIndex(ADDRESS, 0, firstCell)
            args.setAtIndex(ADDRESS, 1, secondCell)
            args.setAtIndex(ADDRESS, 2, colorCell)
            args.setAtIndex(ADDRESS, 3, widthCell)
            args.setAtIndex(ADDRESS, 4, antialiasedCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRect2ColorBoolDoubleBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rect: Rect2,
        color: Color,
        filled: Boolean,
        width: Double,
        antialiased: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val rectCell = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
            val colorCell = arena.allocate(16L, 4L)
            val filledCell = arena.allocate(JAVA_BYTE)
            val widthCell = arena.allocate(JAVA_DOUBLE)
            val antialiasedCell = arena.allocate(JAVA_BYTE)
            writeRect2(rectCell, rect)
            colorCell.set(JAVA_FLOAT, 0, color.r)
            colorCell.set(JAVA_FLOAT, 4, color.g)
            colorCell.set(JAVA_FLOAT, 8, color.b)
            colorCell.set(JAVA_FLOAT, 12, color.a)
            filledCell.set(JAVA_BYTE, 0, if (filled) 1.toByte() else 0.toByte())
            widthCell.set(JAVA_DOUBLE, 0, width)
            antialiasedCell.set(JAVA_BYTE, 0, if (antialiased) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 5)
            args.setAtIndex(ADDRESS, 0, rectCell)
            args.setAtIndex(ADDRESS, 1, colorCell)
            args.setAtIndex(ADDRESS, 2, filledCell)
            args.setAtIndex(ADDRESS, 3, widthCell)
            args.setAtIndex(ADDRESS, 4, antialiasedCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithVector2DoubleColorBoolDoubleBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        position: Vector2,
        radius: Double,
        color: Color,
        filled: Boolean,
        width: Double,
        antialiased: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val positionCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val radiusCell = arena.allocate(JAVA_DOUBLE)
            val colorCell = arena.allocate(16L, 4L)
            val filledCell = arena.allocate(JAVA_BYTE)
            val widthCell = arena.allocate(JAVA_DOUBLE)
            val antialiasedCell = arena.allocate(JAVA_BYTE)
            writeVector2(positionCell, position)
            radiusCell.set(JAVA_DOUBLE, 0, radius)
            colorCell.set(JAVA_FLOAT, 0, color.r)
            colorCell.set(JAVA_FLOAT, 4, color.g)
            colorCell.set(JAVA_FLOAT, 8, color.b)
            colorCell.set(JAVA_FLOAT, 12, color.a)
            filledCell.set(JAVA_BYTE, 0, if (filled) 1.toByte() else 0.toByte())
            widthCell.set(JAVA_DOUBLE, 0, width)
            antialiasedCell.set(JAVA_BYTE, 0, if (antialiased) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 6)
            args.setAtIndex(ADDRESS, 0, positionCell)
            args.setAtIndex(ADDRESS, 1, radiusCell)
            args.setAtIndex(ADDRESS, 2, colorCell)
            args.setAtIndex(ADDRESS, 3, filledCell)
            args.setAtIndex(ADDRESS, 4, widthCell)
            args.setAtIndex(ADDRESS, 5, antialiasedCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithVector2TwoDoubleColorBoolDoubleBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        position: Vector2,
        width: Double,
        height: Double,
        color: Color,
        filled: Boolean,
        lineWidth: Double,
        antialiased: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val positionCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val widthCell = arena.allocate(JAVA_DOUBLE)
            val heightCell = arena.allocate(JAVA_DOUBLE)
            val colorCell = arena.allocate(16L, 4L)
            val filledCell = arena.allocate(JAVA_BYTE)
            val lineWidthCell = arena.allocate(JAVA_DOUBLE)
            val antialiasedCell = arena.allocate(JAVA_BYTE)
            writeVector2(positionCell, position)
            widthCell.set(JAVA_DOUBLE, 0, width)
            heightCell.set(JAVA_DOUBLE, 0, height)
            colorCell.set(JAVA_FLOAT, 0, color.r)
            colorCell.set(JAVA_FLOAT, 4, color.g)
            colorCell.set(JAVA_FLOAT, 8, color.b)
            colorCell.set(JAVA_FLOAT, 12, color.a)
            filledCell.set(JAVA_BYTE, 0, if (filled) 1.toByte() else 0.toByte())
            lineWidthCell.set(JAVA_DOUBLE, 0, lineWidth)
            antialiasedCell.set(JAVA_BYTE, 0, if (antialiased) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 7)
            args.setAtIndex(ADDRESS, 0, positionCell)
            args.setAtIndex(ADDRESS, 1, widthCell)
            args.setAtIndex(ADDRESS, 2, heightCell)
            args.setAtIndex(ADDRESS, 3, colorCell)
            args.setAtIndex(ADDRESS, 4, filledCell)
            args.setAtIndex(ADDRESS, 5, lineWidthCell)
            args.setAtIndex(ADDRESS, 6, antialiasedCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithObjectRect2BoolColorBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        rect: Rect2,
        tile: Boolean,
        color: Color,
        transpose: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val objectCell = arena.allocate(ADDRESS)
            val rectCell = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
            val tileCell = arena.allocate(JAVA_BYTE)
            val colorCell = arena.allocate(16L, 4L)
            val transposeCell = arena.allocate(JAVA_BYTE)
            objectCell.set(ADDRESS, 0, objectArg)
            writeRect2(rectCell, rect)
            tileCell.set(JAVA_BYTE, 0, if (tile) 1.toByte() else 0.toByte())
            colorCell.set(JAVA_FLOAT, 0, color.r)
            colorCell.set(JAVA_FLOAT, 4, color.g)
            colorCell.set(JAVA_FLOAT, 8, color.b)
            colorCell.set(JAVA_FLOAT, 12, color.a)
            transposeCell.set(JAVA_BYTE, 0, if (transpose) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 5)
            args.setAtIndex(ADDRESS, 0, objectCell)
            args.setAtIndex(ADDRESS, 1, rectCell)
            args.setAtIndex(ADDRESS, 2, tileCell)
            args.setAtIndex(ADDRESS, 3, colorCell)
            args.setAtIndex(ADDRESS, 4, transposeCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithObjectTwoRect2ColorTwoBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        first: Rect2,
        second: Rect2,
        color: Color,
        tile: Boolean,
        transpose: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val objectCell = arena.allocate(ADDRESS)
            val firstCell = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
            val secondCell = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
            val colorCell = arena.allocate(16L, 4L)
            val tileCell = arena.allocate(JAVA_BYTE)
            val transposeCell = arena.allocate(JAVA_BYTE)
            objectCell.set(ADDRESS, 0, objectArg)
            writeRect2(firstCell, first)
            writeRect2(secondCell, second)
            colorCell.set(JAVA_FLOAT, 0, color.r)
            colorCell.set(JAVA_FLOAT, 4, color.g)
            colorCell.set(JAVA_FLOAT, 8, color.b)
            colorCell.set(JAVA_FLOAT, 12, color.a)
            tileCell.set(JAVA_BYTE, 0, if (tile) 1.toByte() else 0.toByte())
            transposeCell.set(JAVA_BYTE, 0, if (transpose) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 6)
            args.setAtIndex(ADDRESS, 0, objectCell)
            args.setAtIndex(ADDRESS, 1, firstCell)
            args.setAtIndex(ADDRESS, 2, secondCell)
            args.setAtIndex(ADDRESS, 3, colorCell)
            args.setAtIndex(ADDRESS, 4, tileCell)
            args.setAtIndex(ADDRESS, 5, transposeCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithObjectTwoRect2ColorThreeDoubleArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        first: Rect2,
        second: Rect2,
        color: Color,
        firstDouble: Double,
        secondDouble: Double,
        thirdDouble: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val objectCell = arena.allocate(ADDRESS)
            val firstCell = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
            val secondCell = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
            val colorCell = arena.allocate(16L, 4L)
            val firstDoubleCell = arena.allocate(JAVA_DOUBLE)
            val secondDoubleCell = arena.allocate(JAVA_DOUBLE)
            val thirdDoubleCell = arena.allocate(JAVA_DOUBLE)
            objectCell.set(ADDRESS, 0, objectArg)
            writeRect2(firstCell, first)
            writeRect2(secondCell, second)
            colorCell.set(JAVA_FLOAT, 0, color.r)
            colorCell.set(JAVA_FLOAT, 4, color.g)
            colorCell.set(JAVA_FLOAT, 8, color.b)
            colorCell.set(JAVA_FLOAT, 12, color.a)
            firstDoubleCell.set(JAVA_DOUBLE, 0, firstDouble)
            secondDoubleCell.set(JAVA_DOUBLE, 0, secondDouble)
            thirdDoubleCell.set(JAVA_DOUBLE, 0, thirdDouble)
            val args = arena.allocate(ADDRESS, 7)
            args.setAtIndex(ADDRESS, 0, objectCell)
            args.setAtIndex(ADDRESS, 1, firstCell)
            args.setAtIndex(ADDRESS, 2, secondCell)
            args.setAtIndex(ADDRESS, 3, colorCell)
            args.setAtIndex(ADDRESS, 4, firstDoubleCell)
            args.setAtIndex(ADDRESS, 5, secondDoubleCell)
            args.setAtIndex(ADDRESS, 6, thirdDoubleCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithBoolAndLongArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        enabled: Boolean,
        value: Long,
    ) {
        Arena.ofConfined().use { arena ->
            val boolCell = arena.allocate(JAVA_BYTE)
            val valueCell = arena.allocate(JAVA_LONG)
            boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
            valueCell.set(JAVA_LONG, 0, value)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, boolCell)
            args.setAtIndex(ADDRESS, 1, valueCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithStringBoolDoubleArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        enabled: Boolean,
        value: Double,
    ): Long {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val boolCell = arena.allocate(JAVA_BYTE)
            val valueCell = arena.allocate(JAVA_DOUBLE)
            try {
                GodotStrings.initString(stringCell, text)
                boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
                valueCell.set(JAVA_DOUBLE, 0, value)
                val args = arena.allocate(ADDRESS, 3)
                args.setAtIndex(ADDRESS, 0, stringCell)
                args.setAtIndex(ADDRESS, 1, boolCell)
                args.setAtIndex(ADDRESS, 2, valueCell)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithVector2iAndColorArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        vector: Vector2i,
        color: Color,
    ) {
        Arena.ofConfined().use { arena ->
            val vectorCell = arena.allocate(JAVA_INT, 2)
            val colorCell = arena.allocate(16L, 4L)
            writeVector2i(vectorCell, vector)
            // Color components are fixed 32-bit floats, unlike scalar float ptrcall slots.
            colorCell.set(JAVA_FLOAT, 0, color.r)
            colorCell.set(JAVA_FLOAT, 4, color.g)
            colorCell.set(JAVA_FLOAT, 8, color.b)
            colorCell.set(JAVA_FLOAT, 12, color.a)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, vectorCell)
            args.setAtIndex(ADDRESS, 1, colorCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithVector2AndIntArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        vector: Vector2,
        value: Int,
    ) {
        Arena.ofConfined().use { arena ->
            val vectorCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val valueCell = arena.allocate(JAVA_INT)
            writeVector2(vectorCell, vector)
            valueCell.set(JAVA_INT, 0, value)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, vectorCell)
            args.setAtIndex(ADDRESS, 1, valueCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithStringNameObjectDoubleIntArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
        objectArg: MemorySegment,
        duration: Double,
        index: Int,
    ) {
        Arena.ofConfined().use { arena ->
            val objectCell = arena.allocate(ADDRESS)
            val durationCell = arena.allocate(JAVA_DOUBLE)
            val indexCell = arena.allocate(JAVA_INT)
            objectCell.set(ADDRESS, 0, objectArg)
            durationCell.set(JAVA_DOUBLE, 0, duration)
            indexCell.set(JAVA_INT, 0, index)
            val args = arena.allocate(ADDRESS, 4)
            args.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(name))
            args.setAtIndex(ADDRESS, 1, objectCell)
            args.setAtIndex(ADDRESS, 2, durationCell)
            args.setAtIndex(ADDRESS, 3, indexCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithStringNameIntObjectDoubleArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
        index: Int,
        objectArg: MemorySegment,
        duration: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val indexCell = arena.allocate(JAVA_INT)
            val objectCell = arena.allocate(ADDRESS)
            val durationCell = arena.allocate(JAVA_DOUBLE)
            indexCell.set(JAVA_INT, 0, index)
            objectCell.set(ADDRESS, 0, objectArg)
            durationCell.set(JAVA_DOUBLE, 0, duration)
            val args = arena.allocate(ADDRESS, 4)
            args.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(name))
            args.setAtIndex(ADDRESS, 1, indexCell)
            args.setAtIndex(ADDRESS, 2, objectCell)
            args.setAtIndex(ADDRESS, 3, durationCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithStringNameAndIntArgRetDouble(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
        value: Int,
    ): Double {
        Arena.ofConfined().use { arena ->
            val valueCell = arena.allocate(JAVA_INT)
            valueCell.set(JAVA_INT, 0, value)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(name))
            args.setAtIndex(ADDRESS, 1, valueCell)
            val ret = arena.allocate(JAVA_DOUBLE)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(JAVA_DOUBLE, 0)
        }
    }

    fun ptrcallWithPackedVector2ListArgRetPackedVector2List(
        methodBind: MemorySegment,
        instance: MemorySegment,
        values: List<Vector2>,
    ): List<Vector2> {
        Arena.ofConfined().use { arena ->
            val argPacked = BuiltinTypes.allocatePackedArray(arena)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            try {
                BuiltinTypes.initPackedVector2Array(argPacked, values)
                val args = arena.allocate(ADDRESS, 1)
                args.setAtIndex(ADDRESS, 0, argPacked)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return BuiltinTypes.readPackedVector2Array(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR2_ARRAY, ret)
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR2_ARRAY, argPacked)
            }
        }
    }

    fun ptrcallWithVector2PackedVector2ListArgsRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        vector: Vector2,
        values: List<Vector2>,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val vectorCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val packedCell = BuiltinTypes.allocatePackedArray(arena)
            try {
                writeVector2(vectorCell, vector)
                BuiltinTypes.initPackedVector2Array(packedCell, values)
                val args = arena.allocate(ADDRESS, 2)
                args.setAtIndex(ADDRESS, 0, vectorCell)
                args.setAtIndex(ADDRESS, 1, packedCell)
                val ret = arena.allocate(JAVA_BYTE)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(JAVA_BYTE, 0) != 0.toByte()
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR2_ARRAY, packedCell)
            }
        }
    }

    fun ptrcallWithVector2DoubleVector2Args(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Vector2,
        value: Double,
        second: Vector2,
    ) {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val valueCell = arena.allocate(JAVA_DOUBLE)
            val secondCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            writeVector2(firstCell, first)
            valueCell.set(JAVA_DOUBLE, 0, value)
            writeVector2(secondCell, second)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, firstCell)
            args.setAtIndex(ADDRESS, 1, valueCell)
            args.setAtIndex(ADDRESS, 2, secondCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithTwoVector2DoubleArgsRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Vector2,
        second: Vector2,
        value: Double,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val secondCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val valueCell = arena.allocate(JAVA_DOUBLE)
            writeVector2(firstCell, first)
            writeVector2(secondCell, second)
            valueCell.set(JAVA_DOUBLE, 0, value)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, firstCell)
            args.setAtIndex(ADDRESS, 1, secondCell)
            args.setAtIndex(ADDRESS, 2, valueCell)
            val ret = arena.allocate(JAVA_BYTE)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(JAVA_BYTE, 0) != 0.toByte()
        }
    }

    fun ptrcallWithThreeVector2DoubleArgsRetDouble(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Vector2,
        second: Vector2,
        third: Vector2,
        value: Double,
    ): Double {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val secondCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val thirdCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val valueCell = arena.allocate(JAVA_DOUBLE)
            writeVector2(firstCell, first)
            writeVector2(secondCell, second)
            writeVector2(thirdCell, third)
            valueCell.set(JAVA_DOUBLE, 0, value)
            val args = arena.allocate(ADDRESS, 4)
            args.setAtIndex(ADDRESS, 0, firstCell)
            args.setAtIndex(ADDRESS, 1, secondCell)
            args.setAtIndex(ADDRESS, 2, thirdCell)
            args.setAtIndex(ADDRESS, 3, valueCell)
            val ret = arena.allocate(JAVA_DOUBLE)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(JAVA_DOUBLE, 0)
        }
    }

    fun ptrcallWithFourVector2ArgsRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Vector2,
        second: Vector2,
        third: Vector2,
        fourth: Vector2,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val secondCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val thirdCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val fourthCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            writeVector2(firstCell, first)
            writeVector2(secondCell, second)
            writeVector2(thirdCell, third)
            writeVector2(fourthCell, fourth)
            val args = arena.allocate(ADDRESS, 4)
            args.setAtIndex(ADDRESS, 0, firstCell)
            args.setAtIndex(ADDRESS, 1, secondCell)
            args.setAtIndex(ADDRESS, 2, thirdCell)
            args.setAtIndex(ADDRESS, 3, fourthCell)
            val ret = arena.allocate(JAVA_BYTE)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(JAVA_BYTE, 0) != 0.toByte()
        }
    }

    fun ptrcallWithFourVector2ArgsRetPackedVector2List(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Vector2,
        second: Vector2,
        third: Vector2,
        fourth: Vector2,
    ): List<Vector2> {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val secondCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val thirdCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val fourthCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            writeVector2(firstCell, first)
            writeVector2(secondCell, second)
            writeVector2(thirdCell, third)
            writeVector2(fourthCell, fourth)
            val args = arena.allocate(ADDRESS, 4)
            args.setAtIndex(ADDRESS, 0, firstCell)
            args.setAtIndex(ADDRESS, 1, secondCell)
            args.setAtIndex(ADDRESS, 2, thirdCell)
            args.setAtIndex(ADDRESS, 3, fourthCell)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return try {
                BuiltinTypes.readPackedVector2Array(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR2_ARRAY, ret)
            }
        }
    }

    fun ptrcallWithFourVector2ArgsRetVariantScalar(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Vector2,
        second: Vector2,
        third: Vector2,
        fourth: Vector2,
    ): Any? {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val secondCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val thirdCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val fourthCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            writeVector2(firstCell, first)
            writeVector2(secondCell, second)
            writeVector2(thirdCell, third)
            writeVector2(fourthCell, fourth)
            val args = arena.allocate(ADDRESS, 4)
            args.setAtIndex(ADDRESS, 0, firstCell)
            args.setAtIndex(ADDRESS, 1, secondCell)
            args.setAtIndex(ADDRESS, 2, thirdCell)
            args.setAtIndex(ADDRESS, 3, fourthCell)
            return readVariantReturn(methodBind, instance, args, arena)
        }
    }

    fun ptrcallWithPackedVector3ListArgRetPackedInt32List(
        methodBind: MemorySegment,
        instance: MemorySegment,
        values: List<Vector3>,
    ): List<Int> {
        Arena.ofConfined().use { arena ->
            val argPacked = BuiltinTypes.allocatePackedArray(arena)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            try {
                BuiltinTypes.initPackedVector3Array(argPacked, values)
                val args = arena.allocate(ADDRESS, 1)
                args.setAtIndex(ADDRESS, 0, argPacked)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return BuiltinTypes.readPackedInt32Array(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_INT32_ARRAY, ret)
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR3_ARRAY, argPacked)
            }
        }
    }

    fun ptrcallWithTwoVector3TwoDoubleArgsRetPackedVector3List(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Vector3,
        second: Vector3,
        firstDouble: Double,
        secondDouble: Double,
    ): List<Vector3> {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            val secondCell = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            val firstDoubleCell = arena.allocate(JAVA_DOUBLE)
            val secondDoubleCell = arena.allocate(JAVA_DOUBLE)
            writeVector3(firstCell, first)
            writeVector3(secondCell, second)
            firstDoubleCell.set(JAVA_DOUBLE, 0, firstDouble)
            secondDoubleCell.set(JAVA_DOUBLE, 0, secondDouble)
            val args = arena.allocate(ADDRESS, 4)
            args.setAtIndex(ADDRESS, 0, firstCell)
            args.setAtIndex(ADDRESS, 1, secondCell)
            args.setAtIndex(ADDRESS, 2, firstDoubleCell)
            args.setAtIndex(ADDRESS, 3, secondDoubleCell)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return try {
                BuiltinTypes.readPackedVector3Array(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR3_ARRAY, ret)
            }
        }
    }

    fun ptrcallWithThreeVector3DoubleArgsRetPackedVector3List(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Vector3,
        second: Vector3,
        third: Vector3,
        value: Double,
    ): List<Vector3> {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            val secondCell = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            val thirdCell = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            val valueCell = arena.allocate(JAVA_DOUBLE)
            writeVector3(firstCell, first)
            writeVector3(secondCell, second)
            writeVector3(thirdCell, third)
            valueCell.set(JAVA_DOUBLE, 0, value)
            val args = arena.allocate(ADDRESS, 4)
            args.setAtIndex(ADDRESS, 0, firstCell)
            args.setAtIndex(ADDRESS, 1, secondCell)
            args.setAtIndex(ADDRESS, 2, thirdCell)
            args.setAtIndex(ADDRESS, 3, valueCell)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return try {
                BuiltinTypes.readPackedVector3Array(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR3_ARRAY, ret)
            }
        }
    }

    fun ptrcallWithFourVector3ArgsRetVector3(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Vector3,
        second: Vector3,
        third: Vector3,
        fourth: Vector3,
    ): Vector3 {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            val secondCell = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            val thirdCell = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            val fourthCell = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            writeVector3(firstCell, first)
            writeVector3(secondCell, second)
            writeVector3(thirdCell, third)
            writeVector3(fourthCell, fourth)
            val args = arena.allocate(ADDRESS, 4)
            args.setAtIndex(ADDRESS, 0, firstCell)
            args.setAtIndex(ADDRESS, 1, secondCell)
            args.setAtIndex(ADDRESS, 2, thirdCell)
            args.setAtIndex(ADDRESS, 3, fourthCell)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return Vector3(
                x = GodotReal.readIndex(ret, 0),
                y = GodotReal.readIndex(ret, 1),
                z = GodotReal.readIndex(ret, 2),
            )
        }
    }

    fun ptrcallWithFourVector3ArgsRetPackedVector3List(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Vector3,
        second: Vector3,
        third: Vector3,
        fourth: Vector3,
    ): List<Vector3> {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            val secondCell = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            val thirdCell = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            val fourthCell = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            writeVector3(firstCell, first)
            writeVector3(secondCell, second)
            writeVector3(thirdCell, third)
            writeVector3(fourthCell, fourth)
            val args = arena.allocate(ADDRESS, 4)
            args.setAtIndex(ADDRESS, 0, firstCell)
            args.setAtIndex(ADDRESS, 1, secondCell)
            args.setAtIndex(ADDRESS, 2, thirdCell)
            args.setAtIndex(ADDRESS, 3, fourthCell)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return try {
                BuiltinTypes.readPackedVector3Array(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR3_ARRAY, ret)
            }
        }
    }

    fun ptrcallWithFiveVector3ArgsRetVariantScalar(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Vector3,
        second: Vector3,
        third: Vector3,
        fourth: Vector3,
        fifth: Vector3,
    ): Any? {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            val secondCell = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            val thirdCell = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            val fourthCell = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            val fifthCell = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            writeVector3(firstCell, first)
            writeVector3(secondCell, second)
            writeVector3(thirdCell, third)
            writeVector3(fourthCell, fourth)
            writeVector3(fifthCell, fifth)
            val args = arena.allocate(ADDRESS, 5)
            args.setAtIndex(ADDRESS, 0, firstCell)
            args.setAtIndex(ADDRESS, 1, secondCell)
            args.setAtIndex(ADDRESS, 2, thirdCell)
            args.setAtIndex(ADDRESS, 3, fourthCell)
            args.setAtIndex(ADDRESS, 4, fifthCell)
            return readVariantReturn(methodBind, instance, args, arena)
        }
    }

    fun ptrcallWithThreeStringNameAndDoubleArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: String,
        second: String,
        third: String,
        value: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val valueCell = arena.allocate(JAVA_DOUBLE)
            valueCell.set(JAVA_DOUBLE, 0, value)
            val args = arena.allocate(ADDRESS, 4)
            args.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(first))
            args.setAtIndex(ADDRESS, 1, GodotStrings.makeStringName(second))
            args.setAtIndex(ADDRESS, 2, GodotStrings.makeStringName(third))
            args.setAtIndex(ADDRESS, 3, valueCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithThreeStringNameTwoDoubleBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: String,
        second: String,
        third: String,
        firstDouble: Double,
        secondDouble: Double,
        enabled: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val firstDoubleCell = arena.allocate(JAVA_DOUBLE)
            val secondDoubleCell = arena.allocate(JAVA_DOUBLE)
            val boolCell = arena.allocate(JAVA_BYTE)
            firstDoubleCell.set(JAVA_DOUBLE, 0, firstDouble)
            secondDoubleCell.set(JAVA_DOUBLE, 0, secondDouble)
            boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 6)
            args.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(first))
            args.setAtIndex(ADDRESS, 1, GodotStrings.makeStringName(second))
            args.setAtIndex(ADDRESS, 2, GodotStrings.makeStringName(third))
            args.setAtIndex(ADDRESS, 3, firstDoubleCell)
            args.setAtIndex(ADDRESS, 4, secondDoubleCell)
            args.setAtIndex(ADDRESS, 5, boolCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithStringNameTransform3DTwoVector3LongArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
        transform: Transform3D,
        firstVector: Vector3,
        secondVector: Vector3,
        longValue: Long,
    ) {
        Arena.ofConfined().use { arena ->
            val transformCell = arena.allocate(GodotReal.SIZE_BYTES * 12, GodotReal.ALIGN_BYTES)
            val vector0 = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            val vector1 = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            val longCell = arena.allocate(JAVA_LONG)
            writeTransform3D(transformCell, transform)
            writeVector3(vector0, firstVector)
            writeVector3(vector1, secondVector)
            longCell.set(JAVA_LONG, 0, longValue)
            val args = arena.allocate(ADDRESS, 5)
            args.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(name))
            args.setAtIndex(ADDRESS, 1, transformCell)
            args.setAtIndex(ADDRESS, 2, vector0)
            args.setAtIndex(ADDRESS, 3, vector1)
            args.setAtIndex(ADDRESS, 4, longCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithStringNameAndThreeDoubleArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
        first: Double,
        second: Double,
        third: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(JAVA_DOUBLE)
            val secondCell = arena.allocate(JAVA_DOUBLE)
            val thirdCell = arena.allocate(JAVA_DOUBLE)
            firstCell.set(JAVA_DOUBLE, 0, first)
            secondCell.set(JAVA_DOUBLE, 0, second)
            thirdCell.set(JAVA_DOUBLE, 0, third)
            val args = arena.allocate(ADDRESS, 4)
            args.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(name))
            args.setAtIndex(ADDRESS, 1, firstCell)
            args.setAtIndex(ADDRESS, 2, secondCell)
            args.setAtIndex(ADDRESS, 3, thirdCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithStringNameFourDoubleBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
        first: Double,
        second: Double,
        third: Double,
        fourth: Double,
        enabled: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(JAVA_DOUBLE)
            val secondCell = arena.allocate(JAVA_DOUBLE)
            val thirdCell = arena.allocate(JAVA_DOUBLE)
            val fourthCell = arena.allocate(JAVA_DOUBLE)
            val boolCell = arena.allocate(JAVA_BYTE)
            firstCell.set(JAVA_DOUBLE, 0, first)
            secondCell.set(JAVA_DOUBLE, 0, second)
            thirdCell.set(JAVA_DOUBLE, 0, third)
            fourthCell.set(JAVA_DOUBLE, 0, fourth)
            boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 6)
            args.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(name))
            args.setAtIndex(ADDRESS, 1, firstCell)
            args.setAtIndex(ADDRESS, 2, secondCell)
            args.setAtIndex(ADDRESS, 3, thirdCell)
            args.setAtIndex(ADDRESS, 4, fourthCell)
            args.setAtIndex(ADDRESS, 5, boolCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithStringNameThreeDoubleBoolTwoLongArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        name: String,
        first: Double,
        second: Double,
        third: Double,
        enabled: Boolean,
        firstLong: Long,
        secondLong: Long,
    ) {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(JAVA_DOUBLE)
            val secondCell = arena.allocate(JAVA_DOUBLE)
            val thirdCell = arena.allocate(JAVA_DOUBLE)
            val boolCell = arena.allocate(JAVA_BYTE)
            val firstLongCell = arena.allocate(JAVA_LONG)
            val secondLongCell = arena.allocate(JAVA_LONG)
            firstCell.set(JAVA_DOUBLE, 0, first)
            secondCell.set(JAVA_DOUBLE, 0, second)
            thirdCell.set(JAVA_DOUBLE, 0, third)
            boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
            firstLongCell.set(JAVA_LONG, 0, firstLong)
            secondLongCell.set(JAVA_LONG, 0, secondLong)
            val args = arena.allocate(ADDRESS, 7)
            args.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(name))
            args.setAtIndex(ADDRESS, 1, firstCell)
            args.setAtIndex(ADDRESS, 2, secondCell)
            args.setAtIndex(ADDRESS, 3, thirdCell)
            args.setAtIndex(ADDRESS, 4, boolCell)
            args.setAtIndex(ADDRESS, 5, firstLongCell)
            args.setAtIndex(ADDRESS, 6, secondLongCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithTwoVector2ColorTwoDoubleTwoBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Vector2,
        second: Vector2,
        color: Color,
        firstDouble: Double,
        secondDouble: Double,
        firstBool: Boolean,
        secondBool: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val secondCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val colorCell = arena.allocate(16L, 4L)
            val firstDoubleCell = arena.allocate(JAVA_DOUBLE)
            val secondDoubleCell = arena.allocate(JAVA_DOUBLE)
            val firstBoolCell = arena.allocate(JAVA_BYTE)
            val secondBoolCell = arena.allocate(JAVA_BYTE)
            writeVector2(firstCell, first)
            writeVector2(secondCell, second)
            // Color components are fixed 32-bit floats, unlike scalar float ptrcall slots.
            colorCell.set(JAVA_FLOAT, 0, color.r)
            colorCell.set(JAVA_FLOAT, 4, color.g)
            colorCell.set(JAVA_FLOAT, 8, color.b)
            colorCell.set(JAVA_FLOAT, 12, color.a)
            firstDoubleCell.set(JAVA_DOUBLE, 0, firstDouble)
            secondDoubleCell.set(JAVA_DOUBLE, 0, secondDouble)
            firstBoolCell.set(JAVA_BYTE, 0, if (firstBool) 1.toByte() else 0.toByte())
            secondBoolCell.set(JAVA_BYTE, 0, if (secondBool) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 7)
            args.setAtIndex(ADDRESS, 0, firstCell)
            args.setAtIndex(ADDRESS, 1, secondCell)
            args.setAtIndex(ADDRESS, 2, colorCell)
            args.setAtIndex(ADDRESS, 3, firstDoubleCell)
            args.setAtIndex(ADDRESS, 4, secondDoubleCell)
            args.setAtIndex(ADDRESS, 5, firstBoolCell)
            args.setAtIndex(ADDRESS, 6, secondBoolCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithVector2ThreeDoubleIntColorDoubleBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        vector: Vector2,
        firstDouble: Double,
        secondDouble: Double,
        thirdDouble: Double,
        intValue: Int,
        color: Color,
        fourthDouble: Double,
        enabled: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val vectorCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val firstDoubleCell = arena.allocate(JAVA_DOUBLE)
            val secondDoubleCell = arena.allocate(JAVA_DOUBLE)
            val thirdDoubleCell = arena.allocate(JAVA_DOUBLE)
            val intCell = arena.allocate(JAVA_INT)
            val colorCell = arena.allocate(16L, 4L)
            val fourthDoubleCell = arena.allocate(JAVA_DOUBLE)
            val boolCell = arena.allocate(JAVA_BYTE)
            writeVector2(vectorCell, vector)
            firstDoubleCell.set(JAVA_DOUBLE, 0, firstDouble)
            secondDoubleCell.set(JAVA_DOUBLE, 0, secondDouble)
            thirdDoubleCell.set(JAVA_DOUBLE, 0, thirdDouble)
            intCell.set(JAVA_INT, 0, intValue)
            // Color components are fixed 32-bit floats, unlike scalar float ptrcall slots.
            colorCell.set(JAVA_FLOAT, 0, color.r)
            colorCell.set(JAVA_FLOAT, 4, color.g)
            colorCell.set(JAVA_FLOAT, 8, color.b)
            colorCell.set(JAVA_FLOAT, 12, color.a)
            fourthDoubleCell.set(JAVA_DOUBLE, 0, fourthDouble)
            boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 8)
            args.setAtIndex(ADDRESS, 0, vectorCell)
            args.setAtIndex(ADDRESS, 1, firstDoubleCell)
            args.setAtIndex(ADDRESS, 2, secondDoubleCell)
            args.setAtIndex(ADDRESS, 3, thirdDoubleCell)
            args.setAtIndex(ADDRESS, 4, intCell)
            args.setAtIndex(ADDRESS, 5, colorCell)
            args.setAtIndex(ADDRESS, 6, fourthDoubleCell)
            args.setAtIndex(ADDRESS, 7, boolCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithVector2FourDoubleIntColorDoubleBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        vector: Vector2,
        firstDouble: Double,
        secondDouble: Double,
        thirdDouble: Double,
        fourthDouble: Double,
        intValue: Int,
        color: Color,
        fifthDouble: Double,
        enabled: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val vectorCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val firstDoubleCell = arena.allocate(JAVA_DOUBLE)
            val secondDoubleCell = arena.allocate(JAVA_DOUBLE)
            val thirdDoubleCell = arena.allocate(JAVA_DOUBLE)
            val fourthDoubleCell = arena.allocate(JAVA_DOUBLE)
            val intCell = arena.allocate(JAVA_INT)
            val colorCell = arena.allocate(16L, 4L)
            val fifthDoubleCell = arena.allocate(JAVA_DOUBLE)
            val boolCell = arena.allocate(JAVA_BYTE)
            writeVector2(vectorCell, vector)
            firstDoubleCell.set(JAVA_DOUBLE, 0, firstDouble)
            secondDoubleCell.set(JAVA_DOUBLE, 0, secondDouble)
            thirdDoubleCell.set(JAVA_DOUBLE, 0, thirdDouble)
            fourthDoubleCell.set(JAVA_DOUBLE, 0, fourthDouble)
            intCell.set(JAVA_INT, 0, intValue)
            // Color components are fixed 32-bit floats, unlike scalar float ptrcall slots.
            colorCell.set(JAVA_FLOAT, 0, color.r)
            colorCell.set(JAVA_FLOAT, 4, color.g)
            colorCell.set(JAVA_FLOAT, 8, color.b)
            colorCell.set(JAVA_FLOAT, 12, color.a)
            fifthDoubleCell.set(JAVA_DOUBLE, 0, fifthDouble)
            boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 9)
            args.setAtIndex(ADDRESS, 0, vectorCell)
            args.setAtIndex(ADDRESS, 1, firstDoubleCell)
            args.setAtIndex(ADDRESS, 2, secondDoubleCell)
            args.setAtIndex(ADDRESS, 3, thirdDoubleCell)
            args.setAtIndex(ADDRESS, 4, fourthDoubleCell)
            args.setAtIndex(ADDRESS, 5, intCell)
            args.setAtIndex(ADDRESS, 6, colorCell)
            args.setAtIndex(ADDRESS, 7, fifthDoubleCell)
            args.setAtIndex(ADDRESS, 8, boolCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithObjectStringObjectArgsRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstObject: MemorySegment,
        text: String,
        secondObject: MemorySegment,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val firstObjectCell = arena.allocate(ADDRESS)
            val stringCell = arena.allocate(8L, 8L)
            val secondObjectCell = arena.allocate(ADDRESS)
            try {
                firstObjectCell.set(ADDRESS, 0, firstObject)
                GodotStrings.initString(stringCell, text)
                secondObjectCell.set(ADDRESS, 0, secondObject)
                val args = arena.allocate(ADDRESS, 3)
                args.setAtIndex(ADDRESS, 0, firstObjectCell)
                args.setAtIndex(ADDRESS, 1, stringCell)
                args.setAtIndex(ADDRESS, 2, secondObjectCell)
                val ret = arena.allocate(ADDRESS)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(ADDRESS, 0)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithTwoStringBoolArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: String,
        second: String,
        enabled: Boolean,
    ): Long {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(8L, 8L)
            val secondCell = arena.allocate(8L, 8L)
            val boolCell = arena.allocate(JAVA_BYTE)
            try {
                GodotStrings.initString(firstCell, first)
                GodotStrings.initString(secondCell, second)
                boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
                val args = arena.allocate(ADDRESS, 3)
                args.setAtIndex(ADDRESS, 0, firstCell)
                args.setAtIndex(ADDRESS, 1, secondCell)
                args.setAtIndex(ADDRESS, 2, boolCell)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                GodotStrings.destroyString(secondCell)
                GodotStrings.destroyString(firstCell)
            }
        }
    }

    fun ptrcallWithStringDictionaryStringVariantArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        path: String,
        options: Map<String, Any?>,
        customImporter: String,
        generatorParameters: Any?,
    ): Long {
        Arena.ofConfined().use { arena ->
            val pathCell = arena.allocate(8L, 8L)
            val optionsCell = arena.allocate(8L, 8L)
            val customImporterCell = arena.allocate(8L, 8L)
            val variant = initVariantCell(arena, generatorParameters)
            try {
                GodotStrings.initString(pathCell, path)
                BuiltinTypes.initDictionary(optionsCell, options)
                GodotStrings.initString(customImporterCell, customImporter)
                val args = arena.allocate(ADDRESS, 4)
                args.setAtIndex(ADDRESS, 0, pathCell)
                args.setAtIndex(ADDRESS, 1, optionsCell)
                args.setAtIndex(ADDRESS, 2, customImporterCell)
                args.setAtIndex(ADDRESS, 3, variant)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                BuiltinTypes.destroyVariant(variant)
                GodotStrings.destroyString(customImporterCell)
                BuiltinTypes.destroyTyped(VariantType.DICTIONARY, optionsCell)
                GodotStrings.destroyString(pathCell)
            }
        }
    }

    fun ptrcallWithTwoStringColorBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: String,
        second: String,
        color: Color,
        enabled: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(8L, 8L)
            val secondCell = arena.allocate(8L, 8L)
            val colorCell = arena.allocate(16L, 4L)
            val boolCell = arena.allocate(JAVA_BYTE)
            try {
                GodotStrings.initString(firstCell, first)
                GodotStrings.initString(secondCell, second)
                // Color components are fixed 32-bit floats, unlike scalar float ptrcall slots.
                colorCell.set(JAVA_FLOAT, 0, color.r)
                colorCell.set(JAVA_FLOAT, 4, color.g)
                colorCell.set(JAVA_FLOAT, 8, color.b)
                colorCell.set(JAVA_FLOAT, 12, color.a)
                boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
                val args = arena.allocate(ADDRESS, 4)
                args.setAtIndex(ADDRESS, 0, firstCell)
                args.setAtIndex(ADDRESS, 1, secondCell)
                args.setAtIndex(ADDRESS, 2, colorCell)
                args.setAtIndex(ADDRESS, 3, boolCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(secondCell)
                GodotStrings.destroyString(firstCell)
            }
        }
    }

    fun ptrcallWithTwoStringTwoObjectArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: String,
        second: String,
        firstObject: MemorySegment,
        secondObject: MemorySegment,
    ) {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(8L, 8L)
            val secondCell = arena.allocate(8L, 8L)
            val firstObjectCell = arena.allocate(ADDRESS)
            val secondObjectCell = arena.allocate(ADDRESS)
            try {
                GodotStrings.initString(firstCell, first)
                GodotStrings.initString(secondCell, second)
                firstObjectCell.set(ADDRESS, 0, firstObject)
                secondObjectCell.set(ADDRESS, 0, secondObject)
                val args = arena.allocate(ADDRESS, 4)
                args.setAtIndex(ADDRESS, 0, firstCell)
                args.setAtIndex(ADDRESS, 1, secondCell)
                args.setAtIndex(ADDRESS, 2, firstObjectCell)
                args.setAtIndex(ADDRESS, 3, secondObjectCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(secondCell)
                GodotStrings.destroyString(firstCell)
            }
        }
    }

    fun ptrcallWithStringObjectBoolArgsRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        objectArg: MemorySegment,
        enabled: Boolean,
    ): Int {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val objectCell = arena.allocate(ADDRESS)
            val boolCell = arena.allocate(JAVA_BYTE)
            try {
                GodotStrings.initString(stringCell, text)
                objectCell.set(ADDRESS, 0, objectArg)
                boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
                val args = arena.allocate(ADDRESS, 3)
                args.setAtIndex(ADDRESS, 0, stringCell)
                args.setAtIndex(ADDRESS, 1, objectCell)
                args.setAtIndex(ADDRESS, 2, boolCell)
                val ret = arena.allocate(JAVA_INT)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(JAVA_INT, 0)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithStringObjectIntArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        objectArg: MemorySegment,
        value: Int,
    ) {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val objectCell = arena.allocate(ADDRESS)
            val intCell = arena.allocate(JAVA_INT)
            try {
                GodotStrings.initString(stringCell, text)
                objectCell.set(ADDRESS, 0, objectArg)
                intCell.set(JAVA_INT, 0, value)
                val args = arena.allocate(ADDRESS, 3)
                args.setAtIndex(ADDRESS, 0, stringCell)
                args.setAtIndex(ADDRESS, 1, objectCell)
                args.setAtIndex(ADDRESS, 2, intCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithStringLongObjectTwoBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        longValue: Long,
        objectArg: MemorySegment,
        firstBool: Boolean,
        secondBool: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val longCell = arena.allocate(JAVA_LONG)
            val objectCell = arena.allocate(ADDRESS)
            val bool0 = arena.allocate(JAVA_BYTE)
            val bool1 = arena.allocate(JAVA_BYTE)
            try {
                GodotStrings.initString(stringCell, text)
                longCell.set(JAVA_LONG, 0, longValue)
                objectCell.set(ADDRESS, 0, objectArg)
                bool0.set(JAVA_BYTE, 0, if (firstBool) 1.toByte() else 0.toByte())
                bool1.set(JAVA_BYTE, 0, if (secondBool) 1.toByte() else 0.toByte())
                val args = arena.allocate(ADDRESS, 5)
                args.setAtIndex(ADDRESS, 0, stringCell)
                args.setAtIndex(ADDRESS, 1, longCell)
                args.setAtIndex(ADDRESS, 2, objectCell)
                args.setAtIndex(ADDRESS, 3, bool0)
                args.setAtIndex(ADDRESS, 4, bool1)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithStringObjectUInt32StringArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstText: String,
        objectArg: MemorySegment,
        uintValue: Long,
        secondText: String,
    ): Long {
        Arena.ofConfined().use { arena ->
            val firstString = arena.allocate(8L, 8L)
            val objectCell = arena.allocate(ADDRESS)
            val uintCell = arena.allocate(JAVA_INT)
            val secondString = arena.allocate(8L, 8L)
            try {
                GodotStrings.initString(firstString, firstText)
                objectCell.set(ADDRESS, 0, objectArg)
                uintCell.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(uintValue))
                GodotStrings.initString(secondString, secondText)
                val args = arena.allocate(ADDRESS, 4)
                args.setAtIndex(ADDRESS, 0, firstString)
                args.setAtIndex(ADDRESS, 1, objectCell)
                args.setAtIndex(ADDRESS, 2, uintCell)
                args.setAtIndex(ADDRESS, 3, secondString)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                GodotStrings.destroyString(secondString)
                GodotStrings.destroyString(firstString)
            }
        }
    }

    fun ptrcallWithStringObjectIntStringVariantArgsRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        objectArg: MemorySegment,
        intValue: Int,
        language: String,
        variantValue: Any?,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val objectCell = arena.allocate(ADDRESS)
            val intCell = arena.allocate(JAVA_INT)
            val languageCell = arena.allocate(8L, 8L)
            val variant = initVariantCell(arena, variantValue)
            try {
                GodotStrings.initString(stringCell, text)
                objectCell.set(ADDRESS, 0, objectArg)
                intCell.set(JAVA_INT, 0, intValue)
                GodotStrings.initString(languageCell, language)
                val args = arena.allocate(ADDRESS, 5)
                args.setAtIndex(ADDRESS, 0, stringCell)
                args.setAtIndex(ADDRESS, 1, objectCell)
                args.setAtIndex(ADDRESS, 2, intCell)
                args.setAtIndex(ADDRESS, 3, languageCell)
                args.setAtIndex(ADDRESS, 4, variant)
                val ret = arena.allocate(JAVA_BYTE)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(JAVA_BYTE, 0) != 0.toByte()
            } finally {
                GodotStrings.destroyString(languageCell)
                GodotStrings.destroyString(stringCell)
                BuiltinTypes.destroyVariant(variant)
            }
        }
    }

    fun ptrcallWithStringObjectIntRect2StringArgsRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        objectArg: MemorySegment,
        intValue: Int,
        rect: Rect2,
        language: String,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val objectCell = arena.allocate(ADDRESS)
            val intCell = arena.allocate(JAVA_INT)
            val rectCell = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
            val languageCell = arena.allocate(8L, 8L)
            try {
                GodotStrings.initString(stringCell, text)
                objectCell.set(ADDRESS, 0, objectArg)
                intCell.set(JAVA_INT, 0, intValue)
                writeRect2(rectCell, rect)
                GodotStrings.initString(languageCell, language)
                val args = arena.allocate(ADDRESS, 5)
                args.setAtIndex(ADDRESS, 0, stringCell)
                args.setAtIndex(ADDRESS, 1, objectCell)
                args.setAtIndex(ADDRESS, 2, intCell)
                args.setAtIndex(ADDRESS, 3, rectCell)
                args.setAtIndex(ADDRESS, 4, languageCell)
                val ret = arena.allocate(JAVA_BYTE)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(JAVA_BYTE, 0) != 0.toByte()
            } finally {
                GodotStrings.destroyString(languageCell)
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithStringObjectBoolColorArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        objectArg: MemorySegment,
        enabled: Boolean,
        color: Color,
    ) {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val objectCell = arena.allocate(ADDRESS)
            val boolCell = arena.allocate(JAVA_BYTE)
            val colorCell = arena.allocate(16L, 4L)
            try {
                GodotStrings.initString(stringCell, text)
                objectCell.set(ADDRESS, 0, objectArg)
                boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
                // Color components are fixed 32-bit floats, unlike scalar float ptrcall slots.
                colorCell.set(JAVA_FLOAT, 0, color.r)
                colorCell.set(JAVA_FLOAT, 4, color.g)
                colorCell.set(JAVA_FLOAT, 8, color.b)
                colorCell.set(JAVA_FLOAT, 12, color.a)
                val args = arena.allocate(ADDRESS, 4)
                args.setAtIndex(ADDRESS, 0, stringCell)
                args.setAtIndex(ADDRESS, 1, objectCell)
                args.setAtIndex(ADDRESS, 2, boolCell)
                args.setAtIndex(ADDRESS, 3, colorCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithStringIntStringBoolArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: String,
        value: Int,
        second: String,
        enabled: Boolean,
    ): Long {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(8L, 8L)
            val intCell = arena.allocate(JAVA_INT)
            val secondCell = arena.allocate(8L, 8L)
            val boolCell = arena.allocate(JAVA_BYTE)
            try {
                GodotStrings.initString(firstCell, first)
                intCell.set(JAVA_INT, 0, value)
                GodotStrings.initString(secondCell, second)
                boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
                val args = arena.allocate(ADDRESS, 4)
                args.setAtIndex(ADDRESS, 0, firstCell)
                args.setAtIndex(ADDRESS, 1, intCell)
                args.setAtIndex(ADDRESS, 2, secondCell)
                args.setAtIndex(ADDRESS, 3, boolCell)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                GodotStrings.destroyString(secondCell)
                GodotStrings.destroyString(firstCell)
            }
        }
    }

    fun ptrcallWithStringBoolObjectArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        enabled: Boolean,
        objectArg: MemorySegment,
    ) {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val boolCell = arena.allocate(JAVA_BYTE)
            val objectCell = arena.allocate(ADDRESS)
            try {
                GodotStrings.initString(stringCell, text)
                boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
                objectCell.set(ADDRESS, 0, objectArg)
                val args = arena.allocate(ADDRESS, 3)
                args.setAtIndex(ADDRESS, 0, stringCell)
                args.setAtIndex(ADDRESS, 1, boolCell)
                args.setAtIndex(ADDRESS, 2, objectCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithStringColorThreeBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        color: Color,
        first: Boolean,
        second: Boolean,
        third: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val colorCell = arena.allocate(16L, 4L)
            val firstCell = arena.allocate(JAVA_BYTE)
            val secondCell = arena.allocate(JAVA_BYTE)
            val thirdCell = arena.allocate(JAVA_BYTE)
            try {
                GodotStrings.initString(stringCell, text)
                // Color components are fixed 32-bit floats, unlike scalar float ptrcall slots.
                colorCell.set(JAVA_FLOAT, 0, color.r)
                colorCell.set(JAVA_FLOAT, 4, color.g)
                colorCell.set(JAVA_FLOAT, 8, color.b)
                colorCell.set(JAVA_FLOAT, 12, color.a)
                firstCell.set(JAVA_BYTE, 0, if (first) 1.toByte() else 0.toByte())
                secondCell.set(JAVA_BYTE, 0, if (second) 1.toByte() else 0.toByte())
                thirdCell.set(JAVA_BYTE, 0, if (third) 1.toByte() else 0.toByte())
                val args = arena.allocate(ADDRESS, 5)
                args.setAtIndex(ADDRESS, 0, stringCell)
                args.setAtIndex(ADDRESS, 1, colorCell)
                args.setAtIndex(ADDRESS, 2, firstCell)
                args.setAtIndex(ADDRESS, 3, secondCell)
                args.setAtIndex(ADDRESS, 4, thirdCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithStringLongStringArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: String,
        value: Long,
        second: String,
    ) {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(8L, 8L)
            val longCell = arena.allocate(JAVA_LONG)
            val secondCell = arena.allocate(8L, 8L)
            try {
                GodotStrings.initString(firstCell, first)
                longCell.set(JAVA_LONG, 0, value)
                GodotStrings.initString(secondCell, second)
                val args = arena.allocate(ADDRESS, 3)
                args.setAtIndex(ADDRESS, 0, firstCell)
                args.setAtIndex(ADDRESS, 1, longCell)
                args.setAtIndex(ADDRESS, 2, secondCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(secondCell)
                GodotStrings.destroyString(firstCell)
            }
        }
    }

    fun ptrcallWithStringTwoLongArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        first: Long,
        second: Long,
    ) {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val firstCell = arena.allocate(JAVA_LONG)
            val secondCell = arena.allocate(JAVA_LONG)
            try {
                GodotStrings.initString(stringCell, text)
                firstCell.set(JAVA_LONG, 0, first)
                secondCell.set(JAVA_LONG, 0, second)
                val args = arena.allocate(ADDRESS, 3)
                args.setAtIndex(ADDRESS, 0, stringCell)
                args.setAtIndex(ADDRESS, 1, firstCell)
                args.setAtIndex(ADDRESS, 2, secondCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithStringTwoLongArgsRetDictionary(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        first: Long,
        second: Long,
    ): Map<String, Any?> {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val firstCell = arena.allocate(JAVA_LONG)
            val secondCell = arena.allocate(JAVA_LONG)
            val ret = arena.allocate(8L, 8L)
            try {
                GodotStrings.initString(stringCell, text)
                firstCell.set(JAVA_LONG, 0, first)
                secondCell.set(JAVA_LONG, 0, second)
                val args = arena.allocate(ADDRESS, 3)
                args.setAtIndex(ADDRESS, 0, stringCell)
                args.setAtIndex(ADDRESS, 1, firstCell)
                args.setAtIndex(ADDRESS, 2, secondCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return BuiltinTypes.readDictionaryScalars(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.DICTIONARY, ret)
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithStringFourDoubleArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        first: Double,
        second: Double,
        third: Double,
        fourth: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val firstCell = arena.allocate(JAVA_DOUBLE)
            val secondCell = arena.allocate(JAVA_DOUBLE)
            val thirdCell = arena.allocate(JAVA_DOUBLE)
            val fourthCell = arena.allocate(JAVA_DOUBLE)
            try {
                GodotStrings.initString(stringCell, text)
                firstCell.set(JAVA_DOUBLE, 0, first)
                secondCell.set(JAVA_DOUBLE, 0, second)
                thirdCell.set(JAVA_DOUBLE, 0, third)
                fourthCell.set(JAVA_DOUBLE, 0, fourth)
                val args = arena.allocate(ADDRESS, 5)
                args.setAtIndex(ADDRESS, 0, stringCell)
                args.setAtIndex(ADDRESS, 1, firstCell)
                args.setAtIndex(ADDRESS, 2, secondCell)
                args.setAtIndex(ADDRESS, 3, thirdCell)
                args.setAtIndex(ADDRESS, 4, fourthCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithStringStringNameFourDoubleArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        name: String,
        first: Double,
        second: Double,
        third: Double,
        fourth: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val firstCell = arena.allocate(JAVA_DOUBLE)
            val secondCell = arena.allocate(JAVA_DOUBLE)
            val thirdCell = arena.allocate(JAVA_DOUBLE)
            val fourthCell = arena.allocate(JAVA_DOUBLE)
            try {
                GodotStrings.initString(stringCell, text)
                firstCell.set(JAVA_DOUBLE, 0, first)
                secondCell.set(JAVA_DOUBLE, 0, second)
                thirdCell.set(JAVA_DOUBLE, 0, third)
                fourthCell.set(JAVA_DOUBLE, 0, fourth)
                val args = arena.allocate(ADDRESS, 6)
                args.setAtIndex(ADDRESS, 0, stringCell)
                args.setAtIndex(ADDRESS, 1, GodotStrings.makeStringName(name))
                args.setAtIndex(ADDRESS, 2, firstCell)
                args.setAtIndex(ADDRESS, 3, secondCell)
                args.setAtIndex(ADDRESS, 4, thirdCell)
                args.setAtIndex(ADDRESS, 5, fourthCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithIntLongBoolStringArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        index: Int,
        mode: Long,
        enabled: Boolean,
        text: String,
    ) {
        Arena.ofConfined().use { arena ->
            val intCell = arena.allocate(JAVA_INT)
            val longCell = arena.allocate(JAVA_LONG)
            val boolCell = arena.allocate(JAVA_BYTE)
            val stringCell = arena.allocate(8L, 8L)
            try {
                intCell.set(JAVA_INT, 0, index)
                longCell.set(JAVA_LONG, 0, mode)
                boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
                GodotStrings.initString(stringCell, text)
                val args = arena.allocate(ADDRESS, 4)
                args.setAtIndex(ADDRESS, 0, intCell)
                args.setAtIndex(ADDRESS, 1, longCell)
                args.setAtIndex(ADDRESS, 2, boolCell)
                args.setAtIndex(ADDRESS, 3, stringCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithIntLongIntStringArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        index: Int,
        mode: Long,
        value: Int,
        text: String,
    ) {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(JAVA_INT)
            val longCell = arena.allocate(JAVA_LONG)
            val secondCell = arena.allocate(JAVA_INT)
            val stringCell = arena.allocate(8L, 8L)
            try {
                firstCell.set(JAVA_INT, 0, index)
                longCell.set(JAVA_LONG, 0, mode)
                secondCell.set(JAVA_INT, 0, value)
                GodotStrings.initString(stringCell, text)
                val args = arena.allocate(ADDRESS, 4)
                args.setAtIndex(ADDRESS, 0, firstCell)
                args.setAtIndex(ADDRESS, 1, longCell)
                args.setAtIndex(ADDRESS, 2, secondCell)
                args.setAtIndex(ADDRESS, 3, stringCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithStringUInt32TwoIntArgsRetVector2i(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        flags: Long,
        line: Int,
        column: Int,
    ): Vector2i {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val flagsCell = arena.allocate(JAVA_INT)
            val lineCell = arena.allocate(JAVA_INT)
            val columnCell = arena.allocate(JAVA_INT)
            try {
                GodotStrings.initString(stringCell, text)
                flagsCell.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(flags))
                lineCell.set(JAVA_INT, 0, line)
                columnCell.set(JAVA_INT, 0, column)
                val args = arena.allocate(ADDRESS, 4)
                args.setAtIndex(ADDRESS, 0, stringCell)
                args.setAtIndex(ADDRESS, 1, flagsCell)
                args.setAtIndex(ADDRESS, 2, lineCell)
                args.setAtIndex(ADDRESS, 3, columnCell)
                val ret = arena.allocate(8L, 4L)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return readVector2i(ret)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithRIDAndThreeLongArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        first: Long,
        second: Long,
        third: Long,
    ): Long {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val firstCell = arena.allocate(JAVA_LONG)
            val secondCell = arena.allocate(JAVA_LONG)
            val thirdCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            firstCell.set(JAVA_LONG, 0, first)
            secondCell.set(JAVA_LONG, 0, second)
            thirdCell.set(JAVA_LONG, 0, third)
            val args = arena.allocate(ADDRESS, 4)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, firstCell)
            args.setAtIndex(ADDRESS, 2, secondCell)
            args.setAtIndex(ADDRESS, 3, thirdCell)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(JAVA_LONG, 0)
        }
    }

    fun ptrcallWithRIDFourDoubleArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        first: Double,
        second: Double,
        third: Double,
        fourth: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val firstCell = arena.allocate(JAVA_DOUBLE)
            val secondCell = arena.allocate(JAVA_DOUBLE)
            val thirdCell = arena.allocate(JAVA_DOUBLE)
            val fourthCell = arena.allocate(JAVA_DOUBLE)
            ridCell.set(JAVA_LONG, 0, rid.value)
            firstCell.set(JAVA_DOUBLE, 0, first)
            secondCell.set(JAVA_DOUBLE, 0, second)
            thirdCell.set(JAVA_DOUBLE, 0, third)
            fourthCell.set(JAVA_DOUBLE, 0, fourth)
            val args = arena.allocate(ADDRESS, 5)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, firstCell)
            args.setAtIndex(ADDRESS, 2, secondCell)
            args.setAtIndex(ADDRESS, 3, thirdCell)
            args.setAtIndex(ADDRESS, 4, fourthCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDRect2RIDRect2ColorArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        rect: Rect2,
        texture: RID,
        srcRect: Rect2,
        color: Color,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val rectCell = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
            val textureCell = arena.allocate(JAVA_LONG)
            val srcRectCell = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
            val colorCell = arena.allocate(16L, 4L)
            ridCell.set(JAVA_LONG, 0, rid.value)
            writeRect2(rectCell, rect)
            textureCell.set(JAVA_LONG, 0, texture.value)
            writeRect2(srcRectCell, srcRect)
            colorCell.set(JAVA_FLOAT, 0, color.r)
            colorCell.set(JAVA_FLOAT, 4, color.g)
            colorCell.set(JAVA_FLOAT, 8, color.b)
            colorCell.set(JAVA_FLOAT, 12, color.a)
            val args = arena.allocate(ADDRESS, 5)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, rectCell)
            args.setAtIndex(ADDRESS, 2, textureCell)
            args.setAtIndex(ADDRESS, 3, srcRectCell)
            args.setAtIndex(ADDRESS, 4, colorCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDRect2RIDRect2ColorIntTwoDoubleArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        rect: Rect2,
        texture: RID,
        srcRect: Rect2,
        color: Color,
        intValue: Int,
        firstDouble: Double,
        secondDouble: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val rectCell = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
            val textureCell = arena.allocate(JAVA_LONG)
            val srcRectCell = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
            val colorCell = arena.allocate(16L, 4L)
            val intCell = arena.allocate(JAVA_INT)
            val double0 = arena.allocate(JAVA_DOUBLE)
            val double1 = arena.allocate(JAVA_DOUBLE)
            ridCell.set(JAVA_LONG, 0, rid.value)
            writeRect2(rectCell, rect)
            textureCell.set(JAVA_LONG, 0, texture.value)
            writeRect2(srcRectCell, srcRect)
            colorCell.set(JAVA_FLOAT, 0, color.r)
            colorCell.set(JAVA_FLOAT, 4, color.g)
            colorCell.set(JAVA_FLOAT, 8, color.b)
            colorCell.set(JAVA_FLOAT, 12, color.a)
            intCell.set(JAVA_INT, 0, intValue)
            double0.set(JAVA_DOUBLE, 0, firstDouble)
            double1.set(JAVA_DOUBLE, 0, secondDouble)
            val args = arena.allocate(ADDRESS, 8)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, rectCell)
            args.setAtIndex(ADDRESS, 2, textureCell)
            args.setAtIndex(ADDRESS, 3, srcRectCell)
            args.setAtIndex(ADDRESS, 4, colorCell)
            args.setAtIndex(ADDRESS, 5, intCell)
            args.setAtIndex(ADDRESS, 6, double0)
            args.setAtIndex(ADDRESS, 7, double1)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDTwoRect2RIDTwoVector2TwoLongBoolColorArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        firstRect: Rect2,
        secondRect: Rect2,
        texture: RID,
        firstVector: Vector2,
        secondVector: Vector2,
        firstLong: Long,
        secondLong: Long,
        enabled: Boolean,
        color: Color,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val rect0 = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
            val rect1 = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
            val textureCell = arena.allocate(JAVA_LONG)
            val vector0 = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val vector1 = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val long0 = arena.allocate(JAVA_LONG)
            val long1 = arena.allocate(JAVA_LONG)
            val boolCell = arena.allocate(JAVA_BYTE)
            val colorCell = arena.allocate(16L, 4L)
            ridCell.set(JAVA_LONG, 0, rid.value)
            writeRect2(rect0, firstRect)
            writeRect2(rect1, secondRect)
            textureCell.set(JAVA_LONG, 0, texture.value)
            writeVector2(vector0, firstVector)
            writeVector2(vector1, secondVector)
            long0.set(JAVA_LONG, 0, firstLong)
            long1.set(JAVA_LONG, 0, secondLong)
            boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
            colorCell.set(JAVA_FLOAT, 0, color.r)
            colorCell.set(JAVA_FLOAT, 4, color.g)
            colorCell.set(JAVA_FLOAT, 8, color.b)
            colorCell.set(JAVA_FLOAT, 12, color.a)
            val args = arena.allocate(ADDRESS, 10)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, rect0)
            args.setAtIndex(ADDRESS, 2, rect1)
            args.setAtIndex(ADDRESS, 3, textureCell)
            args.setAtIndex(ADDRESS, 4, vector0)
            args.setAtIndex(ADDRESS, 5, vector1)
            args.setAtIndex(ADDRESS, 6, long0)
            args.setAtIndex(ADDRESS, 7, long1)
            args.setAtIndex(ADDRESS, 8, boolCell)
            args.setAtIndex(ADDRESS, 9, colorCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDRect2Rect2iBoolUInt32BoolVector2FourDoubleArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        rect: Rect2,
        screenRect: Rect2i,
        firstBool: Boolean,
        layer: Long,
        secondBool: Boolean,
        vector: Vector2,
        firstDouble: Double,
        secondDouble: Double,
        thirdDouble: Double,
        fourthDouble: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val rectCell = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
            val screenRectCell = arena.allocate(16L, 4L)
            val bool0 = arena.allocate(JAVA_BYTE)
            val layerCell = arena.allocate(JAVA_INT)
            val bool1 = arena.allocate(JAVA_BYTE)
            val vectorCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val double0 = arena.allocate(JAVA_DOUBLE)
            val double1 = arena.allocate(JAVA_DOUBLE)
            val double2 = arena.allocate(JAVA_DOUBLE)
            val double3 = arena.allocate(JAVA_DOUBLE)
            ridCell.set(JAVA_LONG, 0, rid.value)
            writeRect2(rectCell, rect)
            screenRectCell.set(JAVA_INT, 0, screenRect.position.x)
            screenRectCell.set(JAVA_INT, 4, screenRect.position.y)
            screenRectCell.set(JAVA_INT, 8, screenRect.size.x)
            screenRectCell.set(JAVA_INT, 12, screenRect.size.y)
            bool0.set(JAVA_BYTE, 0, if (firstBool) 1.toByte() else 0.toByte())
            layerCell.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(layer))
            bool1.set(JAVA_BYTE, 0, if (secondBool) 1.toByte() else 0.toByte())
            writeVector2(vectorCell, vector)
            double0.set(JAVA_DOUBLE, 0, firstDouble)
            double1.set(JAVA_DOUBLE, 0, secondDouble)
            double2.set(JAVA_DOUBLE, 0, thirdDouble)
            double3.set(JAVA_DOUBLE, 0, fourthDouble)
            val args = arena.allocate(ADDRESS, 11)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, rectCell)
            args.setAtIndex(ADDRESS, 2, screenRectCell)
            args.setAtIndex(ADDRESS, 3, bool0)
            args.setAtIndex(ADDRESS, 4, layerCell)
            args.setAtIndex(ADDRESS, 5, bool1)
            args.setAtIndex(ADDRESS, 6, vectorCell)
            args.setAtIndex(ADDRESS, 7, double0)
            args.setAtIndex(ADDRESS, 8, double1)
            args.setAtIndex(ADDRESS, 9, double2)
            args.setAtIndex(ADDRESS, 10, double3)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithTwoRIDRect2IntArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: RID,
        second: RID,
        rect: Rect2,
        index: Int,
    ) {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(JAVA_LONG)
            val secondCell = arena.allocate(JAVA_LONG)
            val rectCell = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
            val indexCell = arena.allocate(JAVA_INT)
            firstCell.set(JAVA_LONG, 0, first.value)
            secondCell.set(JAVA_LONG, 0, second.value)
            writeRect2(rectCell, rect)
            indexCell.set(JAVA_INT, 0, index)
            val args = arena.allocate(ADDRESS, 4)
            args.setAtIndex(ADDRESS, 0, firstCell)
            args.setAtIndex(ADDRESS, 1, secondCell)
            args.setAtIndex(ADDRESS, 2, rectCell)
            args.setAtIndex(ADDRESS, 3, indexCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDVector2iLongArgsRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        point: Vector2i,
        value: Long,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val pointCell = arena.allocate(8L, 4L)
            val valueCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            pointCell.set(JAVA_INT, 0, point.x)
            pointCell.set(JAVA_INT, 4, point.y)
            valueCell.set(JAVA_LONG, 0, value)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, pointCell)
            args.setAtIndex(ADDRESS, 2, valueCell)
            val ret = arena.allocate(ADDRESS)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(ADDRESS, 0)
        }
    }

    fun ptrcallWithRIDVector2iLongObjectArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        point: Vector2i,
        value: Long,
        objectArg: MemorySegment,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val pointCell = arena.allocate(8L, 4L)
            val valueCell = arena.allocate(JAVA_LONG)
            val objCell = arena.allocate(ADDRESS)
            ridCell.set(JAVA_LONG, 0, rid.value)
            pointCell.set(JAVA_INT, 0, point.x)
            pointCell.set(JAVA_INT, 4, point.y)
            valueCell.set(JAVA_LONG, 0, value)
            objCell.set(ADDRESS, 0, objectArg)
            val args = arena.allocate(ADDRESS, 4)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, pointCell)
            args.setAtIndex(ADDRESS, 2, valueCell)
            args.setAtIndex(ADDRESS, 3, objCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDBoolVector2iArgsRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        enabled: Boolean,
        size: Vector2i,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val boolCell = arena.allocate(JAVA_BYTE)
            val sizeCell = arena.allocate(8L, 4L)
            ridCell.set(JAVA_LONG, 0, rid.value)
            boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
            sizeCell.set(JAVA_INT, 0, size.x)
            sizeCell.set(JAVA_INT, 4, size.y)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, boolCell)
            args.setAtIndex(ADDRESS, 2, sizeCell)
            val ret = arena.allocate(ADDRESS)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(ADDRESS, 0)
        }
    }

    fun ptrcallWithRIDDoubleBoolVector2iArgsRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Double,
        enabled: Boolean,
        size: Vector2i,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val doubleCell = arena.allocate(JAVA_DOUBLE)
            val boolCell = arena.allocate(JAVA_BYTE)
            val sizeCell = arena.allocate(8L, 4L)
            ridCell.set(JAVA_LONG, 0, rid.value)
            doubleCell.set(JAVA_DOUBLE, 0, value)
            boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
            sizeCell.set(JAVA_INT, 0, size.x)
            sizeCell.set(JAVA_INT, 4, size.y)
            val args = arena.allocate(ADDRESS, 4)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, doubleCell)
            args.setAtIndex(ADDRESS, 2, boolCell)
            args.setAtIndex(ADDRESS, 3, sizeCell)
            val ret = arena.allocate(ADDRESS)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(ADDRESS, 0)
        }
    }

    fun ptrcallWithIntVector2iIntArgsRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        point: Vector2i,
        second: Int,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(JAVA_INT)
            val pointCell = arena.allocate(8L, 4L)
            val secondCell = arena.allocate(JAVA_INT)
            firstCell.set(JAVA_INT, 0, first)
            pointCell.set(JAVA_INT, 0, point.x)
            pointCell.set(JAVA_INT, 4, point.y)
            secondCell.set(JAVA_INT, 0, second)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, firstCell)
            args.setAtIndex(ADDRESS, 1, pointCell)
            args.setAtIndex(ADDRESS, 2, secondCell)
            val ret = arena.allocate(ADDRESS)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(ADDRESS, 0)
        }
    }

    fun ptrcallWithTransform2DVector2ArgsRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        transform: Transform2D,
        vector: Vector2,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val transformCell = arena.allocate(GodotReal.SIZE_BYTES * 6, GodotReal.ALIGN_BYTES)
            val vectorCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            writeTransform2D(transformCell, transform)
            GodotReal.writeIndex(vectorCell, 0, vector.x)
            GodotReal.writeIndex(vectorCell, 1, vector.y)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, transformCell)
            args.setAtIndex(ADDRESS, 1, vectorCell)
            val ret = arena.allocate(ADDRESS)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(ADDRESS, 0)
        }
    }

    private fun writeRect2(dest: MemorySegment, value: Rect2) {
        GodotReal.writeIndex(dest, 0, value.position.x)
        GodotReal.writeIndex(dest, 1, value.position.y)
        GodotReal.writeIndex(dest, 2, value.size.x)
        GodotReal.writeIndex(dest, 3, value.size.y)
    }

    private fun writeVector2(dest: MemorySegment, value: Vector2) {
        GodotReal.writeIndex(dest, 0, value.x)
        GodotReal.writeIndex(dest, 1, value.y)
    }

    private fun writeVector3(dest: MemorySegment, value: Vector3) {
        GodotReal.writeIndex(dest, 0, value.x)
        GodotReal.writeIndex(dest, 1, value.y)
        GodotReal.writeIndex(dest, 2, value.z)
    }

    private fun readVector2i(src: MemorySegment): Vector2i =
        Vector2i(
            x = src.get(JAVA_INT, 0),
            y = src.get(JAVA_INT, 4),
        )

    private fun writeVector2i(dest: MemorySegment, value: Vector2i) {
        dest.set(JAVA_INT, 0, value.x)
        dest.set(JAVA_INT, 4, value.y)
    }

    private fun readVector3i(src: MemorySegment): Vector3i =
        Vector3i(
            x = src.get(JAVA_INT, 0),
            y = src.get(JAVA_INT, 4),
            z = src.get(JAVA_INT, 8),
        )

    private fun writeVector3i(dest: MemorySegment, value: Vector3i) {
        dest.set(JAVA_INT, 0, value.x)
        dest.set(JAVA_INT, 4, value.y)
        dest.set(JAVA_INT, 8, value.z)
    }

    private fun readVector4(src: MemorySegment): Vector4 =
        Vector4(
            x = GodotReal.readIndex(src, 0),
            y = GodotReal.readIndex(src, 1),
            z = GodotReal.readIndex(src, 2),
            w = GodotReal.readIndex(src, 3),
        )

    private fun readRect2(src: MemorySegment): Rect2 =
        Rect2(
            position = Vector2(
                GodotReal.readIndex(src, 0),
                GodotReal.readIndex(src, 1),
            ),
            size = Vector2(
                GodotReal.readIndex(src, 2),
                GodotReal.readIndex(src, 3),
            ),
        )

    private fun readRect2i(src: MemorySegment): Rect2i =
        Rect2i(
            position = Vector2i(
                x = src.get(JAVA_INT, 0),
                y = src.get(JAVA_INT, 4),
            ),
            size = Vector2i(
                x = src.get(JAVA_INT, 8),
                y = src.get(JAVA_INT, 12),
            ),
        )

    private fun readAABB(src: MemorySegment): AABB =
        AABB(
            position = Vector3(
                GodotReal.readIndex(src, 0),
                GodotReal.readIndex(src, 1),
                GodotReal.readIndex(src, 2),
            ),
            size = Vector3(
                GodotReal.readIndex(src, 3),
                GodotReal.readIndex(src, 4),
                GodotReal.readIndex(src, 5),
            ),
        )

    private fun writeAABB(dest: MemorySegment, value: AABB) {
        GodotReal.writeIndex(dest, 0, value.position.x)
        GodotReal.writeIndex(dest, 1, value.position.y)
        GodotReal.writeIndex(dest, 2, value.position.z)
        GodotReal.writeIndex(dest, 3, value.size.x)
        GodotReal.writeIndex(dest, 4, value.size.y)
        GodotReal.writeIndex(dest, 5, value.size.z)
    }

    private fun readPlane(src: MemorySegment): Plane =
        Plane(
            normal = Vector3(
                x = GodotReal.readIndex(src, 0),
                y = GodotReal.readIndex(src, 1),
                z = GodotReal.readIndex(src, 2),
            ),
            d = GodotReal.readIndex(src, 3),
        )

    private fun readBasis(src: MemorySegment): Basis =
        Basis(
            x = Vector3(GodotReal.readIndex(src, 0), GodotReal.readIndex(src, 3), GodotReal.readIndex(src, 6)),
            y = Vector3(GodotReal.readIndex(src, 1), GodotReal.readIndex(src, 4), GodotReal.readIndex(src, 7)),
            z = Vector3(GodotReal.readIndex(src, 2), GodotReal.readIndex(src, 5), GodotReal.readIndex(src, 8)),
        )

    private fun readTransform2D(src: MemorySegment): Transform2D =
        Transform2D(
            x = Vector2(
                GodotReal.readIndex(src, 0),
                GodotReal.readIndex(src, 1),
            ),
            y = Vector2(
                GodotReal.readIndex(src, 2),
                GodotReal.readIndex(src, 3),
            ),
            origin = Vector2(
                GodotReal.readIndex(src, 4),
                GodotReal.readIndex(src, 5),
            ),
        )

    private fun writeTransform2D(dest: MemorySegment, value: Transform2D) {
        GodotReal.writeIndex(dest, 0, value.x.x)
        GodotReal.writeIndex(dest, 1, value.x.y)
        GodotReal.writeIndex(dest, 2, value.y.x)
        GodotReal.writeIndex(dest, 3, value.y.y)
        GodotReal.writeIndex(dest, 4, value.origin.x)
        GodotReal.writeIndex(dest, 5, value.origin.y)
    }

    private fun readTransform3D(src: MemorySegment): Transform3D =
        Transform3D(
            basis = Basis(
                x = Vector3(GodotReal.readIndex(src, 0), GodotReal.readIndex(src, 3), GodotReal.readIndex(src, 6)),
                y = Vector3(GodotReal.readIndex(src, 1), GodotReal.readIndex(src, 4), GodotReal.readIndex(src, 7)),
                z = Vector3(GodotReal.readIndex(src, 2), GodotReal.readIndex(src, 5), GodotReal.readIndex(src, 8)),
            ),
            origin = Vector3(GodotReal.readIndex(src, 9), GodotReal.readIndex(src, 10), GodotReal.readIndex(src, 11)),
        )

    private fun writeTransform3D(dest: MemorySegment, value: Transform3D) {
        GodotReal.writeIndex(dest, 0, value.basis.x.x)
        GodotReal.writeIndex(dest, 1, value.basis.y.x)
        GodotReal.writeIndex(dest, 2, value.basis.z.x)
        GodotReal.writeIndex(dest, 3, value.basis.x.y)
        GodotReal.writeIndex(dest, 4, value.basis.y.y)
        GodotReal.writeIndex(dest, 5, value.basis.z.y)
        GodotReal.writeIndex(dest, 6, value.basis.x.z)
        GodotReal.writeIndex(dest, 7, value.basis.y.z)
        GodotReal.writeIndex(dest, 8, value.basis.z.z)
        GodotReal.writeIndex(dest, 9, value.origin.x)
        GodotReal.writeIndex(dest, 10, value.origin.y)
        GodotReal.writeIndex(dest, 11, value.origin.z)
    }

    private fun readProjection(src: MemorySegment): Projection =
        Projection(
            x = Vector4(
                GodotReal.readIndex(src, 0),
                GodotReal.readIndex(src, 1),
                GodotReal.readIndex(src, 2),
                GodotReal.readIndex(src, 3),
            ),
            y = Vector4(
                GodotReal.readIndex(src, 4),
                GodotReal.readIndex(src, 5),
                GodotReal.readIndex(src, 6),
                GodotReal.readIndex(src, 7),
            ),
            z = Vector4(
                GodotReal.readIndex(src, 8),
                GodotReal.readIndex(src, 9),
                GodotReal.readIndex(src, 10),
                GodotReal.readIndex(src, 11),
            ),
            w = Vector4(
                GodotReal.readIndex(src, 12),
                GodotReal.readIndex(src, 13),
                GodotReal.readIndex(src, 14),
                GodotReal.readIndex(src, 15),
            ),
        )

    fun ptrcallWithDoubleArgRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Double,
    ): Int {
        Arena.ofConfined().use { arena ->
            val arg = arena.allocate(JAVA_DOUBLE)
            arg.set(JAVA_DOUBLE, 0, value)
            val args = arena.allocate(ADDRESS, 1)
            args.setAtIndex(ADDRESS, 0, arg)
            val ret = arena.allocate(JAVA_INT)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(JAVA_INT, 0)
        }
    }

    fun ptrcallWithThreeDoubleArgsRetDouble(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Double,
        second: Double,
        third: Double,
    ): Double {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_DOUBLE)
            val arg1 = arena.allocate(JAVA_DOUBLE)
            val arg2 = arena.allocate(JAVA_DOUBLE)
            arg0.set(JAVA_DOUBLE, 0, first)
            arg1.set(JAVA_DOUBLE, 0, second)
            arg2.set(JAVA_DOUBLE, 0, third)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, arg0)
            args.setAtIndex(ADDRESS, 1, arg1)
            args.setAtIndex(ADDRESS, 2, arg2)
            val ret = arena.allocate(JAVA_DOUBLE)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(JAVA_DOUBLE, 0)
        }
    }

    fun ptrcallWithThreeDoubleBoolArgsRetDouble(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Double,
        second: Double,
        third: Double,
        enabled: Boolean,
    ): Double {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_DOUBLE)
            val arg1 = arena.allocate(JAVA_DOUBLE)
            val arg2 = arena.allocate(JAVA_DOUBLE)
            val arg3 = arena.allocate(JAVA_BYTE)
            arg0.set(JAVA_DOUBLE, 0, first)
            arg1.set(JAVA_DOUBLE, 0, second)
            arg2.set(JAVA_DOUBLE, 0, third)
            arg3.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 4)
            args.setAtIndex(ADDRESS, 0, arg0)
            args.setAtIndex(ADDRESS, 1, arg1)
            args.setAtIndex(ADDRESS, 2, arg2)
            args.setAtIndex(ADDRESS, 3, arg3)
            val ret = arena.allocate(JAVA_DOUBLE)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(JAVA_DOUBLE, 0)
        }
    }

    fun ptrcallWithLongAndTwoStringNameArgsRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Long,
        first: String,
        second: String,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_LONG)
            arg0.set(JAVA_LONG, 0, value)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, arg0)
            args.setAtIndex(ADDRESS, 1, GodotStrings.makeStringName(first))
            args.setAtIndex(ADDRESS, 2, GodotStrings.makeStringName(second))
            val ret = arena.allocate(JAVA_BYTE)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(JAVA_BYTE, 0) != 0.toByte()
        }
    }

    fun ptrcallWithLongAndThreeStringNameArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Long,
        first: String,
        second: String,
        third: String,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_LONG)
            arg0.set(JAVA_LONG, 0, value)
            val args = arena.allocate(ADDRESS, 4)
            args.setAtIndex(ADDRESS, 0, arg0)
            args.setAtIndex(ADDRESS, 1, GodotStrings.makeStringName(first))
            args.setAtIndex(ADDRESS, 2, GodotStrings.makeStringName(second))
            args.setAtIndex(ADDRESS, 3, GodotStrings.makeStringName(third))
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithTwoStringAndIntArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: String,
        second: String,
        value: Int,
    ) {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(8L, 8L)
            val arg1 = arena.allocate(8L, 8L)
            val arg2 = arena.allocate(JAVA_INT)
            try {
                GodotStrings.initString(arg0, first)
                GodotStrings.initString(arg1, second)
                arg2.set(JAVA_INT, 0, value)
                val args = arena.allocate(ADDRESS, 3)
                args.setAtIndex(ADDRESS, 0, arg0)
                args.setAtIndex(ADDRESS, 1, arg1)
                args.setAtIndex(ADDRESS, 2, arg2)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(arg0)
                GodotStrings.destroyString(arg1)
            }
        }
    }

    fun ptrcallWithStringThreeIntLongArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        first: Int,
        second: Int,
        third: Int,
        value: Long,
    ) {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val arg1 = arena.allocate(JAVA_INT)
            val arg2 = arena.allocate(JAVA_INT)
            val arg3 = arena.allocate(JAVA_INT)
            val arg4 = arena.allocate(JAVA_LONG)
            try {
                GodotStrings.initString(stringCell, text)
                arg1.set(JAVA_INT, 0, first)
                arg2.set(JAVA_INT, 0, second)
                arg3.set(JAVA_INT, 0, third)
                arg4.set(JAVA_LONG, 0, value)
                val args = arena.allocate(ADDRESS, 5)
                args.setAtIndex(ADDRESS, 0, stringCell)
                args.setAtIndex(ADDRESS, 1, arg1)
                args.setAtIndex(ADDRESS, 2, arg2)
                args.setAtIndex(ADDRESS, 3, arg3)
                args.setAtIndex(ADDRESS, 4, arg4)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithStringLongBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        value: Long,
        enabled: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val longCell = arena.allocate(JAVA_LONG)
            val boolCell = arena.allocate(JAVA_BYTE)
            try {
                GodotStrings.initString(stringCell, text)
                longCell.set(JAVA_LONG, 0, value)
                boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
                val args = arena.allocate(ADDRESS, 3)
                args.setAtIndex(ADDRESS, 0, stringCell)
                args.setAtIndex(ADDRESS, 1, longCell)
                args.setAtIndex(ADDRESS, 2, boolCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithLongAndTwoStringArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Long,
        first: String,
        second: String,
    ) {
        Arena.ofConfined().use { arena ->
            val longCell = arena.allocate(JAVA_LONG)
            val arg1 = arena.allocate(8L, 8L)
            val arg2 = arena.allocate(8L, 8L)
            try {
                longCell.set(JAVA_LONG, 0, value)
                GodotStrings.initString(arg1, first)
                GodotStrings.initString(arg2, second)
                val args = arena.allocate(ADDRESS, 3)
                args.setAtIndex(ADDRESS, 0, longCell)
                args.setAtIndex(ADDRESS, 1, arg1)
                args.setAtIndex(ADDRESS, 2, arg2)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(arg1)
                GodotStrings.destroyString(arg2)
            }
        }
    }

    fun ptrcallWithRIDAndStringArgRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        text: String,
    ): Int {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val stringCell = arena.allocate(8L, 8L)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                GodotStrings.initString(stringCell, text)
                val args = arena.allocate(ADDRESS, 2)
                args.setAtIndex(ADDRESS, 0, ridCell)
                args.setAtIndex(ADDRESS, 1, stringCell)
                val ret = arena.allocate(JAVA_INT)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(JAVA_INT, 0)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithTwoRIDArgsRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: RID,
        second: RID,
    ): Int {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_LONG)
            val arg1 = arena.allocate(JAVA_LONG)
            arg0.set(JAVA_LONG, 0, first.value)
            arg1.set(JAVA_LONG, 0, second.value)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, arg0)
            args.setAtIndex(ADDRESS, 1, arg1)
            val ret = arena.allocate(JAVA_INT)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(JAVA_INT, 0)
        }
    }

    fun ptrcallWithRIDAndIntArgRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Int,
    ): Long {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val intCell = arena.allocate(JAVA_INT)
            ridCell.set(JAVA_LONG, 0, rid.value)
            intCell.set(JAVA_INT, 0, value)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, intCell)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(JAVA_LONG, 0)
        }
    }

    fun ptrcallWithRIDIntLongArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        index: Int,
        value: Long,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val intCell = arena.allocate(JAVA_INT)
            val longCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            intCell.set(JAVA_INT, 0, index)
            longCell.set(JAVA_LONG, 0, value)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, intCell)
            args.setAtIndex(ADDRESS, 2, longCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDIntAndObjectArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        index: Int,
        objectArg: MemorySegment,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val intCell = arena.allocate(JAVA_INT)
            val objectCell = arena.allocate(ADDRESS)
            ridCell.set(JAVA_LONG, 0, rid.value)
            intCell.set(JAVA_INT, 0, index)
            objectCell.set(ADDRESS, 0, objectArg)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, intCell)
            args.setAtIndex(ADDRESS, 2, objectCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithTwoIntBoolLongByteArrayArgsRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Int,
        boolArg: Boolean,
        mode: Long,
        bytes: ByteArray,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_INT)
            val arg1 = arena.allocate(JAVA_INT)
            val arg2 = arena.allocate(JAVA_BYTE)
            val arg3 = arena.allocate(JAVA_LONG)
            val arg4 = BuiltinTypes.allocatePackedArray(arena)
            try {
                arg0.set(JAVA_INT, 0, first)
                arg1.set(JAVA_INT, 0, second)
                arg2.set(JAVA_BYTE, 0, if (boolArg) 1.toByte() else 0.toByte())
                arg3.set(JAVA_LONG, 0, mode)
                BuiltinTypes.initPackedByteArray(arg4, bytes)
                val args = arena.allocate(ADDRESS, 5)
                args.setAtIndex(ADDRESS, 0, arg0)
                args.setAtIndex(ADDRESS, 1, arg1)
                args.setAtIndex(ADDRESS, 2, arg2)
                args.setAtIndex(ADDRESS, 3, arg3)
                args.setAtIndex(ADDRESS, 4, arg4)
                val ret = arena.allocate(ADDRESS)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(ADDRESS, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, arg4)
            }
        }
    }

    fun ptrcallWithStringPackedStringListBoolArgsRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        values: List<String>,
        enabled: Boolean,
    ): Int {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val packedCell = BuiltinTypes.allocatePackedArray(arena)
            val boolCell = arena.allocate(JAVA_BYTE)
            try {
                GodotStrings.initString(stringCell, text)
                BuiltinTypes.initPackedStringArray(packedCell, values)
                boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
                val args = arena.allocate(ADDRESS, 3)
                args.setAtIndex(ADDRESS, 0, stringCell)
                args.setAtIndex(ADDRESS, 1, packedCell)
                args.setAtIndex(ADDRESS, 2, boolCell)
                val ret = arena.allocate(JAVA_INT)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(JAVA_INT, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_STRING_ARRAY, packedCell)
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithStringPackedStringListBoolArgsRetDictionary(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        values: List<String>,
        enabled: Boolean,
    ): Map<String, Any?> {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val packedCell = BuiltinTypes.allocatePackedArray(arena)
            val boolCell = arena.allocate(JAVA_BYTE)
            val ret = arena.allocate(8L, 8L)
            try {
                GodotStrings.initString(stringCell, text)
                BuiltinTypes.initPackedStringArray(packedCell, values)
                boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
                val args = arena.allocate(ADDRESS, 3)
                args.setAtIndex(ADDRESS, 0, stringCell)
                args.setAtIndex(ADDRESS, 1, packedCell)
                args.setAtIndex(ADDRESS, 2, boolCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return BuiltinTypes.readDictionaryScalars(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.DICTIONARY, ret)
                BuiltinTypes.destroyTyped(VariantType.PACKED_STRING_ARRAY, packedCell)
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithByteArrayTwoStringArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        bytes: ByteArray,
        first: String,
        second: String,
    ) {
        Arena.ofConfined().use { arena ->
            val bytesCell = BuiltinTypes.allocatePackedArray(arena)
            val firstCell = arena.allocate(8L, 8L)
            val secondCell = arena.allocate(8L, 8L)
            try {
                BuiltinTypes.initPackedByteArray(bytesCell, bytes)
                GodotStrings.initString(firstCell, first)
                GodotStrings.initString(secondCell, second)
                val args = arena.allocate(ADDRESS, 3)
                args.setAtIndex(ADDRESS, 0, bytesCell)
                args.setAtIndex(ADDRESS, 1, firstCell)
                args.setAtIndex(ADDRESS, 2, secondCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(secondCell)
                GodotStrings.destroyString(firstCell)
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, bytesCell)
            }
        }
    }

    fun ptrcallWithObjectAndStringArgRetByteArray(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        text: String,
    ): ByteArray {
        Arena.ofConfined().use { arena ->
            val objectCell = arena.allocate(ADDRESS)
            val stringCell = arena.allocate(8L, 8L)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            try {
                objectCell.set(ADDRESS, 0, objectArg)
                GodotStrings.initString(stringCell, text)
                val args = arena.allocate(ADDRESS, 2)
                args.setAtIndex(ADDRESS, 0, objectCell)
                args.setAtIndex(ADDRESS, 1, stringCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return BuiltinTypes.readPackedByteArray(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, ret)
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithRIDAndTwoLongArgsRetByteArray(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        first: Long,
        second: Long,
    ): ByteArray {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val firstCell = arena.allocate(JAVA_LONG)
            val secondCell = arena.allocate(JAVA_LONG)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                firstCell.set(JAVA_LONG, 0, first)
                secondCell.set(JAVA_LONG, 0, second)
                val args = arena.allocate(ADDRESS, 3)
                args.setAtIndex(ADDRESS, 0, ridCell)
                args.setAtIndex(ADDRESS, 1, firstCell)
                args.setAtIndex(ADDRESS, 2, secondCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return BuiltinTypes.readPackedByteArray(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, ret)
            }
        }
    }

    fun ptrcallWithByteArrayAndRIDArgRetRID(
        methodBind: MemorySegment,
        instance: MemorySegment,
        bytes: ByteArray,
        rid: RID,
    ): RID {
        Arena.ofConfined().use { arena ->
            val bytesCell = BuiltinTypes.allocatePackedArray(arena)
            val ridCell = arena.allocate(JAVA_LONG)
            try {
                BuiltinTypes.initPackedByteArray(bytesCell, bytes)
                ridCell.set(JAVA_LONG, 0, rid.value)
                val args = arena.allocate(ADDRESS, 2)
                args.setAtIndex(ADDRESS, 0, bytesCell)
                args.setAtIndex(ADDRESS, 1, ridCell)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return RID(ret.get(JAVA_LONG, 0))
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, bytesCell)
            }
        }
    }

    fun ptrcallWithRIDLongByteArrayArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Long,
        bytes: ByteArray,
    ): Long {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val longCell = arena.allocate(JAVA_LONG)
            val bytesCell = BuiltinTypes.allocatePackedArray(arena)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                longCell.set(JAVA_LONG, 0, value)
                BuiltinTypes.initPackedByteArray(bytesCell, bytes)
                val args = arena.allocate(ADDRESS, 3)
                args.setAtIndex(ADDRESS, 0, ridCell)
                args.setAtIndex(ADDRESS, 1, longCell)
                args.setAtIndex(ADDRESS, 2, bytesCell)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, bytesCell)
            }
        }
    }

    fun ptrcallWithLongByteArrayObjectArgsRetByteArray(
        methodBind: MemorySegment,
        instance: MemorySegment,
        mode: Long,
        bytes: ByteArray,
        objectArg: MemorySegment,
    ): ByteArray {
        Arena.ofConfined().use { arena ->
            val modeCell = arena.allocate(JAVA_LONG)
            val bytesCell = BuiltinTypes.allocatePackedArray(arena)
            val objectCell = arena.allocate(ADDRESS)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            try {
                modeCell.set(JAVA_LONG, 0, mode)
                BuiltinTypes.initPackedByteArray(bytesCell, bytes)
                objectCell.set(ADDRESS, 0, objectArg)
                val args = arena.allocate(ADDRESS, 3)
                args.setAtIndex(ADDRESS, 0, modeCell)
                args.setAtIndex(ADDRESS, 1, bytesCell)
                args.setAtIndex(ADDRESS, 2, objectCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return BuiltinTypes.readPackedByteArray(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, ret)
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, bytesCell)
            }
        }
    }

    fun ptrcallWithLongTwoByteArrayObjectArgsRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        mode: Long,
        first: ByteArray,
        second: ByteArray,
        objectArg: MemorySegment,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val modeCell = arena.allocate(JAVA_LONG)
            val firstCell = BuiltinTypes.allocatePackedArray(arena)
            val secondCell = BuiltinTypes.allocatePackedArray(arena)
            val objectCell = arena.allocate(ADDRESS)
            try {
                modeCell.set(JAVA_LONG, 0, mode)
                BuiltinTypes.initPackedByteArray(firstCell, first)
                BuiltinTypes.initPackedByteArray(secondCell, second)
                objectCell.set(ADDRESS, 0, objectArg)
                val args = arena.allocate(ADDRESS, 4)
                args.setAtIndex(ADDRESS, 0, modeCell)
                args.setAtIndex(ADDRESS, 1, firstCell)
                args.setAtIndex(ADDRESS, 2, secondCell)
                args.setAtIndex(ADDRESS, 3, objectCell)
                val ret = arena.allocate(JAVA_BYTE)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(JAVA_BYTE, 0) != 0.toByte()
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, secondCell)
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, firstCell)
            }
        }
    }

    fun ptrcallWithTwoVector3PlaneListArgsRetPackedVector3List(
        methodBind: MemorySegment,
        instance: MemorySegment,
        from: Vector3,
        to: Vector3,
        planes: List<Plane>,
    ): List<Vector3> {
        Arena.ofConfined().use { arena ->
            val fromCell = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            val toCell = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            val planeArray = arena.allocate(8L, 8L)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            try {
                GodotReal.writeIndex(fromCell, 0, from.x)
                GodotReal.writeIndex(fromCell, 1, from.y)
                GodotReal.writeIndex(fromCell, 2, from.z)
                GodotReal.writeIndex(toCell, 0, to.x)
                GodotReal.writeIndex(toCell, 1, to.y)
                GodotReal.writeIndex(toCell, 2, to.z)
                BuiltinTypes.initArray(planeArray, planes.map { it as Any? })
                val args = arena.allocate(ADDRESS, 3)
                args.setAtIndex(ADDRESS, 0, fromCell)
                args.setAtIndex(ADDRESS, 1, toCell)
                args.setAtIndex(ADDRESS, 2, planeArray)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return BuiltinTypes.readPackedVector3Array(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR3_ARRAY, ret)
                BuiltinTypes.destroyTyped(VariantType.ARRAY, planeArray)
            }
        }
    }

    fun ptrcallWithPackedVector3ListAndPlaneArgRetPackedVector3List(
        methodBind: MemorySegment,
        instance: MemorySegment,
        points: List<Vector3>,
        plane: Plane,
    ): List<Vector3> {
        Arena.ofConfined().use { arena ->
            val pointsCell = BuiltinTypes.allocatePackedArray(arena)
            val planeCell = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            try {
                BuiltinTypes.initPackedVector3Array(pointsCell, points)
                GodotReal.writeIndex(planeCell, 0, plane.normal.x)
                GodotReal.writeIndex(planeCell, 1, plane.normal.y)
                GodotReal.writeIndex(planeCell, 2, plane.normal.z)
                GodotReal.writeIndex(planeCell, 3, plane.d)
                val args = arena.allocate(ADDRESS, 2)
                args.setAtIndex(ADDRESS, 0, pointsCell)
                args.setAtIndex(ADDRESS, 1, planeCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return BuiltinTypes.readPackedVector3Array(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR3_ARRAY, ret)
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR3_ARRAY, pointsCell)
            }
        }
    }

    fun ptrcallWithRIDTwoVector2BoolLongArgsRetPackedVector2List(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        from: Vector2,
        to: Vector2,
        optimize: Boolean,
        layers: Long,
    ): List<Vector2> {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val fromCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val toCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val boolCell = arena.allocate(JAVA_BYTE)
            val longCell = arena.allocate(JAVA_LONG)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                GodotReal.writeIndex(fromCell, 0, from.x)
                GodotReal.writeIndex(fromCell, 1, from.y)
                GodotReal.writeIndex(toCell, 0, to.x)
                GodotReal.writeIndex(toCell, 1, to.y)
                boolCell.set(JAVA_BYTE, 0, if (optimize) 1.toByte() else 0.toByte())
                longCell.set(JAVA_LONG, 0, layers)
                val args = arena.allocate(ADDRESS, 5)
                args.setAtIndex(ADDRESS, 0, ridCell)
                args.setAtIndex(ADDRESS, 1, fromCell)
                args.setAtIndex(ADDRESS, 2, toCell)
                args.setAtIndex(ADDRESS, 3, boolCell)
                args.setAtIndex(ADDRESS, 4, longCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return BuiltinTypes.readPackedVector2Array(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR2_ARRAY, ret)
            }
        }
    }

    fun ptrcallWithRIDTwoVector3BoolLongArgsRetPackedVector3List(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        from: Vector3,
        to: Vector3,
        optimize: Boolean,
        layers: Long,
    ): List<Vector3> {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val fromCell = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            val toCell = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            val boolCell = arena.allocate(JAVA_BYTE)
            val longCell = arena.allocate(JAVA_LONG)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                GodotReal.writeIndex(fromCell, 0, from.x)
                GodotReal.writeIndex(fromCell, 1, from.y)
                GodotReal.writeIndex(fromCell, 2, from.z)
                GodotReal.writeIndex(toCell, 0, to.x)
                GodotReal.writeIndex(toCell, 1, to.y)
                GodotReal.writeIndex(toCell, 2, to.z)
                boolCell.set(JAVA_BYTE, 0, if (optimize) 1.toByte() else 0.toByte())
                longCell.set(JAVA_LONG, 0, layers)
                val args = arena.allocate(ADDRESS, 5)
                args.setAtIndex(ADDRESS, 0, ridCell)
                args.setAtIndex(ADDRESS, 1, fromCell)
                args.setAtIndex(ADDRESS, 2, toCell)
                args.setAtIndex(ADDRESS, 3, boolCell)
                args.setAtIndex(ADDRESS, 4, longCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return BuiltinTypes.readPackedVector3Array(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR3_ARRAY, ret)
            }
        }
    }

    fun ptrcallWithRIDTwoVector2BoolUInt32ArgsRetPackedVector2List(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        from: Vector2,
        to: Vector2,
        optimize: Boolean,
        layers: Long,
    ): List<Vector2> {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val fromCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val toCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val boolCell = arena.allocate(JAVA_BYTE)
            val layersCell = arena.allocate(JAVA_INT)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                GodotReal.writeIndex(fromCell, 0, from.x)
                GodotReal.writeIndex(fromCell, 1, from.y)
                GodotReal.writeIndex(toCell, 0, to.x)
                GodotReal.writeIndex(toCell, 1, to.y)
                boolCell.set(JAVA_BYTE, 0, if (optimize) 1.toByte() else 0.toByte())
                layersCell.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(layers))
                val args = arena.allocate(ADDRESS, 5)
                args.setAtIndex(ADDRESS, 0, ridCell)
                args.setAtIndex(ADDRESS, 1, fromCell)
                args.setAtIndex(ADDRESS, 2, toCell)
                args.setAtIndex(ADDRESS, 3, boolCell)
                args.setAtIndex(ADDRESS, 4, layersCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return BuiltinTypes.readPackedVector2Array(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR2_ARRAY, ret)
            }
        }
    }

    fun ptrcallWithRIDTwoVector3BoolUInt32ArgsRetPackedVector3List(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        from: Vector3,
        to: Vector3,
        optimize: Boolean,
        layers: Long,
    ): List<Vector3> {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val fromCell = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            val toCell = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            val boolCell = arena.allocate(JAVA_BYTE)
            val layersCell = arena.allocate(JAVA_INT)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                GodotReal.writeIndex(fromCell, 0, from.x)
                GodotReal.writeIndex(fromCell, 1, from.y)
                GodotReal.writeIndex(fromCell, 2, from.z)
                GodotReal.writeIndex(toCell, 0, to.x)
                GodotReal.writeIndex(toCell, 1, to.y)
                GodotReal.writeIndex(toCell, 2, to.z)
                boolCell.set(JAVA_BYTE, 0, if (optimize) 1.toByte() else 0.toByte())
                layersCell.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(layers))
                val args = arena.allocate(ADDRESS, 5)
                args.setAtIndex(ADDRESS, 0, ridCell)
                args.setAtIndex(ADDRESS, 1, fromCell)
                args.setAtIndex(ADDRESS, 2, toCell)
                args.setAtIndex(ADDRESS, 3, boolCell)
                args.setAtIndex(ADDRESS, 4, layersCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return BuiltinTypes.readPackedVector3Array(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR3_ARRAY, ret)
            }
        }
    }

    fun ptrcallWithTransform2DObjectTransform2DArgsRetPackedVector2List(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Transform2D,
        objectArg: MemorySegment,
        second: Transform2D,
    ): List<Vector2> {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(GodotReal.SIZE_BYTES * 6, GodotReal.ALIGN_BYTES)
            val objectCell = arena.allocate(ADDRESS)
            val secondCell = arena.allocate(GodotReal.SIZE_BYTES * 6, GodotReal.ALIGN_BYTES)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            try {
                writeTransform2D(firstCell, first)
                objectCell.set(ADDRESS, 0, objectArg)
                writeTransform2D(secondCell, second)
                val args = arena.allocate(ADDRESS, 3)
                args.setAtIndex(ADDRESS, 0, firstCell)
                args.setAtIndex(ADDRESS, 1, objectCell)
                args.setAtIndex(ADDRESS, 2, secondCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return BuiltinTypes.readPackedVector2Array(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR2_ARRAY, ret)
            }
        }
    }

    fun ptrcallWithTransform2DObjectTransform2DArgsRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Transform2D,
        objectArg: MemorySegment,
        second: Transform2D,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(GodotReal.SIZE_BYTES * 6, GodotReal.ALIGN_BYTES)
            val objectCell = arena.allocate(ADDRESS)
            val secondCell = arena.allocate(GodotReal.SIZE_BYTES * 6, GodotReal.ALIGN_BYTES)
            writeTransform2D(firstCell, first)
            objectCell.set(ADDRESS, 0, objectArg)
            writeTransform2D(secondCell, second)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, firstCell)
            args.setAtIndex(ADDRESS, 1, objectCell)
            args.setAtIndex(ADDRESS, 2, secondCell)
            val ret = arena.allocate(JAVA_BYTE)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(JAVA_BYTE, 0) != 0.toByte()
        }
    }

    fun ptrcallWithTransform2DVector2ObjectTransform2DVector2ArgsRetPackedVector2List(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstTransform: Transform2D,
        firstMotion: Vector2,
        objectArg: MemorySegment,
        secondTransform: Transform2D,
        secondMotion: Vector2,
    ): List<Vector2> {
        Arena.ofConfined().use { arena ->
            val transform0 = arena.allocate(GodotReal.SIZE_BYTES * 6, GodotReal.ALIGN_BYTES)
            val motion0 = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val objectCell = arena.allocate(ADDRESS)
            val transform1 = arena.allocate(GodotReal.SIZE_BYTES * 6, GodotReal.ALIGN_BYTES)
            val motion1 = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            try {
                writeTransform2D(transform0, firstTransform)
                GodotReal.writeIndex(motion0, 0, firstMotion.x)
                GodotReal.writeIndex(motion0, 1, firstMotion.y)
                objectCell.set(ADDRESS, 0, objectArg)
                writeTransform2D(transform1, secondTransform)
                GodotReal.writeIndex(motion1, 0, secondMotion.x)
                GodotReal.writeIndex(motion1, 1, secondMotion.y)
                val args = arena.allocate(ADDRESS, 5)
                args.setAtIndex(ADDRESS, 0, transform0)
                args.setAtIndex(ADDRESS, 1, motion0)
                args.setAtIndex(ADDRESS, 2, objectCell)
                args.setAtIndex(ADDRESS, 3, transform1)
                args.setAtIndex(ADDRESS, 4, motion1)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return BuiltinTypes.readPackedVector2Array(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR2_ARRAY, ret)
            }
        }
    }

    fun ptrcallWithTransform2DVector2ObjectTransform2DVector2ArgsRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstTransform: Transform2D,
        firstMotion: Vector2,
        objectArg: MemorySegment,
        secondTransform: Transform2D,
        secondMotion: Vector2,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val transform0 = arena.allocate(GodotReal.SIZE_BYTES * 6, GodotReal.ALIGN_BYTES)
            val motion0 = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val objectCell = arena.allocate(ADDRESS)
            val transform1 = arena.allocate(GodotReal.SIZE_BYTES * 6, GodotReal.ALIGN_BYTES)
            val motion1 = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            writeTransform2D(transform0, firstTransform)
            writeVector2(motion0, firstMotion)
            objectCell.set(ADDRESS, 0, objectArg)
            writeTransform2D(transform1, secondTransform)
            writeVector2(motion1, secondMotion)
            val args = arena.allocate(ADDRESS, 5)
            args.setAtIndex(ADDRESS, 0, transform0)
            args.setAtIndex(ADDRESS, 1, motion0)
            args.setAtIndex(ADDRESS, 2, objectCell)
            args.setAtIndex(ADDRESS, 3, transform1)
            args.setAtIndex(ADDRESS, 4, motion1)
            val ret = arena.allocate(JAVA_BYTE)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(JAVA_BYTE, 0) != 0.toByte()
        }
    }

    fun ptrcallWithVector2PackedVector2ListArgsRetRID(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Vector2,
        points: List<Vector2>,
    ): RID {
        Arena.ofConfined().use { arena ->
            val vectorCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val pointsCell = BuiltinTypes.allocatePackedArray(arena)
            try {
                GodotReal.writeIndex(vectorCell, 0, value.x)
                GodotReal.writeIndex(vectorCell, 1, value.y)
                BuiltinTypes.initPackedVector2Array(pointsCell, points)
                val args = arena.allocate(ADDRESS, 2)
                args.setAtIndex(ADDRESS, 0, vectorCell)
                args.setAtIndex(ADDRESS, 1, pointsCell)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return RID(ret.get(JAVA_LONG, 0))
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR2_ARRAY, pointsCell)
            }
        }
    }

    fun ptrcallWithNodePathAndPackedFloat32ListArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        path: NodePath,
        values: List<Float>,
    ) {
        Arena.ofConfined().use { arena ->
            val nodePath = arena.allocate(8L, 8L)
            val pathString = arena.allocate(8L, 8L)
            val packedCell = BuiltinTypes.allocatePackedArray(arena)
            try {
                GodotStrings.initString(pathString, path.path)
                BuiltinTypes.construct(
                    type = VariantType.NODE_PATH,
                    dest = nodePath,
                    constructorIndex = 2,
                    args = listOf(pathString),
                )
                BuiltinTypes.initPackedFloat32Array(packedCell, values)
                val args = arena.allocate(ADDRESS, 2)
                args.setAtIndex(ADDRESS, 0, nodePath)
                args.setAtIndex(ADDRESS, 1, packedCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_FLOAT32_ARRAY, packedCell)
                BuiltinTypes.destroyTyped(VariantType.NODE_PATH, nodePath)
                GodotStrings.destroyString(pathString)
            }
        }
    }

    fun ptrcallWithTwoDoubleIntLongArgsRetPlaneList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Double,
        second: Double,
        count: Int,
        axis: Long,
    ): List<Plane> {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_DOUBLE)
            val arg1 = arena.allocate(JAVA_DOUBLE)
            val arg2 = arena.allocate(JAVA_INT)
            val arg3 = arena.allocate(JAVA_LONG)
            arg0.set(JAVA_DOUBLE, 0, first)
            arg1.set(JAVA_DOUBLE, 0, second)
            arg2.set(JAVA_INT, 0, count)
            arg3.set(JAVA_LONG, 0, axis)
            val args = arena.allocate(ADDRESS, 4)
            args.setAtIndex(ADDRESS, 0, arg0)
            args.setAtIndex(ADDRESS, 1, arg1)
            args.setAtIndex(ADDRESS, 2, arg2)
            args.setAtIndex(ADDRESS, 3, arg3)
            return callArrayReturn(methodBind, instance, args, BuiltinTypes::readArrayPlanes)
        }
    }

    fun ptrcallWithTwoDoubleTwoIntLongArgsRetPlaneList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Double,
        second: Double,
        firstCount: Int,
        secondCount: Int,
        axis: Long,
    ): List<Plane> {
        Arena.ofConfined().use { arena ->
            val arg0 = arena.allocate(JAVA_DOUBLE)
            val arg1 = arena.allocate(JAVA_DOUBLE)
            val arg2 = arena.allocate(JAVA_INT)
            val arg3 = arena.allocate(JAVA_INT)
            val arg4 = arena.allocate(JAVA_LONG)
            arg0.set(JAVA_DOUBLE, 0, first)
            arg1.set(JAVA_DOUBLE, 0, second)
            arg2.set(JAVA_INT, 0, firstCount)
            arg3.set(JAVA_INT, 0, secondCount)
            arg4.set(JAVA_LONG, 0, axis)
            val args = arena.allocate(ADDRESS, 5)
            args.setAtIndex(ADDRESS, 0, arg0)
            args.setAtIndex(ADDRESS, 1, arg1)
            args.setAtIndex(ADDRESS, 2, arg2)
            args.setAtIndex(ADDRESS, 3, arg3)
            args.setAtIndex(ADDRESS, 4, arg4)
            return callArrayReturn(methodBind, instance, args, BuiltinTypes::readArrayPlanes)
        }
    }

    fun ptrcallWithObjectAndIntArgRetVector2List(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        count: Int,
    ): List<Vector2> {
        Arena.ofConfined().use { arena ->
            val objectCell = arena.allocate(ADDRESS)
            val intCell = arena.allocate(JAVA_INT)
            objectCell.set(ADDRESS, 0, objectArg)
            intCell.set(JAVA_INT, 0, count)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, objectCell)
            args.setAtIndex(ADDRESS, 1, intCell)
            return callArrayReturn(methodBind, instance, args, BuiltinTypes::readArrayVector2)
        }
    }

    fun ptrcallWithObjectAndIntArgRetVector3List(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        count: Int,
    ): List<Vector3> {
        Arena.ofConfined().use { arena ->
            val objectCell = arena.allocate(ADDRESS)
            val intCell = arena.allocate(JAVA_INT)
            objectCell.set(ADDRESS, 0, objectArg)
            intCell.set(JAVA_INT, 0, count)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, objectCell)
            args.setAtIndex(ADDRESS, 1, intCell)
            return callArrayReturn(methodBind, instance, args, BuiltinTypes::readArrayVector3)
        }
    }

    fun ptrcallWithTwoVector2LongRIDListArgsRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        from: Vector2,
        to: Vector2,
        mask: Long,
        exclude: List<RID>,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val fromCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val toCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val maskCell = arena.allocate(JAVA_LONG)
            val excludeCell = arena.allocate(8L, 8L)
            try {
                GodotReal.writeIndex(fromCell, 0, from.x)
                GodotReal.writeIndex(fromCell, 1, from.y)
                GodotReal.writeIndex(toCell, 0, to.x)
                GodotReal.writeIndex(toCell, 1, to.y)
                maskCell.set(JAVA_LONG, 0, mask)
                BuiltinTypes.initArrayOfRids(excludeCell, exclude)
                val args = arena.allocate(ADDRESS, 4)
                args.setAtIndex(ADDRESS, 0, fromCell)
                args.setAtIndex(ADDRESS, 1, toCell)
                args.setAtIndex(ADDRESS, 2, maskCell)
                args.setAtIndex(ADDRESS, 3, excludeCell)
                val ret = arena.allocate(ADDRESS)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(ADDRESS, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, excludeCell)
            }
        }
    }

    fun ptrcallWithTwoVector2UInt32RIDListArgsRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        from: Vector2,
        to: Vector2,
        mask: Long,
        exclude: List<RID>,
    ): MemorySegment {
        val checkedMask = BuiltinTypes.requireUInt32(mask)
        Arena.ofConfined().use { arena ->
            val fromCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val toCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val maskCell = arena.allocate(JAVA_INT)
            val excludeCell = arena.allocate(8L, 8L)
            try {
                GodotReal.writeIndex(fromCell, 0, from.x)
                GodotReal.writeIndex(fromCell, 1, from.y)
                GodotReal.writeIndex(toCell, 0, to.x)
                GodotReal.writeIndex(toCell, 1, to.y)
                maskCell.set(JAVA_INT, 0, checkedMask)
                BuiltinTypes.initArrayOfRids(excludeCell, exclude)
                val args = arena.allocate(ADDRESS, 4)
                args.setAtIndex(ADDRESS, 0, fromCell)
                args.setAtIndex(ADDRESS, 1, toCell)
                args.setAtIndex(ADDRESS, 2, maskCell)
                args.setAtIndex(ADDRESS, 3, excludeCell)
                val ret = arena.allocate(ADDRESS)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(ADDRESS, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, excludeCell)
            }
        }
    }

    fun ptrcallWithTwoVector3LongRIDListArgsRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        from: Vector3,
        to: Vector3,
        mask: Long,
        exclude: List<RID>,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val fromCell = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            val toCell = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            val maskCell = arena.allocate(JAVA_LONG)
            val excludeCell = arena.allocate(8L, 8L)
            try {
                GodotReal.writeIndex(fromCell, 0, from.x)
                GodotReal.writeIndex(fromCell, 1, from.y)
                GodotReal.writeIndex(fromCell, 2, from.z)
                GodotReal.writeIndex(toCell, 0, to.x)
                GodotReal.writeIndex(toCell, 1, to.y)
                GodotReal.writeIndex(toCell, 2, to.z)
                maskCell.set(JAVA_LONG, 0, mask)
                BuiltinTypes.initArrayOfRids(excludeCell, exclude)
                val args = arena.allocate(ADDRESS, 4)
                args.setAtIndex(ADDRESS, 0, fromCell)
                args.setAtIndex(ADDRESS, 1, toCell)
                args.setAtIndex(ADDRESS, 2, maskCell)
                args.setAtIndex(ADDRESS, 3, excludeCell)
                val ret = arena.allocate(ADDRESS)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(ADDRESS, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, excludeCell)
            }
        }
    }

    fun ptrcallWithTwoVector3UInt32RIDListArgsRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        from: Vector3,
        to: Vector3,
        mask: Long,
        exclude: List<RID>,
    ): MemorySegment {
        val checkedMask = BuiltinTypes.requireUInt32(mask)
        Arena.ofConfined().use { arena ->
            val fromCell = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            val toCell = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            val maskCell = arena.allocate(JAVA_INT)
            val excludeCell = arena.allocate(8L, 8L)
            try {
                GodotReal.writeIndex(fromCell, 0, from.x)
                GodotReal.writeIndex(fromCell, 1, from.y)
                GodotReal.writeIndex(fromCell, 2, from.z)
                GodotReal.writeIndex(toCell, 0, to.x)
                GodotReal.writeIndex(toCell, 1, to.y)
                GodotReal.writeIndex(toCell, 2, to.z)
                maskCell.set(JAVA_INT, 0, checkedMask)
                BuiltinTypes.initArrayOfRids(excludeCell, exclude)
                val args = arena.allocate(ADDRESS, 4)
                args.setAtIndex(ADDRESS, 0, fromCell)
                args.setAtIndex(ADDRESS, 1, toCell)
                args.setAtIndex(ADDRESS, 2, maskCell)
                args.setAtIndex(ADDRESS, 3, excludeCell)
                val ret = arena.allocate(ADDRESS)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(ADDRESS, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, excludeCell)
            }
        }
    }

    fun ptrcallWithTwoStringNameIntStringNameArgsRetString(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: String,
        second: String,
        count: Int,
        context: String,
    ): String {
        Arena.ofConfined().use { arena ->
            val countCell = arena.allocate(JAVA_INT)
            val ret = arena.allocate(8L, 8L)
            countCell.set(JAVA_INT, 0, count)
            try {
                val args = arena.allocate(ADDRESS, 4)
                args.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(first))
                args.setAtIndex(ADDRESS, 1, GodotStrings.makeStringName(second))
                args.setAtIndex(ADDRESS, 2, countCell)
                args.setAtIndex(ADDRESS, 3, GodotStrings.makeStringName(context))
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return GodotStrings.readString(ret)
            } finally {
                GodotStrings.destroyString(ret)
            }
        }
    }

    fun ptrcallWithIntAndStringArgRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        intValue: Int,
        text: String,
    ): Long {
        Arena.ofConfined().use { arena ->
            val intCell = arena.allocate(JAVA_INT)
            val stringCell = arena.allocate(8L, 8L)
            try {
                intCell.set(JAVA_INT, 0, intValue)
                GodotStrings.initString(stringCell, text)
                val args = arena.allocate(ADDRESS, 2)
                args.setAtIndex(ADDRESS, 0, intCell)
                args.setAtIndex(ADDRESS, 1, stringCell)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithIntStringAndIntArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstInt: Int,
        text: String,
        secondInt: Int,
    ): Long {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(JAVA_INT)
            val stringCell = arena.allocate(8L, 8L)
            val secondCell = arena.allocate(JAVA_INT)
            try {
                firstCell.set(JAVA_INT, 0, firstInt)
                GodotStrings.initString(stringCell, text)
                secondCell.set(JAVA_INT, 0, secondInt)
                val args = arena.allocate(ADDRESS, 3)
                args.setAtIndex(ADDRESS, 0, firstCell)
                args.setAtIndex(ADDRESS, 1, stringCell)
                args.setAtIndex(ADDRESS, 2, secondCell)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithIntStringObjectArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        intValue: Int,
        text: String,
        objectArg: MemorySegment,
    ): Long {
        Arena.ofConfined().use { arena ->
            val intCell = arena.allocate(JAVA_INT)
            val stringCell = arena.allocate(8L, 8L)
            val objectCell = arena.allocate(ADDRESS)
            try {
                intCell.set(JAVA_INT, 0, intValue)
                GodotStrings.initString(stringCell, text)
                objectCell.set(ADDRESS, 0, objectArg)
                val args = arena.allocate(ADDRESS, 3)
                args.setAtIndex(ADDRESS, 0, intCell)
                args.setAtIndex(ADDRESS, 1, stringCell)
                args.setAtIndex(ADDRESS, 2, objectCell)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithStringStringNameIntStringNameArgsRetString(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        firstName: String,
        index: Int,
        secondName: String,
    ): String {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val indexCell = arena.allocate(JAVA_INT)
            val ret = arena.allocate(8L, 8L)
            try {
                GodotStrings.initString(stringCell, text)
                indexCell.set(JAVA_INT, 0, index)
                val args = arena.allocate(ADDRESS, 4)
                args.setAtIndex(ADDRESS, 0, stringCell)
                args.setAtIndex(ADDRESS, 1, GodotStrings.makeStringName(firstName))
                args.setAtIndex(ADDRESS, 2, indexCell)
                args.setAtIndex(ADDRESS, 3, GodotStrings.makeStringName(secondName))
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return GodotStrings.readString(ret)
            } finally {
                GodotStrings.destroyString(ret)
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithObjectAndStringArgRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        text: String,
    ): Long {
        Arena.ofConfined().use { arena ->
            val objectCell = arena.allocate(ADDRESS)
            val stringCell = arena.allocate(8L, 8L)
            try {
                objectCell.set(ADDRESS, 0, objectArg)
                GodotStrings.initString(stringCell, text)
                val args = arena.allocate(ADDRESS, 2)
                args.setAtIndex(ADDRESS, 0, objectCell)
                args.setAtIndex(ADDRESS, 1, stringCell)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithObjectTwoIntArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        firstInt: Int,
        secondInt: Int,
    ): Long {
        Arena.ofConfined().use { arena ->
            val objectCell = arena.allocate(ADDRESS)
            val firstCell = arena.allocate(JAVA_INT)
            val secondCell = arena.allocate(JAVA_INT)
            objectCell.set(ADDRESS, 0, objectArg)
            firstCell.set(JAVA_INT, 0, firstInt)
            secondCell.set(JAVA_INT, 0, secondInt)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, objectCell)
            args.setAtIndex(ADDRESS, 1, firstCell)
            args.setAtIndex(ADDRESS, 2, secondCell)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(JAVA_LONG, 0)
        }
    }

    fun ptrcallWithStringAndStringNameArgRetString(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        name: String,
    ): String {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val ret = arena.allocate(8L, 8L)
            try {
                GodotStrings.initString(stringCell, text)
                val args = arena.allocate(ADDRESS, 2)
                args.setAtIndex(ADDRESS, 0, stringCell)
                args.setAtIndex(ADDRESS, 1, GodotStrings.makeStringName(name))
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return GodotStrings.readString(ret)
            } finally {
                GodotStrings.destroyString(ret)
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithStringIntStringArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        intValue: Int,
        other: String,
    ): Long {
        Arena.ofConfined().use { arena ->
            val textCell = arena.allocate(8L, 8L)
            val intCell = arena.allocate(JAVA_INT)
            val otherCell = arena.allocate(8L, 8L)
            try {
                GodotStrings.initString(textCell, text)
                intCell.set(JAVA_INT, 0, intValue)
                GodotStrings.initString(otherCell, other)
                val args = arena.allocate(ADDRESS, 3)
                args.setAtIndex(ADDRESS, 0, textCell)
                args.setAtIndex(ADDRESS, 1, intCell)
                args.setAtIndex(ADDRESS, 2, otherCell)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                GodotStrings.destroyString(otherCell)
                GodotStrings.destroyString(textCell)
            }
        }
    }

    fun ptrcallWithStringIntObjectArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        intValue: Int,
        objectArg: MemorySegment,
    ): Long {
        Arena.ofConfined().use { arena ->
            val textCell = arena.allocate(8L, 8L)
            val intCell = arena.allocate(JAVA_INT)
            val objectCell = arena.allocate(ADDRESS)
            try {
                GodotStrings.initString(textCell, text)
                intCell.set(JAVA_INT, 0, intValue)
                objectCell.set(ADDRESS, 0, objectArg)
                val args = arena.allocate(ADDRESS, 3)
                args.setAtIndex(ADDRESS, 0, textCell)
                args.setAtIndex(ADDRESS, 1, intCell)
                args.setAtIndex(ADDRESS, 2, objectCell)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                GodotStrings.destroyString(textCell)
            }
        }
    }

    fun ptrcallWithStringTwoIntBoolArgsRetString(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        firstInt: Int,
        secondInt: Int,
        enabled: Boolean,
    ): String {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val firstCell = arena.allocate(JAVA_INT)
            val secondCell = arena.allocate(JAVA_INT)
            val boolCell = arena.allocate(JAVA_BYTE)
            val ret = arena.allocate(8L, 8L)
            try {
                GodotStrings.initString(stringCell, text)
                firstCell.set(JAVA_INT, 0, firstInt)
                secondCell.set(JAVA_INT, 0, secondInt)
                boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
                val args = arena.allocate(ADDRESS, 4)
                args.setAtIndex(ADDRESS, 0, stringCell)
                args.setAtIndex(ADDRESS, 1, firstCell)
                args.setAtIndex(ADDRESS, 2, secondCell)
                args.setAtIndex(ADDRESS, 3, boolCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return GodotStrings.readString(ret)
            } finally {
                GodotStrings.destroyString(ret)
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithObjectAndLongArgRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        longValue: Long,
    ): Long {
        Arena.ofConfined().use { arena ->
            val objectCell = arena.allocate(ADDRESS)
            val longCell = arena.allocate(JAVA_LONG)
            objectCell.set(ADDRESS, 0, objectArg)
            longCell.set(JAVA_LONG, 0, longValue)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, objectCell)
            args.setAtIndex(ADDRESS, 1, longCell)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(JAVA_LONG, 0)
        }
    }

    fun ptrcallWithObjectTwoDoubleArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        first: Double,
        second: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val objectCell = arena.allocate(ADDRESS)
            val firstCell = arena.allocate(JAVA_DOUBLE)
            val secondCell = arena.allocate(JAVA_DOUBLE)
            objectCell.set(ADDRESS, 0, objectArg)
            firstCell.set(JAVA_DOUBLE, 0, first)
            secondCell.set(JAVA_DOUBLE, 0, second)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, objectCell)
            args.setAtIndex(ADDRESS, 1, firstCell)
            args.setAtIndex(ADDRESS, 2, secondCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (Object*, scalar float, scalar float, scalar float, int64, StringName).
     */
    fun ptrcallWithObjectThreeDoubleLongStringNameArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        first: Double,
        second: Double,
        third: Double,
        longValue: Long,
        name: String,
    ): Long {
        Arena.ofConfined().use { arena ->
            val objectCell = arena.allocate(ADDRESS)
            val firstCell = arena.allocate(JAVA_DOUBLE)
            val secondCell = arena.allocate(JAVA_DOUBLE)
            val thirdCell = arena.allocate(JAVA_DOUBLE)
            val longCell = arena.allocate(JAVA_LONG)
            objectCell.set(ADDRESS, 0, objectArg)
            firstCell.set(JAVA_DOUBLE, 0, first)
            secondCell.set(JAVA_DOUBLE, 0, second)
            thirdCell.set(JAVA_DOUBLE, 0, third)
            longCell.set(JAVA_LONG, 0, longValue)
            val args = arena.allocate(ADDRESS, 6)
            args.setAtIndex(ADDRESS, 0, objectCell)
            args.setAtIndex(ADDRESS, 1, firstCell)
            args.setAtIndex(ADDRESS, 2, secondCell)
            args.setAtIndex(ADDRESS, 3, thirdCell)
            args.setAtIndex(ADDRESS, 4, longCell)
            args.setAtIndex(ADDRESS, 5, GodotStrings.makeStringName(name))
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(JAVA_LONG, 0)
        }
    }

    /**
     * Calls [methodBind] with (Object*, int64, bool, scalar float) and no return value.
     */
    fun ptrcallWithObjectLongBoolDoubleArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        longValue: Long,
        enabled: Boolean,
        value: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val objectCell = arena.allocate(ADDRESS)
            val longCell = arena.allocate(JAVA_LONG)
            val boolCell = arena.allocate(JAVA_BYTE)
            val doubleCell = arena.allocate(JAVA_DOUBLE)
            objectCell.set(ADDRESS, 0, objectArg)
            longCell.set(JAVA_LONG, 0, longValue)
            boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
            doubleCell.set(JAVA_DOUBLE, 0, value)
            val args = arena.allocate(ADDRESS, 4)
            args.setAtIndex(ADDRESS, 0, objectCell)
            args.setAtIndex(ADDRESS, 1, longCell)
            args.setAtIndex(ADDRESS, 2, boolCell)
            args.setAtIndex(ADDRESS, 3, doubleCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithTwoObjectArgsRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstObject: MemorySegment,
        secondObject: MemorySegment,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(ADDRESS)
            val secondCell = arena.allocate(ADDRESS)
            firstCell.set(ADDRESS, 0, firstObject)
            secondCell.set(ADDRESS, 0, secondObject)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, firstCell)
            args.setAtIndex(ADDRESS, 1, secondCell)
            val ret = arena.allocate(ADDRESS)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(ADDRESS, 0)
        }
    }

    fun ptrcallWithTwoObjectLongArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstObject: MemorySegment,
        secondObject: MemorySegment,
        longValue: Long,
    ): Long {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(ADDRESS)
            val secondCell = arena.allocate(ADDRESS)
            val longCell = arena.allocate(JAVA_LONG)
            firstCell.set(ADDRESS, 0, firstObject)
            secondCell.set(ADDRESS, 0, secondObject)
            longCell.set(JAVA_LONG, 0, longValue)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, firstCell)
            args.setAtIndex(ADDRESS, 1, secondCell)
            args.setAtIndex(ADDRESS, 2, longCell)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(JAVA_LONG, 0)
        }
    }

    fun ptrcallWithObjectDoubleTwoBoolArgsRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        scale: Double,
        firstBool: Boolean,
        secondBool: Boolean,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val objectCell = arena.allocate(ADDRESS)
            val doubleCell = arena.allocate(JAVA_DOUBLE)
            val firstBoolCell = arena.allocate(JAVA_BYTE)
            val secondBoolCell = arena.allocate(JAVA_BYTE)
            objectCell.set(ADDRESS, 0, objectArg)
            doubleCell.set(JAVA_DOUBLE, 0, scale)
            firstBoolCell.set(JAVA_BYTE, 0, if (firstBool) 1.toByte() else 0.toByte())
            secondBoolCell.set(JAVA_BYTE, 0, if (secondBool) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 4)
            args.setAtIndex(ADDRESS, 0, objectCell)
            args.setAtIndex(ADDRESS, 1, doubleCell)
            args.setAtIndex(ADDRESS, 2, firstBoolCell)
            args.setAtIndex(ADDRESS, 3, secondBoolCell)
            val ret = arena.allocate(ADDRESS)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(ADDRESS, 0)
        }
    }

    fun ptrcallWithRIDAndBoolArgRetRID(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        enabled: Boolean,
    ): RID {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val boolCell = arena.allocate(JAVA_BYTE)
            ridCell.set(JAVA_LONG, 0, rid.value)
            boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, boolCell)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return RID(ret.get(JAVA_LONG, 0))
        }
    }

    fun ptrcallWithRIDAndBoolArgRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        enabled: Boolean,
    ): Long {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val boolCell = arena.allocate(JAVA_BYTE)
            ridCell.set(JAVA_LONG, 0, rid.value)
            boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, boolCell)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(JAVA_LONG, 0)
        }
    }

    fun ptrcallWithRIDAndTwoUInt32ArgsRetRID(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        first: Long,
        second: Long,
    ): RID {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val firstCell = arena.allocate(JAVA_INT)
            val secondCell = arena.allocate(JAVA_INT)
            ridCell.set(JAVA_LONG, 0, rid.value)
            firstCell.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(first))
            secondCell.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(second))
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, firstCell)
            args.setAtIndex(ADDRESS, 2, secondCell)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return RID(ret.get(JAVA_LONG, 0))
        }
    }

    fun ptrcallWithRIDAndTwoUInt32ArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        first: Long,
        second: Long,
    ): Long {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val firstCell = arena.allocate(JAVA_INT)
            val secondCell = arena.allocate(JAVA_INT)
            ridCell.set(JAVA_LONG, 0, rid.value)
            firstCell.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(first))
            secondCell.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(second))
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, firstCell)
            args.setAtIndex(ADDRESS, 2, secondCell)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(JAVA_LONG, 0)
        }
    }

    fun ptrcallWithRIDAndTwoUInt32ArgsRetByteArray(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        first: Long,
        second: Long,
    ): ByteArray {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val firstCell = arena.allocate(JAVA_INT)
            val secondCell = arena.allocate(JAVA_INT)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                firstCell.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(first))
                secondCell.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(second))
                val args = arena.allocate(ADDRESS, 3)
                args.setAtIndex(ADDRESS, 0, ridCell)
                args.setAtIndex(ADDRESS, 1, firstCell)
                args.setAtIndex(ADDRESS, 2, secondCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return BuiltinTypes.readPackedByteArray(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, ret)
            }
        }
    }

    fun ptrcallWithRIDAndTwoLongArgsRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        first: Long,
        second: Long,
    ): Int {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val firstCell = arena.allocate(JAVA_LONG)
            val secondCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            firstCell.set(JAVA_LONG, 0, first)
            secondCell.set(JAVA_LONG, 0, second)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, firstCell)
            args.setAtIndex(ADDRESS, 2, secondCell)
            val ret = arena.allocate(JAVA_INT)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(JAVA_INT, 0)
        }
    }

    fun ptrcallWithTwoRIDArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: RID,
        second: RID,
    ): Long {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(JAVA_LONG)
            val secondCell = arena.allocate(JAVA_LONG)
            firstCell.set(JAVA_LONG, 0, first.value)
            secondCell.set(JAVA_LONG, 0, second.value)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, firstCell)
            args.setAtIndex(ADDRESS, 1, secondCell)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(JAVA_LONG, 0)
        }
    }

    fun ptrcallWithTwoRIDBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: RID,
        second: RID,
        enabled: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(JAVA_LONG)
            val secondCell = arena.allocate(JAVA_LONG)
            val boolCell = arena.allocate(JAVA_BYTE)
            firstCell.set(JAVA_LONG, 0, first.value)
            secondCell.set(JAVA_LONG, 0, second.value)
            boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, firstCell)
            args.setAtIndex(ADDRESS, 1, secondCell)
            args.setAtIndex(ADDRESS, 2, boolCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDIntDoubleArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        intValue: Int,
        doubleValue: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val intCell = arena.allocate(JAVA_INT)
            val doubleCell = arena.allocate(JAVA_DOUBLE)
            ridCell.set(JAVA_LONG, 0, rid.value)
            intCell.set(JAVA_INT, 0, intValue)
            doubleCell.set(JAVA_DOUBLE, 0, doubleValue)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, intCell)
            args.setAtIndex(ADDRESS, 2, doubleCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDStringNameRIDIntArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        name: String,
        texture: RID,
        index: Int,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val textureCell = arena.allocate(JAVA_LONG)
            val indexCell = arena.allocate(JAVA_INT)
            ridCell.set(JAVA_LONG, 0, rid.value)
            textureCell.set(JAVA_LONG, 0, texture.value)
            indexCell.set(JAVA_INT, 0, index)
            val args = arena.allocate(ADDRESS, 4)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, GodotStrings.makeStringName(name))
            args.setAtIndex(ADDRESS, 2, textureCell)
            args.setAtIndex(ADDRESS, 3, indexCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDStringNameAndIntArgRetRID(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        name: String,
        index: Int,
    ): RID {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val intCell = arena.allocate(JAVA_INT)
            ridCell.set(JAVA_LONG, 0, rid.value)
            intCell.set(JAVA_INT, 0, index)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, GodotStrings.makeStringName(name))
            args.setAtIndex(ADDRESS, 2, intCell)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return RID(ret.get(JAVA_LONG, 0))
        }
    }

    fun ptrcallWithRIDBoolDoubleArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        enabled: Boolean,
        amount: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val boolCell = arena.allocate(JAVA_BYTE)
            val doubleCell = arena.allocate(JAVA_DOUBLE)
            ridCell.set(JAVA_LONG, 0, rid.value)
            boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
            doubleCell.set(JAVA_DOUBLE, 0, amount)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, boolCell)
            args.setAtIndex(ADDRESS, 2, doubleCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDBoolIntThreeDoubleArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        enabled: Boolean,
        intValue: Int,
        first: Double,
        second: Double,
        third: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val boolCell = arena.allocate(JAVA_BYTE)
            val intCell = arena.allocate(JAVA_INT)
            val double0 = arena.allocate(JAVA_DOUBLE)
            val double1 = arena.allocate(JAVA_DOUBLE)
            val double2 = arena.allocate(JAVA_DOUBLE)
            ridCell.set(JAVA_LONG, 0, rid.value)
            boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
            intCell.set(JAVA_INT, 0, intValue)
            double0.set(JAVA_DOUBLE, 0, first)
            double1.set(JAVA_DOUBLE, 0, second)
            double2.set(JAVA_DOUBLE, 0, third)
            val args = arena.allocate(ADDRESS, 6)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, boolCell)
            args.setAtIndex(ADDRESS, 2, intCell)
            args.setAtIndex(ADDRESS, 3, double0)
            args.setAtIndex(ADDRESS, 4, double1)
            args.setAtIndex(ADDRESS, 5, double2)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (RID, bool, scalar float, scalar float) args.
     */
    fun ptrcallWithRIDBoolTwoDoubleArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        enabled: Boolean,
        first: Double,
        second: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val boolCell = arena.allocate(JAVA_BYTE)
            val arg0 = arena.allocate(JAVA_DOUBLE)
            val arg1 = arena.allocate(JAVA_DOUBLE)
            ridCell.set(JAVA_LONG, 0, rid.value)
            boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
            arg0.set(JAVA_DOUBLE, 0, first)
            arg1.set(JAVA_DOUBLE, 0, second)
            val args = arena.allocate(ADDRESS, 4)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, boolCell)
            args.setAtIndex(ADDRESS, 2, arg0)
            args.setAtIndex(ADDRESS, 3, arg1)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDBoolTwoDoubleBoolThreeDoubleArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        firstBool: Boolean,
        firstDouble: Double,
        secondDouble: Double,
        secondBool: Boolean,
        thirdDouble: Double,
        fourthDouble: Double,
        fifthDouble: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val bool0 = arena.allocate(JAVA_BYTE)
            val double0 = arena.allocate(JAVA_DOUBLE)
            val double1 = arena.allocate(JAVA_DOUBLE)
            val bool1 = arena.allocate(JAVA_BYTE)
            val double2 = arena.allocate(JAVA_DOUBLE)
            val double3 = arena.allocate(JAVA_DOUBLE)
            val double4 = arena.allocate(JAVA_DOUBLE)
            ridCell.set(JAVA_LONG, 0, rid.value)
            bool0.set(JAVA_BYTE, 0, if (firstBool) 1.toByte() else 0.toByte())
            double0.set(JAVA_DOUBLE, 0, firstDouble)
            double1.set(JAVA_DOUBLE, 0, secondDouble)
            bool1.set(JAVA_BYTE, 0, if (secondBool) 1.toByte() else 0.toByte())
            double2.set(JAVA_DOUBLE, 0, thirdDouble)
            double3.set(JAVA_DOUBLE, 0, fourthDouble)
            double4.set(JAVA_DOUBLE, 0, fifthDouble)
            val args = arena.allocate(ADDRESS, 8)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, bool0)
            args.setAtIndex(ADDRESS, 2, double0)
            args.setAtIndex(ADDRESS, 3, double1)
            args.setAtIndex(ADDRESS, 4, bool1)
            args.setAtIndex(ADDRESS, 5, double2)
            args.setAtIndex(ADDRESS, 6, double3)
            args.setAtIndex(ADDRESS, 7, double4)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (RID, bool, scalar float, scalar float, scalar float) args.
     */
    fun ptrcallWithRIDBoolThreeDoubleArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        enabled: Boolean,
        first: Double,
        second: Double,
        third: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val boolCell = arena.allocate(JAVA_BYTE)
            val arg0 = arena.allocate(JAVA_DOUBLE)
            val arg1 = arena.allocate(JAVA_DOUBLE)
            val arg2 = arena.allocate(JAVA_DOUBLE)
            ridCell.set(JAVA_LONG, 0, rid.value)
            boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
            arg0.set(JAVA_DOUBLE, 0, first)
            arg1.set(JAVA_DOUBLE, 0, second)
            arg2.set(JAVA_DOUBLE, 0, third)
            val args = arena.allocate(ADDRESS, 5)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, boolCell)
            args.setAtIndex(ADDRESS, 2, arg0)
            args.setAtIndex(ADDRESS, 3, arg1)
            args.setAtIndex(ADDRESS, 4, arg2)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDBoolThreeDoubleBoolRIDArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        firstBool: Boolean,
        first: Double,
        second: Double,
        third: Double,
        secondBool: Boolean,
        texture: RID,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val bool0 = arena.allocate(JAVA_BYTE)
            val double0 = arena.allocate(JAVA_DOUBLE)
            val double1 = arena.allocate(JAVA_DOUBLE)
            val double2 = arena.allocate(JAVA_DOUBLE)
            val bool1 = arena.allocate(JAVA_BYTE)
            val textureCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            bool0.set(JAVA_BYTE, 0, if (firstBool) 1.toByte() else 0.toByte())
            double0.set(JAVA_DOUBLE, 0, first)
            double1.set(JAVA_DOUBLE, 0, second)
            double2.set(JAVA_DOUBLE, 0, third)
            bool1.set(JAVA_BYTE, 0, if (secondBool) 1.toByte() else 0.toByte())
            textureCell.set(JAVA_LONG, 0, texture.value)
            val args = arena.allocate(ADDRESS, 7)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, bool0)
            args.setAtIndex(ADDRESS, 2, double0)
            args.setAtIndex(ADDRESS, 3, double1)
            args.setAtIndex(ADDRESS, 4, double2)
            args.setAtIndex(ADDRESS, 5, bool1)
            args.setAtIndex(ADDRESS, 6, textureCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (RID, bool, four scalar float args).
     */
    fun ptrcallWithRIDBoolFourDoubleArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        enabled: Boolean,
        first: Double,
        second: Double,
        third: Double,
        fourth: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val boolCell = arena.allocate(JAVA_BYTE)
            val arg0 = arena.allocate(JAVA_DOUBLE)
            val arg1 = arena.allocate(JAVA_DOUBLE)
            val arg2 = arena.allocate(JAVA_DOUBLE)
            val arg3 = arena.allocate(JAVA_DOUBLE)
            ridCell.set(JAVA_LONG, 0, rid.value)
            boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
            arg0.set(JAVA_DOUBLE, 0, first)
            arg1.set(JAVA_DOUBLE, 0, second)
            arg2.set(JAVA_DOUBLE, 0, third)
            arg3.set(JAVA_DOUBLE, 0, fourth)
            val args = arena.allocate(ADDRESS, 6)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, boolCell)
            args.setAtIndex(ADDRESS, 2, arg0)
            args.setAtIndex(ADDRESS, 3, arg1)
            args.setAtIndex(ADDRESS, 4, arg2)
            args.setAtIndex(ADDRESS, 5, arg3)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDBoolDoubleDoubleDoubleDoubleDoubleDoubleDoubleDoubleArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        enabled: Boolean,
        first: Double,
        second: Double,
        third: Double,
        fourth: Double,
        fifth: Double,
        sixth: Double,
        seventh: Double,
        eighth: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val boolCell = arena.allocate(JAVA_BYTE)
            val double0 = arena.allocate(JAVA_DOUBLE)
            val double1 = arena.allocate(JAVA_DOUBLE)
            val double2 = arena.allocate(JAVA_DOUBLE)
            val double3 = arena.allocate(JAVA_DOUBLE)
            val double4 = arena.allocate(JAVA_DOUBLE)
            val double5 = arena.allocate(JAVA_DOUBLE)
            val double6 = arena.allocate(JAVA_DOUBLE)
            val double7 = arena.allocate(JAVA_DOUBLE)
            ridCell.set(JAVA_LONG, 0, rid.value)
            boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
            double0.set(JAVA_DOUBLE, 0, first)
            double1.set(JAVA_DOUBLE, 0, second)
            double2.set(JAVA_DOUBLE, 0, third)
            double3.set(JAVA_DOUBLE, 0, fourth)
            double4.set(JAVA_DOUBLE, 0, fifth)
            double5.set(JAVA_DOUBLE, 0, sixth)
            double6.set(JAVA_DOUBLE, 0, seventh)
            double7.set(JAVA_DOUBLE, 0, eighth)
            val args = arena.allocate(ADDRESS, 10)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, boolCell)
            args.setAtIndex(ADDRESS, 2, double0)
            args.setAtIndex(ADDRESS, 3, double1)
            args.setAtIndex(ADDRESS, 4, double2)
            args.setAtIndex(ADDRESS, 5, double3)
            args.setAtIndex(ADDRESS, 6, double4)
            args.setAtIndex(ADDRESS, 7, double5)
            args.setAtIndex(ADDRESS, 8, double6)
            args.setAtIndex(ADDRESS, 9, double7)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDBoolColorDoubleDoubleDoubleDoubleDoubleDoubleDoubleLongArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        enabled: Boolean,
        color: Color,
        first: Double,
        second: Double,
        third: Double,
        fourth: Double,
        fifth: Double,
        sixth: Double,
        seventh: Double,
        longValue: Long,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val boolCell = arena.allocate(JAVA_BYTE)
            val colorCell = arena.allocate(16L, 4L)
            val double0 = arena.allocate(JAVA_DOUBLE)
            val double1 = arena.allocate(JAVA_DOUBLE)
            val double2 = arena.allocate(JAVA_DOUBLE)
            val double3 = arena.allocate(JAVA_DOUBLE)
            val double4 = arena.allocate(JAVA_DOUBLE)
            val double5 = arena.allocate(JAVA_DOUBLE)
            val double6 = arena.allocate(JAVA_DOUBLE)
            val longCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
            colorCell.set(JAVA_FLOAT, 0, color.r)
            colorCell.set(JAVA_FLOAT, 4, color.g)
            colorCell.set(JAVA_FLOAT, 8, color.b)
            colorCell.set(JAVA_FLOAT, 12, color.a)
            double0.set(JAVA_DOUBLE, 0, first)
            double1.set(JAVA_DOUBLE, 0, second)
            double2.set(JAVA_DOUBLE, 0, third)
            double3.set(JAVA_DOUBLE, 0, fourth)
            double4.set(JAVA_DOUBLE, 0, fifth)
            double5.set(JAVA_DOUBLE, 0, sixth)
            double6.set(JAVA_DOUBLE, 0, seventh)
            longCell.set(JAVA_LONG, 0, longValue)
            val args = arena.allocate(ADDRESS, 11)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, boolCell)
            args.setAtIndex(ADDRESS, 2, colorCell)
            args.setAtIndex(ADDRESS, 3, double0)
            args.setAtIndex(ADDRESS, 4, double1)
            args.setAtIndex(ADDRESS, 5, double2)
            args.setAtIndex(ADDRESS, 6, double3)
            args.setAtIndex(ADDRESS, 7, double4)
            args.setAtIndex(ADDRESS, 8, double5)
            args.setAtIndex(ADDRESS, 9, double6)
            args.setAtIndex(ADDRESS, 10, longCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDBoolIntDoubleLongBoolDoubleBoolThreeDoubleArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        firstBool: Boolean,
        intValue: Int,
        firstDouble: Double,
        longValue: Long,
        secondBool: Boolean,
        secondDouble: Double,
        thirdBool: Boolean,
        thirdDouble: Double,
        fourthDouble: Double,
        fifthDouble: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val bool0 = arena.allocate(JAVA_BYTE)
            val intCell = arena.allocate(JAVA_INT)
            val double0 = arena.allocate(JAVA_DOUBLE)
            val longCell = arena.allocate(JAVA_LONG)
            val bool1 = arena.allocate(JAVA_BYTE)
            val double1 = arena.allocate(JAVA_DOUBLE)
            val bool2 = arena.allocate(JAVA_BYTE)
            val double2 = arena.allocate(JAVA_DOUBLE)
            val double3 = arena.allocate(JAVA_DOUBLE)
            val double4 = arena.allocate(JAVA_DOUBLE)
            ridCell.set(JAVA_LONG, 0, rid.value)
            bool0.set(JAVA_BYTE, 0, if (firstBool) 1.toByte() else 0.toByte())
            intCell.set(JAVA_INT, 0, intValue)
            double0.set(JAVA_DOUBLE, 0, firstDouble)
            longCell.set(JAVA_LONG, 0, longValue)
            bool1.set(JAVA_BYTE, 0, if (secondBool) 1.toByte() else 0.toByte())
            double1.set(JAVA_DOUBLE, 0, secondDouble)
            bool2.set(JAVA_BYTE, 0, if (thirdBool) 1.toByte() else 0.toByte())
            double2.set(JAVA_DOUBLE, 0, thirdDouble)
            double3.set(JAVA_DOUBLE, 0, fourthDouble)
            double4.set(JAVA_DOUBLE, 0, fifthDouble)
            val args = arena.allocate(ADDRESS, 11)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, bool0)
            args.setAtIndex(ADDRESS, 2, intCell)
            args.setAtIndex(ADDRESS, 3, double0)
            args.setAtIndex(ADDRESS, 4, longCell)
            args.setAtIndex(ADDRESS, 5, bool1)
            args.setAtIndex(ADDRESS, 6, double1)
            args.setAtIndex(ADDRESS, 7, bool2)
            args.setAtIndex(ADDRESS, 8, double2)
            args.setAtIndex(ADDRESS, 9, double3)
            args.setAtIndex(ADDRESS, 10, double4)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDBoolDoubleTwoColorDoubleDoubleDoubleDoubleDoubleBoolThreeDoubleArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        firstBool: Boolean,
        firstDouble: Double,
        firstColor: Color,
        secondColor: Color,
        secondDouble: Double,
        thirdDouble: Double,
        fourthDouble: Double,
        fifthDouble: Double,
        sixthDouble: Double,
        secondBool: Boolean,
        seventhDouble: Double,
        eighthDouble: Double,
        ninthDouble: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val bool0 = arena.allocate(JAVA_BYTE)
            val double0 = arena.allocate(JAVA_DOUBLE)
            val color0 = arena.allocate(16L, 4L)
            val color1 = arena.allocate(16L, 4L)
            val double1 = arena.allocate(JAVA_DOUBLE)
            val double2 = arena.allocate(JAVA_DOUBLE)
            val double3 = arena.allocate(JAVA_DOUBLE)
            val double4 = arena.allocate(JAVA_DOUBLE)
            val double5 = arena.allocate(JAVA_DOUBLE)
            val bool1 = arena.allocate(JAVA_BYTE)
            val double6 = arena.allocate(JAVA_DOUBLE)
            val double7 = arena.allocate(JAVA_DOUBLE)
            val double8 = arena.allocate(JAVA_DOUBLE)
            ridCell.set(JAVA_LONG, 0, rid.value)
            bool0.set(JAVA_BYTE, 0, if (firstBool) 1.toByte() else 0.toByte())
            double0.set(JAVA_DOUBLE, 0, firstDouble)
            color0.set(JAVA_FLOAT, 0, firstColor.r)
            color0.set(JAVA_FLOAT, 4, firstColor.g)
            color0.set(JAVA_FLOAT, 8, firstColor.b)
            color0.set(JAVA_FLOAT, 12, firstColor.a)
            color1.set(JAVA_FLOAT, 0, secondColor.r)
            color1.set(JAVA_FLOAT, 4, secondColor.g)
            color1.set(JAVA_FLOAT, 8, secondColor.b)
            color1.set(JAVA_FLOAT, 12, secondColor.a)
            double1.set(JAVA_DOUBLE, 0, secondDouble)
            double2.set(JAVA_DOUBLE, 0, thirdDouble)
            double3.set(JAVA_DOUBLE, 0, fourthDouble)
            double4.set(JAVA_DOUBLE, 0, fifthDouble)
            double5.set(JAVA_DOUBLE, 0, sixthDouble)
            bool1.set(JAVA_BYTE, 0, if (secondBool) 1.toByte() else 0.toByte())
            double6.set(JAVA_DOUBLE, 0, seventhDouble)
            double7.set(JAVA_DOUBLE, 0, eighthDouble)
            double8.set(JAVA_DOUBLE, 0, ninthDouble)
            val args = arena.allocate(ADDRESS, 14)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, bool0)
            args.setAtIndex(ADDRESS, 2, double0)
            args.setAtIndex(ADDRESS, 3, color0)
            args.setAtIndex(ADDRESS, 4, color1)
            args.setAtIndex(ADDRESS, 5, double1)
            args.setAtIndex(ADDRESS, 6, double2)
            args.setAtIndex(ADDRESS, 7, double3)
            args.setAtIndex(ADDRESS, 8, double4)
            args.setAtIndex(ADDRESS, 9, double5)
            args.setAtIndex(ADDRESS, 10, bool1)
            args.setAtIndex(ADDRESS, 11, double6)
            args.setAtIndex(ADDRESS, 12, double7)
            args.setAtIndex(ADDRESS, 13, double8)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDLongDoubleBoolDoubleBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        longValue: Long,
        firstDouble: Double,
        firstBool: Boolean,
        secondDouble: Double,
        secondBool: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val longCell = arena.allocate(JAVA_LONG)
            val double0 = arena.allocate(JAVA_DOUBLE)
            val bool0 = arena.allocate(JAVA_BYTE)
            val double1 = arena.allocate(JAVA_DOUBLE)
            val bool1 = arena.allocate(JAVA_BYTE)
            ridCell.set(JAVA_LONG, 0, rid.value)
            longCell.set(JAVA_LONG, 0, longValue)
            double0.set(JAVA_DOUBLE, 0, firstDouble)
            bool0.set(JAVA_BYTE, 0, if (firstBool) 1.toByte() else 0.toByte())
            double1.set(JAVA_DOUBLE, 0, secondDouble)
            bool1.set(JAVA_BYTE, 0, if (secondBool) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 6)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, longCell)
            args.setAtIndex(ADDRESS, 2, double0)
            args.setAtIndex(ADDRESS, 3, bool0)
            args.setAtIndex(ADDRESS, 4, double1)
            args.setAtIndex(ADDRESS, 5, bool1)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDDoubleBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        amount: Double,
        enabled: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val doubleCell = arena.allocate(JAVA_DOUBLE)
            val boolCell = arena.allocate(JAVA_BYTE)
            ridCell.set(JAVA_LONG, 0, rid.value)
            doubleCell.set(JAVA_DOUBLE, 0, amount)
            boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, doubleCell)
            args.setAtIndex(ADDRESS, 2, boolCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (RID, int64/enum, scalar float, scalar float) args.
     */
    fun ptrcallWithRIDLongTwoDoubleArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        mode: Long,
        first: Double,
        second: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val modeCell = arena.allocate(JAVA_LONG)
            val arg0 = arena.allocate(JAVA_DOUBLE)
            val arg1 = arena.allocate(JAVA_DOUBLE)
            ridCell.set(JAVA_LONG, 0, rid.value)
            modeCell.set(JAVA_LONG, 0, mode)
            arg0.set(JAVA_DOUBLE, 0, first)
            arg1.set(JAVA_DOUBLE, 0, second)
            val args = arena.allocate(ADDRESS, 4)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, modeCell)
            args.setAtIndex(ADDRESS, 2, arg0)
            args.setAtIndex(ADDRESS, 3, arg1)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (RID, four scalar float args, int64/enum) args.
     */
    fun ptrcallWithRIDFourDoubleLongArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        first: Double,
        second: Double,
        third: Double,
        fourth: Double,
        mode: Long,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val arg0 = arena.allocate(JAVA_DOUBLE)
            val arg1 = arena.allocate(JAVA_DOUBLE)
            val arg2 = arena.allocate(JAVA_DOUBLE)
            val arg3 = arena.allocate(JAVA_DOUBLE)
            val modeCell = arena.allocate(JAVA_LONG)
            ridCell.set(JAVA_LONG, 0, rid.value)
            arg0.set(JAVA_DOUBLE, 0, first)
            arg1.set(JAVA_DOUBLE, 0, second)
            arg2.set(JAVA_DOUBLE, 0, third)
            arg3.set(JAVA_DOUBLE, 0, fourth)
            modeCell.set(JAVA_LONG, 0, mode)
            val args = arena.allocate(ADDRESS, 6)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, arg0)
            args.setAtIndex(ADDRESS, 2, arg1)
            args.setAtIndex(ADDRESS, 3, arg2)
            args.setAtIndex(ADDRESS, 4, arg3)
            args.setAtIndex(ADDRESS, 5, modeCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDRect2IntArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        rect: Rect2,
        index: Int,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val rectCell = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
            val intCell = arena.allocate(JAVA_INT)
            ridCell.set(JAVA_LONG, 0, rid.value)
            writeRect2(rectCell, rect)
            intCell.set(JAVA_INT, 0, index)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, rectCell)
            args.setAtIndex(ADDRESS, 2, intCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithTwoRIDTwoIntArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: RID,
        second: RID,
        firstInt: Int,
        secondInt: Int,
    ) {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(JAVA_LONG)
            val secondCell = arena.allocate(JAVA_LONG)
            val firstIntCell = arena.allocate(JAVA_INT)
            val secondIntCell = arena.allocate(JAVA_INT)
            firstCell.set(JAVA_LONG, 0, first.value)
            secondCell.set(JAVA_LONG, 0, second.value)
            firstIntCell.set(JAVA_INT, 0, firstInt)
            secondIntCell.set(JAVA_INT, 0, secondInt)
            val args = arena.allocate(ADDRESS, 4)
            args.setAtIndex(ADDRESS, 0, firstCell)
            args.setAtIndex(ADDRESS, 1, secondCell)
            args.setAtIndex(ADDRESS, 2, firstIntCell)
            args.setAtIndex(ADDRESS, 3, secondIntCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithTwoIntDoubleArgsRetRID(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Int,
        radius: Double,
    ): RID {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(JAVA_INT)
            val secondCell = arena.allocate(JAVA_INT)
            val radiusCell = arena.allocate(JAVA_DOUBLE)
            firstCell.set(JAVA_INT, 0, first)
            secondCell.set(JAVA_INT, 0, second)
            radiusCell.set(JAVA_DOUBLE, 0, radius)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, firstCell)
            args.setAtIndex(ADDRESS, 1, secondCell)
            args.setAtIndex(ADDRESS, 2, radiusCell)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return RID(ret.get(JAVA_LONG, 0))
        }
    }

    fun ptrcallWithLongBoolTwoLongArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        list: Long,
        useIndices: Boolean,
        instances: Long,
        proceduralVertexCount: Long,
    ) {
        Arena.ofConfined().use { arena ->
            val listCell = arena.allocate(JAVA_LONG)
            val boolCell = arena.allocate(JAVA_BYTE)
            val instancesCell = arena.allocate(JAVA_LONG)
            val vertexCountCell = arena.allocate(JAVA_LONG)
            listCell.set(JAVA_LONG, 0, list)
            boolCell.set(JAVA_BYTE, 0, if (useIndices) 1.toByte() else 0.toByte())
            instancesCell.set(JAVA_LONG, 0, instances)
            vertexCountCell.set(JAVA_LONG, 0, proceduralVertexCount)
            val args = arena.allocate(ADDRESS, 4)
            args.setAtIndex(ADDRESS, 0, listCell)
            args.setAtIndex(ADDRESS, 1, boolCell)
            args.setAtIndex(ADDRESS, 2, instancesCell)
            args.setAtIndex(ADDRESS, 3, vertexCountCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithFourLongArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Long,
        second: Long,
        third: Long,
        fourth: Long,
    ) {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(JAVA_LONG)
            val secondCell = arena.allocate(JAVA_LONG)
            val thirdCell = arena.allocate(JAVA_LONG)
            val fourthCell = arena.allocate(JAVA_LONG)
            firstCell.set(JAVA_LONG, 0, first)
            secondCell.set(JAVA_LONG, 0, second)
            thirdCell.set(JAVA_LONG, 0, third)
            fourthCell.set(JAVA_LONG, 0, fourth)
            val args = arena.allocate(ADDRESS, 4)
            args.setAtIndex(ADDRESS, 0, firstCell)
            args.setAtIndex(ADDRESS, 1, secondCell)
            args.setAtIndex(ADDRESS, 2, thirdCell)
            args.setAtIndex(ADDRESS, 3, fourthCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithLongRect2Args(
        methodBind: MemorySegment,
        instance: MemorySegment,
        list: Long,
        rect: Rect2,
    ) {
        Arena.ofConfined().use { arena ->
            val listCell = arena.allocate(JAVA_LONG)
            val rectCell = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
            listCell.set(JAVA_LONG, 0, list)
            writeRect2(rectCell, rect)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, listCell)
            args.setAtIndex(ADDRESS, 1, rectCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithTwoRIDArgsRetRID(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: RID,
        second: RID,
    ): RID {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(JAVA_LONG)
            val secondCell = arena.allocate(JAVA_LONG)
            firstCell.set(JAVA_LONG, 0, first.value)
            secondCell.set(JAVA_LONG, 0, second.value)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, firstCell)
            args.setAtIndex(ADDRESS, 1, secondCell)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return RID(ret.get(JAVA_LONG, 0))
        }
    }

    fun ptrcallWithRIDAndRIDListArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        values: List<RID>,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val valuesCell = arena.allocate(8L, 8L)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                BuiltinTypes.initArrayOfRids(valuesCell, values)
                val args = arena.allocate(ADDRESS, 2)
                args.setAtIndex(ADDRESS, 0, ridCell)
                args.setAtIndex(ADDRESS, 1, valuesCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, valuesCell)
            }
        }
    }

    fun ptrcallWithRIDRIDListVector2iArgsRetObjectList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        values: List<RID>,
        size: Vector2i,
    ): List<GodotObject> {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val valuesCell = arena.allocate(8L, 8L)
            val sizeCell = arena.allocate(8L, 4L)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                BuiltinTypes.initArrayOfRids(valuesCell, values)
                writeVector2i(sizeCell, size)
                val args = arena.allocate(ADDRESS, 3)
                args.setAtIndex(ADDRESS, 0, ridCell)
                args.setAtIndex(ADDRESS, 1, valuesCell)
                args.setAtIndex(ADDRESS, 2, sizeCell)
                return callArrayReturn(methodBind, instance, args, BuiltinTypes::readArrayObjects)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, valuesCell)
            }
        }
    }

    fun <T : Any> ptrcallWithRIDRIDListVector2iArgsRetTypedObjectList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        values: List<RID>,
        size: Vector2i,
        wrapper: (MemorySegment) -> T?,
    ): List<T> =
        ptrcallWithRIDRIDListVector2iArgsRetObjectList(methodBind, instance, rid, values, size)
            .mapNotNull { wrapper(it.handle) }

    fun ptrcallWithRIDListObjectListUInt32ArgsRetRID(
        methodBind: MemorySegment,
        instance: MemorySegment,
        ridValues: List<RID>,
        objectValues: List<*>,
        count: Long,
    ): RID {
        val checkedCount = BuiltinTypes.requireUInt32(count)
        Arena.ofConfined().use { arena ->
            val ridArray = arena.allocate(8L, 8L)
            val objectArray = arena.allocate(8L, 8L)
            val countCell = arena.allocate(JAVA_INT)
            try {
                BuiltinTypes.initArrayOfRids(ridArray, ridValues)
                BuiltinTypes.initArrayOfObjects(objectArray, objectValues)
                countCell.set(JAVA_INT, 0, checkedCount)
                val args = arena.allocate(ADDRESS, 3)
                args.setAtIndex(ADDRESS, 0, ridArray)
                args.setAtIndex(ADDRESS, 1, objectArray)
                args.setAtIndex(ADDRESS, 2, countCell)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return RID(ret.get(JAVA_LONG, 0))
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, objectArray)
                BuiltinTypes.destroyTyped(VariantType.ARRAY, ridArray)
            }
        }
    }

    fun ptrcallWithRIDListLongUInt32ArgsRetRID(
        methodBind: MemorySegment,
        instance: MemorySegment,
        ridValues: List<RID>,
        format: Long,
        count: Long,
    ): RID {
        val checkedCount = BuiltinTypes.requireUInt32(count)
        Arena.ofConfined().use { arena ->
            val ridArray = arena.allocate(8L, 8L)
            val formatCell = arena.allocate(JAVA_LONG)
            val countCell = arena.allocate(JAVA_INT)
            try {
                BuiltinTypes.initArrayOfRids(ridArray, ridValues)
                formatCell.set(JAVA_LONG, 0, format)
                countCell.set(JAVA_INT, 0, checkedCount)
                val args = arena.allocate(ADDRESS, 3)
                args.setAtIndex(ADDRESS, 0, ridArray)
                args.setAtIndex(ADDRESS, 1, formatCell)
                args.setAtIndex(ADDRESS, 2, countCell)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return RID(ret.get(JAVA_LONG, 0))
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, ridArray)
            }
        }
    }

    fun ptrcallWithRIDListObjectListLongUInt32ArgsRetRID(
        methodBind: MemorySegment,
        instance: MemorySegment,
        ridValues: List<RID>,
        objectValues: List<*>,
        format: Long,
        count: Long,
    ): RID {
        val checkedCount = BuiltinTypes.requireUInt32(count)
        Arena.ofConfined().use { arena ->
            val ridArray = arena.allocate(8L, 8L)
            val objectArray = arena.allocate(8L, 8L)
            val formatCell = arena.allocate(JAVA_LONG)
            val countCell = arena.allocate(JAVA_INT)
            try {
                BuiltinTypes.initArrayOfRids(ridArray, ridValues)
                BuiltinTypes.initArrayOfObjects(objectArray, objectValues)
                formatCell.set(JAVA_LONG, 0, format)
                countCell.set(JAVA_INT, 0, checkedCount)
                val args = arena.allocate(ADDRESS, 4)
                args.setAtIndex(ADDRESS, 0, ridArray)
                args.setAtIndex(ADDRESS, 1, objectArray)
                args.setAtIndex(ADDRESS, 2, formatCell)
                args.setAtIndex(ADDRESS, 3, countCell)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return RID(ret.get(JAVA_LONG, 0))
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, objectArray)
                BuiltinTypes.destroyTyped(VariantType.ARRAY, ridArray)
            }
        }
    }

    fun ptrcallWithUInt32LongRIDListPackedInt64ListArgsRetRID(
        methodBind: MemorySegment,
        instance: MemorySegment,
        count: Long,
        format: Long,
        ridValues: List<RID>,
        offsets: List<Long>,
    ): RID {
        val checkedCount = BuiltinTypes.requireUInt32(count)
        Arena.ofConfined().use { arena ->
            val countCell = arena.allocate(JAVA_INT)
            val formatCell = arena.allocate(JAVA_LONG)
            val ridArray = arena.allocate(8L, 8L)
            val offsetsArray = BuiltinTypes.allocatePackedArray(arena)
            try {
                countCell.set(JAVA_INT, 0, checkedCount)
                formatCell.set(JAVA_LONG, 0, format)
                BuiltinTypes.initArrayOfRids(ridArray, ridValues)
                BuiltinTypes.initPackedInt64Array(offsetsArray, offsets)
                val args = arena.allocate(ADDRESS, 4)
                args.setAtIndex(ADDRESS, 0, countCell)
                args.setAtIndex(ADDRESS, 1, formatCell)
                args.setAtIndex(ADDRESS, 2, ridArray)
                args.setAtIndex(ADDRESS, 3, offsetsArray)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return RID(ret.get(JAVA_LONG, 0))
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_INT64_ARRAY, offsetsArray)
                BuiltinTypes.destroyTyped(VariantType.ARRAY, ridArray)
            }
        }
    }

    fun ptrcallWithTwoLongUInt32RIDListPackedInt64ListArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        drawList: Long,
        vertexFormat: Long,
        vertexCount: Long,
        vertexBuffers: List<RID>,
        offsets: List<Long>,
    ) {
        val checkedVertexCount = BuiltinTypes.requireUInt32(vertexCount)
        Arena.ofConfined().use { arena ->
            val drawListCell = arena.allocate(JAVA_LONG)
            val vertexFormatCell = arena.allocate(JAVA_LONG)
            val vertexCountCell = arena.allocate(JAVA_INT)
            val vertexBufferArray = arena.allocate(8L, 8L)
            val offsetsArray = BuiltinTypes.allocatePackedArray(arena)
            try {
                drawListCell.set(JAVA_LONG, 0, drawList)
                vertexFormatCell.set(JAVA_LONG, 0, vertexFormat)
                vertexCountCell.set(JAVA_INT, 0, checkedVertexCount)
                BuiltinTypes.initArrayOfRids(vertexBufferArray, vertexBuffers)
                BuiltinTypes.initPackedInt64Array(offsetsArray, offsets)
                val args = arena.allocate(ADDRESS, 5)
                args.setAtIndex(ADDRESS, 0, drawListCell)
                args.setAtIndex(ADDRESS, 1, vertexFormatCell)
                args.setAtIndex(ADDRESS, 2, vertexCountCell)
                args.setAtIndex(ADDRESS, 3, vertexBufferArray)
                args.setAtIndex(ADDRESS, 4, offsetsArray)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_INT64_ARRAY, offsetsArray)
                BuiltinTypes.destroyTyped(VariantType.ARRAY, vertexBufferArray)
            }
        }
    }

    fun ptrcallWithRIDLongPackedColorListDoubleUInt32Rect2UInt32ArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        framebuffer: RID,
        drawFlags: Long,
        clearColorValues: List<Color>,
        clearDepthValue: Double,
        clearStencilValue: Long,
        region: Rect2,
        breadcrumb: Long,
    ): Long {
        val checkedClearStencil = BuiltinTypes.requireUInt32(clearStencilValue)
        val checkedBreadcrumb = BuiltinTypes.requireUInt32(breadcrumb)
        Arena.ofConfined().use { arena ->
            val framebufferCell = arena.allocate(JAVA_LONG)
            val drawFlagsCell = arena.allocate(JAVA_LONG)
            val clearColorArray = BuiltinTypes.allocatePackedArray(arena)
            val clearDepthCell = arena.allocate(JAVA_DOUBLE)
            val clearStencilCell = arena.allocate(JAVA_INT)
            val regionCell = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
            val breadcrumbCell = arena.allocate(JAVA_INT)
            try {
                framebufferCell.set(JAVA_LONG, 0, framebuffer.value)
                drawFlagsCell.set(JAVA_LONG, 0, drawFlags)
                BuiltinTypes.initPackedColorArray(clearColorArray, clearColorValues)
                clearDepthCell.set(JAVA_DOUBLE, 0, clearDepthValue)
                clearStencilCell.set(JAVA_INT, 0, checkedClearStencil)
                writeRect2(regionCell, region)
                breadcrumbCell.set(JAVA_INT, 0, checkedBreadcrumb)
                val args = arena.allocate(ADDRESS, 7)
                args.setAtIndex(ADDRESS, 0, framebufferCell)
                args.setAtIndex(ADDRESS, 1, drawFlagsCell)
                args.setAtIndex(ADDRESS, 2, clearColorArray)
                args.setAtIndex(ADDRESS, 3, clearDepthCell)
                args.setAtIndex(ADDRESS, 4, clearStencilCell)
                args.setAtIndex(ADDRESS, 5, regionCell)
                args.setAtIndex(ADDRESS, 6, breadcrumbCell)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_COLOR_ARRAY, clearColorArray)
            }
        }
    }

    fun ptrcallWithRIDUInt32FourLongPackedColorListDoubleUInt32Rect2RIDListArgsRetPackedInt64List(
        methodBind: MemorySegment,
        instance: MemorySegment,
        framebuffer: RID,
        splits: Long,
        initialColorAction: Long,
        finalColorAction: Long,
        initialDepthAction: Long,
        finalDepthAction: Long,
        clearColorValues: List<Color>,
        clearDepth: Double,
        clearStencil: Long,
        region: Rect2,
        storageTextures: List<RID>,
    ): List<Long> {
        val checkedSplits = BuiltinTypes.requireUInt32(splits)
        val checkedClearStencil = BuiltinTypes.requireUInt32(clearStencil)
        Arena.ofConfined().use { arena ->
            val framebufferCell = arena.allocate(JAVA_LONG)
            val splitsCell = arena.allocate(JAVA_INT)
            val initialColorActionCell = arena.allocate(JAVA_LONG)
            val finalColorActionCell = arena.allocate(JAVA_LONG)
            val initialDepthActionCell = arena.allocate(JAVA_LONG)
            val finalDepthActionCell = arena.allocate(JAVA_LONG)
            val clearColorArray = BuiltinTypes.allocatePackedArray(arena)
            val clearDepthCell = arena.allocate(JAVA_DOUBLE)
            val clearStencilCell = arena.allocate(JAVA_INT)
            val regionCell = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
            val storageTextureArray = arena.allocate(8L, 8L)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            try {
                framebufferCell.set(JAVA_LONG, 0, framebuffer.value)
                splitsCell.set(JAVA_INT, 0, checkedSplits)
                initialColorActionCell.set(JAVA_LONG, 0, initialColorAction)
                finalColorActionCell.set(JAVA_LONG, 0, finalColorAction)
                initialDepthActionCell.set(JAVA_LONG, 0, initialDepthAction)
                finalDepthActionCell.set(JAVA_LONG, 0, finalDepthAction)
                BuiltinTypes.initPackedColorArray(clearColorArray, clearColorValues)
                clearDepthCell.set(JAVA_DOUBLE, 0, clearDepth)
                clearStencilCell.set(JAVA_INT, 0, checkedClearStencil)
                writeRect2(regionCell, region)
                BuiltinTypes.initArrayOfRids(storageTextureArray, storageTextures)
                val args = arena.allocate(ADDRESS, 11)
                args.setAtIndex(ADDRESS, 0, framebufferCell)
                args.setAtIndex(ADDRESS, 1, splitsCell)
                args.setAtIndex(ADDRESS, 2, initialColorActionCell)
                args.setAtIndex(ADDRESS, 3, finalColorActionCell)
                args.setAtIndex(ADDRESS, 4, initialDepthActionCell)
                args.setAtIndex(ADDRESS, 5, finalDepthActionCell)
                args.setAtIndex(ADDRESS, 6, clearColorArray)
                args.setAtIndex(ADDRESS, 7, clearDepthCell)
                args.setAtIndex(ADDRESS, 8, clearStencilCell)
                args.setAtIndex(ADDRESS, 9, regionCell)
                args.setAtIndex(ADDRESS, 10, storageTextureArray)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return BuiltinTypes.readPackedInt64Array(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_INT64_ARRAY, ret)
                BuiltinTypes.destroyTyped(VariantType.ARRAY, storageTextureArray)
                BuiltinTypes.destroyTyped(VariantType.PACKED_COLOR_ARRAY, clearColorArray)
            }
        }
    }

    fun ptrcallWithRIDTwoUInt32PackedByteArrayArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        offset: Long,
        size: Long,
        data: ByteArray,
    ): Long {
        val checkedOffset = BuiltinTypes.requireUInt32(offset)
        val checkedSize = BuiltinTypes.requireUInt32(size)
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val offsetCell = arena.allocate(JAVA_INT)
            val sizeCell = arena.allocate(JAVA_INT)
            val dataArray = BuiltinTypes.allocatePackedArray(arena)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                offsetCell.set(JAVA_INT, 0, checkedOffset)
                sizeCell.set(JAVA_INT, 0, checkedSize)
                BuiltinTypes.initPackedByteArray(dataArray, data)
                val args = arena.allocate(ADDRESS, 4)
                args.setAtIndex(ADDRESS, 0, ridCell)
                args.setAtIndex(ADDRESS, 1, offsetCell)
                args.setAtIndex(ADDRESS, 2, sizeCell)
                args.setAtIndex(ADDRESS, 3, dataArray)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, dataArray)
            }
        }
    }

    fun ptrcallWithUInt32LongPackedByteArrayBoolLongArgsRetRID(
        methodBind: MemorySegment,
        instance: MemorySegment,
        size: Long,
        format: Long,
        data: ByteArray,
        useRestartIndices: Boolean,
        creationBits: Long,
    ): RID {
        val checkedSize = BuiltinTypes.requireUInt32(size)
        Arena.ofConfined().use { arena ->
            val sizeCell = arena.allocate(JAVA_INT)
            val formatCell = arena.allocate(JAVA_LONG)
            val dataArray = BuiltinTypes.allocatePackedArray(arena)
            val restartCell = arena.allocate(JAVA_BYTE)
            val creationBitsCell = arena.allocate(JAVA_LONG)
            try {
                sizeCell.set(JAVA_INT, 0, checkedSize)
                formatCell.set(JAVA_LONG, 0, format)
                BuiltinTypes.initPackedByteArray(dataArray, data)
                restartCell.set(JAVA_BYTE, 0, if (useRestartIndices) 1.toByte() else 0.toByte())
                creationBitsCell.set(JAVA_LONG, 0, creationBits)
                val args = arena.allocate(ADDRESS, 5)
                args.setAtIndex(ADDRESS, 0, sizeCell)
                args.setAtIndex(ADDRESS, 1, formatCell)
                args.setAtIndex(ADDRESS, 2, dataArray)
                args.setAtIndex(ADDRESS, 3, restartCell)
                args.setAtIndex(ADDRESS, 4, creationBitsCell)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return RID(ret.get(JAVA_LONG, 0))
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, dataArray)
            }
        }
    }

    fun ptrcallWithUInt32PackedByteArrayTwoLongArgsRetRID(
        methodBind: MemorySegment,
        instance: MemorySegment,
        size: Long,
        data: ByteArray,
        usage: Long,
        creationBits: Long,
    ): RID {
        val checkedSize = BuiltinTypes.requireUInt32(size)
        Arena.ofConfined().use { arena ->
            val sizeCell = arena.allocate(JAVA_INT)
            val dataArray = BuiltinTypes.allocatePackedArray(arena)
            val usageCell = arena.allocate(JAVA_LONG)
            val creationBitsCell = arena.allocate(JAVA_LONG)
            try {
                sizeCell.set(JAVA_INT, 0, checkedSize)
                BuiltinTypes.initPackedByteArray(dataArray, data)
                usageCell.set(JAVA_LONG, 0, usage)
                creationBitsCell.set(JAVA_LONG, 0, creationBits)
                val args = arena.allocate(ADDRESS, 4)
                args.setAtIndex(ADDRESS, 0, sizeCell)
                args.setAtIndex(ADDRESS, 1, dataArray)
                args.setAtIndex(ADDRESS, 2, usageCell)
                args.setAtIndex(ADDRESS, 3, creationBitsCell)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return RID(ret.get(JAVA_LONG, 0))
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, dataArray)
            }
        }
    }

    fun ptrcallWithPackedByteArrayStringObjectUInt32ArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        data: ByteArray,
        path: String,
        state: MemorySegment,
        flags: Long,
    ): Long {
        val checkedFlags = BuiltinTypes.requireUInt32(flags)
        Arena.ofConfined().use { arena ->
            val dataArray = BuiltinTypes.allocatePackedArray(arena)
            val pathCell = arena.allocate(8L, 8L)
            val stateCell = arena.allocate(ADDRESS)
            val flagsCell = arena.allocate(JAVA_INT)
            try {
                BuiltinTypes.initPackedByteArray(dataArray, data)
                GodotStrings.initString(pathCell, path)
                stateCell.set(ADDRESS, 0, state)
                flagsCell.set(JAVA_INT, 0, checkedFlags)
                val args = arena.allocate(ADDRESS, 4)
                args.setAtIndex(ADDRESS, 0, dataArray)
                args.setAtIndex(ADDRESS, 1, pathCell)
                args.setAtIndex(ADDRESS, 2, stateCell)
                args.setAtIndex(ADDRESS, 3, flagsCell)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                GodotStrings.destroyString(pathCell)
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, dataArray)
            }
        }
    }

    fun ptrcallWithTwoStringNameTwoObjectBoolArgsRetRID(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstName: String,
        secondName: String,
        firstObject: MemorySegment,
        secondObject: MemorySegment,
        enabled: Boolean,
    ): RID {
        Arena.ofConfined().use { arena ->
            val firstObjectCell = arena.allocate(ADDRESS)
            val secondObjectCell = arena.allocate(ADDRESS)
            val boolCell = arena.allocate(JAVA_BYTE)
            firstObjectCell.set(ADDRESS, 0, firstObject)
            secondObjectCell.set(ADDRESS, 0, secondObject)
            boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 5)
            args.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(firstName))
            args.setAtIndex(ADDRESS, 1, GodotStrings.makeStringName(secondName))
            args.setAtIndex(ADDRESS, 2, firstObjectCell)
            args.setAtIndex(ADDRESS, 3, secondObjectCell)
            args.setAtIndex(ADDRESS, 4, boolCell)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return RID(ret.get(JAVA_LONG, 0))
        }
    }

    fun ptrcallWithThreeStringNameObjectArgsRetRID(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstName: String,
        secondName: String,
        thirdName: String,
        objectArg: MemorySegment,
    ): RID {
        Arena.ofConfined().use { arena ->
            val objectCell = arena.allocate(ADDRESS)
            objectCell.set(ADDRESS, 0, objectArg)
            val args = arena.allocate(ADDRESS, 4)
            args.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(firstName))
            args.setAtIndex(ADDRESS, 1, GodotStrings.makeStringName(secondName))
            args.setAtIndex(ADDRESS, 2, GodotStrings.makeStringName(thirdName))
            args.setAtIndex(ADDRESS, 3, objectCell)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return RID(ret.get(JAVA_LONG, 0))
        }
    }

    fun ptrcallWithTwoStringNameFourUInt32ObjectArgsRetRID(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstName: String,
        secondName: String,
        first: Long,
        second: Long,
        third: Long,
        fourth: Long,
        objectArg: MemorySegment,
    ): RID {
        val checkedFirst = BuiltinTypes.requireUInt32(first)
        val checkedSecond = BuiltinTypes.requireUInt32(second)
        val checkedThird = BuiltinTypes.requireUInt32(third)
        val checkedFourth = BuiltinTypes.requireUInt32(fourth)
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(JAVA_INT)
            val secondCell = arena.allocate(JAVA_INT)
            val thirdCell = arena.allocate(JAVA_INT)
            val fourthCell = arena.allocate(JAVA_INT)
            val objectCell = arena.allocate(ADDRESS)
            firstCell.set(JAVA_INT, 0, checkedFirst)
            secondCell.set(JAVA_INT, 0, checkedSecond)
            thirdCell.set(JAVA_INT, 0, checkedThird)
            fourthCell.set(JAVA_INT, 0, checkedFourth)
            objectCell.set(ADDRESS, 0, objectArg)
            val args = arena.allocate(ADDRESS, 7)
            args.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(firstName))
            args.setAtIndex(ADDRESS, 1, GodotStrings.makeStringName(secondName))
            args.setAtIndex(ADDRESS, 2, firstCell)
            args.setAtIndex(ADDRESS, 3, secondCell)
            args.setAtIndex(ADDRESS, 4, thirdCell)
            args.setAtIndex(ADDRESS, 5, fourthCell)
            args.setAtIndex(ADDRESS, 6, objectCell)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return RID(ret.get(JAVA_LONG, 0))
        }
    }

    fun ptrcallWithObjectRIDArgsRetRID(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        rid: RID,
    ): RID {
        Arena.ofConfined().use { arena ->
            val objectCell = arena.allocate(ADDRESS)
            val ridCell = arena.allocate(JAVA_LONG)
            objectCell.set(ADDRESS, 0, objectArg)
            ridCell.set(JAVA_LONG, 0, rid.value)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, objectCell)
            args.setAtIndex(ADDRESS, 1, ridCell)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return RID(ret.get(JAVA_LONG, 0))
        }
    }

    fun ptrcallWithObjectStringArgsRetRID(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        text: String,
    ): RID {
        Arena.ofConfined().use { arena ->
            val objectCell = arena.allocate(ADDRESS)
            val stringCell = arena.allocate(8L, 8L)
            try {
                objectCell.set(ADDRESS, 0, objectArg)
                GodotStrings.initString(stringCell, text)
                val args = arena.allocate(ADDRESS, 2)
                args.setAtIndex(ADDRESS, 0, objectCell)
                args.setAtIndex(ADDRESS, 1, stringCell)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return RID(ret.get(JAVA_LONG, 0))
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithTwoObjectByteArrayListArgsRetRID(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstObject: MemorySegment,
        secondObject: MemorySegment,
        data: List<ByteArray>,
    ): RID {
        Arena.ofConfined().use { arena ->
            val firstObjectCell = arena.allocate(ADDRESS)
            val secondObjectCell = arena.allocate(ADDRESS)
            val dataArray = arena.allocate(8L, 8L)
            try {
                firstObjectCell.set(ADDRESS, 0, firstObject)
                secondObjectCell.set(ADDRESS, 0, secondObject)
                BuiltinTypes.initArray(dataArray, data.map { it as Any? })
                val args = arena.allocate(ADDRESS, 3)
                args.setAtIndex(ADDRESS, 0, firstObjectCell)
                args.setAtIndex(ADDRESS, 1, secondObjectCell)
                args.setAtIndex(ADDRESS, 2, dataArray)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return RID(ret.get(JAVA_LONG, 0))
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, dataArray)
            }
        }
    }

    fun ptrcallWithObjectRIDThreeUInt32LongArgsRetRID(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        rid: RID,
        first: Long,
        second: Long,
        third: Long,
        mode: Long,
    ): RID {
        val checkedFirst = BuiltinTypes.requireUInt32(first)
        val checkedSecond = BuiltinTypes.requireUInt32(second)
        val checkedThird = BuiltinTypes.requireUInt32(third)
        Arena.ofConfined().use { arena ->
            val objectCell = arena.allocate(ADDRESS)
            val ridCell = arena.allocate(JAVA_LONG)
            val firstCell = arena.allocate(JAVA_INT)
            val secondCell = arena.allocate(JAVA_INT)
            val thirdCell = arena.allocate(JAVA_INT)
            val modeCell = arena.allocate(JAVA_LONG)
            objectCell.set(ADDRESS, 0, objectArg)
            ridCell.set(JAVA_LONG, 0, rid.value)
            firstCell.set(JAVA_INT, 0, checkedFirst)
            secondCell.set(JAVA_INT, 0, checkedSecond)
            thirdCell.set(JAVA_INT, 0, checkedThird)
            modeCell.set(JAVA_LONG, 0, mode)
            val args = arena.allocate(ADDRESS, 6)
            args.setAtIndex(ADDRESS, 0, objectCell)
            args.setAtIndex(ADDRESS, 1, ridCell)
            args.setAtIndex(ADDRESS, 2, firstCell)
            args.setAtIndex(ADDRESS, 3, secondCell)
            args.setAtIndex(ADDRESS, 4, thirdCell)
            args.setAtIndex(ADDRESS, 5, modeCell)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return RID(ret.get(JAVA_LONG, 0))
        }
    }

    fun ptrcallWithRIDRIDListPackedInt64ListObjectArgsRetRID(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        rids: List<RID>,
        values: List<Long>,
        objectArg: MemorySegment,
    ): RID {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val ridArray = arena.allocate(8L, 8L)
            val valuesArray = BuiltinTypes.allocatePackedArray(arena)
            val objectCell = arena.allocate(ADDRESS)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                BuiltinTypes.initArrayOfRids(ridArray, rids)
                BuiltinTypes.initPackedInt64Array(valuesArray, values)
                objectCell.set(ADDRESS, 0, objectArg)
                val args = arena.allocate(ADDRESS, 4)
                args.setAtIndex(ADDRESS, 0, ridCell)
                args.setAtIndex(ADDRESS, 1, ridArray)
                args.setAtIndex(ADDRESS, 2, valuesArray)
                args.setAtIndex(ADDRESS, 3, objectCell)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return RID(ret.get(JAVA_LONG, 0))
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_INT64_ARRAY, valuesArray)
                BuiltinTypes.destroyTyped(VariantType.ARRAY, ridArray)
            }
        }
    }

    fun ptrcallWithRIDThreeLongFourObjectLongUInt32ObjectListArgsRetRID(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        first: Long,
        second: Long,
        third: Long,
        firstObject: MemorySegment,
        secondObject: MemorySegment,
        thirdObject: MemorySegment,
        fourthObject: MemorySegment,
        flags: Long,
        count: Long,
        values: List<*>,
    ): RID {
        val checkedCount = BuiltinTypes.requireUInt32(count)
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val firstCell = arena.allocate(JAVA_LONG)
            val secondCell = arena.allocate(JAVA_LONG)
            val thirdCell = arena.allocate(JAVA_LONG)
            val firstObjectCell = arena.allocate(ADDRESS)
            val secondObjectCell = arena.allocate(ADDRESS)
            val thirdObjectCell = arena.allocate(ADDRESS)
            val fourthObjectCell = arena.allocate(ADDRESS)
            val flagsCell = arena.allocate(JAVA_LONG)
            val countCell = arena.allocate(JAVA_INT)
            val objectArray = arena.allocate(8L, 8L)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                firstCell.set(JAVA_LONG, 0, first)
                secondCell.set(JAVA_LONG, 0, second)
                thirdCell.set(JAVA_LONG, 0, third)
                firstObjectCell.set(ADDRESS, 0, firstObject)
                secondObjectCell.set(ADDRESS, 0, secondObject)
                thirdObjectCell.set(ADDRESS, 0, thirdObject)
                fourthObjectCell.set(ADDRESS, 0, fourthObject)
                flagsCell.set(JAVA_LONG, 0, flags)
                countCell.set(JAVA_INT, 0, checkedCount)
                BuiltinTypes.initArrayOfObjects(objectArray, values)
                val args = arena.allocate(ADDRESS, 11)
                args.setAtIndex(ADDRESS, 0, ridCell)
                args.setAtIndex(ADDRESS, 1, firstCell)
                args.setAtIndex(ADDRESS, 2, secondCell)
                args.setAtIndex(ADDRESS, 3, thirdCell)
                args.setAtIndex(ADDRESS, 4, firstObjectCell)
                args.setAtIndex(ADDRESS, 5, secondObjectCell)
                args.setAtIndex(ADDRESS, 6, thirdObjectCell)
                args.setAtIndex(ADDRESS, 7, fourthObjectCell)
                args.setAtIndex(ADDRESS, 8, flagsCell)
                args.setAtIndex(ADDRESS, 9, countCell)
                args.setAtIndex(ADDRESS, 10, objectArray)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return RID(ret.get(JAVA_LONG, 0))
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, objectArray)
            }
        }
    }

    fun ptrcallWithRIDBoolPackedFloat32ListFourDoubleLongFourDoubleRIDArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        enabled: Boolean,
        levels: List<Float>,
        first: Double,
        second: Double,
        third: Double,
        fourth: Double,
        mode: Long,
        fifth: Double,
        sixth: Double,
        seventh: Double,
        eighth: Double,
        target: RID,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val enabledCell = arena.allocate(JAVA_BYTE)
            val levelsCell = BuiltinTypes.allocatePackedArray(arena)
            val firstCell = arena.allocate(JAVA_DOUBLE)
            val secondCell = arena.allocate(JAVA_DOUBLE)
            val thirdCell = arena.allocate(JAVA_DOUBLE)
            val fourthCell = arena.allocate(JAVA_DOUBLE)
            val modeCell = arena.allocate(JAVA_LONG)
            val fifthCell = arena.allocate(JAVA_DOUBLE)
            val sixthCell = arena.allocate(JAVA_DOUBLE)
            val seventhCell = arena.allocate(JAVA_DOUBLE)
            val eighthCell = arena.allocate(JAVA_DOUBLE)
            val targetCell = arena.allocate(JAVA_LONG)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                enabledCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
                BuiltinTypes.initPackedFloat32Array(levelsCell, levels)
                firstCell.set(JAVA_DOUBLE, 0, first)
                secondCell.set(JAVA_DOUBLE, 0, second)
                thirdCell.set(JAVA_DOUBLE, 0, third)
                fourthCell.set(JAVA_DOUBLE, 0, fourth)
                modeCell.set(JAVA_LONG, 0, mode)
                fifthCell.set(JAVA_DOUBLE, 0, fifth)
                sixthCell.set(JAVA_DOUBLE, 0, sixth)
                seventhCell.set(JAVA_DOUBLE, 0, seventh)
                eighthCell.set(JAVA_DOUBLE, 0, eighth)
                targetCell.set(JAVA_LONG, 0, target.value)
                val args = arena.allocate(ADDRESS, 13)
                args.setAtIndex(ADDRESS, 0, ridCell)
                args.setAtIndex(ADDRESS, 1, enabledCell)
                args.setAtIndex(ADDRESS, 2, levelsCell)
                args.setAtIndex(ADDRESS, 3, firstCell)
                args.setAtIndex(ADDRESS, 4, secondCell)
                args.setAtIndex(ADDRESS, 5, thirdCell)
                args.setAtIndex(ADDRESS, 6, fourthCell)
                args.setAtIndex(ADDRESS, 7, modeCell)
                args.setAtIndex(ADDRESS, 8, fifthCell)
                args.setAtIndex(ADDRESS, 9, sixthCell)
                args.setAtIndex(ADDRESS, 10, seventhCell)
                args.setAtIndex(ADDRESS, 11, eighthCell)
                args.setAtIndex(ADDRESS, 12, targetCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_FLOAT32_ARRAY, levelsCell)
            }
        }
    }

    fun ptrcallWithRIDPackedInt32ListPackedVector2ListPackedColorListPackedVector2ListPackedInt32ListPackedFloat32ListRIDIntArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        indices: List<Int>,
        points: List<Vector2>,
        colors: List<Color>,
        uvs: List<Vector2>,
        bones: List<Int>,
        weights: List<Float>,
        texture: RID,
        count: Int,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val indicesCell = BuiltinTypes.allocatePackedArray(arena)
            val pointsCell = BuiltinTypes.allocatePackedArray(arena)
            val colorsCell = BuiltinTypes.allocatePackedArray(arena)
            val uvsCell = BuiltinTypes.allocatePackedArray(arena)
            val bonesCell = BuiltinTypes.allocatePackedArray(arena)
            val weightsCell = BuiltinTypes.allocatePackedArray(arena)
            val textureCell = arena.allocate(JAVA_LONG)
            val countCell = arena.allocate(JAVA_INT)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                BuiltinTypes.initPackedInt32Array(indicesCell, indices)
                BuiltinTypes.initPackedVector2Array(pointsCell, points)
                BuiltinTypes.initPackedColorArray(colorsCell, colors)
                BuiltinTypes.initPackedVector2Array(uvsCell, uvs)
                BuiltinTypes.initPackedInt32Array(bonesCell, bones)
                BuiltinTypes.initPackedFloat32Array(weightsCell, weights)
                textureCell.set(JAVA_LONG, 0, texture.value)
                countCell.set(JAVA_INT, 0, count)
                val args = arena.allocate(ADDRESS, 9)
                args.setAtIndex(ADDRESS, 0, ridCell)
                args.setAtIndex(ADDRESS, 1, indicesCell)
                args.setAtIndex(ADDRESS, 2, pointsCell)
                args.setAtIndex(ADDRESS, 3, colorsCell)
                args.setAtIndex(ADDRESS, 4, uvsCell)
                args.setAtIndex(ADDRESS, 5, bonesCell)
                args.setAtIndex(ADDRESS, 6, weightsCell)
                args.setAtIndex(ADDRESS, 7, textureCell)
                args.setAtIndex(ADDRESS, 8, countCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_FLOAT32_ARRAY, weightsCell)
                BuiltinTypes.destroyTyped(VariantType.PACKED_INT32_ARRAY, bonesCell)
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR2_ARRAY, uvsCell)
                BuiltinTypes.destroyTyped(VariantType.PACKED_COLOR_ARRAY, colorsCell)
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR2_ARRAY, pointsCell)
                BuiltinTypes.destroyTyped(VariantType.PACKED_INT32_ARRAY, indicesCell)
            }
        }
    }

    fun ptrcallWithStringPackedStringListArrayTwoBoolArgsRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        path: String,
        arguments: List<String>,
        output: List<Any?>,
        readStderr: Boolean,
        openConsole: Boolean,
    ): Int {
        Arena.ofConfined().use { arena ->
            val pathCell = arena.allocate(8L, 8L)
            val argumentsCell = BuiltinTypes.allocatePackedArray(arena)
            val outputCell = arena.allocate(8L, 8L)
            val readStderrCell = arena.allocate(JAVA_BYTE)
            val openConsoleCell = arena.allocate(JAVA_BYTE)
            try {
                GodotStrings.initString(pathCell, path)
                BuiltinTypes.initPackedStringArray(argumentsCell, arguments)
                BuiltinTypes.initArray(outputCell, output)
                readStderrCell.set(JAVA_BYTE, 0, if (readStderr) 1.toByte() else 0.toByte())
                openConsoleCell.set(JAVA_BYTE, 0, if (openConsole) 1.toByte() else 0.toByte())
                val args = arena.allocate(ADDRESS, 5)
                args.setAtIndex(ADDRESS, 0, pathCell)
                args.setAtIndex(ADDRESS, 1, argumentsCell)
                args.setAtIndex(ADDRESS, 2, outputCell)
                args.setAtIndex(ADDRESS, 3, readStderrCell)
                args.setAtIndex(ADDRESS, 4, openConsoleCell)
                val ret = arena.allocate(JAVA_INT)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(JAVA_INT, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, outputCell)
                BuiltinTypes.destroyTyped(VariantType.PACKED_STRING_ARRAY, argumentsCell)
                GodotStrings.destroyString(pathCell)
            }
        }
    }

    fun ptrcallWithTwoStringPackedStringListStringArrayIntArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: String,
        second: String,
        arguments: List<String>,
        third: String,
        output: List<Any?>,
        port: Int,
    ): Long {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(8L, 8L)
            val secondCell = arena.allocate(8L, 8L)
            val argumentsCell = BuiltinTypes.allocatePackedArray(arena)
            val thirdCell = arena.allocate(8L, 8L)
            val outputCell = arena.allocate(8L, 8L)
            val portCell = arena.allocate(JAVA_INT)
            try {
                GodotStrings.initString(firstCell, first)
                GodotStrings.initString(secondCell, second)
                BuiltinTypes.initPackedStringArray(argumentsCell, arguments)
                GodotStrings.initString(thirdCell, third)
                BuiltinTypes.initArray(outputCell, output)
                portCell.set(JAVA_INT, 0, port)
                val args = arena.allocate(ADDRESS, 6)
                args.setAtIndex(ADDRESS, 0, firstCell)
                args.setAtIndex(ADDRESS, 1, secondCell)
                args.setAtIndex(ADDRESS, 2, argumentsCell)
                args.setAtIndex(ADDRESS, 3, thirdCell)
                args.setAtIndex(ADDRESS, 4, outputCell)
                args.setAtIndex(ADDRESS, 5, portCell)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, outputCell)
                GodotStrings.destroyString(thirdCell)
                BuiltinTypes.destroyTyped(VariantType.PACKED_STRING_ARRAY, argumentsCell)
                GodotStrings.destroyString(secondCell)
                GodotStrings.destroyString(firstCell)
            }
        }
    }

    fun ptrcallWithIntObjectStringNameArrayArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        peerId: Int,
        objectArg: MemorySegment,
        method: String,
        arguments: List<Any?>,
    ): Long {
        Arena.ofConfined().use { arena ->
            val peerIdCell = arena.allocate(JAVA_INT)
            val objectCell = arena.allocate(ADDRESS)
            val argumentsCell = arena.allocate(8L, 8L)
            try {
                peerIdCell.set(JAVA_INT, 0, peerId)
                objectCell.set(ADDRESS, 0, objectArg)
                BuiltinTypes.initArray(argumentsCell, arguments)
                val args = arena.allocate(ADDRESS, 4)
                args.setAtIndex(ADDRESS, 0, peerIdCell)
                args.setAtIndex(ADDRESS, 1, objectCell)
                args.setAtIndex(ADDRESS, 2, GodotStrings.makeStringName(method))
                args.setAtIndex(ADDRESS, 3, argumentsCell)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, argumentsCell)
            }
        }
    }

    fun ptrcallWithIntBoolIntColorBoolIntColorTwoObjectBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        slot: Int,
        leftEnabled: Boolean,
        leftType: Int,
        leftColor: Color,
        rightEnabled: Boolean,
        rightType: Int,
        rightColor: Color,
        leftIcon: MemorySegment,
        rightIcon: MemorySegment,
        drawStyleBox: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val slotCell = arena.allocate(JAVA_INT)
            val leftEnabledCell = arena.allocate(JAVA_BYTE)
            val leftTypeCell = arena.allocate(JAVA_INT)
            val leftColorCell = arena.allocate(16L, 4L)
            val rightEnabledCell = arena.allocate(JAVA_BYTE)
            val rightTypeCell = arena.allocate(JAVA_INT)
            val rightColorCell = arena.allocate(16L, 4L)
            val leftIconCell = arena.allocate(ADDRESS)
            val rightIconCell = arena.allocate(ADDRESS)
            val drawStyleBoxCell = arena.allocate(JAVA_BYTE)
            slotCell.set(JAVA_INT, 0, slot)
            leftEnabledCell.set(JAVA_BYTE, 0, if (leftEnabled) 1.toByte() else 0.toByte())
            leftTypeCell.set(JAVA_INT, 0, leftType)
            leftColorCell.set(JAVA_FLOAT, 0, leftColor.r)
            leftColorCell.set(JAVA_FLOAT, 4, leftColor.g)
            leftColorCell.set(JAVA_FLOAT, 8, leftColor.b)
            leftColorCell.set(JAVA_FLOAT, 12, leftColor.a)
            rightEnabledCell.set(JAVA_BYTE, 0, if (rightEnabled) 1.toByte() else 0.toByte())
            rightTypeCell.set(JAVA_INT, 0, rightType)
            rightColorCell.set(JAVA_FLOAT, 0, rightColor.r)
            rightColorCell.set(JAVA_FLOAT, 4, rightColor.g)
            rightColorCell.set(JAVA_FLOAT, 8, rightColor.b)
            rightColorCell.set(JAVA_FLOAT, 12, rightColor.a)
            leftIconCell.set(ADDRESS, 0, leftIcon)
            rightIconCell.set(ADDRESS, 0, rightIcon)
            drawStyleBoxCell.set(JAVA_BYTE, 0, if (drawStyleBox) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 10)
            args.setAtIndex(ADDRESS, 0, slotCell)
            args.setAtIndex(ADDRESS, 1, leftEnabledCell)
            args.setAtIndex(ADDRESS, 2, leftTypeCell)
            args.setAtIndex(ADDRESS, 3, leftColorCell)
            args.setAtIndex(ADDRESS, 4, rightEnabledCell)
            args.setAtIndex(ADDRESS, 5, rightTypeCell)
            args.setAtIndex(ADDRESS, 6, rightColorCell)
            args.setAtIndex(ADDRESS, 7, leftIconCell)
            args.setAtIndex(ADDRESS, 8, rightIconCell)
            args.setAtIndex(ADDRESS, 9, drawStyleBoxCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithStringNameArrayArgsRetVariantScalar(
        methodBind: MemorySegment,
        instance: MemorySegment,
        method: String,
        arguments: List<Any?>,
    ): Any? {
        Arena.ofConfined().use { arena ->
            val argumentsCell = arena.allocate(8L, 8L)
            try {
                BuiltinTypes.initArray(argumentsCell, arguments)
                val args = arena.allocate(ADDRESS, 2)
                args.setAtIndex(ADDRESS, 0, GodotStrings.makeStringName(method))
                args.setAtIndex(ADDRESS, 1, argumentsCell)
                return readVariantReturn(methodBind, instance, args, arena)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, argumentsCell)
            }
        }
    }

    fun ptrcallWithArrayObjectTwoBoolArgsRetVariantScalar(
        methodBind: MemorySegment,
        instance: MemorySegment,
        inputs: List<Any?>,
        objectArg: MemorySegment,
        firstFlag: Boolean,
        secondFlag: Boolean,
    ): Any? {
        Arena.ofConfined().use { arena ->
            val inputsCell = arena.allocate(8L, 8L)
            val objectCell = arena.allocate(ADDRESS)
            val firstFlagCell = arena.allocate(JAVA_BYTE)
            val secondFlagCell = arena.allocate(JAVA_BYTE)
            try {
                BuiltinTypes.initArray(inputsCell, inputs)
                objectCell.set(ADDRESS, 0, objectArg)
                firstFlagCell.set(JAVA_BYTE, 0, if (firstFlag) 1.toByte() else 0.toByte())
                secondFlagCell.set(JAVA_BYTE, 0, if (secondFlag) 1.toByte() else 0.toByte())
                val args = arena.allocate(ADDRESS, 4)
                args.setAtIndex(ADDRESS, 0, inputsCell)
                args.setAtIndex(ADDRESS, 1, objectCell)
                args.setAtIndex(ADDRESS, 2, firstFlagCell)
                args.setAtIndex(ADDRESS, 3, secondFlagCell)
                return readVariantReturn(methodBind, instance, args, arena)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, inputsCell)
            }
        }
    }

    fun ptrcallWithLongStringArrayArgsRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        result: Long,
        format: String,
        blends: List<Any?>,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val resultCell = arena.allocate(JAVA_LONG)
            val formatCell = arena.allocate(8L, 8L)
            val blendsCell = arena.allocate(8L, 8L)
            try {
                resultCell.set(JAVA_LONG, 0, result)
                GodotStrings.initString(formatCell, format)
                BuiltinTypes.initArray(blendsCell, blends)
                val args = arena.allocate(ADDRESS, 3)
                args.setAtIndex(ADDRESS, 0, resultCell)
                args.setAtIndex(ADDRESS, 1, formatCell)
                args.setAtIndex(ADDRESS, 2, blendsCell)
                val ret = arena.allocate(JAVA_BYTE)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(JAVA_BYTE, 0) != 0.toByte()
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, blendsCell)
                GodotStrings.destroyString(formatCell)
            }
        }
    }

    fun ptrcallWithLongArrayStringArgsRetVector3iList(
        methodBind: MemorySegment,
        instance: MemorySegment,
        parserType: Long,
        arguments: List<Any?>,
        text: String,
    ): List<Vector3i> {
        Arena.ofConfined().use { arena ->
            val parserTypeCell = arena.allocate(JAVA_LONG)
            val argumentsCell = arena.allocate(8L, 8L)
            val textCell = arena.allocate(8L, 8L)
            try {
                parserTypeCell.set(JAVA_LONG, 0, parserType)
                BuiltinTypes.initArray(argumentsCell, arguments)
                GodotStrings.initString(textCell, text)
                val args = arena.allocate(ADDRESS, 3)
                args.setAtIndex(ADDRESS, 0, parserTypeCell)
                args.setAtIndex(ADDRESS, 1, argumentsCell)
                args.setAtIndex(ADDRESS, 2, textCell)
                return callArrayReturn(methodBind, instance, args, BuiltinTypes::readArrayVector3i)
            } finally {
                GodotStrings.destroyString(textCell)
                BuiltinTypes.destroyTyped(VariantType.ARRAY, argumentsCell)
            }
        }
    }

    fun ptrcallWithTransform3DPackedVector2ListPackedInt32ListArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        transform: Transform3D,
        points: List<Vector2>,
        indices: List<Int>,
    ) {
        Arena.ofConfined().use { arena ->
            val transformCell = arena.allocate(GodotReal.SIZE_BYTES * 12, GodotReal.ALIGN_BYTES)
            val pointsCell = BuiltinTypes.allocatePackedArray(arena)
            val indicesCell = BuiltinTypes.allocatePackedArray(arena)
            try {
                writeTransform3D(transformCell, transform)
                BuiltinTypes.initPackedVector2Array(pointsCell, points)
                BuiltinTypes.initPackedInt32Array(indicesCell, indices)
                val args = arena.allocate(ADDRESS, 3)
                args.setAtIndex(ADDRESS, 0, transformCell)
                args.setAtIndex(ADDRESS, 1, pointsCell)
                args.setAtIndex(ADDRESS, 2, indicesCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_INT32_ARRAY, indicesCell)
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR2_ARRAY, pointsCell)
            }
        }
    }

    fun ptrcallWithRIDPackedVector3ListPackedInt32ListArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        points: List<Vector3>,
        indices: List<Int>,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val pointsCell = BuiltinTypes.allocatePackedArray(arena)
            val indicesCell = BuiltinTypes.allocatePackedArray(arena)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                BuiltinTypes.initPackedVector3Array(pointsCell, points)
                BuiltinTypes.initPackedInt32Array(indicesCell, indices)
                val args = arena.allocate(ADDRESS, 3)
                args.setAtIndex(ADDRESS, 0, ridCell)
                args.setAtIndex(ADDRESS, 1, pointsCell)
                args.setAtIndex(ADDRESS, 2, indicesCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_INT32_ARRAY, indicesCell)
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR3_ARRAY, pointsCell)
            }
        }
    }

    fun ptrcallWithRIDPackedVector3ListPackedColorListTwoPackedInt32ListArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        points: List<Vector3>,
        colors: List<Color>,
        firstIndices: List<Int>,
        secondIndices: List<Int>,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val pointsCell = BuiltinTypes.allocatePackedArray(arena)
            val colorsCell = BuiltinTypes.allocatePackedArray(arena)
            val firstIndicesCell = BuiltinTypes.allocatePackedArray(arena)
            val secondIndicesCell = BuiltinTypes.allocatePackedArray(arena)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                BuiltinTypes.initPackedVector3Array(pointsCell, points)
                BuiltinTypes.initPackedColorArray(colorsCell, colors)
                BuiltinTypes.initPackedInt32Array(firstIndicesCell, firstIndices)
                BuiltinTypes.initPackedInt32Array(secondIndicesCell, secondIndices)
                val args = arena.allocate(ADDRESS, 5)
                args.setAtIndex(ADDRESS, 0, ridCell)
                args.setAtIndex(ADDRESS, 1, pointsCell)
                args.setAtIndex(ADDRESS, 2, colorsCell)
                args.setAtIndex(ADDRESS, 3, firstIndicesCell)
                args.setAtIndex(ADDRESS, 4, secondIndicesCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_INT32_ARRAY, secondIndicesCell)
                BuiltinTypes.destroyTyped(VariantType.PACKED_INT32_ARRAY, firstIndicesCell)
                BuiltinTypes.destroyTyped(VariantType.PACKED_COLOR_ARRAY, colorsCell)
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR3_ARRAY, pointsCell)
            }
        }
    }

    fun ptrcallWithTransform3DAABBVector3ThreeByteArrayPackedInt32ListArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        transform: Transform3D,
        bounds: AABB,
        size: Vector3,
        firstData: ByteArray,
        secondData: ByteArray,
        thirdData: ByteArray,
        indices: List<Int>,
    ) {
        Arena.ofConfined().use { arena ->
            val transformCell = arena.allocate(GodotReal.SIZE_BYTES * 12, GodotReal.ALIGN_BYTES)
            val boundsCell = arena.allocate(GodotReal.SIZE_BYTES * 6, GodotReal.ALIGN_BYTES)
            val sizeCell = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            val firstDataCell = BuiltinTypes.allocatePackedArray(arena)
            val secondDataCell = BuiltinTypes.allocatePackedArray(arena)
            val thirdDataCell = BuiltinTypes.allocatePackedArray(arena)
            val indicesCell = BuiltinTypes.allocatePackedArray(arena)
            try {
                writeTransform3D(transformCell, transform)
                writeAABB(boundsCell, bounds)
                writeVector3(sizeCell, size)
                BuiltinTypes.initPackedByteArray(firstDataCell, firstData)
                BuiltinTypes.initPackedByteArray(secondDataCell, secondData)
                BuiltinTypes.initPackedByteArray(thirdDataCell, thirdData)
                BuiltinTypes.initPackedInt32Array(indicesCell, indices)
                val args = arena.allocate(ADDRESS, 7)
                args.setAtIndex(ADDRESS, 0, transformCell)
                args.setAtIndex(ADDRESS, 1, boundsCell)
                args.setAtIndex(ADDRESS, 2, sizeCell)
                args.setAtIndex(ADDRESS, 3, firstDataCell)
                args.setAtIndex(ADDRESS, 4, secondDataCell)
                args.setAtIndex(ADDRESS, 5, thirdDataCell)
                args.setAtIndex(ADDRESS, 6, indicesCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_INT32_ARRAY, indicesCell)
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, thirdDataCell)
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, secondDataCell)
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, firstDataCell)
            }
        }
    }

    fun ptrcallWithRIDTransform3DAABBVector3iThreeByteArrayPackedInt32ListArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        transform: Transform3D,
        bounds: AABB,
        size: Vector3i,
        firstData: ByteArray,
        secondData: ByteArray,
        thirdData: ByteArray,
        indices: List<Int>,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val transformCell = arena.allocate(GodotReal.SIZE_BYTES * 12, GodotReal.ALIGN_BYTES)
            val boundsCell = arena.allocate(GodotReal.SIZE_BYTES * 6, GodotReal.ALIGN_BYTES)
            val sizeCell = arena.allocate(12L, 4L)
            val firstDataCell = BuiltinTypes.allocatePackedArray(arena)
            val secondDataCell = BuiltinTypes.allocatePackedArray(arena)
            val thirdDataCell = BuiltinTypes.allocatePackedArray(arena)
            val indicesCell = BuiltinTypes.allocatePackedArray(arena)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                writeTransform3D(transformCell, transform)
                writeAABB(boundsCell, bounds)
                writeVector3i(sizeCell, size)
                BuiltinTypes.initPackedByteArray(firstDataCell, firstData)
                BuiltinTypes.initPackedByteArray(secondDataCell, secondData)
                BuiltinTypes.initPackedByteArray(thirdDataCell, thirdData)
                BuiltinTypes.initPackedInt32Array(indicesCell, indices)
                val args = arena.allocate(ADDRESS, 8)
                args.setAtIndex(ADDRESS, 0, ridCell)
                args.setAtIndex(ADDRESS, 1, transformCell)
                args.setAtIndex(ADDRESS, 2, boundsCell)
                args.setAtIndex(ADDRESS, 3, sizeCell)
                args.setAtIndex(ADDRESS, 4, firstDataCell)
                args.setAtIndex(ADDRESS, 5, secondDataCell)
                args.setAtIndex(ADDRESS, 6, thirdDataCell)
                args.setAtIndex(ADDRESS, 7, indicesCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_INT32_ARRAY, indicesCell)
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, thirdDataCell)
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, secondDataCell)
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, firstDataCell)
            }
        }
    }

    fun ptrcallWithTwoLongStringTwoLongPackedFloat32ListArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        firstMode: Long,
        secondMode: Long,
        text: String,
        thirdMode: Long,
        flags: Long,
        values: List<Float>,
    ) {
        Arena.ofConfined().use { arena ->
            val firstModeCell = arena.allocate(JAVA_LONG)
            val secondModeCell = arena.allocate(JAVA_LONG)
            val stringCell = arena.allocate(8L, 8L)
            val thirdModeCell = arena.allocate(JAVA_LONG)
            val flagsCell = arena.allocate(JAVA_LONG)
            val valuesCell = BuiltinTypes.allocatePackedArray(arena)
            try {
                firstModeCell.set(JAVA_LONG, 0, firstMode)
                secondModeCell.set(JAVA_LONG, 0, secondMode)
                GodotStrings.initString(stringCell, text)
                thirdModeCell.set(JAVA_LONG, 0, thirdMode)
                flagsCell.set(JAVA_LONG, 0, flags)
                BuiltinTypes.initPackedFloat32Array(valuesCell, values)
                val args = arena.allocate(ADDRESS, 6)
                args.setAtIndex(ADDRESS, 0, firstModeCell)
                args.setAtIndex(ADDRESS, 1, secondModeCell)
                args.setAtIndex(ADDRESS, 2, stringCell)
                args.setAtIndex(ADDRESS, 3, thirdModeCell)
                args.setAtIndex(ADDRESS, 4, flagsCell)
                args.setAtIndex(ADDRESS, 5, valuesCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_FLOAT32_ARRAY, valuesCell)
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithStringObjectIntRect2ColorIntColorArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        objectArg: MemorySegment,
        firstSize: Int,
        rect: Rect2,
        firstColor: Color,
        secondSize: Int,
        secondColor: Color,
    ) {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val objectCell = arena.allocate(ADDRESS)
            val firstSizeCell = arena.allocate(JAVA_INT)
            val rectCell = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
            val firstColorCell = arena.allocate(16L, 4L)
            val secondSizeCell = arena.allocate(JAVA_INT)
            val secondColorCell = arena.allocate(16L, 4L)
            try {
                GodotStrings.initString(stringCell, text)
                objectCell.set(ADDRESS, 0, objectArg)
                firstSizeCell.set(JAVA_INT, 0, firstSize)
                writeRect2(rectCell, rect)
                firstColorCell.set(JAVA_FLOAT, 0, firstColor.r)
                firstColorCell.set(JAVA_FLOAT, 4, firstColor.g)
                firstColorCell.set(JAVA_FLOAT, 8, firstColor.b)
                firstColorCell.set(JAVA_FLOAT, 12, firstColor.a)
                secondSizeCell.set(JAVA_INT, 0, secondSize)
                secondColorCell.set(JAVA_FLOAT, 0, secondColor.r)
                secondColorCell.set(JAVA_FLOAT, 4, secondColor.g)
                secondColorCell.set(JAVA_FLOAT, 8, secondColor.b)
                secondColorCell.set(JAVA_FLOAT, 12, secondColor.a)
                val args = arena.allocate(ADDRESS, 7)
                args.setAtIndex(ADDRESS, 0, stringCell)
                args.setAtIndex(ADDRESS, 1, objectCell)
                args.setAtIndex(ADDRESS, 2, firstSizeCell)
                args.setAtIndex(ADDRESS, 3, rectCell)
                args.setAtIndex(ADDRESS, 4, firstColorCell)
                args.setAtIndex(ADDRESS, 5, secondSizeCell)
                args.setAtIndex(ADDRESS, 6, secondColorCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    fun ptrcallWithPackedVector3ListPackedVector2ListPackedColorListPackedVector2ListPackedVector3ListPlaneListArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        vertices: List<Vector3>,
        uvs: List<Vector2>,
        colors: List<Color>,
        uv2s: List<Vector2>,
        normals: List<Vector3>,
        tangents: List<Plane>,
    ) {
        Arena.ofConfined().use { arena ->
            val verticesCell = BuiltinTypes.allocatePackedArray(arena)
            val uvsCell = BuiltinTypes.allocatePackedArray(arena)
            val colorsCell = BuiltinTypes.allocatePackedArray(arena)
            val uv2sCell = BuiltinTypes.allocatePackedArray(arena)
            val normalsCell = BuiltinTypes.allocatePackedArray(arena)
            val tangentsCell = arena.allocate(8L, 8L)
            try {
                BuiltinTypes.initPackedVector3Array(verticesCell, vertices)
                BuiltinTypes.initPackedVector2Array(uvsCell, uvs)
                BuiltinTypes.initPackedColorArray(colorsCell, colors)
                BuiltinTypes.initPackedVector2Array(uv2sCell, uv2s)
                BuiltinTypes.initPackedVector3Array(normalsCell, normals)
                BuiltinTypes.initArray(tangentsCell, tangents.map { it as Any? })
                val args = arena.allocate(ADDRESS, 6)
                args.setAtIndex(ADDRESS, 0, verticesCell)
                args.setAtIndex(ADDRESS, 1, uvsCell)
                args.setAtIndex(ADDRESS, 2, colorsCell)
                args.setAtIndex(ADDRESS, 3, uv2sCell)
                args.setAtIndex(ADDRESS, 4, normalsCell)
                args.setAtIndex(ADDRESS, 5, tangentsCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.ARRAY, tangentsCell)
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR3_ARRAY, normalsCell)
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR2_ARRAY, uv2sCell)
                BuiltinTypes.destroyTyped(VariantType.PACKED_COLOR_ARRAY, colorsCell)
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR2_ARRAY, uvsCell)
                BuiltinTypes.destroyTyped(VariantType.PACKED_VECTOR3_ARRAY, verticesCell)
            }
        }
    }

    fun ptrcallWithRIDVector2IntArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        vector: Vector2,
        index: Int,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val vectorCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val intCell = arena.allocate(JAVA_INT)
            ridCell.set(JAVA_LONG, 0, rid.value)
            writeVector2(vectorCell, vector)
            intCell.set(JAVA_INT, 0, index)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, vectorCell)
            args.setAtIndex(ADDRESS, 2, intCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithRIDColorDoubleArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        color: Color,
        energy: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val colorCell = arena.allocate(16L, 4L)
            val energyCell = arena.allocate(JAVA_DOUBLE)
            ridCell.set(JAVA_LONG, 0, rid.value)
            colorCell.set(JAVA_FLOAT, 0, color.r)
            colorCell.set(JAVA_FLOAT, 4, color.g)
            colorCell.set(JAVA_FLOAT, 8, color.b)
            colorCell.set(JAVA_FLOAT, 12, color.a)
            energyCell.set(JAVA_DOUBLE, 0, energy)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, colorCell)
            args.setAtIndex(ADDRESS, 2, energyCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithVector2iLongLongArgsRetRID(
        methodBind: MemorySegment,
        instance: MemorySegment,
        size: Vector2i,
        samples: Long,
        format: Long,
    ): RID {
        Arena.ofConfined().use { arena ->
            val sizeCell = arena.allocate(8L, 4L)
            val samplesCell = arena.allocate(JAVA_LONG)
            val formatCell = arena.allocate(JAVA_LONG)
            writeVector2i(sizeCell, size)
            samplesCell.set(JAVA_LONG, 0, samples)
            formatCell.set(JAVA_LONG, 0, format)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, sizeCell)
            args.setAtIndex(ADDRESS, 1, samplesCell)
            args.setAtIndex(ADDRESS, 2, formatCell)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return RID(ret.get(JAVA_LONG, 0))
        }
    }

    fun ptrcallWithAABBRIDArgsRetPackedInt64List(
        methodBind: MemorySegment,
        instance: MemorySegment,
        aabb: AABB,
        scenario: RID,
    ): List<Long> {
        Arena.ofConfined().use { arena ->
            val aabbCell = arena.allocate(GodotReal.SIZE_BYTES * 6, GodotReal.ALIGN_BYTES)
            val scenarioCell = arena.allocate(JAVA_LONG)
            writeAABB(aabbCell, aabb)
            scenarioCell.set(JAVA_LONG, 0, scenario.value)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, aabbCell)
            args.setAtIndex(ADDRESS, 1, scenarioCell)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return try {
                BuiltinTypes.readPackedInt64Array(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_INT64_ARRAY, ret)
            }
        }
    }

    fun ptrcallWithTwoVector3RIDArgsRetPackedInt64List(
        methodBind: MemorySegment,
        instance: MemorySegment,
        from: Vector3,
        to: Vector3,
        scenario: RID,
    ): List<Long> {
        Arena.ofConfined().use { arena ->
            val fromCell = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            val toCell = arena.allocate(GodotReal.SIZE_BYTES * 3, GodotReal.ALIGN_BYTES)
            val scenarioCell = arena.allocate(JAVA_LONG)
            writeVector3(fromCell, from)
            writeVector3(toCell, to)
            scenarioCell.set(JAVA_LONG, 0, scenario.value)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, fromCell)
            args.setAtIndex(ADDRESS, 1, toCell)
            args.setAtIndex(ADDRESS, 2, scenarioCell)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return try {
                BuiltinTypes.readPackedInt64Array(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_INT64_ARRAY, ret)
            }
        }
    }

    fun ptrcallWithBoolTwoDoubleArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        enabled: Boolean,
        first: Double,
        second: Double,
    ) {
        Arena.ofConfined().use { arena ->
            val boolCell = arena.allocate(JAVA_BYTE)
            val firstCell = arena.allocate(JAVA_DOUBLE)
            val secondCell = arena.allocate(JAVA_DOUBLE)
            boolCell.set(JAVA_BYTE, 0, if (enabled) 1.toByte() else 0.toByte())
            firstCell.set(JAVA_DOUBLE, 0, first)
            secondCell.set(JAVA_DOUBLE, 0, second)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, boolCell)
            args.setAtIndex(ADDRESS, 1, firstCell)
            args.setAtIndex(ADDRESS, 2, secondCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithTwoStringAndTwoBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: String,
        second: String,
        firstFlag: Boolean,
        secondFlag: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(8L, 8L)
            val secondCell = arena.allocate(8L, 8L)
            val firstFlagCell = arena.allocate(JAVA_BYTE)
            val secondFlagCell = arena.allocate(JAVA_BYTE)
            try {
                GodotStrings.initString(firstCell, first)
                GodotStrings.initString(secondCell, second)
                firstFlagCell.set(JAVA_BYTE, 0, if (firstFlag) 1.toByte() else 0.toByte())
                secondFlagCell.set(JAVA_BYTE, 0, if (secondFlag) 1.toByte() else 0.toByte())
                val args = arena.allocate(ADDRESS, 4)
                args.setAtIndex(ADDRESS, 0, firstCell)
                args.setAtIndex(ADDRESS, 1, secondCell)
                args.setAtIndex(ADDRESS, 2, firstFlagCell)
                args.setAtIndex(ADDRESS, 3, secondFlagCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(secondCell)
                GodotStrings.destroyString(firstCell)
            }
        }
    }

    fun ptrcallWithTwoStringBoolLongArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: String,
        second: String,
        flag: Boolean,
        mode: Long,
    ): Long {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(8L, 8L)
            val secondCell = arena.allocate(8L, 8L)
            val flagCell = arena.allocate(JAVA_BYTE)
            val modeCell = arena.allocate(JAVA_LONG)
            try {
                GodotStrings.initString(firstCell, first)
                GodotStrings.initString(secondCell, second)
                flagCell.set(JAVA_BYTE, 0, if (flag) 1.toByte() else 0.toByte())
                modeCell.set(JAVA_LONG, 0, mode)
                val args = arena.allocate(ADDRESS, 4)
                args.setAtIndex(ADDRESS, 0, firstCell)
                args.setAtIndex(ADDRESS, 1, secondCell)
                args.setAtIndex(ADDRESS, 2, flagCell)
                args.setAtIndex(ADDRESS, 3, modeCell)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                GodotStrings.destroyString(secondCell)
                GodotStrings.destroyString(firstCell)
            }
        }
    }

    fun ptrcallWithStringPackedStringListLongByteArrayArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        headers: List<String>,
        method: Long,
        body: ByteArray,
    ): Long {
        Arena.ofConfined().use { arena ->
            val textCell = arena.allocate(8L, 8L)
            val headersCell = BuiltinTypes.allocatePackedArray(arena)
            val methodCell = arena.allocate(JAVA_LONG)
            val bodyCell = BuiltinTypes.allocatePackedArray(arena)
            try {
                GodotStrings.initString(textCell, text)
                BuiltinTypes.initPackedStringArray(headersCell, headers)
                methodCell.set(JAVA_LONG, 0, method)
                BuiltinTypes.initPackedByteArray(bodyCell, body)
                val args = arena.allocate(ADDRESS, 4)
                args.setAtIndex(ADDRESS, 0, textCell)
                args.setAtIndex(ADDRESS, 1, headersCell)
                args.setAtIndex(ADDRESS, 2, methodCell)
                args.setAtIndex(ADDRESS, 3, bodyCell)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, bodyCell)
                BuiltinTypes.destroyTyped(VariantType.PACKED_STRING_ARRAY, headersCell)
                GodotStrings.destroyString(textCell)
            }
        }
    }

    fun ptrcallWithStringPackedStringListLongStringArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        headers: List<String>,
        method: Long,
        body: String,
    ): Long {
        Arena.ofConfined().use { arena ->
            val textCell = arena.allocate(8L, 8L)
            val headersCell = BuiltinTypes.allocatePackedArray(arena)
            val methodCell = arena.allocate(JAVA_LONG)
            val bodyCell = arena.allocate(8L, 8L)
            try {
                GodotStrings.initString(textCell, text)
                BuiltinTypes.initPackedStringArray(headersCell, headers)
                methodCell.set(JAVA_LONG, 0, method)
                GodotStrings.initString(bodyCell, body)
                val args = arena.allocate(ADDRESS, 4)
                args.setAtIndex(ADDRESS, 0, textCell)
                args.setAtIndex(ADDRESS, 1, headersCell)
                args.setAtIndex(ADDRESS, 2, methodCell)
                args.setAtIndex(ADDRESS, 3, bodyCell)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                GodotStrings.destroyString(bodyCell)
                BuiltinTypes.destroyTyped(VariantType.PACKED_STRING_ARRAY, headersCell)
                GodotStrings.destroyString(textCell)
            }
        }
    }

    fun ptrcallWithLongStringPackedStringListByteArrayArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        method: Long,
        text: String,
        headers: List<String>,
        body: ByteArray,
    ): Long {
        Arena.ofConfined().use { arena ->
            val methodCell = arena.allocate(JAVA_LONG)
            val textCell = arena.allocate(8L, 8L)
            val headersCell = BuiltinTypes.allocatePackedArray(arena)
            val bodyCell = BuiltinTypes.allocatePackedArray(arena)
            try {
                methodCell.set(JAVA_LONG, 0, method)
                GodotStrings.initString(textCell, text)
                BuiltinTypes.initPackedStringArray(headersCell, headers)
                BuiltinTypes.initPackedByteArray(bodyCell, body)
                val args = arena.allocate(ADDRESS, 4)
                args.setAtIndex(ADDRESS, 0, methodCell)
                args.setAtIndex(ADDRESS, 1, textCell)
                args.setAtIndex(ADDRESS, 2, headersCell)
                args.setAtIndex(ADDRESS, 3, bodyCell)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, bodyCell)
                BuiltinTypes.destroyTyped(VariantType.PACKED_STRING_ARRAY, headersCell)
                GodotStrings.destroyString(textCell)
            }
        }
    }

    fun ptrcallWithLongStringPackedStringListStringArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        method: Long,
        text: String,
        headers: List<String>,
        body: String,
    ): Long {
        Arena.ofConfined().use { arena ->
            val methodCell = arena.allocate(JAVA_LONG)
            val textCell = arena.allocate(8L, 8L)
            val headersCell = BuiltinTypes.allocatePackedArray(arena)
            val bodyCell = arena.allocate(8L, 8L)
            try {
                methodCell.set(JAVA_LONG, 0, method)
                GodotStrings.initString(textCell, text)
                BuiltinTypes.initPackedStringArray(headersCell, headers)
                GodotStrings.initString(bodyCell, body)
                val args = arena.allocate(ADDRESS, 4)
                args.setAtIndex(ADDRESS, 0, methodCell)
                args.setAtIndex(ADDRESS, 1, textCell)
                args.setAtIndex(ADDRESS, 2, headersCell)
                args.setAtIndex(ADDRESS, 3, bodyCell)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                GodotStrings.destroyString(bodyCell)
                BuiltinTypes.destroyTyped(VariantType.PACKED_STRING_ARRAY, headersCell)
                GodotStrings.destroyString(textCell)
            }
        }
    }

    fun ptrcallWithStringAndTransform3DArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        transform: Transform3D,
    ) {
        Arena.ofConfined().use { arena ->
            val textCell = arena.allocate(8L, 8L)
            val transformCell = arena.allocate(GodotReal.SIZE_BYTES * 12, GodotReal.ALIGN_BYTES)
            try {
                GodotStrings.initString(textCell, text)
                writeTransform3D(transformCell, transform)
                val args = arena.allocate(ADDRESS, 2)
                args.setAtIndex(ADDRESS, 0, textCell)
                args.setAtIndex(ADDRESS, 1, transformCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(textCell)
            }
        }
    }

    fun ptrcallWithStringTwoIntTwoBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        first: Int,
        second: Int,
        firstFlag: Boolean,
        secondFlag: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val textCell = arena.allocate(8L, 8L)
            val firstCell = arena.allocate(JAVA_INT)
            val secondCell = arena.allocate(JAVA_INT)
            val firstFlagCell = arena.allocate(JAVA_BYTE)
            val secondFlagCell = arena.allocate(JAVA_BYTE)
            try {
                GodotStrings.initString(textCell, text)
                firstCell.set(JAVA_INT, 0, first)
                secondCell.set(JAVA_INT, 0, second)
                firstFlagCell.set(JAVA_BYTE, 0, if (firstFlag) 1.toByte() else 0.toByte())
                secondFlagCell.set(JAVA_BYTE, 0, if (secondFlag) 1.toByte() else 0.toByte())
                val args = arena.allocate(ADDRESS, 5)
                args.setAtIndex(ADDRESS, 0, textCell)
                args.setAtIndex(ADDRESS, 1, firstCell)
                args.setAtIndex(ADDRESS, 2, secondCell)
                args.setAtIndex(ADDRESS, 3, firstFlagCell)
                args.setAtIndex(ADDRESS, 4, secondFlagCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(textCell)
            }
        }
    }

    fun ptrcallWithVector2Rect2ArgsRetBool(
        methodBind: MemorySegment,
        instance: MemorySegment,
        point: Vector2,
        rect: Rect2,
    ): Boolean {
        Arena.ofConfined().use { arena ->
            val pointCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val rectCell = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
            writeVector2(pointCell, point)
            writeRect2(rectCell, rect)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, pointCell)
            args.setAtIndex(ADDRESS, 1, rectCell)
            val ret = arena.allocate(JAVA_BYTE)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(JAVA_BYTE, 0) != 0.toByte()
        }
    }

    fun ptrcallWithIntTransform2DDoubleBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        index: Int,
        transform: Transform2D,
        amount: Double,
        persistent: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val indexCell = arena.allocate(JAVA_INT)
            val transformCell = arena.allocate(GodotReal.SIZE_BYTES * 6, GodotReal.ALIGN_BYTES)
            val amountCell = arena.allocate(JAVA_DOUBLE)
            val persistentCell = arena.allocate(JAVA_BYTE)
            indexCell.set(JAVA_INT, 0, index)
            writeTransform2D(transformCell, transform)
            amountCell.set(JAVA_DOUBLE, 0, amount)
            persistentCell.set(JAVA_BYTE, 0, if (persistent) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 4)
            args.setAtIndex(ADDRESS, 0, indexCell)
            args.setAtIndex(ADDRESS, 1, transformCell)
            args.setAtIndex(ADDRESS, 2, amountCell)
            args.setAtIndex(ADDRESS, 3, persistentCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    fun ptrcallWithIntTransform3DDoubleBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        index: Int,
        transform: Transform3D,
        amount: Double,
        persistent: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val indexCell = arena.allocate(JAVA_INT)
            val transformCell = arena.allocate(GodotReal.SIZE_BYTES * 12, GodotReal.ALIGN_BYTES)
            val amountCell = arena.allocate(JAVA_DOUBLE)
            val persistentCell = arena.allocate(JAVA_BYTE)
            indexCell.set(JAVA_INT, 0, index)
            writeTransform3D(transformCell, transform)
            amountCell.set(JAVA_DOUBLE, 0, amount)
            persistentCell.set(JAVA_BYTE, 0, if (persistent) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 4)
            args.setAtIndex(ADDRESS, 0, indexCell)
            args.setAtIndex(ADDRESS, 1, transformCell)
            args.setAtIndex(ADDRESS, 2, amountCell)
            args.setAtIndex(ADDRESS, 3, persistentCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    // --- Phase 1.1 helpers: new scalar-ish combinations ---

    // (StringName, enum) -> void reuses the existing ptrcallWithStringNameAndLongArg;
    // only a CALL_SHAPES entry was added in Phase 1.1.

    /**
     * Calls [methodBind] with (String, long, long) args and Long return.
     * Used for String+bitfield+int64 -> enum shapes (e.g. ZIPPacker.add_directory).
     */
    fun ptrcallWithStringTwoLongArgsRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        first: Long,
        second: Long,
    ): Long {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val firstCell = arena.allocate(JAVA_LONG)
            val secondCell = arena.allocate(JAVA_LONG)
            val ret = arena.allocate(JAVA_LONG)
            try {
                GodotStrings.initString(stringCell, text)
                firstCell.set(JAVA_LONG, 0, first)
                secondCell.set(JAVA_LONG, 0, second)
                val args = arena.allocate(ADDRESS, 3)
                args.setAtIndex(ADDRESS, 0, stringCell)
                args.setAtIndex(ADDRESS, 1, firstCell)
                args.setAtIndex(ADDRESS, 2, secondCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    /**
     * Calls [methodBind] with one uint32 arg and Vector2 return value.
     * Used for e.g. CollisionObject2D.get_shape_owner_one_way_collision_direction.
     */
    fun ptrcallWithUInt32ArgRetVector2(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Long,
    ): Vector2 {
        Arena.ofConfined().use { arena ->
            val arg = arena.allocate(JAVA_INT)
            arg.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(value))
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, arg)
            val ret = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return Vector2(
                x = GodotReal.readIndex(ret, 0),
                y = GodotReal.readIndex(ret, 1),
            )
        }
    }

    /**
     * Calls [methodBind] with (uint32, Vector2) args and no return value.
     * Used for e.g. CollisionObject2D.shape_owner_set_one_way_collision_direction.
     */
    fun ptrcallWithUInt32AndVector2Args(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Long,
        vector: Vector2,
    ) {
        Arena.ofConfined().use { arena ->
            val intCell = arena.allocate(JAVA_INT)
            val vectorCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            intCell.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(value))
            GodotReal.writeIndex(vectorCell, 0, vector.x)
            GodotReal.writeIndex(vectorCell, 1, vector.y)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, intCell)
            args.setAtIndex(ADDRESS, 1, vectorCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with one Vector3i arg and Vector3i return value.
     * Used for e.g. GridMap.get_octant_coords_from_cell_coords.
     */
    fun ptrcallWithVector3iArgRetVector3i(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Vector3i,
    ): Vector3i {
        Arena.ofConfined().use { arena ->
            val vec = arena.allocate(12L, 4L)
            writeVector3i(vec, value)
            val arr = arena.allocate(ADDRESS, 1)
            arr.setAtIndex(ADDRESS, 0, vec)
            val ret = arena.allocate(12L, 4L)
            objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
            return readVector3i(ret)
        }
    }

    /**
     * Calls [methodBind] with (String, bool, bool, float) args and Long return.
     * Used for e.g. Image.save_exr.
     */
    fun ptrcallWithStringTwoBoolAndDoubleArgRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        first: Boolean,
        second: Boolean,
        doubleArg: Double,
    ): Long {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val firstCell = arena.allocate(JAVA_BYTE)
            val secondCell = arena.allocate(JAVA_BYTE)
            val doubleCell = arena.allocate(JAVA_DOUBLE)
            val ret = arena.allocate(JAVA_LONG)
            try {
                GodotStrings.initString(stringCell, text)
                firstCell.set(JAVA_BYTE, 0, if (first) 1.toByte() else 0.toByte())
                secondCell.set(JAVA_BYTE, 0, if (second) 1.toByte() else 0.toByte())
                doubleCell.set(JAVA_DOUBLE, 0, doubleArg)
                val args = arena.allocate(ADDRESS, 4)
                args.setAtIndex(ADDRESS, 0, stringCell)
                args.setAtIndex(ADDRESS, 1, firstCell)
                args.setAtIndex(ADDRESS, 2, secondCell)
                args.setAtIndex(ADDRESS, 3, doubleCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    /**
     * Calls [methodBind] with (bool, bool, float) args and PackedByteArray return.
     * Used for e.g. Image.save_exr_to_buffer.
     */
    fun ptrcallWithTwoBoolAndDoubleArgRetByteArray(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Boolean,
        second: Boolean,
        doubleArg: Double,
    ): ByteArray {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(JAVA_BYTE)
            val secondCell = arena.allocate(JAVA_BYTE)
            val doubleCell = arena.allocate(JAVA_DOUBLE)
            firstCell.set(JAVA_BYTE, 0, if (first) 1.toByte() else 0.toByte())
            secondCell.set(JAVA_BYTE, 0, if (second) 1.toByte() else 0.toByte())
            doubleCell.set(JAVA_DOUBLE, 0, doubleArg)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, firstCell)
            args.setAtIndex(ADDRESS, 1, secondCell)
            args.setAtIndex(ADDRESS, 2, doubleCell)
            val ret = BuiltinTypes.allocatePackedArray(arena)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return try {
                BuiltinTypes.readPackedByteArray(ret)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, ret)
            }
        }
    }

    /**
     * Calls [methodBind] with (RID, int32, int32) args and Int return.
     * Used for e.g. NativeMenu.set_item_index.
     */
    fun ptrcallWithRIDAndTwoIntArgsRetInt(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        first: Int,
        second: Int,
    ): Int {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val firstCell = arena.allocate(JAVA_INT)
            val secondCell = arena.allocate(JAVA_INT)
            ridCell.set(JAVA_LONG, 0, rid.value)
            firstCell.set(JAVA_INT, 0, first)
            secondCell.set(JAVA_INT, 0, second)
            val args = arena.allocate(ADDRESS, 3)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, firstCell)
            args.setAtIndex(ADDRESS, 2, secondCell)
            val ret = arena.allocate(JAVA_INT)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(JAVA_INT, 0)
        }
    }

    /**
     * Calls [methodBind] with (RID, int32, bool, float, Vector2) args and no return value.
     * Used for e.g. PhysicsServer2D.body_set_shape_as_one_way_collision.
     */
    fun ptrcallWithRIDIntBoolDoubleVector2Args(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        intArg: Int,
        boolArg: Boolean,
        doubleArg: Double,
        vector: Vector2,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val intCell = arena.allocate(JAVA_INT)
            val boolCell = arena.allocate(JAVA_BYTE)
            val doubleCell = arena.allocate(JAVA_DOUBLE)
            val vectorCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            ridCell.set(JAVA_LONG, 0, rid.value)
            intCell.set(JAVA_INT, 0, intArg)
            boolCell.set(JAVA_BYTE, 0, if (boolArg) 1.toByte() else 0.toByte())
            doubleCell.set(JAVA_DOUBLE, 0, doubleArg)
            GodotReal.writeIndex(vectorCell, 0, vector.x)
            GodotReal.writeIndex(vectorCell, 1, vector.y)
            val args = arena.allocate(ADDRESS, 5)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, intCell)
            args.setAtIndex(ADDRESS, 2, boolCell)
            args.setAtIndex(ADDRESS, 3, doubleCell)
            args.setAtIndex(ADDRESS, 4, vectorCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (RID, uint32) args and RID return value.
     * Used for e.g. RenderingDevice.hit_sbt_create.
     */
    fun ptrcallWithRIDAndUInt32ArgRetRID(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Long,
    ): RID {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val valueCell = arena.allocate(JAVA_INT)
            ridCell.set(JAVA_LONG, 0, rid.value)
            valueCell.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(value))
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, valueCell)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return RID(ret.get(JAVA_LONG, 0))
        }
    }

    /**
     * Calls [methodBind] with (RID, uint32) args and Long return value.
     * Used for e.g. RenderingDevice.hit_sbt_range_alloc.
     */
    fun ptrcallWithRIDAndUInt32ArgRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        value: Long,
    ): Long {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val valueCell = arena.allocate(JAVA_INT)
            ridCell.set(JAVA_LONG, 0, rid.value)
            valueCell.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(value))
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, valueCell)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return ret.get(JAVA_LONG, 0)
        }
    }

    /**
     * Calls [methodBind] with (RID, int32, int32, int32) args and no return value.
     * Used for e.g. RenderingServer.viewport_set_size.
     */
    fun ptrcallWithRIDAndThreeIntArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        first: Int,
        second: Int,
        third: Int,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val firstCell = arena.allocate(JAVA_INT)
            val secondCell = arena.allocate(JAVA_INT)
            val thirdCell = arena.allocate(JAVA_INT)
            ridCell.set(JAVA_LONG, 0, rid.value)
            firstCell.set(JAVA_INT, 0, first)
            secondCell.set(JAVA_INT, 0, second)
            thirdCell.set(JAVA_INT, 0, third)
            val args = arena.allocate(ADDRESS, 4)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, firstCell)
            args.setAtIndex(ADDRESS, 2, secondCell)
            args.setAtIndex(ADDRESS, 3, thirdCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (RID, bool, bool, bool, Color) args and no return value.
     * Used for e.g. AccessibilityServer.update_set_text_decorations.
     */
    fun ptrcallWithRIDThreeBoolAndColorArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        first: Boolean,
        second: Boolean,
        third: Boolean,
        color: Color,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val firstCell = arena.allocate(JAVA_BYTE)
            val secondCell = arena.allocate(JAVA_BYTE)
            val thirdCell = arena.allocate(JAVA_BYTE)
            val colorCell = arena.allocate(16L, 4L)
            ridCell.set(JAVA_LONG, 0, rid.value)
            firstCell.set(JAVA_BYTE, 0, if (first) 1.toByte() else 0.toByte())
            secondCell.set(JAVA_BYTE, 0, if (second) 1.toByte() else 0.toByte())
            thirdCell.set(JAVA_BYTE, 0, if (third) 1.toByte() else 0.toByte())
            // Color components are fixed 32-bit floats, unlike scalar float ptrcall slots.
            colorCell.set(JAVA_FLOAT, 0, color.r)
            colorCell.set(JAVA_FLOAT, 4, color.g)
            colorCell.set(JAVA_FLOAT, 8, color.b)
            colorCell.set(JAVA_FLOAT, 12, color.a)
            val args = arena.allocate(ADDRESS, 5)
            args.setAtIndex(ADDRESS, 0, ridCell)
            args.setAtIndex(ADDRESS, 1, firstCell)
            args.setAtIndex(ADDRESS, 2, secondCell)
            args.setAtIndex(ADDRESS, 3, thirdCell)
            args.setAtIndex(ADDRESS, 4, colorCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (Object, float, int32, StringName) args and no return value.
     * Used for e.g. AnimationNodeBlendSpace1D.add_blend_point.
     */
    fun ptrcallWithObjectDoubleIntStringNameArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        obj: MemorySegment,
        doubleArg: Double,
        intArg: Int,
        name: String,
    ) {
        Arena.ofConfined().use { arena ->
            val objCell = arena.allocate(ADDRESS)
            val doubleCell = arena.allocate(JAVA_DOUBLE)
            val intCell = arena.allocate(JAVA_INT)
            objCell.set(ADDRESS, 0, obj)
            doubleCell.set(JAVA_DOUBLE, 0, doubleArg)
            intCell.set(JAVA_INT, 0, intArg)
            val args = arena.allocate(ADDRESS, 4)
            args.setAtIndex(ADDRESS, 0, objCell)
            args.setAtIndex(ADDRESS, 1, doubleCell)
            args.setAtIndex(ADDRESS, 2, intCell)
            args.setAtIndex(ADDRESS, 3, GodotStrings.makeStringName(name))
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (Object, Vector2, int32, StringName) args and no return value.
     * Used for e.g. AnimationNodeBlendSpace2D.add_blend_point.
     */
    fun ptrcallWithObjectVector2IntStringNameArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        obj: MemorySegment,
        vector: Vector2,
        intArg: Int,
        name: String,
    ) {
        Arena.ofConfined().use { arena ->
            val objCell = arena.allocate(ADDRESS)
            val vectorCell = arena.allocate(GodotReal.SIZE_BYTES * 2, GodotReal.ALIGN_BYTES)
            val intCell = arena.allocate(JAVA_INT)
            objCell.set(ADDRESS, 0, obj)
            GodotReal.writeIndex(vectorCell, 0, vector.x)
            GodotReal.writeIndex(vectorCell, 1, vector.y)
            intCell.set(JAVA_INT, 0, intArg)
            val args = arena.allocate(ADDRESS, 4)
            args.setAtIndex(ADDRESS, 0, objCell)
            args.setAtIndex(ADDRESS, 1, vectorCell)
            args.setAtIndex(ADDRESS, 2, intCell)
            args.setAtIndex(ADDRESS, 3, GodotStrings.makeStringName(name))
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (int32, int32, enum/long, Color, bool) args and no return value.
     * Used for e.g. DrawableTexture2D.setup.
     */
    fun ptrcallWithTwoIntLongColorBoolArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Int,
        longArg: Long,
        color: Color,
        boolArg: Boolean,
    ) {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(JAVA_INT)
            val secondCell = arena.allocate(JAVA_INT)
            val longCell = arena.allocate(JAVA_LONG)
            val colorCell = arena.allocate(16L, 4L)
            val boolCell = arena.allocate(JAVA_BYTE)
            firstCell.set(JAVA_INT, 0, first)
            secondCell.set(JAVA_INT, 0, second)
            longCell.set(JAVA_LONG, 0, longArg)
            // Color components are fixed 32-bit floats, unlike scalar float ptrcall slots.
            colorCell.set(JAVA_FLOAT, 0, color.r)
            colorCell.set(JAVA_FLOAT, 4, color.g)
            colorCell.set(JAVA_FLOAT, 8, color.b)
            colorCell.set(JAVA_FLOAT, 12, color.a)
            boolCell.set(JAVA_BYTE, 0, if (boolArg) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 5)
            args.setAtIndex(ADDRESS, 0, firstCell)
            args.setAtIndex(ADDRESS, 1, secondCell)
            args.setAtIndex(ADDRESS, 2, longCell)
            args.setAtIndex(ADDRESS, 3, colorCell)
            args.setAtIndex(ADDRESS, 4, boolCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (int32, int32, enum/long, Color, bool) args and RID return value.
     * Used for e.g. RenderingServer.texture_drawable_create.
     */
    fun ptrcallWithTwoIntLongColorBoolArgsRetRID(
        methodBind: MemorySegment,
        instance: MemorySegment,
        first: Int,
        second: Int,
        longArg: Long,
        color: Color,
        boolArg: Boolean,
    ): RID {
        Arena.ofConfined().use { arena ->
            val firstCell = arena.allocate(JAVA_INT)
            val secondCell = arena.allocate(JAVA_INT)
            val longCell = arena.allocate(JAVA_LONG)
            val colorCell = arena.allocate(16L, 4L)
            val boolCell = arena.allocate(JAVA_BYTE)
            firstCell.set(JAVA_INT, 0, first)
            secondCell.set(JAVA_INT, 0, second)
            longCell.set(JAVA_LONG, 0, longArg)
            // Color components are fixed 32-bit floats, unlike scalar float ptrcall slots.
            colorCell.set(JAVA_FLOAT, 0, color.r)
            colorCell.set(JAVA_FLOAT, 4, color.g)
            colorCell.set(JAVA_FLOAT, 8, color.b)
            colorCell.set(JAVA_FLOAT, 12, color.a)
            boolCell.set(JAVA_BYTE, 0, if (boolArg) 1.toByte() else 0.toByte())
            val args = arena.allocate(ADDRESS, 5)
            args.setAtIndex(ADDRESS, 0, firstCell)
            args.setAtIndex(ADDRESS, 1, secondCell)
            args.setAtIndex(ADDRESS, 2, longCell)
            args.setAtIndex(ADDRESS, 3, colorCell)
            args.setAtIndex(ADDRESS, 4, boolCell)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return RID(ret.get(JAVA_LONG, 0))
        }
    }

    /**
     * Calls [methodBind] with (int64, uint32, RID, uint32, uint32, uint32) args and no return.
     * Used for e.g. RenderingDevice.raytracing_list_trace_rays.
     */
    fun ptrcallWithLongUInt32RIDThreeUInt32Args(
        methodBind: MemorySegment,
        instance: MemorySegment,
        longArg: Long,
        first: Long,
        rid: RID,
        second: Long,
        third: Long,
        fourth: Long,
    ) {
        Arena.ofConfined().use { arena ->
            val longCell = arena.allocate(JAVA_LONG)
            val firstCell = arena.allocate(JAVA_INT)
            val ridCell = arena.allocate(JAVA_LONG)
            val secondCell = arena.allocate(JAVA_INT)
            val thirdCell = arena.allocate(JAVA_INT)
            val fourthCell = arena.allocate(JAVA_INT)
            longCell.set(JAVA_LONG, 0, longArg)
            firstCell.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(first))
            ridCell.set(JAVA_LONG, 0, rid.value)
            secondCell.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(second))
            thirdCell.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(third))
            fourthCell.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(fourth))
            val args = arena.allocate(ADDRESS, 6)
            args.setAtIndex(ADDRESS, 0, longCell)
            args.setAtIndex(ADDRESS, 1, firstCell)
            args.setAtIndex(ADDRESS, 2, ridCell)
            args.setAtIndex(ADDRESS, 3, secondCell)
            args.setAtIndex(ADDRESS, 4, thirdCell)
            args.setAtIndex(ADDRESS, 5, fourthCell)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
        }
    }

    /**
     * Calls [methodBind] with (uint32, bitfield/long) args and RID return value.
     * Used for e.g. RenderingDevice.tlas_create.
     */
    fun ptrcallWithUInt32AndLongArgRetRID(
        methodBind: MemorySegment,
        instance: MemorySegment,
        value: Long,
        longArg: Long,
    ): RID {
        Arena.ofConfined().use { arena ->
            val intCell = arena.allocate(JAVA_INT)
            val longCell = arena.allocate(JAVA_LONG)
            intCell.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(value))
            longCell.set(JAVA_LONG, 0, longArg)
            val args = arena.allocate(ADDRESS, 2)
            args.setAtIndex(ADDRESS, 0, intCell)
            args.setAtIndex(ADDRESS, 1, longCell)
            val ret = arena.allocate(JAVA_LONG)
            objectMethodBindPtrcall.invoke(methodBind, instance, args, ret)
            return RID(ret.get(JAVA_LONG, 0))
        }
    }

    // --- Phase 1.3 — Variant-return / Variant-arg / Packed*-arg helper shapes ---

    /** (Dictionary) -> Variant. Dictionary arg destroyed in finally; Variant return via readVariantReturn. */
    fun ptrcallWithDictionaryArgRetVariantScalar(
        methodBind: MemorySegment,
        instance: MemorySegment,
        values: Map<String, Any?>,
    ): Any? {
        Arena.ofConfined().use { arena ->
            val dict = arena.allocate(8L, 8L)
            try {
                BuiltinTypes.initDictionary(dict, values)
                val arr = arena.allocate(ADDRESS, 1)
                arr.setAtIndex(ADDRESS, 0, dict)
                return readVariantReturn(methodBind, instance, arr, arena)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.DICTIONARY, dict)
            }
        }
    }

    /** (String, Variant, int32) -> void. Note arg order: String, Variant, int32. */
    fun ptrcallWithStringVariantAndIntArg(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        value: Any?,
        index: Int,
    ) {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val variant = initVariantCell(arena, value)
            val intCell = arena.allocate(JAVA_INT)
            try {
                GodotStrings.initString(stringCell, text)
                intCell.set(JAVA_INT, 0, index)
                val arr = arena.allocate(ADDRESS, 3)
                arr.setAtIndex(ADDRESS, 0, stringCell)
                arr.setAtIndex(ADDRESS, 1, variant)
                arr.setAtIndex(ADDRESS, 2, intCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyVariant(variant)
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    /**
     * RichTextLabel.add_image:
     * (Object, float, float, Color, enum, Rect2, Variant, bool, String, enum, enum, String) -> void.
     */
    fun ptrcallWithObjectTwoDoubleColorLongRect2VariantBoolStringTwoLongStringArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        image: MemorySegment,
        width: Double,
        height: Double,
        color: Color,
        inlineAlign: Long,
        region: Rect2,
        key: Any?,
        pad: Boolean,
        tooltip: String,
        widthInPercent: Long,
        heightInPercent: Long,
        text: String,
    ) {
        Arena.ofConfined().use { arena ->
            val imageCell = arena.allocate(ADDRESS)
            val widthCell = arena.allocate(JAVA_DOUBLE)
            val heightCell = arena.allocate(JAVA_DOUBLE)
            val colorCell = arena.allocate(16L, 4L)
            val inlineAlignCell = arena.allocate(JAVA_LONG)
            val regionCell = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
            val keyVariant = initVariantCell(arena, key)
            val padCell = arena.allocate(JAVA_BYTE)
            val tooltipCell = arena.allocate(8L, 8L)
            val widthInPercentCell = arena.allocate(JAVA_LONG)
            val heightInPercentCell = arena.allocate(JAVA_LONG)
            val textCell = arena.allocate(8L, 8L)
            try {
                imageCell.set(ADDRESS, 0, image)
                widthCell.set(JAVA_DOUBLE, 0, width)
                heightCell.set(JAVA_DOUBLE, 0, height)
                colorCell.set(JAVA_FLOAT, 0, color.r)
                colorCell.set(JAVA_FLOAT, 4, color.g)
                colorCell.set(JAVA_FLOAT, 8, color.b)
                colorCell.set(JAVA_FLOAT, 12, color.a)
                inlineAlignCell.set(JAVA_LONG, 0, inlineAlign)
                writeRect2(regionCell, region)
                padCell.set(JAVA_BYTE, 0, if (pad) 1.toByte() else 0.toByte())
                GodotStrings.initString(tooltipCell, tooltip)
                widthInPercentCell.set(JAVA_LONG, 0, widthInPercent)
                heightInPercentCell.set(JAVA_LONG, 0, heightInPercent)
                GodotStrings.initString(textCell, text)
                val args = arena.allocate(ADDRESS, 12)
                args.setAtIndex(ADDRESS, 0, imageCell)
                args.setAtIndex(ADDRESS, 1, widthCell)
                args.setAtIndex(ADDRESS, 2, heightCell)
                args.setAtIndex(ADDRESS, 3, colorCell)
                args.setAtIndex(ADDRESS, 4, inlineAlignCell)
                args.setAtIndex(ADDRESS, 5, regionCell)
                args.setAtIndex(ADDRESS, 6, keyVariant)
                args.setAtIndex(ADDRESS, 7, padCell)
                args.setAtIndex(ADDRESS, 8, tooltipCell)
                args.setAtIndex(ADDRESS, 9, widthInPercentCell)
                args.setAtIndex(ADDRESS, 10, heightInPercentCell)
                args.setAtIndex(ADDRESS, 11, textCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(textCell)
                GodotStrings.destroyString(tooltipCell)
                BuiltinTypes.destroyVariant(keyVariant)
            }
        }
    }

    /**
     * RichTextLabel.update_image:
     * (Variant, bitfield, Object, float, float, Color, enum, Rect2, bool, String, enum, enum) -> void.
     */
    fun ptrcallWithVariantLongObjectTwoDoubleColorLongRect2BoolStringTwoLongArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        key: Any?,
        mask: Long,
        image: MemorySegment,
        width: Double,
        height: Double,
        color: Color,
        inlineAlign: Long,
        region: Rect2,
        pad: Boolean,
        tooltip: String,
        widthInPercent: Long,
        heightInPercent: Long,
    ) {
        Arena.ofConfined().use { arena ->
            val keyVariant = initVariantCell(arena, key)
            val maskCell = arena.allocate(JAVA_LONG)
            val imageCell = arena.allocate(ADDRESS)
            val widthCell = arena.allocate(JAVA_DOUBLE)
            val heightCell = arena.allocate(JAVA_DOUBLE)
            val colorCell = arena.allocate(16L, 4L)
            val inlineAlignCell = arena.allocate(JAVA_LONG)
            val regionCell = arena.allocate(GodotReal.SIZE_BYTES * 4, GodotReal.ALIGN_BYTES)
            val padCell = arena.allocate(JAVA_BYTE)
            val tooltipCell = arena.allocate(8L, 8L)
            val widthInPercentCell = arena.allocate(JAVA_LONG)
            val heightInPercentCell = arena.allocate(JAVA_LONG)
            try {
                maskCell.set(JAVA_LONG, 0, mask)
                imageCell.set(ADDRESS, 0, image)
                widthCell.set(JAVA_DOUBLE, 0, width)
                heightCell.set(JAVA_DOUBLE, 0, height)
                colorCell.set(JAVA_FLOAT, 0, color.r)
                colorCell.set(JAVA_FLOAT, 4, color.g)
                colorCell.set(JAVA_FLOAT, 8, color.b)
                colorCell.set(JAVA_FLOAT, 12, color.a)
                inlineAlignCell.set(JAVA_LONG, 0, inlineAlign)
                writeRect2(regionCell, region)
                padCell.set(JAVA_BYTE, 0, if (pad) 1.toByte() else 0.toByte())
                GodotStrings.initString(tooltipCell, tooltip)
                widthInPercentCell.set(JAVA_LONG, 0, widthInPercent)
                heightInPercentCell.set(JAVA_LONG, 0, heightInPercent)
                val args = arena.allocate(ADDRESS, 12)
                args.setAtIndex(ADDRESS, 0, keyVariant)
                args.setAtIndex(ADDRESS, 1, maskCell)
                args.setAtIndex(ADDRESS, 2, imageCell)
                args.setAtIndex(ADDRESS, 3, widthCell)
                args.setAtIndex(ADDRESS, 4, heightCell)
                args.setAtIndex(ADDRESS, 5, colorCell)
                args.setAtIndex(ADDRESS, 6, inlineAlignCell)
                args.setAtIndex(ADDRESS, 7, regionCell)
                args.setAtIndex(ADDRESS, 8, padCell)
                args.setAtIndex(ADDRESS, 9, tooltipCell)
                args.setAtIndex(ADDRESS, 10, widthInPercentCell)
                args.setAtIndex(ADDRESS, 11, heightInPercentCell)
                objectMethodBindPtrcall.invoke(methodBind, instance, args, MemorySegment.NULL)
            } finally {
                GodotStrings.destroyString(tooltipCell)
                BuiltinTypes.destroyVariant(keyVariant)
            }
        }
    }

    /** PCKPacker.add_file_from_buffer: (String, PackedByteArray, bool) -> enum. */
    fun ptrcallWithStringByteArrayAndBoolArgRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        text: String,
        bytes: ByteArray,
        flag: Boolean,
    ): Long {
        Arena.ofConfined().use { arena ->
            val stringCell = arena.allocate(8L, 8L)
            val byteArray = BuiltinTypes.allocatePackedArray(arena)
            val boolCell = arena.allocate(JAVA_BYTE)
            try {
                GodotStrings.initString(stringCell, text)
                BuiltinTypes.initPackedByteArray(byteArray, bytes)
                boolCell.set(JAVA_BYTE, 0, if (flag) 1.toByte() else 0.toByte())
                val arr = arena.allocate(ADDRESS, 3)
                arr.setAtIndex(ADDRESS, 0, stringCell)
                arr.setAtIndex(ADDRESS, 1, byteArray)
                arr.setAtIndex(ADDRESS, 2, boolCell)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_BYTE_ARRAY, byteArray)
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    /** JavaClassWrapper.create_proxy: (Object, PackedStringArray) -> Object. */
    fun ptrcallWithObjectAndPackedStringListArgsRetObject(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        values: List<String>,
    ): MemorySegment {
        Arena.ofConfined().use { arena ->
            val objCell = arena.allocate(ADDRESS)
            val packed = BuiltinTypes.allocatePackedArray(arena)
            try {
                objCell.set(ADDRESS, 0, objectArg)
                BuiltinTypes.initPackedStringArray(packed, values)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, objCell)
                arr.setAtIndex(ADDRESS, 1, packed)
                val ret = arena.allocate(ADDRESS)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(ADDRESS, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_STRING_ARRAY, packed)
            }
        }
    }

    /** GDScriptWorkspace.apply_new_signal: (Object, String, PackedStringArray) -> void. */
    fun ptrcallWithObjectStringAndPackedStringListArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        objectArg: MemorySegment,
        text: String,
        values: List<String>,
    ) {
        Arena.ofConfined().use { arena ->
            val objCell = arena.allocate(ADDRESS)
            val stringCell = arena.allocate(8L, 8L)
            val packed = BuiltinTypes.allocatePackedArray(arena)
            try {
                objCell.set(ADDRESS, 0, objectArg)
                GodotStrings.initString(stringCell, text)
                BuiltinTypes.initPackedStringArray(packed, values)
                val arr = arena.allocate(ADDRESS, 3)
                arr.setAtIndex(ADDRESS, 0, objCell)
                arr.setAtIndex(ADDRESS, 1, stringCell)
                arr.setAtIndex(ADDRESS, 2, packed)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_STRING_ARRAY, packed)
                GodotStrings.destroyString(stringCell)
            }
        }
    }

    /** TextServer.font_set_palette_custom_colors: (RID, PackedColorArray) -> void. */
    fun ptrcallWithRIDAndPackedColorListArgs(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        values: List<Color>,
    ) {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val packed = BuiltinTypes.allocatePackedArray(arena)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                BuiltinTypes.initPackedColorArray(packed, values)
                val arr = arena.allocate(ADDRESS, 2)
                arr.setAtIndex(ADDRESS, 0, ridCell)
                arr.setAtIndex(ADDRESS, 1, packed)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, MemorySegment.NULL)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_COLOR_ARRAY, packed)
            }
        }
    }

    /** RenderingDevice.hit_sbt_range_update: (RID, int64, uint32, PackedInt32Array) -> enum. */
    fun ptrcallWithRIDLongUInt32AndPackedInt32ListArgRetLong(
        methodBind: MemorySegment,
        instance: MemorySegment,
        rid: RID,
        longArg: Long,
        uintArg: Long,
        values: List<Int>,
    ): Long {
        Arena.ofConfined().use { arena ->
            val ridCell = arena.allocate(JAVA_LONG)
            val longCell = arena.allocate(JAVA_LONG)
            val uintCell = arena.allocate(JAVA_INT)
            val packed = BuiltinTypes.allocatePackedArray(arena)
            try {
                ridCell.set(JAVA_LONG, 0, rid.value)
                longCell.set(JAVA_LONG, 0, longArg)
                uintCell.set(JAVA_INT, 0, BuiltinTypes.requireUInt32(uintArg))
                BuiltinTypes.initPackedInt32Array(packed, values)
                val arr = arena.allocate(ADDRESS, 4)
                arr.setAtIndex(ADDRESS, 0, ridCell)
                arr.setAtIndex(ADDRESS, 1, longCell)
                arr.setAtIndex(ADDRESS, 2, uintCell)
                arr.setAtIndex(ADDRESS, 3, packed)
                val ret = arena.allocate(JAVA_LONG)
                objectMethodBindPtrcall.invoke(methodBind, instance, arr, ret)
                return ret.get(JAVA_LONG, 0)
            } finally {
                BuiltinTypes.destroyTyped(VariantType.PACKED_INT32_ARRAY, packed)
            }
        }
    }

    /** Destroys a Godot Object pointer allocated via classdb_construct_object2. */
    fun destroyObject(instance: MemorySegment) {
        if (instance.address() == 0L) return
        objectDestroy.invoke(instance)
    }
}
