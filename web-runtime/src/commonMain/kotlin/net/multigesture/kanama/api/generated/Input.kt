// Generated by scripts/generate_web_wrappers.py — do not edit.
@file:OptIn(InternalKanamaBackendApi::class)

package net.multigesture.kanama.api

import net.multigesture.kanama.backend.GodotBackendCalls
import net.multigesture.kanama.backend.InitialGodotCallDescriptors as D
import net.multigesture.kanama.types.Vector2
import net.multigesture.kanama.backend.InternalKanamaBackendApi

object Input {
  fun setCustomMouseCursor(image: Resource?, shape: Long = 0L, hotspot: Vector2 = Vector2.ZERO) {
    GodotBackendCalls.invokeObjectLongVector2Args(
      D.INPUT_SET_CUSTOM_MOUSE_CURSOR,
      image?.requireOpenHandle(),
      shape,
      hotspot.toBackend(),
    )
  }

  fun isActionPressed(action: String, exactMatch: Boolean = false): Boolean {
    require(exactMatch == false) { "Web Input.is_action_pressed supports only exactMatch = false" }
    return GodotBackendCalls.invokeStringNameRetBoolSingleton(D.INPUT_IS_ACTION_PRESSED, action)
  }

  fun actionPress(action: String, strength: Double = 1.0) {
    require(strength == 1.0) { "Web Input.action_press supports only strength = 1.0" }
    GodotBackendCalls.invokeStringNameArgSingleton(D.INPUT_ACTION_PRESS, action)
  }

  fun actionRelease(action: String) {
    GodotBackendCalls.invokeStringNameArgSingleton(D.INPUT_ACTION_RELEASE, action)
  }

  fun isActionJustPressed(action: String, exactMatch: Boolean = false): Boolean {
    require(exactMatch == false) { "Web Input.is_action_just_pressed supports only exactMatch = false" }
    return GodotBackendCalls.invokeStringNameRetBoolSingleton(
      D.INPUT_IS_ACTION_JUST_PRESSED,
      action,
    )
  }

  fun getAxis(negativeAction: String, positiveAction: String): Double =
    GodotBackendCalls.invokeStringNameStringNameRetDoubleSingleton(
      D.INPUT_GET_AXIS,
      negativeAction,
      positiveAction,
    )

  fun setMouseMode(mode: Long) {
    GodotBackendCalls.invokeLongArgSingleton(D.INPUT_SET_MOUSE_MODE, mode)
  }

  fun getMouseMode(): Long =
    GodotBackendCalls.invokeNoArgsRetLongSingleton(D.INPUT_GET_MOUSE_MODE)

  fun getActionRawStrength(action: String, exactMatch: Boolean = false): Double {
    require(exactMatch == false) { "Web Input.get_action_raw_strength supports only exactMatch = false" }
    return GodotBackendCalls.invokeStringNameRetDoubleSingleton(
      D.INPUT_GET_ACTION_RAW_STRENGTH,
      action,
    )
  }

  fun isActionJustReleased(action: String, exactMatch: Boolean = false): Boolean {
    require(exactMatch == false) { "Web Input.is_action_just_released supports only exactMatch = false" }
    return GodotBackendCalls.invokeStringNameRetBoolSingleton(
      D.INPUT_IS_ACTION_JUST_RELEASED,
      action,
    )
  }

  fun getActionStrength(action: String, exactMatch: Boolean = false): Double {
    require(exactMatch == false) { "Web Input.get_action_strength supports only exactMatch = false" }
    return GodotBackendCalls.invokeStringNameRetDoubleSingleton(D.INPUT_GET_ACTION_STRENGTH, action)
  }

  var mouseMode: Long
    get() = getMouseMode()
    set(newValue) = setMouseMode(newValue)

  /**
   * Composed from two get_axis reads (deadzone-normalized identically for digital keys, the only
   * inputs the Web demos drive), clamped to unit length like Godot's get_vector.
   */
  fun getVector(
    negativeX: String,
    positiveX: String,
    negativeY: String,
    positiveY: String,
    deadzone: Double = -1.0,
  ): Vector2 {
    val vector = Vector2(getAxis(negativeX, positiveX), getAxis(negativeY, positiveY))
    val length = vector.length()
    if (deadzone >= 0.0) {
      // Godot's deadzone remap: inside the deadzone reads zero, outside rescales to [0, 1].
      if (length <= deadzone) return Vector2.ZERO
      if (length > 1.0) return vector / length
      return vector * ((length - deadzone) / (1.0 - deadzone) / length)
    }
    return if (length > 1.0) vector / length else vector
  }

  const val MOUSE_MODE_VISIBLE: Long = 0L
  const val MOUSE_MODE_HIDDEN: Long = 1L
  const val MOUSE_MODE_CAPTURED: Long = 2L
  const val MOUSE_MODE_CONFINED: Long = 3L
  const val MOUSE_MODE_CONFINED_HIDDEN: Long = 4L
  const val MOUSE_MODE_MAX: Long = 5L
}

@Suppress("EXTENSION_SHADOWED_BY_MEMBER")
fun Input.setCustomMouseCursor(image: Resource?, shape: Long = 0L, hotspot: Vector2 = Vector2.ZERO) = setCustomMouseCursor(image, shape, hotspot)

@Suppress("EXTENSION_SHADOWED_BY_MEMBER")
fun Input.isActionPressed(action: String, exactMatch: Boolean = false): Boolean = isActionPressed(action, exactMatch)

@Suppress("EXTENSION_SHADOWED_BY_MEMBER")
fun Input.actionPress(action: String, strength: Double = 1.0) = actionPress(action, strength)

@Suppress("EXTENSION_SHADOWED_BY_MEMBER")
fun Input.actionRelease(action: String) = actionRelease(action)

@Suppress("EXTENSION_SHADOWED_BY_MEMBER")
fun Input.isActionJustPressed(action: String, exactMatch: Boolean = false): Boolean = isActionJustPressed(action, exactMatch)

@Suppress("EXTENSION_SHADOWED_BY_MEMBER")
fun Input.getAxis(negativeAction: String, positiveAction: String): Double = getAxis(negativeAction, positiveAction)

@Suppress("EXTENSION_SHADOWED_BY_MEMBER")
fun Input.setMouseMode(mode: Long) = setMouseMode(mode)

@Suppress("EXTENSION_SHADOWED_BY_MEMBER")
fun Input.getMouseMode(): Long = getMouseMode()

@Suppress("EXTENSION_SHADOWED_BY_MEMBER")
fun Input.getActionRawStrength(action: String, exactMatch: Boolean = false): Double = getActionRawStrength(action, exactMatch)

@Suppress("EXTENSION_SHADOWED_BY_MEMBER")
fun Input.isActionJustReleased(action: String, exactMatch: Boolean = false): Boolean = isActionJustReleased(action, exactMatch)

@Suppress("EXTENSION_SHADOWED_BY_MEMBER")
fun Input.getActionStrength(action: String, exactMatch: Boolean = false): Double = getActionStrength(action, exactMatch)

@Suppress("EXTENSION_SHADOWED_BY_MEMBER")
var Input.mouseMode: Long
  get() = mouseMode
  set(newValue) {
    mouseMode = newValue
  }
